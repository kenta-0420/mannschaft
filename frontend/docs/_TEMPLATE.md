# Fxx: 機能名

> **ステータス**: 🔴 未着手 / 🟡 設計中 / 🟢 設計確定 / ✅ 実装完了
> **実装フェーズ**: Phase x
> **最終更新**: YYYY-MM-DD

---

## 1. 概要

この機能が解決する課題と、ユーザーにとっての価値を2〜3文で記述する。

---

## 2. スコープ

### 対象ロール
| ロール | 操作可能な範囲 |
|--------|--------------|
| SYSTEM_ADMIN | |
| ADMIN | |
| DEPUTY_ADMIN | |
| MEMBER | |
| SUPPORTER | |
| GUEST | |

### 対象レベル
- [ ] 組織 (Organization)
- [ ] チーム (Team)
- [ ] 個人 (Personal)

---

## 3. DB設計

### テーブル一覧
| テーブル名 | 役割 | 論理削除 |
|-----------|------|---------|
| `table_name` | 説明 | あり / なし |

### テーブル定義

#### `table_name`

| カラム名 | 型 | NULL | デフォルト | 説明 |
|---------|---|------|-----------|------|
| `id` | BIGINT UNSIGNED | NO | AUTO_INCREMENT | PK |
| `created_at` | DATETIME | NO | CURRENT_TIMESTAMP | |
| `updated_at` | DATETIME | NO | CURRENT_TIMESTAMP ON UPDATE | |

**インデックス**
```sql
INDEX idx_xxx (column_a, column_b)   -- 用途の説明
UNIQUE KEY uq_xxx (column_c, column_d)
```

**制約・備考**
- 論理削除: `deleted_at DATETIME nullable`（SoftDeletableEntity 適用）
- その他の設計判断・注意点

### ER図（テキスト形式）
```
table_a (1) ──── (N) table_b
table_b (N) ──── (1) table_c
```

---

## 4. API設計

### エンドポイント一覧
| メソッド | パス | 認証 | 説明 |
|---------|-----|------|------|
| GET | `/api/v1/xxx` | 必要 | 一覧取得 |
| POST | `/api/v1/xxx` | 必要 | 作成 |

### リクエスト／レスポンス仕様

#### `POST /api/v1/xxx`

**リクエストボディ**
```json
{
  "field_name": "string"
}
```

**レスポンス（201 Created）**
```json
{
  "data": {
    "id": 1
  }
}
```

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 400 | バリデーションエラー |
| 403 | 権限不足 |
| 404 | リソース不存在 |

---

## 5. ビジネスロジック

### 主要フロー

```
1. ユーザーが〇〇を操作
2. バックエンドが△△を検証
3. □□を更新し、ApplicationEvent を発行
4. 通知サービスがイベントを受信してプッシュ通知
```

### 重要な判定ロジック
- 条件A の場合: 〇〇
- 条件B の場合: △△

---

## 6. セキュリティ考慮事項

- **認可チェック**: 〇〇Service の入り口で `team_id` と `currentUser` の所属を必ず検証する
- **レートリミット**: 〇〇APIには `Bucket4j` で 1分間に△回の制限を適用
- **その他**: 特記事項

---

## 7. Flywayマイグレーション

```
V1.001__create_xxx_table.sql     -- テーブル作成
V1.002__seed_xxx_master.sql      -- マスターデータ投入（該当する場合）
```

**マイグレーション上の注意点**
- 依存するテーブル（外部キー先）が先に作成されていること

---

## 8. 未解決事項

設計中に判断を保留した事項を記録する。解決したら「~~取り消し線~~」で消して解決内容を追記する。

- [ ] 〇〇の挙動をどうするか（期限: Phase x 開始前）
- [ ] △△の仕様を確認する

---

## 9. 変更履歴

| 日付 | 変更内容 |
|------|---------|
| YYYY-MM-DD | 初版作成 |
