# AGENTS.md — Codex 向け入口

このファイルは Codex 固有の差分だけを定める。プロジェクト規約を二重管理しない。

## 正本

- 全体規約・開発フローの正本は [`CLAUDE.md`](CLAUDE.md)。作業開始時に必ず全文を読む。
- バックエンドを扱う場合は [`backend/.claudecode.md`](backend/.claudecode.md) と [`backend/BACKEND_CODING_CONVENTION.md`](backend/BACKEND_CODING_CONVENTION.md) を読む。
- フロントエンドを扱う場合は [`frontend/FRONTEND_CODING_CONVENTION.md`](frontend/FRONTEND_CODING_CONVENTION.md) を読む。
- テストを扱う場合は [`TEST_CONVENTION.md`](TEST_CONVENTION.md) を読む。
- 認証・認可・Cookie・セキュリティヘッダー・依存関係を扱う場合は [`docs/security/README.md`](docs/security/README.md) から該当文書を読む。
- 規約間に矛盾がある場合は、推測で解消せずユーザーへ報告する。

## Codex の担当

Codex は設計、調査、実装、修正、テスト、文書更新など通常の開発作業全般を担当する。検分を依頼される機会が多いが、レビュー専任ではない。変更を求める依頼では、必要な修正と検証まで自律的に進める。

### 検分（レビュー）

ユーザーから「検分」またはレビューを依頼されたら、[`.claude/commands/検分.md`](.claude/commands/検分.md) を全文読み、その手順と品質基準を正本として使う。Claude Code 専用ツール名やモデル名は Codex で利用可能な同等手段へ読み替えるが、レビュー観点・品質ゲート・受け入れ条件のトレーサビリティは省略しない。

検分では次を守る。

1. 原則として読み取り専用で調査する。ユーザーが修正まで依頼していない限り、コードや文書を変更しない。
2. 対象ブランチ・PR・コミット・比較元が明示されていなければ、ローカル状態と会話から安全に特定する。複数候補で結論が変わる場合のみ質問する。
3. 差分だけでなく、呼び出し元、既存テスト、仕様書、DB migration、認可境界まで追って実害を確認する。
4. 受け入れ条件がある場合は、各条件を実装と green テストへ対応付ける。条件一覧自体についても、正常系・空/0/null・境界値・途中失敗・認可・性能の漏れを確認する。
5. 指摘は重大度順に、根拠となるファイルと行、発生条件、影響を具体的に示す。憶測だけの指摘は出さない。
6. 問題が見つからなければ、その旨と残存リスク、実行できなかった検証を簡潔に示す。
7. 自動テストは変更範囲に比例して実行する。環境要因で実行できない場合は、未実行を合格扱いにしない。

## 作業場所と既存資産

- 本陣 `C:\Claude\mannschaft` の既存変更・未追跡ファイルはユーザーまたは他エージェントの資産として扱い、勝手に変更・削除・整形しない。
- 実装を依頼された場合も、`CLAUDE.md` の大名システムと worktree 隔離規則に従う。本陣で直接実装・commit しない。
- `.claude/commands/`、`.claude/handoffs/`、`.claude/campaigns/`、`docs/` は Claude Code 専用物として無視せず、Codex からも同じ正本を参照する。
- 大名コマンドの呼び出しにはプロジェクトスキル `mannschaft-daimyo` を使う。このスキルは `.claude/commands/*.md` を正本として参照する。
- Codex 専用に同内容の規約、検分チェックリスト、引継書を複製しない。Codex 固有の差分が増えた場合だけ、このファイルへ短く追記する。

## コミュニケーション

- 会話、レビュー報告、コードコメント、コミットメッセージ、追加文書は `CLAUDE.md` に従い日本語で記述する。
- レビュー結果は `.claude/commands/検分.md` の報告項目を保ちつつ、結論と指摘が素早く読める形式にする。
