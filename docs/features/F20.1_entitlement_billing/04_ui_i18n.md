# F20.1 — 04 画面設計・i18n

> **2026-08-31 補足**: U-1/U-3〜U-5 は、公開 `/pricing` と認証後 `/billing` の料金・契約センターへ統合する。月別請求、支払方法、税込主表示、日割り、月末解約/撤回の画面仕様と文言原則は [05_billing_center.md §3](05_billing_center.md#3-画面と導線) を正本とする。既存 URL は互換導線として scope 選択済みのハブを開く。

> **ステータス**: 🟢 設計完了（マスター御裁可済・実装待ち／営利自動切替・オーナー変更は Phase 2 保留）
> **⚠️ Phase 2 保留（マスター 2026-07-08）**: 営利自動切替の通知 UI／i18n（`orgType.*`・`notification.billing.org_type_*`）は初期スコープ外（README §3.3）。プラン一覧・ペイウォール・課金管理の UI/i18n は初期スコープに残る。
> ADHD フレンドリー原則（入力摩擦ゼロ・必須最小・ページはシンプル＋詳細はモーダルの段階開示）に従う。新規ページは `/統一`（PageHeader/SectionCard）と `/手助け` を適用（memory `feedback_new_pages_use_touitsu_and_tasuke`）。

---

## 1. 画面一覧

| # | 画面 | パス | 主対象 |
|---|---|---|---|
| U-1 | 公開料金/プラン一覧 | `/pricing`（公開）・`/billing?tab=plan`（認証後） | 全利用者 |
| U-2 | ペイウォールモーダル | （モーダル・全画面から 402 で発火） | 全ユーザー |
| U-3〜U-5 | 統合料金・契約センター | `/billing?scopeKind&scopeId`（旧URLは互換遷移） | USER本人 / BillingAccessGuard管理者 |
| U-6 | シスアド課金マスタ管理 | `/system-admin/billing` | SYSTEM_ADMIN |

### U-1 プラン一覧

- 3 カラムのプランカード（FREE / BASIC / FULL）。各カードに月額（**未定のプランは「価格はベータ終了後に決定」バッジ**）・機能リスト（`FeatureItem.displayNameKey` を `$t()` 解決）・現在プランのハイライト。
- **BASIC カードの表示可否（L-2）**: BASIC は構成・価格ともに R-3 未確定。**R-3 確定までは BASIC カードを「準備中」表示（CTA 無効・機能リスト非表示）**とする（推奨）。または `plans.enabled=false` で完全非表示にする運用も可（マスタ側で切替）。中身が空のカードを「選べるプラン」として出さない（誤契約防止）。
- チーム/組織文脈で開いた場合は人数バンド表を折りたたみ（`<details>` 相当）で段階開示。
- CTA: 「このプランにする」→ 確認モーダル（現在→新プランの差分機能を明示）→ `POST .../billing/contracts`。

### U-2 ペイウォールモーダル（402 ハンドリング）✅実装済み（#2442 BE details 追補＋FE 結線 PR）

- API が `ENTITLEMENT_003`(402) を返した際に共通ハンドラ（`useApi`）が `usePaywallStore` へ `message`＋`details`（featureKey / addonAvailable / addonPriceJpy / plansContaining / scopeKind / scopeId）を積んで発火。details 無し（旧 BE・後方互換）は従来どおり汎用メッセージ＋プラン一覧導線のみ。
- **CTA は 1 画面 1 主行動（ADHD 原則）**。details の内容で以下のいずれか 1 系統のみを描画する（選択肢を並べて迷わせない）:
  - `addonAvailable=true` かつベータ（`addonPriceJpy` が null または 0 円以下・null と 0 円を等価に扱う）→ 主 CTA「無料で有効にする」（`betaCta`）1 ボタン。押下で即 `POST .../billing/contracts`（`contractKind=ADDON`）を無償契約し、成功したらモーダルを閉じてページを再読み込みし権利を反映する（決済ステップなし）。
  - `addonAvailable=true` かつ有償（`addonPriceJpy>0`。ベータ終了後の想定経路）→ 主 CTA「この機能だけ追加する」（`addonCta`）。契約 API が `checkoutUrl` を返せば Stripe Checkout へ遷移する（U-1 と同じ流儀）。
  - `addonAvailable=false` かつ `plansContaining` 非空（プラン専用機能）→ 主 CTA「プランをまとめて見る」（`planCta`）のみ。インライン契約はせず `/billing/plans` へ遷移する（詳細比較はそちらで段階開示）。
  - 契約中は `activating` 表示＋二重押下防止。契約失敗（`ENTITLEMENT_006` 既契約 409 は成功同義でクローズ、それ以外は `activateError` トーストでモーダル維持）。
- **FE はこのモーダルを出すだけで機能を解放しない**（BE ゲートが正・03 §4。ワンクリック契約も BE の契約 API を必ず経由し、権利は entitlements 行で発行される）。

### U-3/U-4/U-5 課金管理

- scope 切替、現在の PLAN（0又は1件）と ADDON 一覧（0件以上）、次回請求日、税込主表示/税内訳、月別請求、請求書URL、支払方法/請求先を一画面で段階表示する。`GET /me/billing/summary` の `plan`/`addons` をそのまま区別して描画し、各有償ADDONにも自身の終了日、期末取消、撤回を表示する。一般メンバーには金額・invoice・Portalを出さない。
- ベータ特典由来（`sourceKind=BETA_GRANT`）は「ベータ特典」バッジ＋有効期限（チーム/組織は `validUntil`、個人は「サービス提供期間中無償」— **「永久」の語は UI でも使用禁止**・F20.3 README §3）。
- 操作: PLAN変更・ADDON追加/個別解約・PLAN解約。解約は Contract ごとに終了日と影響を示す一度の確認で月末予約とし、月末まで同じ Contract のみ撤回可能（詳細は 05 §3.3/§4/§7）。
- 権利サマリの閲覧はメンバーにも許可（U-3/U-4 の読み取り専用ビュー）。操作ボタンは ADMIN のみ表示（BE 認可と一致）。

### U-6 シスアド課金マスタ管理

- plans / feature_catalog / plan_features / plan_price_bands の CRUD テーブル＋手動付与フォーム＋契約横断検索。`/system-admin/fee-policies`（F22.1 P2-f 実装済）の画面様式を踏襲。

---

## 2. FE 実装規約

- 402 共通ハンドラ: `useApi` 系コンポーザブルのエラーインターセプタに `ENTITLEMENT_003` 分岐を追加（既存 `RESERVATION_029` の 402 表示があれば共通化。無ければ新設し、既存 402 系も段階的に寄せる）。
- 型は**生成型を優先**（`frontend/app/types/generated/`・BE マージ後 `npm run generate:types`）。型アサーションでフィールドを偽らない（memory `feedback_fe_api_type_assertion_field_lie`）。
- ページルートは単一要素ルート（memory `project_pagetransition_single_element_root`）。

---

## 3. i18n（`billing.json` 新設・6 言語）

- **新設ファイル**: `frontend/app/locales/{ja,en,zh,ko,es,de}/billing.json`（6 言語すべて。未翻訳は暫定で日本語と同値可）。
- **nuxt.config への登録が必須**: `i18n.locales[].files` 配列の各言語に `billing.json` を追加しないと読み込まれない（memory `project_i18n_locale_file_registration_required`・JSON が存在するだけでは効かない）。現状 `nuxt.config.ts` の `files` は各言語 `common.json`/`auth.json`/`validation.json`/`landing.json` を列挙（実確認）。ここへ `{lang}/billing.json` を 6 言語分追加する。
- **キー格納形の確定（⑤・origin/main 実挙動を確認して確定）**: 実ファイルの慣習は「**各ロケールファイルはトップレベルに名前空間キーを持ち、それが `$t()` の前置になる**」である（実確認: `landing.json` は `{ "landing": { … } }` で `$t('landing.hero.badge')`／`common.json` は `{ "chat": …, "button": … }` で `$t('button.save')`／唯一 `validation.json` だけが裸キー `{ "required": … }` で `$t('required')`）。
  - よって **`billing.json` はトップレベルを単一の `billing` キーでラップ**し（`landing.json` と同型・feature スコープファイルの標準形）、参照は **`$t('billing.plans.full.name')`** とする。
  - **DB 格納形の確定**: `feature_catalog.display_name_key` / `description_key`・`plans.display_name_key` 等には**フルパス（`billing.` 前置を含む実解決キー）を格納**する。例: `billing.features.reservation_notification_recipients_extended.name`・`billing.plans.full.name`。FE は `$t(row.displayNameKey)` でそのまま解決できる（実装時判断に流さない・V146 シードにこの実値を書く）。
- 文言に裸の `@` を使わない（`{'@'}` エスケープ・memory `project_i18n_at_sign_breaks_nuxt_build`）。

### 3.1 新規キー一覧（ja 例・全キー 6 言語必須・トップレベル `billing` ラップ）

```jsonc
// frontend/app/locales/ja/billing.json（新設・全体を "billing" でラップ → $t('billing.plans.full.name')）
{
 "billing": {
  "plans": {
    "title": "料金プラン",
    "free": { "name": "フリー", "description": "基本機能を無料で利用できます" },
    "basic": { "name": "ベーシック", "description": "よく使う機能をまとめたプランです" },
    "full": { "name": "フル", "description": "すべての機能を利用できるプランです" },
    "currentPlan": "現在のプラン",
    "priceUndecided": "価格はベータ終了後に決定します",
    "betaFree": "ベータ期間中は無料でお試しいただけます",
    "perMonth": "/月",
    "memberBand": "人数帯別料金",
    "selectCta": "このプランにする",
    "changeConfirmTitle": "プランを変更しますか？",
    "changeConfirmGained": "追加される機能",
    "changeConfirmLost": "使えなくなる機能"
  },
  "features": {
    "template_premium_modules": { "name": "プレミアムモジュール", "description": "有料モジュールを有効化できます" },
    "reservation_notification_recipients_extended": { "name": "予約通知宛先の拡張", "description": "予約通知メールの宛先を4件以上登録できます" },
    "ads_hide": { "name": "広告非表示", "description": "画面内の広告を非表示にします" },
    "monetization_paywall": { "name": "ペイウォール", "description": "有料コンテンツを開設できます" },
    "monetization_membership_fee": { "name": "会費徴収", "description": "メンバーからの会費徴収を開設できます" }
  },
  "paywall": {
    "title": "この機能は有料プランで利用できます",
    "addonCta": "この機能だけ追加する",
    "planCta": "プランをまとめて見る",
    "notAvailable": "この機能は現在ご利用いただけません"
  },
  "manage": {
    "title": "課金・プラン管理",
    "activeContract": "契約中のプラン",
    "addons": "アドオン",
    "entitledFeatures": "利用できる機能",
    "noContract": "契約中のプランはありません（フリー）",
    "cancelCta": "解約する",
    "cancelConfirm": "{endDate} まで利用できます。{nextBillingDate} 以降は請求されません。",
    "sourceBadge": { "plan": "プラン", "addon": "アドオン", "betaGrant": "ベータ特典" },
    "validUntil": "有効期限: {date}",
    "noExpiry": "サービス提供期間中無償"
  },
  "orgType": {
    "_comment": "【Phase 2 保留・営利自動切替（README §3.3）】初期スコープでは未使用。Phase 2 実装時に有効化",
    "autoUpdatedTitle": "組織区分を「会社・企業」に変更しました",
    "autoUpdatedBody": "収益機能の利用開始に伴い、組織区分を営利区分へ変更しました。内容をご確認ください。",
    "reviewRequestedTitle": "収益機能の利用開始について確認が必要です",
    "reviewRequestedBody": "この組織（または所属チーム）で収益機能の利用が開始されました。組織区分の確認をお願いします。"
  },
  "errors": {
    "notEntitled": "この機能を利用する権利がありません",
    "scopeForbidden": "この操作を行う権限がありません",
    "contractAlreadyActive": "すでに契約中のプランがあります",
    "contractNotFound": "契約が見つかりません",
    "addonNotAvailable": "この機能は単体追加できません"
  }
 }
}
```

> 上記は全体が `"billing"` でラップされている（`$t('billing.plans.title')` 等で参照）。DB `display_name_key` 等には `billing.plans.full.name` のようにフルパスで格納する。
- **UI 文言の直書き禁止**（CLAUDE.md i18n ルール）。上記キーを en/zh/ko/es/de にも同構造で追加。
- 通知文言（org_type イベント・02 §7.2）は BE 側 `messages*.properties`（6 言語）にも同旨キーを追加: `notification.billing.org_type_auto_updated.title/body`・`notification.billing.org_type_review_requested.title/body`。**【Phase 2 保留】これらは営利自動切替に属し初期スコープ外**（README §3.3）。Phase 2 で追加する。

---

## 4. 手助け（使い方モーダル）

- U-1 / U-3〜U-5 に `/手助け` 方式の使い方モーダルを付与（PageHeader help フラグ＋カード方式ガイド）。
- 内容: 「プランとは」「アドオンとは」「ベータ期間中の扱い（無料・価格は後日決定）」「ベータ特典との違い（F20.3）」の 4 カード。

---

## 5. 料金・契約センター必須 i18n（2026-08-31）

`frontend/app/locales/{ja,en,zh,ko,es,de}/billing.json` の **6ファイルすべて**に、次の同一キー集合を追加する。各 locale の値は当該言語へ翻訳し、キーや日本語をフォールバック表示しない。`nuxt.config.ts` の各 locale `files` にも `billing.json` を登録する。

```text
billing.center.title
billing.center.scopeSelector
billing.center.tabs.plan / invoices / payment / cancel
billing.center.noManageableScope
billing.center.currentPlan / addons / noAddons / addonCancel / addonRescind
billing.pricing.monthlyAutoRenew / proratedFirstMonth / taxIncluded / cancellationPolicy / comingSoon
billing.quote.initialTotal / nextMonthlyTotal / period / expiresAt / unavailableAt / refreshAtMonthStart
billing.checkout.start / expired / failed
billing.invoice.title / period / paid / open / failed / void / downloadDocument / documentExpiresAt
billing.invoice.taxIncluded / subtotal / discount / tax / total / issuer / billingAddress
billing.payment.title / managePaymentMethod / manageBillingAddress / portalExpiresAt
billing.change.upgrade / downgrade / amountDueNow / effectiveNow / effectiveNextMonth / paymentPending / paymentFailedKeepCurrent
billing.cancel.cta / confirmTitle / cancelConfirm / confirmCta / continueCta / scheduled / endDate / noFutureCharge / rescindCta / rescinded
billing.access.readOnly / noBillingPermission / manageTeamBilling / manageOrganizationBilling / permissionRequired
billing.errors.monthBoundary / quoteExpired / previewExpired / changeConflict / priceNotSellable / migrationRequired / stripeUnavailable
```

`billing.cancel.cancelConfirm` は必ず `{endDate}` と `{nextBillingDate}` を含め、即時失効を意味する文言にしてはならない。E2E は6言語で全キーが解決され、month-boundary・解約予定・撤回・支払失敗を表示できることを確認する（05 AC-BC-03/04/10）。
