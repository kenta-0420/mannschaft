# F15.1 チーム・組織ページ全面リデザイン

## 概要

チームページ（`/teams`）と組織ページ（`/organizations`）を、検索メインのUIから「マイチームハブ」「マイ組織ハブ」へ全面リデザインした。

旧来の設計では `/teams` がチーム検索ページとして機能していたが、ユーザーが最もアクセスする「自分の所属チーム」への導線が弱かった。本フェーズでは以下の方針に従いリデザインを実施した。

- **ハブページ（`/teams`, `/organizations`）**: ストアから直接取得した所属チーム/組織一覧 + ウィジェット埋め込み
- **検索ページ（`/teams/search`, `/organizations/search`）**: 既存の検索UIを専用ページに分離

## 変更ファイル一覧

### 新規作成

| ファイル | 内容 |
|---|---|
| `frontend/app/pages/teams/search.vue` | チーム検索専用ページ（旧 `teams/index.vue` の検索UIを移植） |
| `frontend/app/pages/organizations/search.vue` | 組織検索専用ページ（旧 `organizations/index.vue` の検索UIを移植） |
| `docs/features/F15.1_team_org_page_redesign.md` | 本設計書 |

### 変更

| ファイル | 変更内容 |
|---|---|
| `frontend/app/pages/teams/index.vue` | マイチームハブへ全面書き換え |
| `frontend/app/pages/organizations/index.vue` | マイ組織ハブへ全面書き換え |
| `frontend/app/components/widgets/WidgetTeamAnnouncements.vue` | `embedded` prop 追加 |
| `frontend/app/components/widgets/WidgetOrgAnnouncements.vue` | `embedded` prop 追加 |
| `frontend/app/locales/{ja,en,zh,ko,es,de}/common.json` | `teamHub`, `orgHub` i18nキー追加 |

## UIレイアウト

### マイチームハブ（`/teams`）

```
[マイチーム]                    [チームを作成] [チームを検索]
───────────────────────────────────────────────────────────────
┌─────────────────────────────────────────────────────────────┐
│ チームのお知らせ                                              │
│  <WidgetTeamAnnouncements embedded />                        │
└─────────────────────────────────────────────────────────────┘

所属チーム                                      [グリッド] [リスト]
  ┌──────┐ ┌──────┐ ┌──────┐
  │ icon │ │ icon │ │ icon │
  │ name │ │ name │ │ name │
  │ tag  │ │ tag  │ │ tag  │
  │ loc  │ │ loc  │ │ loc  │
  └──────┘ └──────┘ └──────┘
```

### チーム検索（`/teams/search`）

```
[← 戻る] チーム検索                        [チームを作成]
─────────────────────────────────────────
[SearchBar（keyword + prefecture + template）]

検索結果グリッド
Paginator
```

## 実装詳細

### グリッド/リスト切り替え

- `viewMode: 'grid' | 'list'` で管理
- `localStorage` で永続化
  - チーム: キー `mannschaft:teams:viewMode`
  - 組織: キー `mannschaft:orgs:viewMode`
- SSR対応のため `import.meta.client` チェック済み

### WidgetTeamAnnouncements / WidgetOrgAnnouncements の embedded prop

```vue
<WidgetTeamAnnouncements :embedded="true" />
```

- `embedded=false`（デフォルト）: 従来通り `DashboardWidgetCard` ラッパーあり（ダッシュボードページ用）
- `embedded=true`: `DashboardWidgetCard` を外してコンテンツのみ描画（ハブページに直接埋め込み）

### i18nキー

`common.json` の `teamHub` および `orgHub` セクションに追加。
6言語（ja/en/zh/ko/es/de）すべてに対応済み。

### ストアからの直接参照

ハブページではAPIを呼び出さず、Piniaストアの値を直接参照する:
- `teamStore.myTeams`: ログイン時に取得済みの所属チーム一覧
- `orgStore.myOrganizations`: ログイン時に取得済みの所属組織一覧

## フロントエンドルーティング移行（スラッグ対応）

チーム・組織の詳細ページは URL パラメータを BIGINT ID からスラッグに変更する。

| 変更前 | 変更後 |
|---|---|
| `/teams/[id]/` | `/teams/[slug]/` |
| `/organizations/[id]/` | `/organizations/[slug]/` |

**変更ポイント:**

- ルートパラメータ: `route.params.id` → `route.params.slug`
- API 呼び出し: `/api/v1/teams/{id}` → `/api/v1/teams/{slug}`（slug を URL パスに使用）
- Nuxt のファイルシステムルーティング: `pages/teams/[id]/` → `pages/teams/[slug]/`、`pages/organizations/[id]/` → `pages/organizations/[slug]/`
- 内部リンク生成: `{ name: 'teams-id', params: { id: team.id } }` → `{ name: 'teams-slug', params: { slug: team.slug } }`

**移行時の注意点:**

- 既存の BIGINT ID を使用した URL（ブックマーク・SNS 共有済みリンク等）は 301 リダイレクトで slug ベース URL に転送する（BE 側で `/api/v1/teams/by-id/{id}` または slug lookup で対応）
- slug は作成時にユーザーが指定するか自動生成（チーム名から英字変換）する

## Phase 2 への接続点

以下は Phase 2 以降で実装予定:

1. **クイックアクション**: チームカードから直接「掲示板」「チャット」「カレンダー」へのショートカット
2. **未読バッジ**: 各チームの未読通知数をカードに表示
3. **ソート/フィルタ**: 所属チーム一覧のソート（参加日順、アクティビティ順）
4. **ピン留め**: よく使うチームをトップにピン留め
5. **最近アクセス**: 最近見たチームの履歴表示
