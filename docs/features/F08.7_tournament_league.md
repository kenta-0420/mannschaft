# F08.7: 大会・リーグ管理

> **ステータス**: 🟢 設計完了（順位UI Wave0〜3a 実装済・Wave3b 設計書同期済。実機 E2E 未踏）
> **実装フェーズ**: Phase 8〜9（順位UI Wave0〜3a main 済）
> **最終更新**: 2026-06-11
> **モジュール種別**: 選択式モジュール #14（大会・リーグ管理）
> **関連ドキュメント**: F08.1 マッチング・対外交流（対戦相手募集との連携）、F06.4 活動記録（試合結果の活動記録自動作成）、F07.2 パフォーマンス管理（個人成績の統計連携）、F03.1 スケジュール・出欠管理（試合日程のカレンダー連携）、F04.3 プッシュ通知（結果通知・昇降格通知）、[F08.10/05 tournament 統合](F08.10_match_record_analytics/05_tournament_integration.md)（試合記録→順位連携入口①）

---

## 1. 概要

組織内のリーグ戦・トーナメント・大会を管理する機能。ディビジョン（部・リーグ）を自由に作成し、参加チームの対戦成績を記録、順位表・個人ランキングを自動計算する。シーズン終了後の昇格・降格もADMINが操作可能。

競技ごとの勝点配分・タイブレークルール・記録項目はテンプレートで定義し、SYSTEM_ADMINが主要競技のプリセットを提供しつつ、ADMINが複製・カスタマイズまたはゼロから自作できる。

**主要ユースケース**:
- 都道府県サッカー協会: 1部〜4部リーグの運営、勝点順位表、得点ランキング、シーズン後の昇降格
- バスケットボールリーグ: 勝率ベースの順位、個人スタッツ（得点・リバウンド・アシスト）
- 地域バレーボール協会: セット率・得点率ベースの順位
- 将棋・囲碁クラブ: 勝敗のみのリーグ戦、段位別グループ
- eスポーツコミュニティ: トーナメント形式の大会運営
- 社内フットサル大会: グループステージ + 決勝トーナメント

### 本ドキュメントのスコープ

| 機能 | 説明 |
|------|------|
| テンプレート管理 | 競技別の勝点ルール・タイブレーク・個人成績項目の定義 |
| 大会・リーグ CRUD | 大会の作成・編集・開催・終了 |
| ディビジョン管理 | 部/リーグの階層作成・参加チーム登録 |
| 対戦カード・スコア記録 | 節(matchday)ごとの対戦カード作成・結果入力 |
| 順位表自動計算 | 勝点・タイブレークルールに基づく順位の自動算出 |
| 個人ランキング | 得点王・アシスト王等の個人成績集計 |
| 昇格・降格管理 | シーズン終了後のチーム入れ替え操作 |
| PDF出力 | 順位表・トーナメント表のPDFエクスポート |

**スコープ外**:
- 対戦相手の募集・応募 → F08.1 マッチング
- 個人パフォーマンスの長期追跡・目標管理 → F07.2 パフォーマンス管理
- 試合の詳細な活動記録（練習メニュー等） → F06.4 活動記録

---

## 2. スコープ

### 対象ロール
| ロール | 操作可能な範囲 |
|--------|--------------|
| SYSTEM_ADMIN | プリセットテンプレートの管理。全組織の大会データ参照 |
| ADMIN | 大会・リーグの CRUD、テンプレート管理、対戦カード作成・結果入力、昇降格操作、PDF出力 |
| DEPUTY_ADMIN | `MANAGE_TOURNAMENT` 権限を持つ場合: 対戦カード作成・結果入力、順位表・ランキング閲覧（※ `MANAGE_TOURNAMENT` は F01.2 の `permissions` テーブルに Flyway で追加が必要） |
| MEMBER | 順位表・ランキング・対戦結果の閲覧。自チームの試合詳細閲覧 |
| SUPPORTER | 公開設定の大会の順位表・結果閲覧のみ |
| GUEST | 公開設定の大会の順位表・結果閲覧のみ（SSR ページ） |

### 対象レベル
- [x] 組織 (Organization) — 大会・リーグの主催。ディビジョン・参加チーム管理
- [x] チーム (Team) — 参加チームとして大会に登録される側。自チームの成績閲覧
- [ ] 個人 (Personal) — 対象外（個人戦は将来検討）

> **設計判断**: 大会は「組織が主催し、組織傘下のチームが参加する」モデル。大分県サッカー協会（組織）が1部〜4部リーグ（ディビジョン）を作成し、各チーム（チーム）が所属する構造。組織に属さないチーム同士の大会は、先に組織（協会・連盟等）を作成してからチームを招待する運用。

---

## 3. DB設計

### テーブル一覧
| テーブル名 | 役割 | 論理削除 |
|-----------|------|---------|
| `system_tournament_presets` | SYSTEM_ADMIN管理の競技別プリセットテンプレート | あり(`deleted_at`) |
| `system_tournament_preset_tiebreakers` | プリセットのタイブレーク優先順位 | なし |
| `system_tournament_preset_stat_defs` | プリセットの個人成績項目定義 | なし |
| `tournament_templates` | 組織ごとのカスタムテンプレート | あり(`deleted_at`) |
| `tournament_template_tiebreakers` | テンプレートのタイブレーク優先順位 | なし |
| `tournament_template_stat_defs` | テンプレートの個人成績項目定義 | なし |
| `tournaments` | 大会・リーグのインスタンス | あり(`deleted_at`) |
| `tournament_tiebreakers` | 大会のタイブレーク優先順位（作成時にテンプレートからコピー） | なし |
| `tournament_stat_defs` | 大会の個人成績項目定義（作成時にテンプレートからコピー） | なし |
| `tournament_divisions` | ディビジョン（1部、2部等） | なし |
| `tournament_participants` | ディビジョンへの参加チーム | なし |
| `tournament_matchdays` | 節・ラウンド | なし |
| `tournament_matches` | 対戦カード | なし |
| `tournament_match_sets` | セット別・延長戦等の詳細スコア | なし |
| `tournament_match_rosters` | 試合出場メンバー登録 | なし |
| `tournament_match_player_stats` | 試合ごとの個人成績 | なし |
| `tournament_standings` | 順位表キャッシュ（非正規化） | なし |
| `tournament_individual_rankings` | 個人ランキングキャッシュ（非正規化） | なし |
| `tournament_promotion_records` | 昇格・降格の履歴 | なし |
| `tournament_entry_members` | 大会エントリー登録（事前選手リスト） | なし |
| `tournament_entry_templates` | チーム別エントリーテンプレート（最大5件） | なし |
| `tournament_entry_template_members` | テンプレート選手（背番号・ポジション） | なし |

**既存テーブル（他フェーズで作成済み）の参照**:
- `organizations`（Phase 1）: 大会主催組織
- `teams`（Phase 1）: 参加チーム
- `users`（Phase 1）: 個人成績の選手
- `schedules`（Phase 3、F03.1）: 試合日程の自動スケジュール作成
- `activity_results`（Phase 6、F06.4）: 試合結果の活動記録自動作成

**既存テーブルへの追加カラム**:
- `activity_results` に `source_type VARCHAR(30) NULL` + `source_id BIGINT UNSIGNED NULL` を追加（Flyway マイグレーション）。ポリモーフィック参照で外部機能からの自動作成元を記録。`UNIQUE INDEX uq_ar_source (source_type, source_id)` で重複作成を防止。将来的に他機能（スケジュール完了→活動記録等）でも汎用利用可能

### テーブル定義

#### `system_tournament_presets`

SYSTEM_ADMIN が管理するプラットフォーム標準の競技別テンプレート。ADMINが `tournament_templates` に複製して利用。

| カラム名 | 型 | NULL | デフォルト | 説明 |
|---------|---|------|-----------|------|
| `id` | BIGINT UNSIGNED | NO | AUTO_INCREMENT | PK |
| `name` | VARCHAR(100) | NO | — | プリセット名（例: サッカー、バスケットボール、バレーボール） |
| `sport_category` | VARCHAR(50) | NO | — | 競技カテゴリ（例: FOOTBALL, BASKETBALL, VOLLEYBALL, GENERAL）。検索・分類用 |
| `description` | VARCHAR(500) | YES | NULL | 説明 |
| `icon` | VARCHAR(30) | YES | NULL | アイコン（emoji or アイコン名） |
| `supported_formats` | JSON | NO | — | 対応する大会形式の配列（例: `["LEAGUE","KNOCKOUT","GROUP_KNOCKOUT"]`） |
| `win_points` | TINYINT UNSIGNED | NO | 3 | 勝利時の勝点 |
| `draw_points` | TINYINT UNSIGNED | NO | 1 | 引分時の勝点（トーナメント形式では未使用） |
| `loss_points` | TINYINT UNSIGNED | NO | 0 | 敗北時の勝点 |
| `has_draw` | BOOLEAN | NO | TRUE | 引分けありか（バスケ等は FALSE → 延長で決着） |
| `has_sets` | BOOLEAN | NO | FALSE | セット制か（バレー、テニス等） |
| `sets_to_win` | TINYINT UNSIGNED | YES | NULL | 勝利に必要なセット数（`has_sets = TRUE` 時。例: バレー3、テニス2 or 3） |
| `has_extra_time` | BOOLEAN | NO | FALSE | 延長戦ありか |
| `has_penalties` | BOOLEAN | NO | FALSE | PK戦/SO戦ありか |
| `score_unit_label` | VARCHAR(20) | NO | '点' | スコアの単位ラベル（点、ゴール、ポイント、セット等） |
| `bonus_point_rules` | JSON | YES | NULL | 条件付き勝点加算ルール（NULL = 固定勝点のみ） |
| `sort_order` | INT | NO | 0 | 表示順 |
| `created_at` | DATETIME | NO | CURRENT_TIMESTAMP | |
| `updated_at` | DATETIME | NO | CURRENT_TIMESTAMP ON UPDATE | |
| `deleted_at` | DATETIME | YES | NULL | 論理削除 |

**インデックス**
```sql
INDEX idx_stp_category (sport_category, sort_order)   -- カテゴリ別一覧取得
```

> **プリセット名の一意性**: MySQL 8.0 では `UNIQUE (name, deleted_at)` は `deleted_at = NULL` 同士を区別するため、論理削除と併用できない。Service 層で `SELECT ... WHERE name = ? AND deleted_at IS NULL` の存在チェックにより一意性を保証する。

#### `system_tournament_preset_tiebreakers`

プリセットのタイブレーク（順位決定）ルール。優先順位順に適用。

| カラム名 | 型 | NULL | デフォルト | 説明 |
|---------|---|------|-----------|------|
| `id` | BIGINT UNSIGNED | NO | AUTO_INCREMENT | PK |
| `preset_id` | BIGINT UNSIGNED | NO | — | FK → system_tournament_presets。ON DELETE CASCADE |
| `priority` | TINYINT UNSIGNED | NO | — | 優先順位（1が最優先） |
| `criteria` | ENUM('POINTS', 'SCORE_DIFFERENCE', 'SCORE_FOR', 'HEAD_TO_HEAD_POINTS', 'HEAD_TO_HEAD_SCORE_DIFFERENCE', 'WINS', 'SET_RATIO', 'POINT_RATIO', 'LOSSES', 'DRAWS') | NO | — | 順位決定基準 |
| `direction` | ENUM('DESC', 'ASC') | NO | 'DESC' | ソート方向（DESC = 多い方が上位） |

**インデックス**
```sql
UNIQUE INDEX uq_stp_tb (preset_id, priority)           -- 同一プリセット内で優先順位ユニーク
```

**制約・備考**
- サッカーの典型例: 1. POINTS DESC → 2. SCORE_DIFFERENCE DESC → 3. SCORE_FOR DESC → 4. HEAD_TO_HEAD_POINTS DESC
- バスケの典型例: 1. WINS DESC → 2. HEAD_TO_HEAD_POINTS DESC
- ENUM値 `SET_RATIO` `POINT_RATIO` はバレー等セット制競技用

#### `system_tournament_preset_stat_defs`

プリセットの個人成績項目定義。得点王・アシスト王等のランキング対象。

| カラム名 | 型 | NULL | デフォルト | 説明 |
|---------|---|------|-----------|------|
| `id` | BIGINT UNSIGNED | NO | AUTO_INCREMENT | PK |
| `preset_id` | BIGINT UNSIGNED | NO | — | FK → system_tournament_presets。ON DELETE CASCADE |
| `name` | VARCHAR(50) | NO | — | 成績項目名（例: 得点、アシスト、リバウンド、三振） |
| `stat_key` | VARCHAR(30) | NO | — | プログラム内キー（例: goals, assists, rebounds）。英数字+アンダースコア |
| `unit` | VARCHAR(20) | YES | NULL | 単位（例: 点、本、回） |
| `data_type` | ENUM('INTEGER', 'DECIMAL', 'TIME') | NO | 'INTEGER' | データ型 |
| `aggregation_type` | ENUM('SUM', 'AVG', 'MAX', 'MIN') | NO | 'SUM' | ランキング集計方法 |
| `is_ranking_target` | BOOLEAN | NO | TRUE | 個人ランキング表示対象か |
| `ranking_label` | VARCHAR(50) | YES | NULL | ランキングタイトル（例: 得点王、アシスト王）。`is_ranking_target = TRUE` 時に使用 |
| `sort_order` | INT | NO | 0 | 表示順 |

**インデックス**
```sql
UNIQUE INDEX uq_stp_sd_key (preset_id, stat_key)      -- 同一プリセット内でキーユニーク
INDEX idx_stp_sd_ranking (preset_id, is_ranking_target, sort_order)
```

#### `tournament_templates`

組織の ADMIN が作成するカスタムテンプレート。プリセットから複製、または独自作成。

| カラム名 | 型 | NULL | デフォルト | 説明 |
|---------|---|------|-----------|------|
| `id` | BIGINT UNSIGNED | NO | AUTO_INCREMENT | PK |
| `organization_id` | BIGINT UNSIGNED | NO | — | FK → organizations。ON DELETE CASCADE |
| `source_preset_id` | BIGINT UNSIGNED | YES | NULL | FK → system_tournament_presets（ON DELETE SET NULL）。複製元プリセット（NULL = 独自作成） |
| `name` | VARCHAR(100) | NO | — | テンプレート名（例: フットサル、ミニバスケ） |
| `description` | VARCHAR(500) | YES | NULL | 説明 |
| `icon` | VARCHAR(30) | YES | NULL | アイコン |
| `supported_formats` | JSON | NO | — | 対応する大会形式の配列 |
| `win_points` | TINYINT UNSIGNED | NO | 3 | 勝利時の勝点 |
| `draw_points` | TINYINT UNSIGNED | NO | 1 | 引分時の勝点 |
| `loss_points` | TINYINT UNSIGNED | NO | 0 | 敗北時の勝点 |
| `has_draw` | BOOLEAN | NO | TRUE | 引分けありか |
| `has_sets` | BOOLEAN | NO | FALSE | セット制か |
| `sets_to_win` | TINYINT UNSIGNED | YES | NULL | 勝利に必要なセット数 |
| `has_extra_time` | BOOLEAN | NO | FALSE | 延長戦ありか |
| `has_penalties` | BOOLEAN | NO | FALSE | PK戦ありか |
| `score_unit_label` | VARCHAR(20) | NO | '点' | スコアの単位ラベル |
| `bonus_point_rules` | JSON | YES | NULL | 条件付き勝点加算ルール（NULL = 固定勝点のみ） |
| `version` | BIGINT | NO | 0 | 楽観的ロック用（§22） |
| `created_by` | BIGINT UNSIGNED | NO | — | FK → users。作成した ADMIN |
| `created_at` | DATETIME | NO | CURRENT_TIMESTAMP | |
| `updated_at` | DATETIME | NO | CURRENT_TIMESTAMP ON UPDATE | |
| `deleted_at` | DATETIME | YES | NULL | 論理削除 |

**インデックス**
```sql
INDEX idx_tt_org (organization_id, deleted_at)          -- 組織内テンプレート一覧
```

#### `tournament_template_tiebreakers`

テンプレートのタイブレークルール。構造は `system_tournament_preset_tiebreakers` と同一。

| カラム名 | 型 | NULL | デフォルト | 説明 |
|---------|---|------|-----------|------|
| `id` | BIGINT UNSIGNED | NO | AUTO_INCREMENT | PK |
| `template_id` | BIGINT UNSIGNED | NO | — | FK → tournament_templates。ON DELETE CASCADE |
| `priority` | TINYINT UNSIGNED | NO | — | 優先順位 |
| `criteria` | ENUM('POINTS', 'SCORE_DIFFERENCE', 'SCORE_FOR', 'HEAD_TO_HEAD_POINTS', 'HEAD_TO_HEAD_SCORE_DIFFERENCE', 'WINS', 'SET_RATIO', 'POINT_RATIO', 'LOSSES', 'DRAWS') | NO | — | 順位決定基準 |
| `direction` | ENUM('DESC', 'ASC') | NO | 'DESC' | ソート方向 |

**インデックス**
```sql
UNIQUE INDEX uq_tt_tb (template_id, priority)
```

#### `tournament_template_stat_defs`

テンプレートの個人成績項目定義。構造は `system_tournament_preset_stat_defs` と同一。

| カラム名 | 型 | NULL | デフォルト | 説明 |
|---------|---|------|-----------|------|
| `id` | BIGINT UNSIGNED | NO | AUTO_INCREMENT | PK |
| `template_id` | BIGINT UNSIGNED | NO | — | FK → tournament_templates。ON DELETE CASCADE |
| `name` | VARCHAR(50) | NO | — | 成績項目名 |
| `stat_key` | VARCHAR(30) | NO | — | プログラム内キー |
| `unit` | VARCHAR(20) | YES | NULL | 単位 |
| `data_type` | ENUM('INTEGER', 'DECIMAL', 'TIME') | NO | 'INTEGER' | データ型 |
| `aggregation_type` | ENUM('SUM', 'AVG', 'MAX', 'MIN') | NO | 'SUM' | ランキング集計方法 |
| `is_ranking_target` | BOOLEAN | NO | TRUE | 個人ランキング表示対象か |
| `ranking_label` | VARCHAR(50) | YES | NULL | ランキングタイトル |
| `sort_order` | INT | NO | 0 | 表示順 |

**インデックス**
```sql
UNIQUE INDEX uq_tt_sd_key (template_id, stat_key)
INDEX idx_tt_sd_ranking (template_id, is_ranking_target, sort_order)
```

#### `tournaments`

大会・リーグのインスタンス。組織が主催。

| カラム名 | 型 | NULL | デフォルト | 説明 |
|---------|---|------|-----------|------|
| `id` | BIGINT UNSIGNED | NO | AUTO_INCREMENT | PK |
| `organization_id` | BIGINT UNSIGNED | NO | — | FK → organizations。ON DELETE CASCADE。主催組織 |
| `template_id` | BIGINT UNSIGNED | YES | NULL | FK → tournament_templates（ON DELETE SET NULL）。使用テンプレート |
| `previous_tournament_id` | BIGINT UNSIGNED | YES | NULL | FK → tournaments（ON DELETE SET NULL）。前シーズンの大会。シーズン継続作成時に設定 |
| `name` | VARCHAR(200) | NO | — | 大会名（例: 2026年度 大分県サッカーリーグ） |
| `description` | TEXT | YES | NULL | 大会説明 |
| `format` | ENUM('LEAGUE', 'KNOCKOUT', 'GROUP_KNOCKOUT') | NO | — | 大会形式 |
| `season` | VARCHAR(50) | YES | NULL | シーズン名（例: 2026年度前期、2026 Spring） |
| `start_date` | DATE | YES | NULL | 開始日 |
| `end_date` | DATE | YES | NULL | 終了日 |
| `win_points` | TINYINT UNSIGNED | NO | 3 | 勝利時の勝点（テンプレートから複製、大会単位で上書き可） |
| `draw_points` | TINYINT UNSIGNED | NO | 1 | 引分時の勝点 |
| `loss_points` | TINYINT UNSIGNED | NO | 0 | 敗北時の勝点 |
| `has_draw` | BOOLEAN | NO | TRUE | 引分けありか |
| `has_sets` | BOOLEAN | NO | FALSE | セット制か |
| `sets_to_win` | TINYINT UNSIGNED | YES | NULL | 勝利に必要なセット数 |
| `has_extra_time` | BOOLEAN | NO | FALSE | 延長戦ありか |
| `has_penalties` | BOOLEAN | NO | FALSE | PK戦ありか |
| `score_unit_label` | VARCHAR(20) | NO | '点' | スコアの単位ラベル |
| `bonus_point_rules` | JSON | YES | NULL | 条件付き勝点加算ルール（NULL = 固定勝点のみ）。セットスコアに応じた勝点変動（バレー）やボーナスポイント（ラグビー）を定義 |
| `league_round_type` | ENUM('SINGLE', 'DOUBLE') | NO | 'SINGLE' | リーグ戦の対戦回数（SINGLE = 片道総当たり、DOUBLE = ホーム&アウェイ2回戦）。`format = LEAGUE` 時に使用 |
| `knockout_legs` | TINYINT UNSIGNED | NO | 1 | トーナメント/ノックアウトの対戦数（1 = 一発勝負、2 = ホーム&アウェイ） |
| `visibility` | ENUM('PUBLIC', 'SUPPORTERS_AND_ABOVE', 'MEMBERS_AND_ABOVE', 'ADMINS_AND_ABOVE', 'SCOPE_AFFILIATED', 'PARTICIPANTS_ONLY') | NO | 'PUBLIC' | 公開設定（F08.7 順位UI Wave0 で 2 値→6 値へ拡張・V82.001）。F00 正準 StandardVisibility 5 値 + 大会専用軸 PARTICIPANTS_ONLY（参加チーム関係者＝応援者含む）。旧 MEMBERS_ONLY は SCOPE_AFFILIATED へ移行。PUBLIC = SSRで外部閲覧可 |
| `status` | ENUM('DRAFT', 'OPEN', 'IN_PROGRESS', 'COMPLETED', 'CANCELLED', 'ARCHIVED') | NO | 'DRAFT' | ステータス |
| `version` | BIGINT | NO | 0 | 楽観的ロック用（§22） |
| `created_by` | BIGINT UNSIGNED | NO | — | FK → users。作成した ADMIN |
| `created_at` | DATETIME | NO | CURRENT_TIMESTAMP | |
| `updated_at` | DATETIME | NO | CURRENT_TIMESTAMP ON UPDATE | |
| `deleted_at` | DATETIME | YES | NULL | 論理削除 |

**インデックス**
```sql
INDEX idx_t_org_status (organization_id, status, deleted_at)  -- 組織内の大会一覧
INDEX idx_t_season (organization_id, season)                   -- シーズン検索
INDEX idx_t_visibility (visibility, status, deleted_at)        -- 公開大会検索
INDEX idx_t_prev (previous_tournament_id)                       -- シーズン系譜追跡
```

**制約・備考**
- `start_date <= end_date` は Service 層でバリデーション
- `format = KNOCKOUT` の場合、`draw_points` は未使用（延長/PK戦で決着）
- `status` 遷移: DRAFT → OPEN → IN_PROGRESS → COMPLETED → ARCHIVED / CANCELLED。`ARCHIVED` は一覧のデフォルト表示から除外（フィルタで表示可能）
- ルール値（`win_points` 等）はテンプレートから初期値をコピーするが、大会単位で上書き可能
- `bonus_point_rules` JSON スキーマ:
  - **キー形式**: `{条件}` — 条件名は英大文字+アンダースコア。Service層で対応するEvaluatorを呼び出す
  - **値**: 整数。正 = 加算、負 = 減算。セットスコア系は基本勝点を**上書き**（`win_points` の代わりに適用）
  - **セットスコア系条件**（`has_sets = true` 時に評価）:
    - `3_SET_WIN` / `4_SET_WIN` / `5_SET_WIN`: 勝利時のセット数に応じた勝点（上書き）
    - `3_SET_LOSS` / `4_SET_LOSS` / `5_SET_LOSS`: 敗北時のセット数に応じた勝点（上書き）
  - **ボーナス加算系条件**（基本勝点に加算）:
    - `4_TRIES_BONUS`: 4トライ以上のボーナス（ラグビー）。トライ数は `tournament_match_player_stats` の `stat_key = 'tries'` を集計
    - `LOSS_WITHIN_7`: 7点差以内の敗北ボーナス（ラグビー）。`score_difference` で判定
  - 例（バレー）: `{"3_SET_WIN": 3, "4_SET_WIN": 3, "5_SET_WIN": 2, "5_SET_LOSS": 1}`
  - 例（ラグビー）: `{"4_TRIES_BONUS": 1, "LOSS_WITHIN_7": 1}`
  - ADMINがカスタムテンプレートで独自条件キーを定義した場合、Service層に対応Evaluatorがなければ無視（ログ出力のみ）

#### `tournament_tiebreakers`

大会のタイブレーク（順位決定）ルール。大会作成時にテンプレートからディープコピー。テンプレートを変更しても進行中の大会に影響しない。

| カラム名 | 型 | NULL | デフォルト | 説明 |
|---------|---|------|-----------|------|
| `id` | BIGINT UNSIGNED | NO | AUTO_INCREMENT | PK |
| `tournament_id` | BIGINT UNSIGNED | NO | — | FK → tournaments。ON DELETE CASCADE |
| `priority` | TINYINT UNSIGNED | NO | — | 優先順位（1が最優先） |
| `criteria` | ENUM('POINTS', 'SCORE_DIFFERENCE', 'SCORE_FOR', 'HEAD_TO_HEAD_POINTS', 'HEAD_TO_HEAD_SCORE_DIFFERENCE', 'WINS', 'SET_RATIO', 'POINT_RATIO', 'LOSSES', 'DRAWS') | NO | — | 順位決定基準 |
| `direction` | ENUM('DESC', 'ASC') | NO | 'DESC' | ソート方向 |

**インデックス**
```sql
UNIQUE INDEX uq_t_tb (tournament_id, priority)           -- 同一大会内で優先順位ユニーク
```

#### `tournament_stat_defs`

大会の個人成績項目定義。大会作成時にテンプレートからディープコピー。

| カラム名 | 型 | NULL | デフォルト | 説明 |
|---------|---|------|-----------|------|
| `id` | BIGINT UNSIGNED | NO | AUTO_INCREMENT | PK |
| `tournament_id` | BIGINT UNSIGNED | NO | — | FK → tournaments。ON DELETE CASCADE |
| `name` | VARCHAR(50) | NO | — | 成績項目名 |
| `stat_key` | VARCHAR(30) | NO | — | プログラム内キー |
| `unit` | VARCHAR(20) | YES | NULL | 単位 |
| `data_type` | ENUM('INTEGER', 'DECIMAL', 'TIME') | NO | 'INTEGER' | データ型 |
| `aggregation_type` | ENUM('SUM', 'AVG', 'MAX', 'MIN') | NO | 'SUM' | ランキング集計方法 |
| `is_ranking_target` | BOOLEAN | NO | TRUE | 個人ランキング表示対象か |
| `ranking_label` | VARCHAR(50) | YES | NULL | ランキングタイトル |
| `sort_order` | INT | NO | 0 | 表示順 |

**インデックス**
```sql
UNIQUE INDEX uq_t_sd_key (tournament_id, stat_key)       -- 同一大会内でキーユニーク
INDEX idx_t_sd_ranking (tournament_id, is_ranking_target, sort_order)
```

**制約・備考**
- 大会作成時、テンプレートの `tournament_template_tiebreakers` → `tournament_tiebreakers`、`tournament_template_stat_defs` → `tournament_stat_defs` にディープコピーする
- コピー後は大会単位で自由に編集可能（テンプレートとは独立）
- 編集は `PATCH /organizations/{orgId}/tournaments/{tournamentId}` のリクエストボディに `tiebreakers` / `statDefs` 配列を含めて送信。Service層で既存レコードを全削除→再INSERTする置換方式（子テーブルのため楽観的ロックは親の `tournaments.version` で制御）
- `status = IN_PROGRESS` 以降のタイブレーク変更は順位表の全再計算をトリガー
- テンプレートが論理削除されても大会のルール・成績項目は保持される

#### `tournament_divisions`

ディビジョン（1部、2部等）。リーグ戦では部の階層、トーナメントではグループ（A組、B組等）として使用。

| カラム名 | 型 | NULL | デフォルト | 説明 |
|---------|---|------|-----------|------|
| `id` | BIGINT UNSIGNED | NO | AUTO_INCREMENT | PK |
| `tournament_id` | BIGINT UNSIGNED | NO | — | FK → tournaments。ON DELETE CASCADE |
| `name` | VARCHAR(100) | NO | — | ディビジョン名（例: 1部、2部、グループA） |
| `level` | TINYINT UNSIGNED | NO | 1 | 階層レベル（1 = 最上位。昇降格の上下関係に使用） |
| `promotion_slots` | TINYINT UNSIGNED | NO | 0 | 昇格枠数（上位N チームが上のディビジョンへ） |
| `relegation_slots` | TINYINT UNSIGNED | NO | 0 | 降格枠数（下位N チームが下のディビジョンへ） |
| `playoff_promotion_slots` | TINYINT UNSIGNED | NO | 0 | 入替戦による昇格枠数 |
| `max_participants` | SMALLINT UNSIGNED | YES | NULL | 最大参加チーム数（NULL = 無制限） |
| `min_entry_count` | SMALLINT UNSIGNED | YES | NULL | エントリー最少人数（NULL = 制限なし） |
| `max_entry_count` | SMALLINT UNSIGNED | YES | NULL | エントリー最大人数（NULL = 制限なし） |
| `sort_order` | INT | NO | 0 | 表示順 |
| `created_at` | DATETIME | NO | CURRENT_TIMESTAMP | |
| `updated_at` | DATETIME | NO | CURRENT_TIMESTAMP ON UPDATE | |

**インデックス**
```sql
INDEX idx_td_tournament (tournament_id, level, sort_order)  -- 大会内ディビジョン一覧
```

**制約・備考**
- `format = LEAGUE`: ディビジョン = 部（1部、2部、3部…）。`level` で上下関係を定義
- `format = KNOCKOUT`: ディビジョンは1つ（大会全体 = 1ディビジョン）
- `format = GROUP_KNOCKOUT`: グループステージ用にグループA/B/C… + 決勝トーナメント用の1ディビジョン

#### `tournament_participants`

ディビジョンへの参加チーム。

| カラム名 | 型 | NULL | デフォルト | 説明 |
|---------|---|------|-----------|------|
| `id` | BIGINT UNSIGNED | NO | AUTO_INCREMENT | PK |
| `division_id` | BIGINT UNSIGNED | NO | — | FK → tournament_divisions。ON DELETE CASCADE |
| `team_id` | BIGINT UNSIGNED | NO | — | FK → teams。ON DELETE CASCADE |
| `seed` | SMALLINT UNSIGNED | YES | NULL | シード順（トーナメントの場合。NULL = シードなし） |
| `display_name` | VARCHAR(100) | YES | NULL | 大会内での表示名（NULL = チーム名をそのまま使用） |
| `status` | ENUM('REGISTERED', 'ACTIVE', 'WITHDRAWN', 'DISQUALIFIED') | NO | 'REGISTERED' | 参加ステータス |
| `joined_at` | DATETIME | NO | CURRENT_TIMESTAMP | 参加日時 |

**インデックス**
```sql
UNIQUE INDEX uq_tp_div_team (division_id, team_id)     -- 同一ディビジョンに同一チームは1回のみ
INDEX idx_tp_team (team_id)                              -- チーム別の大会参加一覧
```

**ステータス遷移**:
- `REGISTERED` → `ACTIVE`: 大会ステータスが OPEN → IN_PROGRESS になった時点で、全 REGISTERED を一括 ACTIVE に変更
- `ACTIVE` → `WITHDRAWN`: チームが途中辞退（ADMIN操作）
- `ACTIVE` → `DISQUALIFIED`: 規律違反等で失格（ADMIN操作）
- `WITHDRAWN` / `DISQUALIFIED` のチームの未消化試合は `result = FORFEIT_*_WIN`（相手の不戦勝）に自動設定

#### `tournament_matchdays`

節・ラウンド。リーグ戦の「第1節」「第2節」やトーナメントの「1回戦」「準決勝」等。

| カラム名 | 型 | NULL | デフォルト | 説明 |
|---------|---|------|-----------|------|
| `id` | BIGINT UNSIGNED | NO | AUTO_INCREMENT | PK |
| `division_id` | BIGINT UNSIGNED | NO | — | FK → tournament_divisions。ON DELETE CASCADE |
| `name` | VARCHAR(100) | NO | — | 節名（例: 第1節、Round 1、準決勝） |
| `matchday_number` | SMALLINT UNSIGNED | NO | — | 節番号（時系列順） |
| `scheduled_date` | DATE | YES | NULL | 予定日（NULL = 未定） |
| `status` | ENUM('SCHEDULED', 'IN_PROGRESS', 'COMPLETED') | NO | 'SCHEDULED' | ステータス |
| `created_at` | DATETIME | NO | CURRENT_TIMESTAMP | |
| `updated_at` | DATETIME | NO | CURRENT_TIMESTAMP ON UPDATE | |

**インデックス**
```sql
UNIQUE INDEX uq_tm_div_num (division_id, matchday_number)  -- ディビジョン内で節番号ユニーク
INDEX idx_tm_date (scheduled_date, status)                   -- 日付検索
```

#### `tournament_matches`

対戦カード。

| カラム名 | 型 | NULL | デフォルト | 説明 |
|---------|---|------|-----------|------|
| `id` | BIGINT UNSIGNED | NO | AUTO_INCREMENT | PK |
| `matchday_id` | BIGINT UNSIGNED | NO | — | FK → tournament_matchdays。ON DELETE CASCADE |
| `home_participant_id` | BIGINT UNSIGNED | YES | NULL | FK → tournament_participants。ホームチーム（NULL = BYEまたは未定） |
| `away_participant_id` | BIGINT UNSIGNED | YES | NULL | FK → tournament_participants。アウェイチーム（NULL = BYEまたは未定） |
| `match_number` | SMALLINT UNSIGNED | YES | NULL | 節内の試合番号 |
| `scheduled_datetime` | DATETIME | YES | NULL | 試合予定日時 |
| `venue` | VARCHAR(200) | YES | NULL | 会場 |
| `home_score` | SMALLINT UNSIGNED | YES | NULL | ホームスコア（NULL = 未入力） |
| `away_score` | SMALLINT UNSIGNED | YES | NULL | アウェイスコア |
| `home_extra_score` | SMALLINT UNSIGNED | YES | NULL | ホーム延長戦スコア |
| `away_extra_score` | SMALLINT UNSIGNED | YES | NULL | アウェイ延長戦スコア |
| `home_penalty_score` | SMALLINT UNSIGNED | YES | NULL | ホームPK戦スコア |
| `away_penalty_score` | SMALLINT UNSIGNED | YES | NULL | アウェイPK戦スコア |
| `winner_participant_id` | BIGINT UNSIGNED | YES | NULL | FK → tournament_participants。勝者（NULL = 未決定 or 引分） |
| `result` | ENUM('HOME_WIN', 'AWAY_WIN', 'DRAW', 'PENDING', 'CANCELLED', 'BYE', 'FORFEIT_HOME_WIN', 'FORFEIT_AWAY_WIN') | NO | 'PENDING' | 試合結果。`FORFEIT_*` = 没収試合（出場辞退・規律違反等） |
| `leg` | TINYINT UNSIGNED | NO | 1 | 第何戦か（1 = 第1戦、2 = 第2戦。H&A方式用） |
| `next_match_id` | BIGINT UNSIGNED | YES | NULL | FK → tournament_matches（自己参照）。トーナメントで勝者が進む次の試合 |
| `next_match_slot` | ENUM('HOME', 'AWAY') | YES | NULL | 次の試合でのスロット |
| `notes` | TEXT | YES | NULL | 備考（中止理由、特記事項等） |
| `schedule_id` | BIGINT UNSIGNED | YES | NULL | FK → schedules（ON DELETE SET NULL）。自動作成されたスケジュール |
| `version` | BIGINT | NO | 0 | 楽観的ロック用（§22。スコア同時入力防止） |
| `status` | ENUM('SCHEDULED', 'IN_PROGRESS', 'COMPLETED', 'POSTPONED', 'CANCELLED') | NO | 'SCHEDULED' | ステータス |
| `created_at` | DATETIME | NO | CURRENT_TIMESTAMP | |
| `updated_at` | DATETIME | NO | CURRENT_TIMESTAMP ON UPDATE | |

**インデックス**
```sql
INDEX idx_tmatch_matchday (matchday_id, match_number)         -- 節内の試合一覧
INDEX idx_tmatch_home (home_participant_id, status)            -- ホームチームの試合検索
INDEX idx_tmatch_away (away_participant_id, status)            -- アウェイチームの試合検索
INDEX idx_tmatch_schedule (scheduled_datetime, status)         -- 日程検索
INDEX idx_tmatch_next (next_match_id)                           -- トーナメントの進行追跡
```

**制約・備考**
- `CHECK (home_participant_id IS NULL OR away_participant_id IS NULL OR home_participant_id != away_participant_id)` で同一チーム対戦を防止（BYE時は片方NULLのため通過）
- BYE（不戦勝）: 片方が NULL、`result = BYE`、もう一方が自動勝利
- トーナメント形式: `next_match_id` + `next_match_slot` で勝ち上がりのツリー構造を表現
- H&A方式（`knockout_legs = 2`）: 同じ対戦カードで `leg = 1` と `leg = 2` の2レコード。アグリゲートスコアで勝者を Service 層で判定

#### `tournament_match_sets`

セット制競技（バレー、テニス等）のセット別スコア。

| カラム名 | 型 | NULL | デフォルト | 説明 |
|---------|---|------|-----------|------|
| `id` | BIGINT UNSIGNED | NO | AUTO_INCREMENT | PK |
| `match_id` | BIGINT UNSIGNED | NO | — | FK → tournament_matches。ON DELETE CASCADE |
| `set_number` | TINYINT UNSIGNED | NO | — | セット番号（1, 2, 3…） |
| `home_score` | SMALLINT UNSIGNED | NO | — | ホームスコア |
| `away_score` | SMALLINT UNSIGNED | NO | — | アウェイスコア |

**インデックス**
```sql
UNIQUE INDEX uq_tms_match_set (match_id, set_number)   -- 1試合内でセット番号ユニーク
```

#### `tournament_match_rosters`

試合の出場メンバー登録。個人成績入力の前提データ。「出場したが成績なし」の選手も記録でき、出場試合数の正確な集計に利用。

| カラム名 | 型 | NULL | デフォルト | 説明 |
|---------|---|------|-----------|------|
| `id` | BIGINT UNSIGNED | NO | AUTO_INCREMENT | PK |
| `match_id` | BIGINT UNSIGNED | NO | — | FK → tournament_matches。ON DELETE CASCADE |
| `participant_id` | BIGINT UNSIGNED | NO | — | FK → tournament_participants。所属チーム |
| `user_id` | BIGINT UNSIGNED | NO | — | FK → users。選手 |
| `is_starter` | BOOLEAN | NO | TRUE | スタメンか（FALSE = 途中出場/ベンチ） |
| `jersey_number` | SMALLINT UNSIGNED | YES | NULL | 背番号（NULL = 未設定） |
| `position` | VARCHAR(30) | YES | NULL | ポジション（自由記述。例: GK, FW, PG） |
| `created_at` | DATETIME | NO | CURRENT_TIMESTAMP | |

**インデックス**
```sql
UNIQUE INDEX uq_tmr_match_user (match_id, user_id)       -- 同一試合に同一選手は1回のみ
INDEX idx_tmr_participant (participant_id)                   -- チーム別の出場一覧
INDEX idx_tmr_user (user_id)                                 -- 選手別の出場一覧
```

**制約・備考**
- `tournament_match_player_stats` の `user_id` は `tournament_match_rosters` に存在するユーザーのみ許可（Service 層バリデーション）
- ロスター未登録でも個人成績の入力は可能（後方互換。ロスター登録は任意）

#### `tournament_match_player_stats`

試合ごとの個人成績。大会の `tournament_stat_defs` で定義された項目を記録。

| カラム名 | 型 | NULL | デフォルト | 説明 |
|---------|---|------|-----------|------|
| `id` | BIGINT UNSIGNED | NO | AUTO_INCREMENT | PK |
| `match_id` | BIGINT UNSIGNED | NO | — | FK → tournament_matches。ON DELETE CASCADE |
| `participant_id` | BIGINT UNSIGNED | NO | — | FK → tournament_participants。所属チーム |
| `user_id` | BIGINT UNSIGNED | NO | — | FK → users。選手 |
| `stat_key` | VARCHAR(30) | NO | — | 成績項目キー（テンプレートの `stat_key` に対応） |
| `value_int` | INT | YES | NULL | 整数値（`data_type = INTEGER` 時） |
| `value_decimal` | DECIMAL(15,4) | YES | NULL | 小数値（`data_type = DECIMAL` 時） |
| `value_time` | TIME | YES | NULL | 時間値（`data_type = TIME` 時） |
| `created_at` | DATETIME | NO | CURRENT_TIMESTAMP | |
| `updated_at` | DATETIME | NO | CURRENT_TIMESTAMP ON UPDATE | |

**インデックス**
```sql
UNIQUE INDEX uq_tmps (match_id, user_id, stat_key)      -- 同一試合・選手・項目でユニーク
INDEX idx_tmps_user (user_id, stat_key)                   -- 選手別の成績検索
INDEX idx_tmps_participant (participant_id, stat_key)     -- チーム別の成績検索
```

**制約・備考**
- `value_int`, `value_decimal`, `value_time` のうち、テンプレートの `data_type` に対応する1つのみ使用。他は NULL
- Service 層で `stat_key` が大会の `tournament_stat_defs` に存在するかバリデーション

#### `tournament_standings`

順位表の非正規化キャッシュ。試合結果の入力・変更時に再計算。

| カラム名 | 型 | NULL | デフォルト | 説明 |
|---------|---|------|-----------|------|
| `id` | BIGINT UNSIGNED | NO | AUTO_INCREMENT | PK |
| `division_id` | BIGINT UNSIGNED | NO | — | FK → tournament_divisions。ON DELETE CASCADE |
| `participant_id` | BIGINT UNSIGNED | NO | — | FK → tournament_participants。ON DELETE CASCADE |
| `rank` | SMALLINT UNSIGNED | NO | — | 現在順位 |
| `played` | SMALLINT UNSIGNED | NO | 0 | 試合数 |
| `wins` | SMALLINT UNSIGNED | NO | 0 | 勝利数 |
| `draws` | SMALLINT UNSIGNED | NO | 0 | 引分数 |
| `losses` | SMALLINT UNSIGNED | NO | 0 | 敗北数 |
| `score_for` | INT | NO | 0 | 総得点（競技によりゴール・ポイント・セット等。`score_unit_label` で表示名を制御） |
| `score_against` | INT | NO | 0 | 総失点 |
| `score_difference` | INT | NO | 0 | 得失点差（非正規化: `score_for - score_against`） |
| `points` | INT | NO | 0 | 勝点 |
| `bonus_points` | SMALLINT | NO | 0 | ボーナスポイント合計（`bonus_point_rules` 由来の加算分。表示内訳用。`points` に含まれる） |
| `sets_won` | SMALLINT UNSIGNED | NO | 0 | 獲得セット数（セット制競技用） |
| `sets_lost` | SMALLINT UNSIGNED | NO | 0 | 失セット数 |
| `form` | VARCHAR(10) | YES | NULL | 直近5試合の結果（例: 'WWDLW'）。表示用 |
| `promotion_zone` | ENUM('PROMOTED', 'PLAYOFF', 'SAFE', 'RELEGATED') | YES | NULL | 昇降格ゾーン（リアルタイム判定。表示装飾用） |
| `last_calculated_at` | DATETIME | NO | CURRENT_TIMESTAMP | 最終計算日時 |

**インデックス**
```sql
UNIQUE INDEX uq_ts_div_part (division_id, participant_id)  -- ディビジョン×参加チームでユニーク
INDEX idx_ts_rank (division_id, rank)                        -- 順位順表示
```

**制約・備考**
- 試合結果の INSERT/UPDATE 時に、該当ディビジョンの全参加チームの順位を再計算して UPSERT
- 大会の `tournament_tiebreakers` の優先順位に従って `rank` を算出
- `promotion_zone` は `division.promotion_slots` / `relegation_slots` と現在の `rank` から自動判定

#### `tournament_individual_rankings`

個人ランキングの非正規化キャッシュ。

| カラム名 | 型 | NULL | デフォルト | 説明 |
|---------|---|------|-----------|------|
| `id` | BIGINT UNSIGNED | NO | AUTO_INCREMENT | PK |
| `tournament_id` | BIGINT UNSIGNED | NO | — | FK → tournaments。ON DELETE CASCADE |
| `user_id` | BIGINT UNSIGNED | NO | — | FK → users。ON DELETE CASCADE |
| `participant_id` | BIGINT UNSIGNED | NO | — | FK → tournament_participants。ON DELETE CASCADE |
| `stat_key` | VARCHAR(30) | NO | — | 成績項目キー |
| `rank` | SMALLINT UNSIGNED | NO | — | ランキング順位 |
| `total_value_int` | INT | YES | NULL | 集計値（整数） |
| `total_value_decimal` | DECIMAL(15,4) | YES | NULL | 集計値（小数） |
| `total_value_time` | TIME | YES | NULL | 集計値（時間。`data_type = TIME` 時） |
| `matches_played` | SMALLINT UNSIGNED | NO | 0 | 出場試合数 |
| `last_calculated_at` | DATETIME | NO | CURRENT_TIMESTAMP | 最終計算日時 |

**インデックス**
```sql
UNIQUE INDEX uq_tir (tournament_id, stat_key, user_id)    -- 大会×項目×選手でユニーク
INDEX idx_tir_rank (tournament_id, stat_key, rank)          -- ランキング順表示
INDEX idx_tir_user (user_id, stat_key)                       -- 選手別のランキング検索
```

#### `tournament_promotion_records`

昇格・降格の確定記録。シーズン終了後に ADMIN が操作。

| カラム名 | 型 | NULL | デフォルト | 説明 |
|---------|---|------|-----------|------|
| `id` | BIGINT UNSIGNED | NO | AUTO_INCREMENT | PK |
| `tournament_id` | BIGINT UNSIGNED | NO | — | FK → tournaments。ON DELETE CASCADE |
| `team_id` | BIGINT UNSIGNED | NO | — | FK → teams。ON DELETE CASCADE |
| `from_division_id` | BIGINT UNSIGNED | NO | — | FK → tournament_divisions。移動元ディビジョン |
| `to_division_id` | BIGINT UNSIGNED | NO | — | FK → tournament_divisions。移動先ディビジョン |
| `type` | ENUM('PROMOTION', 'RELEGATION', 'PLAYOFF_PROMOTION', 'PLAYOFF_RELEGATION') | NO | — | 昇降格種別 |
| `final_rank` | SMALLINT UNSIGNED | NO | — | 移動元ディビジョンでの最終順位 |
| `reason` | VARCHAR(200) | YES | NULL | 理由（入替戦結果、特例措置等の備考） |
| `executed_by` | BIGINT UNSIGNED | NO | — | FK → users。操作した ADMIN |
| `executed_at` | DATETIME | NO | CURRENT_TIMESTAMP | 実行日時 |

**インデックス**
```sql
UNIQUE INDEX uq_tpr_tournament_team (tournament_id, team_id)  -- 同一大会で同一チームの重複昇降格防止
INDEX idx_tpr_tournament (tournament_id, type)                  -- 大会別の昇降格一覧
INDEX idx_tpr_team (team_id)                                     -- チーム別の昇降格履歴
```

---

## 4. API設計

### エンドポイント一覧

全エンドポイントプレフィックス: `/api/v1`

> 📝 **実装注記（2026-05-17）**: 本セクションの全エンドポイント（約 52 件）は
> `TournamentController`, `DivisionController`, `MatchController`, `StandingsController`,
> `TournamentTemplateController`, `TournamentEntryMemberController`,
> `TournamentEntryTemplateController`, `TournamentPdfController`, `PromotionController`
> で実装済み。API drift baseline は本セクションのパス記載が `/api/v1` プレフィックスを
> 省略しているため「実装あり・設計なし」として誤検出するが、上記の共通プレフィックス注記により
> 各行のパスは `/api/v1` + 行頭パスで構成される仕様であり、実装と設計に齟齬は無い。
> スキャナ v5 改修ではセクション冒頭の共通プレフィックス記法を解釈する処理を追加すべし。

> **認可の共通ルール**: 全 `/organizations/{orgId}/...` エンドポイントは、Service 層でリクエストユーザーが当該組織のメンバーであることを検証する（F01.2 の `organization_members` テーブルで確認）。パスパラメータの `{orgId}` を信頼せず、必ずユーザーの所属組織と照合すること。`/public/...` エンドポイントは `visibility = PUBLIC` のリソースのみ返却し、`MEMBERS_ONLY` のデータは一切含めない。

#### テンプレート管理（SYSTEM_ADMIN）

| メソッド | パス | 説明 | 認可 |
|---------|------|------|------|
| GET | `/system-admin/tournament-presets` | プリセット一覧 | SYSTEM_ADMIN |
| POST | `/system-admin/tournament-presets` | プリセット作成 | SYSTEM_ADMIN |
| GET | `/system-admin/tournament-presets/{presetId}` | プリセット詳細（タイブレーク・成績項目含む） | SYSTEM_ADMIN |
| PATCH | `/system-admin/tournament-presets/{presetId}` | プリセット更新 | SYSTEM_ADMIN |
| DELETE | `/system-admin/tournament-presets/{presetId}` | プリセット論理削除 | SYSTEM_ADMIN |

#### テンプレート管理（組織 ADMIN）

| メソッド | パス | 説明 | 認可 |
|---------|------|------|------|
| GET | `/organizations/{orgId}/tournament-templates` | テンプレート一覧 | ADMIN, DEPUTY_ADMIN |
| POST | `/organizations/{orgId}/tournament-templates` | テンプレート作成（ゼロから） | ADMIN |
| POST | `/organizations/{orgId}/tournament-templates/clone/{presetId}` | プリセットから複製して作成 | ADMIN |
| GET | `/organizations/{orgId}/tournament-templates/{templateId}` | テンプレート詳細 | ADMIN, DEPUTY_ADMIN |
| PATCH | `/organizations/{orgId}/tournament-templates/{templateId}` | テンプレート更新（`version` 必須） | ADMIN |
| DELETE | `/organizations/{orgId}/tournament-templates/{templateId}` | テンプレート論理削除 | ADMIN |
| GET | `/tournament-presets` | 公開プリセット一覧（ADMIN がプリセット選択時に使用） | ADMIN |

#### 大会・リーグ管理

| メソッド | パス | 説明 | 認可 |
|---------|------|------|------|
| GET | `/organizations/{orgId}/tournaments` | 大会一覧（ページネーション・ステータスフィルタ） | MEMBER+ |
| POST | `/organizations/{orgId}/tournaments` | 大会作成 | ADMIN |
| GET | `/organizations/{orgId}/tournaments/{tournamentId}` | 大会詳細 | MEMBER+ |
| PATCH | `/organizations/{orgId}/tournaments/{tournamentId}` | 大会更新（`version` 必須。`tiebreakers` / `stat_defs` 配列のネスト更新可） | ADMIN |
| DELETE | `/organizations/{orgId}/tournaments/{tournamentId}` | 大会論理削除 | ADMIN |
| PATCH | `/organizations/{orgId}/tournaments/{tournamentId}/status` | ステータス変更（OPEN→IN_PROGRESS→COMPLETED→ARCHIVED等） | ADMIN |
| POST | `/organizations/{orgId}/tournaments/continue/{previousTournamentId}` | 前シーズンから継続作成（ディビジョン構成コピー + 昇降格反映チーム配置提案） | ADMIN |

#### ディビジョン管理

| メソッド | パス | 説明 | 認可 |
|---------|------|------|------|
| GET | `/organizations/{orgId}/tournaments/{tId}/divisions` | ディビジョン一覧 | MEMBER+ |
| POST | `/organizations/{orgId}/tournaments/{tId}/divisions` | ディビジョン作成 | ADMIN |
| PATCH | `/organizations/{orgId}/tournaments/{tId}/divisions/{divId}` | ディビジョン更新 | ADMIN |
| DELETE | `/organizations/{orgId}/tournaments/{tId}/divisions/{divId}` | ディビジョン削除 | ADMIN |

#### 参加チーム管理

| メソッド | パス | 説明 | 認可 |
|---------|------|------|------|
| GET | `/organizations/{orgId}/tournaments/{tId}/divisions/{divId}/participants` | 参加チーム一覧 | MEMBER+ |
| POST | `/organizations/{orgId}/tournaments/{tId}/divisions/{divId}/participants` | チーム追加 | ADMIN |
| PATCH | `/organizations/{orgId}/tournaments/{tId}/divisions/{divId}/participants/{pId}` | 参加情報更新（シード順、ステータス等） | ADMIN |
| DELETE | `/organizations/{orgId}/tournaments/{tId}/divisions/{divId}/participants/{pId}` | チーム除外 | ADMIN |

#### 対戦カード・結果管理

| メソッド | パス | 説明 | 認可 |
|---------|------|------|------|
| GET | `/organizations/{orgId}/tournaments/{tId}/divisions/{divId}/matchdays` | 節一覧 | MEMBER+ |
| POST | `/organizations/{orgId}/tournaments/{tId}/divisions/{divId}/matchdays` | 節作成 | ADMIN, DEPUTY_ADMIN(MANAGE_TOURNAMENT) |
| POST | `/organizations/{orgId}/tournaments/{tId}/divisions/{divId}/matchdays/generate` | 対戦カード自動生成（総当たり/トーナメント表） | ADMIN |
| GET | `/organizations/{orgId}/tournaments/{tId}/matches/{matchId}` | 試合詳細（スコア・個人成績含む） | MEMBER+ |
| PATCH | `/organizations/{orgId}/tournaments/{tId}/matches/{matchId}/score` | スコア入力・更新（`version` 必須。順位表自動再計算トリガー） | ADMIN, DEPUTY_ADMIN(MANAGE_TOURNAMENT) |
| PATCH | `/organizations/{orgId}/tournaments/{tId}/matches/{matchId}/player-stats` | 個人成績一括入力（`version` 必須） | ADMIN, DEPUTY_ADMIN(MANAGE_TOURNAMENT) |
| PATCH | `/organizations/{orgId}/tournaments/{tId}/matches/{matchId}/status` | 試合ステータス変更（延期・中止等） | ADMIN |
| POST | `/organizations/{orgId}/tournaments/{tId}/divisions/{divId}/matchdays/{mdId}/scores/batch` | 節内全試合スコア一括入力（順位表再計算は1回のみ発火） | ADMIN, DEPUTY_ADMIN(MANAGE_TOURNAMENT) |
| POST | `/organizations/{orgId}/tournaments/{tId}/divisions/{divId}/matchdays/{mdId}/scores/import` | CSVアップロードによるスコア一括インポート | ADMIN |

#### 出場メンバー管理

| メソッド | パス | 説明 | 認可 |
|---------|------|------|------|
| GET | `/organizations/{orgId}/tournaments/{tId}/matches/{matchId}/rosters` | 試合の出場メンバー一覧 | MEMBER+ |
| POST | `/organizations/{orgId}/tournaments/{tId}/matches/{matchId}/rosters` | 出場メンバー一括登録（配列） | ADMIN, DEPUTY_ADMIN(MANAGE_TOURNAMENT) |
| DELETE | `/organizations/{orgId}/tournaments/{tId}/matches/{matchId}/rosters/{rosterId}` | 出場メンバー削除 | ADMIN, DEPUTY_ADMIN(MANAGE_TOURNAMENT) |

#### エントリー表管理

| メソッド | パス | 説明 | 認可 |
|---------|------|------|------|
| GET | `/organizations/{orgId}/tournaments/{tId}/divisions/{divId}/participants/{pId}/entry-members` | エントリー一覧＋チームメンバー候補取得 | MEMBER+（自チームのみ） |
| POST | `/organizations/{orgId}/tournaments/{tId}/divisions/{divId}/participants/{pId}/entry-members/load-from-team` | チームメンバーから一括ロード（テンプレート）| ADMIN, DEPUTY_ADMIN(MANAGE_TOURNAMENT), チームADMIN |
| PUT | `/organizations/{orgId}/tournaments/{tId}/divisions/{divId}/participants/{pId}/entry-members` | エントリー全置換（確定保存）| ADMIN, DEPUTY_ADMIN(MANAGE_TOURNAMENT), チームADMIN |
| DELETE | `/organizations/{orgId}/tournaments/{tId}/divisions/{divId}/participants/{pId}/entry-members/{entryMemberId}` | エントリーメンバー個別削除 | ADMIN, DEPUTY_ADMIN(MANAGE_TOURNAMENT), チームADMIN |
| GET | `/organizations/{orgId}/tournaments/{tId}/divisions/{divId}/participants/{pId}/entry-members/pdf` | エントリー表PDF出力 | MEMBER+（自チームのみ） |
| GET | `/organizations/{orgId}/tournaments/{tId}/divisions/{divId}/entry-summary` | 全チームエントリー状況サマリー（主催者用）| ADMIN, DEPUTY_ADMIN(MANAGE_TOURNAMENT) |

**エントリーテンプレート管理（チームコンテキスト）**
| GET    | `/organizations/{orgId}/teams/{teamId}/entry-templates` | テンプレート一覧（最大5件）|
| POST   | `/organizations/{orgId}/teams/{teamId}/entry-templates` | テンプレート作成 |
| GET    | `/organizations/{orgId}/teams/{teamId}/entry-templates/{templateId}` | テンプレート詳細 |
| PUT    | `/organizations/{orgId}/teams/{teamId}/entry-templates/{templateId}` | テンプレート更新 |
| DELETE | `/organizations/{orgId}/teams/{teamId}/entry-templates/{templateId}` | テンプレート削除 |

**テンプレート適用（トーナメントコンテキスト）**
| POST | `/organizations/{orgId}/tournaments/{tId}/divisions/{divId}/participants/{pId}/entry-members/apply-template` | テンプレートをエントリー表に一括適用 |


#### 順位表・ランキング

| メソッド | パス | 説明 | 認可 |
|---------|------|------|------|
| GET | `/organizations/{orgId}/tournaments/{tId}/divisions/{divId}/standings` | 順位表 | MEMBER+ |
| GET | `/organizations/{orgId}/tournaments/{tId}/divisions/{divId}/matrix` | 対戦マトリクス（縦軸×横軸チーム、交差点にスコア） | MEMBER+ |
| GET | `/organizations/{orgId}/tournaments/{tId}/rankings/{statKey}` | 個人ランキング（得点王等） | MEMBER+ |
| GET | `/organizations/{orgId}/tournaments/{tId}/rankings` | 全ランキング一覧（項目別サマリー） | MEMBER+ |
| POST | `/organizations/{orgId}/tournaments/{tId}/divisions/{divId}/standings/recalculate` | 順位表の手動再計算（トラブルリカバリ用） | ADMIN |
| GET | `/teams/{teamId}/tournament-history` | チームの大会参加履歴・成績 | MEMBER+（閲覧者が見られない大会は per-tournament で除外） |
| GET | `/teams/{teamId}/tournament-stats` | チーム通算成績（通算勝敗、通算得点、参加大会数、最高順位等） | MEMBER+（閲覧者が見られない大会は per-tournament で除外） |

> **F08.7 順位UI Wave0 検分フォロー（認可根治・2026-06-11 / PR #1453）**
>
> Wave0 検分で炙り出した認可の穴を以下のとおり根治した。`@EnableMethodSecurity`（#1266 点火済）により
> `@PreAuthorize` は実機で実効する。
>
> - **大会 CRUD 書込（B-1）**: `TournamentController` の create / update / delete / changeStatus / continue に
>   `@accessGuard.isScopeAdmin(authentication, #orgId, 'ORGANIZATION')` を付与。従来は認可ゼロで、認証さえあれば
>   他組織の大会を改竄できる IDOR/権限昇格の穴だった。読取（list / detail）は対象外。
> - **対戦カード GET 可視性（B-2a）**: `MatchController` の節一覧 / 試合詳細 / 出場メンバー一覧に
>   F00 共通可視性ガード（`canView(TOURNAMENT, tId, viewer)`・不可視は IDOR 防止のため 404）を付与。
> - **チーム横断集計の漏洩（B-2b）**: `/teams/{teamId}/tournament-history` `/tournament-stats` は大会単位 tId を
>   持たないため、`StandingsQueryService` 側で per-tournament 可視性フィルタを掛け、閲覧者が `canView` できない
>   非公開大会の成績を履歴・通算成績の双方から除外する（漏洩防止）。
> - **手動再計算 recalculate（🟡）**: 書込（再計算）系のため `@accessGuard.isScopeAdmin(..., 'ORGANIZATION')` を付与。
>   読取権限だけで起動できる非対称を是正（冪等だが書込系ゆえ admin 限定が正）。
>
> **副作用（重要）**: 大会のスコア入力・順位再計算・大会 CRUD は **主催組織（ORGANIZATION スコープ）の
> ADMIN/DEPUTY_ADMIN 限定**であり、`@AccessGuard` は SYSTEM_ADMIN を内部短絡で許可する。**参加チーム側の
> チーム admin（TEAM スコープ admin）は tournament スコープのスコア入力・再計算を実行できない**（組織 admin に
> 委ねる運用）。チーム admin にスコア入力を委譲する要件が出た場合は別 Wave で per-participant-team の
> 委譲ガードを設計すること（現状はエントリー表管理のみチーム admin に開放されている＝§のエントリー表 PUT/DELETE）。
>
> **観点5（PARTICIPANTS_ONLY と応援者・現状維持で正）**: PARTICIPANTS_ONLY の `evaluateCustom` は参加チームの
> メンバーシップをロールで絞らないため、参加チームの **SUPPORTER も可視**になる。これは「参加チーム関係者＝
> 応援者を含む内輪」の意図に合致するため現状維持（_AND_ABOVE ラダーの MEMBERS_AND_ABOVE が応援者を除外するのとは
> 別軸の、大会専用 CUSTOM 軸）。

#### 公開API（SSR・未認証アクセス用）

`visibility = PUBLIC` の大会のみアクセス可能。SSRページのサーバーサイドレンダリングおよびGUEST閲覧用。

| メソッド | パス | 説明 | 認可 |
|---------|------|------|------|
| GET | `/public/organizations/{orgId}/tournaments` | 公開大会一覧 | 認証不要 |
| GET | `/public/organizations/{orgId}/tournaments/{tId}` | 公開大会詳細 | 認証不要 |
| GET | `/public/organizations/{orgId}/tournaments/{tId}/divisions/{divId}/standings` | 公開順位表 | 認証不要 |
| GET | `/public/organizations/{orgId}/tournaments/{tId}/rankings/{statKey}` | 公開個人ランキング | 認証不要 |
| GET | `/public/organizations/{orgId}/tournaments/{tId}/bracket` | 公開トーナメント表 | 認証不要 |
| GET | `/public/organizations/{orgId}/tournaments/{tId}/divisions/{divId}/matrix` | 公開対戦マトリクス | 認証不要 |

#### 埋め込みウィジェット

外部サイトにiframe等で埋め込むための専用エンドポイント。`visibility = PUBLIC` の大会のみ。CORSヘッダー付きHTML/JSONを返却。Mannschaftブランドカラーで統一されたスタイル。

| メソッド | パス | 説明 | 認可 |
|---------|------|------|------|
| GET | `/embed/organizations/{orgId}/tournaments/{tId}/standings/{divId}` | 埋め込み用順位表（HTML/JSON） | 認証不要 |
| GET | `/embed/organizations/{orgId}/tournaments/{tId}/bracket` | 埋め込み用トーナメント表 | 認証不要 |
| GET | `/embed/organizations/{orgId}/tournaments/{tId}/rankings/{statKey}` | 埋め込み用個人ランキング | 認証不要 |

> **レスポンス形式**: `Accept` ヘッダーで分岐。`text/html` → スタイル付きHTMLフラグメント（iframe用）、`application/json` → 構造化JSON（独自レンダリング用）。

#### 昇格・降格

| メソッド | パス | 説明 | 認可 |
|---------|------|------|------|
| POST | `/organizations/{orgId}/tournaments/{tId}/promotions` | 昇格・降格の実行 | ADMIN |
| GET | `/organizations/{orgId}/tournaments/{tId}/promotions` | 昇降格履歴 | MEMBER+ |
| POST | `/organizations/{orgId}/tournaments/{tId}/promotions/preview` | 昇降格プレビュー（現在の順位から自動判定した候補表示） | ADMIN |

#### PDF出力

| メソッド | パス | 説明 | 認可 |
|---------|------|------|------|
| GET | `/organizations/{orgId}/tournaments/{tId}/divisions/{divId}/standings/pdf` | 順位表PDF | ADMIN, DEPUTY_ADMIN(MANAGE_TOURNAMENT) |
| GET | `/organizations/{orgId}/tournaments/{tId}/bracket/pdf` | トーナメント表PDF（KNOCKOUT/GROUP_KNOCKOUT形式のみ） | ADMIN, DEPUTY_ADMIN(MANAGE_TOURNAMENT) |
| GET | `/organizations/{orgId}/tournaments/{tId}/rankings/{statKey}/pdf` | 個人ランキングPDF | ADMIN, DEPUTY_ADMIN(MANAGE_TOURNAMENT) |
| GET | `/organizations/{orgId}/tournaments/{tId}/divisions/{divId}/matrix/pdf` | 対戦マトリクスPDF | ADMIN, DEPUTY_ADMIN(MANAGE_TOURNAMENT) |

---

## 5. 主要ビジネスロジック

### 5.1 対戦カード自動生成

#### リーグ戦（LEAGUE）
- **総当たり方式**: `league_round_type` に応じてカード数を決定
  - `SINGLE`: N*(N-1)/2 カード（片道総当たり）
  - `DOUBLE`: N*(N-1) カード（ホーム&アウェイ2回戦）
- 節への振り分けはラウンドロビンアルゴリズム（参加チーム数が奇数の場合は BYE を自動追加）

#### トーナメント（KNOCKOUT）
- **シード順考慮**: `seed` 値に基づいてトーナメントブラケットを自動生成
- 参加チーム数が 2^n でない場合、上位シードに1回戦 BYE を割り当て
- `next_match_id` + `next_match_slot` でツリー構造を構築

#### グループステージ + 決勝（GROUP_KNOCKOUT）
- グループステージ: 各グループ内で総当たり
- 決勝トーナメント: グループステージ完了後に ADMIN が手動で決勝トーナメントのディビジョンにチームを配置、またはグループ順位から自動配置

### 5.2 順位表計算

1. 該当ディビジョンの全 `COMPLETED` 試合を集計
2. 各チームの wins/draws/losses/score_for/score_against を算出（没収試合 `FORFEIT_*` は勝敗に含め、スコアは大会規定に従い ADMIN が入力）
3. 基本勝点 = wins * `win_points` + draws * `draw_points` + losses * `loss_points`
4. `bonus_point_rules` が非NULLの場合、各試合のセットスコア・点差等の条件に基づきボーナス/ペナルティポイントを加算。例:
   - バレー 3-2勝利 → `5_SET_WIN` = 2点（通常の3点ではなく2点）、3-2敗北 → `5_SET_LOSS` = 1点
   - ラグビー 4トライ以上 → `4_TRIES_BONUS` = +1点、7点差以内の負け → `LOSS_WITHIN_7` = +1点
5. 大会の `tournament_tiebreakers` の優先順位に従ってソート
6. `HEAD_TO_HEAD_*` 系タイブレーク: 同勝点チーム間の直接対決成績のみで小グループ内順位を再計算
7. `tournament_standings` テーブルに UPSERT
8. `promotion_zone` を `promotion_slots` / `relegation_slots` から判定

**トリガー**: 試合スコアの INSERT/UPDATE 時に `StandingsRecalculationEvent` を発火 → 非同期で再計算。一括スコア入力（`/scores/batch`）時はイベントを1回だけ発火（N回ではなく）。

**冪等性**: 再計算ロジックは毎回全 `COMPLETED` 試合からゼロ計算して UPSERT する冪等方式。イベント二重発火時も結果が同一になることを保証。手動再計算 API（`POST /.../{divId}/standings/recalculate`）も同一ロジックを使用し、ADMINのトラブルリカバリ手段として提供。

**Valkey キャッシュ戦略**:
- `mannschaft:cache:standings:{divisionId}` — TTL 1時間。スコア更新・再計算完了時に即時 invalidate
- `mannschaft:cache:rankings:{tournamentId}:{statKey}` — TTL 1時間。個人成績更新時に即時 invalidate
- `mannschaft:cache:matrix:{divisionId}` — TTL 1時間。スコア更新時に即時 invalidate
- 公開API・埋め込みAPIはキャッシュヒット時にDBクエリを発行しない

**監査ログ**: スコア更新時は `audit_logs`（Phase 10, F10.3）に `TOURNAMENT_MATCH_SCORE_UPDATED` イベントを記録（変更前後の値を保持）。`status = COMPLETED` の試合スコアを修正する場合は以下の制約を適用:
- ADMIN のみ実行可（DEPUTY_ADMIN は不可）
- `notes`（修正理由）の入力を必須化
- 順位表・個人ランキングの全再計算をトリガー

### 5.3 個人ランキング計算

1. 大会の全 `COMPLETED` 試合から `tournament_match_player_stats` を集計
2. 大会の `tournament_stat_defs` で `is_ranking_target = TRUE` の項目ごとに集計
3. `aggregation_type`（SUM/AVG/MAX/MIN）に従って集計値を算出
4. `tournament_individual_rankings` テーブルに UPSERT

**トリガー**: 個人成績の INSERT/UPDATE 時に `RankingsRecalculationEvent` を発火

### 5.4 昇降格処理

1. ADMIN が `/promotions/preview` で昇降格候補を確認
   - 各ディビジョンの `promotion_slots` / `relegation_slots` に基づき自動判定
   - 入替戦枠（`playoff_promotion_slots`）がある場合はその旨も表示
2. ADMIN がプレビュー内容を確認・調整して `/promotions` で確定実行
3. `tournament_promotion_records` に記録
4. 次シーズンの大会作成時、前シーズンの昇降格結果を参照して自動でディビジョン振り分けを提案

### 5.5 シーズン継続作成

`POST /organizations/{orgId}/tournaments/continue/{previousTournamentId}` で前シーズンから新シーズンを作成する処理:

1. 前シーズン大会の `status = COMPLETED` or `ARCHIVED` を検証
2. ディビジョン構成（名前・レベル・昇降格枠）をコピー
3. ルール値（`win_points` 等）・タイブレーク・成績項目をコピー
4. `tournament_promotion_records` を参照し、昇降格を反映したチーム配置を**提案**（DRAFT状態で返却）
5. ADMINが提案内容を確認・微調整して確定（新規チーム追加・脱退チーム除外等）
6. `previous_tournament_id` に前シーズンの大会IDを設定（シーズン系譜の追跡）

### 5.6 PDF出力

- **順位表PDF**: ディビジョン名、順位・チーム名・試合数・勝敗・得失点・勝点のテーブル。昇格/降格ゾーンを色分け
- **トーナメント表PDF**: ブラケット図。スコア付き。未決定のカードは空欄
- **個人ランキングPDF**: 順位・選手名・所属チーム・集計値のテーブル
- **対戦マトリクスPDF**: 縦軸×横軸チーム名、交差点にスコアの表形式

生成方式は既存の PDF 基盤（OpenPDF / Flying Saucer）を使用。日本語フォント対応済み。

> **パフォーマンス方針**: Phase 8 初期リリースでは同期 GET で提供。大半のユースケース（10〜20チーム）では問題ないと想定。大規模大会（40チーム以上）でタイムアウトが発生した場合、既存のデータエクスポート基盤（`POST /export` → 202 + `jobId` → `GET /export/status/{jobId}` → `GET /export/download/{jobId}`）への非同期化を検討。

### 5.7 WebSocket リアルタイム更新

既存の WebSocket 基盤（STOMP over SockJS）を利用し、順位表・トーナメント表のリアルタイム更新を提供。

| トピック | 発火タイミング | ペイロード |
|---------|--------------|-----------|
| `/topic/tournaments/{tId}/standings/{divId}` | 順位表再計算完了時 | 更新後の順位表全体（`StandingsResponse`） |
| `/topic/tournaments/{tId}/bracket` | トーナメント試合結果確定時 | 更新された試合カード（`MatchResponse`） |
| `/topic/tournaments/{tId}/rankings/{statKey}` | 個人ランキング再計算完了時 | 上位N件のランキング（`RankingResponse`） |

DB・テーブル追加なし。`StandingsRecalculationEvent` / `RankingsRecalculationEvent` のリスナーに WebSocket 送信処理を追加するだけ。

### 5.8 連携機能

| 連携先 | 内容 |
|--------|------|
| F03.1 スケジュール | 試合日程 → 組織スケジュールに自動追加（`schedule_id` で紐付け） |
| F06.4 活動記録 | 試合完了 → 活動記録を自動作成（テンプレート連携）。`activity_results.source_type = 'TOURNAMENT_MATCH'` + `source_id = match_id` で逆引き。重複作成防止にも利用 |
| F07.2 パフォーマンス | 個人成績 → パフォーマンス指標に自動反映（`linked_activity_field_id` 連携） |
| F04.3 プッシュ通知 | 試合結果確定・昇降格確定時に通知 |

### 5.9 ディビジョン別ターゲティング（他機能との横断連携）

> **連絡スペースとしての具体化は [F08.7.1 / 01_communication.md](F08.7.1_tournament_extensions/01_communication.md) を参照。** 本節の「掲示板/通知/DM の配信フィルタ」は一過性のターゲティングであり、大会/ディビジョンに紐づく**常設の連絡スペース（掲示板・チャット）**は F08.7.1 で `tournament_contact_space` ＋ bulletin `ScopeType`（`TOURNAMENT`/`TOURNAMENT_DIVISION`）＋ chat `ChannelType`（`TOURNAMENT_CHAT`/`TOURNAMENT_DIVISION_CHAT`）として実装可能レベルに具体化済み（read/write 分離認可・公開トグル・自動付帯）。本節と F08.7.1 はチーム解決の源泉（`tournament_participants`・status=ACTIVE 除外ルール）を共有する。

ディビジョン所属（`tournament_participants` のチーム×ディビジョン紐付け）を、他機能の**配信ターゲット**として横断的に利用する。

**ユースケース例**:
- 「1部リーグ所属チームのみに審判派遣の案内を掲示板に投稿したい」
- 「2部リーグのチームだけに昇格プレーオフの日程変更をプッシュ通知したい」
- 「3部・4部の全チームにスポンサー募集のダイレクトメールを送りたい」

**実装方針**:
- 既存の通知・掲示板・DM等の配信対象選択UIに「大会ディビジョンで絞り込み」フィルタを追加
- ADMINが配信時に「大会 → ディビジョン」を選択すると、`tournament_participants` から対象チームを解決 → チームのメンバー一覧を取得 → 配信対象に設定
- API側は汎用のターゲティングパラメータとして `target_type = 'TOURNAMENT_DIVISION'` + `target_id = {divisionId}` を受け付ける

| 連携先 | ターゲティング方式 |
|--------|------------------|
| F04.3 プッシュ通知 | `notification_targets` に `target_type = 'TOURNAMENT_DIVISION'` を追加。該当ディビジョンの全参加チームメンバーに配信 |
| F05.1 掲示板 | 投稿の `visibility_scope` に `TOURNAMENT_DIVISION` を追加。該当ディビジョン所属チームのメンバーのみ閲覧可 |
| F09.6 ダイレクトメール | 配信対象フィルタに「大会ディビジョン」を追加。宛先をディビジョン単位で一括選択 |
| F04.1 タイムライン | フィード配信のスコープに `TOURNAMENT_DIVISION` を追加。ディビジョン限定の投稿が可能 |

> **DB変更**: 既存の各連携先テーブルの `target_type` ENUM に `TOURNAMENT_DIVISION` 値を追加するだけ。新テーブル不要。`tournament_participants` がチーム解決の源泉となる。`status = ACTIVE` のチームのみ配信対象（WITHDRAWN / DISQUALIFIED は除外）。

---

## 6. エラーコード

プレフィックス: `TOUR`。HTTPステータスとの紐付けは `GlobalExceptionHandler` で管理（§3.2）。

| コード | Severity | HTTPステータス（参考） | 説明 |
|--------|----------|----------------------|------|
| TOUR_001 | WARN | 404 | 大会が見つからない |
| TOUR_002 | WARN | 404 | ディビジョンが見つからない |
| TOUR_003 | WARN | 404 | 試合が見つからない |
| TOUR_004 | WARN | 409 | 大会ステータスが操作を許可しない（例: DRAFT状態で結果入力） |
| TOUR_005 | WARN | 409 | 対戦カード自動生成には2チーム以上必要 |
| TOUR_006 | WARN | 422 | 同一チームの重複登録 |
| TOUR_007 | WARN | 422 | スコア入力値が不正（負の値等） |
| TOUR_008 | WARN | 422 | セット制競技でセット数が `sets_to_win` を超過 |
| TOUR_009 | WARN | 409 | 全試合完了前に昇降格は実行不可 |
| TOUR_010 | WARN | 422 | 大会の `tournament_stat_defs` に存在しない `stat_key` |
| TOUR_011 | WARN | 409 | 既に昇降格が実行済み |
| TOUR_012 | WARN | 422 | ディビジョンの `max_participants` を超過 |
| TOUR_013 | WARN | 404 | テンプレートが見つからない |
| TOUR_014 | WARN | 404 | プリセットが見つからない |
| TOUR_015 | WARN | 409 | KNOCKOUT形式で引分けは不可（延長/PK戦で決着が必要） |
| TOUR_016 | WARN | 422 | `league_round_type` が大会形式と不整合 |
| TOUR_017 | WARN | 409 | 参加チームのステータスが `ACTIVE` でないため試合に登録不可 |
| TOUR_018 | WARN | 404 | 参加チームが見つからない |

---

## 7. 初期プリセット（SYSTEM_ADMIN投入）

| プリセット名 | 勝点(W/D/L) | 引分 | セット制 | タイブレーク順 |
|-------------|------------|------|---------|--------------|
| サッカー | 3/1/0 | あり | - | 勝点 → 得失点差 → 総得点 → 直接対決勝点 |
| フットサル | 3/1/0 | あり | - | 勝点 → 得失点差 → 総得点 → 直接対決勝点 |
| バスケットボール | 2/0/1 | なし | - | 勝数 → 直接対決 → 得失点差 |
| バレーボール | 3/0/0 (3-0,3-1) 2/1/0 (3-2) | なし | あり(3) | 勝点 → セット率 → 得点率 |
| 卓球（団体） | 2/0/0 | なし | - | 勝数 → 直接対決 → セット率 |
| バドミントン（団体） | 2/0/0 | なし | - | 勝数 → 直接対決 → ゲーム率 |
| 野球・ソフトボール | 2/1/0 | あり | - | 勝率 → 直接対決 → 得失点差 |
| ハンドボール | 2/1/0 | あり | - | 勝点 → 得失点差 → 総得点 → 直接対決 |
| ラグビー | 4/0/0 +BP | あり | - | 勝点 → 得失点差 → 総得点 → 直接対決 |
| 汎用（勝敗のみ） | 2/0/0 | なし | - | 勝数 → 直接対決 |

> **ラグビーのボーナスポイント（BP）・バレーボールの可変勝点**: `bonus_point_rules` JSON カラムで対応。ラグビー: `{"4_TRIES_BONUS": 1, "LOSS_WITHIN_7": 1}`、バレー: `{"3_SET_WIN": 3, "4_SET_WIN": 3, "5_SET_WIN": 2, "5_SET_LOSS": 1}`。Service層の順位計算でJSON条件を評価し、基本勝点に加算/上書きする。

---

## 8. 画面構成（参考）

| 画面 | 主要コンポーネント |
|------|------------------|
| 大会一覧 | 大会カード（名称・期間・ステータス・形式バッジ） |
| 大会詳細 | ディビジョンタブ切替、概要・順位表・対戦日程・ランキング |
| 順位表 | テーブル（順位・チーム名・試合数・勝敗・得失点・勝点）。昇格/降格ゾーン色分け |
| 対戦日程 | 節ごとのカード一覧。スコア表示。ADMIN はインライン編集 |
| トーナメントブラケット | ブラケット図（SVG/Canvas）。スコア付き。クリックで試合詳細 |
| 個人ランキング | タブ切替（得点王、アシスト王等）。テーブル（順位・選手名・所属・値） |
| スコア入力 | 試合詳細パネル。スコア入力 + セット別入力 + 個人成績入力 |
| 昇降格管理 | プレビュー（自動判定結果） + 手動調整 + 確定ボタン |
| 対戦マトリクス | 縦軸×横軸チーム名、交差点にスコア。色分けで勝敗を視覚化 |
| テンプレート管理 | プリセット一覧 + 複製ボタン + カスタム作成フォーム |
| シーズン継続作成 | 前シーズン選択 → 昇降格反映チーム配置提案 → 微調整 → 確定 |
| 埋め込みプレビュー | 埋め込みURL生成 + iframe コードコピー + プレビュー表示 |
| チーム通算成績 | 参加大会一覧・通算勝敗・最高順位等の横断集計 |

---

## 未解決事項

| No. | 内容 | ステータス |
|-----|------|-----------|
| ~~1~~ | ~~ラグビーのボーナスポイント等、特殊加算ルールへの対応方法~~ | **解決済み**（`bonus_point_rules` JSON カラムで対応） |
| 2 | 個人戦（将棋・囲碁等、チームではなく個人が参加するリーグ）への対応 | 将来検討（`participant_type` ENUM追加） |
| 3 | 対戦カードの自動スケジューリング（会場・日時の最適化） | スコープ外（手動設定で運用） |
| ~~4~~ | ~~IN_PROGRESS中の緊急エントリー修正の監査要否~~ | **解決済み**（MANAGE_TOURNAMENT権限者のみ可。`updated_at` で追跡） |

---

## 変更履歴

| 日付 | 内容 |
|------|------|
| 2026-03-21 | 初版作成。DB設計（19テーブル）、API設計（62エンドポイント）、ビジネスロジック、初期プリセット10種 |
| 2026-03-21 | 精査・修正（軽微）: エラーコードプレフィックス `TOUR` に統一、楽観的ロック追加（tournaments/templates/matches）、カラム名汎用化（`goals_*` → `score_*`）、FORFEIT結果追加、リーグH&A設定（`league_round_type`）追加、公開API追加、PUT→PATCH統一、ランキングTIME型対応、promotion_records ユニーク制約追加、エラーコード3件追加（TOUR_016〜018） |
| 2026-03-21 | 精査・修正（設計判断）: タイブレーク＋成績項目の大会レベルスナップショット（`tournament_tiebreakers`, `tournament_stat_defs` 2テーブル追加）、活動記録連携FK（`activity_results` にポリモーフィック `source_type`+`source_id` 追加）、PDF同期方針の明文化、スコア修正時の監査ログ＋COMPLETED修正制約、`bonus_point_rules` JSON追加（バレー可変勝点＋ラグビーBP対応、未解決事項#1を解決） |
| 2026-03-21 | 2次精査: 重複行削除、ステップ番号修正、テンプレート→大会への参照修正4箇所、CHECK制約NULL考慮、`uq_stp_name` NULL問題をService層バリデーションに変更、`MANAGE_TOURNAMENT`パーミッション登録注記、クロス組織アクセス防止の認可ルール明記、`tournament_participants.status`遷移図追加、`bonus_point_rules` JSONスキーマ正式定義、`tournament_standings.bonus_points`カラム追加、大会PATCHでタイブレーク・成績項目のネスト更新仕様を明記 |
| 2026-03-21 | 機能拡充（10件）: シーズン継続作成（`previous_tournament_id`）、一括スコア入力（batch/CSV import）、対戦マトリクス表示・PDF、WebSocketリアルタイム更新、埋め込みウィジェット、出場メンバー管理（`tournament_match_rosters` テーブル追加）、冪等性保証＋手動再計算API、Valkeyキャッシュ戦略明記、ARCHIVEDステータス追加、チーム通算成績API。ディビジョン別ターゲティング（F04.3/F05.1/F09.6/F04.1との横断連携）追加 |
| 2026-05-12 | Phase 9: 大会エントリー表機能追加。新規テーブル `tournament_entry_members`（UUIDv7）追加（計20テーブル）。`tournament_divisions` に `min_entry_count`/`max_entry_count` 追加。新規 API 6 本追加（計 68 エンドポイント）。エラーコード TOUR_019〜025 追加。フロントエンド型定義・i18n・テスト方針を策定。未解決事項 3 件を明記。|
| 2026-05-12 | Phase 9-B: エントリーテンプレート管理追加。新規テーブル `tournament_entry_templates`/`tournament_entry_template_members`（UUIDv7）追加（計22テーブル）。テンプレートCRUD 5本 + apply-template 1本追加（計74エンドポイント）。エラーコード TOUR_026〜028 追加。チーム単位で最大5件保存、複数大会に再利用可能。 |

---

## Phase 9: 大会エントリー表機能

### 概要・目的

大会参加前に「この大会に連れていく選手のリスト（エントリー表）」を登録・管理できる機能。
既存の `tournament_match_rosters`（試合ごとの出場記録）とは独立した **大会全体の事前選手登録** として設計する。

**チームメンバー一覧からワンクリックで一括ロード**するテンプレート機能により、
エントリー作業を 3〜5 操作で完了できる。
生成したエントリー表は PDF として出力でき、外部の大会主催者への提出にも使える。

#### 既存機能との違い

| 機能 | タイミング | 粒度 | 目的 |
|---|---|---|---|
| `tournament_entry_members`（本機能） | 大会参加前 | 大会全体 | 参加選手の事前登録・提出 |
| `tournament_match_rosters`（既存） | 試合ごと | 各試合 | 出場した選手の記録 |

両者は完全に独立。エントリー未登録でも試合ロスターへの登録は可能（既存後方互換を維持）。

---

### 新規テーブル: `tournament_entry_members`

#### DDL

```sql
CREATE TABLE tournament_entry_members (
    id             CHAR(36)          NOT NULL,               -- UUIDv7（新テーブル必須：CLAUDE.md 原則6）
    participant_id BIGINT UNSIGNED   NOT NULL,               -- FK → tournament_participants（同ドメイン内）
    user_id        BIGINT UNSIGNED   NOT NULL,               -- INDEX のみ（クロスドメイン FK 禁止：CLAUDE.md 原則1）
    jersey_number  SMALLINT UNSIGNED NULL,
    position       VARCHAR(30)       NULL,
    notes          VARCHAR(200)      NULL,
    sort_order     SMALLINT UNSIGNED NOT NULL DEFAULT 0,
    created_at     DATETIME          NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at     DATETIME          NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    PRIMARY KEY (id),
    UNIQUE INDEX uq_tem_participant_user (participant_id, user_id),
    INDEX idx_tem_participant (participant_id, sort_order),
    INDEX idx_tem_user (user_id),
    CONSTRAINT fk_tem_participant FOREIGN KEY (participant_id)
        REFERENCES tournament_participants (id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### アーキテクチャ原則チェック

| 原則 | 確認 |
|---|---|
| 原則1: クロスドメインFKなし | `user_id` は INDEX のみ。`participant_id` は tournament ドメイン内 FK。✅ |
| 原則2: CASCADE は同一ドメイン内のみ | `fk_tem_participant` は tournament_participants → tournament_entry_members（同ドメイン）。✅ |
| 原則6: 新規テーブルは UUIDv7 | `id CHAR(36)` + `UuidV7Entity` 継承。✅ |
| 原則7: AbstractTenantAwareRepository | `tournament_entry_members` は `organization_id` カラムを持たない（participant 経由で org に紐づく）。標準 `JpaRepository` で OK。✅ |

#### Flyway マイグレーション

- `tournament_divisions` への `min_entry_count` / `max_entry_count` 追加（ALTER TABLE）
- `tournament_entry_members` の CREATE TABLE

> **注意**: Flyway バージョン番号は実装時に `git log` で最新番号を確認して採番すること（設計時の最大は V65.001）。

---

### 既存テーブル変更: `tournament_divisions`

#### 追加カラム

| カラム | 型 | NULL | デフォルト | 説明 |
|---|---|---|---|---|
| `min_entry_count` | SMALLINT UNSIGNED | YES | NULL | エントリー最少人数（NULL = 制限なし） |
| `max_entry_count` | SMALLINT UNSIGNED | YES | NULL | エントリー最大人数（NULL = 制限なし） |

`max_participants`（チーム数上限）の直後に配置する。

**既存データへの影響**: 両カラムとも NULL デフォルトのため既存レコードへの影響ゼロ。

---

### 新規 API エンドポイント（6本）

全エンドポイントのプレフィックス: `/api/v1/organizations/{orgId}/tournaments/{tId}`

#### エンドポイント一覧

| # | メソッド | パス（プレフィックス省略）| 目的 |
|---|---|---|---|
| 1 | GET | `/divisions/{divId}/participants/{pId}/entry-members` | エントリー一覧＋チームメンバー候補取得 |
| 2 | POST | `/divisions/{divId}/participants/{pId}/entry-members/load-from-team` | チームメンバーから一括ロード（テンプレート）|
| 3 | PUT | `/divisions/{divId}/participants/{pId}/entry-members` | エントリー全置換（確定保存）|
| 4 | DELETE | `/divisions/{divId}/participants/{pId}/entry-members/{entryMemberId}` | 個別削除 |
| 5 | GET | `/divisions/{divId}/participants/{pId}/entry-members/pdf` | エントリー表 PDF 出力 |
| 6 | GET | `/divisions/{divId}/entry-summary` | 全チームエントリー状況サマリー（主催者用）|

#### #1: GET エントリー一覧

**クエリパラメータ**: `includeTeamMembers=true/false`（デフォルト `false`）

**レスポンス (200 OK)**:
```json
{
  "data": {
    "entryMembers": [
      {
        "id": "018f1234-...",
        "participantId": 123,
        "userId": 456,
        "displayName": "田中 太郎",
        "jerseyNumber": 9,
        "position": "FW",
        "notes": null,
        "sortOrder": 0,
        "createdAt": "2026-05-12T10:00:00",
        "updatedAt": "2026-05-12T10:00:00"
      }
    ],
    "teamMemberCandidates": [
      {
        "userId": 789,
        "displayName": "山田 花子",
        "memberNumber": "7",
        "position": "MF",
        "isAlreadyEntered": false
      }
    ],
    "entryCount": 1,
    "minEntryCount": null,
    "maxEntryCount": 20
  }
}
```

`teamMemberCandidates` は `includeTeamMembers=true` の場合のみ返却。

**IDOR 検証チェーン** (Service 層で必ず実施):
```
orgId → tId の帰属確認
  → divId → tId の帰属確認
    → pId → divId の帰属確認
```

**権限**: 自チームの ADMIN/MEMBER または MANAGE_TOURNAMENT 権限者のみ。
他チームのエントリー一覧へのアクセスは **403**（userId 等の個人情報保護のため 404 ではなく 403）。

---

#### #2: POST load-from-team（テンプレート機能の核心）

**リクエストボディ**:
```json
{
  "userIds": null,
  "overwriteExisting": false
}
```

- `userIds: null` → チームの全アクティブメンバーを対象
- `userIds: [101, 102]` → 指定ユーザーのみを対象
- `overwriteExisting: false` → 既存エントリーはスキップ（冪等）
- `overwriteExisting: true` → 既存の `jersey_number`/`position`/`notes` を上書き

**レスポンス (200 OK)**:
```json
{
  "data": {
    "added": 5,
    "skipped": 2,
    "total": 7,
    "entryMembers": [ ... ]
  }
}
```

**実装依存**: アクティブメンバー取得には `MemberQueryDispatcher.queryMembers(teamId, ScopeType.TEAM, null)` を使用する。

**クロスドメイン境界の取り扱い**:
```java
// @Transactional コメント必須
// TODO: tournamentドメインとmemberドメインをまたいでいる。将来はTeamMemberLoadRequestedEventで分離予定
@Transactional
public EntryLoadResponse loadFromTeamMembers(Long pId, LoadFromTeamRequest req) {
    // 1. memberドメインへの読み取りは @Transactional 境界の外で実施（推奨）
    // 2. INSERT のみを @Transactional 内に閉じる
}
```

**冪等性保証**: `uq_tem_participant_user (participant_id, user_id)` のユニーク制約を利用。既存 userId セットを1クエリで取得し、差分のみ INSERT する。

---

#### #3: PUT エントリー全置換

**リクエストボディ**:
```json
{
  "members": [
    {
      "userId": 456,
      "jerseyNumber": 9,
      "position": "FW",
      "notes": null,
      "sortOrder": 0
    }
  ]
}
```

**ビジネスロジック**: 同一 `@Transactional` 内で `deleteAllByParticipantId(pId)` → `saveAll(entries)`。
バリデーションは置換後の件数に対して実施（`validateEntryCount` を参照）。

---

#### #4: DELETE 個別削除

**レスポンス**: 204 No Content

**エラー**: `entryMemberId` が `pId` に属さない場合は TOUR_022（IDOR 防止も兼ねる）。

`min_entry_count` を下回る削除は TOUR_023 を返す（強制フラグ `?force=true` で突破可能）。

---

#### #5: GET PDF 出力

**レスポンス**: `application/pdf`

**ファイル名**: `エントリー表_{チーム名}_{大会名}_{YYYYMMDD}.pdf`

**PDF レイアウト**:
- ヘッダー: 大会名 / ディビジョン名 / チーム名 / 出力日
- テーブル: `sort_order` 順に「No. / 氏名 / 背番号 / ポジション / 備考」
- フッター: 合計人数

**実装**: 既存の `PdfGeneratorService.generateFromTemplate("pdf/tournament-entry-members", variables)` パターンを踏襲する。

---

#### #6: GET 全チームエントリーサマリー（主催者用）

**レスポンス (200 OK)**:
```json
{
  "data": {
    "divisionId": 10,
    "divisionName": "1部",
    "minEntryCount": 15,
    "maxEntryCount": 20,
    "summary": [
      {
        "participantId": 123,
        "teamId": 45,
        "displayName": "チームA",
        "entryCount": 18,
        "isMinMet": true,
        "isMaxExceeded": false,
        "lastUpdatedAt": "2026-05-10T15:30:00"
      },
      {
        "participantId": 124,
        "teamId": 46,
        "displayName": "チームB",
        "entryCount": 0,
        "isMinMet": false,
        "isMaxExceeded": false,
        "lastUpdatedAt": null
      }
    ]
  }
}
```

**N+1 対策**: 参加チームリスト取得後、`GROUP BY participant_id` の集計クエリ1本でエントリー件数を一括取得する。個別チームの `userId` は含めない（個人情報保護）。

```java
@Query("SELECT e.participantId as participantId, COUNT(e) as entryCount, MAX(e.updatedAt) as lastUpdatedAt " +
       "FROM TournamentEntryMemberEntity e WHERE e.participantId IN :participantIds " +
       "GROUP BY e.participantId")
List<EntryCountProjection> countByParticipantIds(@Param("participantIds") List<Long> participantIds);
```

**権限**: MANAGE_TOURNAMENT 権限者のみ（一般チームメンバーは自チーム分のみ閲覧可）。

---

### ビジネスロジック

#### 編集ロック条件

| 大会ステータス | チームADMIN による編集 | MANAGE_TOURNAMENT による編集 |
|---|---|---|
| DRAFT | 可 | 可 |
| OPEN | 可 | 可 |
| IN_PROGRESS | **ロック** → TOUR_019 | 緊急修正可（`updated_at` で追跡） |
| COMPLETED | ロック → TOUR_019 | ロック → TOUR_019 |
| CANCELLED | ロック → TOUR_019 | ロック → TOUR_019 |
| ARCHIVED | ロック → TOUR_019 | ロック → TOUR_019 |

#### 人数バリデーション

```java
private void validateEntryCount(int count, Integer min, Integer max) {
    if (min != null && count < min) {
        throw new BusinessException(TournamentErrorCode.MIN_ENTRY_COUNT_VIOLATION);
    }
    if (max != null && count > max) {
        throw new BusinessException(TournamentErrorCode.MAX_ENTRY_COUNT_EXCEEDED);
    }
}
```

- `PUT`（全置換）: 置換後の件数でバリデーション
- `DELETE`（個別削除）: 削除後の件数が `min_entry_count` を下回る場合は TOUR_023（`?force=true` で突破可）
- `POST`（ロード）: ロード後の件数が `max_entry_count` を超える場合は TOUR_020

#### `tournament_match_rosters` との関係

**完全に独立**。`tournament_entry_members` はエントリー表（事前）、`tournament_match_rosters` は試合記録（事後）であり、相互のバリデーション連鎖は行わない。
「エントリー未登録の選手でも試合ロスターに登録可能」という既存ルールを維持する（後方互換）。

> 将来オプション: Phase 10 以降で「エントリー登録済み選手のみ試合ロスターに登録可」バリデーションの追加を検討可能。

---

### 権限設計

#### アクセスマトリクス

| 操作 | 大会ADMIN（MANAGE_TOURNAMENT）| 自チームADMIN | 自チームMEMBER | 他チームメンバー | 未認証 |
|---|---|---|---|---|---|
| GET 一覧（自チーム・候補含む）| ✅ | ✅ | ✅ | ❌ 403 | ❌ 401 |
| GET 一覧（他チーム）| ✅ | ❌ 403 | ❌ 403 | ❌ 403 | ❌ 401 |
| POST テンプレートロード | ✅ | ✅ | ❌ 403 | ❌ 403 | ❌ 401 |
| PUT 全置換 | ✅ | ✅ | ❌ 403 | ❌ 403 | ❌ 401 |
| DELETE 個別削除 | ✅ | ✅ | ❌ 403 | ❌ 403 | ❌ 401 |
| GET PDF（自チーム）| ✅ | ✅ | ✅ | ❌ 403 | ❌ 401 |
| GET PDF（他チーム）| ✅ | ❌ 403 | ❌ 403 | ❌ 403 | ❌ 401 |
| GET サマリー | ✅ | ❌ 403 | ❌ 403 | ❌ 403 | ❌ 401 |

#### 個人情報の可視性設計

- `userId` はレスポンスに含めるが、**自チームのメンバーまたは大会 ADMIN 以外には返さない**。
- エントリーサマリー（#6）では `userId` を含む個別メンバー情報を**返さない**（件数のみ）。

---

### エラーコード

新規追加分（`TOUR_018` の次から連番）:

| コード | 定数名 | HTTP Status | 説明 |
|---|---|---|---|
| `TOUR_019` | `ENTRY_LOCKED` | 409 | 大会ステータスによりエントリー変更がロック |
| `TOUR_020` | `MAX_ENTRY_COUNT_EXCEEDED` | 422 | `max_entry_count` を超過 |
| `TOUR_021` | `USER_NOT_TEAM_MEMBER` | 422 | 指定ユーザーがチームのメンバーではない |
| `TOUR_022` | `ENTRY_MEMBER_NOT_FOUND` | 404 | エントリーメンバーが見つからない |
| `TOUR_023` | `MIN_ENTRY_COUNT_VIOLATION` | 422 | `min_entry_count` を下回る |
| `TOUR_024` | `ENTRY_MEMBER_DUPLICATE` | 409 | 同一選手が既にエントリー済み |
| `TOUR_025` | `ENTRY_LOAD_NO_MEMBERS` | 422 | ロード対象のチームメンバーが存在しない |

---

### バックエンド実装方針

#### パッケージ構成（`tournament.entry` サブパッケージに分離）

```
com.mannschaft.app.tournament.entry/
  ├── TournamentEntryMemberEntity.java     # UuidV7Entity 継承
  ├── TournamentEntryMemberRepository.java # JpaRepository<TournamentEntryMemberEntity, UUID>
  ├── TournamentEntryMemberService.java
  ├── TournamentEntryMemberController.java
  └── dto/
      ├── EntryMemberResponse.java
      ├── EntryMemberListResponse.java
      ├── TeamMemberCandidateResponse.java
      ├── LoadFromTeamRequest.java
      ├── EntryLoadResponse.java
      ├── UpsertEntryMembersRequest.java
      └── EntryMemberSummaryResponse.java
```

#### `TournamentEntryMemberEntity`

```java
@Entity
@Table(name = "tournament_entry_members")
public class TournamentEntryMemberEntity extends UuidV7Entity {
    @Column(nullable = false)
    private Long participantId;

    @Column(nullable = false)
    private Long userId;               // クロスドメインのため FK なし

    private Integer jerseyNumber;
    private String position;
    private String notes;

    @Column(nullable = false)
    private Short sortOrder = 0;

    @CreationTimestamp
    private LocalDateTime createdAt;

    @UpdateTimestamp
    private LocalDateTime updatedAt;
}
```

#### PDF テンプレート

`resources/templates/pdf/tournament-entry-members.html`（または `.jte` 等）を追加する。
既存の `tournament-standings.html` を参考に作成すること。

---

### フロントエンド実装方針

#### 変更ファイル

| ファイル | 変更内容 |
|---|---|
| `frontend/app/types/tournament.ts` | `TournamentEntryMember` 等の型を追加 |
| `frontend/app/composables/useTournamentApi.ts` | entry-members の API 関数 6 本を追加 |
| `frontend/app/pages/organizations/[id]/tournaments.vue` | 参加チーム一覧に「エントリー」バッジ追加 |
| `frontend/app/components/TournamentEntryModal.vue` | 新規コンポーネント（エントリー管理モーダル）|
| `frontend/app/locales/{ja,en,zh,ko,es,de}/tournament.json` | `tournament.entry.*` キーを6言語すべてに追加 |

#### 型定義（`frontend/app/types/tournament.ts` への追加分）

```typescript
export interface TournamentEntryMember {
  id: string  // UUID v7
  participantId: number
  userId: number
  displayName: string
  memberNumber: string | null
  position: string | null
  jerseyNumber: number | null
  notes: string | null
  sortOrder: number
  createdAt: string
  updatedAt: string
}

export interface TeamMemberCandidate {
  userId: number
  displayName: string
  memberNumber: string | null
  position: string | null
  isAlreadyEntered: boolean
}

export interface EntryMemberListResponse {
  entryMembers: TournamentEntryMember[]
  teamMemberCandidates?: TeamMemberCandidate[]
  entryCount: number
  minEntryCount: number | null
  maxEntryCount: number | null
}

export interface EntryLoadResponse {
  added: number
  skipped: number
  total: number
  entryMembers: TournamentEntryMember[]
}

export interface EntryMemberSummaryItem {
  participantId: number
  teamId: number
  displayName: string
  entryCount: number
  isMinMet: boolean
  isMaxExceeded: boolean
  lastUpdatedAt: string | null
}

export interface EntryMemberSummary {
  divisionId: number
  divisionName: string
  minEntryCount: number | null
  maxEntryCount: number | null
  summary: EntryMemberSummaryItem[]
}
```

#### UXフロー（テンプレートロード）

```
① 大会詳細ページ → 参加チーム一覧 → 自チームの「エントリー管理」ボタンをクリック
② エントリー管理モーダルが開く（現在のエントリー一覧を表示）
③ 「チームメンバーから一括読み込み」ボタン → POST load-from-team → チーム全員が追加される
④ 不参加メンバーのチェックを外す（少数の操作で完了）
⑤ 各メンバーの背番号・ポジションを入力（任意）
⑥ 「保存」ボタン → PUT 全置換で確定
⑦ 「PDFで出力」ボタン → エントリー表 PDF をダウンロード
```

#### i18n キー（6言語すべてに追加）

```json
{
  "tournament": {
    "entry": {
      "title": "エントリー表",
      "manage": "エントリー管理",
      "loadFromTeam": "チームメンバーから一括読み込み",
      "loadConfirm": "チームメンバー全員を読み込みます。既存のエントリーはスキップされます。",
      "memberCount": "{count}名",
      "noEntries": "エントリー登録なし",
      "status": {
        "complete": "提出済み",
        "partial": "一部登録",
        "empty": "未登録"
      },
      "field": {
        "jerseyNumber": "背番号",
        "position": "ポジション",
        "notes": "備考",
        "sortOrder": "順番"
      },
      "validation": {
        "minCount": "最低{min}名以上のエントリーが必要です",
        "maxCount": "最大{max}名までエントリーできます",
        "duplicate": "同一選手を重複してエントリーすることはできません"
      },
      "pdf": {
        "title": "大会エントリー表",
        "downloadButton": "エントリー表PDF"
      },
      "summary": {
        "title": "エントリー状況サマリー",
        "teamCount": "参加チーム数",
        "submittedCount": "提出済み"
      },
      "lock": {
        "message": "大会が{status}のためエントリーの変更はできません"
      },
      "error": {
        "TOUR_019": "大会のステータスではエントリーを変更できません",
        "TOUR_020": "エントリーの最大人数を超えています（最大{max}名）",
        "TOUR_021": "チームのメンバーではないユーザーが含まれています",
        "TOUR_022": "エントリーメンバーが見つかりません",
        "TOUR_023": "最小{min}名以上のエントリーが必要です",
        "TOUR_024": "同一選手が既にエントリーされています",
        "TOUR_025": "チームメンバーが登録されていません。メンバープロフィールを先に登録してください"
      }
    }
  }
}
```

---

### テスト方針

#### 単体テスト: `TournamentEntryMemberServiceTest.java`

配置: `backend/src/test/java/com/mannschaft/app/tournament/entry/TournamentEntryMemberServiceTest.java`

**正常系（7パターン）**:
1. `エントリーメンバー一覧を取得できる`
2. `includeTeamMembers=true でチームメンバー候補も返る`
3. `チームメンバーから全員ロードできる`
4. `冪等性: 既存2名の状態で5名ロード → added=3, skipped=2`
5. `overwriteExisting=true で既存の jersey_number を上書きできる`
6. `PUT 全置換でエントリーが入れ替わる`
7. `min/max が NULL の場合は人数制限なし`

**異常系（9パターン）**:
1. COMPLETED 状態での変更 → `TOUR_019`
2. `max_entry_count` 超過 → `TOUR_020`
3. チーム非所属ユーザーのロード → `TOUR_021`
4. 存在しない `entryMemberId` の削除 → `TOUR_022`
5. `min_entry_count` 未満での削除 → `TOUR_023`
6. PUT リクエスト内での `userId` 重複 → `TOUR_024`
7. チームメンバー 0 名でのロード → `TOUR_025`
8. 他チームの `participantId` 経由アクセス → `TOUR_018`（IDOR 防止）
9. `divId` と `pId` の不整合 → `TOUR_018`

**境界値（3パターン）**:
1. `max_entry_count=0` → 常に `TOUR_020`
2. `min=max=20` → ちょうど 20 名で OK
3. IN_PROGRESS + MANAGE_TOURNAMENT 権限 → 書き込み可

#### 結合テスト: `TournamentEntryMemberControllerIntegrationTest.java`

配置: `backend/src/test/java/com/mannschaft/app/tournament/entry/TournamentEntryMemberControllerIntegrationTest.java`

各エンドポイントに最低 1 本（計 8 本）。`AbstractIntegrationTest` を継承する。

1. `GET /entry-members 200 候補付きで返る`
2. `POST /load-from-team 200 一括ロード成功`
3. `PUT 200 全置換成功`
4. `DELETE 204 個別削除成功`
5. `GET /pdf 200 PDF バイトが返る`
6. `GET /entry-summary 200 主催者サマリー返る`
7. `GET /entry-members 403 他チームユーザーはアクセス不可`
8. `POST /load-from-team 409 COMPLETED 大会はロック`

---

### 未解決事項

| No. | 内容 | 対応方針 |
|---|---|---|
| 1 | エントリー締切日時の自動ロック | 将来検討。`tournament_divisions.entry_deadline DATETIME NULL` の追加で対応可能。Phase 9 では手動運用（主催者が大会ステータスを変更してロック） |
| 2 | エントリー変更の監査ログ | 将来検討。F10.3 audit_logs との連携。Phase 9 では `updated_at` で代替 |
| 3 | エントリー提出時の主催者への自動通知 | 将来検討。F04.3 プッシュ通知との連携（「チームBがエントリーを提出しました」）|
| ~~4~~ | ~~テンプレート機能の設計~~ | **Phase 9-B として設計完了（本ドキュメント内）** |

---

## Phase 9-B: エントリーテンプレート管理

### 概要・目的

チームが最大5件のエントリーテンプレート（選手リスト＋背番号＋ポジション）を保存し、
大会エントリー表に1クリックで一括適用できる機能。

**テンプレートはチーム単位で保持し複数の大会に再利用できる。**

```
チームA のテンプレート（最大5件）
  1. 定番20名リスト
  2. フットサル用18名
  3. 遠征軽量版15名
  4. (空き)
  5. (空き)

→ 大会X にも大会Y にも同じテンプレートを適用可能
```

**適用後の操作**: テンプレートに含まれない選手（テンプレート外のチームメンバー）を
`GET /entry-members?includeTeamMembers=true` の候補リストから手動追加できる。

---

### 新規テーブル: `tournament_entry_templates`

#### DDL

```sql
CREATE TABLE tournament_entry_templates (
    id          CHAR(36)         NOT NULL,            -- UUIDv7（CLAUDE.md 原則6）
    team_id     BIGINT UNSIGNED  NOT NULL,            -- チーム所有（クロスドメイン FK 禁止：原則1）
    name        VARCHAR(50)      NOT NULL,
    description VARCHAR(200)     NULL,
    sort_order  TINYINT UNSIGNED NOT NULL DEFAULT 0,  -- チーム内の表示順（0〜4）
    created_by  BIGINT UNSIGNED  NOT NULL,            -- 作成者 userId（FK 禁止）
    created_at  DATETIME         NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at  DATETIME         NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    deleted_at  DATETIME         NULL,                -- 論理削除
    PRIMARY KEY (id),
    INDEX idx_tet_team (team_id, deleted_at, sort_order)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

**制約**: `team_id` ごとに `deleted_at IS NULL` のレコードが最大5件。Service 層で強制。
**配置根拠**: テンプレートはトーナメントエントリー専用のため、tournament ドメインに配置する。
`team_id` はクロスドメイン参照のため FK 制約なし・INDEX のみ（CLAUDE.md 原則1）。

---

### 新規テーブル: `tournament_entry_template_members`

#### DDL

```sql
CREATE TABLE tournament_entry_template_members (
    id            CHAR(36)          NOT NULL,  -- UUIDv7
    template_id   CHAR(36)          NOT NULL,  -- FK → tournament_entry_templates（同ドメイン）
    user_id       BIGINT UNSIGNED   NOT NULL,  -- INDEX のみ（クロスドメイン FK 禁止）
    jersey_number SMALLINT UNSIGNED NULL,
    position      VARCHAR(30)       NULL,
    sort_order    SMALLINT UNSIGNED NOT NULL DEFAULT 0,
    PRIMARY KEY (id),
    UNIQUE INDEX uq_tetm_template_user (template_id, user_id),
    INDEX idx_tetm_template (template_id, sort_order),
    INDEX idx_tetm_user (user_id),
    CONSTRAINT fk_tetm_template FOREIGN KEY (template_id)
        REFERENCES tournament_entry_templates (id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

---

### テーブル設計のアーキテクチャ原則チェック

| 原則 | tournament_entry_templates | tournament_entry_template_members |
|---|---|---|
| 原則1: クロスドメインFKなし | `team_id`, `created_by` は INDEX のみ ✅ | `user_id` は INDEX のみ ✅ |
| 原則2: CASCADE は同ドメイン内のみ | なし ✅ | `fk_tetm_template` は同ドメイン内 CASCADE ✅ |
| 原則3: コアエンティティは論理削除 | `deleted_at` あり ✅ | 物理削除（親 CASCADE で連鎖）✅ |
| 原則6: 新規テーブルは UUIDv7 | `id CHAR(36)` ✅ | `id CHAR(36)` ✅ |
| 原則7: AbstractTenantAwareRepository | `organization_id` カラムなし → 非継承・JpaRepository で OK ✅ | 同左 ✅ |

---

### 新規 API エンドポイント（6本）

#### テンプレート CRUD（5本）— チームコンテキスト

全プレフィックス: `/api/v1/organizations/{orgId}/teams/{teamId}/entry-templates`

| # | メソッド | パス | 目的 |
|---|---|---|---|
| 1 | GET | `/` | テンプレート一覧（最大5件）|
| 2 | POST | `/` | テンプレート作成（5件超は TOUR_027）|
| 3 | GET | `/{templateId}` | テンプレート詳細＋選手一覧 |
| 4 | PUT | `/{templateId}` | テンプレート更新（名前・選手・背番号を全置換）|
| 5 | DELETE | `/{templateId}` | テンプレート論理削除 |

**IDOR 検証チェーン**: `orgId → teamId の帰属` を Service 層で確認。
他チームのテンプレートへのアクセスは 403。

##### GET テンプレート一覧レスポンス (200 OK)

```json
{
  "data": [
    {
      "id": "018f1234-...",
      "name": "定番20名リスト",
      "description": "通常試合用",
      "sortOrder": 0,
      "memberCount": 20,
      "updatedAt": "2026-05-12T10:00:00"
    }
  ]
}
```

##### GET テンプレート詳細レスポンス (200 OK)

```json
{
  "data": {
    "id": "018f1234-...",
    "name": "定番20名リスト",
    "description": "通常試合用",
    "sortOrder": 0,
    "members": [
      {
        "id": "018f5678-...",
        "userId": 456,
        "displayName": "田中 太郎",
        "jerseyNumber": 9,
        "position": "FW",
        "sortOrder": 0
      }
    ]
  }
}
```

##### POST/PUT リクエストボディ

```json
{
  "name": "定番20名リスト",
  "description": "通常試合用",
  "sortOrder": 0,
  "members": [
    { "userId": 456, "jerseyNumber": 9, "position": "FW", "sortOrder": 0 }
  ]
}
```

PUT は `members` を全置換する（削除→再 INSERT ではなく差分更新を推奨）。

---

#### テンプレート適用（1本）— トーナメントコンテキスト

`POST /api/v1/organizations/{orgId}/tournaments/{tId}/divisions/{divId}/participants/{pId}/entry-members/apply-template`

**目的**: 保存済みテンプレートを大会エントリー表に一括適用する。

**リクエストボディ**:
```json
{
  "templateId": "018f1234-...",
  "overwriteExisting": false
}
```

- `overwriteExisting: false`（デフォルト）: 既存エントリーをスキップ（冪等）
- `overwriteExisting: true`: 既存エントリーの `jerseyNumber`/`position` を上書き

**レスポンス (200 OK)**:
```json
{
  "data": {
    "applied": 18,
    "skipped": 2,
    "skippedInactive": 1,
    "total": 21,
    "entryMembers": [ ... ]
  }
}
```

- `skippedInactive`: テンプレート内のユーザーがチームの非アクティブメンバーになっていた数（静かにスキップ・ログ記録）

**セキュリティ検証チェーン**:
```
orgId → tId → divId → pId の帰属確認
  → participant.teamId が template.team_id と一致することを確認（不一致 → TOUR_028）
  → 大会ステータスロック確認（ロック中 → TOUR_019）
```

---

### ビジネスロジック

#### テンプレート件数制限

```java
// team_id ごとに最大5件（deleted_at IS NULL のもの）
long count = templateRepository.countByTeamIdAndDeletedAtIsNull(teamId);
if (count >= 5) {
    throw new BusinessException(TournamentErrorCode.MAX_TEMPLATE_COUNT_EXCEEDED);
}
```

#### テンプレート適用時の非アクティブメンバー処理

テンプレート保存後にチームメンバーが退会・無効化された場合は静かにスキップする。

```java
// TODO: tournamentドメインとmemberドメインをまたいでいる。将来はTeamMemberLoadRequestedEventで分離予定
Set<Long> activeTeamMemberIds = memberQueryDispatcher
    .queryMembers(teamId, ScopeType.TEAM, null)
    .stream().map(m -> m.getUserId()).collect(Collectors.toSet());

int skippedInactive = 0;
for (EntryTemplateMemberEntity m : templateMembers) {
    if (!activeTeamMemberIds.contains(m.getUserId())) {
        skippedInactive++;
        log.info("テンプレート適用スキップ: userId={} はチームの非アクティブメンバー", m.getUserId());
        continue;
    }
    // エントリーに追加（uq_tem_participant_user で冪等保証）
}
```

#### テンプレート論理削除後の扱い

テンプレートを論理削除しても `tournament_entry_members` に影響しない（独立）。
物理削除は行わず、`deleted_at` で論理削除する。

#### `load-from-team` との使い分け

| 操作 | 用途 |
|---|---|
| `POST load-from-team` | チームの**全**アクティブメンバーを素早くロード（背番号・ポジション未設定）|
| `POST apply-template` | **事前設定済み**テンプレート（背番号・ポジション付き）を適用。定番メンバーは背番号まで自動入力 |

---

### 権限設計

#### テンプレート CRUD

| 操作 | チームADMIN | チームMEMBER | 他チームメンバー | 未認証 |
|---|---|---|---|---|
| テンプレート一覧・詳細 | ✅ | ✅ | ❌ 403 | ❌ 401 |
| テンプレート作成・更新・削除 | ✅ | ❌ 403 | ❌ 403 | ❌ 401 |
| apply-template | ✅ | ❌ 403 | ❌ 403 | ❌ 401 |

#### apply-template の追加チェック

- 大会 ADMIN（MANAGE_TOURNAMENT）も apply-template を実行可（代理操作）
- `templateId` がリクエストチームのものでなければ TOUR_028（他チームのテンプレートを適用しようとした場合）

---

### エラーコード（新規 TOUR_026〜028）

| コード | 定数名 | HTTP | 説明 |
|---|---|---|---|
| `TOUR_026` | `ENTRY_TEMPLATE_NOT_FOUND` | 404 | テンプレートが見つからない（論理削除済み含む）|
| `TOUR_027` | `MAX_TEMPLATE_COUNT_EXCEEDED` | 422 | テンプレートは team_id ごとに最大5件まで |
| `TOUR_028` | `TEMPLATE_TEAM_MISMATCH` | 403 | 適用テンプレートが参加チームのものではない |

---

### バックエンド実装方針

#### パッケージ構成（`tournament.entry` サブパッケージに追加）

```
com.mannschaft.app.tournament.entry/
  ├── TournamentEntryMemberEntity.java       （Phase 9 で定義済み）
  ├── TournamentEntryMemberRepository.java   （Phase 9 で定義済み）
  ├── TournamentEntryMemberService.java      （Phase 9 で定義済み）
  ├── TournamentEntryMemberController.java   （Phase 9 で定義済み）
  ├── TournamentEntryTemplateEntity.java     ★ 新規（UuidV7Entity 継承）
  ├── TournamentEntryTemplateRepository.java ★ 新規（JpaRepository<..., UUID>）
  ├── TournamentEntryTemplateMemberEntity.java ★ 新規（UuidV7Entity 継承）
  ├── TournamentEntryTemplateMemberRepository.java ★ 新規
  ├── TournamentEntryTemplateService.java    ★ 新規
  ├── TournamentEntryTemplateController.java ★ 新規（チームコンテキスト + apply）
  └── dto/
      ├── EntryTemplateResponse.java         ★ 新規（一覧用）
      ├── EntryTemplateDetailResponse.java   ★ 新規（詳細用）
      ├── EntryTemplateMemberResponse.java   ★ 新規
      ├── CreateEntryTemplateRequest.java    ★ 新規
      ├── UpdateEntryTemplateRequest.java    ★ 新規
      └── ApplyTemplateRequest.java          ★ 新規
```

#### `TournamentEntryTemplateEntity` スケルトン

```java
@Entity
@Table(name = "tournament_entry_templates")
public class TournamentEntryTemplateEntity extends UuidV7Entity {
    @Column(nullable = false)
    private Long teamId;          // クロスドメインのため FK なし

    @Column(nullable = false, length = 50)
    private String name;

    @Column(length = 200)
    private String description;

    @Column(nullable = false)
    private Byte sortOrder = 0;

    @Column(nullable = false)
    private Long createdBy;       // FK なし

    @CreationTimestamp
    private LocalDateTime createdAt;

    @UpdateTimestamp
    private LocalDateTime updatedAt;

    private LocalDateTime deletedAt;
}
```

---

### フロントエンド実装方針

#### 変更ファイル

| ファイル | 変更内容 |
|---|---|
| `frontend/app/types/tournament.ts` | `EntryTemplate`, `EntryTemplateDetail`, `EntryTemplateMember`, `ApplyTemplateResponse` 追加 |
| `frontend/app/composables/useTournamentApi.ts` | テンプレート CRUD + apply 計6関数追加 |
| `frontend/app/components/TournamentEntryModal.vue` | テンプレート選択ドロップダウン UI を追加 |
| `frontend/app/pages/teams/[id]/settings.vue` | テンプレート管理セクション追加（またはサブページ）|
| `frontend/app/locales/{ja,en,zh,ko,es,de}/tournament.json` | `tournament.entry.template.*` キーを6言語に追加 |

#### 型定義（`frontend/app/types/tournament.ts` への追加分）

```typescript
export interface EntryTemplate {
  id: string  // UUID v7
  name: string
  description: string | null
  sortOrder: number
  memberCount: number
  updatedAt: string
}

export interface EntryTemplateMember {
  id: string
  userId: number
  displayName: string
  jerseyNumber: number | null
  position: string | null
  sortOrder: number
}

export interface EntryTemplateDetail extends EntryTemplate {
  members: EntryTemplateMember[]
}

export interface ApplyTemplateResponse {
  applied: number
  skipped: number
  skippedInactive: number
  total: number
  entryMembers: TournamentEntryMember[]
}
```

#### UXフロー（テンプレート適用）

```
[エントリー管理モーダルを開く]
① テンプレート選択ドロップダウン（最大5件を表示）
② テンプレートを選択して「一括適用」ボタンをクリック
   → POST apply-template → テンプレート選手が追加される
③ 「チームメンバー候補」リストでテンプレート外の選手を確認
④ 追加したい選手にチェック → PUT 全置換で保存

[チーム設定 → エントリーテンプレート]
⑤ テンプレート一覧を確認・編集・新規作成（最大5件）
   → チームメンバーから選択 + 背番号・ポジション設定
```

#### i18n キー（6言語すべてに追加）

`tournament.entry.template` 名前空間を使用（Phase 9 の `tournament.entry` に追加）：

```json
{
  "tournament": {
    "entry": {
      "template": {
        "title": "エントリーテンプレート",
        "select": "テンプレートを選択",
        "apply": "一括適用",
        "applyConfirm": "テンプレート「{name}」を適用します",
        "create": "テンプレートを作成",
        "edit": "テンプレートを編集",
        "delete": "テンプレートを削除",
        "deleteConfirm": "テンプレート「{name}」を削除しますか？",
        "maxCount": "最大5件まで作成できます（現在{count}件）",
        "noTemplates": "テンプレートがありません",
        "applyResult": "{applied}名を追加しました（スキップ: {skipped}名）",
        "skippedInactive": "{count}名はチームの非アクティブメンバーのためスキップしました",
        "loadDiff": "テンプレート適用後、テンプレート外のメンバーも手動で追加できます",
        "error": {
          "TOUR_026": "テンプレートが見つかりません",
          "TOUR_027": "テンプレートは最大5件まで作成できます",
          "TOUR_028": "このテンプレートは別のチームのものです"
        }
      }
    }
  }
}
```

---

### テスト方針

#### 単体テスト（`TournamentEntryTemplateServiceTest.java`）

配置: `backend/src/test/java/com/mannschaft/app/tournament/entry/TournamentEntryTemplateServiceTest.java`

**正常系（7パターン）**:
1. `テンプレート一覧を取得できる（5件以下）`
2. `テンプレート詳細を選手一覧付きで取得できる`
3. `テンプレートを作成できる（4件目）`
4. `テンプレートを更新できる（members 全置換）`
5. `テンプレートを論理削除できる`
6. `apply-template で正常適用できる（applied=18, skipped=2）`
7. `apply-template は冪等（同じテンプレートを2回適用してもエントリーが重複しない）`

**異常系（5パターン）**:
1. `6件目の作成 → TOUR_027`
2. `存在しないテンプレートの apply → TOUR_026`
3. `別チームのテンプレートを apply → TOUR_028`
4. `COMPLETED 大会への apply → TOUR_019`
5. `テンプレート内の非アクティブメンバーは skippedInactive にカウントされスキップ`

#### 結合テスト（`TournamentEntryTemplateControllerIntegrationTest.java`）

配置: `backend/src/test/java/com/mannschaft/app/tournament/entry/TournamentEntryTemplateControllerIntegrationTest.java`

各エンドポイントに最低1本（計7本）:
1. `GET /entry-templates 200 一覧返却`
2. `POST /entry-templates 201 テンプレート作成`
3. `PUT /entry-templates/{id} 200 テンプレート更新`
4. `DELETE /entry-templates/{id} 204 論理削除`
5. `POST apply-template 200 正常適用`
6. `POST apply-template 403 別チームのテンプレート`
7. `POST /entry-templates 422 5件超`

---

### 未解決事項

| No. | 内容 | 対応方針 |
|---|---|---|
| 1 | テンプレートのチーム間コピー機能 | 将来検討（チーム合併時等に別チームのテンプレートを引き継ぐ機能。Phase 9-C 候補） |
| 2 | テンプレート適用結果の通知 | 将来検討（適用後に「○名を追加しました」のトースト通知は実装推奨だがフロントエンド実装時に確定） |

---

## F08.7 順位UI Wave 3b: 設計書最終同期（2026-06-11）

> **ステータス**: 🟢 設計書同期完了（Wave0〜Wave3a 実装済・設計書に反映）
> **対象 PR**: Wave0(#1434→認可基盤), Wave1(#1441→閲覧UI), Wave2(#1448→入力UI), Wave3a(#1459→楽観ロック)
> **未踏**: 実機 E2E（§Wave3b-5 参照）

本節は Wave0〜3a で main マージ済みの実装内容を設計書へ後追い同期する。コードは変更しない（ドキュメントのみ）。

---

### Wave3b-1: 可視性モデル（`tournaments.visibility` 6レベル）【実装済】

#### 概要

`tournaments.visibility` カラムを旧 2 値（`PUBLIC` / `MEMBERS_ONLY`）から **6 レベル** に拡張した（Wave0 / Flyway `V9.*__alter_tournaments_visibility_six_levels.sql`）。既存行は `MEMBERS_ONLY` → `SCOPE_AFFILIATED` に Flyway で移行済み。

#### レベル定義

| レベル | 閲覧可能な立場 | F00 正準 StandardVisibility への写像 |
|--------|----------------|--------------------------------------|
| `PUBLIC` | 誰でも（未認証含む）。SSR で外部公開。 | `PUBLIC`（恒等） |
| `SUPPORTERS_AND_ABOVE` | 主催組織の応援者以上（SUPPORTER/MEMBER/DEPUTY_ADMIN/ADMIN）。 | `SUPPORTERS_AND_ABOVE`（恒等） |
| `MEMBERS_AND_ABOVE` | 主催組織のメンバー以上（応援者除外）。 | `MEMBERS_AND_ABOVE`（恒等） |
| `ADMINS_AND_ABOVE` | 主催組織の管理者のみ（ADMIN/DEPUTY_ADMIN）。 | `ADMINS_AND_ABOVE`（恒等） |
| `SCOPE_AFFILIATED` | 主催組織に直接所属する全員（応援者含む直接所属軸）。旧 `MEMBERS_ONLY` 相当。 | `SCOPE_AFFILIATED`（恒等） |
| `PARTICIPANTS_ONLY` | 参加チーム関係者のみ（大会専用軸）。応援者含む。未認証は不可視。 | `CUSTOM`（`TournamentVisibilityResolver.evaluateCustom` で判定） |

**既定値**: 新規大会の `visibility` デフォルトは `PUBLIC`。

**PARTICIPANTS_ONLY の判定ロジック**: `TournamentVisibilityResolver.evaluateCustom` が `tournament_participants`（`status = ACTIVE`）の `team_id` に対して、閲覧者がいずれかのチームのメンバーか（ロール不問・応援者含む）を確認する。参加チームが 0 件の場合は不可視（fail-closed）。

**設定 API**: `PATCH /api/v1/organizations/{orgId}/tournaments/{tId}` の `visibility` フィールドで変更（org 管理者のみ）。

---

### Wave3b-2: 閲覧 UI（Wave1・実装済）

Wave1 で以下のフロントエンドページ・コンポーネントを実装した。

| ファイル | 内容 |
|---------|------|
| `frontend/app/pages/organizations/[id]/tournaments/[tId]/standings.vue` | 順位表ページ（ディビジョンタブ切替・直近フォーム・PDF DL・手動再計算） |
| `frontend/app/pages/organizations/[id]/tournaments/[tId]/rankings.vue` | 個人ランキングページ（statKey 切替・BaseChart bar グラフ・PDF DL） |
| `frontend/app/pages/organizations/[id]/tournaments/[tId]/matrix.vue` | 対戦マトリクスページ（縦軸×横軸チーム・スコアセル・PDF DL） |
| `frontend/app/pages/organizations/[id]/tournaments/[tId]/index.vue` | 大会詳細 index（standings/rankings/matrix への導線） |

**認可フロー（閲覧）**:

```
閲覧リクエスト
  → StandingsController#verifyVisibility
  → contentVisibilityChecker.canView(ReferenceType.TOURNAMENT, tId, viewer)
  → TournamentVisibilityResolver（評価）
  → 不可視 → 404（IDOR 防止のため存在確認と同一レスポンス）
  → 可視 → 順位表 / ランキング / マトリクスを返却
```

FE は 404 を `'forbidden'` 扱いとして「閲覧権限がありません」を i18n で表示（症状を握りつぶさない）。

---

### Wave3b-3: スコア入力 UI（Wave2・実装済）

Wave2 で以下のフロントエンドページ・コンポーネントを実装した。

| ファイル | 内容 |
|---------|------|
| `frontend/app/pages/organizations/[id]/tournaments/[tId]/score-entry.vue` | スコア入力ページ（管理者のみ表示・非管理者は「権限なし」提示） |
| `frontend/app/components/tournament/ScoreEntryGrid.vue` | グリッド入力 UI（節選択→一括保存→StandingsRecalc 通知） |
| `frontend/app/components/tournament/ScoreCsvImport.vue` | CSV 取込 UI（テンプレ DL→アップロード→importScores 呼び出し） |

**スコア入力フロー**:

```
管理者: ディビジョン → 節 を選択
  → ScoreEntryGrid: getMatrix で参加チーム名を取得、matchday の fixture 一覧を表示
  → 各 fixture に home/away スコアを入力（version を必ず同梱）
  → batchUpdateScores（POST /scores/batch）で一括保存
  → BE: StandingsRecalculationEvent を 1 回発火
  → StandingsCalculationService@TransactionalEventListener(AFTER_COMMIT)+@Async で順位再計算
  → FE: 'saved' イベントで親に通知 → 順位表側が再取得
```

または

```
管理者: CSV 取込（ScoreCsvImport）
  → ひな形 CSV（matchId/homeScore/awayScore）をダウンロード → 記入 → アップロード
  → importScores（POST /scores/import）
  → BE が内部で batchUpdateScores を呼び出し → 同一の StandingsRecalc 経路
```

---

### Wave3b-4: 楽観ロック（Wave3a・実装済）

スコアの同時更新（管理者複数人による誤書き込み）を防ぐため、楽観ロックを実装した（Wave3a / PR #1459）。

**実装方針**:

| 項目 | 実装 |
|------|------|
| ロック対象 | `tournament_matches.version`（BIGINT、スコア更新ごとにインクリメント） |
| client 送信 | `batchUpdateScores` / `updateScore` リクエストに各 fixture の `version` を含める |
| BE 検証 | `MatchService.checkOptimisticLock` で client の `version` と DB の `version` を比較 |
| 不一致時 | `ObjectOptimisticLockingFailureException` を投げ → `GlobalExceptionHandler` が HTTP **409 (Conflict)** に変換 |
| FE ハンドリング | 409 受信時は `notification.error(t('tournament.scoreEntry.conflict'))` を表示し、当該節を再取得して `version` を最新化（再入力を促す） |
| 冪等性 | 同一 `version` で同一スコアを再送しても `checkOptimisticLock` が通過（同値再送は許可）。値を変更して再送する場合は再取得必須 |

**エラーコード**: `TOUR_SCORE_STALE`（波0の `TOUR_*` 既定コードに追加。HTTP 409）。

---

### Wave3b-5: 残課題（未踏）

以下は Wave0〜3a で未実施。別タスクとして起票予定。

#### 🔴 実機 E2E 未実施（最優先）

[[feedback_e2e_real_full_crud]] の方針に従い、認証付き一気通貫 E2E を別タスクで起票する。
前提: backend 8080 起動 + seed（複数ロールユーザー・大会データ）整備。

必要なシナリオ:

| シナリオ | 未踏理由 |
|---------|---------|
| 可視性 6 レベル × ロール（admin/member/supporter/未認証/参加チーム員/非参加）の見える・見えない検証 | 実機 E2E 基盤未整備 |
| スコア入力 → 順位表・ランキング・マトリクスへの反映一気通貫 | 同上 |
| 楽観ロック: 同時更新 → 409 → 再取得 → 再保存の UX フロー | 同上 |
| CSV 取込の end-to-end（template DL → 記入 → upload → 順位反映） | 同上 |

#### 🟡 ランキング選手名フォールバック

`rankings.vue` は現状 `#userId` フォールバック表示（BE が `displayName` を未提供）。
BE `RankingsCalculationService` が `displayName` を返すよう拡張が必要。別 Wave で対応。

#### 🟡 セット制・延長 PK スコア入力 UI

`ScoreEntryGrid` は基本の `homeScore`/`awayScore` のみ対応。
セット別スコア（バレー等）・延長別スコア・PK 戦スコアの入力 UI は MVP 外として未実装。
別 Wave で `tournament_match_sets` テーブルと連携して実装する。

#### 🟡 編集権限細分化

現状は org の ADMIN/DEPUTY_ADMIN が一律にスコア入力可。
`MANAGE_TOURNAMENT` 権限（`DEPUTY_ADMIN` サブセット）による細分化は設計のみ（§2 参照）。
Flyway でパーミッション定義を追加する別タスクが前提。

#### 参考: F08.10 との接続（入口①）

F08.10 試合記録（`matches` テーブル）から `tournament_matches` への順位連携は
[F08.10/05_tournament_integration.md](F08.10_match_record_analytics/05_tournament_integration.md)
の **H.0 中道（入口① 第一陣〜第三陣）** で実装済み。
`MatchScoreFixtureListener` が `MatchCompletedEvent` を `@TransactionalEventListener(AFTER_COMMIT)` で受信し、
既存 `MatchService.updateScore` → `StandingsRecalculationEvent` の経路を起動する。
本 Wave（F08.7 UI）との矛盾はない。

---

## 変更履歴

| 日付 | 変更内容 |
|------|---------|
| 2026-05-31 | 機能拡張 F08.7.1（トーナメント機能拡張）を新設し相互参照を追加: (1) §5.9 のディビジョン別ターゲティングに、常設の**連絡スペース**（掲示板・チャット）としての具体化は [F08.7.1/01_communication.md](F08.7.1_tournament_extensions/01_communication.md) を参照する旨のリンクを追加 (2) 成績ウィジェット 3 種は [F08.7.1/02_dashboard_widgets.md](F08.7.1_tournament_extensions/02_dashboard_widgets.md)、組織をまたぐ昇降格移籍（プル型招待＋プッシュ型送り出し）は [F08.7.1/03_league_pyramid_and_transfer.md](F08.7.1_tournament_extensions/03_league_pyramid_and_transfer.md) で具体化。同一大会内の昇降格（`PromotionService`）は本書のまま流用 |
| 2026-06-11 | Wave 3b: Wave0〜3a の実装内容を設計書に後追い同期。可視性 6 レベル（`PARTICIPANTS_ONLY`=CUSTOM 写像・`TournamentVisibilityResolver.evaluateCustom`）、閲覧 UI（standings/rankings/matrix ページ・F00 canView ガード・不可視 404）、スコア入力 UI（ScoreEntryGrid/ScoreCsvImport・batchUpdateScores/importScores・StandingsRecalc AFTER_COMMIT）、楽観ロック（client version 比較・409・FE 再取得）を反映。残課題（実機 E2E 未実施・表示名フォールバック・セット制/PK 入力 UI・MANAGE_TOURNAMENT 細分化）を別タスク起票予定として明記 |
