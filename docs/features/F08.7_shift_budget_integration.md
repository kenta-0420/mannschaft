# F08.7: シフト-予算-TODO 連携機能

> **ステータス**: 🟢 設計完了
> **実装フェーズ**: Phase 9（F03.5 Phase 4-γ 完了後を想定）
> **最終更新**: 2026-05-03
> **モジュール種別**: オプション機能（組織が予算管理 F08.6 を有効化し、かつシフト管理 F03.5 を運用している場合のみ。フィーチャーフラグ `feature.shiftBudget.enabled` 単位で組織別 ON/OFF）
> **関連ドキュメント**: F03.5（シフト管理）、F08.6（予算・会計管理）、F02.3（TODO 管理 + プロジェクト進捗）、F02.5（シフト→TODO 連動）、F05.6（ワークフロー・承認エンジン）、F04.3（プッシュ通知）、F10.3（監査ログ）

## 変更履歴

- **v1.0 (2026-05-03)**: TODO 一覧 #38（シフト連動予算管理）を起点に F03.5 / F08.6 / F02.3 を横断統合する設計書として初版作成
- **v1.1 (2026-05-03)**: 一次検分（足軽2）指摘 BLOCKER 2 件・MAJOR 5 件・MINOR 6 件を全件決着

---

## 1. 概要

### 1.1 背景・目的

TODO 一覧 #38 として記録された運用要望は次のとおり:

> シフトと連動した予算管理機能。追記の予算を決め、時給などを考慮してシフト作成できるといい。TODO は予算を決めて TODO を決められる？

文面は具体性に乏しいが、ヒアリングで意図を分解すると次の三層になる:

1. **「予算を決めてシフトを組む」** — 月次の人件費予算が先にあり、そこから「平均時給」「1 スロット時間」「必要枠数」を逆算したい。シフト確定後に予算超過していたら気づきたい
2. **「シフト確定 → 予算消化」** — シフトが公開された段階で、F08.6 の費目（人件費）から自動的に予算を引き当て、消化率をリアルタイムに見たい
3. **「TODO/プロジェクトに予算をつける」** — 「文化祭 30 万円まで」「夏祭りスタッフ人件費 5 万円まで」のように、TODO（プロジェクト）の単位でも予算枠を持ちたい

これら三層は既存機能（F03.5 シフト・F08.6 予算・F02.3 TODO）の境界をまたぐため、各設計書に追補するのではなく **F08.7 として独立した連携層** を立て、3 機能の間に位置する「予算割当 → 消化 → 紐付け → 警告」という一貫したフローを定義する。

### 1.2 解決する課題

1. **予算策定とシフト編成の分断** — 現状 F08.6 で年間予算を立てても、それがシフト編成画面に伝わらず、管理者は別画面・別エクセルで突合している
2. **人件費の事後発覚** — 月末に F08.6 の集計を見て初めて予算超過が判明する。事前警告がない
3. **TODO ベースの予算管理ができない** — 「このイベントに使える予算」を TODO/プロジェクト単位で見える化する手段がない

### 1.3 非目標

本機能は次を **扱わない**:

- **給与計算そのもの** — 源泉徴収・社会保険料・所得税・年末調整等は対象外。あくまで「予算管理目的の概算」に留める
- **税務処理・確定申告書類の生成** — F08.6 の年次報告書（既存）に委ねる
- **タイムカード/打刻による実労働時間集計** — F03.1 出欠管理の領域。本機能はシフト枠（計画値）を基に予算消化を計算する。実績差分は将来拡張（Phase 10 以降）
- **複数通貨の同時運用** — Phase 9 では単一通貨（既存 F08.6 と同様、日本円固定）。**ただし Phase 10 拡張ポイントとして** `shift_budget_allocations` / `shift_budget_consumptions` / `todo_budget_links` の各テーブルに `currency CHAR(3) NOT NULL DEFAULT 'JPY'` を予め配置済（§5.2 / §5.3 / §5.4）。Phase 10 では (1) per-organization の主通貨設定、(2) 為替レート変換テーブル、(3) 集計時の通貨統一ロジックを追加することで多通貨切替可能化を達成する設計
- **AI による最適シフト提案** — 予算制約を満たすシフトの自動生成は本機能のスコープ外。既存 F03.5 の自動割当に「予算上限」を **入力パラメータ** として渡すだけに留める

---

## 2. スコープ

### 2.1 含むもの

- 予算→必要シフト枠数の **逆算計算 API**（数式提供）
- シフトスロット ↔ 予算費目の **割当テーブル**（`shift_budget_allocations`）
- シフト確定時の **消化記録**（`shift_budget_consumptions`、計画値ベース）
- TODO/プロジェクト → 予算割当の **紐付テーブル**（`todo_budget_links`）
- リアルタイム消化率の **集計 API**
- 80% / 100% / 超過の **閾値警告**（`budget_threshold_alerts`、F04.3 通知連携）
- 月跨ぎの **按分ルール**（深夜跨ぎスロットを含む）
- シフトキャンセル時の **逆仕訳**
- 監査ログ（F10.3）への記録

### 2.2 含まないもの

- 既存 `budget_allocations`（年度×費目で 1 レコード）の置換 — F08.7 は **シフト期間（月単位）** で割り当てる別概念。年度予算配分の上に乗る「月割り内訳」として位置づける
- 既存 `budget_transactions` への自動仕訳作成 — 消化記録は F08.7 専用テーブルに保持し、F08.6 トランザクション帳へは「月次締め確定時」にまとめて反映する（v1.0 時点では別画面で確認、自動連携は Phase 9-δ 末尾で評価）
- 給与明細・賞与・退職金管理

### 2.3 既存機能との境界線

| 機能 | 担当範囲 | F08.7 から見た境界 |
|------|----------|--------------------|
| F03.5 シフト管理 | スロット定義、希望収集、自動割当、確定、公開 | 確定/公開された `shift_slots.assigned_user_ids` を入力として読み取る |
| F08.6 予算会計 | 年度・費目・配分・収支トランザクション・月次/年次報告書 | `budget_categories.id` と `budget_fiscal_years.id` を参照する。逆向きに F08.6 のトランザクション帳に書き戻すのは月次締めバッチのみ |
| F02.3 TODO/プロジェクト | TODO・プロジェクト・進捗率・マイルストーン | `projects.id` または `todos.id` を参照する。TODO 側の進捗率算出には影響しない |
| F02.5 シフト→TODO 連動 | シフト公開時の Todo 自動生成 | 連動 TODO に対しても予算紐付けを許容する（特別扱いしない） |
| F05.6 ワークフロー承認 | 承認ルート・閾値・通知 | 予算超過警告の段階に応じて承認リクエストを起動する |

---

## 3. ユースケース

### UC-1: 予算上限から必要シフト枠数を逆算する

**アクター**: ADMIN / DEPUTY_ADMIN（`MANAGE_SHIFTS` + `BUDGET_VIEW`）
**事前条件**: 当月の人件費予算が `budget_allocations` に登録済み（例: 30 万円）。チームメンバーの時給が `shift_hourly_rates` に登録済み

**フロー**:
1. ADMIN がシフト作成画面を開き、対象月（例: 2026 年 6 月）を選択
2. 「予算逆算」ボタンを押下
3. システムは `POST /api/v1/shift-budget/calc/required-slots` に当月の `budget_amount` と `slot_hours`（既定: 4 時間）を送信
4. サーバは `shift_hourly_rates` の現行平均時給を計算し、必要枠数を返却:
   `必要枠数 = floor(予算額 / (平均時給 × 1スロット時間))`
5. UI に「予算 30 万円 / 平均時給 1,200 円 / 4 時間スロット → 最大 62 枠」と表示
6. ADMIN はこの上限を参考にシフト編成を進める

**事後条件**: 計算結果はステートレス（DB 書き込みなし）。割当を確定したい場合は UC-2 に進む。

### UC-2: シフト確定時に人件費見込を予算費目から消化する

**アクター**: ADMIN / DEPUTY_ADMIN
**事前条件**: 当月の `shift_budget_allocations` レコードが作成済み（費目=人件費、期間=2026-06-01〜2026-06-30、配分額=30 万円）

**フロー**:
1. シフトスケジュールを `ADJUSTING → PUBLISHED` に遷移（F03.5 §5）
2. システムは公開対象スケジュール内の全 `shift_slots.assigned_user_ids` を走査し、各 (slot, user) について `shift_hourly_rates` から適用時給を取得
3. `shift_budget_consumptions` に `status = PLANNED` で 1 行ずつ INSERT（`hourly_rate_snapshot`、`hours`、`amount` を保存）
4. 同時に `shift_budget_allocations` の集計値を更新（非正規化キャッシュ `consumed_amount`）
5. 公開イベント（F03.5 既存）を発火し、UI ダッシュボードに反映
6. 消化率が閾値を超えていれば UC-5 を発火

**事後条件**: 消化記録は `PLANNED`。月次締めで `CONFIRMED` に昇格する。

### UC-3: TODO（プロジェクト・タスク）に予算枠を紐付け、進捗とともに消化を可視化する

**アクター**: ADMIN / DEPUTY_ADMIN（`MANAGE_TODO` + `BUDGET_VIEW`）
**事前条件**: プロジェクト「文化祭 2026」が登録済み。当該プロジェクトに紐付く予算割当（人件費 5 万円、消耗品費 3 万円）を立てたい

**フロー**:
1. プロジェクト編集画面で「予算」タブを開く
2. `POST /api/v1/todo-budget/links` で `project_id` と `allocation_id`、`link_amount=50000` を送信
3. プロジェクト詳細画面に「予算 5 万円 / 消化 1.2 万円 / 残 3.8 万円（24%）」と表示
4. プロジェクトに紐付く F03.5 シフトの消化が進むと、紐付分が自動的に集計される
5. プロジェクト完了時には残予算と実消化額を比較したサマリーが報告書に出る

**事後条件**: TODO 単位の紐付けも可能だが、UI 主導線はプロジェクト単位。

### UC-4: 月跨ぎ・締め処理時の予算精算

**アクター**: ADMIN（経理担当）
**事前条件**: 月末を迎え、当月の `shift_budget_consumptions` がすべて `PLANNED` 状態

**フロー**:
1. 月次締めバッチ（手動起動 or cron）が起動
2. 当月の `PLANNED` レコードを `CONFIRMED` に昇格
3. 同時に F08.6 `budget_transactions` に「シフト人件費 月次集計」として 1 件にまとめて INSERT（`source_type = 'SHIFT_BUDGET_MONTHLY'`、`source_id = allocation_id`）
4. 翌月の `shift_budget_allocations` を自動コピー（金額・費目を引き継ぎ。ADMIN が後から微調整可）

**事後条件**: 当月の予算は確定。以降キャンセル等の修正は **取消仕訳** で記録する（F08.6 のパターンに準拠）。

### UC-5: 予算超過警告と承認フロー連携

**アクター**: ADMIN（受信者）、予算管理者（承認者）
**事前条件**: 消化率が 80% を初めて超えた

**フロー**:
1. UC-2 内のチェックで閾値超過を検知
2. `budget_threshold_alerts` に 1 件 INSERT（`threshold_percent=80`、`triggered_at=now()`）
3. F04.3 プッシュ通知で ADMIN/予算管理者に通知（i18n キー `shiftBudget.alert.warn80`）
4. 100% 到達時は別レコード（`threshold_percent=100`）と承認ワークフロー起動（F05.6）
5. 100% 超過してもシフト確定操作はブロックされない（運用判断を妨げない）。ただし監査ログに `BUDGET_OVER_LIMIT_PROCEED` が記録され、承認待ちフラグが立つ
6. 予算管理者が `POST /api/v1/shift-budget/alerts/{id}/acknowledge` で確認応答すると `acknowledged_at` がセットされる

**事後条件**: 同一 allocation × 同一閾値で重複通知しないよう、`UNIQUE (allocation_id, threshold_percent)` 制約を貼る。

---

## 4. 機能要件

### 4.1 予算→シフト枠の逆算機能

**数式**:

```
必要枠数 = floor(予算額 / (平均時給 × 1スロット時間))
```

- `予算額`: `shift_budget_allocations.allocated_amount`（円）
- `平均時給`: 後述のパラメータで決定
- `1スロット時間`: 既定 4 時間（API パラメータで上書き可能、最小 0.25h、最大 24h）

**平均時給算出パラメータ** — リクエストボディで以下のいずれかを指定:

| `mode` | 説明 | 計算式 |
|--------|------|--------|
| `MEMBER_AVG`（既定） | 対象チームの全アクティブメンバーの現行時給を単純平均 | `SUM(hourly_rate) / COUNT(user_id)` |
| `POSITION_AVG` | ポジション（役割）別に平均時給を出し、必要人数で重み付け | Σ(position_avg × required_count_in_position) ÷ Σ(required_count) |

**`POSITION_AVG` モードの詳細リクエスト形式（v1.1 追記）**:

```json
{
  "team_id": 12,
  "budget_amount": 300000,
  "slot_hours": 4.0,
  "rate_mode": "POSITION_AVG",
  "position_required_counts": [
    { "position_id": 1, "required_count": 5 },
    { "position_id": 2, "required_count": 3 },
    { "position_id": 3, "required_count": 2 }
  ]
}
```

**サーバー側ロジック**:

1. `position_required_counts` の各エントリについて、当該 `position_id` を持つ `team_id` 内アクティブメンバーの `shift_hourly_rates` から **ポジション別平均時給** を自動取得（`SELECT AVG(hourly_rate) FROM shift_hourly_rates h JOIN team_members tm ON h.user_id = tm.user_id WHERE tm.team_id = :teamId AND tm.position_id = :positionId AND h.deleted_at IS NULL`）
2. **加重平均** を計算: `weighted_avg = Σ(position_avg × required_count) ÷ Σ(required_count)`
3. その加重平均を `avg_hourly_rate` として通常の必要枠数計算に投入
4. レスポンスに `position_breakdown` 配列を含める（各ポジションの平均時給と寄与度を可視化）

**バリデーション**:
- `position_required_counts` が空配列 → 400 + `error: EMPTY_POSITION_LIST`
- 重複する position_id → 400 + `error: DUPLICATE_POSITION_ID`
- `required_count <= 0` → 400 + `error: INVALID_REQUIRED_COUNT`
- 当該 position に時給設定済メンバーが 0 人 → そのポジションをスキップ + `warning: POSITION_NO_RATE_DATA` を含む

| `EXPLICIT` | 呼び出し側が `avg_hourly_rate` を直接指定 | リクエストボディの値をそのまま使用 |

**境界**:
- `平均時給 = 0` または `予算額 = 0` の場合 → 必要枠数 0 を返却し、`warning: "AVG_RATE_ZERO"` または `"BUDGET_ZERO"` を併記
- 平均時給未設定メンバーが過半数を占める場合 → `warning: "INSUFFICIENT_RATE_DATA"` を返却（計算は未設定者を除外して継続）
- スロット時間 ≤ 0 → 400 エラー

### 4.2 シフト→予算消化リンク

公開トリガー:
- `shift_schedules.status` が `ADJUSTING → PUBLISHED` に遷移したタイミング
- F03.5 既存の公開イベント（`ShiftPublishedEvent`）に対する `@EventListener` で受け取る

書き込みルール:
- 1 つの `shift_slots` に対し、`assigned_user_ids` の **配列要素ごと** に `shift_budget_consumptions` の 1 行を作成
- `hourly_rate_snapshot` には公開時点で適用される時給を保存（後で時給が変わっても消化額は変動しない）
- `hours` は `end_time - start_time`。深夜跨ぎ（end < start）の場合は + 24 時間

按分ルール（同一スロット複数アサイン時）:
- 各メンバーに **同一の勤務時間** を割当（按分しない）。理由: F03.5 のスロットは「全員が同時に同じ時間働く」前提。ペア勤務 = 2 人分の人件費が発生するのが正

### 4.3 TODO→予算紐付（粒度: プロジェクト単位を主、TODO 単位はオプション）

紐付テーブル `todo_budget_links` の制約:
- `project_id` と `todo_id` は **排他**（CHECK 制約 `chk_tbl_target_xor`）
- 同一 (project_id, allocation_id) は重複禁止
- 同一 (todo_id, allocation_id) は重複禁止
- `link_amount` と `link_percentage` も排他（どちらか一方、または両方 NULL = 全額紐付）

**プロジェクト単位の予算割当戦略 — v1.1 で確定**:

シフト→プロジェクトへの予算集計には **2 経路** を併用する:

1. **直接経路（Direct）**: シフトスケジュール自身がプロジェクトに紐付いている場合
   - `shift_schedules.linked_project_id` を Phase 9-γ で V3.130 にて追加（NULLABLE、ON DELETE SET NULL、**1:1 関係**。M:N にはしない — 1 つのスケジュールは多くて 1 つのプロジェクトに属する単純化を採用）
   - その shift_schedules に紐付く全ての shift_budget_consumptions を、当該プロジェクトの消化として SUM
   - **割当側**: `shift_budget_allocations.project_id`（NULLABLE、§5.2 参照）に対象プロジェクトを設定して「プロジェクト専用割当」とすることもできる

2. **TODO 経由（Via TODO）**: シフト/プロジェクト自体は紐付いていないが、TODO 単位で予算を引きたいケース
   - `todo_budget_links` で TODO（または親プロジェクト）と allocation を紐付
   - 紐付額（`link_amount` または `link_percentage`）を上限として、当該 allocation の消化額のうち按分された分をプロジェクトへ加算

**重複防止ルール**:

直接経路と TODO 経由の二重計上を防ぐため、以下のルールを Service 層で強制する:

- **直接経路が存在する shift_schedules** に紐付く consumption は、`todo_budget_links` 経由の集計から **除外** する（`shift_schedules.linked_project_id IS NOT NULL` を WHERE で検出）
- **`shift_budget_allocations.project_id` が NOT NULL の割当** は「プロジェクト専用割当」として扱い、当該 allocation の消化は当該プロジェクト以外の `todo_budget_links` から参照不可（API 層で 409）
- それ以外の通常の割当は両形式（月×費目×team 単独 / 月×費目×team×project）を **両方許容** する設計

**集計 SQL の概要**（プロジェクト P の消化額）:

```sql
-- 経路1: 直接経路（shift_schedules.linked_project_id = P）
SELECT COALESCE(SUM(c.amount), 0) AS direct_amount
FROM shift_budget_consumptions c
JOIN shift_schedules s ON c.shift_id = s.id
WHERE s.linked_project_id = :projectId
  AND c.status IN ('PLANNED', 'CONFIRMED');

-- 経路2: TODO 経由（todo_budget_links 経由、直接経路と重複する分は除外）
SELECT COALESCE(SUM(
  CASE
    WHEN tbl.link_amount IS NOT NULL THEN LEAST(c.amount, tbl.link_amount)
    WHEN tbl.link_percentage IS NOT NULL THEN c.amount * tbl.link_percentage / 100
    ELSE c.amount
  END
), 0) AS via_todo_amount
FROM todo_budget_links tbl
JOIN shift_budget_consumptions c ON c.allocation_id = tbl.allocation_id
LEFT JOIN shift_schedules s ON c.shift_id = s.id
WHERE tbl.project_id = :projectId
  AND c.status IN ('PLANNED', 'CONFIRMED')
  AND (s.linked_project_id IS NULL OR s.linked_project_id != :projectId);  -- 直接経路と重複する分を除外

-- 合計
total_amount = direct_amount + via_todo_amount;
```

**集計の流れ**（テーブル形式）:

| 経路 | データソース | 紐付方法 | 重複時の優先度 |
|------|--------------|----------|----------------|
| 直接 | `shift_schedules.linked_project_id` → `shift_budget_consumptions` | スケジュール単位 | 最優先（TODO 経由から除外） |
| TODO経由・上限金額 | `todo_budget_links.link_amount` | 紐付分のみ、`LEAST(消化, 上限)` | 直接経路が無い場合のみ |
| TODO経由・割合 | `todo_budget_links.link_percentage` | 消化額 × 割合 | 直接経路が無い場合のみ |
| TODO経由・全額 | `todo_budget_links` (両 NULL) | 当該 allocation の全消化 | 直接経路が無い場合のみ |

**v1.1 における決着**:

- F03.5 `shift_schedules.linked_project_id` の追加は **Phase 9-γ で V3.130 にて確定** （F03.5 修正ブランチで実施。詳細 §5.7 / §12.1）
- `shift_budget_allocations.project_id` を **NULLABLE で追加**（§5.2 確定）。両形式（月×費目×team 主形式、project_id 付加形式）を許容
- 割当 — シフト — プロジェクトの関係は **1:1**（M:N は採用しない）。複数プロジェクトに分配したい場合は `todo_budget_links` の `link_percentage` で表現する

### 4.4 リアルタイム消化率表示

集計 API:
- `GET /api/v1/shift-budget/allocations/{id}/consumption-summary`
- レスポンスに `allocated_amount`、`consumed_amount`、`consumption_rate`（0.0000〜1.0000+）、`remaining_amount`、`status`（`OK` / `WARN` / `EXCEEDED`）

非正規化:
- `shift_budget_allocations` に `consumed_amount`（DECIMAL(12,0)）を持ち、消化記録 INSERT 時にアトミック更新
- `consumption_rate` は SELECT 時に計算（DB カラム不要）
- 月次締めで `consumed_amount` を `confirmed_amount` に固定（PLANNED→CONFIRMED 昇格時に再計算）

### 4.5 閾値超過時の警告

閾値: 80%、100%、120%（超過マーカー）

通知:
- F04.3 プッシュ通知で対象ロール（ADMIN + 予算管理者）に送信
- 通知本文の i18n キー: `shiftBudget.alert.warn80`、`shiftBudget.alert.exceeded`、`shiftBudget.alert.severeExceeded120`

承認連携:
- 100% 到達時のみ F05.6 ワークフローを自動起動（ワークフロー定義 ID は組織設定 `budget_configs.over_limit_workflow_id` を参照）
- 80% / 120% は通知のみ

冪等性:
- `UNIQUE KEY uq_bta_allocation_threshold (allocation_id, threshold_percent)` で重複通知を防止

### 4.6 月跨ぎ・締め処理

**月跨ぎシフトの按分**:
- 22:00-06:00 のような深夜跨ぎスロットは、`slot_date` ベースで「開始日が属する月」に全額計上する（簡易ルール）
- 月末日 23:00-翌日 03:00 のスロットは、当月 1 時間 + 翌月 3 時間の按分ではなく、当月に 4 時間まとめて計上
- 理由: スロット単位の整合性を優先。Phase 10 で精緻化予定（要件次第）

**締め処理**:
- バッチ `monthlyShiftBudgetCloseJob`（手動起動 or `cron(0 2 1 * ?)`、毎月 1 日 02:00 JST）
- 当月分 `PLANNED` を `CONFIRMED` に一括昇格
- F08.6 `budget_transactions` に月次集計を 1 件 INSERT（`description = '2026年6月 シフト人件費（自動集計）'`）
- 監査ログ `SHIFT_BUDGET_MONTHLY_CLOSED` を記録

---

## 5. DB 設計

Flyway バージョンは **§5.7 で確定済**（V11.030〜V11.036 を F08.7 用に予約。`shift_schedules.linked_project_id` は F03.5 修正ブランチで V3.130 として別途追加）。

### 5.1 テーブル一覧

| テーブル名 | 役割 | 論理削除 |
|------------|------|----------|
| `shift_budget_allocations` | シフト期間（月）×費目に予算を割当 | あり |
| `shift_budget_consumptions` | シフト確定時の消化記録（計画値→確定値） | なし（`status = CANCELLED` で論理キャンセル） |
| `todo_budget_links` | TODO/プロジェクトへの予算紐付 | なし |
| `budget_threshold_alerts` | 閾値超過警告通知履歴 | なし |

### 5.2 `shift_budget_allocations`

シフト人件費を月単位で予算費目に割り当てるテーブル。F08.6 の `budget_allocations`（年度×費目で 1 行）の細分化として位置づける。

| カラム名 | 型 | NULL | デフォルト | 説明 |
|----------|----|------|------------|------|
| `id` | BIGINT UNSIGNED | NO | AUTO_INCREMENT | PK |
| `organization_id` | BIGINT UNSIGNED | NO | — | FK → organizations。多テナント分離。ON DELETE CASCADE |
| `team_id` | BIGINT UNSIGNED | YES | NULL | FK → teams。チーム単位で割当する場合のみ。NULL = 組織全体。ON DELETE CASCADE |
| `project_id` | BIGINT UNSIGNED | YES | NULL | FK → projects。プロジェクト専用割当の場合のみ。NULL = 通常の月×費目×team 割当。ON DELETE RESTRICT（プロジェクト消滅時に予算データを失わない）。Phase 9-γ で V11.035 により追加 |
| `fiscal_year_id` | BIGINT UNSIGNED | NO | — | FK → budget_fiscal_years。ON DELETE RESTRICT |
| `budget_category_id` | BIGINT UNSIGNED | NO | — | FK → budget_categories。通常は「人件費」配下の費目を指定。ON DELETE RESTRICT |
| `period_start` | DATE | NO | — | 適用開始日（通常は月初） |
| `period_end` | DATE | NO | — | 適用終了日（通常は月末） |
| `allocated_amount` | DECIMAL(12,0) | NO | — | 割当額（円） |
| `consumed_amount` | DECIMAL(12,0) | NO | 0 | 消化額キャッシュ（PLANNED + CONFIRMED の合計） |
| `confirmed_amount` | DECIMAL(12,0) | NO | 0 | 確定済み消化額（CONFIRMED のみ） |
| `currency` | CHAR(3) | NO | 'JPY' | ISO 4217。Phase 9 では JPY 固定 |
| `note` | VARCHAR(500) | YES | NULL | 備考 |
| `created_by` | BIGINT UNSIGNED | NO | — | FK → users。ON DELETE RESTRICT |
| `version` | BIGINT | NO | 0 | 楽観的ロック |
| `created_at` | DATETIME | NO | CURRENT_TIMESTAMP | |
| `updated_at` | DATETIME | NO | CURRENT_TIMESTAMP ON UPDATE | |
| `deleted_at` | DATETIME | YES | NULL | 論理削除（消化レコードがある場合は削除不可、APIで弾く） |

**インデックス**:
```sql
INDEX idx_sba_org_period (organization_id, period_start, period_end)
INDEX idx_sba_team_period (team_id, period_start, period_end)
INDEX idx_sba_project (project_id)  -- v1.1 追加
UNIQUE KEY uq_sba_scope_category_period (organization_id, team_id, project_id, budget_category_id, period_start, period_end, deleted_at)  -- v1.1: project_id を unique key に追加
INDEX idx_sba_fiscal (fiscal_year_id)
INDEX idx_sba_currency (currency)  -- v1.1 追加（Phase 10 多通貨集計用、§1.3 拡張ポイント）
```

**制約**:
```sql
CONSTRAINT chk_sba_amount CHECK (allocated_amount >= 0)
CONSTRAINT chk_sba_period CHECK (period_start <= period_end)
CONSTRAINT chk_sba_consumed CHECK (consumed_amount >= 0 AND confirmed_amount >= 0 AND confirmed_amount <= consumed_amount)
```

**HAS_CONSUMPTIONS 制約の詳細運用ルール（v1.1 追記）**:

割当の論理削除（DELETE）時、以下のロジックを Service 層で強制する:

| 残存 consumption の状態 | 削除可否 | 返却ステータス |
|--------------------------|----------|----------------|
| `PLANNED` が 1 件以上残存 | **不可** | 409 + `error: HAS_CONSUMPTIONS_PLANNED` |
| `CONFIRMED` が 1 件以上残存 | **不可** | 409 + `error: HAS_CONSUMPTIONS_CONFIRMED`（経理影響大、月次締め前提） |
| `CANCELLED` のみ存在 | **可能** | 200。論理削除を実行（監査証跡として CANCELLED レコードは残置） |
| consumption 0 件 | **可能** | 200 |

**年度跨ぎ運用**:

- 年度コピー時（UC-4 の翌月コピーロジックを年度版に拡張）は **新しい allocation_id で新規 INSERT** する。allocation_id は変更されるため、過去の consumption は古い allocation_id を引き続き参照する（監査証跡を完全に保持）
- 旧年度 allocation は論理削除しない（経理参照用）。Phase 11 でアーカイブテーブル分離を検討
- **多年度集計**（複数年度をまたぐプロジェクト等）は `todo_budget_links` で複数の allocation を紐付けることで表現。Service 層で年度単位 / プロジェクト全期間の 2 軸サマリ API を提供（Phase 9-δ で `/shift-budget/projects/{id}/multi-year-summary` を追加）

---

### 5.3 `shift_budget_consumptions`

シフト公開時に作成される消化記録。1 レコード = 1 (slot, user) ペア。

| カラム名 | 型 | NULL | デフォルト | 説明 |
|----------|----|------|------------|------|
| `id` | BIGINT UNSIGNED | NO | AUTO_INCREMENT | PK |
| `allocation_id` | BIGINT UNSIGNED | NO | — | FK → shift_budget_allocations。ON DELETE RESTRICT |
| `shift_id` | BIGINT UNSIGNED | NO | — | FK → shift_schedules。ON DELETE RESTRICT |
| `slot_id` | BIGINT UNSIGNED | NO | — | FK → shift_slots。ON DELETE RESTRICT |
| `user_id` | BIGINT UNSIGNED | NO | — | FK → users。ON DELETE RESTRICT |
| `hourly_rate_snapshot` | DECIMAL(10,2) | NO | — | 公開時点の適用時給（後の時給改定の影響を受けない） |
| `hours` | DECIMAL(5,2) | NO | — | 勤務時間（深夜跨ぎは + 24h で計算済み） |
| `amount` | DECIMAL(12,2) | NO | — | `hourly_rate_snapshot * hours` |
| `currency` | CHAR(3) | NO | 'JPY' | ISO 4217。Phase 9 では JPY 固定。Phase 10 で多通貨対応の拡張ポイント（§1.3） |
| `status` | VARCHAR(20) | NO | 'PLANNED' | `PLANNED` / `CONFIRMED` / `CANCELLED` |
| `recorded_at` | DATETIME | NO | CURRENT_TIMESTAMP | 公開イベント発火時刻 |
| `confirmed_at` | DATETIME | YES | NULL | CONFIRMED 昇格時刻（月次締め） |
| `cancelled_at` | DATETIME | YES | NULL | CANCELLED 遷移時刻 |
| `cancel_reason` | VARCHAR(100) | YES | NULL | キャンセル理由（`SHIFT_DELETED`, `SLOT_REMOVED`, `USER_UNASSIGNED`, `MANUAL_REVERSE`） |

**インデックス**:
```sql
INDEX idx_sbc_allocation_status (allocation_id, status)
INDEX idx_sbc_slot (slot_id)
INDEX idx_sbc_user_recorded (user_id, recorded_at)
INDEX idx_sbc_shift (shift_id, status)
UNIQUE KEY uq_sbc_slot_user_status (slot_id, user_id, status)  -- 同一スロット×同一ユーザー×同一状態は重複禁止（CANCELLED は status 違いで再記録可）
```

**制約**:
```sql
CONSTRAINT chk_sbc_amount CHECK (amount >= 0 AND hours >= 0)
CONSTRAINT chk_sbc_status CHECK (status IN ('PLANNED', 'CONFIRMED', 'CANCELLED'))
```

**運用ルール**:
- **物理削除禁止**。誤って書いた場合も `status = CANCELLED` に遷移させる（監査証跡）
- `status = CONFIRMED` 昇格後は `amount` / `hours` / `hourly_rate_snapshot` の UPDATE 不可（アプリ層で禁止、CHECK では表現困難）
- キャンセル時は逆仕訳ではなく status 遷移で表現。`shift_budget_allocations.consumed_amount` から差し引く

---

### 5.4 `todo_budget_links`

TODO/プロジェクトへの予算紐付。

| カラム名 | 型 | NULL | デフォルト | 説明 |
|----------|----|------|------------|------|
| `id` | BIGINT UNSIGNED | NO | AUTO_INCREMENT | PK |
| `project_id` | BIGINT UNSIGNED | YES | NULL | FK → projects。ON DELETE CASCADE |
| `todo_id` | BIGINT UNSIGNED | YES | NULL | FK → todos。ON DELETE CASCADE |
| `allocation_id` | BIGINT UNSIGNED | NO | — | FK → shift_budget_allocations。ON DELETE RESTRICT |
| `link_amount` | DECIMAL(12,0) | YES | NULL | 紐付上限金額（円）。NULL かつ link_percentage NULL = 割当全額 |
| `link_percentage` | DECIMAL(5,2) | YES | NULL | 割合（0.00〜100.00）。link_amount と排他 |
| `currency` | CHAR(3) | NO | 'JPY' | ISO 4217。Phase 9 では JPY 固定。link_amount の通貨単位を表す（Phase 10 多通貨対応の拡張ポイント） |
| `created_by` | BIGINT UNSIGNED | NO | — | FK → users。ON DELETE RESTRICT |
| `created_at` | DATETIME | NO | CURRENT_TIMESTAMP | |
| `updated_at` | DATETIME | NO | CURRENT_TIMESTAMP ON UPDATE | |

**インデックス**:
```sql
INDEX idx_tbl_project (project_id)
INDEX idx_tbl_todo (todo_id)
INDEX idx_tbl_allocation (allocation_id)
UNIQUE KEY uq_tbl_project_alloc (project_id, allocation_id)  -- project_id NULL の行同士は別扱い（MySQL 仕様）
UNIQUE KEY uq_tbl_todo_alloc (todo_id, allocation_id)
```

**制約**:
```sql
-- target は project か todo どちらか一方のみ
CONSTRAINT chk_tbl_target_xor CHECK (
  (project_id IS NOT NULL AND todo_id IS NULL)
  OR
  (project_id IS NULL AND todo_id IS NOT NULL)
)
-- 紐付指定は amount か percentage のどちらか（または両方 NULL）
CONSTRAINT chk_tbl_link_xor CHECK (
  link_amount IS NULL OR link_percentage IS NULL
)
CONSTRAINT chk_tbl_amount CHECK (link_amount IS NULL OR link_amount >= 0)
CONSTRAINT chk_tbl_percentage CHECK (link_percentage IS NULL OR (link_percentage >= 0 AND link_percentage <= 100))
```

---

### 5.5 `budget_threshold_alerts`

閾値超過警告の通知履歴。

| カラム名 | 型 | NULL | デフォルト | 説明 |
|----------|----|------|------------|------|
| `id` | BIGINT UNSIGNED | NO | AUTO_INCREMENT | PK |
| `allocation_id` | BIGINT UNSIGNED | NO | — | FK → shift_budget_allocations。ON DELETE CASCADE |
| `threshold_percent` | SMALLINT UNSIGNED | NO | — | 80 / 100 / 120 |
| `triggered_at` | DATETIME | NO | CURRENT_TIMESTAMP | 検知時刻 |
| `consumed_amount_at_trigger` | DECIMAL(12,0) | NO | — | 検知時点の消化額（事後分析用） |
| `notified_user_ids` | JSON | NO | — | 通知先ユーザー ID 配列 |
| `workflow_request_id` | BIGINT UNSIGNED | YES | NULL | F05.6 起動時の workflow_requests.id。100% 到達時のみセット。ON DELETE SET NULL |
| `acknowledged_at` | DATETIME | YES | NULL | 予算管理者が確認応答した時刻 |
| `acknowledged_by` | BIGINT UNSIGNED | YES | NULL | FK → users。ON DELETE SET NULL |

**インデックス**:
```sql
UNIQUE KEY uq_bta_allocation_threshold (allocation_id, threshold_percent)
INDEX idx_bta_triggered (triggered_at)
INDEX idx_bta_unack (acknowledged_at, allocation_id)  -- 未確認一覧用（acknowledged_at IS NULL）
```

**制約**:
```sql
CONSTRAINT chk_bta_threshold CHECK (threshold_percent IN (80, 100, 120))
```

---

### 5.6 ER 概略

```
organizations (1) ──── (N) shift_budget_allocations
teams (1) ──── (N) shift_budget_allocations [team_id; nullable]
budget_fiscal_years (1) ──── (N) shift_budget_allocations
budget_categories (1) ──── (N) shift_budget_allocations

shift_budget_allocations (1) ──── (N) shift_budget_consumptions
shift_schedules (1) ──── (N) shift_budget_consumptions
shift_slots (1) ──── (N) shift_budget_consumptions
users (1) ──── (N) shift_budget_consumptions

shift_budget_allocations (1) ──── (N) todo_budget_links
projects (1) ──── (N) todo_budget_links [project_id; nullable]
todos (1) ──── (N) todo_budget_links [todo_id; nullable]

shift_budget_allocations (1) ──── (N) budget_threshold_alerts
workflow_requests (1) ──── (1) budget_threshold_alerts [workflow_request_id; nullable]
```

### 5.7 Flyway 配置順（予約済バージョン番号）

**確定バージョン番号**:

F08.6 の最終投入済バージョンは V11.026 であることを調査済（2026-05-03 時点）。F08.7 は **V11.030〜V11.036 を予約** とする。V11.027〜V11.029 は緊急パッチ用にバッファとして確保する。

```
V11.030__create_shift_budget_allocations.sql               -- Phase 9-β
V11.031__create_shift_budget_consumptions.sql              -- Phase 9-β
V11.032__create_todo_budget_links.sql                      -- Phase 9-γ
V11.033__create_budget_threshold_alerts.sql                -- Phase 9-δ
V11.034__extend_budget_configs_for_shift_budget.sql        -- Phase 9-δ（over_limit_workflow_id, shift_budget_enabled 追加）
V11.035__add_project_id_to_shift_budget_allocations.sql    -- Phase 9-γ（プロジェクト単位割当 NULLABLE 追加）
V11.036__add_currency_indexes_for_shift_budget.sql         -- Phase 9-δ（多通貨拡張用インデックス予備）
```

**`shift_schedules.linked_project_id` の追加は F03.5 領域**:

- 当該カラム追加は F08.7 ではなく **F03.5 修正ブランチで V3.130 として実施** する（F03.5 系列の最新バージョン V3.129 の後続）
- F08.7 Phase 9-γ 着手と同期して F03.5 担当軍へ依頼を発注する
- 依存関係上、V3.130 が V11.032 / V11.035 より先に投入されている必要がある（マイグレーション実行順序は数値順なので V3.130 が必ず先に走り、整合する）

```
V3.130__add_linked_project_id_to_shift_schedules.sql       -- F03.5 修正ブランチで実施（F08.7 とは別 PR）
```

**選択理由**:
- 連番（V11.030〜036）で隣接配置することで、Flyway リプレイ時の依存関係（CASCADE 制約）が自然に解決される
- 緊急パッチ枠（V11.027〜029）を残すことで、運用中の F08.6 緊急修正に即応可能
- linked_project_id は F03.5 のスキーマであり、設計責務分離の原則から F03.5 のバージョン体系で管理する（保守性優先）

依存関係: F03.5（shift_*、V3.130 含む）、F08.6（budget_*）、F02.3（projects, todos）が先に投入されていること。

---

## 6. API 設計

REST、ベースパス `/api/v1/shift-budget` および `/api/v1/todo-budget`。認証必須。組織コンテキスト（X-Organization-Id ヘッダ）必須。

### 6.1 一覧

| # | メソッド | パス | 用途 | 必要権限 |
|---|----------|------|------|----------|
| 1 | GET | `/shift-budget/allocations` | 割当一覧取得 | `BUDGET_VIEW` |
| 2 | POST | `/shift-budget/allocations` | 割当新規作成 | `BUDGET_MANAGE` |
| 3 | PUT | `/shift-budget/allocations/{id}` | 割当更新 | `BUDGET_MANAGE` |
| 4 | DELETE | `/shift-budget/allocations/{id}` | 割当論理削除 | `BUDGET_MANAGE` |
| 5 | GET | `/shift-budget/allocations/{id}/consumption-summary` | 消化サマリ取得 | `BUDGET_VIEW` |
| 6 | POST | `/shift-budget/calc/required-slots` | 逆算計算（ステートレス） | `MANAGE_SHIFTS` |
| 7 | POST | `/todo-budget/links` | TODO 予算紐付作成 | `MANAGE_TODO` + `BUDGET_VIEW` |
| 8 | DELETE | `/todo-budget/links/{id}` | 予算紐付削除 | `MANAGE_TODO` + `BUDGET_VIEW` |
| 9 | GET | `/shift-budget/alerts` | 警告一覧（未承認優先） | `BUDGET_VIEW` |
| 10 | POST | `/shift-budget/alerts/{id}/acknowledge` | 警告承認応答 | `BUDGET_MANAGE` |

### 6.2 主要エンドポイント詳細

#### 6.2.1 `POST /api/v1/shift-budget/allocations`

リクエスト:
```json
{
  "team_id": 12,
  "fiscal_year_id": 3,
  "budget_category_id": 17,
  "period_start": "2026-06-01",
  "period_end": "2026-06-30",
  "allocated_amount": 300000,
  "currency": "JPY",
  "note": "6 月度 ホールスタッフ人件費"
}
```

レスポンス（201）:
```json
{
  "id": 42,
  "organization_id": 1,
  "team_id": 12,
  "fiscal_year_id": 3,
  "budget_category_id": 17,
  "period_start": "2026-06-01",
  "period_end": "2026-06-30",
  "allocated_amount": 300000,
  "consumed_amount": 0,
  "confirmed_amount": 0,
  "currency": "JPY",
  "version": 0,
  "created_at": "2026-05-03T10:00:00Z"
}
```

エラー:
- 400: `period_start > period_end` / `allocated_amount < 0`
- 403: `BUDGET_MANAGE` 権限なし
- 409: 既存と (org, team, category, period_start, period_end) が衝突

#### 6.2.2 `POST /api/v1/shift-budget/calc/required-slots`

ステートレス計算 API。

リクエスト:
```json
{
  "team_id": 12,
  "budget_amount": 300000,
  "slot_hours": 4.0,
  "rate_mode": "MEMBER_AVG"
}
```

または `rate_mode = "EXPLICIT"` の場合:
```json
{
  "budget_amount": 300000,
  "slot_hours": 4.0,
  "rate_mode": "EXPLICIT",
  "avg_hourly_rate": 1200
}
```

レスポンス（200）:
```json
{
  "budget_amount": 300000,
  "avg_hourly_rate": 1200,
  "slot_hours": 4.0,
  "required_slots": 62,
  "calculation": "floor(300000 / (1200 * 4.0)) = 62",
  "warnings": []
}
```

#### 6.2.3 `GET /api/v1/shift-budget/allocations/{id}/consumption-summary`

レスポンス（200）:
```json
{
  "allocation_id": 42,
  "allocated_amount": 300000,
  "consumed_amount": 245000,
  "confirmed_amount": 200000,
  "planned_amount": 45000,
  "remaining_amount": 55000,
  "consumption_rate": 0.8167,
  "status": "WARN",
  "alerts": [
    { "threshold_percent": 80, "triggered_at": "2026-06-25T14:30:00Z", "acknowledged_at": null }
  ],
  "by_user": [
    { "user_id": 5, "amount": 80000, "hours": 66.67 },
    { "user_id": 6, "amount": 60000, "hours": 50.0 }
  ]
}
```

`status` 判定:
- `< 0.80` → `OK`
- `0.80 ≤ rate < 1.00` → `WARN`
- `1.00 ≤ rate < 1.20` → `EXCEEDED`
- `≥ 1.20` → `SEVERE_EXCEEDED`

#### 6.2.4 `POST /api/v1/todo-budget/links`

リクエスト:
```json
{
  "project_id": 88,
  "allocation_id": 42,
  "link_amount": 50000
}
```

または:
```json
{
  "todo_id": 1024,
  "allocation_id": 42,
  "link_percentage": 10.0
}
```

レスポンス（201）:
```json
{
  "id": 7,
  "project_id": 88,
  "todo_id": null,
  "allocation_id": 42,
  "link_amount": 50000,
  "link_percentage": null,
  "created_by": 1,
  "created_at": "2026-05-03T10:30:00Z"
}
```

#### 6.2.5 `POST /api/v1/shift-budget/alerts/{id}/acknowledge`

リクエスト:
```json
{ "comment": "了解。来月分の予算を増額予定" }
```

レスポンス（200）:
```json
{
  "id": 99,
  "allocation_id": 42,
  "threshold_percent": 80,
  "acknowledged_at": "2026-06-25T15:00:00Z",
  "acknowledged_by": 1
}
```

### 6.3 共通ルール

- 全エンドポイントは `X-Organization-Id` ヘッダで組織スコープを強制
- `version` フィールドで楽観的ロック（PUT 時 409 Conflict）
- レスポンスから `hourly_rate_snapshot` は **`BUDGET_ADMIN` または `MANAGE_SHIFTS`+`BUDGET_VIEW` を持つロール** 以外には返却しない（v1.1: 新権限 `BUDGET_ADMIN` 導入、§8 参照）
- マスキング実装は **Jackson `@JsonView` 方式に確定**（v1.1）。詳細は §9.3
- 集計 API（`/consumption-summary`）の `by_user` 配列は権限なし時 **空配列 `[]`** を返却（フィールド省略ではなく明示的な空配列。フロントが「権限なし」と「データなし」を区別できるよう、`status` フィールドに `BY_USER_HIDDEN` を含める）
- 全 DELETE は論理削除 or status 遷移、物理削除 API は提供しない

---

## 7. 画面設計

### 7.1 予算割当画面（管理者向け、新規）

ルート: `/admin/shift-budget/allocations`

レイアウト:
- 上部: 月セレクタ + チームセレクタ + 「新規割当」ボタン
- 中央: 割当一覧テーブル（費目 / 期間 / 配分額 / 消化額 / 消化率バー / 状態バッジ）
- 各行クリック: 詳細パネル（消化サマリ + 紐付 TODO 一覧 + 警告履歴）

### 7.2 シフト作成画面の予算インジケーター（既存 F03.5 画面への追加）

**配置 — 案A（サイドバー配置）を採用と確定（v1.1）**:

検討した代替案:
- **案A（採用）**: 既存の右サイドバー下部に「予算カード」コンポーネントとして表示
- 案B（不採用）: ヘッダ右上のミニバー — 既存 F03.5 ヘッダが密集しておりタブレットで折りたたまれる懸念
- 案C（不採用）: フッター固定 — モバイルで操作領域を圧迫する

**採用理由**:
- サイドバーは F03.5 既存のフィルタ・統計エリアと同じレイアウト規約に従い、保守性が高い
- カード形式なら詳細リンク（ダッシュボード遷移）や複数割当の縦積み表示が自然
- タブレット時はサイドバー折りたたみと連動するため、予算カードも同時に隠れて整合する

**追加要素**:

- 予算カードのヘッダに「当月予算: 30 万円 / 使用見込: 24.5 万円（81%）」のミニバー
- 81% 超で黄バー、100% 超で赤バー、120% 超で赤+点滅アイコン
- カード内「予算逆算」ボタン押下で UC-1 のモーダル表示
- 複数割当が存在する場合はカード内で縦積み表示（最大 3 件、超過分は「他 N 件」リンクでダッシュボード遷移）

**表示条件（v1.1 確定）**:

予算カードは **以下の AND 条件をすべて満たすときに限り表示** する:

1. グローバル feature flag: `feature.shiftBudget.enabled = true`
2. 組織別 feature flag: `budget_configs.shift_budget_enabled = TRUE`
3. 当月かつ当該 team_id（または組織全体）の `shift_budget_allocations` レコードが 1 件以上存在
4. ログインユーザーが `MANAGE_SHIFTS` 権限を保持

**MEMBER（一般メンバー）への非表示を確定**:
- MEMBER は MANAGE_SHIFTS 権限を持たないため、上記条件 4 で必ず非表示
- 自分自身の時給確認は別途 `VIEW_OWN_HOURLY_RATE`（§8）の専用 API/画面で対応
- 予算情報がメンバー一覧画面の片隅に漏れることを防ぐ（時給漏洩防止 §9.3 と整合）

### 7.3 TODO/プロジェクト編集画面の予算紐付タブ（既存 F02.3 画面への追加）

追加要素:
- プロジェクト編集画面に「予算」タブを追加（`MANAGE_TODO` + `BUDGET_VIEW` 保有時のみ表示）
- タブ内: 紐付済み割当一覧 + 「割当を追加」ボタン → 割当選択モーダル + 紐付方式（全額 / 上限金額 / 割合）
- TODO 単位の紐付は詳細画面の「...」メニューから選択（主導線ではない）

### 7.4 予算消化ダッシュボード（新規）

ルート: `/dashboard/shift-budget`

ウィジェット:
- 今月の消化率ゲージ（複数割当をまとめて表示）
- 過去 6 ヶ月の消化額推移（棒グラフ）
- 直近の警告 5 件
- プロジェクト別 TOP 5（紐付額順）

### 7.5 警告通知 UI

- 通知センター（F04.3 既存）に「予算 80% 警告」「予算超過」などを追加
- ダッシュボード上部にバナー（未承認警告がある場合のみ）
- バナー右端に「確認」ボタン → POST `/alerts/{id}/acknowledge`

### 7.6 レイアウト方針

| 画面 | PC（≥1280px） | タブレット（768〜1279px） | モバイル（<768px） |
|------|---------------|---------------------------|--------------------|
| 7.1 割当画面 | テーブル + 詳細パネル横並び | テーブル全幅 + モーダル詳細 | カードリスト + フルスクリーン詳細 |
| 7.2 インジケーター | サイドバー下部に予算カード（v1.1 確定） | サイドバー折りたたみと連動 | サイドバー折りたたみと連動 |
| 7.3 予算タブ | サイドタブ | 上部タブ | 上部タブ（横スクロール） |
| 7.4 ダッシュボード | 4 カラムグリッド | 2 カラムグリッド | 1 カラムスタック |
| 7.5 バナー | 固定上部、幅 100% | 同左 | 同左 + テキスト省略 |

---

## 8. 権限・ロール

### 8.1 新権限の導入（v1.1 で確定）

検分指摘 M-5 を受け、既存権限の組み合わせで全運用を表現することは煩雑かつ権限設定の取り違えを誘発するため、**以下の 2 権限を新規導入** する:

| 権限名 | 役割 | 含意 |
|--------|------|------|
| **`BUDGET_ADMIN`** | F08.7 専用の管理権限。`MANAGE_SHIFTS` + `BUDGET_VIEW` の組み合わせを **置換する単独権限** として位置づけ | 時給情報を含む全データへのアクセス、予算割当の作成・編集・削除、警告の承認応答、月次締めバッチ起動を一元的に表現 |
| **`VIEW_OWN_HOURLY_RATE`** | 自分自身の時給情報のみを参照する権限 | デフォルトで全 MEMBER に付与。組織管理者が剥奪することも可能（時給非表示運用の組織向け） |

**移行ルール**:
- 既存組織で `MANAGE_SHIFTS` + `BUDGET_VIEW` の両方を保有するユーザーには、Phase 9-α 投入時に `BUDGET_ADMIN` を自動付与（マイグレーション V11.034 内で実施）
- Service 層の権限チェックは「`BUDGET_ADMIN` または `MANAGE_SHIFTS`+`BUDGET_VIEW` の両方」を OR で評価（後方互換性）

### 8.2 権限マトリックス

| 操作 | 必要権限 |
|------|----------|
| 予算割当の閲覧 | `BUDGET_VIEW`（ADMIN / DEPUTY_ADMIN(BUDGET_VIEW) / 部門責任者） |
| 予算割当の CRUD | `BUDGET_MANAGE` または `BUDGET_ADMIN` |
| 自分の時給情報の閲覧 | `VIEW_OWN_HOURLY_RATE`（v1.1 新規。デフォルトで全 MEMBER に付与） |
| 他人の時給情報の閲覧 | `BUDGET_ADMIN`（推奨） または `MANAGE_SHIFTS` + `BUDGET_VIEW`（後方互換） |
| 消化サマリの閲覧（個人別 by_user 含まない） | `BUDGET_VIEW` |
| 消化サマリの閲覧（個人別 by_user 含む） | `BUDGET_ADMIN` または `MANAGE_SHIFTS` + `BUDGET_VIEW` |
| 逆算計算 API の呼出 | `MANAGE_SHIFTS`（時給データを参照するため） |
| TODO 予算紐付の作成・削除 | `MANAGE_TODO` + `BUDGET_VIEW` |
| 警告の閲覧 | `BUDGET_VIEW` |
| 警告の承認応答 | `BUDGET_MANAGE` または `BUDGET_ADMIN` |
| 月次締めバッチの起動 | `BUDGET_MANAGE` または `BUDGET_ADMIN`（手動起動 UI） |

### 8.3 既存ロール体系との整合

- `BUDGET_VIEW` / `BUDGET_MANAGE` は F08.6 で既に定義済み（変更なし）
- `MANAGE_SHIFTS` / `MANAGE_TODO` は F03.5 / F02.3 で既定義（変更なし）
- **新規追加権限（v1.1）**: `BUDGET_ADMIN`、`VIEW_OWN_HOURLY_RATE` の 2 種

### 8.4 ワークフロー承認者の指定方式

F05.6 の予算超過承認ワークフローについて:

- **承認者は組織が `budget_configs.over_limit_workflow_id` で指定するワークフローテンプレートに従う**
- ワークフローテンプレート側でロール条件（例: `BUDGET_ADMIN` 保有者の中から指定 N 名）を定義する
- F08.7 設計書では承認者ロジックを直接記述せず、F05.6 の責務に委ねる
- 組織が `over_limit_workflow_id` を未設定の場合、警告通知のみ送信（承認ワークフローはスキップ）+ 監査ログに `WORKFLOW_NOT_CONFIGURED` を記録

### 8.5 `budget_configs` テーブルへの新カラム追加（既存 F08.6）

- `over_limit_workflow_id BIGINT UNSIGNED NULL FK workflow_definitions(id) ON DELETE SET NULL` — 100% 超過時に起動するワークフロー定義
- `shift_budget_enabled BOOLEAN NULL DEFAULT NULL` — 組織別フィーチャーフラグの **三値論理**（v1.1 確定。詳細 §13）:
  - `NULL`: 組織未設定 → グローバル既定値 `feature.shiftBudget.enabled` を継承
  - `FALSE`: 明示的にオプトアウト
  - `TRUE`: 明示的に有効化

---

## 9. セキュリティ

### 9.1 監査ログ（F10.3）

以下のイベントを `audit_logs` に記録:

| イベント名 | 発火タイミング |
|------------|----------------|
| `SHIFT_BUDGET_ALLOCATION_CREATED` | 割当新規作成時 |
| `SHIFT_BUDGET_ALLOCATION_UPDATED` | 割当更新時（`amount` の変更を `before/after` で記録） |
| `SHIFT_BUDGET_ALLOCATION_DELETED` | 論理削除時 |
| `SHIFT_BUDGET_CONSUMPTION_RECORDED` | 公開時の消化記録一括 INSERT 後（slot_count を記録） |
| `SHIFT_BUDGET_CONSUMPTION_CANCELLED` | 個別消化の CANCELLED 遷移時 |
| `SHIFT_BUDGET_MONTHLY_CLOSED` | 月次締めバッチ完了時 |
| `BUDGET_THRESHOLD_ALERT_TRIGGERED` | 警告発火時 |
| `BUDGET_THRESHOLD_ALERT_ACKNOWLEDGED` | 承認応答時 |
| `BUDGET_OVER_LIMIT_PROCEED` | 100% 超過状態でシフト公開を強行した時 |
| `TODO_BUDGET_LINK_CREATED` / `..._DELETED` | 紐付の作成・削除時 |

### 9.2 楽観的ロック

- `shift_budget_allocations` に `version` カラム必須
- PUT エンドポイントは `If-Match` ヘッダまたはリクエストボディの `version` で検証、不一致時は 409 Conflict

### 9.3 時給情報の漏洩防止

**実装方式 — Jackson `@JsonView` 採用に確定（v1.1）**:

`@JsonView` を採用する理由:
- DTO クラスを 1 つに保てる（権限ごとの DTO 分離より保守性が高い）
- Spring MVC の `@JsonView` アノテーションで Controller メソッド単位に切替可能
- フィールド単位のマスキング粒度を細かく制御できる

**View クラス定義例**:

```java
// org.mannschaft.shiftbudget.view.BudgetView
public final class BudgetView {
    /** 一般ユーザー向け（金額・時給を全てマスク） */
    public interface Public {}
    /** BUDGET_VIEW 保有者向け（金額は見えるが個人別時給は見えない） */
    public interface BudgetViewer extends Public {}
    /** BUDGET_ADMIN または MANAGE_SHIFTS+BUDGET_VIEW 保有者向け（個人別時給まで全公開） */
    public interface BudgetAdmin extends BudgetViewer {}
}
```

**DTO 例**:

```java
public class ConsumptionSummaryDto {
    @JsonView(BudgetView.Public.class)
    private Long allocationId;

    @JsonView(BudgetView.BudgetViewer.class)
    private BigDecimal allocatedAmount;

    @JsonView(BudgetView.BudgetViewer.class)
    private BigDecimal consumedAmount;

    @JsonView(BudgetView.BudgetAdmin.class)
    private List<UserConsumptionDto> byUser;  // 個人別時給は ADMIN のみ

    @JsonView(BudgetView.Public.class)
    private String status;  // BY_USER_HIDDEN を含めうる
}
```

**Controller での切替（権限チェック関数のサンプル）**:

```java
@GetMapping("/allocations/{id}/consumption-summary")
public MappingJacksonValue getSummary(@PathVariable Long id, @AuthenticationPrincipal CurrentUser user) {
    ConsumptionSummaryDto dto = service.getSummary(id, user.getOrganizationId());
    MappingJacksonValue wrapper = new MappingJacksonValue(dto);
    wrapper.setSerializationView(resolveView(user));
    return wrapper;
}

/**
 * ユーザー権限から最も詳細な View を解決する。
 * 上位 View ほど多くのフィールドを公開する（継承関係: Public ⊂ BudgetViewer ⊂ BudgetAdmin）。
 */
private Class<?> resolveView(CurrentUser user) {
    if (user.hasPermission("BUDGET_ADMIN")
            || (user.hasPermission("MANAGE_SHIFTS") && user.hasPermission("BUDGET_VIEW"))) {
        return BudgetView.BudgetAdmin.class;
    }
    if (user.hasPermission("BUDGET_VIEW")) {
        return BudgetView.BudgetViewer.class;
    }
    return BudgetView.Public.class;
}
```

**集計 API の `by_user` 配列の権限なし時挙動**:

- `BudgetAdmin` View に該当しないユーザーが `/consumption-summary` を呼ぶと、`by_user` フィールドはシリアライズから除外される
- ただし、フロントが「権限なし」と「データなし」を区別できるよう、Service 層で **空配列 `[]` を返却** + `status` フィールドに `BY_USER_HIDDEN` フラグを追加（§6.3 共通ルール参照）
- 自分自身の時給だけは `VIEW_OWN_HOURLY_RATE` 権限（v1.1 新規、§8）で別エンドポイント `/shift-budget/me/hourly-rate` から取得可能

**その他**:

- ログ出力（`logger.info`）に時給数値を出力しない（DTO の `toString()` でマスキング）
- アクセスログには `hourly_rate_snapshot` を含めない（リクエスト/レスポンスダンプから除外設定）

### 9.4 改竄防止

- `shift_budget_consumptions` は **物理削除禁止**。CANCEL のみ
- `status = CONFIRMED` 後は `amount` / `hours` / `hourly_rate_snapshot` の UPDATE を Service 層で禁止（IllegalStateException）
- 月次締め後の割当 UPDATE は ADMIN のみ + 監査ログ必須

### 9.5 多テナント分離

- 全クエリに `WHERE organization_id = ?` を強制（Hibernate `@Filter` または `BaseRepository` の override）
- 検索時、`X-Organization-Id` ヘッダと `current_user` の所属組織が一致しない場合は 403
- TODO 紐付時、`projects.scope_id` または `todos` 経由のチーム/組織が `allocation.organization_id` と一致することを検証

---

## 10. i18n

ロケールファイル: `frontend/app/locales/{ja,en,zh,ko,es,de}/shiftBudget.json`（新規ファイル）。プレフィックスは `shiftBudget.*`。

**v1.1 で 28 キーを確定**（ja は本書で確定。en/zh/ko/es/de は Phase 9-δ で翻訳）:

| # | キー | 日本語（確定） | 用途 |
|---|------|----------------|------|
| 1 | `shiftBudget.title` | シフト予算管理 | 画面タイトル |
| 2 | `shiftBudget.allocation.list` | 予算割当一覧 | 一覧見出し |
| 3 | `shiftBudget.allocation.create` | 新規割当 | ボタン |
| 4 | `shiftBudget.allocation.amount` | 配分額 | 列名 |
| 5 | `shiftBudget.allocation.period` | 適用期間 | 列名 |
| 6 | `shiftBudget.allocation.category` | 費目 | 列名 |
| 7 | `shiftBudget.consumption.summary` | 消化サマリ | 見出し |
| 8 | `shiftBudget.consumption.consumed` | 消化額 | 数値ラベル |
| 9 | `shiftBudget.consumption.remaining` | 残額 | 数値ラベル |
| 10 | `shiftBudget.consumption.rate` | 消化率 | パーセント表示 |
| 11 | `shiftBudget.consumption.byUserHidden` | 個人別内訳は権限がないため表示できません | by_user 非表示時メッセージ |
| 12 | `shiftBudget.threshold.warn80` | 予算 80% に到達しました | 通知本文 |
| 13 | `shiftBudget.threshold.exceeded` | 予算を超過しました | 通知本文 |
| 14 | `shiftBudget.threshold.severeExceeded120` | 予算 120% を超過しました（重大） | 通知本文 |
| 15 | `shiftBudget.alert.acknowledge` | 確認 | ボタン |
| 16 | `shiftBudget.alert.unacknowledged` | 未確認の警告 | バッジ |
| 17 | `shiftBudget.link.project` | プロジェクトに紐付 | タブ |
| 18 | `shiftBudget.link.todo` | TODO に紐付 | サブメニュー |
| 19 | `shiftBudget.link.fullAmount` | 全額紐付 | ラジオ |
| 20 | `shiftBudget.link.partialAmount` | 上限金額を指定 | ラジオ |
| 21 | `shiftBudget.link.percentage` | 割合を指定 | ラジオ |
| 22 | `shiftBudget.calc.requiredSlots` | 必要枠数を逆算 | ボタン |
| 23 | `shiftBudget.calc.result` | 必要枠数: {count} 枠 | 計算結果 |
| 24 | `shiftBudget.calc.formula` | 計算式: floor({budget} / ({rate} × {hours})) | 説明 |
| 25 | `shiftBudget.calc.warning.zeroRate` | 平均時給が 0 円のため計算できません | 警告 |
| 26 | `shiftBudget.calc.warning.insufficientData` | 時給未設定のメンバーが多いため精度が低い可能性があります | 警告 |
| 27 | `shiftBudget.dashboard.title` | 予算消化ダッシュボード | 画面タイトル |
| 28 | `shiftBudget.feature.disabled` | この機能は組織管理者によって無効化されています | フラグ OFF 時 |

**合計: 28 キー × 6 言語 = 168 翻訳エントリ**

**翻訳投入計画**:
- Phase 9-α 着手時: ja を本表のとおり確定投入
- Phase 9-α 〜 γ: en / zh / ko / es / de は ja と同一値で暫定投入（i18n キー欠落エラーを防止）
- Phase 9-δ: 機械翻訳ベース → 人間レビューで他 5 言語を確定

---

## 11. エッジケース・例外処理

| ケース | 設計上の決着 |
|--------|--------------|
| **月跨ぎシフト（深夜跨ぎ）** | `slot_date` が属する月に全額計上。22:00-翌03:00 のスロットは開始日の月に 5 時間まとめて計上 |
| **月跨ぎシフト（複数日連続）** | F03.5 の現状は 1 スロット = 同日内のため、複数日連続スロットは存在しない。将来追加された場合は本書 §15 ではなくこのケースに追加 |
| **予算ゼロ円の割当** | 作成自体は許可。逆算 API は `required_slots = 0` + `warning: BUDGET_ZERO`。消化発生時は即 100% 超過 → 警告発火 |
| **時給未設定メンバーがアサインされた場合** | 消化記録は INSERT する。`hourly_rate_snapshot = 0`、`amount = 0`、`status = PLANNED`。集計時に `warning: USER_HAS_NO_RATE` を含めて UI に「時給未設定: N 名」と表示。給与概算は 0 円扱い（F03.5 の既存挙動と整合） |
| **多通貨混在** | Phase 9 では `currency = 'JPY'` 固定。CHECK 制約は付けず、将来拡張余地として `currency` カラムだけ用意。同一割当内で異なる通貨が混在する設計は本書スコープ外 |
| **シフトキャンセル時の消化レコード逆仕訳** | `shift_schedules` の論理削除や `shift_slots` の `assigned_user_ids` 配列要素削除を `ShiftCancelledEvent` で受信し、対応する `shift_budget_consumptions` を `status = CANCELLED` + `cancel_reason = 'USER_UNASSIGNED'` に遷移。`allocation.consumed_amount` をアトミックに減算 |
| **予算超過してもシフト確定可能** | UC-5 の通り、シフト公開操作はブロックしない。警告 + 監査ログ + 承認ワークフロー起動のみ。運用判断（緊急対応・特例承認）を妨げない |
| **削除されたメンバーの過去消化レコード** | `shift_budget_consumptions.user_id` の FK は `ON DELETE RESTRICT`。ユーザーが論理削除（F00 既存）された場合は `users.deleted_at` で判別。集計時に「退会者: N 円」として括る |
| **同一スロットに複数メンバーがアサイン** | §4.2 の通り按分しない。各メンバー分を 1 行ずつ INSERT。理由: 全員が同時間働く前提のため |
| **同一 (slot, user) に複数回 INSERT を試行（公開を取り消して再公開）** | UNIQUE `(slot_id, user_id, status)` で衝突 → 409 ではなく Service 層で「既存 PLANNED を CANCELLED に遷移してから新規 INSERT」する更新ロジック（v1.1: 擬似コード §11.1 参照） |
| **月次締め後の遅延キャンセル** | CONFIRMED 状態の消化は UPDATE 不可。代わりに `cancel_reason = 'POST_CLOSE_REVERSE'` の **新規 CANCELLED 行** を INSERT し、`allocation.confirmed_amount` から差し引く（取消仕訳パターン）。F08.6 `budget_transactions` にも対応する取消仕訳を起こす |
| **`allocation` 削除を試行（消化レコードあり）** | API で 409 Conflict + `error: HAS_CONSUMPTIONS`。消化を全て CANCEL してから再試行を促す |
| **TODO/プロジェクトが論理削除された** | `todo_budget_links.project_id` / `todo_id` は `ON DELETE CASCADE`。物理削除は通常起きない（F02.3 は論理削除）が、復元時の再紐付は別途運用 |
| **割当の `period_end` が過去日になる UPDATE** | 許可（過去予算の修正用）。ただし監査ログ必須 + 承認応答時に再評価しない |
| **フィーチャーフラグ OFF 時の API 呼出** | 全 F08.7 API は 503 Service Unavailable + `error: FEATURE_DISABLED` を返却。既存データ（過去に有効化していた場合）は閲覧のみ可能 |
| **「1スロット = 当日中」の限界（v1.1 明記）** | F03.5 / F08.7 の前提として **1 つの shift_slot は同一日内で完結** する。月跨ぎは深夜跨ぎ（22:00-翌03:00 など）に限り許可し、§4.6 の按分ルール（開始日属する月へ全額計上）で処理する。**複数日連続の連泊・夜勤連続スロットは F03.5 のスキーマで表現できないため、本書のスコープ外**。Phase 10 以降で F03.5 拡張時に再検討 |

### 11.1 同一 (slot, user) 再 INSERT のサービス層擬似コード（v1.1 追記）

```java
// org.mannschaft.shiftbudget.service.ConsumptionService
@Transactional
public void recordConsumption(ShiftPublishedEvent event) {
    for (ShiftSlot slot : event.getSlots()) {
        for (Long userId : slot.getAssignedUserIds()) {
            BigDecimal hourlyRate = hourlyRateRepo.findCurrentRate(userId, slot.getOrgId())
                .orElse(BigDecimal.ZERO);
            BigDecimal hours = calcHours(slot.getStartTime(), slot.getEndTime());

            // (1) 同一 (slot_id, user_id) の PLANNED を CANCELLED に遷移
            consumptionRepo.findBySlotAndUserAndStatus(slot.getId(), userId, "PLANNED")
                .ifPresent(existing -> {
                    existing.setStatus("CANCELLED");
                    existing.setCancelledAt(LocalDateTime.now());
                    existing.setCancelReason("RE_PUBLISHED");
                    consumptionRepo.save(existing);
                    // allocation.consumed_amount から差し引き
                    allocationRepo.decrementConsumed(existing.getAllocationId(), existing.getAmount());
                });

            // (2) CONFIRMED が既に存在する場合は再 INSERT を拒否（月次締め後の再公開は不可）
            if (consumptionRepo.existsBySlotAndUserAndStatus(slot.getId(), userId, "CONFIRMED")) {
                throw new IllegalStateException(
                    "Cannot re-record consumption: CONFIRMED record exists for slot=" + slot.getId() + ", user=" + userId);
            }

            // (3) 新規 PLANNED を INSERT
            ShiftBudgetConsumption fresh = new ShiftBudgetConsumption();
            fresh.setAllocationId(resolveAllocationId(slot, event.getMonth()));
            fresh.setShiftId(event.getShiftScheduleId());
            fresh.setSlotId(slot.getId());
            fresh.setUserId(userId);
            fresh.setHourlyRateSnapshot(hourlyRate);
            fresh.setHours(hours);
            fresh.setAmount(hourlyRate.multiply(hours));
            fresh.setStatus("PLANNED");
            fresh.setRecordedAt(LocalDateTime.now());
            consumptionRepo.save(fresh);

            // (4) allocation.consumed_amount に加算
            allocationRepo.incrementConsumed(fresh.getAllocationId(), fresh.getAmount());

            // (5) 閾値判定 → 必要なら警告発火（重複防止 §4.5 の UNIQUE 制約に依拠）
            thresholdAlertService.evaluateAndTrigger(fresh.getAllocationId());
        }
    }
}
```

**設計上のポイント**:
- (1) で UNIQUE 制約衝突を未然に防ぐ（CANCELLED 遷移後は status が異なるため新規 INSERT が通る）
- (2) で月次締め後の再公開を物理的に禁止（経理整合性を守る）
- (4) で `consumed_amount` の二重加算を防ぐため、(1) で減算してから (4) で加算する順序を厳守
- 全体を `@Transactional` で囲み、途中失敗時に整合性を保つ

---

## 12. 既存機能との連携

### 12.1 F03.5 シフト管理

接続点:
- 入力: `shift_schedules.id`、`shift_slots.id`、`shift_slots.assigned_user_ids`、`shift_hourly_rates`
- イベント連携: `ShiftPublishedEvent` / `ShiftCancelledEvent` / `ShiftSlotAssignmentChangedEvent` を購読
- 追加カラム（要 Phase 9-γ）: `shift_schedules.linked_project_id BIGINT UNSIGNED NULL FK projects(id) ON DELETE SET NULL`（プロジェクト単位の集計を可能にする）

### 12.2 F08.6 予算会計

接続点:
- 入力: `budget_fiscal_years.id`、`budget_categories.id`
- 月次締め時に `budget_transactions` へ「シフト人件費 月次集計」の 1 行を INSERT（`source_type = 'SHIFT_BUDGET_MONTHLY'`、`source_id = allocation_id`）
- `budget_configs` を拡張（§8 参照）

### 12.3 F02.3 TODO/プロジェクト

接続点:
- 入力: `projects.id`、`todos.id`
- TODO 進捗率（`projects.progress_rate`）には影響しない（独立指標）

### 12.4 F05.6 ワークフロー承認

接続点:
- 100% 超過時に `workflow_requests` を起動。ワークフロー定義 ID は `budget_configs.over_limit_workflow_id`
- 承認結果を `budget_threshold_alerts.workflow_request_id` 経由で参照

### 12.5 F02.5 シフト→TODO 連動

接続点:
- F02.5 が自動生成する Todo は通常の TODO と同じ扱い。`todo_budget_links` で予算紐付可能
- F02.5 側で生成された Todo の `project_id` が設定されていれば、プロジェクト経由の紐付も継承される（紐付方法は ADMIN の運用判断）

### 12.6 F04.3 プッシュ通知

- 警告通知のチャネル: `BUDGET_THRESHOLD_WARN`、`BUDGET_THRESHOLD_EXCEEDED`、`BUDGET_THRESHOLD_SEVERE`
- 受信ロール: 当該組織の ADMIN + `BUDGET_MANAGE` 保有 DEPUTY_ADMIN

---

## 13. 段階導入計画

| フェーズ | 含む UC | 含む DB | 含む API | 完遂判定 |
|----------|---------|---------|----------|----------|
| **Phase 9-α** | UC-1（逆算） | （DDL なし） | #6 のみ | 逆算 API が動き、画面に枠数表示 |
| **Phase 9-β** | UC-2（消化記録） | `shift_budget_allocations`、`shift_budget_consumptions` | #1〜#5 | シフト公開 → 消化記録 INSERT → サマリ表示 |
| **Phase 9-γ** | UC-3（TODO 紐付） | `todo_budget_links`（V11.032）+ `shift_budget_allocations.project_id` 追加（V11.035）+ `shift_schedules.linked_project_id` 追加（**V3.130 — F03.5 修正ブランチで別途実施**） | #7、#8 | プロジェクト編集画面で予算紐付動作 |
| **Phase 9-δ** | UC-4（締め）、UC-5（警告） | `budget_threshold_alerts` + `budget_configs` 拡張 | #9、#10 + 月次締めバッチ | 80%/100% 警告発火、承認応答可、F08.6 への月次反映 |

**フィーチャーフラグの三値論理（v1.1 確定）**:

| グローバル<br>`feature.shiftBudget.enabled` | 組織別<br>`budget_configs.shift_budget_enabled` | 結果 |
|---|---|---|
| `false` | （任意） | **OFF**（強制無効。グローバル不許可は組織でも上書き不可） |
| `true` | `NULL`（未設定） | **ON**（グローバル既定値を継承） |
| `true` | `FALSE`（明示的オプトアウト） | **OFF**（組織が明示的に無効化） |
| `true` | `TRUE`（明示的有効化） | **ON** |

**カラム定義**:
- `budget_configs.shift_budget_enabled BOOLEAN DEFAULT NULL`（NOT NULL ではない）
- 組織が API でフラグを切替時、`null` / `true` / `false` の 3 値を送信可能
- フロントの管理画面では「グローバル設定を継承」「明示的に有効」「明示的に無効」のラジオ 3 択

**判定ロジック**:

```java
public boolean isFeatureEnabled(Long organizationId) {
    if (!globalProperties.shiftBudgetEnabled()) {
        return false;  // グローバルが OFF なら全組織 OFF
    }
    Boolean orgFlag = budgetConfigsRepo.findByOrgId(organizationId)
        .map(BudgetConfig::getShiftBudgetEnabled)
        .orElse(null);
    if (orgFlag == null) {
        return true;  // 組織未設定 → グローバル既定値（ON）を継承
    }
    return orgFlag;  // 明示値（TRUE/FALSE）に従う
}
```

ロールバック方針:
- フィーチャーフラグを OFF にしても既存データは保持。再 ON で復活
- DDL ロールバック用の `Vundo_FUTURE_F087__*.sql` も同時に用意（実装フェーズで作成）

---

## 14. テスト方針

### 14.1 ユニットテスト

| 対象 | テストケース |
|------|--------------|
| 逆算ロジック | `budget=300000, rate=1200, hours=4 → 62`、`budget=0 → 0 + warning`、`rate=0 → 0 + warning`、`hours=0 → 400` |
| 平均時給計算 | `MEMBER_AVG`（全員設定済 / 一部未設定 / 全員未設定）、`POSITION_AVG`（重み付け検証）、`EXPLICIT`（パススルー） |
| 消化額計算 | 通常スロット、深夜跨ぎ、複数アサイン |
| ステータス遷移 | PLANNED → CONFIRMED、PLANNED → CANCELLED、CONFIRMED → CANCELLED（取消仕訳） |
| 閾値判定 | 79.99%（OK）、80.00%（WARN）、99.99%（WARN）、100.00%（EXCEEDED）、120.00%（SEVERE） |
| 紐付バリデーション | project_id と todo_id の排他、link_amount と link_percentage の排他、percentage 範囲外 |
| 月跨ぎ按分（v1.1 追加） | 5/31 23:00 開始 〜 6/1 05:00 終了の 6 時間スロット → §4.6 ルールに従い **5 月の allocation に全 6 時間分（hours=6.0）まとめて計上** されること、6 月の allocation には計上されないこと |
| 月跨ぎ按分・複数連続（v1.1 追加） | 5/31 18:00 〜 6/1 02:00 の 8 時間 + 5/31 23:00 〜 6/1 07:00 の 8 時間 → 両方とも開始日（5/31）の月に計上 |
| 月跨ぎ按分・月初開始（v1.1 追加） | 6/1 00:00 〜 6/1 04:00 → 6 月の allocation のみに計上（境界条件確認） |
| 月跨ぎ按分・月末終了同日（v1.1 追加） | 5/31 18:00 〜 5/31 23:59 → 5 月の allocation のみに計上（月跨ぎ判定が偽陽性にならないこと） |
| 権限マスキング（v1.1 追加） | `BUDGET_VIEW` のみ保有ユーザーに対し、`/consumption-summary` の `by_user` が空配列で返り、`status` に `BY_USER_HIDDEN` が含まれること |
| BUDGET_ADMIN 移行（v1.1 追加） | V11.034 マイグレーション後、既存の `MANAGE_SHIFTS` + `BUDGET_VIEW` 保有ユーザーに `BUDGET_ADMIN` が自動付与されること |
| 三値論理フィーチャーフラグ（v1.1 追加） | グローバル ON × 組織 NULL → 有効、グローバル ON × 組織 FALSE → 無効、グローバル ON × 組織 TRUE → 有効、グローバル OFF × 組織 TRUE → 無効 |

### 14.2 統合テスト

- 予算→シフト→消化の一連フロー（割当作成 → シフト公開 → 消化サマリ取得）
- 月次締めバッチ（PLANNED → CONFIRMED → F08.6 トランザクション生成）
- シフトキャンセル → 逆仕訳 → consumed_amount 減算
- 警告発火 → F04.3 通知 → 承認応答

### 14.3 E2E テスト（Playwright）

- ADMIN がシフト作成画面で逆算 → 枠数表示 → シフト確定 → ダッシュボードで消化率確認
- 80% 警告発火 → バナー表示 → 確認応答
- 100% 超過 → 承認ワークフロー起動 → 承認後にバッジ消失
- TODO 編集画面で予算紐付 → プロジェクト詳細に消化額表示

### 14.4 多テナント分離テスト

- 組織 A の ADMIN が組織 B の `allocation_id` を URL 直叩き → 403
- 組織 A のシフト公開イベントが組織 B の allocation を消化しない（`organization_id` 一致チェック）

### 14.5 セキュリティテスト

- 一般 MEMBER のレスポンスに他人の `hourly_rate_snapshot` が含まれないこと
- 監査ログに必須イベントが全て記録されること
- フィーチャーフラグ OFF 時に API が 503 を返すこと

---

## 15. 未解決事項

**現時点で未解決事項なし — 全項目を本設計書内で決着。**

v1.1 改訂時点で、一次検分（足軽2）が指摘した BLOCKER 2 件・MAJOR 5 件・MINOR 6 件をすべて本設計書内で明示的に決着した。決着内容の参照先:

| 旧来の未決事項 | 決着場所 | 決着内容（要約） |
|------|----------|------------------|
| Flyway バージョン番号 | §5.7 | V11.030〜V11.036 を予約。`shift_schedules.linked_project_id` は F03.5 修正ブランチで V3.130 として別途追加 |
| `shift_schedules.linked_project_id` 追加時期と関係 | §4.3 / §5.7 / §12.1 | Phase 9-γ で V3.130 にて追加。1:1 関係（M:N 不採用） |
| プロジェクト単位の予算割当戦略 | §4.3 / §5.2 | `shift_budget_allocations.project_id` を NULLABLE で追加。両形式（月×費目×team / 月×費目×team×project）を許容 |
| 直接経路 vs TODO 経由の重複防止 | §4.3 | 直接経路が存在する shift_schedules は TODO 経由集計から除外。集計 SQL を明記 |
| HAS_CONSUMPTIONS 削除制約 | §5.2 | PLANNED/CONFIRMED 残存で 409、CANCELLED のみなら削除可 |
| 年度跨ぎ運用 | §5.2 | 新 allocation_id で新規 INSERT、旧データは経理参照用に保持 |
| 時給マスキング実装 | §9.3 | Jackson `@JsonView` 採用。サンプルコード掲載 |
| シフト作成画面インジケーター配置 | §7.2 | 案A（サイドバー配置）採用。表示条件 4 条件 + MEMBER 非表示確定 |
| 新権限の必要性 | §8.1 / §8.2 | `BUDGET_ADMIN` および `VIEW_OWN_HOURLY_RATE` を新規導入 |
| ワークフロー承認者指定方式 | §8.4 | 組織が `over_limit_workflow_id` で指定するワークフロー定義に従う |
| 多通貨対応の拡張ポイント | §1.3 / §5.2 / §5.3 / §5.4 | currency CHAR(3) を Phase 9 から事前配置、Phase 10 で多通貨切替可能化 |
| POSITION_AVG リクエスト形式 | §4.1 | `position_required_counts` 配列形式を確定。サーバー側自動取得+加重平均 |
| i18n キー一覧 | §10 | 28 キーに確定。ja を本書で確定、他 5 言語は Phase 9-δ で翻訳 |
| 同一スロット×ユーザー再 INSERT ロジック | §11.1 | Service 層擬似コードを掲載 |
| フィーチャーフラグの三値論理 | §13 | `BOOLEAN DEFAULT NULL` で「未設定=継承」「FALSE=オプトアウト」「TRUE=有効」を確定 |
| 月跨ぎテストケース | §14.1 | 5/31 23:00〜6/1 05:00 等の境界ケースを 4 種類明記 |

実装フェーズ着手時に AI 開発フロー §1（要件確認）として提示するのは、本設計書全体の最終承認のみ。

---

## 16. 改訂履歴

| バージョン | 日付 | 変更内容 |
|------------|------|----------|
| v1.0 | 2026-05-03 | 初版作成。TODO 一覧 #38（シフト連動予算管理）を起点に F03.5（シフト）/ F08.6（予算）/ F02.3（TODO）の 3 設計書を横断統合する F08.7 を新規策定。予算→時給→必要枠数の逆算、シフト確定時の予算消化記録、TODO/プロジェクトへの予算紐付、80%/100%/120% 閾値超過警告、月次締め処理、多テナント分離・楽観ロック・監査ログ・時給漏洩防止のセキュリティ要件、6 言語 i18n キー定義、Phase 9-α〜δ の段階導入計画まで 16 章で完結 |
| v1.1 | 2026-05-03 | 一次検分（足軽2）指摘反映: BLOCKER 2件・MAJOR 5件・MINOR 6件を全件決着。主要変更: linked_project_id 追加時期確定（V3.130）、F08.7 Flyway バージョン確定（V11.030-036）、新権限 BUDGET_ADMIN / VIEW_OWN_HOURLY_RATE 導入、Jackson @JsonView 採用、shift_budget_allocations に project_id NULLABLE 追加、currency カラム事前配置（多通貨拡張ポイント）、i18n 28キー確定、月跨ぎテストケース追加。 |
