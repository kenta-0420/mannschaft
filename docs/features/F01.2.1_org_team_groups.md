# F01.2.1: チーム加盟の双方向化とチームグループ

> **ステータス**: 🟡 設計中
> **実装フェーズ**: Phase 2（F01.2 の後続。実装は後続の戦役で行う）
> **最終更新**: 2026-09-26（第2パス精査・実機E2E耐性の指摘とマスター御裁可 M1〜M3 を反映）
> **モジュール種別**: F01.2 組織・チーム・メンバー・ロールのサブ機能
> **関連ドキュメント**: [F01.2 組織・チーム・メンバー・ロール](./F01.2_org_team_member_role/README.md)（加盟の正本・§3/§4/§5 を本書に合わせて改訂済み）、[F02.8 ダッシュボード告知](./F02.8_dashboard_announcement.md)（グループ宛て告知・push 絞り込みを本書に合わせて改訂済み）、[F00 コンテンツ可視性リゾルバ](./F00_content_visibility_resolver.md)（親組織の解決）、[F08.7.1 大会拡張](./F08.7.1_tournament_extensions/)（ディビジョン・リーグ移籍）、[F15.4 組織内チーム検索](./F15.4_team_store_search_within_org.md)、[docs/architecture/domain_db_design_principles.md](../architecture/domain_db_design_principles.md)、[docs/security/README.md](../security/README.md)

---

## 1. 概要

組織（連盟・協会・同窓会など）とチームの「加盟」関係を、次の4点で拡張する。

1. **加盟を双方向にする。** これまでの「組織がチームを招待し、チームが承諾する」に加えて、「チームが組織に申請し、組織が承認する」を設ける。チームからの申請は、組織が受付を許可しているときだけ行える。
2. **1チームが複数の組織へ同時に加盟できることを正式に保証する。** DB（`UNIQUE (team_id, organization_id)`）は元から複数加盟を許していたが、読み手の一部が「親組織は1つ」と前提していた。これを本書の範囲で改修する（§9）。
3. **チームグループを導入する。** 組織は加盟チームを「チームグループ」（例: 平成20年度卒、北地区、U-12）に分けられる。1つの加盟につき所属できるグループは1つで、グループには並び順がある。
4. **お知らせをグループ単位で送れるようにする。** 送信時にグループを個別に選ぶことも、「平成20年度以前」のように並び順の範囲で選ぶこともできる。ダッシュボードの表示は閲覧時点の所属で判定し（動的）、push 通知は送信時点の所属で宛先を確定する（スナップショット）。

### 1.1 解決する課題

| 課題 | 解決策 |
|---|---|
| チームが自分から組織へ加盟したくても手段がない（招待を待つしかない） | チーム→組織の加盟申請（組織が受付を許可している場合のみ） |
| 加盟の書き込み API が設計だけあって実装されていない（参照系のみ実装） | 書き込み API 群を本書で正式に定義し直す（実装は後続の戦役） |
| 複数の組織に加盟したチームで、可視性や行事カテゴリの親組織が1つしか解決されない | 単一親前提の読み手を洗い出し、複数親の意味論を定めて改修する（§9） |
| 加盟チームが数百に増えると、組織が「どのチームがどの区分か」を管理できない | チームグループ（平坦・並び順付き・1加盟1グループ） |
| 「平成20年度以前の卒業生チームだけ」に連絡したい | 個別選択と並び順の範囲指定の両方で宛先を決める |
| 組織のお知らせを特定チームに絞っても、アンケートの push は組織全体に届く | 宛先を絞ったアンケート告知（チームを選ぶ・チームグループ）は、push も宛先チームのメンバーと直属メンバーだけに届ける（§8.5） |

### 1.2 本書のスコープ

| 範囲 | 内容 |
|---|---|
| 対象 | 加盟の申請・招待・承認・拒否・取下げ・取消・離脱・除名、申請受付設定、再申請の抑止、PENDING の期限切れと組織・チームの削除／アーカイブ時の片付け、複数親組織の読み手改修、チームグループの CRUD・並び替え・割当、グループ宛てお知らせ（表示・push）、範囲テンプレートのグループ対応 |
| 対象外 | **閲覧制限（アクセス制御）**。グループ宛てのお知らせでも、元になった掲示板スレッドやアンケートは、従来どおり visibility ルールに従って組織メンバー全員が開ける。本機能が絞るのは「ダッシュボードのお知らせ欄に出るか」と「push 通知が届くか」の2点だけである（F02.8 §7 の既存方針を踏襲する） |
| 対象外 | 現在 push を出していないチャネル（掲示板・タイムライン・ブログ・TODO・スケジュール）に push を新設すること。グループで絞っても、これらは従来どおり push しない（§8.5） |
| 対象外 | グループの階層化（親子グループ）。グループは平坦とする |
| 対象外 | 1チームが同じ組織で複数のグループに入ること |

---

## 2. 用語と、似た概念との違い

UI での名称は **「チームグループ」** とする。単に「グループ」と書くと、ユーザー向け権限機能の「権限グループ」（`permission_groups`）と区別できないためである。コード上の名前も `TeamGroup` / `org_team_groups` とし、`PermissionGroup` とは語を重ねない。

| 概念 | 何を分けるか | 単位・寿命 | 本機能との違い |
|---|---|---|---|
| **チームグループ**（本機能 `org_team_groups`） | 組織に加盟している**チーム**を区分する | 組織ごとに常設。加盟1件につきグループは最大1つ | — |
| 権限グループ（`permission_groups`、F01.2） | **ユーザー**の権限（RBAC）を束ねる | チーム／組織ごと。ユーザーに複数割当 | 対象がユーザーで、目的が権限。チームは区分しない |
| 大会ディビジョン（`tournament_divisions`、F08.7） | 1つの**大会**の出場チームを 1部／2部などに分ける | 大会ごと。昇降格で毎シーズン入れ替わる | 大会の成績で決まる一時的な区分。組織が管理画面で自由に付け替える常設ラベルではない |
| リーグ移籍（LeagueTransfer、F08.7.1） | **子組織（下位リーグの組織）どうしの間**でチームを昇格・降格させる | 移籍1件ごとの「送り出し＋承認」 | 移籍は**加盟先の組織そのもの**が変わる（`team_org_memberships` の行が組織をまたいで移る）。チームグループは**同じ組織の中**でラベルを付け替えるだけで、加盟先は変わらない |
| 子組織（`organizations.parent_organization_id`） | 組織の下に別の**組織（テナント）**を作る | 子組織は独自の管理者・メンバー・設定・加盟チームを持つ | 子組織は管理権限が独立した別組織。グループは権限を持たない「ラベル」にすぎず、管理者は親組織の管理者だけ |
| 委員会（F04.10） | 組織内の**ユーザー**のサブスコープ | 組織配下 | ユーザーの集まりであり、チームの区分ではない |

使い分けの目安:

- 区分ごとに別の管理者・別の設定が要る → **子組織**
- 大会の成績で区分が決まり、区分をまたぐときに組織も変わる（1部リーグの組織から2部リーグの組織へ） → **子組織＋リーグ移籍**
- 1つの大会の中だけで 1部／2部に分けたい → **大会ディビジョン**
- 管理者は同じで、一覧・お知らせの宛先を分けたいだけ（卒業年度・地区・年代） → **チームグループ**

---

## 3. 対象ロールと権限

### 3.1 権限表

チーム側の列は、加盟操作の権限 `MANAGE_ORG_AFFILIATION`（§3.2）で分ける。

| 操作 | 組織 ADMIN | 組織 DEPUTY_ADMIN | 組織 MEMBER | チーム ADMIN | チーム DEPUTY_ADMIN | 権限を付与されたチームメンバー（DEPUTY／MEMBER） | 付与されていないチームメンバー（DEPUTY／MEMBER 以下） | 他組織の ADMIN | SYSTEM_ADMIN |
|---|---|---|---|---|---|---|---|---|---|
| 申請受付設定の閲覧・変更 | ✅ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ✅（閲覧のみ・監査用） |
| 受付状況（受付中か）の閲覧 | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅（閲覧のみ・申請ボタンは出ない） | ✅ | ✅ |
| チームから組織へ申請・取下げ | — | — | — | ✅ | 付与時のみ | ✅ | ❌ | — | ❌ |
| 招待の承諾・拒否 | — | — | — | ✅ | 付与時のみ | ✅ | ❌ | — | ❌ |
| 離脱（チーム側） | — | — | — | ✅ | 付与時のみ | ✅ | ❌ | — | ❌ |
| チームが止めている招待の一覧・解除 | — | — | — | ✅ | 付与時のみ | ✅ | ❌ | — | ❌ |
| 加盟操作権限の付与・剥奪 | — | — | — | ✅ | ❌ | ❌ | ❌ | — | ❌ |
| 組織からチームへ招待・取消 | ✅ | ❌ | ❌ | — | — | — | — | ❌ | ❌ |
| 申請の承認・拒否 | ✅ | ❌（§18 で委任を優先検討） | ❌ | — | — | — | — | ❌ | ❌ |
| 除名（組織側） | ✅ | ❌ | ❌ | — | — | — | — | ❌ | ❌ |
| チームグループの閲覧 | ✅ | ✅ | ✅ | 自チームのグループ名のみ | 同左 | 同左 | 同左 | ❌ | ✅ |
| チームグループの作成・変更・削除・並び替え | ✅ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ |
| チームのグループ割当（単体・一括） | ✅ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ |
| 宛先を絞ったお知らせの送信（表示の絞り込み） | ✅ | ✅（MANAGE_CONTENT） | ✅（F02.8 §3 の MEMBER 制限の範囲内） | — | — | — | — | ❌ | ✅ |
| 宛先を絞ったお知らせ（チームを選ぶ・チームグループ）の **push**（アンケートのみ。§8.5） | ✅ | ✅（**MANAGE_CONTENT を持つ場合のみ**。持たない DEPUTY は push なし） | ❌（表示の絞り込みだけ） | — | — | — | — | ❌ | ✅ |

- 組織側の操作（招待・承認・拒否・除名・グループ）は、F01.2 の既存フローに合わせて**組織 ADMIN のみ**とする（マスター確定）。DEPUTY_ADMIN への承認委任は §18 の段階拡張のうち最優先で扱う。
- チーム側の加盟操作をする人は、組織のメンバーである必要はない。
- 以下、本書で「**チームの加盟操作者**」と書いたら、そのチームで `MANAGE_ORG_AFFILIATION` を持つ人（チーム ADMIN は常に含む）を指す。

### 3.2 チーム側の加盟操作権限 `MANAGE_ORG_AFFILIATION`（マスター確定 M1）

| 項目 | 設計 |
|---|---|
| 権限名 | `MANAGE_ORG_AFFILIATION`（表示名「組織への加盟操作」、scope=TEAM） |
| 対象の操作 | 申請・取下げ・招待の承諾／拒否・離脱・チームが止めている招待の一覧と解除・申請フォームの取得・受付中組織での申請ボタンの表示 |
| ADMIN | **常に持つ**。`role_permissions` に ADMIN・`is_default=1` の行を置く（`checkPermission` 経路は `is_default=1` の行に依存し、暗黙の全権ではないため）|
| DEPUTY_ADMIN・MEMBER | **付与されたときだけ持つ**。付与は既存の権限グループ（`permission_groups`、`target_role` = DEPUTY_ADMIN または MEMBER）に `MANAGE_ORG_AFFILIATION` を含め、そのグループを個別のユーザーに割り当てる（`PUT /api/v1/teams/{slug}/members/{userId}/permission-groups`）。**DEPUTY_ADMIN・MEMBER の `role_permissions` 天井行は作らない**（V183 `add_manage_recruitments_permission` と同じ作法。TEAM の経路は天井行の `is_default` を絞らずに集約するため、行を作ると全 DEPUTY に黙って付与される）|
| 判定 | `AccessControlService.hasPermission(userId, teamId, "TEAM", "MANAGE_ORG_AFFILIATION")`（`RoleService.resolveEffectivePermissions` 経由）。DEPUTY_ADMIN は権限グループ経由、MEMBER は target_role=MEMBER の権限グループ割当経由で真になることを IT で確かめる（AC-P03・P04）|
| 権限名の定義 | 権限名の enum は無いため、定数は加盟の Service に `static final String MANAGE_ORG_AFFILIATION = "MANAGE_ORG_AFFILIATION"` で置く。正本は Flyway の `permissions` への INSERT |
| Flyway | `INSERT INTO permissions ... WHERE NOT EXISTS`（名前・表示名・scope=TEAM）と、ADMIN の `role_permissions`（`is_default=1`）。§12 の8本目 |
| 付与の画面導線 | **チームシェルに新タブ「権限グループ」（`/teams/{slug}/permission-groups`）を追加する**（main 時点でチームの権限グループ API は `TeamController` にあるが、画面が無く `useTeamMembers.ts` の関数も未使用）。組織の `/organizations/{slug}/permission-groups`（`components/organization/OrgPermissionGroupList.vue`）をチームでも使えるよう汎用化して流用する。タブの表示はチーム ADMIN・管理レンズのみ。割当はメンバー一覧（`/teams/{slug}/members`）の各メンバーの操作メニュー「権限グループ」から行う |
| 既定権限画面（`/admin/member-permissions`）との関係 | この画面は MEMBER 既定3権限（MANAGE_SCHEDULES / FILES / POSTS）専用のため、`MANAGE_ORG_AFFILIATION` は載せない（ロール全員への一括付与は想定しない。加盟操作は指定した人だけに任せる）|

---

## 4. 状態遷移とライフサイクル

### 4.1 加盟（`team_org_memberships`）

`team_org_memberships` の行は **PENDING と ACTIVE の2状態だけ**を持つ（既存どおり）。新しく `direction` 列で「誰が起こした PENDING か」を区別する。

```
                         (組織ADMIN が招待)                     (チームADMIN が申請)
                        direction=ORG_INVITE                  direction=TEAM_APPLY
                               │                                       │
                               ▼                                       ▼
                      ┌─────────────────┐                     ┌─────────────────┐
                      │ PENDING         │                     │ PENDING         │
                      │ (ORG_INVITE)    │                     │ (TEAM_APPLY)    │
                      └─────────────────┘                     └─────────────────┘
   チームADMIN が拒否 │  │ 組織ADMIN が取消  │期限切れ   組織ADMIN が拒否 │  │ チームADMIN が取下げ │期限切れ
     (DECLINED)     │  │ (CANCELLED)     │(EXPIRED)   (REJECTED)   │  │ (WITHDRAWN)      │(EXPIRED)
         ▼          ▼  │                 ▼               ▼         ▼  │                  ▼
        [行を物理削除＋必要なら制限を記録]  │              [行を物理削除＋必要なら制限を記録]  │
                        │ チームADMIN が承諾                         │ 組織ADMIN が承認
                        │ (ACCEPTED)                               │ (APPROVED・グループを確定)
                        ▼                                           ▼
                      ┌──────────────────────────────────────────────────────────┐
                      │ ACTIVE（direction は成立経路の記録として保持・group_id 可変）  │
                      └──────────────────────────────────────────────────────────┘
                         │ チームADMIN が離脱 (LEFT)      │ 組織ADMIN が除名 (REMOVED)
                         ▼                               ▼
                               [行を物理削除（既存どおり）]
```

| 遷移 | 起点 | 操作者 | 結果 | 制限の記録（§5.4） | 監査ログ action |
|---|---|---|---|---|---|
| 招待 | なし | 組織 ADMIN | PENDING / ORG_INVITE | — | `TEAM_ORG_INVITE_SENT` |
| 申請 | なし | チームの加盟操作者 | PENDING / TEAM_APPLY | — | `TEAM_ORG_APPLICATION_SUBMITTED` |
| 承諾 | PENDING / ORG_INVITE | チームの加盟操作者 | ACTIVE | — | `TEAM_ORG_MEMBERSHIP_CREATED`（metadata.via=ORG_INVITE） |
| 承認 | PENDING / TEAM_APPLY | 組織 ADMIN | ACTIVE（group_id を確定） | — | `TEAM_ORG_MEMBERSHIP_CREATED`（metadata.via=TEAM_APPLY, group_id） |
| 招待の拒否 | PENDING / ORG_INVITE | チームの加盟操作者 | 行を削除 | ORG_INVITE 方向に30日（`block=true` なら無期限） | `TEAM_ORG_INVITE_REJECTED` |
| 招待の取消 | PENDING / ORG_INVITE | 組織 ADMIN | 行を削除 | ORG_INVITE 方向に24時間（通知の連打防止） | `TEAM_ORG_INVITE_CANCELLED` |
| 申請の拒否 | PENDING / TEAM_APPLY | 組織 ADMIN | 行を削除 | TEAM_APPLY 方向に30日（`block=true` なら無期限） | `TEAM_ORG_APPLICATION_REJECTED` |
| 申請の取下げ | PENDING / TEAM_APPLY | チームの加盟操作者 | 行を削除 | TEAM_APPLY 方向に24時間（通知の連打防止） | `TEAM_ORG_APPLICATION_WITHDRAWN` |
| 期限切れ | PENDING（両方向） | 夜間バッチ | 60日応答が無ければ行を削除し、起こした側へ通知 | なし | `TEAM_ORG_PENDING_EXPIRED` |
| 離脱 | ACTIVE | チームの加盟操作者 | 行を削除 | なし | `TEAM_ORG_MEMBERSHIP_REMOVED`（reason=TEAM_LEFT） |
| 除名 | ACTIVE | 組織 ADMIN | 行を削除 | なし（除名と同時に再申請を止めるオプションは §18） | `TEAM_ORG_MEMBERSHIP_REMOVED`（reason=ORG_REMOVED） |
| グループ変更 | ACTIVE | 組織 ADMIN | group_id を更新 | — | `TEAM_ORG_GROUP_CHANGED`（from, to） |

- ACTIVE 行の `direction` は「どちらの経路で成立したか」の記録であり、以後の振る舞いには影響しない。
- 状態を変える更新はすべて **条件付き UPDATE／DELETE**（`WHERE id=? AND organization_id=? AND status='PENDING' AND direction=?`）で行う。影響行数 0 の扱いは §6.4 の判定表に従う。
- PENDING の期限（60日、`mannschaft.affiliation.pending-ttl-days`）を設けるのは、相手が応答しないまま PENDING が残り、チームの同時申請枠（10件）を埋め続けることを防ぐためである。

### 4.2 物理削除を維持する判断（現行設計の見直し結果）

拒否・取消・取下げ・期限切れ・離脱・除名で行を**物理削除する現行方針は維持する**。そのうえで、再申請の抑止には専用の制限テーブル（§5.4）を、履歴には既存の `audit_logs` を使う。

理由は次のとおり。

1. **読み手を壊さない。** `team_org_memberships` は ScopeAncestorResolver、OrgFanoutRecipientSource、スケジュール、大会、LeagueTransfer、シフト予算など、main 時点で17ファイル・約90箇所から参照されている。REJECTED などの終端状態を行として残すと、`status='ACTIVE'` で絞っていない読み手が1つでもあれば、拒否済みのチームが加盟扱いになる。全読み手の監査と番人の追加が前提になり、加盟の書き込み実装より影響が大きい。
2. **一意制約の意味を保てる。** `UNIQUE (team_id, organization_id)` は「同じ組み合わせで生きている関係は最大1件」を DB で保証している。終端行を残すと、この制約を生成列などで作り直す必要がある。
3. **抑止に必要な情報は少ない。** 再申請やスパムの抑止に要るのは「この組み合わせは、この方向で、いつまで申請（招待）できないか」だけである。これは制限テーブル1行で表せ、解除も1行の削除で済む。
4. **履歴は監査ログで足りる。** 誰がいつ拒否したかの記録は `audit_logs` に残る。業務ルール（冷却期間）は監査ログを参照せず、制限テーブルだけで判定する。

### 4.3 申請受付を off に切り替えたときの PENDING 申請

**受付を off にしても、既に届いている PENDING 申請はそのまま残し、組織 ADMIN は引き続き承認・拒否できる。** チーム側も取下げできる。off にした時点で止まるのは新規の申請だけである。PENDING は §4.1 の期限（60日）で自然に片付く。

- 理由: off は「これ以上受け付けない」という意思表示であり、届いた申請を捨てる意思とは限らない。自動で拒否すると取り消しが効かず、申請したチームにとっても不意打ちになる。
- 設定画面で off に切り替えるとき、PENDING 申請が1件以上あれば「受付済みの申請が N 件残っています。申請一覧から個別に承認・拒否してください」と確認を出す。
- 受付 off の組織でも、申請一覧（組織側）と申請中一覧（チーム側）は表示し続ける。

### 4.4 グループ機能を off にしたとき

`team_groups_enabled` を off にしても、グループと割当（`group_id`）は削除しない。off の間は次のように振る舞い、on に戻せば元の状態に戻る。

- グループ管理画面・割当 UI・お知らせのグループ選択は表示しない。グループ系の書き込み API は 409 `ORG_067` を返す。
- 申請時のグループ選択は「使わない（OFF）」として扱う（保存値の `application_group_mode` は変えない）。
- 加盟チーム一覧のグループ列は表示しない。
- 送信済みのグループ宛てお知らせの表示判定は、off の間も保存された条件で続ける（off は新規送信を止めるだけで、過去のお知らせの宛先を変えない）。

### 4.5 組織・チームの削除とアーカイブ時の片付け

| 契機 | PENDING 行 | ACTIVE 行 | チームグループ | 制限（§5.4） | 通知 |
|---|---|---|---|---|---|
| 組織の論理削除 | 削除（既存フロー step 5 の `DELETE WHERE organization_id=?` に含まれる） | 削除（既存どおり） | 同一トランザクションで全件を論理削除（同じ organization ドメイン） | `OrganizationDeletedEvent`（AFTER_COMMIT）で team ドメインが当該組織の行を物理削除 | PENDING の相手側 ADMIN へ「組織が削除されたため、申請（招待）は取り消されました」 |
| 組織のアーカイブ | 削除（新規。申請・招待を受け付けない状態で PENDING を残すと、チームの申請枠を埋め続けるため。main にアーカイブのイベントは無いため、アーカイブ処理の Service から加盟の Service を呼ぶか、`TeamOrgLifecycleCleanupBatch` で片付ける）| 既存のアーカイブ規則どおり（残す） | 残す（アーカイブ解除で元に戻る） | 残す | PENDING の相手側 ADMIN へ同上 |
| 組織のアーカイブ解除 | 復元しない | — | そのまま | そのまま | なし |
| チームの論理削除 | 削除（`TeamDeletedEvent` AFTER_COMMIT で当該チームの PENDING・ACTIVE を削除。既存のチーム削除フローに追加） | 削除 | — | 当該チームの行を物理削除 | PENDING の相手側（組織）ADMIN へ同上 |
| チームのアーカイブ | 削除（同上の理由） | 残す（既存規則どおり） | — | 残す | 同上 |

- 申請・招待・承認・承諾の各操作は、**実行時に組織とチームの状態を再確認する**（§6.2 step 3）。画面を開いた後に相手が削除・アーカイブされた場合、状態変更は行わず、削除済みは 404、アーカイブ済みは既存のアーカイブ拒否（422）を返す。
- 片付けはイベント経由の非同期処理なので、ごく短い間 PENDING 行が残り得る。承認時の再確認があるため、この間に誤って ACTIVE になることはない。

### 4.6 時刻の扱い

- 本機能の日時列（`team_org_memberships.invited_at` / `responded_at` / `updated_at`、`team_org_affiliation_restrictions.restricted_until` / `created_at`、`org_team_groups.deleted_at`）はすべて `DATETIME`（`LocalDateTime`）で、**アプリの壁時計（JST）**で書く。JVM 既定ゾーンは `TimeZoneConfig` で JST に固定されている。
- 現在時刻は **`@Qualifier("wallClock") Clock`**（`ClockConfig.wallClock()`）から `LocalDateTime.now(wallClock)` で取り、**判定・更新クエリには引数 `:now` として渡す**。SQL の `NOW()`・`CURRENT_TIMESTAMP` は判定・更新の条件式に使わない（DB の時計とアプリの時計がずれても結果が変わらないようにし、IT で `wallClock` を固定 Clock に差し替えれば時間を進められるようにするため）。列の `DEFAULT CURRENT_TIMESTAMP` は、アプリが値を渡し忘れたときの保険として残してよい。
- `@Primary` の `utcClock` は使わない（壁時計の列と比べると9時間ずれる）。
- 実機で時間を進めるときは、**DB の日時列を過去へずらす準備**をしたうえで、**バッチを手動起動**（`POST /api/v1/system-admin/batch/{name}/trigger?sync=true`、SYSTEM_ADMIN）する（§16.0）。テスト用の Clock 変更エンドポイントは作らない。

---

## 5. DB 設計

### 5.1 所属ドメインと Repository の方針

| テーブル | ドメイン（パッケージ） | テナント列 | Repository |
|---|---|---|---|
| `org_team_groups`（新規） | organization（`com.mannschaft.app.organization.teamgroup`） | `organization_id` | `AbstractTenantAwareRepository<OrgTeamGroupEntity, UUID>` を継承する |
| `team_org_memberships`（既存・列追加） | team（既存 `TeamOrgMembershipEntity` の所在に合わせる） | `organization_id` | 既存 `TeamOrgMembershipRepository`（**`AbstractTenantAwareRepository` は継承しない**。理由は下記） |
| `team_org_affiliation_restrictions`（新規） | team（加盟と同じドメイン） | `organization_id` | 新規 `TeamOrgAffiliationRestrictionRepository`（**継承しない**。理由は下記） |
| `organizations`（既存・列追加） | organization | — | 既存 |
| `announcement_feeds` / `announcement_range_templates`（既存・列追加） | social | — | 既存 |
| `notification_fanout_job_target_teams`（新規） | notification | — | 新規（job 子表） |

**`AbstractTenantAwareRepository` を `team_org_memberships` と `team_org_affiliation_restrictions` に適用しない理由**

1. `AbstractTenantAwareRepository` は `findByOrganizationIdAndDeletedAtIsNull` / `findByIdAndOrganizationIdAndDeletedAtIsNull` を宣言しており、`deleted_at` 列を前提にしている。両テーブルは物理削除で管理し（§4.2）、`deleted_at` 列を持たない。継承すると派生クエリの生成で起動に失敗する。
2. 両テーブルは**チーム側から組織を横断して引く**正当な読み手を持つ（チームの所属組織一覧、チームの申請中一覧、チームが止めている招待の一覧）。これらは `team_id` が起点で、1つの `organization_id` に閉じない。

代わりに、組織側から引くクエリは必ず `organization_id` を条件に含め、チーム側から引くクエリは必ず `team_id` を条件に含める。ID 指定の取得は `(id, organization_id)` または `(id, team_id)` の組でだけ行う。これを `TeamAffiliationScopeContractIT`（非メンバー 403・越境 404・正当成功）で担保する（§12）。

**クロスドメイン FK**

- `team_org_memberships.group_id` は organization ドメインの `org_team_groups.id` を指すが、**クロスドメイン FK は張らない**（原則1）。インデックスだけを張り、整合性はアプリ層で保証する（グループ削除後の非同期の付け替え。§7.3）。
- 読み手は、`group_id` が削除済みグループを指していても「未分類」として扱う（`org_team_groups.deleted_at IS NULL` で JOIN する）。付け替えが非同期なので、この防御は必須である。
- 新規テーブルは `UuidV7Entity` を継承し、主キーは `BINARY(16)`（原則6）。照合順序は `utf8mb4_0900_ai_ci`（列単位の COLLATE 上書きはしない）。

### 5.2 新規 `org_team_groups`

```sql
-- 組織に加盟するチームを区分する「チームグループ」（F01.2.1）。平坦・並び順付き・論理削除
CREATE TABLE org_team_groups (
    id              BINARY(16)       NOT NULL COMMENT 'UUIDv7',
    organization_id BIGINT UNSIGNED  NOT NULL COMMENT '組織ID（クロスドメインFKなし）',
    name            VARCHAR(50)      NOT NULL COMMENT '表示名（例: 平成20年度卒）',
    description     VARCHAR(200)     NULL     COMMENT '補足説明（申請時の選択肢にも表示）',
    sort_order      INT              NOT NULL COMMENT '並び順（昇順）。範囲指定の基準',
    created_by      BIGINT UNSIGNED  NULL,
    updated_by      BIGINT UNSIGNED  NULL,
    created_at      DATETIME         NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at      DATETIME         NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    deleted_at      DATETIME         NULL,
    -- 生存行だけで名前の重複を禁じるための生成列（削除済みは NULL になり UNIQUE の対象外）
    active_name     VARCHAR(50)      GENERATED ALWAYS AS (IF(deleted_at IS NULL, name, NULL)) STORED,
    PRIMARY KEY (id),
    UNIQUE KEY uq_org_team_groups_org_active_name (organization_id, active_name),
    INDEX idx_org_team_groups_org_sort (organization_id, deleted_at, sort_order)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci
  COMMENT='チームグループ（組織内のチーム区分）';
```

| 項目 | 仕様 |
|---|---|
| 名前 | 前後の空白を除いて 1〜50 文字（コードポイント数）。生存グループ内で重複不可（照合順序により大文字小文字・全半角の一部は同一視される）|
| 上限 | 1組織あたり生存グループ **200 件**。超えると 422 `ORG_066` |
| `sort_order` | 作成時は「現在の最大値 + 1」。並び替えは全件の順序を一括で受け取り、0 から振り直す。同じ値の重複は DB では禁じず、サービス層で並び替え時に解消する |
| 削除 | 論理削除（`deleted_at`）。削除済みグループは一覧・選択肢・範囲計算・表示判定のすべてから除外する |
| 階層 | 持たない（親グループ列なし） |

### 5.3 既存 `team_org_memberships` への列追加

```sql
ALTER TABLE team_org_memberships
    ADD COLUMN direction  VARCHAR(20)  NOT NULL DEFAULT 'ORG_INVITE'
        COMMENT '起点: ORG_INVITE=組織からの招待 / TEAM_APPLY=チームからの申請',
    ADD COLUMN group_id   BINARY(16)   NULL
        COMMENT 'チームグループID（org_team_groups.id・クロスドメインFKなし）。PENDING/TEAM_APPLY では希望グループ',
    ADD COLUMN message    VARCHAR(500) NULL
        COMMENT '申請・招待時の添え書き（PENDING の間だけ意味を持つ）',
    ADD COLUMN updated_at DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    ADD CONSTRAINT chk_team_org_memberships_direction CHECK (direction IN ('ORG_INVITE','TEAM_APPLY')),
    ADD INDEX idx_team_org_memberships_org_status_dir (organization_id, status, direction, invited_at),
    ADD INDEX idx_team_org_memberships_team_status_dir (team_id, status, direction),
    ADD INDEX idx_team_org_memberships_org_group_status (organization_id, group_id, status),
    ADD INDEX idx_team_org_memberships_status_invited (status, invited_at);
```

| 列 | 既存行の値 | 備考 |
|---|---|---|
| `direction` | `ORG_INVITE`（DEFAULT で一括設定） | 既存フローは組織からの招待だけだったため |
| `group_id` | `NULL`（未分類） | 組織 ADMIN が後から割り当てる（単体・一括） |
| `message` | `NULL` | ACTIVE への遷移時に NULL へ戻す（PII を不要に残さない） |
| `updated_at` | 移行時刻 | F01.2 01_db_design は `updated_at` を記載していたが V2.011 に無かった乖離をここで解消する |

| 索引 | 使う読み手 |
|---|---|
| `(organization_id, status, direction, invited_at)` | 組織側の申請一覧・招待一覧 |
| `(team_id, status, direction)` | チーム側の申請中・受信招待一覧、同時申請数の数え上げ、チームの所属組織（ACTIVE）の解決 |
| `(organization_id, group_id, status)` | グループ絞り込みの加盟チーム一覧、グループごとの所属数、お知らせのグループ展開（ACTIVE のみを索引内で絞る）|
| `(status, invited_at)` | PENDING 期限切れバッチ |

- 既存の `invited_by` / `invited_at` は「PENDING 行を作った人・時刻」として両方向で使う（TEAM_APPLY では申請したチーム ADMIN）。`responded_by` / `responded_at` は「承諾・承認した人・時刻」。
- `status` は PENDING / ACTIVE の2値のまま変えない（§4.2）。

### 5.4 新規 `team_org_affiliation_restrictions`（再申請・再招待の抑止）

```sql
-- 加盟申請・招待の再送を抑止する制限（拒否後の冷却・ブロック・連打防止）（F01.2.1）
CREATE TABLE team_org_affiliation_restrictions (
    id               BINARY(16)       NOT NULL COMMENT 'UUIDv7',
    organization_id  BIGINT UNSIGNED  NOT NULL COMMENT '組織ID（クロスドメインFKなし）',
    team_id          BIGINT UNSIGNED  NOT NULL COMMENT 'チームID（既存方針に合わせFKは張らない）',
    direction        VARCHAR(20)      NOT NULL COMMENT '止める向き: TEAM_APPLY=チームからの申請 / ORG_INVITE=組織からの招待',
    kind             VARCHAR(20)      NOT NULL COMMENT 'COOLDOWN=期限付き / BLOCK=無期限ブロック',
    reason           VARCHAR(20)      NOT NULL COMMENT 'REJECTED / DECLINED / WITHDRAWN / CANCELLED',
    restricted_until DATETIME         NULL     COMMENT 'COOLDOWN の期限（アプリの壁時計・JST）。BLOCK は NULL',
    created_by       BIGINT UNSIGNED  NULL,
    created_at       DATETIME         NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at       DATETIME         NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    PRIMARY KEY (id),
    UNIQUE KEY uq_toar_org_team_dir (organization_id, team_id, direction),
    INDEX idx_toar_team_dir (team_id, direction),
    INDEX idx_toar_kind_until (kind, restricted_until),
    CONSTRAINT chk_toar_direction CHECK (direction IN ('TEAM_APPLY','ORG_INVITE')),
    CONSTRAINT chk_toar_kind CHECK (kind IN ('COOLDOWN','BLOCK')),
    CONSTRAINT chk_toar_reason CHECK (reason IN ('REJECTED','DECLINED','WITHDRAWN','CANCELLED')),
    CONSTRAINT chk_toar_until CHECK ((kind = 'BLOCK' AND restricted_until IS NULL)
                                  OR (kind = 'COOLDOWN' AND restricted_until IS NOT NULL))
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci
  COMMENT='チーム加盟の申請・招待の再送制限';
```

| 仕様 | 内容 |
|---|---|
| 拒否後の冷却 | 申請の拒否（REJECTED）・招待の拒否（DECLINED）で、その向きに **30日**（`mannschaft.affiliation.reject-cooldown-days`）止める |
| 連打防止の冷却 | 申請の取下げ（WITHDRAWN）・招待の取消（CANCELLED）で、その向きに **24時間**（`mannschaft.affiliation.resend-cooldown-hours`）止める。取下げ→再申請の繰り返しで相手に通知が連打されるのを防ぐ |
| 既存行との合成 | UPSERT で1行にまとめる。BLOCK は COOLDOWN で上書きしない。COOLDOWN どうしは期限の遅いほうを残す |
| ブロック | 拒否時に `block=true` を付けると、期限なしで止める。解除は止めた側の ADMIN が一覧から行う |
| 判定 | `kind='BLOCK' OR restricted_until > :now`（`:now` は `wallClock` の現在時刻。§4.6）の行があれば拒否（403 `TEAM_068`）。期限切れの行は判定で無視し、夜間バッチで物理削除する |
| 相手に見せる情報 | 冷却中かブロックかは相手に**区別して見せない**（どちらも「現在この組織には申請できません」）。止めた側の ADMIN だけが種別・理由・期限を見られる |
| 解除一覧に出す行 | 止めた側が意思を持って作った REJECTED・DECLINED の行だけ。WITHDRAWN・CANCELLED（自分の操作で自分側が止まる24時間）は一覧に出さない |
| 除名・離脱 | 制限を自動では作らない |

### 5.5 組織設定の保存先: `organizations` への列追加

組織の設定は、専用の設定テーブルを持たず `organizations` の列として保存している（`supporter_enabled`、`public_events_enabled`、`timeline_posts_public` など）。これに合わせ、次の列を追加する。

```sql
ALTER TABLE organizations
    ADD COLUMN team_application_enabled   BOOLEAN      NOT NULL DEFAULT FALSE
        COMMENT 'チームからの加盟申請を受け付けるか（既定 off）',
    ADD COLUMN team_groups_enabled        BOOLEAN      NOT NULL DEFAULT FALSE
        COMMENT 'チームグループ機能を使うか',
    ADD COLUMN team_application_group_mode VARCHAR(10) NOT NULL DEFAULT 'OFF'
        COMMENT '申請時のグループ選択: OFF / OPTIONAL / REQUIRED',
    ADD COLUMN team_application_guidance  VARCHAR(500) NULL
        COMMENT '申請フォームに表示する案内文（例: 卒業年度のグループを選んでください）',
    ADD CONSTRAINT chk_organizations_team_app_group_mode
        CHECK (team_application_group_mode IN ('OFF','OPTIONAL','REQUIRED'));
```

- boolean 列には単独の索引を張らない（選択性が低く、公開組織検索では他の条件と組み合わせて絞るため）。

| 設定 | 実効値の決まり方 |
|---|---|
| 申請受付 | `team_application_enabled` そのまま |
| 申請時のグループ選択 | `team_groups_enabled = FALSE` のときは保存値にかかわらず **OFF** として扱う |
| REQUIRED の前提 | 保存時に「グループ機能 on かつ生存グループ1件以上」を満たさなければ 422 `ORG_070`。運用中に全グループを削除した場合、REQUIRED は実効 OPTIONAL に落とし、設定画面に警告を出す |

### 5.6 お知らせ・通知側の列追加（詳細は F02.8 §7.1〜§8）

```sql
ALTER TABLE announcement_feeds
    ADD COLUMN target_group_ids   JSON    NULL
        COMMENT 'グループ宛て: 送信時に範囲を展開したチームグループID（UUID文字列配列）。表示は閲覧時に所属と照合',
    ADD COLUMN include_unassigned BOOLEAN NOT NULL DEFAULT FALSE
        COMMENT 'グループ宛て: 未分類チームも表示対象にするか',
    ADD COLUMN target_audience    JSON    NULL
        COMMENT '送信時の宛先指定の記録（グループ名・範囲・push 宛先数）。表示判定には使わない',
    ADD INDEX idx_af_target_groups ((CAST(target_group_ids->'$[*]' AS CHAR(36) ARRAY)));

ALTER TABLE announcement_range_templates
    ADD COLUMN target_group_ids   JSON    NULL COMMENT '個別選択したチームグループID（UUID文字列配列）',
    ADD COLUMN target_group_range JSON    NULL COMMENT '並び順の範囲指定 {"from_group_id":..,"to_group_id":..}',
    ADD COLUMN include_unassigned BOOLEAN NOT NULL DEFAULT FALSE COMMENT '未分類チームを含めるか';

-- グループ宛てお知らせの送信時スナップショット（グループ削除後の表示判定に使う。F01.2.1 §8.2）
CREATE TABLE announcement_feed_group_snapshots (
    feed_id  BIGINT UNSIGNED NOT NULL COMMENT 'announcement_feeds.id（同一ドメイン）',
    group_id CHAR(36)        NOT NULL COMMENT '送信時に展開したチームグループID（UUID文字列・クロスドメインFKなし）',
    team_id  BIGINT UNSIGNED NOT NULL COMMENT '送信時点でそのグループに ACTIVE で所属していたチームID',
    PRIMARY KEY (feed_id, group_id, team_id),
    INDEX idx_afgs_team_feed (team_id, feed_id),
    CONSTRAINT fk_afgs_feed FOREIGN KEY (feed_id) REFERENCES announcement_feeds (id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci
  COMMENT='グループ宛てお知らせの送信時の対象チーム（グループ単位）';

-- fan-out ジョブの宛先チーム（ORGANIZATION_TEAMS 戦略用の子表）
CREATE TABLE notification_fanout_job_target_teams (
    job_id  BINARY(16)      NOT NULL COMMENT 'notification_fanout_jobs.id（同一ドメイン）',
    team_id BIGINT UNSIGNED NOT NULL COMMENT '宛先チームID（クロスドメインFKなし）',
    PRIMARY KEY (job_id, team_id),
    CONSTRAINT fk_nfjtt_job FOREIGN KEY (job_id) REFERENCES notification_fanout_jobs (id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci
  COMMENT='ORGANIZATION_TEAMS fan-out の宛先チーム（送信時点のスナップショット）';
```

> `notification_fanout_jobs.id` は `BINARY(16)`（UUIDv7。V173）であり、子表の `job_id` も `BINARY(16)` とする。同一ドメイン内の CASCADE なので原則2に反しない。
> `target_group_ids` の多値インデックス（`CHAR(36) ARRAY`）は、UUID 文字列で保存する前提である。
> `announcement_feed_group_snapshots` は social ドメインの表で、`announcement_feeds` と同じドメインなので CASCADE を張れる（原則2）。行数は「グループ宛てお知らせ1件あたりの送信時の対象チーム数」で、件数上限は掛けない（1行が小さく、JSON の多値インデックスのような1レコードあたりの上限が無いため）。
> `announcement_feeds.id` は `BIGINT UNSIGNED`（AUTO_INCREMENT。V13.019）であり、子表の `feed_id` も `BIGINT UNSIGNED` とする。

---

## 6. 加盟のビジネスロジック

### 6.1 チームからの申請

```
POST /api/v1/teams/{teamSlug}/org-applications   body: {organizationSlug, groupId?, message?}
1. チームが存在・未削除か → 無ければ 404（既存のチーム不在コード）
2. 操作者がそのチームで加盟操作権限（`MANAGE_ORG_AFFILIATION`。§3.2）を持つか → 無ければ 403
3. チームのアーカイブ確認 → アーカイブ済みなら既存のアーカイブ拒否（422）
4. 組織が存在・未削除で、操作者から visibility 上見えるか → 見えなければ 404（不在と同じコード）
5. 組織のアーカイブ確認 → 既存のアーカイブ拒否（422）
6. 組織の team_application_enabled が TRUE か → FALSE なら 403 TEAM_064
7. 制限（TEAM_APPLY 方向）の有無 → あれば 403 TEAM_068
8. 同じ (team, org) の行があるか
     ACTIVE → 409 TEAM_065 / PENDING(TEAM_APPLY) → 409 TEAM_066 / PENDING(ORG_INVITE) → 409 TEAM_066（「招待が届いています」の案内付き）
9. チームの PENDING(TEAM_APPLY) 件数が 10 以上 → 422 TEAM_069
10. グループ選択の検証（実効モード）
     OFF: group_id を指定されたら 400 TEAM_072（黙って捨てない）
     OPTIONAL: 指定時のみ、その組織の生存グループか検証 → 違えば 400 TEAM_072
     REQUIRED: 未指定なら 400 TEAM_067、指定時は OPTIONAL と同じ検証
11. INSERT（status=PENDING, direction=TEAM_APPLY, group_id, message, invited_by=操作者, invited_at=:now。§4.6）
    一意制約違反（並行申請）は 409 TEAM_066 に写像する
12. 組織の ADMIN 全員へ通知（`TEAM_ORG_APPLICATION_RECEIVED`。§6.7）
13. audit_logs: TEAM_ORG_APPLICATION_SUBMITTED
14. 201 Created
```

- 1チームが**複数の組織へ同時に申請・加盟してよい**（§9）。step 8 の重複判定は同じ組み合わせ（team, org）だけを見る。

### 6.2 組織による承認

```
POST /api/v1/organizations/{slug}/team-applications/{membershipId}/approve   body: {overrideGroup: boolean, groupId?: UUID|null}
1. 組織の存在・未削除 → 404 / 操作者が組織 ADMIN か → 403
2. 行を (id, organization_id, status=PENDING, direction=TEAM_APPLY) で取得 → §6.4 の判定表
3. 状態の再確認: 組織がアーカイブ済み → 422（既存のアーカイブ拒否）。
   チームが削除済み → 404 TEAM_070（行は §4.5 の片付けで消える）、チームがアーカイブ済み → 422
4. 確定グループを決める（リクエストの overrideGroup で区別する。§10.5）
     overrideGroup=false → 申請時の group_id をそのまま使う（削除済みグループなら NULL）
     overrideGroup=true かつ groupId が UUID → その組織の生存グループか検証（違えば 400 TEAM_072）
     overrideGroup=true かつ groupId が null → 未分類で承認
     グループ機能 off → 常に NULL
5. 条件付き UPDATE: status=ACTIVE, group_id=確定値, message=NULL, responded_by, responded_at=:now
   影響行数 0 → §6.4 の判定表
6. チームの加盟操作者全員へ通知（`TEAM_ORG_APPLICATION_APPROVED`。§6.7）
7. audit_logs: TEAM_ORG_MEMBERSHIP_CREATED（via=TEAM_APPLY, requested_group_id, group_id）
8. 200 OK（加盟1件の表現を返す）
```

- グループ選択が REQUIRED の組織でも、承認時に組織 ADMIN が「未分類で承認」を選ぶことは許す（REQUIRED は申請者への入力要求であり、管理者の判断を縛らない）。
- 承諾（招待を受けたチームが承諾する）でも同じ step 3 の再確認を行う。

### 6.3 組織による拒否

```
POST /api/v1/organizations/{slug}/team-applications/{membershipId}/reject   body: {reason?: string(≤500), block?: boolean}
1〜2. 承認と同じ
3. 条件付き DELETE（PENDING かつ TEAM_APPLY）→ 影響行数 0 なら §6.4 の判定表
4. 制限を UPSERT（direction=TEAM_APPLY, reason=REJECTED, kind=block ? BLOCK : COOLDOWN, restricted_until=block ? NULL : :now+30日）
5. チームの加盟操作者全員へ通知（`TEAM_ORG_APPLICATION_REJECTED`。reason 指定時は理由入りの本文。§6.7）
6. audit_logs: TEAM_ORG_APPLICATION_REJECTED（reason, block）
7. 200 OK
```

### 6.4 チームによる取下げと、競合時の応答の判定表

```
DELETE /api/v1/teams/{teamSlug}/org-applications/{membershipId}
1. チームの存在 → 404 / 操作者がそのチームの加盟操作者か → 403
2. 条件付き DELETE（id, team_id, PENDING, TEAM_APPLY）→ 影響行数 0 なら下の判定表
3. 制限を UPSERT（direction=TEAM_APPLY, reason=WITHDRAWN, kind=COOLDOWN, restricted_until=:now+24時間）
4. audit_logs: TEAM_ORG_APPLICATION_WITHDRAWN
5. 204 No Content
```

membershipId を取る操作の応答は、**操作時点の行の状態だけ**で決まる（先にどちらが来たかで一意に決まる）。

| 操作時点の行（`id` と自スコープの組で引く） | 応答 |
|---|---|
| 行が無い（存在しない ID・他組織／他チームの ID・既に削除済み） | 404 `TEAM_070` |
| 行はあるが、状態または向きが操作の前提と違う（例: 既に ACTIVE の行を取り下げる・招待行を承認する） | 409 `TEAM_071` |
| 前提どおり | 成功 |

例（AC-B14）: 取下げ→承認の順なら、承認は 404 `TEAM_070`。承認→取下げの順なら、取下げは 409 `TEAM_071`。

### 6.5 組織からの招待・承諾・拒否・取消（既存設計の正式化）

F01.2 03_business_logic の①〜④を、次の点を改めたうえで正とする。

| 点 | 改める内容 |
|---|---|
| パス | すべて slug（`{slug}` / `{teamSlug}`）。F01.2 の `{orgId}` / `{teamId}` 表記は廃止 |
| 招待の body | `{teamSlug, groupId?, message?}`。`groupId` はグループ機能 on のときだけ受け付け、承諾時にそのまま確定グループになる |
| 招待先の可視性 | 招待する組織 ADMIN から、チームの visibility 上**見えないチーム**は、存在しない slug と**同じステータス・同じエラーコード**（既存のチーム不在 404）を返す。以降の判定（制限・既存加盟・申請の有無）は、見えるチームに対してだけ行う。これにより、非公開チームの存在・加盟状況・制限状況を応答の違いから推測できない。非公開チームはチーム側から申請してもらう |
| 招待前の検証 | 見えるチームに対して: 制限（ORG_INVITE 方向）があれば 403 `TEAM_068`。既に加盟済みなら 409 `TEAM_065`。相手チームから申請（PENDING/TEAM_APPLY）が来ていれば 409 `TEAM_066`（申請一覧から承認するよう案内） |
| 招待の拒否 | body `{block?: boolean}`。行を削除し、ORG_INVITE 方向に30日の冷却（またはブロック）を記録する（reason=DECLINED） |
| 招待の取消 | `DELETE /organizations/{slug}/team-invites/{teamSlug}`。ORG_INVITE 方向に24時間の冷却（reason=CANCELLED） |
| 承諾 | 条件付き UPDATE。§6.2 step 3 と同じ状態の再確認を行う。承諾時にグループは変えられない（グループは組織の管理事項のため）|
| チームの受付 | チーム側に「招待の受付可否」の設定は設けない。不要な招待はブロックで止める（§18 で再検討）|

### 6.6 離脱・除名

F01.2 の⑤⑥を slug パスに改めて正とする（`DELETE /teams/{teamSlug}/organizations/{orgSlug}`、`DELETE /organizations/{slug}/teams/{teamSlug}`）。行を物理削除すると `group_id` も一緒に消える。再加盟したときは未分類から始まる。1チームが複数の組織に加盟している場合、離脱・除名は指定した1組織との関係だけを消し、他の組織との関係には影響しない。

### 6.7 通知の宛先と量

加盟の通知は、組織側なら**組織 ADMIN**、チーム側なら**チームの加盟操作者**（チーム ADMIN と `MANAGE_ORG_AFFILIATION` を付与されたメンバー）に送る（1件の操作で数名）。人数が少ないので `NotificationHelper.notify` による同期送信で足り、fan-out ジョブは使わない。文面キーは §14.2。

| `notification_type` | 契機 | 受信者 | `action_url` | `scope_type` / `scope_id` |
|---|---|---|---|---|
| `TEAM_ORG_APPLICATION_RECEIVED` | 申請（§6.1）| 組織 ADMIN | `/organizations/{orgSlug}/member-teams?view=applications` | ORGANIZATION / 組織ID |
| `TEAM_ORG_APPLICATION_APPROVED` | 承認（§6.2）| チームの加盟操作者 | `/teams/{teamSlug}/affiliations` | TEAM / チームID |
| `TEAM_ORG_APPLICATION_REJECTED` | 拒否（§6.3）| チームの加盟操作者 | `/teams/{teamSlug}/affiliations?view=applications` | TEAM / チームID |
| `TEAM_ORG_INVITE_RECEIVED` | 招待（§6.5）| チームの加盟操作者 | `/teams/{teamSlug}/affiliations?view=invites` | TEAM / チームID |
| `TEAM_ORG_INVITE_ACCEPTED` | 承諾（§6.5）| 組織 ADMIN | `/organizations/{orgSlug}/member-teams` | ORGANIZATION / 組織ID |
| `TEAM_ORG_PENDING_EXPIRED` | 期限切れ（§6.8）| 起こした側（申請なら申請チームの加盟操作者、招待なら組織 ADMIN）| 申請: `/teams/{teamSlug}/affiliations?view=applications`、招待: `/organizations/{orgSlug}/member-teams?view=invites` | 起こした側のスコープ |
| `TEAM_ORG_PENDING_CANCELLED_BY_SYSTEM` | 相手の削除・アーカイブによる取消（§4.5）| 残った側（申請なら申請チームの加盟操作者、招待なら組織 ADMIN）| 同上 | 同上 |

- `action_url` は slug を使う（URL 識別子は slug に一本化する方針。数値 ID の `action_url` は既存の一部にあるが踏襲しない）。
- `source_type` は `TEAM_ORG_MEMBERSHIP`、`source_id` は membershipId（期限切れ・取消の後は行が無いが、通知の記録として残す）。

### 6.8 PENDING の期限切れバッチ

- 夜間バッチ `TeamOrgPendingExpiryBatch` が `status='PENDING' AND invited_at < :now - 60日`（`:now` は `wallClock`。§4.6）の行を物理削除し、起こした側へ `TEAM_ORG_PENDING_EXPIRED`（§6.7）を送る。制限は作らない。
- 同じバッチで、期限切れの COOLDOWN 制限（`kind='COOLDOWN' AND restricted_until < :now`）を物理削除する。
- **本書で新設するバッチはすべて `@BatchEndpoint` を付けて手動起動 API（`POST /api/v1/system-admin/batch/{name}/trigger?sync=true`）に登録する**。実機E2E では DB の日時列を過去へずらしてからこの API で起動する（§16.0）。

| バッチ（クラス） | `@BatchEndpoint(name=...)` | 定時実行 | 処理 |
|---|---|---|---|
| `TeamOrgPendingExpiryBatch` | `team-org-pending-expiry` | 毎日 03:10（JST）| PENDING の期限切れ削除と通知、期限切れ COOLDOWN 制限の削除 |
| `TeamOrgGroupOrphanRepairBatch` | `team-org-group-orphan-repair` | 毎日 03:20（JST）| 削除済みグループを指す `group_id` を NULL に戻す（§7.3 の取りこぼしの修復）|
| `TeamOrgLifecycleCleanupBatch` | `team-org-lifecycle-cleanup` | 毎日 03:30（JST）| アーカイブ済み・削除済みの組織／チームに残った PENDING と制限を片付ける（§4.5 のイベント処理の取りこぼしの修復）|

---

## 7. チームグループのビジネスロジック

### 7.1 作成・変更

- 作成: 名前の正規化（前後空白除去）→ 重複（生存行）なら 409 `ORG_065` → 生存件数が 200 なら 422 `ORG_066` → `sort_order = max + 1` で INSERT。
- 変更: 名前・説明のみ。並び順は §7.2 の API だけで変える。
- 名前を変えても、送信済みお知らせの記録（`target_audience`）に残した名前は変えない（送信時点の記録のため）。

### 7.2 並び替え

`PUT /organizations/{slug}/team-groups/order` で、生存グループ全件の ID を新しい順に受け取る。受け取った集合が現在の生存グループ集合と一致しなければ 409 `ORG_068`（他の管理者が同時に追加・削除した）。一致すれば 0 から振り直す。

### 7.3 削除（越境トランザクションを使わない）

```
DELETE /organizations/{slug}/team-groups/{groupId}
1. 組織 ADMIN 検証・グループ機能 on 検証
2. (id, organization_id, deleted_at IS NULL) で取得 → 無ければ 404 ORG_064
3. org_team_groups.deleted_at = :now（wallClock。organization ドメインのトランザクションだけで完結）
4. OrgTeamGroupDeletedEvent(organizationId, groupId) を発行
5. audit_logs: ORG_TEAM_GROUP_DELETED
6. 204 No Content

[team ドメイン] TeamOrgGroupUnassignListener（@TransactionalEventListener(AFTER_COMMIT) + @Async）
  UPDATE team_org_memberships SET group_id = NULL
   WHERE organization_id = ? AND group_id = ?        -- ACTIVE・PENDING とも
  冪等（何度実行しても同じ結果）。失敗時はログに残し、夜間バッチ TeamOrgGroupOrphanRepairBatch
  （@BatchEndpoint name=team-org-group-orphan-repair。§6.8）が「削除済みグループを指す group_id」を拾って NULL に戻す
```

- 付け替えが非同期なので、ごく短い間 `group_id` が削除済みグループを指す。**すべての読み手は `org_team_groups.deleted_at IS NULL` で JOIN し、削除済みを指す行を「未分類」として扱う**（§5.1）。このため、付け替え前後で画面・判定の結果は変わらない。
- 削除の確認ダイアログで「所属している N チームは未分類に戻ります」と件数を示す。
- 送信済みのグループ宛てお知らせ（`target_group_ids` に削除済みグループを含むもの）は、**削除済みのグループについては送信時スナップショット（`announcement_feed_group_snapshots`）で表示を判定する**（§8.2）。そのため、削除したグループ宛ての過去のお知らせは、**送信時点でそのグループの対象だったチームに表示され続ける**（御裁可済み #6）。
- 範囲テンプレートは削除時に書き換えない（social ドメインへ波及させない）。テンプレートを使うときにサーバー側で解決する（§8.6）。

### 7.4 割当

| 操作 | API | 内容 |
|---|---|---|
| 単体 | `PUT /organizations/{slug}/teams/{teamSlug}/team-group` body `{groupId: UUID\|null}` | ACTIVE の加盟1件の group_id を変更。null で未分類 |
| 一括 | `PUT /organizations/{slug}/team-group-assignments` body `{groupId: UUID\|null, teamSlugs: string[1..500]}` | 指定チームをまとめて同じグループへ（または未分類へ）。1件でもその組織の ACTIVE 加盟でないチームを含めば、**全体を** 400 `ORG_069` で拒否し、該当 slug を返す（部分成功にしない） |

- 既存加盟チームは移行時に全件が未分類（`group_id = NULL`）になる。組織 ADMIN は加盟チーム一覧で「未分類」に絞り込み、一括割当で振り分ける。
- 割当を変えると、グループ宛てお知らせの**表示**は閲覧時の所属で判定するため、変更後の所属に従う（§8.2）。送信済みの push には影響しない。

---

## 8. グループ宛てお知らせ（F02.8 との接続）

### 8.1 宛先の指定方法

組織スコープの告知ウィザード（ステップ1）の「対象チーム」を、次の3つから選べるようにする。グループ機能 off の組織では、従来どおり「すべて／チームを選ぶ」の2択である。

| 選び方 | 入力 | 対象 |
|---|---|---|
| すべてのチーム | なし | 制限なし（`target_team_ids = NULL`・従来どおり） |
| チームを選ぶ | `targetTeamIds`（1〜5,000件） | 指定チーム（従来どおり） |
| チームグループで選ぶ | `targetGroupIds`（個別チェック）、`targetGroupRange`（範囲）、`includeUnassigned` | 個別チェックしたグループ ∪ 範囲に入るグループ に所属するチーム、∪（`includeUnassigned` なら）未分類のチーム。**加えて、組織の直属メンバー**（どのチームにも属さない組織役員など）|

範囲の定義（`sort_order` の昇順＝グループ管理画面の上から下の並び）:

| UI の表示 | 送る値 | 含まれるグループ |
|---|---|---|
| 「{A} 以前」（先頭から A まで） | `{fromGroupId: null, toGroupId: A}` | `sort_order <= A.sort_order` |
| 「{A} 以降」（A から末尾まで） | `{fromGroupId: A, toGroupId: null}` | `sort_order >= A.sort_order` |
| 「{A} 〜 {B}」 | `{fromGroupId: A, toGroupId: B}` | `A.sort_order <= sort_order <= B.sort_order`。A が B より後ろなら 400 `BROADCAST_008` |

- 「以前／以降」は**グループ管理画面の並び順で上か下か**を意味する。年度のように古い順に並べれば「平成20年度以前」がそのまま読める。画面に「並び順はチームグループ管理で変えられます」と添える。
- 個別チェックと範囲は併用でき、和集合を取る。範囲に未分類は含まれない。
- 同じ送信内で「チームを選ぶ」と「チームグループで選ぶ」は併用しない（400 `BROADCAST_011`）。
- **直属メンバーを含める理由**（マスター確定）: 組織役員などチームに属さない人は、どのグループにも入れようがない。グループ宛てで彼らを外すと、組織の運営者自身にお知らせが届かなくなる。

### 8.2 表示は動的、push は送信時スナップショット（殿の判断・以前のスナップショット推奨を上書き）

| 観点 | 解決する時点 | 保存するもの |
|---|---|---|
| **グループの集合**（範囲をどのグループに展開するか） | 送信時 | 範囲を送信時の `sort_order` で展開したグループ ID 集合を `announcement_feeds.target_group_ids` に保存。未分類の有無は `include_unassigned` |
| **表示**（どのチームのダッシュボードに出るか） | **グループが生きている間は閲覧時（動的）、グループが削除された後は送信時スナップショット** | 送信時に、展開したグループごとの所属チームを `announcement_feed_group_snapshots` に保存する（push の有無にかかわらず、グループ宛ての告知では常に保存）。判定の切替条件は下の表 |
| **push**（誰に通知を送るか） | **送信時（スナップショット）** | 上と同じスナップショットのチーム集合（重複排除）を、fan-out ジョブの子表 `notification_fanout_job_target_teams` に写す |

**表示判定の切替条件**（`target_group_ids` の各グループ g について、閲覧時に `org_team_groups.deleted_at` を見て決める）

| g の状態（閲覧時） | 判定方法 | チームT に表示する条件 |
|---|---|---|
| 生存（`deleted_at IS NULL`） | **動的** | T がその組織に ACTIVE で加盟し、その組織での現在の所属グループが g |
| 削除済み（`deleted_at IS NOT NULL`） | **スナップショット** | `announcement_feed_group_snapshots` に (feed, g, T) の行があり、T がその組織に**今も** ACTIVE で加盟している |
| 未分類（`include_unassigned` が真） | 動的 | T の現在の所属グループが無い（NULL または削除済みを指す）|

- お知らせは、上のいずれか1つでも満たせば T に表示する（OR）。
- 切替はグループごとに独立している。同じお知らせの中で、生存グループは動的、削除済みグループはスナップショットで判定する。
- 送信後に g へ移ってきたチームは、g が生きている間は表示されるが、g の削除後は（スナップショットに無いため）表示されなくなる。送信時に g にいて後で別グループへ移ったチームは、g が生きている間は表示されず、g の削除後は表示される。これは「削除後は送信時点で対象だったチームに見せる」という御裁可の定義どおりの挙動である。

理由:

1. **表示を動的にする理由**: グループの付け忘れ・付け間違いを後から直したとき、直したチームにも過去のお知らせが見えるべきである（例: 平成18年度卒のチームを未分類のまま放置していた）。また、送信時に宛先チーム ID を展開して保存する方式は、大きな組織では1件のお知らせに数千のチーム ID を持たせることになり、多値インデックスの1行あたりのキー数の上限（未検証。§8.3）に近づく。グループ ID で保存すれば、1件あたり最大200件で済み、**グループ指定には5,000件の上限を掛けなくてよい**。
2. **グループの集合を送信時に固める理由**: 「平成20年度以前」と送った意味は、後でグループの並びを変えたりグループを追加したりしても変わるべきではない。
3. **push をスナップショットにする理由**: push は送信時に一度だけ配るものであり、後から所属が変わっても配り直せない。宛先の決定を送信時に確定させるほかない。

この結果、**表示と push で解決時点が異なる**。送信後にグループの所属が変わったチームでは、「push は来なかったが表示される」「push は来たが表示されない」が起こり得る。これは上の理由による意図した挙動であり、UI の送信履歴には「表示は現在の所属で判定されます」と注記する。

- 「チームを選ぶ」（`target_team_ids`）は従来どおり送信時のチーム ID をそのまま保存し、表示もそのチーム ID で判定する（表示の挙動は変えない。push は §8.5.1 で変わる）。

### 8.3 上限と空集合

| 条件 | 扱い |
|---|---|
| 「チームを選ぶ」で5,000チームを超える | 400 `BROADCAST_010`。「すべてのチーム」かチームグループを使うよう案内する |
| グループ指定 | 件数上限なし（グループは1組織200件まで） |
| グループ指定の解決結果が0チームで、直属メンバーもいない | 400 `BROADCAST_009`（「対象になる人がいません」） |
| グループ機能 off の組織でグループ系の項目を指定 | 400 `BROADCAST_007` |
| 他組織・削除済みのグループ ID | 400 `BROADCAST_006`（存在するかどうかを区別しない） |
| TEAM スコープの告知でグループ系の項目を指定 | 400 `BROADCAST_012` |

> **未検証事項**: 「チームを選ぶ」の5,000件は、`announcement_feeds.target_team_ids` の多値インデックスで1行が持てるキー数の上限を意識した値だが、MySQL 8.0 の実際の上限（1レコードあたりの多値キー数・索引レコードサイズ）は**実測していない**。実装の試練で 5,000 件の配列を INSERT し、多値インデックス経由の `JSON_CONTAINS` 検索が成立することを実測し、失敗すれば上限を下げる。

### 8.4 送信前プレビュー

`POST /api/v1/organizations/{orgId}/broadcast/audience-preview`（告知系のため数値 `{orgId}`。§10）で、宛先指定から解決されるチーム数・直属メンバー数と、先頭50件のチーム名を返す。ウィザードは入力が変わるたび（300ms デバウンス）に呼び、「該当: 12チーム＋直属メンバー3人」と表示する。0件のときは「次へ」を押せない。

### 8.5 push 通知の絞り込み

#### 8.5.1 対象になるチャネル（マスター確定）

**push を絞るのは、現在 push を出しているチャネルだけである。** main 時点でお知らせのチャネルのうち組織スコープで push を出しているのは、アンケートの公開通知（`SurveyPublishNotificationListener`、組織スコープ×ALL は `ORGANIZATION` fan-out）だけである。掲示板・タイムライン・ブログ・TODO・スケジュールは、グループで絞っても従来どおり push を出さない（push を新設しない）。

**宛先を絞った告知（「チームを選ぶ」と「チームグループで選ぶ」）は、push も同じ規則で絞る**（御裁可済み #19。「チームを選ぶ」はグループ宛てと同じ規則に揃える）。

| 告知の宛先 | 送信者 | アンケートの push |
|---|---|---|
| すべてのチーム | 誰でも | **従来どおり**（リスナーが `ORGANIZATION` fan-out を1件） |
| チームを選ぶ（`targetTeamIds`） | 組織 ADMIN / DEPUTY_ADMIN(MANAGE_CONTENT) / SYSTEM_ADMIN | **絞る（変更）**: 告知ウィザードが `ORGANIZATION_TEAMS` fan-out を1件出す。宛先は選んだチームのメンバー＋組織の直属メンバー。リスナーの `ORGANIZATION` fan-out は出さない |
| チームを選ぶ（`targetTeamIds`） | 組織 MEMBER | **出さない（変更）**（表示の絞り込みだけ。リスナーの `ORGANIZATION` fan-out も出さない）|
| チームグループで選ぶ | 組織 ADMIN / DEPUTY_ADMIN(MANAGE_CONTENT) / SYSTEM_ADMIN | **絞る**: 告知ウィザードが `ORGANIZATION_TEAMS` fan-out を1件出し、リスナーの `ORGANIZATION` fan-out は出さない |
| チームグループで選ぶ | 組織 MEMBER | **出さない**（表示の絞り込みだけ。リスナーの `ORGANIZATION` fan-out も出さない）|

**「チームを選ぶ」の既存挙動からの変更点**（御裁可済み #19）

| 観点 | 変更前（main） | 変更後 |
|---|---|---|
| ADMIN / DEPUTY が選んだチーム宛てのアンケート | 組織全体（直属＋配下チーム全部＋子組織）に push | 選んだチームのメンバー＋組織の直属メンバーだけに push |
| MEMBER が選んだチーム宛てのアンケート | 組織全体に push | push しない |
| 表示（ダッシュボード） | 選んだチームだけ | 変更なし |
| 「すべてのチーム」 | 組織全体に push | 変更なし |
| push の無いチャネル（掲示板など） | push なし | 変更なし |
| enqueue の経路 | リスナーが AFTER_COMMIT で非同期に enqueue | 告知ウィザードが告知と同一トランザクションで enqueue（§8.5.3）|

#### 8.5.2 受信者ソース `ORGANIZATION_TEAMS`

| 項目 | 設計 |
|---|---|
| 実装 | `OrgTeamsFanoutRecipientSource`（戦略キー `ORGANIZATION_TEAMS`、role ドメイン `com.mannschaft.app.role.fanout`。`OrgFanoutRecipientSource` と同じ理由で role ドメインに置く） |
| scope_ref | 組織 ID の文字列 |
| 宛先チーム | `notification_fanout_job_target_teams`（「チームを選ぶ」は選んだチーム ID、「チームグループで選ぶ」は送信時スナップショットのチーム ID）|
| 母集団 | ①宛先チームの現役メンバー（`TeamFanoutRecipientSource` と同じ条件: memberships `left_at IS NULL` ∪ user_roles、users 生存・ACTIVE）のうち、**配信時点でも**そのチームが組織に ACTIVE で加盟しているもの ∪ ②**組織の直属メンバー**（組織スコープの memberships・user_roles。`OrgFanoutRecipientSource` の「直属」枝と同じ条件）。ユーザー単位で重複排除 |
| 含めないもの | 宛先外チームのメンバー、子組織のメンバー |
| 応援者トグル | `include_supporters` は告知の `target_role` から決める: `MEMBERS_AND_ABOVE` → false（純 SUPPORTER を除く）、`SUPPORTERS_AND_ABOVE` / `PUBLIC` → true |
| ページング | user_id のキーセット、シャード時は `MOD(user_id, shardCount)`。既存 ORG ソースと同じ形 |

#### 8.5.3 enqueue の方式（transactional outbox 相当）

- 告知ウィザードの `broadcast()` は、コンテンツ作成・フィード登録と**同じトランザクションの中で** fan-out ジョブ1件（＋文面6行＋宛先チーム子表）を INSERT する。ジョブ行はコミットされて初めてワーカーから見えるので、告知がロールバックされれば push も出ない（outbox と同じ性質）。
- 既存の `NotificationFanoutJobService.enqueue` は `REQUIRES_NEW` で独立コミットするため、呼び出し側のトランザクションに参加する `enqueueInCurrentTransaction(...)`（`Propagation.MANDATORY`）を追加する。
- 冪等キー `source_event_uuid` = `UUID.nameUUIDFromBytes(("F02.8:broadcast:" + feedId).getBytes(UTF_8))`（フィード ID から決定的に導出。同じ告知の二重 enqueue は `uk_fanout_idempotency` で1件に収束する）。
- 通知種別 `notification_type` = `SURVEY_CREATED`、文面 `FanoutMessageKind.SURVEY_PUBLISHED`（引数はアンケート名）。**push の文面はアンケート公開通知と同じにする**（受け取る人から見れば同じ出来事であり、チャネルの既存文面を使う）。6言語のキーは既存の `notification.fanout.survey.published.title` / `.body`（`messages*.properties` に6言語とも存在）を使い、新設しない。
- リスナー側の抑止: `SurveyAnnouncementAdapter` は main 時点で `surveyService.createSurvey(...)` の直後に `surveyService.publishSurvey(scopeType, scopeId, surveyId)` を呼んで即公開している（失敗は伝播し broadcast 全体がロールバックする）。この経路に印を通すため次のようにする。
  - `SurveyService.publishSurvey(String scopeType, Long scopeId, Long surveyId, boolean audienceControlledByBroadcast)` の4引数版を追加し、既存の3引数版は `false` で委譲する（他の呼び出し元は変えない）。
  - `SurveyPublishedEvent` に `boolean audienceControlledByBroadcast` フィールドを追加する（既存の生成箇所は `false`）。
  - `SurveyAnnouncementAdapter.createContent` は、`BroadcastAudience` が「宛先を絞った告知」なら4引数版を `true` で呼ぶ。
  - `SurveyPublishNotificationListener.onSurveyPublished` は、`event.isAudienceControlledByBroadcast()` が真なら何もしない（push は告知ウィザードの1件に一本化する）。送信者が push 権限を持たない場合（組織 MEMBER・MANAGE_CONTENT を持たない DEPUTY）も印を載せ、ウィザード側は enqueue しない（=push なし）。
- 印を載せるのは「チームを選ぶ」と「チームグループで選ぶ」の場合である。「すべてのチーム」では載せず、リスナーは従来どおり動く。


### 8.6 範囲テンプレートのサーバー側解決

テンプレートは**グループの指定**（個別グループ ID・範囲・未分類の有無）を保存し、使うたびにサーバーが解決する。

```
broadcast / audience-preview で templateId が指定されたとき
1. テンプレートを (id, scope_type, scope_id) で取得 → 不一致・不在は 400 BROADCAST_003（既存）
2. リクエストで明示された宛先項目があれば、それを優先する（既存の規則）
3. 個別グループ: テンプレートの target_group_ids から、その組織の生存グループだけを残す（削除済み・他組織は黙って除外し、
   レスポンスの warnings に TEMPLATE_GROUPS_REMOVED と除外件数を載せる）
4. 範囲: from / to の各端が生存グループか確認。どちらかが削除済みなら 400 BROADCAST_013（範囲が成り立たない）
   端が生きていれば、送信時点の sort_order で展開する
5. 3 と 4 の和集合 ＋ include_unassigned で §8.2 の保存・push 宛先を決める。0件なら §8.3 の BROADCAST_009
```

- この結果、テンプレートの保存後にグループを追加・並べ替えても、使った時点の並びで範囲が展開される（AC-J01）。
- 範囲の端が消えたテンプレートは、画面では読み込み時に範囲を空にして再選択を求め、API で `templateId` だけを送った場合は 400 `BROADCAST_013` になる（AC-J03）。

---

## 9. 複数組織への同時加盟（単一親前提の読み手の改修）

### 9.1 方針（マスター確定）

1チームは複数の組織へ同時に ACTIVE で加盟できる。これを本書の範囲で保証するため、「チームの親組織は1つ」と前提している読み手を改修する。改修は加盟の書き込み API の**リリースより前**（§15 の段階①に同梱）に行う。書き込み API が出るまでは複数加盟の行を作る経路が無いため、先に読み手を直しておけば、複数加盟が生じた瞬間に壊れる箇所を残さない。

意味論の原則:

| 種類 | 複数の親があるときの意味 |
|---|---|
| 可視性・権限（「親組織のメンバーなら見える」等） | **いずれかの親**で条件を満たせば真（OR）|
| 一覧・集計（親組織の一覧、配下チームの一覧、人数の集計） | **すべての親**を集合として返す。JOIN による行の重複はユーザー・チーム単位で `DISTINCT` する |
| マージ（親組織のカテゴリ・設定をチームに足し込む） | **すべての親**の分を和集合で足し込み、どの親由来かを応答に含める |
| 単一の値が要る箇所（テナントキー・課金主体など） | 呼び出し側（URL・リクエスト）に**親組織を明示させる**。明示が無い既存 API には「代表親組織」を決定的に選ぶ規則（§9.3）を使い、応答にどの組織を採ったかを含める |

### 9.2 単一親前提の読み手の一覧（origin/main 59ae48ee66 時点）

origin/main を `team_org_memberships` で走査し、単一親前提の箇所を洗い出した（パスは `be/` = `backend/src/main/java/com/mannschaft/app/`、`fe/` = `frontend/app/`）。**要改修はバックエンド18件・フロントエンド4件の計22件**である。根本原因は3つ（`findOrganizationIdByTeamIdIn` の Map、ShiftBudget の `LIMIT 1`、課金の `orgIds.get(0)`）で、2〜6 は 1 から、F1〜F3 は 7 から派生する。なお `findTeamOrgIdProjectionsByTeamIdIn`・`findByTeamIdAndStatus` には ORDER BY が無く、「後勝ち」「先頭」は実際には DB の返す順に依存した**任意の1件**である。

| # | 箇所 | 単一とみなしているもの（用途） | 複数親時の現在の挙動 | 重大度 | 複数親の意味論と改修 |
|---|---|---|---|---|---|
| 1 | be/team/repository/TeamOrgMembershipRepository.java:109-118 `findOrganizationIdByTeamIdIn` | チームID→組織IDの `Map<Long, Long>` | `HashMap.put` の後勝ち（任意の1件） | 高 | `Map<Long, Set<Long>> findOrganizationIdsByTeamIdIn` に置き換え、旧メソッドは削除する（番人で再追加を禁止）|
| 2 | be/common/visibility/ScopeAncestorResolver.java:62-72 `resolveParentOrgIds` | TEAM スコープの親組織1つ | 1 の任意の1件 | 高 | 戻り値を `Map<ScopeKey, Set<Long>>` にし、TEAM は ACTIVE の全親組織を返す |
| 3 | be/common/visibility/MembershipBatchQueryService.java:133-135, 149-156, 178 `snapshotForUser` | 親組織 ID 集合（ロール・停止状態の取得元） | 2 で選ばれた1組織しか入らない | 高 | 全親組織の和集合でロール・memberships・停止状態を取る（SQL 本数は IN 句の拡大だけで増やさない）|
| 4 | be/common/visibility/UserScopeRoleSnapshot.java:184-188 `isMemberOfParentOrg`（呼び出し元 AbstractContentVisibilityResolver:466、MembershipBatchQueryService:653） | ORGANIZATION_WIDE 公開の判定 | 選ばれなかった親組織のメンバーに見えない | 高 | **いずれかの親組織**のメンバーなら真（OR）。ただし #6 の停止中の親は除く |
| 5 | UserScopeRoleSnapshot.java:216-221 `hasParentOrgRoleOrAbove`（呼び出し元 ScheduleVisibilityResolver:166） | 予定の min_view_role を親組織のロールで判定 | 別の親組織でのロールが評価されない | 高 | 停止中でない親組織のうち**いずれか**で必要ロール以上なら真 |
| 6 | UserScopeRoleSnapshot.java:234-235 `isParentOrgInactive`（呼び出し元 AbstractContentVisibilityResolver:423） | 親組織が停止中なら閲覧不可（fail-closed） | 停止中の親が選ばれなければ閲覧できてしまう | 高 | 「親組織経由の閲覧権」は親組織ごとに評価し、**停止中の親組織を経由する権利は数えない**。停止中でない親組織で権利があれば閲覧可、すべての経路が停止中なら不可 |
| 7 | be/role/controller/MeController.java:100, 133 `MyTeamResponse.organizationId` | 試合機能（F08.10）が使う組織ID | 1 の任意の1件 | 高 | `organizations: [{id, slug, name}]`（全親組織）を追加する。既存の `organization_id` は互換のため残し、値を §9.3 の代表親組織に固定する（非推奨と明記）|
| 8 | be/schedule/service/ScheduleService.java:1087-1091（呼び出し元 :323, :1057） | 予約タスク（アンケート・出欠）登録時のテナント organizationId | 任意の1件（実行のたびに変わり得る） | 中 | §9.3 の代表親組織（決定的）。チームの予定のテナントキーであり、どの親組織でも意味が変わらないため明示は求めない |
| 9 | be/schedule/controller/TeamAnnualScheduleController.java:79-82 `findFirstByTeamIdAndStatus` | 年間行事ビューにマージする親組織の行事カテゴリ | 1組織分しか出ない | 中 | **全親組織**のカテゴリを和集合でマージし、応答の各カテゴリに `sourceOrganizationId` を付ける |
| 10 | be/schedule/controller/TeamEventCategoryController.java:50-53 `findFirstByTeamIdAndStatus` | チーム行事カテゴリ一覧 | 同上 | 中 | 同上 |
| 11 | be/shiftbudget/repository/ShiftBudgetRateQueryRepository.java:107-115 `findOrganizationIdByTeamId`（ORDER BY なしの `LIMIT 1`） | シフト予算の組織 | 任意の1件 | 高 | 削除する。予算の組織は「そのチームに予算割当（allocation）を持つ組織」で決める（#12〜#16 の前提）|
| 12 | be/shiftbudget/service/ShiftBudgetCalcService.java:145-149 | 機能フラグ判定の organizationId | 選ばれた組織のフラグ次第で 404 になるか変わる | 中 | 呼び出し側（予算画面の URL）が組織を明示する。フラグはその組織で判定する |
| 13 | be/shiftbudget/service/TodoBudgetLinkService.java:293-295 `isScopeInOrganization` | 「チームがこの組織に属するか」 | 正当な所属でも false になり得る | 高 | 「指定組織への ACTIVE 加盟が存在するか」の存在判定に変える（等値比較をやめる）|
| 14 | be/shiftbudget/listener/ShiftBudgetConsumptionRecordListener.java:92, 248 | シフト公開時に予算消化を計上する先の組織 | 任意の組織の予算に計上される | 高 | そのチーム・期間に**予算割当を持つ組織**へ計上する。同じチーム・重なる期間に複数の組織が割当を持つことは、割当の作成時に 409 で禁止する（計上先を一意に保つ）。割当が無ければ計上しない |
| 15 | be/shiftbudget/listener/ShiftBudgetConsumptionCancelListener.java:84, 140 | 消化を取り消す先の組織 | 計上時と別の組織が選ばれ得る | 高 | 再解決せず、**計上時に消化行へ記録した組織 ID** を使う |
| 16 | be/shiftbudget/batch/ShiftBudgetConsumptionReconcileBatchService.java:109 | 監査ログの organizationId | 任意の1件 | 低 | 消化行に記録した組織 ID を使う（#15 と同じ）|
| 17 | be/billing/api/BillingContractApplicationService.java:135-143、be/billing/api/SystemAdminBillingService.java:304-312、be/billing/beta/api/SystemAdminBetaPerkController.java:212-220（`orgIds.get(0)`） | TEAM スコープの契約・特典のテナント organizationId（主所属組織） | 並び順不定の先頭（任意の1件） | 中 | 作成時は §9.3 の代表親組織を採り、**作成時に契約行へ記録した組織を以後の正とする**（再解決しない）。SYSTEM_ADMIN の画面では組織を選べるようにする |
| 18 | be/errorreport/repository/ErrorReportRepository.java:67-86 `findOrganizationIdByUserId`（UNION に ORDER BY なしの `LIMIT 1`。呼び出し元 ErrorReportService:489） | エラーレポートに紐づける組織 | 任意の1件（ユーザーが複数組織に属する時点で元々曖昧） | 低 | ORDER BY を付けて決定的にする（直属組織を優先し、次にチーム経由の §9.3 の代表親組織）。参考情報のため明示は求めない |
| F1 | fe/composables/match/useMatchOrgContext.ts:88, 123-124 | `/me/teams` の `organizationId` を試合 API `/organizations/{orgId}/teams/{teamId}` に使う | 返る組織が変わると既存の試合が 404（試合は organizationId で保存・取得される） | 高 | #7 の `organizations` を使い、親組織が複数なら画面で組織を選ばせる（既定は代表親組織）。選択は URL クエリ `org` に載せ、再読込で変わらないようにする |
| F2 | fe/pages/organizations/[slug]/tournaments/[tId]/fixtures.vue:128-143 | 大会の対戦表から試合を記録するときの orgId | 表示中の大会の組織ではなく F1 の orgId で試合を作る | 高 | **表示中の大会の組織**（ページの `[slug]`）を使う。チームの親組織から推測しない |
| F3 | F1 の呼び出し元（matches/index.vue:27、new.vue:24、live.vue:197、teams/[slug]/match-analytics.vue:36、members/[userId]/match-analytics.vue:48、me/match-analytics.vue:54、WidgetTeamMatchSummary.vue:54、EventDetailPanel.vue:68） | 試合の一覧・作成・ライブ・分析の orgId | F1 と同じ | 高 | F1 の組織選択を共通で使う。一覧・分析は選択中の組織の試合を出す |
| F4 | fe/pages/teams/[slug]/repair-plan.vue:41-53 | `getOrganizations` の `orgs[0]`（「最初の組織を使用」） | 任意の先頭。さらに API の `id` が slug 文字列のため `typeof === 'number'` が常に偽で、現状は常に null（単一親とは別のバグ） | 低 | 親組織が複数なら選ばせる。id/slug の取り違えは別バグとして同時に直す |

**集合として扱っており問題が無いと確認したもの**: TeamOrgMembershipQueryService、ScopeClassificationService、TimetableTermService、TeamService.getOrganizations（:991）、TeamSearchSpecifications、OrganizationHierarchyService（isDescendantMember・getAnchorOrgIdsByTeamIds）、OrganizationMembershipService、OrganizationService、AccessControlService、UserRoleRepository（53箇所すべて IN・UNION・DISTINCT・COUNT(DISTINCT)）、LeagueTransferService（候補が1件でなければ明示的に例外）、TournamentEntryTemplateService、RepairPlanModuleGuard、ShiftBudgetRateQueryRepository.countTeamInOrganization、ShiftBudgetAllocationService:361、ScheduleAttendanceService、OrgFanoutRecipientSource、TimelineDeliveryScopeResolver、ModuleService.isModuleEnabledForTeam、`findActiveOrganizationIds(userId)` の利用箇所、TeamPurgeEventListener／TeamPurgeBackfillBatchService、PersonalDataCollector。分析系には `team_org_memberships` の参照が無く、組織のプラン・モジュールをチームへ継承する実装も無い。`Collectors.toMap` でチーム→組織を作る箇所・スカラーサブクエリ・重複計上する JOIN も見つからなかった。

> この一覧は 2026-09-25 時点の origin/main（59ae48ee66）の走査結果である。2026-09-26 に origin/main（67607e4525）を取り込んで該当ファイルに変更が無いことを確かめた。以後の新規追加は §9.4 の番人が機械的に止める。

### 9.3 代表親組織の規則（単一値がどうしても要る既存 API のみ）

明示の親組織を受け取れない既存 API（後方互換のため引数を増やせないもの）に限り、次の規則で1つを選ぶ。

1. `team_org_memberships.responded_at` が最も古い ACTIVE の加盟（最初に成立した加盟）
2. 同時刻なら `organization_id` が最小のもの

この規則は `TeamOrgMembershipQueryService.findPrimaryParentOrganizationId(teamId)` の1箇所にまとめ、他の箇所で `findFirst` や `LIMIT 1` を書かない。番人テスト（§12）で `team_org_memberships` に対する `findFirstBy...` / `LIMIT 1` の新規追加を禁じる。

### 9.4 番人

- `TeamOrgSingleParentAssumptionGuardTest`（ArchUnit＋ソース走査）: `TeamOrgMembershipRepository` の `findFirstBy*` メソッド、`Map<Long, Long>` を返す team→org 解決メソッド、`team_org_memberships` を含むネイティブクエリの `LIMIT 1` を、許可リスト（`findPrimaryParentOrganizationId` の実装だけ）以外で検出したら失敗する。
- 複数親のフィクスチャ（チームT が組織X と組織Y の両方に ACTIVE）を持つ IT を、§9.2 の各項目について1本ずつ用意する（AC-N 群）。

---

## 10. API 契約

共通事項:

- **JSON のフィールド名は camelCase**（Jackson の既定。`application.yml` に命名戦略の設定は無く、`BroadcastRequestDto` など既存 DTO も camelCase）。本書では、JSON のフィールドを camelCase（例 `targetGroupIds`）、DB の列を snake_case（例 `target_group_ids`）で書き分ける。新規 DTO に `@JsonProperty("snake_case")` を付けてはならない。
- パスの識別子は slug（組織 `{slug}`、チーム `{teamSlug}`／`{orgSlug}`）。加盟行の `{membershipId}` は BIGINT、グループの `{groupId}` は UUID 文字列。
- **例外: 告知系（broadcast・宛先プレビュー・範囲テンプレート）だけは数値の `{orgId}`（Long）を使う。** 既存の `AnnouncementBroadcastController`（`POST /api/v1/organizations/{orgId}/broadcast`）と `AnnouncementRangeTemplateController`（`/api/v1/organizations/{orgId}/announcement-templates`）が `@PathVariable Long orgId` であり、告知ウィザード（`useAnnouncementBroadcast.ts` / `useAnnouncementTemplates.ts`）も数値 ID で呼んでいる。同じウィザードが呼ぶ新 API だけを slug にすると、1画面で識別子が2種類混ざり、取り違えの温床になる。告知系の slug 化は告知系全体でまとめて行う別の課題とする（§18）。
- 日時は ISO-8601（オフセット付き）。
- 一覧のページングは既存の `PaginationMeta`（`page` / `size` / `total` / `hasNext`）。`size` 既定 20・上限 100。
- **越境の扱い**: 他組織・他チームの `membershipId` / `groupId` を指定したときは、存在しない場合と**同じステータス・同じエラーコード**を返す（存在オラクルを作らない）。
- 認可の順序: 認証（401）→ スコープの存在と可視性（404）→ 権限（403）→ 入力検証（400）→ 状態（409/422）。
- **「キーを送らない」と「null を送る」を区別しない。** 区別が要る箇所（承認時のグループ上書き）は、別の boolean フラグで意図を明示させる（§10.5）。`JsonNullable` は使わない。
- BE の API を変えたら、`docs/openapi.json` を更新したうえで `cd frontend && npm run generate:types` で `frontend/app/types/generated/index.ts` を再生成し、同じ PR でコミットする（手編集禁止）。

### 10.1 エンドポイント一覧

状態列の 🔵 は「設計済み・未実装」を表す（`scan_api_drift.py` の将来機能タグ）。認可の「チーム加盟操作」は §3.2 の `MANAGE_ORG_AFFILIATION`（チーム ADMIN は常に持つ）。

| 状態 | メソッド | パス | 認可 | 説明 |
|---|---|---|---|---|
| 🔵 | GET | `/api/v1/organizations/{slug}/team-affiliation-settings` | 組織 ADMIN | 申請受付・グループ設定の取得 |
| 🔵 | PUT | `/api/v1/organizations/{slug}/team-affiliation-settings` | 組織 ADMIN | 同 更新 |
| 🔵 | GET | `/api/v1/organizations/{slug}/team-application-form` | 認証済み・組織が見えること | 申請フォームの内容 |
| 🔵 | GET | `/api/v1/me/org-affiliation-eligibility?organizationSlug={slug}` | 認証済み | 申請ボタンを出すか（§10.3）|
| 🔵 | POST | `/api/v1/teams/{teamSlug}/org-applications` | チーム加盟操作 | 組織へ加盟申請 |
| 🔵 | GET | `/api/v1/teams/{teamSlug}/org-applications` | チーム加盟操作 | 申請中一覧（PENDING/TEAM_APPLY）|
| 🔵 | DELETE | `/api/v1/teams/{teamSlug}/org-applications/{membershipId}` | チーム加盟操作 | 申請の取下げ |
| 🔵 | GET | `/api/v1/organizations/{slug}/team-applications` | 組織 ADMIN | 受信した申請一覧 |
| 🔵 | POST | `/api/v1/organizations/{slug}/team-applications/{membershipId}/approve` | 組織 ADMIN | 承認（グループ確定）|
| 🔵 | POST | `/api/v1/organizations/{slug}/team-applications/{membershipId}/reject` | 組織 ADMIN | 拒否（冷却・ブロック）|
| 🔵 | POST | `/api/v1/organizations/{slug}/team-invites` | 組織 ADMIN | チームを招待 |
| 🔵 | GET | `/api/v1/organizations/{slug}/team-invites` | 組織 ADMIN | 送信済み招待一覧 |
| 🔵 | DELETE | `/api/v1/organizations/{slug}/team-invites/{teamSlug}` | 組織 ADMIN | 招待の取消 |
| 🔵 | GET | `/api/v1/teams/{teamSlug}/org-invites` | チーム加盟操作 | 受信した招待一覧 |
| 🔵 | POST | `/api/v1/teams/{teamSlug}/org-invites/{membershipId}/accept` | チーム加盟操作 | 招待の承諾 |
| 🔵 | POST | `/api/v1/teams/{teamSlug}/org-invites/{membershipId}/reject` | チーム加盟操作 | 招待の拒否 |
| 🔵 | DELETE | `/api/v1/teams/{teamSlug}/organizations/{orgSlug}` | チーム加盟操作 | 離脱 |
| 🔵 | DELETE | `/api/v1/organizations/{slug}/teams/{teamSlug}` | 組織 ADMIN | 除名 |
| 🔵 | GET | `/api/v1/organizations/{slug}/team-affiliation-restrictions` | 組織 ADMIN | 組織が止めている申請（TEAM_APPLY・REJECTED）一覧 |
| 🔵 | DELETE | `/api/v1/organizations/{slug}/team-affiliation-restrictions/{restrictionId}` | 組織 ADMIN | 解除 |
| 🔵 | GET | `/api/v1/teams/{teamSlug}/org-affiliation-restrictions` | チーム加盟操作 | チームが止めている招待（ORG_INVITE・DECLINED）一覧 |
| 🔵 | DELETE | `/api/v1/teams/{teamSlug}/org-affiliation-restrictions/{restrictionId}` | チーム加盟操作 | 解除 |
| 🔵 | GET | `/api/v1/organizations/{slug}/team-groups` | 組織 MEMBER 以上 | チームグループ一覧（並び順・所属チーム数）|
| 🔵 | POST | `/api/v1/organizations/{slug}/team-groups` | 組織 ADMIN | 作成 |
| 🔵 | PATCH | `/api/v1/organizations/{slug}/team-groups/{groupId}` | 組織 ADMIN | 名前・説明の変更 |
| 🔵 | DELETE | `/api/v1/organizations/{slug}/team-groups/{groupId}` | 組織 ADMIN | 削除（所属チームは未分類へ）|
| 🔵 | PUT | `/api/v1/organizations/{slug}/team-groups/order` | 組織 ADMIN | 並び替え |
| 🔵 | PUT | `/api/v1/organizations/{slug}/teams/{teamSlug}/team-group` | 組織 ADMIN | 1チームの割当 |
| 🔵 | PUT | `/api/v1/organizations/{slug}/team-group-assignments` | 組織 ADMIN | 一括割当 |
| 🔵 | POST | `/api/v1/organizations/{orgId}/broadcast/audience-preview` | 組織 MEMBER 以上 | お知らせ宛先の解決結果プレビュー（告知系のため `{orgId}`）|

既存 API の拡張:

| メソッド | パス | 追加点 |
|---|---|---|
| GET | `/api/v1/organizations/{slug}/teams` | 各要素に `teamGroup: {id, name, sortOrder} \| null`（グループ機能 on のときだけ。off は常に null）。クエリ `teamGroupId`（UUID）／`unassigned=true` で絞り込み |
| GET | `/api/v1/teams/{teamSlug}/organizations` | 各要素に `teamGroup: {id, name} \| null`（自チームが所属するグループ名のみ）。複数の組織に加盟していれば全件を返す（既存どおり）|
| GET | `/api/v1/organizations/{slug}` | `teamApplication: {enabled: boolean}` を追加（組織シェル内の申請ボタンの出し分け用）|
| GET | `/api/v1/public/organizations/{id}` | `acceptingTeamApplications: boolean` を追加（`fetchPublicOrganization` が呼ぶ公開 API。§10.3）|
| GET | `/api/v1/public/organizations/search` | クエリ `acceptingTeamApplications=true`、各要素に `acceptingTeamApplications: boolean`（`/discover/organizations` が使う公開検索。認証不要）|
| POST | `/api/v1/organizations/{orgId}/broadcast` | `targetGroupIds` / `targetGroupRange` / `includeUnassigned` を追加（F02.8 §9.1）|
| GET/POST/PUT | `/api/v1/organizations/{orgId}/announcement-templates` | 同上の3項目を追加（F02.8 §9.2）|
| （§9.2 の各 API） | — | 複数親組織を返す形・親組織を明示させる形への変更（§9.2 の表の「改修」列）|

### 10.2 設定

**GET / PUT `/api/v1/organizations/{slug}/team-affiliation-settings`**

| フィールド | 型 | null | 説明 |
|---|---|---|---|
| `teamApplicationEnabled` | boolean | 不可 | チームからの加盟申請を受け付ける |
| `teamGroupsEnabled` | boolean | 不可 | チームグループ機能を使う |
| `applicationGroupMode` | `"OFF" \| "OPTIONAL" \| "REQUIRED"` | 不可 | 申請時のグループ選択 |
| `effectiveApplicationGroupMode` | 同上 | 不可 | GET のみ。§5.5 の実効値 |
| `applicationGuidance` | string(≤500) | 可 | 申請フォームの案内文 |
| `pendingApplicationCount` | integer | 不可 | GET のみ。受付 off 切替時の確認に使う |

PUT は全項目必須（部分更新にしない）。`REQUIRED` を保存する条件を満たさなければ 422 `ORG_070`。成功は 200 で GET と同じ形を返す。

### 10.3 申請ボタンの判定と申請フォーム

**公開ページ（`/public/organizations/{slug}`）の申請ボタン**（マスター確定 M3）

1. 公開 API `GET /api/v1/public/organizations/{id}` の応答に `acceptingTeamApplications: boolean` を加える。この API は既に公開（visibility=PUBLIC）の組織しか返さない（非公開の組織は従来どおり不在と同じ応答）。受付フラグは**既に公開されている組織にだけ**載るので、受付 off の組織・非公開の組織について新しい存在オラクルは生まれない。
2. ボタンを出すかどうかは、ログイン中のときだけ認証付きの `GET /api/v1/me/org-affiliation-eligibility?organizationSlug={slug}` で判定する。応答は `{canApply: boolean}` の1項目で、**常に 200**。`canApply` は「組織が見えて・受付中で・閲覧者が加盟操作権限を持つチームが1つ以上ある」ときだけ true。組織が存在しない・見えない・受付 off・権限のあるチームが無い、のいずれでも同じ `false` を返す（どの理由で false かを区別しない＝存在オラクルにならない）。
3. 画面は `acceptingTeamApplications && canApply` のときだけボタンを出す。未ログインではボタンを出さない（ログイン誘導も出さない）。

> **前提欠陥**: main 時点で、FE の `fetchPublicOrganization(orgSlug)` はルートの slug を渡しているのに、BE の `PublicOrganizationController` は `@PathVariable Long id` を要求している。本機能の公開ページ導線はこの API に依存するため、実装の段階①で「slug と数値 ID の両方を受ける」か「FE が数値 ID を解決して渡す」かのどちらかで先に直す（§18 の最初の項目）。

**GET `/api/v1/organizations/{slug}/team-application-form`**

- 受付 off の組織では 403 `TEAM_064`（画面は申請ボタン自体を出さないので、ここに来るのは URL 直打ちか、画面表示後に off にされた場合だけ）。

| フィールド | 型 | null | 説明 |
|---|---|---|---|
| `organization` | `{slug, name, iconUrl}` | 不可 | |
| `guidance` | string | 可 | |
| `groupMode` | `"OFF" \| "OPTIONAL" \| "REQUIRED"` | 不可 | 実効値 |
| `groups` | `[{id, name, description}]` | 不可 | `groupMode = OFF` なら空配列。並び順どおり |
| `myTeams` | `[{slug, name, iconUrl, affiliationStatus}]` | 不可 | 操作者が加盟操作権限を持つチーム全件（上限 100）。`affiliationStatus`: `NONE` / `APPLYING` / `INVITED` / `ACTIVE` / `UNAVAILABLE`（制限中。冷却かブロックかは区別しない）|

### 10.4 申請

**POST `/api/v1/teams/{teamSlug}/org-applications`**

| フィールド | 型 | null | 制約 |
|---|---|---|---|
| `organizationSlug` | string | 不可 | 3〜30 |
| `groupId` | string(UUID) | 可 | §6.1 step 10 |
| `message` | string | 可 | ≤500 コードポイント。空文字は null に正規化 |

201 レスポンス（申請・招待・加盟の共通表現 `TeamOrgAffiliationResponse`）:

| フィールド | 型 | null |
|---|---|---|
| `id` | integer（membershipId）| 不可 |
| `status` | `"PENDING" \| "ACTIVE"` | 不可 |
| `direction` | `"ORG_INVITE" \| "TEAM_APPLY"` | 不可 |
| `team` | `{slug, name, iconUrl}` | 不可 |
| `organization` | `{slug, name, iconUrl}` | 不可 |
| `teamGroup` | `{id, name} \| null` | 可（グループ機能 off なら常に null）|
| `message` | string | 可（PENDING のときだけ）|
| `requestedBy` | `{id, displayName} \| null` | 可（退会者は null）|
| `requestedAt` | datetime | 不可 |
| `respondedAt` | datetime | 可 |
| `expiresAt` | datetime | 可（PENDING のときだけ。`requestedAt` + 60日）|

エラー: 400 `TEAM_067` / `TEAM_072`、403（加盟操作権限なし）/ `TEAM_064` / `TEAM_068`、404（チーム・組織）、409 `TEAM_065` / `TEAM_066`、422 `TEAM_069`・アーカイブ、429（10件/時/ユーザー）。

### 10.5 承認・拒否

**POST `.../team-applications/{membershipId}/approve`**

| フィールド | 型 | null | 説明 |
|---|---|---|---|
| `overrideGroup` | boolean | 不可 | `false` = 申請時の希望グループのまま承認する（`groupId` は無視する）。`true` = `groupId` のグループで承認する |
| `groupId` | string(UUID) | 可 | `overrideGroup=true` のときだけ使う。`null` なら未分類で承認 |

- 「キーを送らない」と「null を送る」の区別に頼らず、`overrideGroup` で意図を明示させる（§6.2 step 4）。旧案の `keep_requested_group` と `JsonNullable` 案は廃止した。
- 200 で `TeamOrgAffiliationResponse`（status=ACTIVE）。

**POST `.../team-applications/{membershipId}/reject`**

| フィールド | 型 | null | 説明 |
|---|---|---|---|
| `reason` | string(≤500) | 可 | チームへ通知する理由 |
| `block` | boolean | 可（既定 false） | true なら無期限ブロック |

200 で `{restriction: {kind, restrictedUntil}}`。

### 10.6 一覧（申請・招待）

`GET .../team-applications`、`GET .../team-invites`、`GET .../org-invites`、`GET .../org-applications` は `TeamOrgAffiliationResponse` の配列＋`PaginationMeta`。並びは `requestedAt` の降順。`team-applications` はクエリ `teamGroupId` で希望グループ絞り込み可。

### 10.7 チームグループ

**GET `/api/v1/organizations/{slug}/team-groups`**（ページングなし・最大200件）

| フィールド | 型 | null |
|---|---|---|
| `data[].id` | string(UUID) | 不可 |
| `data[].name` | string | 不可 |
| `data[].description` | string | 可 |
| `data[].sortOrder` | integer | 不可 |
| `data[].teamCount` | integer | 不可（ACTIVE の加盟数）|
| `meta.unassignedTeamCount` | integer | 不可 |
| `meta.limit` | integer | 不可（200）|

グループ機能 off の組織では 409 `ORG_067`。

**POST** body `{name: string(1..50), description?: string(≤200)}` → 201 で1件。**PATCH** は同じ項目の部分更新 → 200。**DELETE** → 204。**PUT `/order`** body `{groupIds: UUID[]}` → 200 で一覧。

### 10.8 割当

**PUT `/api/v1/organizations/{slug}/teams/{teamSlug}/team-group`** body `{groupId: UUID|null}` → 200 で `TeamOrgAffiliationResponse`。対象チームがその組織の ACTIVE 加盟でなければ 404 `TEAM_070`。他組織・削除済みのグループなら 404 `ORG_064`。

**PUT `/api/v1/organizations/{slug}/team-group-assignments`** body `{groupId: UUID|null, teamSlugs: string[1..500]}` → 200 で `{updatedCount: integer}`。不正が1件でもあれば 400 `ORG_069` と `{invalidTeamSlugs: string[]}`、何も更新しない。

### 10.9 宛先プレビュー

**POST `/api/v1/organizations/{orgId}/broadcast/audience-preview`**（告知系のため数値 `{orgId}`）

リクエストは broadcast と同じ宛先項目（`targetTeamIds` / `targetGroupIds` / `targetGroupRange` / `includeUnassigned` / `templateId`）と `channel`・`targetRole`。レスポンス:

| フィールド | 型 | null |
|---|---|---|
| `resolvedTeamCount` | integer | 不可 |
| `directMemberCount` | integer | 不可（宛先を絞ったときだけ意味を持つ。それ以外は 0）|
| `sampleTeams` | `[{slug, name}]`（最大50）| 不可 |
| `groups` | `[{id, name}]`（範囲展開後のグループ）| 不可 |
| `pushEnabled` | boolean | 不可（送信者が組織 ADMIN・MANAGE_CONTENT を持つ DEPUTY・SYSTEM_ADMIN のいずれでもない場合、またはチャネルが push を出さない場合は false。UI で「push は送られません」と表示する）|
| `warnings` | `string[]` | 不可（`TEMPLATE_GROUPS_REMOVED` など）|

レートリミット 60件/分/ユーザー。

### 10.10 レートリミット

| 操作 | 制限 |
|---|---|
| 申請 `POST /teams/{teamSlug}/org-applications` | 10件/時/ユーザー |
| 招待 `POST /organizations/{slug}/team-invites` | 30件/時/ユーザー |
| 一括割当 | 20件/分/ユーザー |
| 宛先プレビュー | 60件/分/ユーザー |
| 申請可否 `GET /me/org-affiliation-eligibility` | 60件/分/ユーザー |

---

## 11. エラーコード

**予約値である。** 本書時点（origin/main 59ae48ee66）の最大値（`ORG_063` / `TEAM_063` / `BROADCAST_005`）の次から予約した。**実装 PR のマージ時に origin/main の最大値を必ず取り直し、衝突していれば繰り下げて本書も直すこと。** HTTP ステータスは `GlobalExceptionHandler` のコード別ステータス表に登録する。メッセージは `messages*.properties` の `error.{domain}.{number}`（例: `error.team.064`）に6言語で置く（§14.2）。

| コード | HTTP | メッセージ（ja） | 発生箇所 |
|---|---|---|---|
| `ORG_064` | 404 | チームグループが見つかりません | グループ PATCH/DELETE、割当（他組織・削除済みも同じ）|
| `ORG_065` | 409 | 同じ名前のチームグループがすでにあります | 作成・改名 |
| `ORG_066` | 422 | チームグループは1組織あたり200件までです | 作成 |
| `ORG_067` | 409 | この組織ではチームグループ機能が無効です | グループ系 API（off 時）|
| `ORG_068` | 409 | チームグループの構成が変わっています。画面を更新してください | 並び替え |
| `ORG_069` | 400 | この組織に加盟していないチームが含まれています | 一括割当 |
| `ORG_070` | 422 | グループ選択を必須にするには、グループ機能を有効にしてグループを1件以上作成してください | 設定 PUT |
| `TEAM_064` | 403 | この組織はチームからの加盟申請を受け付けていません | 申請・申請フォーム |
| `TEAM_065` | 409 | このチームはすでにこの組織に加盟しています | 申請・招待 |
| `TEAM_066` | 409 | このチームとこの組織の間には、処理中の申請または招待があります | 申請・招待 |
| `TEAM_067` | 400 | この組織への申請にはチームグループの選択が必要です | 申請 |
| `TEAM_068` | 403 | 現在この組織には申請（招待）できません | 申請・招待（冷却・ブロック共通）|
| `TEAM_069` | 422 | 同時に申請できる組織は10件までです | 申請 |
| `TEAM_070` | 404 | 申請・招待・加盟が見つかりません | membershipId を取る API 全般（越境・削除済みも同じ）|
| `TEAM_071` | 409 | この申請・招待はすでに処理されています | 承認・拒否・承諾・取下げ・取消（§6.4）|
| `TEAM_072` | 400 | 指定したチームグループは選択できません | 申請・招待・承認 |
| `BROADCAST_006` | 400 | 指定したチームグループは選択できません | 他組織・削除済みのグループ ID |
| `BROADCAST_007` | 400 | この組織ではチームグループ機能が無効です | グループ機能 off の組織でグループ指定 |
| `BROADCAST_008` | 400 | 範囲の指定が正しくありません（開始が終了より後ろです） | 範囲指定 |
| `BROADCAST_009` | 400 | 対象になる人がいません | 解決結果が0チームかつ直属メンバー0人 |
| `BROADCAST_010` | 400 | 個別に選べるチームは5,000までです。「すべてのチーム」かチームグループを使ってください | 「チームを選ぶ」の件数超過 |
| `BROADCAST_011` | 400 | チームの個別指定とチームグループ指定は同時に使えません | `targetTeamIds` とグループ項目の併用 |
| `BROADCAST_012` | 400 | チームの告知ではチームグループを指定できません | TEAM スコープでグループ項目を指定 |
| `BROADCAST_013` | 400 | テンプレートの範囲に削除されたチームグループが含まれています。範囲を選び直してください | テンプレートの範囲端が削除済み |

- `TEAM_064` を 403 にする理由: 組織の存在自体は（visibility 上見えるなら）公開情報であり、受付 off は「その組織の方針で拒否した」ことを表すため。組織が見えない場合は §6.1 step 4 で先に 404 を返すので、受付状態が存在オラクルになることはない。
- 招待先チームが見えない場合は、既存のチーム不在コード（404）を返し、`TEAM_065`・`TEAM_066`・`TEAM_068` は返さない（§6.5）。

---

## 12. Flyway と番人

- **major は実装 PR のマージ時に origin/main 全体の最大 major + 1 で確定する**（2026-09-26 時点の origin/main の最大は V224。予定値を本書に書かない）。minor は `date -u '+%Y%m%d%H%M%S'` のタイムスタンプ（連番禁止。`FlywayTimestampNamingGuardTest` が拒否する）。
- 1ファイル1論理変更とし、次の順に分ける。

| 順 | 内容 | 既存データ |
|---|---|---|
| 1 | `organizations` に設定4列を追加（§5.5）| 全組織が既定値（受付 off・グループ off・OFF・案内 NULL）|
| 2 | `org_team_groups` を作成（§5.2）| — |
| 3 | `team_org_memberships` に列・索引を追加（§5.3）| 既存行は `direction='ORG_INVITE'`・`group_id=NULL`・`message=NULL`・`updated_at`=移行時刻 |
| 4 | `team_org_affiliation_restrictions` を作成（§5.4）| — |
| 5 | `announcement_feeds` の3列と多値インデックス、`announcement_range_templates` の3列を追加（§5.6）| 既存行は NULL / FALSE（従来の挙動と同じ）|
| 6 | `notification_fanout_job_target_teams` を作成（§5.6）| — |
| 7 | `announcement_feed_group_snapshots` を作成（§5.6）| — |
| 8 | 権限 `MANAGE_ORG_AFFILIATION` を `permissions` に追加し、ADMIN の `role_permissions`（`is_default=1`）を追加（§3.2。V183 と同じ作法。DEPUTY_ADMIN・MEMBER の天井行は作らない）| 既存チームの ADMIN は即日この権限を持つ。DEPUTY・MEMBER は付与されるまで持たない |

- COMMENT 句を改行で分けて隣接文字列リテラルにしない（MySQL は連結しない）。
- 既存データの移行は列の DEFAULT だけで完結し、UPDATE 文は要らない。

**番人テストの要否**

| 番人 | 要否 | 理由 |
|---|---|---|
| `FlywayFromScratchMigrationTest` | 既存で足りる（通過必須）| 新規列・表の DDL と Entity の整合 |
| Entity↔DDL の UNIQUE 照合 | **要追加確認** | `uq_org_team_groups_org_active_name` と `uq_toar_org_team_dir` が試練の DB（ddl-auto）でも効いていることを IT で確かめる（Entity↔DDL 番人が CHECK しか見ていない既知の穴への対策）|
| `team_org_memberships` 読み手の status 絞り込み番人 | 不要 | §4.2 により終端状態を行として残さないため |
| **単一親前提の番人** `TeamOrgSingleParentAssumptionGuardTest` | **追加する** | §9.4 |
| 新規 EP の認可番人（ArchUnit）| 既存で足りる（対象に入ることを確認）| 新 Controller が認可アノテーションを持つこと |
| `*ScopeContractIT` | **追加する** | `TeamAffiliationScopeContractIT` / `OrgTeamGroupScopeContractIT`（非メンバー 403・越境 404・正当成功）。`AbstractTenantAwareRepository` を使わない2表のテナント絞り込みの担保を兼ねる（§5.1）|
| fan-out 宛先の番人 | **追加する** | グループ宛てのアンケート告知で `ORGANIZATION` スコープの fan-out ジョブが作られないこと（AC-H07）|
| 多値インデックスの上限実測 | **試練で実測する** | §8.3 の未検証事項 |
| バッチ登録の番人 | 既存で足りる | `BatchEndpointRegistry` が `@BatchEndpoint` の名前重複・引数ありを起動時に拒否する。§6.8 の3バッチが一覧 API（`GET /api/v1/system-admin/batch`）に出ることを IT で確かめる（AC-E06）|

---

## 13. フロントエンド画面と導線

**画面から辿り着けない機能を作らない。** 各機能の入口を次に定める。新しいページは `/統一`（PageHeader / SectionCard）と `/手助け` を適用する。

| 機能 | 画面（ルート） | 入口（どこから辿り着くか） | 表示条件・ガード |
|---|---|---|---|
| 組織の申請受付・グループ設定 | `/organizations/{slug}/settings/team-affiliation`（新規）| ①組織シェルの「所属チーム」タブ（`member-teams`）右上の「加盟の設定」ボタン ②管理コンソール `/organizations/{slug}/admin` のカード「チーム加盟の設定」| `definePageMeta({ layout: 'organization', middleware: ['auth', 'org-role-guard'] })`。`org-role-guard` は ADMIN でなければエラートーストを出して `/organizations/{slug}` へ戻す（AC-K01 の追い返し先）。ボタン・カードは組織 ADMIN・管理レンズのときだけ出す |
| 所属チーム一覧（グループ列・絞り込み・割当）| `/organizations/{slug}/member-teams`（既存を拡張）| 組織シェルのタブ | 閲覧は既存どおり。割当 UI は組織 ADMIN・管理レンズかつグループ機能 on |
| 組織側の申請一覧・承認・拒否 | `/organizations/{slug}/member-teams?view=applications` | ①`member-teams` 上部の切替「加盟申請（N）」 ②申請通知（`TEAM_ORG_APPLICATION_RECEIVED`）のリンク ③管理コンソールのカード（未処理件数バッジ）| 組織 ADMIN・管理レンズ。それ以外が `view=applications` 等で開いた場合は `view` を外した一覧表示に戻す（AC-K02）|
| 組織側の招待送信・一覧・取消 | `/organizations/{slug}/member-teams?view=invites` | `member-teams` 上部の切替「送った招待」、同画面の「チームを招待」ボタン（公開チーム検索ダイアログ）| 同上 |
| 申請制限の一覧・解除 | `/organizations/{slug}/member-teams?view=restrictions` | `member-teams` 上部の切替「申請の制限」| 同上 |
| チームグループ管理（作成・改名・並び替え・削除）| `/organizations/{slug}/member-teams?view=groups` | `member-teams` 上部の切替「チームグループ」、設定画面の「グループを管理」リンク | 同上かつグループ機能 on（off なら設定画面への誘導カードを出す）|
| チーム側: 加盟先の管理（所属組織・受信招待・申請中・招待の制限）| `/teams/{slug}/affiliations`（**チームシェルの新タブ「所属組織」**）| チームシェルのタブ、招待・承認・拒否・期限切れ通知のリンク（§6.7）| タブの表示条件と滞在ガードは下記「チームシェルへのタブ追加」|
| チーム側: 権限グループ（加盟操作権限の付与）| `/teams/{slug}/permission-groups`（**チームシェルの新タブ「権限グループ」**）| チームシェルのタブ、メンバー一覧（`/teams/{slug}/members`）の各メンバーの操作メニュー「権限グループ」| チーム ADMIN・管理レンズのみ（§3.2）|
| チーム側: 加盟申請 | 申請ダイアログ `TeamAffiliationApplyDialog` | ①組織シェル（`/organizations/{slug}`）のヘッダーの「チームとして加盟を申請」ボタン ②公開ページ `/public/organizations/{slug}` の同ボタン ③`/teams/{slug}/affiliations` の「加盟先の組織を探す」→ `/discover/organizations?acceptingTeamApplications=true` → 組織カード → 公開ページ → ② | 組織が受付中 **かつ** 閲覧者がいずれかのチームで加盟操作権限を持つ（§10.3 の `canApply`）。どちらかを満たさなければボタン自体を出さない。未ログインでは出さない |
| 受付中の組織を探す | `/discover/organizations`（既存を拡張。`GET /api/v1/public/organizations/search` を使う）| ①`/teams/{slug}/affiliations` の「加盟先の組織を探す」ボタン（ログイン中のチームの加盟操作者向けの主導線）②ランディングページの既存リンク（`LandingHero` / `LandingCta` / `LpHero`）| 誰でも。絞り込みチップ「チーム加盟を受付中」と、カードの「加盟受付中」バッジ。ページは URL クエリ `acceptingTeamApplications=true` を読んでチップの初期状態にする（main の同ページは URL クエリを読んでいないため、この読み取りを追加する）|
| お知らせのグループ選択 | 告知ウィザード ステップ1 `BroadcastStep1Audience.vue`（既存を拡張）| 組織ダッシュボードの「+ 組織内告知」| 組織スコープかつグループ機能 on |
| 範囲テンプレートのグループ保存 | 同ステップ1の「テンプレートとして保存」| 同上 | ADMIN / DEPUTY_ADMIN(MANAGE_CONTENT) |

**チームシェルへのタブ追加**（`frontend/app/pages/teams/[slug].vue`）

| 追加先 | 内容 |
|---|---|
| `SHELL_SEGMENTS` | `'affiliations'` と `'permission-groups'` を追加する |
| `SEGMENT_TO_TAB` | `affiliations: 'affiliations'`、`'permission-groups': 'permission-groups'` を追加する |
| タブ定義 `tabs` | `{ key: 'affiliations', to: \`${base}/affiliations\`, icon: 'pi pi-sitemap', labelKey: 'teamShell.tab.affiliations', visible: adminLens && (isAdmin \|\| permissions.includes('MANAGE_ORG_AFFILIATION')) }`、`{ key: 'permission-groups', ..., labelKey: 'teamShell.tab.permissionGroups', visible: adminLens && isAdmin }` |
| 滞在ガード | `permission-groups` は既存の `ADMIN_ONLY_SEGMENTS` と同じ扱い（ただし DEPUTY も追い返すため `isAdmin` で判定）。`affiliations` は「レンズ OFF、または `isAdmin` でも `MANAGE_ORG_AFFILIATION` 保持者でもない」とき `/teams/{slug}` へ戻す watch を追加する。権限は `useRoleAccess('team', slug)` の `permissions`（`GET /api/v1/teams/{slug}/me/permissions`）で判定する |
| 親シェルの middleware | 付けない（既存の方針どおり。子ページ側で `auth` を付ける）|

画面の要点:

- `member-teams` の切替はクエリ `view` で行い、子ルートを作らない（永続シェル配下の兄弟ルートには親側 `<NuxtPage>` が要る既知の罠を避ける）。`/teams/{slug}/affiliations` も同様に `view=invites` 等のクエリで区画を切り替える。
- 申請ダイアログ: チーム選択（`myTeams`。`affiliationStatus` が `NONE` 以外は選べず理由を表示）→ 案内文 → グループ選択（`groupMode` が REQUIRED なら必須表示・未選択では送信ボタンが押せない、OPTIONAL なら「選ばない」を含む）→ 添え書き → 送信。
- 承認ダイアログ: 希望グループを初期選択したドロップダウン（「未分類」を含む）と「承認」ボタン。変えた場合は「希望: {A} → 承認時: {B}」と表示し、`overrideGroup=true` と選んだ `groupId`（未分類なら null）を送る。希望のままなら `overrideGroup=false` を送る。
- 拒否ダイアログ: 理由（任意）と「今後この組織（チーム）からの申請を受け付けない」チェック。
- 受付を off にする操作で PENDING が残っていれば確認ダイアログ（§4.3）。
- 告知ウィザードのステップ1（組織・グループ機能 on）:
  - 「対象チーム」: 「すべてのチーム」「チームを選ぶ」「チームグループで選ぶ」
  - 「チームグループで選ぶ」を選ぶと、グループのチェックリスト（並び順）、「範囲で選ぶ」（種類: 〜以前／〜以降／A〜B とグループのドロップダウン）、「未分類のチームを含める」、「該当: N チーム＋直属メンバー M 人」のプレビューを出す。0件なら「次へ」を押せない。
  - `pushEnabled=false`（組織 MEMBER・MANAGE_CONTENT を持たない DEPUTY、または push の無いチャネル）のときは「このお知らせの push 通知は送られません」と表示する。
  - 送信履歴には「表示は現在の所属で判定されます（削除されたグループは送信時点の対象チームで判定）」と注記する（§8.2）。
  - テンプレートを読み込んだとき、削除済みグループを除外した旨を警告表示する。範囲の端が削除済みなら範囲を空にして再選択を求める。
- お知らせ欄の送信者向け詳細（送信履歴）に `targetAudience` から「宛先: 平成20年度以前（送信時 12 チーム）」と表示する。

---

## 14. i18n キー

### 14.1 フロントエンド

6言語（ja / en / zh / ko / es / de）すべてに追加する。未翻訳の言語は日本語と同じ値でよい。新しいロケールファイル `team_affiliation.json` は **`nuxt.config.ts` の locales の files に6言語とも登録する**（登録漏れはキーがそのまま表示される既知の罠）。

`frontend/app/locales/{lang}/team_affiliation.json`（新規）:

```json
{
  "teamAffiliation": {
    "settings": {
      "title": "チーム加盟の設定",
      "entry_button": "加盟の設定",
      "application_enabled": "チームからの加盟申請を受け付ける",
      "application_enabled_hint": "オンにすると、組織ページに「チームとして加盟を申請」ボタンが表示されます",
      "groups_enabled": "チームグループを使う",
      "groups_enabled_hint": "加盟チームを年度や地区などのグループに分け、お知らせの宛先に使えます",
      "group_mode": "申請時のチームグループ選択",
      "group_mode_off": "選ばせない",
      "group_mode_optional": "任意で選べる",
      "group_mode_required": "必ず選ばせる",
      "guidance": "申請フォームの案内文",
      "turn_off_pending_confirm": "受付済みの申請が {count} 件残っています。受付を停止しても、これらの申請は申請一覧から承認・拒否できます。",
      "required_mode_degraded": "グループが1件も無いため、グループ選択は「任意」として扱われています"
    },
    "view": {
      "teams": "所属チーム",
      "applications": "加盟申請",
      "invites": "送った招待",
      "restrictions": "申請の制限",
      "groups": "チームグループ"
    },
    "apply": {
      "button": "チームとして加盟を申請",
      "dialog_title": "{org} への加盟申請",
      "select_team": "申請するチーム",
      "select_group": "チームグループ",
      "group_none": "選ばない",
      "message": "添え書き（任意）",
      "submit": "申請する",
      "success": "加盟を申請しました。組織の承認をお待ちください",
      "expires_hint": "{date} までに応答が無い場合、申請は自動で取り消されます",
      "status": {
        "NONE": "申請できます",
        "APPLYING": "申請中です",
        "INVITED": "この組織から招待が届いています",
        "ACTIVE": "加盟済みです",
        "UNAVAILABLE": "現在この組織には申請できません"
      },
      "find_orgs": "加盟先の組織を探す"
    },
    "review": {
      "approve": "承認",
      "reject": "拒否",
      "approve_dialog_title": "{team} の加盟を承認",
      "approve_group": "所属させるチームグループ",
      "requested_group": "希望: {requested} → 承認時: {decided}",
      "unassigned": "未分類",
      "reject_reason": "理由（任意・相手チームに通知されます）",
      "reject_block": "今後このチームからの申請を受け付けない",
      "approved": "承認しました",
      "rejected": "拒否しました"
    },
    "invite": {
      "button": "チームを招待",
      "accept": "承諾",
      "decline": "辞退",
      "decline_block": "今後この組織からの招待を受け付けない",
      "cancel": "招待を取り消す"
    },
    "restriction": {
      "kind_COOLDOWN": "冷却期間中（{until} まで）",
      "kind_BLOCK": "ブロック中",
      "lift": "解除"
    },
    "leave": {
      "button": "この組織から離脱",
      "confirm": "{org} から離脱しますか？ 組織からのお知らせが届かなくなります（他の組織への加盟には影響しません）"
    },
    "remove": {
      "button": "除名",
      "confirm": "{team} を組織から除名しますか？"
    },
    "badge_accepting": "加盟受付中",
    "public_page_apply_hint": "あなたが加盟の手続きを任されているチームから申請できます",
    "filter_accepting": "チーム加盟を受付中"
  },
  "teamGroup": {
    "title": "チームグループ",
    "create": "チームグループを作成",
    "name": "グループ名",
    "description": "説明",
    "team_count": "{count} チーム",
    "unassigned": "未分類",
    "unassigned_count": "未分類のチーム: {count}",
    "reorder_hint": "並び順は、お知らせの「〜以前／〜以降」の基準になります",
    "delete_confirm": "「{name}」を削除しますか？ 所属している {count} チームは未分類に戻ります。このグループ宛ての過去のお知らせは、送信時点で対象だったチームに引き続き表示されます",
    "limit_reached": "チームグループは200件までです",
    "assign": "グループを割り当てる",
    "bulk_assign": "選択したチームをグループに割り当てる",
    "filter_all": "すべてのグループ",
    "disabled_cta": "チームグループ機能は無効です。設定から有効にできます"
  }
}
```

`frontend/app/locales/{lang}/announcement.json` の `announcement` に追加:

```json
{
  "target_mode_all": "すべてのチーム",
  "target_mode_teams": "チームを選ぶ",
  "target_mode_groups": "チームグループで選ぶ",
  "target_groups_label": "チームグループ",
  "target_range_label": "範囲で選ぶ",
  "target_range_up_to": "{name} 以前（並び順で先頭から）",
  "target_range_from": "{name} 以降（並び順で末尾まで）",
  "target_range_between": "{from} 〜 {to}",
  "target_range_kind_up_to": "〜以前",
  "target_range_kind_from": "〜以降",
  "target_range_kind_between": "A〜B",
  "target_include_unassigned": "未分類のチームを含める",
  "target_direct_members_note": "組織の直属メンバー（どのチームにも属さない人）にも届きます",
  "target_resolved_count": "該当: {teams} チーム＋直属メンバー {members} 人",
  "target_resolved_empty": "該当する人がいません",
  "target_order_hint": "並び順はチームグループ管理で変えられます",
  "push_disabled_note": "このお知らせの push 通知は送られません",
  "dynamic_display_note": "表示は現在の所属で判定されます（削除されたグループは送信時点の対象チームで判定）",
  "template_deleted_groups_removed": "削除されたチームグループがテンプレートから除外されました",
  "template_range_invalid": "テンプレートの範囲に削除されたチームグループが含まれています。範囲を選び直してください",
  "audience_summary": "宛先: {label}（送信時 {count} チーム）"
}
```

`frontend/app/locales/{lang}/common.json` の既存 `teamShell.tab` に `affiliations`（「所属組織」）と `permissionGroups`（「権限グループ」）を追加する。管理コンソールのカード文言は `frontend/app/locales/{lang}/admin_console.json`（トップレベル `adminConsole`）に `adminConsole.card.teamAffiliation`（「チーム加盟の設定」）と `adminConsole.card.teamAffiliationDescription`（「チームからの加盟申請の受付とチームグループを設定します」）を追加する。

### 14.2 バックエンド（`backend/src/main/resources/messages*.properties`）

`messages.properties`（既定）と `messages_{ja,en,zh,ko,es,de}.properties` の7ファイルすべてに追加する。未翻訳の言語は日本語と同じ値でよい。`{0}` 等は `MessageFormat` の引数で、利用者が書いた文字列（組織名・チーム名・理由）をそのまま差し込む。

通知文面（加盟の通知。§6.7）:

| キー | ja |
|---|---|
| `notification.teamAffiliation.applicationReceived.title` | 加盟申請が届きました |
| `notification.teamAffiliation.applicationReceived.body` | {0} から {1} への加盟申請が届きました |
| `notification.teamAffiliation.applicationApproved.title` | 加盟が承認されました |
| `notification.teamAffiliation.applicationApproved.body` | {0} への加盟が承認されました（チームグループ: {1}） |
| `notification.teamAffiliation.applicationRejected.title` | 加盟申請は承認されませんでした |
| `notification.teamAffiliation.applicationRejected.body` | {0} への加盟申請は承認されませんでした |
| `notification.teamAffiliation.applicationRejected.bodyWithReason` | {0} への加盟申請は承認されませんでした。理由: {1} |
| `notification.teamAffiliation.inviteReceived.title` | 組織から招待が届きました |
| `notification.teamAffiliation.inviteReceived.body` | {0} から {1} への加盟の招待が届きました |
| `notification.teamAffiliation.inviteAccepted.title` | 招待が承諾されました |
| `notification.teamAffiliation.inviteAccepted.body` | {0} が {1} への加盟を承諾しました |
| `notification.teamAffiliation.pendingExpired.title` | 申請（招待）の期限が切れました |
| `notification.teamAffiliation.pendingExpired.body` | {0} と {1} の間の申請（招待）は、60日間応答が無かったため取り消されました |
| `notification.teamAffiliation.pendingCancelledBySystem.title` | 申請（招待）が取り消されました |
| `notification.teamAffiliation.pendingCancelledBySystem.body` | {0} が削除またはアーカイブされたため、申請（招待）は取り消されました |
| `notification.teamAffiliation.group.unassigned` | 未分類 |

- 「チームグループ: {1}」の `{1}` はグループ名（利用者が書いた文字列）か、未分類なら `notification.teamAffiliation.group.unassigned` の値を差し込む。
- 各キーと §6.7 の `notification_type` の対応: applicationReceived=`TEAM_ORG_APPLICATION_RECEIVED`、applicationApproved=`TEAM_ORG_APPLICATION_APPROVED`、applicationRejected(.bodyWithReason)=`TEAM_ORG_APPLICATION_REJECTED`、inviteReceived=`TEAM_ORG_INVITE_RECEIVED`、inviteAccepted=`TEAM_ORG_INVITE_ACCEPTED`、pendingExpired=`TEAM_ORG_PENDING_EXPIRED`、pendingCancelledBySystem=`TEAM_ORG_PENDING_CANCELLED_BY_SYSTEM`。
- 権限の表示名: `permissions.display_name` は Flyway で「組織への加盟操作」を入れる（権限名の多言語化は既存の権限と同じ扱い）。
- グループ宛てアンケートの push 文面は新設せず、既存の `notification.fanout.survey.published.title` / `.body` を使う（§8.5.3）。

エラーメッセージ（`GlobalExceptionHandler.resolveMessage` の規則 `error.{domain}.{number}`）:

| キー | ja |
|---|---|
| `error.org.064` 〜 `error.org.070` | §11 の `ORG_064`〜`ORG_070` のメッセージ |
| `error.team.064` 〜 `error.team.072` | §11 の `TEAM_064`〜`TEAM_072` のメッセージ |
| `error.broadcast.006` 〜 `error.broadcast.013` | §11 の `BROADCAST_006`〜`BROADCAST_013` のメッセージ |

---

## 15. 非機能要件

| 観点 | 設計 |
|---|---|
| 規模の前提 | 1,000万ユーザー。1組織の加盟チームは最大で数万（大規模連盟）、グループは200件まで。1チームの加盟組織数は通常数件 |
| 申請一覧 | `(organization_id, status, direction, invited_at)` で範囲走査。PENDING は通常少数 |
| 加盟チーム一覧のグループ絞り込み・グループ所属数 | `(organization_id, group_id, status)` の索引だけで絞り込み・集計できる |
| 宛先展開（push のスナップショット） | グループ ID 集合 → `(organization_id, group_id, status)` で ACTIVE のチームを取得。件数上限なし（ジョブ子表の行数＝宛先チーム数）|
| 表示判定（グループ宛て） | チームダッシュボードでは、閲覧中チームの「組織ごとの所属グループ」を `(team_id, status, direction)` 索引で1回引き、親組織の告知候補を `target_group_ids` の多値インデックスで絞る。削除済みグループの判定用に、閲覧中チームのスナップショット行を `(team_id, feed_id)` 索引で候補フィードの範囲だけ1回引く。候補の判定は共通の `AnnouncementAudienceMatcher` に集約し、`DashboardService` の既存判定（`target_team_ids`）と並べて使う |
| 表示判定（チームを選ぶ） | 既存の多値インデックス（`target_team_ids`）のまま。5,000件の上限は**未検証**（§8.3）|
| push fan-out | 1ジョブ＋宛先チーム子表。受信者数は宛先チームのメンバー＋直属メンバー（数千〜数十万）で、既存のシャード・キーセットの仕組みに乗る。enqueue は O(宛先チーム数) で、告知のトランザクション内で行う |
| 通知（加盟）| 相手側 ADMIN のみ・同期送信 |
| 後方互換 | 既存行は `ORG_INVITE`・未分類。グループ機能・申請受付は既定 off で、何もしなければ従来と同じ。「すべてのチーム」の告知は表示も push も従来どおり。**「チームを選ぶ」は、アンケートの push が組織全体から「選んだチーム＋直属メンバー」に変わり（MEMBER の送信は push なし）、5,000件の上限が新たに掛かる**（§8.5.1。表示は変わらない）。参照系 API はフィールド追加のみ。§9 の改修は、複数加盟が無いデータでは結果が変わらない |
| 段階リリース | ①§9 の単一親前提の改修＋DDL＋加盟書き込み API（招待・申請・承認）＋画面、②チームグループ＋割当、③お知らせのグループ宛て（表示の動的判定・push の絞り込み・テンプレート）。表示と push は同じリリース（③）に入れる |
| 監査 | すべての状態遷移とグループの作成・変更・削除・割当を `audit_logs` に記録 |
| 個人情報 | `message` は PENDING の間だけ保持し、ACTIVE 化・削除で消える。GDPR の個人データ収集（`PersonalDataCollector`）の対象に `invited_by` / `responded_by` が入っていることを確認する（現状は未実装の旨がログに出ている）|

---

## 16. 受け入れ条件

形式: 「誰が・何をしたら・どうなる（観測できる結果）」。ロールは次の固定人物で書く。

- 組織X の ADMIN = **XA**、組織X の MEMBER = **XM**、組織X の DEPUTY_ADMIN（MANAGE_CONTENT あり）= **XD**、組織X の DEPUTY_ADMIN（MANAGE_CONTENT なし）= **XD2**、組織X の直属メンバー（どのチームにも属さない役員）= **XO**
- チームT の ADMIN = **TA**（組織X には未加盟）、チームT の DEPUTY_ADMIN（加盟操作権限なし）= **TD**、チームT の MEMBER（加盟操作権限なし）= **TM**、チームT の MEMBER で権限グループにより `MANAGE_ORG_AFFILIATION` を付与された人 = **TG**
- 組織Y（別組織）の ADMIN = **YA**、組織Y に加盟するチームU
- どこにも属さない認証済みユーザー = **N**

### 16.0 実機E2E の共通手順

各 AC の「検証」列は、**実機**（実際に起動した BE・FE・MySQL をモックなしで操作する）か **IT 限定**（実機では状態を作れない・観測できないもの。理由を併記）のどちらかを示す。実機の AC は、この節の共通手順に従って準備→操作→観測する。

| 手順 | 方法 |
|---|---|
| 固定人物の用意 | 上の XA〜N をシードまたは API で作る。TG は、TA が `/teams/T/permission-groups` で `MANAGE_ORG_AFFILIATION` を含む権限グループ（target_role=MEMBER）を作り、メンバー一覧から TG に割り当てて作る |
| **時間を進める** | テスト用の Clock エンドポイントは作らない。**DB の日時列を過去へずらす準備**をする（例: `UPDATE team_org_affiliation_restrictions SET restricted_until = restricted_until - INTERVAL 31 DAY WHERE id = UUID_TO_BIN(?)`、`UPDATE team_org_memberships SET invited_at = invited_at - INTERVAL 61 DAY WHERE id = ?`）。判定は `wallClock` の現在時刻と比べるので、列をずらせば「時間が経った」状態になる |
| **バッチを動かす** | SYSTEM_ADMIN で `POST /api/v1/system-admin/batch/{name}/trigger?sync=true` を呼ぶ（`team-org-pending-expiry` / `team-org-group-orphan-repair` / `team-org-lifecycle-cleanup`。§6.8）。`sync=true` なので応答が返った時点で処理は終わっている |
| **push を観測する** | 受信者でログインし、通知一覧（`GET /api/v1/notifications`、画面では通知ベル）を見る。fan-out ワーカーは5秒間隔（`NotificationFanoutWorker` の `fixedDelay = 5_000`）なので、送信から**10秒以上**待ってから観測する。「届かない」ことの確認も同じ待ち時間を置く |
| ジョブの観測 | fan-out ジョブの種類・宛先は DB の `notification_fanout_jobs`（`scope_type`）と `notification_fanout_job_target_teams` を SELECT して確かめる |
| URL 直打ち | ブラウザのアドレス欄に直接入力し、表示された画面と URL（追い返し先）とトーストを観測する |

### A. 申請受付の設定

| ID | 条件 | 検証 |
|---|---|---|
| AC-A01 | 新規作成した組織の設定を XA が GET すると `teamApplicationEnabled=false`、`teamGroupsEnabled=false`、`applicationGroupMode="OFF"` が返る | 実機 |
| AC-A02 | XA が受付を on にして PUT すると 200。以後 `GET /organizations/X` の `teamApplication.enabled` が true になる | 実機 |
| AC-A03 | XD・XM・TA・YA が設定を GET/PUT すると 403 | 実機 |
| AC-A04 | 未認証で設定 API を呼ぶと 401 | 実機（未ログインで API を呼ぶ） |
| AC-A05 | グループ機能 off のまま `applicationGroupMode="REQUIRED"` を PUT すると 422 `ORG_070`、設定は変わらない | 実機 |
| AC-A06 | グループ機能 on・生存グループ0件で REQUIRED を PUT すると 422 `ORG_070` | 実機 |
| AC-A07 | PENDING 申請が2件ある状態で XA が受付を off にすると 200 で、2件はそのまま残り、XA は2件とも承認・拒否できる | 実機 |
| AC-A08 | 受付 off の組織Xのページを TA が開くと「チームとして加盟を申請」ボタンが表示されない | 実機 |
| AC-A09 | 受付 on の組織Xのページを、どのチームでも加盟操作権限を持たない N・TM・TD が開くとボタンが表示されない | 実機 |
| AC-A10 | `/discover/organizations` で「チーム加盟を受付中」に絞ると、`GET /api/v1/public/organizations/search?acceptingTeamApplications=true` が呼ばれ、受付 on の組織だけが出て、カードに「加盟受付中」バッジが付く。`/discover/organizations?acceptingTeamApplications=true` を直接開いても同じ絞り込みで表示される | 実機 |
| AC-A11 | **（M3）** 受付 on の公開組織Xの公開ページ `/public/organizations/X` を TA または TG がログインして開くと「チームとして加盟を申請」ボタンが出て、押すと申請ダイアログが開く。TM・TD・N（ログイン中）には出ない。未ログインでは出ない | 実機 |
| AC-A12 | **（M3・存在オラクルなし）** `GET /api/v1/me/org-affiliation-eligibility?organizationSlug=` に、存在しない slug・非公開組織の slug・受付 off の組織の slug を渡すと、3つとも 200 `{canApply:false}` で応答本文が同一である。公開 API `GET /api/v1/public/organizations/{id}` は、受付 off の公開組織に `acceptingTeamApplications=false` を返し、非公開・存在しない組織には従来と同じ不在の応答を返す（受付フラグの有無で応答が変わらない）| 実機 |

### B. チームからの申請

| ID | 条件 | 検証 |
|---|---|---|
| AC-B01 | 受付 on の組織Xへ TA が申請すると 201 で、X の申請一覧に PENDING/TEAM_APPLY が1件増え、XA に通知が1件届く | 実機 |
| AC-B02 | 受付 off の組織Xへ TA が API で直接申請すると 403 `TEAM_064` で、行は作られない | 実機 |
| AC-B03 | 受付 off の組織Xの申請フォーム API を TA が直打ちすると 403 `TEAM_064` | 実機 |
| AC-B04 | TM・TD（加盟操作権限の無いチームの MEMBER・DEPUTY_ADMIN）が申請すると 403、行は作られない。TG が申請すると 201 | 実機 |
| AC-B05 | 受付 on でも visibility 上 TA から見えない組織へ申請すると、存在しない slug と同じ 404 が返る | 実機 |
| AC-B06 | 既に加盟済みのチームで申請すると 409 `TEAM_065` | 実機 |
| AC-B07 | 同じ組織へ2回申請すると2回目は 409 `TEAM_066`。同時に2件送っても行は1件だけ | 実機（逐次の2回目）＋ IT 限定（同時2件の並行送信は実機で再現できないため） |
| AC-B08 | 組織Xから招待が届いている状態で TA が X へ申請すると 409 `TEAM_066` | 実機 |
| AC-B09 | PENDING 申請を10件持つチームが11件目を申請すると 422 `TEAM_069`。10件目までは成功する（境界）| 実機（準備: 受付 on の組織を10件用意し、API で10件申請してから11件目を送る） |
| AC-B10 | グループ選択 REQUIRED の組織へ group_id なしで申請すると 400 `TEAM_067`。画面では送信ボタンが押せない | 実機 |
| AC-B11 | グループ選択 OFF の組織へ group_id 付きで申請すると 400 `TEAM_072` | 実機 |
| AC-B12 | 組織Yのグループ ID を付けて組織Xへ申請すると 400 `TEAM_072`（削除済みグループを付けた場合と同じ応答）| 実機 |
| AC-B13 | TA が申請を取り下げると 204 で行が消え、XA の申請一覧から消える。直後の再申請は 403 `TEAM_068`、24時間後（時計を進めて）は 201 | 実機（準備なしで取下げ→即再申請して 403 を観測→ `UPDATE team_org_affiliation_restrictions SET restricted_until = restricted_until - INTERVAL 25 HOUR` →再申請して 201 を観測） |
| AC-B14 | 取下げ→承認の順に実行すると、承認は 404 `TEAM_070`。承認→取下げの順に実行すると、取下げは 409 `TEAM_071`。並行に実行した場合は、ちょうど一方だけが 2xx を返し、最終状態は「行が無い」か「ACTIVE」のどちらか一方になる | 実機（逐次の2通りの順序）＋ IT 限定（並行実行は実機で競合を再現できないため） |
| AC-B15 | message に 501 文字を送ると 400、500 文字は成功する | 実機 |
| AC-B16 | 組織Yに加盟済みのチームUの ADMIN が組織Xへ申請すると 201（他組織への加盟は申請を妨げない）| 実機 |

### C. 承認・拒否・制限

| ID | 条件 | 検証 |
|---|---|---|
| AC-C01 | XA が `overrideGroup=false` で承認すると 200 で、加盟が ACTIVE になり、申請時の希望グループに所属する。TA に通知が1件届く | 実機 |
| AC-C02 | XA が別グループを指定して承認すると、指定したグループに所属し、監査ログに希望と確定の両方が残る | 実機 |
| AC-C03 | XA が `overrideGroup=true`・`groupId=null` で承認すると未分類で加盟する（REQUIRED の組織でも成功する）| 実機 |
| AC-C04 | チームT が組織Y に加盟済みの状態で組織X への申請が承認されると、次がすべて成り立つ: (a) `GET /organizations/X/teams` と `GET /organizations/Y/teams` の両方に T が出る (b) `GET /teams/T/organizations` に X と Y の2件が出て、X の要素にグループ名が付く (c) T のメンバーが、X の ORGANIZATION_WIDE 投稿と Y の ORGANIZATION_WIDE 投稿の両方を閲覧できる（`ScopeAncestorResolver` が X・Y の両方を返す）(d) `GET /me/teams` の T の要素に X と Y の両方の組織が含まれる | 実機 |
| AC-C05 | XD・XM・YA が承認・拒否すると 403 | 実機 |
| AC-C06 | YA が組織Y の slug で組織X宛て申請の membershipId を承認すると 404 `TEAM_070`（存在しない ID と同じ応答）| 実機 |
| AC-C07 | XA が拒否すると 200 で行が消え、TA に通知（理由を付けた場合は理由入り）が届く。TA が直後に再申請すると 403 `TEAM_068` | 実機 |
| AC-C08 | 拒否から30日を過ぎると、TA の再申請が 201 になる（30日以内は 403 `TEAM_068`）。判定は `wallClock` の現在時刻と `restricted_until` の比較で行い、SQL の `NOW()` は使わない（§4.6）| 実機（準備: 拒否後に `UPDATE team_org_affiliation_restrictions SET restricted_until = restricted_until - INTERVAL 31 DAY` →操作: TA が再申請→観測: 201。ずらす前は 403）。IT では `wallClock` を固定 Clock に差し替えて同じことを確かめる |
| AC-C09 | `block=true` で拒否すると、30日後でも 403 `TEAM_068`。XA が制限一覧から解除すると、直後の申請が 201 | 実機（準備: `block=true` で拒否。BLOCK は `restricted_until` が NULL なので日時をずらしても 403 のまま→解除→再申請で 201） |
| AC-C10 | 冷却中とブロック中で、TA が受け取るステータス・エラーコード・メッセージが同一である | 実機 |
| AC-C11 | 申請フォーム API の `myTeams` で、制限中のチームは `UNAVAILABLE` になり、冷却かブロックかは区別されない | 実機 |
| AC-C12 | XA が別組織Yの制限 ID を解除しようとすると 404 | 実機 |
| AC-C13 | 組織Xがアーカイブされた後に、アーカイブ前から開いていた承認ダイアログで XA が承認すると 422（既存のアーカイブ拒否）で、行は PENDING のまま（片付けバッチ後は消える）| 実機（準備: 承認ダイアログを開いた状態で、別タブから組織Xをアーカイブ→操作: 承認→観測: 422 と PENDING のまま） |
| AC-C14 | 申請中にチームT が削除されると、`TeamDeletedEvent` 処理後に行が消え、XA に取消の通知が届く。処理前に XA が承認すると 404 `TEAM_070` | 実機（前半: チーム削除後に行が消え通知が届く）＋ IT 限定（後半: イベント処理前の瞬間を実機で作れないため） |

### D. 組織からの招待

| ID | 条件 | 検証 |
|---|---|---|
| AC-D01 | XA が公開チームT を招待すると 201 で、TA の受信招待一覧に1件出て、TA に通知が届く | 実機 |
| AC-D02 | TA が承諾すると 200 で ACTIVE になり、招待時に指定したグループに所属する | 実機 |
| AC-D03 | TA が辞退すると行が消え、XA が直後に再招待すると 403 `TEAM_068`。`block=true` なら30日後も同じ | 実機（30日後の確認は AC-C08 と同じく `restricted_until` をずらす） |
| AC-D04 | TA が招待の制限一覧から解除すると、XA は再招待できる | 実機 |
| AC-D05 | XA が招待を取り消すと 204、TA の受信一覧から消える。直後の再招待は 403 `TEAM_068`、24時間後は 201 | 実機（24時間後の確認は AC-B13 と同じく `restricted_until` をずらす） |
| AC-D06 | TM が承諾・辞退すると 403 | 実機 |
| AC-D07 | チームU（組織Yの加盟チーム）の ADMIN が、チームT宛て招待の membershipId を自チームの slug で承諾すると 404 `TEAM_070` | 実機 |
| AC-D08 | 既存（移行前）の PENDING 招待行を TA が承諾でき、`direction="ORG_INVITE"` として扱われる | 実機 |
| AC-D09 | XA から見えない非公開チームP を招待すると、存在しない slug を指定した場合と**同じステータス・同じエラーコード**（既存のチーム不在 404）が返る。P が組織Xに加盟済み・制限中・申請中のいずれであっても応答は変わらない | 実機 |

### E. 離脱・除名・期限切れ

| ID | 条件 | 検証 |
|---|---|---|
| AC-E01 | TA が組織Xから離脱すると 204 で行が消え、以後 X の組織告知が T のダッシュボードに出ない。T が組織Yにも加盟していれば、Y の告知は引き続き出る | 実機 |
| AC-E02 | XA がチームTを除名すると 204 で行が消える。再加盟したときは未分類から始まる | 実機 |
| AC-E03 | 未加盟の組み合わせで離脱・除名すると 404 `TEAM_070` | 実機 |
| AC-E04 | 60日応答の無い PENDING 申請は、期限切れバッチの実行後に消え、TA に期限切れの通知が届き、チームの同時申請数が1減る| 実機（準備: `UPDATE team_org_memberships SET invited_at = invited_at - INTERVAL 61 DAY WHERE id = ?` →操作: `POST /api/v1/system-admin/batch/team-org-pending-expiry/trigger?sync=true` →観測: 行が消え、TA の通知一覧に `TEAM_ORG_PENDING_EXPIRED`、同時申請数が1減る） |
| AC-E06 | `GET /api/v1/system-admin/batch` の一覧に `team-org-pending-expiry`・`team-org-group-orphan-repair`・`team-org-lifecycle-cleanup` の3件が出て、それぞれ `trigger?sync=true` で 200 が返る | IT 限定（登録の確認は起動した Spring Boot に対する API 呼び出しで足りるが、一覧の網羅は IT で固定する）＋実機（trigger の 200） |
| AC-E05 | 組織Xをアーカイブすると、X に対する PENDING（申請・招待とも）が消え、相手側 ADMIN に取消の通知が届く。ACTIVE の加盟は残る | 実機（準備: PENDING の申請と招待を1件ずつ作る→操作: 組織Xをアーカイブ→観測: 行が消え、相手側の通知一覧に `TEAM_ORG_PENDING_CANCELLED_BY_SYSTEM`。イベントが無い経路の取りこぼしは `team-org-lifecycle-cleanup` を trigger して確かめる） |

### F. チームグループ

| ID | 条件 | 検証 |
|---|---|---|
| AC-F01 | グループ機能 on の組織Xで XA がグループを作成すると 201 で、並び順の末尾に入る | 実機 |
| AC-F02 | 生存グループと同名で作成すると 409 `ORG_065`。削除済みグループと同名なら作成できる | 実機 |
| AC-F03 | 200件ある状態で201件目を作ると 422 `ORG_066`。200件目は成功する（境界）| 実機 |
| AC-F04 | グループ機能 off の組織でグループ API を呼ぶと 409 `ORG_067` | 実機 |
| AC-F05 | XM がグループ一覧を GET すると 200、作成・変更・削除すると 403 | 実機 |
| AC-F06 | YA が組織Xのグループ一覧を GET すると 403、組織Xのグループ ID を組織Y の slug で PATCH すると 404 `ORG_064` | 実機 |
| AC-F07 | 並び替えで ID 集合が現在と一致しないと 409 `ORG_068` で順序は変わらない | 実機 |
| AC-F08 | 3チームが所属するグループを削除すると 204。削除直後（付け替えリスナーの処理前）でも、加盟チーム一覧の3チームは「未分類」と表示され、グループ一覧と選択肢から消える。リスナー処理後は DB の `group_id` も NULL になる | 実機（削除後の表示とリスナー処理後の DB）＋ IT 限定（「リスナー処理前」の瞬間を実機で作れないため） |
| AC-F09 | グループ機能を off→on に戻すと、off 前のグループと割当がそのまま戻る | 実機 |
| AC-F10 | チームTの ADMIN・メンバーは、`GET /teams/T/organizations` で自チームのグループ名だけを見られ、組織Xの他のグループ一覧は取得できない | 実機 |
| AC-F11 | 付け替えリスナーが失敗して `group_id` が削除済みグループを指したまま残っても、夜間の修復バッチ後に NULL になる | 実機（準備: `UPDATE team_org_memberships SET group_id = <削除済みグループの ID>` →操作: `trigger` で `team-org-group-orphan-repair` →観測: `group_id` が NULL） |

### G. 割当

| ID | 条件 | 検証 |
|---|---|---|
| AC-G01 | 移行直後、既存の加盟チームはすべて未分類で、`member-teams` を「未分類」で絞ると全件が出る | 実機 |
| AC-G02 | XA が1チームをグループAに割り当てると 200 で、一覧のグループ列が A になる | 実機 |
| AC-G03 | XA が3チームを一括でグループBに割り当てると `updated_count=3` | 実機 |
| AC-G04 | 一括割当に未加盟チームが1件でも含まれると 400 `ORG_069` と該当 slug が返り、他のチームも更新されない | 実機 |
| AC-G05 | 一括割当の team_slugs が501件だと 400 | 実機 |
| AC-G06 | XM・XD が割り当てると 403 | 実機 |

### H. グループ宛てお知らせ（表示と push）

前提: 組織Xにグループ G1〜G4（この順）、各グループにチーム1つ（T1〜T4）、未分類チーム T0、直属メンバー XO。

| ID | 条件 | 検証 |
|---|---|---|
| AC-H01 | XA が「G2 以前」で掲示板告知を送ると、T1・T2 のダッシュボードに出て、T3・T4・T0 には出ない。XO は組織ダッシュボードで見られる | 実機 |
| AC-H02 | 「G3 以降」なら T3・T4 だけ、「G2〜G3」なら T2・T3 だけに出る | 実機 |
| AC-H03 | G1 と G4 を個別チェックし「未分類を含める」を付けると、T1・T4・T0 に出る | 実機 |
| AC-H04 | 「G2 以前」で送信した後に T3 を G2 へ移すと、そのお知らせは T3 のダッシュボードに**出るようになる**（表示は閲覧時の所属で判定）。T3 のメンバーに push は届かない（push は送信時の所属で確定済み）| 実機（表示は画面で、push は通知一覧で観測。送信後10秒以上待つ） |
| AC-H05 | 範囲の開始が終了より後ろだと 400 `BROADCAST_008` | 実機 |
| AC-H06 | 解決結果が0チームで直属メンバーもいない組織で、空のグループだけを選ぶと 400 `BROADCAST_009`。画面では「次へ」が押せない | 実機 |
| AC-H07 | XA が「G2 以前」でアンケートを告知すると、`ORGANIZATION` スコープの fan-out ジョブは作られず、`ORGANIZATION_TEAMS` ジョブが1件だけ作られ、その冪等キーはフィード ID から導出した値である | 実機（観測: `notification_fanout_jobs` の `scope_type` と `notification_fanout_job_target_teams` を SELECT） |
| AC-H08 | AC-H07 の配信後、T1・T2 のメンバーと XO は push を1件ずつ受け取り、T3・T4・T0 のメンバーは0件である | 実機（観測: 各受信者の通知一覧。送信後10秒以上待つ） |
| AC-H09 | T1 と T3 の両方に所属するユーザーは、「G1 と G3」宛ての告知で push を1件だけ受け取る（重複しない）| 実機（観測: 当該ユーザーの通知一覧で1件だけ） |
| AC-H10 | **（IT 限定。実機E2E では再現しない）** fan-out ジョブの enqueue 後・ワーカー実行前に T2 が組織Xを離脱した状態を IT で作り、ワーカーを実行すると、T2 のメンバーには push が届かない | IT 限定（enqueue 後・ワーカー実行前にチームを離脱させる状態は、5秒間隔で動くワーカーと競合するため実機では作れない） |
| AC-H11 | 宛先を「すべてのチーム」にしたアンケート告知では、従来どおり `ORGANIZATION` の fan-out が1件作られる（後方互換）| 実機（観測: `notification_fanout_jobs` の `scope_type=ORGANIZATION` が1件） |
| AC-H12 | 組織Yのグループ ID を指定すると 400 `BROADCAST_006`、グループ機能 off の組織でグループを指定すると 400 `BROADCAST_007`、TEAM スコープでグループを指定すると 400 `BROADCAST_012`、`targetTeamIds` と併用すると 400 `BROADCAST_011` | 実機 |
| AC-H13 | XM（MEMBER）がグループ指定で MEMBERS_AND_ABOVE のアンケート告知を送ると、表示は宛先どおり絞られ、**push は誰にも届かず**、fan-out ジョブは `ORGANIZATION`・`ORGANIZATION_TEAMS` のどちらも作られない | 実機（観測: 通知一覧で0件・`notification_fanout_jobs` に新規行なし） |
| AC-H14 | 「チームを選ぶ」で5,001チームを指定すると 400 `BROADCAST_010`、5,000チームは成功する（境界）。グループ指定で解決結果が5,001チーム以上になっても成功する | 実機 |
| AC-H15 | 宛先プレビューで「G2 以前」を指定すると `resolvedTeamCount=2`、`directMemberCount=1`、T1・T2 の名前が返る。XM・XD2 が呼ぶと `pushEnabled=false` | 実機 |
| AC-H16 | 宛先外チーム T3 のメンバーでも、元の掲示板スレッドの URL を開けば閲覧できる（閲覧制限は対象外であることの確認）| 実機 |
| AC-H17 | 送信者の送信履歴に「宛先: G2 以前（送信時 2 チーム）」と表示され、後で G2 を改名・削除しても表示は変わらない | 実機 |
| AC-H18 | **（変更。御裁可 #19）** XA が「チームを選ぶ」で T1 だけを指定してアンケート告知を送ると、`ORGANIZATION` の fan-out は作られず、`ORGANIZATION_TEAMS` ジョブが1件だけ作られ、その宛先チーム子表は T1 だけである。配信後、T1 のメンバーと XO は push を1件ずつ受け取り、T2・T3・T4・T0 のメンバーは0件である。表示は T1 のダッシュボードにだけ出る（表示は従来どおり）| 実機（観測: ジョブ表と各受信者の通知一覧。送信後10秒以上待つ） |
| AC-H19 | XA が「G2 以前」で掲示板告知を送っても、fan-out ジョブは1件も作られず、個別の push も送られない（push の無いチャネルに push を新設しない）| 実機 |
| AC-H20 | **（変更。御裁可 #6）** G2 を削除した後も、「G2 以前」で送った過去のお知らせは T1 と T2 のダッシュボードに引き続き出る（T2 は送信時スナップショットで判定）。T3・T4 には出ない | 実機 |
| AC-H21 | 告知のコンテンツ作成が失敗してロールバックすると、fan-out ジョブも作られない（同一トランザクション）| IT 限定（コンテンツ作成の失敗を実機で意図的に起こせないため） |
| AC-H22 | `target_role=MEMBERS_AND_ABOVE` のグループ宛てアンケートでは純 SUPPORTER に push が届かず、`SUPPORTERS_AND_ABOVE` では届く | 実機（観測: 純 SUPPORTER の通知一覧） |
| AC-H23 | 「G2 以前」で送信した後、T2 を G3 へ移してから G2 を削除すると、そのお知らせは T2 に出る（送信時に G2 の対象だったため）。一方、送信後に T3 を G2 へ移してから G2 を削除すると、T3 には出ない（送信時スナップショットに無いため）。G2 を削除する前は、移動後の所属で動的に判定される（AC-H04 と同じ）| 実機 |
| AC-H24 | 「G1 と G2」で送信した後に G2 だけを削除すると、同じお知らせについて、T1 は動的判定（現在 G1 に所属）で、T2 はスナップショット判定で表示される（グループごとに切り替わる）| 実機 |
| AC-H25 | 送信時スナップショットに入っていたチームT2 が、G2 の削除後に組織Xを離脱すると、そのお知らせは T2 に出ない（スナップショット判定でも現在の ACTIVE 加盟を要求する）| 実機（準備: G2 削除→ T2 を離脱させる→観測: T2 ダッシュボード） |
| AC-H26 | グループ宛ての告知を送ると、push の有無（送信者が MEMBER・チャネルが掲示板など）にかかわらず、`announcement_feed_group_snapshots` に送信時の (グループ, チーム) の行が作られる。フィードを削除すると行も消える | 実機（観測: `announcement_feed_group_snapshots` を SELECT） |
| AC-H27 | **（変更。御裁可 #19）** XM（MEMBER）が「チームを選ぶ」で T1 を指定してアンケート告知を送ると、表示は T1 にだけ出て、push は誰にも届かず、fan-out ジョブは `ORGANIZATION`・`ORGANIZATION_TEAMS` のどちらも作られない | 実機（観測: 通知一覧で0件・ジョブ表に新規行なし） |
| AC-H28 | **（回帰防止）** XA が「すべてのチーム」でアンケート告知を送ると、変更前と同じく `ORGANIZATION` の fan-out が1件だけ作られ、`ORGANIZATION_TEAMS` は作られない（AC-H11 と対）。「チームを選ぶ」で掲示板告知を送っても fan-out ジョブは1件も作られない（AC-H19 と対）| 実機（観測: ジョブ表） |
| AC-H30 | **（M2）** 同じアンケートを「チームを選ぶ」と「チームグループで選ぶ」のそれぞれで送ったとき、`ORGANIZATION_TEAMS` の fan-out ジョブが作られ push が届くのは、送信者が XA・XD（MANAGE_CONTENT あり）・SYSTEM_ADMIN の場合だけである。XD2（MANAGE_CONTENT なし）と XM が送った場合は、表示は宛先どおり絞られるが fan-out ジョブは1件も作られず、push は誰にも届かない | 実機（観測: ジョブ表と各受信者の通知一覧。送信後10秒以上待つ） |
| AC-H29 | **（回帰防止）** 「チームを選ぶ」の表示判定は変更前と同じで、T1 を選んだ告知が T1 以外のチームのダッシュボードに出ず、送信後に T1 がグループを移っても T1 に出続ける | 実機 |

### J. 範囲テンプレート

| ID | 条件 | 検証 |
|---|---|---|
| AC-J01 | XA が「G2 以前」をテンプレートとして保存し、後で G5 を作って G1 の前へ並べ替えてから、API で `templateId` だけを指定して送ると、サーバーが送信時の並びで範囲を展開し、保存される `target_group_ids` は G5・G1・G2 になる | 実機 |
| AC-J02 | 個別選択に含まれるグループを削除してからテンプレートで送ると、そのグループが除外されて送信は成功し、レスポンスの `warnings` に `TEMPLATE_GROUPS_REMOVED` が入る。画面では読み込み時に警告が表示される | 実機 |
| AC-J03 | 範囲の端のグループを削除してから API で `templateId` だけを指定して送ると 400 `BROADCAST_013`。画面では読み込み時に範囲が空になり、再選択を求められる | 実機 |

### K. URL 直打ち・越境（IDOR）

| ID | 条件 | 検証 |
|---|---|---|
| AC-K01 | XM・XD（ログイン中）が `/organizations/X/settings/team-affiliation` を直打ちすると、`org-role-guard` によりエラートースト（`adminConsole.middleware.accessDeniedTitle`）が出て `/organizations/X` へ戻され、設定 API も 403 を返す。未ログインでは `auth` によりログイン画面へ送られる | 実機 |
| AC-K02 | XM が `/organizations/X/member-teams?view=applications`（`invites` / `restrictions` / `groups` も同じ）を直打ちすると、所属チーム一覧の表示に戻り、該当 API は 403 | 実機 |
| AC-K03 | TM・TD が `/teams/T/affiliations` を直打ちすると `/teams/T` へ戻され、加盟操作 API はすべて 403。TG は同じ URL で画面が表示される。TD・TG が `/teams/T/permission-groups` を直打ちすると `/teams/T` へ戻される | 実機 |
| AC-K04 | 連番の membershipId を1〜1000 まで総当たりしても、自組織（自チーム）の行以外はすべて 404 `TEAM_070` で、存在する行と存在しない行を応答から区別できない | 実機（スクリプトで ID を総当たりし、応答のステータス・本文を比較） |
| AC-K05 | YA が組織Y の告知で `targetTeamIds` に組織Xのチームを入れると 400（既存 `BROADCAST_002`）、`targetGroupIds` に組織Xのグループを入れると 400 `BROADCAST_006` | 実機 |
| AC-K06 | 他組織のチームグループ ID で一括割当・単体割当すると 404 `ORG_064` | 実機 |
| AC-K07 | 組織X の告知で `targetTeamIds` に、`user_roles` 上は X の組織ロールを持つメンバーがいるが `team_org_memberships` で X に ACTIVE 加盟していないチームを入れると 400 `BROADCAST_002`（候補は ACTIVE の加盟だけ。§17 #20）| 実機 |

### L. 導線（画面から到達できること）

| ID | 条件 | 検証 |
|---|---|---|
| AC-L01 | XA が組織トップから「所属チーム」タブ→「加盟の設定」で設定画面に到達でき、管理コンソールのカードからも到達できる | 実機 |
| AC-L02 | TA が自チームの「所属組織」タブ→「加盟先の組織を探す」→受付中の組織→「チームとして加盟を申請」→送信まで、URL を手で打たずに完了できる | 実機 |
| AC-L03 | TA が組織Xへ申請した後、XA が通知ベルの一覧に出た `TEAM_ORG_APPLICATION_RECEIVED` をクリックすると `/organizations/X/member-teams?view=applications` が開き、その申請を承認・拒否できる | 実機 |
| AC-L04 | XA が「所属チーム」タブからチームグループ管理と一括割当に到達できる | 実機 |
| AC-L05 | XA が組織ダッシュボードの「+ 組織内告知」からグループ宛て送信まで到達できる | 実機 |
| AC-L06 | XA がチームTを招待した後、TA（と TG）が通知ベルの一覧に出た `TEAM_ORG_INVITE_RECEIVED` をクリックすると `/teams/T/affiliations?view=invites` が開き、承諾できる。チームシェルの「所属組織」タブからも同じ画面に到達できる | 実機 |

### M. 移行・互換

| ID | 条件 | 検証 |
|---|---|---|
| AC-M01 | 移行後、既存の全 `team_org_memberships` 行が `direction='ORG_INVITE'`・`group_id IS NULL` である | 実機（移行後の DB を SELECT） |
| AC-M02 | 移行後、既存の参照系 API（`GET /organizations/{slug}/teams`・`GET /teams/{slug}/organizations`）の既存フィールドの値が移行前と一致する | 実機 |
| AC-M03 | `FlywayFromScratchMigrationTest` が通る | IT 限定（CI の `FlywayFromScratchMigrationTest`） |
| AC-M04 | 複数加盟の行が1件も無いデータでは、§9 の改修前後で §9.2 の各 API の応答（追加フィールドを除く）が一致する | IT 限定（改修前後の応答比較をフィクスチャで固定するため） |

### P. チーム側の加盟操作権限（§3.2・M1）

| ID | 条件 | 検証 |
|---|---|---|
| AC-P01 | 移行後、既存チームの ADMIN（TA）の `GET /api/v1/teams/T/me/permissions` に `MANAGE_ORG_AFFILIATION` が含まれ、TD・TM には含まれない（DEPUTY・MEMBER に天井行を作っていない）| 実機（`GET /api/v1/teams/T/me/permissions`） |
| AC-P02 | TD・TM が申請・取下げ・招待の承諾・拒否・離脱・招待制限の解除の API を呼ぶと、すべて 403 で状態は変わらない。チームシェルに「所属組織」タブが出ない | 実機 |
| AC-P03 | TA が `/teams/T/permission-groups` で target_role=DEPUTY_ADMIN の権限グループに `MANAGE_ORG_AFFILIATION` を含めて作り、メンバー一覧から TD に割り当てると、TD の `me/permissions` にこの権限が現れ、TD は申請（201）・取下げ（204）・承諾（200）・離脱（204）ができ、「所属組織」タブが出る | 実機 |
| AC-P04 | target_role=MEMBER の権限グループで TG に付与すると、TG は AC-P03 と同じ操作ができる。付与されていない TM は引き続き 403 | 実機 |
| AC-P05 | TA が TG から権限グループの割当を外すと、TG の同じ操作は 403 になり、「所属組織」タブが消え、`/teams/T/affiliations` を開くと `/teams/T` へ戻される | 実機 |
| AC-P06 | TG・TD（権限付与済み）が権限グループの作成・割当 API を呼ぶと 403（付与できるのはチーム ADMIN だけ）| 実機 |
| AC-P07 | チーム側の通知（承認・拒否・招待・期限切れ）は TA と TG に届き、TM・TD（付与なし）には届かない | 実機（観測: 各人の通知一覧） |
| AC-P08 | 組織側の承認・拒否・招待・除名は、チーム側で権限を付与されていても関係なく、組織 ADMIN 以外は 403（XD も 403）| 実機 |
| AC-P09 | `/admin/member-permissions`（MEMBER 既定3権限の画面）には `MANAGE_ORG_AFFILIATION` が出ない | 実機 |

### N. 複数組織への同時加盟（§9）

前提: チームT が組織X と組織Y の両方に ACTIVE で加盟（フィクスチャで作る）。

| ID | 条件 | 検証 |
|---|---|---|
| AC-N01 | `ScopeAncestorResolver` に T を渡すと、親組織として X と Y の両方が返る（後勝ちで1つに潰れない）| IT 限定（内部コンポーネントの戻り値の確認） |
| AC-N02 | T のメンバーは、X の ORGANIZATION_WIDE 投稿と Y の ORGANIZATION_WIDE 投稿の両方を閲覧でき、どちらにも加盟していない組織Z の ORGANIZATION_WIDE 投稿は閲覧できない | 実機 |
| AC-N03 | `GET /me/teams` の T の要素が X と Y の両方の組織を含む（§9.2 の改修後の形）| 実機 |
| AC-N04 | T の年間行事ビュー・行事カテゴリ一覧に、X のカテゴリと Y のカテゴリの両方が、どの組織由来かの印付きで出る | 実機 |
| AC-N05 | シフト予算の画面（§9.2 #12。組織を URL で明示する）で、X を指定すれば X の機能フラグで、Y を指定すれば Y の機能フラグで判定される。代表親組織を使う箇所（§9.2 #7・#8・#17・#18）では、採られる組織が §9.3 の規則どおり「最初に成立した加盟の組織」になる | 実機 |
| AC-N06 | 組織X と組織Y の配下メンバー数・配下チーム一覧の集計で、T のメンバーは各組織で1回ずつ数えられ、同じ組織の中で二重に数えられない | IT 限定（集計の重複の有無をフィクスチャで固定するため） |
| AC-N07 | `TeamOrgSingleParentAssumptionGuardTest` が、`team_org_memberships` に対する `findFirstBy*`・`LIMIT 1` の新規追加を検出して失敗する（検出器の偽陰性確認として、わざと1箇所足した状態で赤になることを確かめる）| IT 限定（番人テスト） |
| AC-N08 | T の予定の閲覧で、X では MEMBER・Y では ADMIN のユーザーは、min_view_role=ADMIN の予定を閲覧できる（いずれかの親でのロール。§9.2 #5）| 実機 |
| AC-N09 | X が停止中・Y が稼働中のとき、T のメンバーは Y 経由で見える ORGANIZATION_WIDE 投稿を閲覧でき、X 経由でしか見えない投稿は閲覧できない（停止中の親を経由する権利を数えない。§9.2 #6）| 実機 |
| AC-N10 | X だけが T に予算割当を持つとき、T のシフト公開で予算消化は X に計上され、取消は計上時の X から取り消される。Y が T に重なる期間の割当を作ろうとすると 409 になる（§9.2 #14・#15）| IT 限定（予算計上先の確認をフィクスチャで固定するため）＋実機（重なる割当の作成が 409） |
| AC-N11 | `TodoBudgetLinkService` で T を Y の予算に紐づける操作が、T が Y に ACTIVE 加盟していれば成功する（任意の1件と比べて誤拒否しない。§9.2 #13）| IT 限定（サービス内部の判定） |
| AC-N12 | T のメンバーが試合画面を開くと組織の選択肢に X と Y が出て、Y を選ぶと Y の試合だけが一覧に出る。再読込しても選択は変わらない。組織X の大会の対戦表から試合を記録すると、試合は X の下に作られる（§9.2 F1〜F3）| 実機 |
| AC-N13 | T の TEAM スコープ契約を作成すると、契約行に代表親組織が記録され、その後 T が別の組織へ加盟しても契約の組織は変わらない（§9.2 #17）| IT 限定（契約行の記録の確認） |
| AC-N14 | 同じデータで `GET /me/teams` を10回呼んでも、`organization_id`（代表親組織）が毎回同じ値になる（§9.3 の決定性）| 実機 |

---

## 17. 要マスター裁可（殿の既定判断）

以下は、マスターの確定事項の上に、殿と本書が置いた既定判断である。変える場合は本書の該当節と受け入れ条件を合わせて直す。

**マスター確定事項（2026-09-26 時点）**: 双方向加盟、申請受付の設定（既定 off）、1加盟1グループ、範囲指定、閲覧制限は対象外、1チームの複数組織への同時加盟と読み手の改修（§9）、宛先を絞った告知の push を送れるのは組織 ADMIN と MANAGE_CONTENT を持つ DEPUTY だけ（M2。MEMBER・MANAGE_CONTENT の無い DEPUTY は表示の絞り込みまで）、宛先を絞った告知に直属メンバーを含める、push を絞るのは現に push しているチャネル（アンケート）だけ、チーム側の加盟操作はチーム ADMIN が DEPUTY・指定メンバーへ付与できる（M1）、公開ページに加盟申請ボタンを出す（M3）。

**殿の判断（2026-09-25）**: グループ指定は条件を保存して表示を閲覧時に解決し、push は送信時の所属でスナップショットを取る。5,000件の上限は「チームを選ぶ」にだけ残す（以前の「スナップショット推奨」を上書き）。

**殿の判断（2026-09-26）**: 実機で時間を進める手段は「DB の日時列を過去へずらす準備＋バッチ手動起動（`/api/v1/system-admin/batch/{name}/trigger?sync=true`）」に統一し、テスト用の Clock エンドポイントは作らない。新設バッチはすべてこの trigger に登録する。push は受信者の通知一覧で観測する（ワーカー5秒間隔）。各 AC に実機か IT 限定かを明記する（§16）。

| # | 既定判断 | 本書の節 |
|---|---|---|
| 1 | 未実装の加盟書き込み API 群を本書で正式に定義し直す（実装は後続の戦役）。パスは slug に統一 | §6・§10 |
| 2 | 組織設定に「グループ機能を使う」と「申請時のグループ選択（OFF/任意/必須）」を置く。必須のときはグループ未選択では送信できない | §5.5・§6.1 |
| 3 | 承認時、組織 ADMIN は希望グループを確認して上書きでき、承認と同時にそのグループへ所属する。必須の組織でも「未分類で承認」は許す。承認 API は `group_id` キーの有無で「希望のまま」と「上書き・未分類」を区別する | §6.2・§10.5 |
| 4 | 既存の加盟チームは未分類で残し、組織 ADMIN が後から単体・一括で割り当てる | §5.3・§7.4 |
| 5 | グループを削除したら、所属チームは AFTER_COMMIT のイベントで非同期に未分類へ戻す（越境トランザクションを使わない）。読み手は `deleted_at IS NULL` の JOIN で防御する | §7.3 |
| 6 | **【御裁可済み 2026-09-26】** グループを削除しても、そのグループ宛ての過去のお知らせは、**送信時点で対象だったチームに表示し続ける**。送信時に (グループ, チーム) のスナップショットを保存し、グループが生きている間は動的、削除後はスナップショットで判定する | §5.6・§7.3・§8.2・AC-H20・H23〜H26 |
| 7 | UI 名称は「チームグループ」、i18n の名前空間は `teamGroup` / `teamAffiliation` | §2・§14 |
| 8 | グループは平坦、1組織200件まで | §5.2 |
| 9 | 拒否・取消・取下げ・期限切れ・離脱・除名の物理削除は**維持**。再申請の抑止は制限テーブル（拒否で30日、任意で無期限ブロック）、履歴は audit_logs | §4.2・§5.4 |
| 10 | 取下げ・取消でも24時間の冷却を置く（取下げと再申請の繰り返しによる通知の連打防止） | §5.4 |
| 11 | PENDING は60日で自動的に取り消す（同時申請枠を埋め続けないため）| §4.1・§6.8 |
| 12 | 組織・チームのアーカイブ時に PENDING を削除する（ACTIVE は既存規則どおり残す）| §4.5 |
| 13 | 受付 off に切り替えても PENDING 申請は残し、承認・拒否できる（自動拒否しない） | §4.3 |
| 14 | 冷却中かブロック中かは申請側に区別して見せない | §5.4 |
| 15 | 招待の拒否にも同じ冷却・ブロックを適用する（チーム側に「招待を受け付けない」設定は作らない）| §6.5 |
| 16 | 招待先が見えない（非公開）チームは、存在しない場合と同じ 404 を返す。非公開チームは招待できず、チーム側からの申請で加盟する | §6.5 |
| 17 | **【御裁可済み 2026-09-26（M1）】** 組織側の招待・承認・拒否・除名は**組織 ADMIN のみ**（DEPUTY_ADMIN への承認委任は §18 で最優先に検討）。チーム側の申請・取下げ・承諾・拒否・離脱は、新権限 `MANAGE_ORG_AFFILIATION` を持つ人が行う。チーム ADMIN は常に持ち、DEPUTY_ADMIN・MEMBER は権限グループで付与されたときだけ持つ | §3.1・§3.2・AC-P01〜P09 |
| 18 | 1チームの同時申請は10件まで。申請は10件/時/ユーザー | §6.1・§10.10 |
| 19 | **【御裁可済み 2026-09-26】** 「チームを選ぶ」の push も**選んだチームだけに絞る**。送信者の権限（ADMIN / DEPUTY のみ。MEMBER は push なし）と直属メンバーを含める規則は、グループ宛てと同じにする。既存挙動の変更点は §8.5.1 の表 | §8.5.1・AC-H18・H27〜H29 |
| 20 | **【御裁可済み 2026-09-26】** 告知の宛先候補は**加盟済み（ACTIVE の `team_org_memberships`）のチームだけ**（現行の `user_roles` 経由の候補を除く）。組織ロールだけを持つ人がいるが加盟していないチームは、宛先に選べない | F02.8 §11.1・AC-K07 |
| 21 | 単一の親組織が要る既存 API は、親組織の明示を求めるか、明示できないものに限り「最初に成立した加盟」を代表親組織とする | §9.1・§9.3 |
| 22 | グループ宛て push の文面はアンケート公開通知と同じにする（新しい文面を作らない）| §8.5.3 |
| 23 | 「チームを選ぶ」に5,000件の上限を新たに掛ける（従来は上限なし）。上限値は未検証で、試練の実測で下げる可能性がある | §8.3 |
| 24 | **【御裁可済み 2026-09-26】** シフト予算は「そのチームに予算割当を持つ組織」に計上し、同じチーム・重なる期間に複数の組織が割当を持つことを 409 で禁止する | §9.2 #11〜#16・AC-N10 |
| 25 | TEAM スコープの契約・特典は、作成時の代表親組織を契約行に記録し、以後は再解決しない | §9.2 #17 |
| 26 | 複数親の可視性は「停止中でない親組織のいずれかで権利があれば可」とする（停止中の親を経由する権利は数えない）| §9.2 #4〜#6 |
| 27 | **【御裁可済み 2026-09-26（M2）】** 宛先を絞った告知（チームを選ぶ・チームグループ）の push を送れるのは組織 ADMIN・MANAGE_CONTENT を持つ DEPUTY_ADMIN・SYSTEM_ADMIN だけ | §3.1・§8.5.1・AC-H30 |
| 28 | **【御裁可済み 2026-09-26（M3）】** 組織の公開ページに「加盟申請」ボタンを出す。表示条件は受付中かつ閲覧者がいずれかのチームで加盟操作権限を持つこと。公開 API は受付フラグだけを返し、ボタンの可否は認証付き API で判定する（存在オラクルを作らない）| §10.3・§13・AC-A11・A12 |
| 29 | 告知系 API（broadcast・宛先プレビュー・範囲テンプレート）だけは数値 `{orgId}` を使う（既存の告知系 API とウィザードに揃える）| §10 |
| 30 | 承認時のグループ上書きは `overrideGroup` フラグで明示させる（キー欠落と null の区別に頼らない）| §10.5 |

---

## 18. 未実装／段階拡張

| 項目 | 状態・方針 |
|---|---|
| **前提欠陥: 公開組織 API の識別子** | main 時点で FE `fetchPublicOrganization(orgSlug)` が slug を渡し、BE `PublicOrganizationController` は `@PathVariable Long id` を要求している。公開ページの申請ボタン（M3）はこの API に依存するため、段階①の最初に直す（§10.3）|
| 本書の API・DDL・画面の全体 | 🔵 未実装。§15 の3段階でリリースする。§9 の改修は段階①に同梱する |
| **DEPUTY_ADMIN への承認委任（優先度: 高）** | 権限（例: `MANAGE_TEAM_AFFILIATIONS`）を新設し、申請の承認・拒否・グループ割当を DEPUTY_ADMIN に委任できるようにする。大規模組織では ADMIN だけで申請をさばけない見込みのため、段階拡張の中で最初に着手する |
| 告知系 API の slug 化 | 告知系（broadcast・宛先プレビュー・範囲テンプレート・告知フィード）は数値 `{orgId}` のまま（§10）。slug 化は告知系全体でまとめて行う |
| 「チームを選ぶ」の push の絞り込み | 御裁可済み（§17 #19）。§8.5.1 のとおり実装する |
| 除名時のブロック | 除名と同時に再申請を止めるオプション |
| チーム側の「招待を受け付けない」設定 | 現状はブロックで個別に止める。招待スパムが問題になれば検討 |
| 「同じ宛先で再送」 | 送信履歴の `target_audience` から宛先を復元して再送する |
| push の無いチャネルへの push 新設 | 対象外（マスター確定）。新設する場合は、そのチャネルにも §8.5 の絞り込みを最初から適用する |
| グループ単位の閲覧制限 | 対象外（マスター確定）。必要になれば visibility の拡張として別設計 |
| チームのメンバーへの所属組織表示 | `/teams/{slug}/affiliations` の所属組織一覧をメンバーにも見せるか（現状 ADMIN の管理レンズのみ）|
| 多値インデックスの上限 | 未検証（§8.3）。試練で実測する |

---

## 19. 変更履歴

| 日付 | 変更内容 |
|---|---|
| 2026-09-25 | 初版（🟡 設計中）。双方向加盟（direction 列）、申請受付設定、再申請の抑止（制限テーブル）、チームグループ（平坦・200件・並び順）、グループ宛てお知らせを設計。F01.2・F02.8 を合わせて改訂 |
| 2026-09-25 | 第1パス精査の反映: ①複数組織への同時加盟を本体化し、単一親前提の読み手の一覧・意味論・番人・AC-N を追加（§9）②グループ宛て push は ADMIN/DEPUTY のみ・直属メンバーを含める・push を絞るのはアンケートだけ（§8.5）③表示は閲覧時に動的解決・push は送信時スナップショットへ変更し、5,000件上限は「チームを選ぶ」に限定（§8.2・§8.3）④enqueue を告知と同一トランザクションに（§8.5.3）⑤グループ削除の付け替えを AFTER_COMMIT イベントへ（§7.3）⑥組織・チームの削除／アーカイブ時の片付けと PENDING の60日期限（§4.5・§6.8）⑦範囲テンプレートのサーバー側解決（§8.6）⑧招待先の可視性チェック、取下げ・取消の24時間冷却、競合時の応答の判定表（§6.4・§6.5）⑨`AbstractTenantAwareRepository` を適用しない理由（§5.1）⑩索引の見直し（§5.3・§5.5）⑪BROADCAST エラーの分割（006〜013）⑫BE の6言語メッセージキー（§14.2）⑬LeagueTransfer・ディビジョンとの使い分け（§2）。以後の節番号は §9 の新設に伴い1つずつ繰り下げた |
| 2026-09-26 | 裁可論点の御裁可を反映: #6 グループ削除後も送信時点の対象チームに表示し続ける（`announcement_feed_group_snapshots` を追加し、生存グループは動的・削除済みグループはスナップショットで判定。§8.2 に切替条件）、#19「チームを選ぶ」の push も選んだチーム＋直属メンバーに絞り、送信者の権限規則をグループ宛てと揃えた（§8.5.1 に変更点の表）、#20・#24 を確定。AC-H18・H20 を改め、AC-H23〜H29 を追加 |
| 2026-09-26 | 第2パス精査・実機E2E耐性の指摘とマスター御裁可 M1〜M3 を反映: ①チーム側の加盟操作権限 `MANAGE_ORG_AFFILIATION`（ADMIN 常時・DEPUTY/MEMBER は権限グループで付与、チームシェルに「権限グループ」タブを新設）と権限表の列追加・AC-P 群 ②宛先を絞った告知の push は ADMIN と MANAGE_CONTENT を持つ DEPUTY のみ（AC-H30）③公開ページの加盟申請ボタンと申請可否 API（存在オラクルなし）④JSON を camelCase に統一し、承認のグループ上書きを `overrideGroup` に ⑤告知系 API は `{orgId}` に揃えた ⑥`MEMBERS_AND_ABOVE` への名称是正 ⑦時刻は `wallClock` から `:now` を渡し SQL の NOW() を判定に使わない ⑧新設バッチ3本を `@BatchEndpoint` で trigger に登録し、各 AC に実機／IT 限定の検証方法を追記 ⑨通知の `notification_type`・`action_url` を定義 ⑩チームシェルの `affiliations` タブ追加と組織設定ページの `org-role-guard` ⑪SurveyAnnouncementAdapter の即時公開を前提に `publishSurvey` の4引数版で印を渡す経路を定義 ⑫Flyway 最大 V224、表の型を確定表記に、変更履歴を時系列順に |
