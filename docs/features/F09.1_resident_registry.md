# F09.1: 住民台帳・物件情報

> **ステータス**: 🟢 設計完了
> **実装フェーズ**: Phase 9
> **最終更新**: 2026-03-15
> **モジュール種別**: 選択式モジュール #9
> **関連ドキュメント**: F09.3 駐車場区画管理（譲渡掲示の棲み分け）、F04.3 プッシュ通知（入退居・掲示・契約満了通知）

---

## 1. 概要

マンション管理組合・集合住宅・社宅等で、居室（部屋番号）と区分所有者・賃借人を紐付けて管理する機能。所有者/賃借人の区別、入退居日の記録、共有持分比率の管理を行う。台帳全体を ADMIN/DEPUTY_ADMIN のみ閲覧可に設定でき、個人は自室・自身の情報のみ閲覧可能（プライバシー保護）。居住者間の物件売買・賃貸希望を掲示する物件掲示板を併設し、コミュニティ内での住み替え・転貸の情報共有を支援する。駐車場区画の譲渡希望は駐車場区画管理モジュール（F09.3）で掲示するため、本機能の対象外とする。

### テンプレート推奨

| テンプレート | 住民台帳 | 備考 |
|-------------|---------|------|
| マンション | ○（自動有効化） | 主要ターゲット |
| その他全テンプレート | — | 手動で有効化可能 |

---

## 2. スコープ

### 対象ロール
| ロール | 操作可能な範囲 |
|--------|--------------|
| SYSTEM_ADMIN | 全組織・チームの台帳参照 |
| ADMIN | 居室 CRUD・居住者 CRUD・台帳プライバシー設定・物件掲示板管理・居住者確認（verify） |
| DEPUTY_ADMIN | `MANAGE_RESIDENTS` 権限を持つ場合: ADMIN と同等 |
| MEMBER | 自室・自身の情報のみ閲覧・物件掲示板の閲覧/投稿（自室の物件のみ） |
| SUPPORTER | 物件掲示板の閲覧のみ（台帳アクセス不可） |
| GUEST | 対象外 |

### 対象レベル
- [x] 組織 (Organization) — 管理組合全体の台帳
- [x] チーム (Team) — 棟別・建物別の台帳
- [x] 個人 (Personal) — 自室・自身の情報のみ閲覧

---

## 3. DB設計

### テーブル一覧
| テーブル名 | 役割 | 論理削除 |
|-----------|------|---------|
| `dwelling_units` | 居室マスター（部屋番号・間取り・面積等） | あり |
| `resident_registry` | 居住者情報（区分所有者/賃借人・入退居日・共有持分） | あり |
| `resident_documents` | 居住者の書類添付（契約書・本人確認書類等） | なし |
| `property_listings` | 物件掲示板（売買/賃貸希望の掲示） | あり |
| `property_listing_inquiries` | 物件掲示への問い合わせ（「興味あり」） | なし |

### テーブル定義

#### `dwelling_units`

居室マスター。1建物内の各部屋を管理する。

| カラム名 | 型 | NULL | デフォルト | 説明 |
|---------|---|------|-----------|------|
| `id` | BIGINT UNSIGNED | NO | AUTO_INCREMENT | PK |
| `scope_type` | VARCHAR(20) | NO | — | スコープ（`TEAM` / `ORGANIZATION`） |
| `team_id` | BIGINT UNSIGNED | YES | NULL | FK → teams。TEAM の場合に設定 |
| `organization_id` | BIGINT UNSIGNED | YES | NULL | FK → organizations。ORGANIZATION の場合に設定 |
| `unit_number` | VARCHAR(50) | NO | — | 部屋番号（例: 「101」「202A」「3F-5」） |
| `floor` | SMALLINT | YES | NULL | 階数 |
| `area_sqm` | DECIMAL(8,2) | YES | NULL | 専有面積（㎡） |
| `layout` | VARCHAR(20) | YES | NULL | 間取り（例: 「3LDK」「1R」「2DK」） |
| `unit_type` | VARCHAR(20) | NO | 'STANDARD' | 居室種別（`STANDARD`: 住戸 / `COMMERCIAL`: 店舗 / `OFFICE`: 事務所 / `COMMON`: 共用施設） |
| `notes` | TEXT | YES | NULL | 備考（管理者用メモ） |
| `resident_count` | SMALLINT | NO | 0 | 現入居者数（denormalize。`move_out_date IS NULL` の居住者数） |
| `created_at` | DATETIME | NO | CURRENT_TIMESTAMP | |
| `updated_at` | DATETIME | NO | CURRENT_TIMESTAMP ON UPDATE | |
| `deleted_at` | DATETIME | YES | NULL | 論理削除日時 |

**インデックス**
```sql
UNIQUE KEY uq_du_team_unit (team_id, unit_number)            -- TEAM スコープ内の部屋番号重複防止
UNIQUE KEY uq_du_org_unit (organization_id, unit_number)     -- ORGANIZATION スコープ内の部屋番号重複防止
INDEX idx_du_scope (scope_type, team_id, organization_id, unit_type)  -- スコープ別居室一覧
```

**制約・備考**
- `scope_type` のスコープ制約: TEAM → `team_id` NOT NULL、ORGANIZATION → `organization_id` NOT NULL（XOR 制約）
- `unit_number` はスコープ内でユニーク。UNIQUE KEY を `team_id` / `organization_id` 別に2本定義（単一の UNIQUE KEY に nullable カラムを含むと MySQL で重複防止が効かないため）
- `unit_type = 'COMMON'`: エントランス・集会室等の共用施設。居住者は登録不可
- `resident_count`: 居住者の追加/退去時にアトミック更新（+1/-1）
- 論理削除: 居住者が紐付いている居室は削除不可（先に全居住者の退去処理が必要）

---

#### `resident_registry`

居住者情報。区分所有者/賃借人の区別・入退居日・連絡先を管理する。

| カラム名 | 型 | NULL | デフォルト | 説明 |
|---------|---|------|-----------|------|
| `id` | BIGINT UNSIGNED | NO | AUTO_INCREMENT | PK |
| `dwelling_unit_id` | BIGINT UNSIGNED | NO | — | FK → dwelling_units。ON DELETE RESTRICT |
| `user_id` | BIGINT UNSIGNED | YES | NULL | FK → users。ON DELETE SET NULL。プラットフォーム登録済みユーザーとの紐付け。未登録居住者は NULL |
| `resident_type` | VARCHAR(20) | NO | — | 居住者種別（`OWNER`: 区分所有者 / `RENTER`: 賃借人） |
| `resident_name` | VARCHAR(100) | NO | — | 居住者氏名（`user_id` が設定されている場合でも管理台帳用に独立して保持） |
| `resident_phone` | VARCHAR(255) | YES | NULL | 連絡先電話番号（AES-256 暗号化 + Base64 エンコードして保存。平文は最大20桁だが暗号化後は44文字以上） |
| `resident_email` | VARCHAR(255) | YES | NULL | 連絡先メールアドレス |
| `move_in_date` | DATE | NO | — | 入居日 |
| `move_out_date` | DATE | YES | NULL | 退居日。NULL = 現入居者 |
| `is_primary` | BOOLEAN | NO | FALSE | 主たる居住者フラグ（通知・連絡の宛先）。1居室に複数居住者がいる場合、1人を主居住者に指定。Service 層で自動判定: 居室の最初の居住者は自動的に TRUE |
| `ownership_share` | DECIMAL(5,2) | YES | NULL | 共有持分比率（%）。`OWNER` の場合に使用（例: 50.00 = 50%）。単独所有の場合は NULL or 100.00 |
| `lease_end_date` | DATE | YES | NULL | 賃貸借契約満了日。`RENTER` の場合に使用 |
| `is_public` | BOOLEAN | NO | FALSE | 個人への公開フラグ。`FALSE` = ADMIN/DEPUTY_ADMIN のみ閲覧可。`TRUE` = 本人が自分の情報を閲覧可能 |
| `verified_at` | DATETIME | YES | NULL | ADMIN による確認日時 |
| `verified_by` | BIGINT UNSIGNED | YES | NULL | FK → users。確認した管理者 |
| `emergency_contact_name` | VARCHAR(100) | YES | NULL | 緊急連絡先氏名（家族・親族等） |
| `emergency_contact_phone` | VARCHAR(255) | YES | NULL | 緊急連絡先電話番号（AES-256 暗号化） |
| `last_lease_notified_days` | INT | YES | NULL | 最後に契約満了通知を送信した残日数ポイント（90 or 30）。同一ポイントの重複送信を防止 |
| `notes` | TEXT | YES | NULL | 備考（管理者用メモ。例: 「長期海外出張中」） |
| `created_at` | DATETIME | NO | CURRENT_TIMESTAMP | |
| `updated_at` | DATETIME | NO | CURRENT_TIMESTAMP ON UPDATE | |
| `deleted_at` | DATETIME | YES | NULL | 論理削除日時 |

**インデックス**
```sql
INDEX idx_rr_unit (dwelling_unit_id, move_out_date)         -- 居室別の現入居者（move_out_date IS NULL）
INDEX idx_rr_user (user_id)                                  -- ユーザー別の居住情報
INDEX idx_rr_type (dwelling_unit_id, resident_type)          -- 居室別の所有者/賃借人一覧
-- 同一居室・同一ユーザーの現入居重複防止は Service 層で実装（上記制約・備考参照）
```

**制約・備考**
- **重複登録防止**: 同一 `dwelling_unit_id` + `user_id`（NOT NULL）+ `move_out_date IS NULL` の組み合わせは Service 層で重複チェック（MySQL 8.0 では条件付き UNIQUE が制限されるため、DB 制約ではなくアプリ層で実装。INSERT 前に `SELECT COUNT(*) WHERE dwelling_unit_id = ? AND user_id = ? AND move_out_date IS NULL AND deleted_at IS NULL` で検証）
- `emergency_contact_phone`: `resident_phone` と同じく AES-256 で暗号化。F03.6 安否確認で ADMIN が緊急連絡先を参照可能
- `dwelling_unit_id` ON DELETE RESTRICT: 居住者がいる居室は削除不可
- `user_id`: プラットフォームに登録済みのユーザーと紐付け。高齢者等の未登録居住者は NULL のまま `resident_name` のみで管理
- `resident_phone`: 個人情報のため AES-256 で暗号化して保存（`users.postal_code` 等と同じ方式）
- `move_out_date` >= `move_in_date`（Service 層バリデーション）
- `lease_end_date` >= `move_in_date`（Service 層バリデーション）
- `ownership_share`: `OWNER` の場合に 0.01〜100.00 の範囲。同一居室の全 OWNER の合計が 100.00 を超えないことを Service 層で検証
- `is_public` のデフォルト `FALSE`: プライバシー保護を優先。ADMIN が明示的に `TRUE` に設定しない限り、居住者本人にも情報は非公開
- `is_primary`: 1居室あたり `is_primary = TRUE` は最大1名（Service 層バリデーション）
- 論理削除: `move_out_date IS NOT NULL`（退去済み）の居住者のみ論理削除可能。現入居者を論理削除すると `resident_count` との不整合が発生するため、先に退去処理（PATCH /move-out）を行う必要がある。退去済みでも台帳履歴として保持可能（論理削除しなければ past_residents に表示される）

---

#### `property_listings`

物件掲示板。居住者間の売買希望・賃貸希望を掲示する。

| カラム名 | 型 | NULL | デフォルト | 説明 |
|---------|---|------|-----------|------|
| `id` | BIGINT UNSIGNED | NO | AUTO_INCREMENT | PK |
| `dwelling_unit_id` | BIGINT UNSIGNED | NO | — | FK → dwelling_units。ON DELETE CASCADE |
| `posted_by` | BIGINT UNSIGNED | NO | — | FK → users。ON DELETE RESTRICT |
| `listing_type` | VARCHAR(20) | NO | — | 掲示種別（`SALE`: 売買希望 / `RENT`: 賃貸希望） |
| `title` | VARCHAR(200) | NO | — | 掲示タイトル（例: 「101号室 売却希望」） |
| `description` | TEXT | YES | NULL | 詳細説明（間取り・リフォーム状況・希望条件等） |
| `asking_price` | DECIMAL(15,2) | YES | NULL | 売却希望価格（円）。`SALE` の場合に使用 |
| `monthly_rent` | DECIMAL(10,2) | YES | NULL | 希望月額賃料（円）。`RENT` の場合に使用 |
| `contact_method` | VARCHAR(20) | NO | 'IN_APP' | 連絡方法（`IN_APP`: アプリ内メッセージ / `EMAIL`: メール / `PHONE`: 電話） |
| `status` | VARCHAR(20) | NO | 'ACTIVE' | ステータス（`ACTIVE`: 掲示中 / `CLOSED`: 成約済み / `WITHDRAWN`: 取り下げ） |
| `expires_at` | DATE | YES | NULL | 掲示期限。到達時に自動 `WITHDRAWN` |
| `photo_keys` | JSON | YES | NULL | 写真の S3 キー配列（最大5枚。例: `["listings/1/photo1.webp", ...]`）。Pre-signed URL で配信 |
| `view_count` | INT UNSIGNED | NO | 0 | 閲覧数（denormalize） |
| `inquiry_count` | INT UNSIGNED | NO | 0 | 問い合わせ数（denormalize。「興味あり」ボタンのクリック数） |
| `created_at` | DATETIME | NO | CURRENT_TIMESTAMP | |
| `updated_at` | DATETIME | NO | CURRENT_TIMESTAMP ON UPDATE | |
| `deleted_at` | DATETIME | YES | NULL | 論理削除日時 |

**インデックス**
```sql
INDEX idx_pl_scope (dwelling_unit_id, status, created_at DESC)  -- 居室別掲示一覧
INDEX idx_pl_type (listing_type, status, created_at DESC)        -- 種別別の掲示一覧
INDEX idx_pl_posted_by (posted_by, created_at DESC)              -- 投稿者別一覧
INDEX idx_pl_expires (status, expires_at)                         -- 期限切れバッチ用
```

**制約・備考**
- `posted_by` ON DELETE RESTRICT: 投稿者退会時は先に掲示の取り下げ or 引継ぎが必要
- `SALE` の場合: `asking_price` が必須（Service 層バリデーション）。`monthly_rent` は NULL
- `RENT` の場合: `monthly_rent` が必須。`asking_price` は NULL
- 投稿権限: 該当居室の現入居者（`move_out_date IS NULL`）のみ。ADMIN は任意の居室に投稿可能
- 公開範囲: スコープ内のメンバーのみ閲覧可能（外部公開なし）
- 駐車場区画の譲渡希望は `parking_listings`（F09.3）で管理。本テーブルには含まない
- ステータス遷移: `ACTIVE` → `CLOSED`（成約）/ `WITHDRAWN`（取り下げ）。`CLOSED` / `WITHDRAWN` からの復帰は不可（再投稿で対応）
- `view_count`: `GET /property-listings/{id}` の詳細取得時にアトミック更新（+1）。一覧取得ではカウントしない。同一ユーザーの複数回閲覧もカウント（厳密なユニーク管理はコスト対効果が合わないためシンプルに総閲覧数）
- 論理削除: `ACTIVE` の掲示は論理削除不可（先に `WITHDRAWN` に遷移してから削除）。`CLOSED` / `WITHDRAWN` は論理削除可能
- `inquiry_count`: 「興味あり」ボタン押下時にアトミック更新（+1）。1ユーザーにつき1掲示1回まで（`property_listing_inquiries` テーブルで重複防止）

---

#### `resident_documents`

居住者に紐付く書類。賃貸借契約書・本人確認書類等のスキャンを保管する。

| カラム名 | 型 | NULL | デフォルト | 説明 |
|---------|---|------|-----------|------|
| `id` | BIGINT UNSIGNED | NO | AUTO_INCREMENT | PK |
| `resident_id` | BIGINT UNSIGNED | NO | — | FK → resident_registry。ON DELETE CASCADE |
| `file_key` | VARCHAR(500) | NO | — | S3 オブジェクトキー |
| `original_filename` | VARCHAR(255) | NO | — | アップロード時の元ファイル名 |
| `file_size` | INT | NO | — | ファイルサイズ（バイト） |
| `mime_type` | VARCHAR(100) | NO | — | MIMEタイプ |
| `document_type` | VARCHAR(20) | NO | 'OTHER' | 書類種別（`CONTRACT`: 契約書 / `ID_PROOF`: 本人確認書類 / `OTHER`: その他） |
| `uploaded_by` | BIGINT UNSIGNED | NO | — | FK → users。アップロードした ADMIN |
| `created_at` | DATETIME | NO | CURRENT_TIMESTAMP | |

**インデックス**
```sql
INDEX idx_rd_resident (resident_id, document_type)  -- 居住者別・種別ごとの書類一覧
```

**制約・備考**
- ADMIN / DEPUTY_ADMIN（MANAGE_RESIDENTS）のみアップロード・閲覧可能。居住者本人には非公開（契約書・身分証は管理者保管の書類のため）
- 対応ファイル形式: PDF, JPEG, PNG, WebP
- ファイルサイズ上限: 1ファイル10MB、1居住者あたり最大3ファイル
- S3 配信: CloudFront 経由の Pre-signed URL（有効期限1時間）
- 居住者の論理削除時もカスケード削除せず保持（証跡保全）。ただし居住者レコードの物理削除時は CASCADE

---

#### `property_listing_inquiries`

物件掲示への問い合わせ（「興味あり」表明）。1ユーザーにつき1掲示1回まで。

| カラム名 | 型 | NULL | デフォルト | 説明 |
|---------|---|------|-----------|------|
| `id` | BIGINT UNSIGNED | NO | AUTO_INCREMENT | PK |
| `listing_id` | BIGINT UNSIGNED | NO | — | FK → property_listings。ON DELETE CASCADE |
| `user_id` | BIGINT UNSIGNED | NO | — | FK → users。ON DELETE CASCADE |
| `message` | VARCHAR(500) | YES | NULL | 問い合わせメッセージ（任意。「内覧希望です」等） |
| `created_at` | DATETIME | NO | CURRENT_TIMESTAMP | |

**インデックス**
```sql
UNIQUE KEY uq_pli_listing_user (listing_id, user_id)  -- 同一ユーザーの重複問い合わせ防止
INDEX idx_pli_listing (listing_id, created_at DESC)     -- 掲示別の問い合わせ一覧
```

**制約・備考**
- MEMBER 以上がスコープ内の `ACTIVE` な掲示に問い合わせ可能
- 投稿者本人への自己問い合わせは不可（Service 層バリデーション）
- 問い合わせ時に `property_listings.inquiry_count` をアトミック更新（+1）
- 投稿者にプッシュ通知（「{user_name} さんが {title} に興味を示しています」）

### ER図（テキスト形式）
```
teams (1) ──── (N) dwelling_units（team_id; TEAM）
organizations (1) ──── (N) dwelling_units（organization_id; ORGANIZATION）
dwelling_units (1) ──── (N) resident_registry
dwelling_units (1) ──── (N) property_listings

resident_registry (1) ──── (N) resident_documents
property_listings (1) ──── (N) property_listing_inquiries

users (1) ──── (N) resident_registry（user_id; nullable）
users (1) ──── (N) property_listings（posted_by）
users (1) ──── (N) property_listing_inquiries（user_id）
```

---

## 4. API設計

### エンドポイント一覧
| メソッド | パス | 認証 | 説明 |
|---------|-----|------|------|
| GET | `/api/v1/dwelling-units` | 必要 | 居室一覧取得 |
| POST | `/api/v1/dwelling-units` | 必要 | 居室作成 |
| GET | `/api/v1/dwelling-units/{id}` | 必要 | 居室詳細取得（居住者一覧含む） |
| PUT | `/api/v1/dwelling-units/{id}` | 必要 | 居室更新 |
| DELETE | `/api/v1/dwelling-units/{id}` | 必要 | 居室削除（論理削除。居住者0人のみ） |
| POST | `/api/v1/dwelling-units/import` | 必要 | 居室 CSV 一括インポート（最大500件） |
| GET | `/api/v1/dwelling-units/{id}/residents` | 必要 | 居室の居住者一覧 |
| POST | `/api/v1/dwelling-units/{id}/residents` | 必要 | 居住者追加 |
| PUT | `/api/v1/dwelling-units/{unitId}/residents/{id}` | 必要 | 居住者情報更新 |
| DELETE | `/api/v1/dwelling-units/{unitId}/residents/{id}` | 必要 | 居住者削除（論理削除。退去済みのみ） |
| PATCH | `/api/v1/dwelling-units/{unitId}/residents/{id}/move-out` | 必要 | 退去処理（move_out_date 設定） |
| PATCH | `/api/v1/dwelling-units/{id}/move-out-all` | 必要 | 居室の全居住者を一括退去 |
| PATCH | `/api/v1/dwelling-units/{unitId}/residents/{id}/verify` | 必要 | 居住者確認（ADMIN 承認） |
| PATCH | `/api/v1/dwelling-units/{unitId}/residents/{id}/renew-lease` | 必要 | 賃貸借契約更新（lease_end_date 延長） |
| POST | `/api/v1/dwelling-units/{id}/invite` | 必要 | 居住者セルフ登録用の招待リンク生成 |
| POST | `/api/v1/dwelling-units/self-register` | 必要 | 居住者のセルフ登録（招待トークンで認証） |
| GET | `/api/v1/dwelling-units/my` | 必要 | 自分の居室・居住者情報 |
| GET | `/api/v1/dwelling-units/export` | 必要 | 台帳 CSV エクスポート（ADMIN のみ） |
| GET | `/api/v1/dwelling-units/residents/search` | 必要 | 居住者グローバル検索（スコープ内全居室横断） |
| PATCH | `/api/v1/dwelling-units/bulk-privacy` | 必要 | 全居住者の is_public 一括変更 |
| POST | `/api/v1/dwelling-units/{unitId}/residents/{id}/documents` | 必要 | 居住者書類アップロード |
| DELETE | `/api/v1/dwelling-units/residents/documents/{documentId}` | 必要 | 居住者書類削除 |
| GET | `/api/v1/property-listings` | 必要 | 物件掲示一覧 |
| POST | `/api/v1/property-listings` | 必要 | 物件掲示作成 |
| GET | `/api/v1/property-listings/{id}` | 必要 | 物件掲示詳細 |
| PUT | `/api/v1/property-listings/{id}` | 必要 | 物件掲示更新 |
| DELETE | `/api/v1/property-listings/{id}` | 必要 | 物件掲示削除（論理削除） |
| PATCH | `/api/v1/property-listings/{id}/close` | 必要 | 掲示の成約処理（ACTIVE → CLOSED） |
| PATCH | `/api/v1/property-listings/{id}/withdraw` | 必要 | 掲示の取り下げ（ACTIVE → WITHDRAWN） |
| POST | `/api/v1/property-listings/{id}/inquire` | 必要 | 物件掲示への問い合わせ（「興味あり」） |
| GET | `/api/v1/property-listings/{id}/inquiries` | 必要 | 問い合わせ一覧（投稿者 / ADMIN のみ） |
| GET | `/api/v1/dwelling-units/stats` | 必要 | 統計情報（総居室数・入居率・所有者/賃借人比率等） |

### リクエスト／レスポンス仕様

#### `POST /api/v1/dwelling-units`

**認可**: ADMIN / DEPUTY_ADMIN（MANAGE_RESIDENTS）

居室を作成する。

**リクエストボディ**
```json
{
  "scope_type": "ORGANIZATION",
  "organization_id": 1,
  "unit_number": "101",
  "floor": 1,
  "area_sqm": 72.50,
  "layout": "3LDK",
  "unit_type": "STANDARD",
  "notes": "角部屋・南向き"
}
```

- `unit_number`: 必須。スコープ内でユニーク
- `floor` / `area_sqm` / `layout`: 任意
- `unit_type`: 任意（デフォルト: `STANDARD`）

**レスポンス（201 Created）**: 居室詳細を返却

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 400 | バリデーションエラー / scope_type とスコープ ID の不整合 |
| 403 | 権限不足 |
| 409 | 同一スコープ内に同じ `unit_number` が存在 |

---

#### `GET /api/v1/dwelling-units`

**認可**: ADMIN / DEPUTY_ADMIN（MANAGE_RESIDENTS）

スコープ別の居室一覧をオフセットベースのページネーションで取得する。

**クエリパラメータ**
| パラメータ | 型 | 必須 | 説明 |
|-----------|---|------|------|
| `scope_type` | String | Yes | `TEAM` / `ORGANIZATION` |
| `scope_id` | Long | Yes | 対象スコープの ID |
| `floor` | Integer | No | 階数フィルタ |
| `unit_type` | String | No | 居室種別フィルタ |
| `has_residents` | Boolean | No | `true` = 入居中の居室のみ / `false` = 空室のみ |
| `keyword` | String | No | 部屋番号の部分一致検索 |
| `page` | Integer | No | ページ番号（デフォルト: 0） |
| `size` | Integer | No | 取得件数（デフォルト: 50、最大: 100） |
| `sort` | String | No | ソート（`unit_number` / `floor`。デフォルト: `unit_number`） |

**レスポンス（200 OK）**
```json
{
  "data": [
    {
      "id": 1,
      "unit_number": "101",
      "floor": 1,
      "area_sqm": 72.50,
      "layout": "3LDK",
      "unit_type": "STANDARD",
      "resident_count": 2,
      "primary_resident": {
        "id": 10,
        "resident_name": "山田太郎",
        "resident_type": "OWNER"
      }
    }
  ],
  "meta": {
    "page": 0,
    "size": 50,
    "total_elements": 120,
    "total_pages": 3
  }
}
```

- `primary_resident`: 主居住者のサマリー。居住者がいない場合は `null`

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 403 | ADMIN / DEPUTY_ADMIN（MANAGE_RESIDENTS）ではない |

---

#### `GET /api/v1/dwelling-units/{id}`

**認可**: ADMIN / DEPUTY_ADMIN（MANAGE_RESIDENTS）。本人の居室は MEMBER も閲覧可能（`resident_registry.user_id = currentUser` の居室であれば `is_public` に関わらずアクセス可能。ただしレスポンス内容はロールで制御）

居室の詳細を取得する。現入居者・過去の入居者（退去済み）を含む。

**レスポンス（200 OK）**
```json
{
  "data": {
    "id": 1,
    "scope_type": "ORGANIZATION",
    "unit_number": "101",
    "floor": 1,
    "area_sqm": 72.50,
    "layout": "3LDK",
    "unit_type": "STANDARD",
    "notes": "角部屋・南向き",
    "resident_count": 2,
    "residents": [
      {
        "id": 10,
        "user_id": 100,
        "resident_name": "山田太郎",
        "resident_type": "OWNER",
        "move_in_date": "2020-04-01",
        "move_out_date": null,
        "is_primary": true,
        "ownership_share": 50.00,
        "is_public": true,
        "verified_at": "2020-04-15T10:00:00+09:00"
      },
      {
        "id": 11,
        "user_id": 101,
        "resident_name": "山田花子",
        "resident_type": "OWNER",
        "move_in_date": "2020-04-01",
        "move_out_date": null,
        "is_primary": false,
        "ownership_share": 50.00,
        "is_public": true,
        "verified_at": "2020-04-15T10:00:00+09:00"
      }
    ],
    "past_residents": [
      {
        "id": 5,
        "resident_name": "前田次郎",
        "resident_type": "RENTER",
        "move_in_date": "2018-01-01",
        "move_out_date": "2020-03-31"
      }
    ],
    "active_listing": null,
    "parking_assignments": [
      {
        "space_number": "P-5",
        "resident_name": "山田太郎",
        "vehicle_nickname": "マイカー"
      }
    ],
    "created_at": "2015-01-01T00:00:00+09:00"
  }
}
```

- `residents`: 現入居者（`move_out_date IS NULL`）
- `past_residents`: 退去済み居住者。ADMIN のみ表示。直近5件
- `active_listing`: 掲示中の物件情報。なければ `null`
- `parking_assignments`: 居室の居住者に割り当てられている駐車場区画を F09.3 `parking_assignments` + `parking_spaces` から取得して表示。ADMIN のみ。F09.3 モジュールが無効の場合はフィールド自体を省略
- MEMBER が自室を閲覧する場合: `resident_phone` / `resident_email` / `notes` / `past_residents` / `parking_assignments` は非表示。自分の `resident_name` / `resident_type` / `move_in_date` / `ownership_share` / `verified_at` のみ返却。他居住者は `resident_name` と `resident_type` のみ（連絡先・備考は非公開）

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 403 | 権限不足（ADMIN でなく、自室でもない） |
| 404 | 居室が存在しない |

---

#### `GET /api/v1/dwelling-units/{id}/residents`

**認可**: ADMIN / DEPUTY_ADMIN（MANAGE_RESIDENTS）

居室の居住者一覧を取得する。

**クエリパラメータ**
| パラメータ | 型 | 必須 | 説明 |
|-----------|---|------|------|
| `include_past` | Boolean | No | `true` = 退去済み居住者を含む（デフォルト: `false`） |
| `resident_type` | String | No | `OWNER` / `RENTER`（フィルタ） |

**レスポンス（200 OK）**: 居住者配列。ADMIN は全フィールド、MEMBER（自室の場合）は制限付きレスポンス

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 403 | 権限不足（ADMIN でなく、自室でもない） |
| 404 | 居室が存在しない |

---

#### `POST /api/v1/dwelling-units/{id}/residents`

**認可**: ADMIN / DEPUTY_ADMIN（MANAGE_RESIDENTS）

居室に居住者を追加する。

**リクエストボディ**
```json
{
  "user_id": 100,
  "resident_type": "OWNER",
  "resident_name": "山田太郎",
  "resident_phone": "090-1234-5678",
  "resident_email": "yamada@example.com",
  "move_in_date": "2020-04-01",
  "is_primary": true,
  "ownership_share": 50.00,
  "is_public": true,
  "notes": null
}
```

- `user_id`: 任意。プラットフォーム登録済みユーザーと紐付ける場合に指定
- `resident_phone`: 任意。暗号化して保存
- `ownership_share`: `OWNER` の場合のみ有効。`RENTER` では無視
- `is_primary`: 任意。省略時は自動判定（居室に現入居者がいなければ `TRUE`、いれば `FALSE`）。明示的に `TRUE` を指定した場合、既存の主居住者を `FALSE` に自動変更

**レスポンス（201 Created）**: 居住者詳細を返却

**副作用**:
1. `dwelling_units.resident_count` をアトミック更新（+1）
2. `user_id` が指定された場合: ユーザーにプッシュ通知（「○○号室の居住者として登録されました」）

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 400 | バリデーションエラー / `unit_type = 'COMMON'` の居室 / `OWNER` の合計 `ownership_share` が 100% 超過 |
| 403 | 権限不足 |
| 404 | 居室が存在しない |

---

#### `PATCH /api/v1/dwelling-units/{unitId}/residents/{id}/move-out`

**認可**: ADMIN / DEPUTY_ADMIN（MANAGE_RESIDENTS）

居住者の退去を記録する。

**リクエストボディ**
```json
{
  "move_out_date": "2026-03-31"
}
```

**レスポンス（200 OK）**: 居住者詳細を返却（`move_out_date` 設定済み）

**副作用**:
1. `dwelling_units.resident_count` をアトミック更新（-1）
2. 退去者が `is_primary = TRUE` の場合: 同居室の他の居住者に `is_primary` を自動移譲（最も入居日が古い居住者に設定。他に居住者がいない場合はスキップ）
3. 退去者の掲示中物件があれば `WITHDRAWN` に自動遷移
4. `user_id` が設定されている場合: ユーザーにプッシュ通知（「○○号室の退去が記録されました」）

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 400 | `move_out_date` < `move_in_date` / 既に退去済み |
| 403 | 権限不足 |
| 404 | 居住者が存在しない |

---

#### `PATCH /api/v1/dwelling-units/{unitId}/residents/{id}/verify`

**認可**: ADMIN / DEPUTY_ADMIN（MANAGE_RESIDENTS）

居住者情報を ADMIN が確認済みにする（本人確認書類等の確認後）。

**リクエストボディ**: なし

**レスポンス（200 OK）**: 居住者詳細を返却（`verified_at` / `verified_by` 設定済み）

**副作用**: `verified_at = NOW()`、`verified_by = currentUser` を設定

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 403 | 権限不足 |
| 404 | 居住者が存在しない |
| 409 | 既に確認済み |

---

#### `GET /api/v1/dwelling-units/my`

**認可**: ログイン済みユーザー

自分が紐付けられている居室・居住者情報を取得する。

**レスポンス（200 OK）**
```json
{
  "data": [
    {
      "dwelling_unit": {
        "id": 1,
        "unit_number": "101",
        "floor": 1,
        "layout": "3LDK"
      },
      "my_resident_info": {
        "id": 10,
        "resident_type": "OWNER",
        "move_in_date": "2020-04-01",
        "is_primary": true,
        "ownership_share": 50.00,
        "verified_at": "2020-04-15T10:00:00+09:00"
      }
    }
  ]
}
```

- `is_public = FALSE` の居住者情報でも、本人の情報は常に返却（自分自身の確認用）
- 他居住者の情報は含まない

---

#### `GET /api/v1/property-listings`

**認可**: スコープメンバー（SUPPORTER 以上）

物件掲示の一覧をカーソルベースで取得する。

**クエリパラメータ**
| パラメータ | 型 | 必須 | 説明 |
|-----------|---|------|------|
| `scope_type` | String | Yes | `TEAM` / `ORGANIZATION` |
| `scope_id` | Long | Yes | 対象スコープの ID |
| `listing_type` | String | No | `SALE` / `RENT`（フィルタ） |
| `status` | String | No | `ACTIVE` / `CLOSED` / `WITHDRAWN`（デフォルト: `ACTIVE`） |
| `cursor` | Long | No | カーソル |
| `limit` | Integer | No | 取得件数（デフォルト: 20） |

**レスポンス（200 OK）**
```json
{
  "data": [
    {
      "id": 1,
      "dwelling_unit": {
        "id": 1,
        "unit_number": "101",
        "floor": 1,
        "layout": "3LDK",
        "area_sqm": 72.50
      },
      "listing_type": "SALE",
      "title": "101号室 売却希望",
      "asking_price": 35000000,
      "monthly_rent": null,
      "status": "ACTIVE",
      "photo_urls": ["https://cdn.example.com/listings/1/photo1.webp?signed=..."],
      "posted_by": { "id": 100, "display_name": "山田太郎" },
      "view_count": 15,
      "created_at": "2026-03-01T10:00:00+09:00"
    }
  ],
  "meta": {
    "next_cursor": null,
    "limit": 20,
    "has_next": false
  }
}
```

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 403 | スコープメンバーでない |

---

#### `POST /api/v1/property-listings`

**認可**: MEMBER 以上（該当居室の現入居者のみ）。ADMIN は任意居室に投稿可能

物件掲示を作成する。

**リクエスト（multipart/form-data）**
| フィールド | 型 | 必須 | 説明 |
|-----------|---|------|------|
| `dwelling_unit_id` | Long | Yes | 対象居室 ID |
| `listing_type` | String | Yes | `SALE` / `RENT` |
| `title` | String | Yes | 掲示タイトル（1〜200文字） |
| `description` | String | No | 詳細説明 |
| `asking_price` | Long | Conditional | 売却希望価格（`SALE` の場合必須） |
| `monthly_rent` | Long | Conditional | 希望月額賃料（`RENT` の場合必須） |
| `contact_method` | String | No | `IN_APP` / `EMAIL` / `PHONE`（デフォルト: `IN_APP`） |
| `expires_at` | String | No | 掲示期限（ISO 8601 date） |
| `photos` | MultipartFile[] | No | 写真（最大5枚、各10MB以下。JPEG/PNG/WebP） |

**レスポンス（201 Created）**: 物件掲示詳細（`photo_urls` に Pre-signed URL 配列を含む）

**副作用**: スコープ内メンバーにプッシュ通知（「新しい物件掲示が投稿されました: {title}」）

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 400 | バリデーションエラー / `SALE` で `asking_price` 未設定 / `RENT` で `monthly_rent` 未設定 |
| 403 | 該当居室の現入居者でない / 権限不足 |
| 409 | 同一居室で `ACTIVE` の掲示が既に存在（1居室1掲示の制約） |

---

#### `PUT /api/v1/property-listings/{id}`

**認可**: 投稿者本人 / ADMIN。`ACTIVE` のみ更新可能

物件掲示を更新する。

**リクエスト（multipart/form-data）**: `POST /property-listings` と同一フィールド（`dwelling_unit_id` は変更不可）

**レスポンス（200 OK）**: 物件掲示詳細

**備考**:
- `photos` を送信した場合: 既存の写真を全て置換（差分更新ではなく全量置換）
- `photos` を送信しない場合: 既存の写真を維持

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 400 | バリデーションエラー |
| 403 | 投稿者本人でも ADMIN でもない |
| 409 | `ACTIVE` 以外のステータス |

---

#### `PATCH /api/v1/property-listings/{id}/close`

**認可**: 投稿者本人 / ADMIN

掲示を成約処理する（ACTIVE → CLOSED）。

**リクエストボディ**: なし

**レスポンス（200 OK）**: 物件掲示詳細（`status: "CLOSED"`）

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 403 | 投稿者本人でも ADMIN でもない |
| 409 | ACTIVE 以外のステータス |

---

#### `PATCH /api/v1/property-listings/{id}/withdraw`

**認可**: 投稿者本人 / ADMIN

掲示を取り下げる（ACTIVE → WITHDRAWN）。

**リクエストボディ**: なし

**レスポンス（200 OK）**: 物件掲示詳細（`status: "WITHDRAWN"`）

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 403 | 投稿者本人でも ADMIN でもない |
| 409 | ACTIVE 以外のステータス |

---

#### `GET /api/v1/dwelling-units/stats`

**認可**: ADMIN / DEPUTY_ADMIN（MANAGE_RESIDENTS）

台帳の統計情報を取得する。

**クエリパラメータ**
| パラメータ | 型 | 必須 | 説明 |
|-----------|---|------|------|
| `scope_type` | String | Yes | `TEAM` / `ORGANIZATION` |
| `scope_id` | Long | Yes | 対象スコープの ID |

**レスポンス（200 OK）**
```json
{
  "data": {
    "total_units": 120,
    "occupied_units": 105,
    "vacant_units": 15,
    "occupancy_rate": 87.5,
    "owner_count": 80,
    "renter_count": 40,
    "unverified_count": 5,
    "active_listings": {
      "sale": 3,
      "rent": 2
    },
    "floors": [
      { "floor": 1, "total_units": 10, "occupied_units": 9, "vacant_units": 1 },
      { "floor": 2, "total_units": 10, "occupied_units": 8, "vacant_units": 2 }
    ]
  }
}
```

---

#### `POST /api/v1/dwelling-units/import`

**認可**: ADMIN / DEPUTY_ADMIN（MANAGE_RESIDENTS）

居室マスターを CSV で一括インポートする。

**リクエスト（multipart/form-data）**
| フィールド | 型 | 必須 | 説明 |
|-----------|---|------|------|
| `file` | MultipartFile | Yes | CSV ファイル（UTF-8） |
| `scope_type` | String | Yes | `TEAM` / `ORGANIZATION` |
| `scope_id` | Long | Yes | 対象スコープの ID |

**CSV フォーマット**
```csv
unit_number,floor,area_sqm,layout,unit_type
101,1,72.50,3LDK,STANDARD
102,1,55.00,2DK,STANDARD
201,2,72.50,3LDK,STANDARD
```

- 必須カラム: `unit_number` のみ。他は任意
- `unit_type` 省略時は `STANDARD`
- 最大500件/リクエスト

**レスポンス（201 Created）**
```json
{
  "data": {
    "imported_count": 120,
    "skipped_count": 0
  }
}
```

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 400 | CSV パースエラー / 必須カラム欠落 / `unit_number` 重複 / 500件超過 |
| 403 | 権限不足 |
| 409 | スコープ内に既存の `unit_number` と重複 |

**備考**: 1件でもエラーがあれば全体ロールバック（all-or-nothing）。エラーレスポンスで該当行番号とエラー理由を返却

---

#### `GET /api/v1/dwelling-units/export`

**認可**: ADMIN / DEPUTY_ADMIN（MANAGE_RESIDENTS）

台帳データを CSV でエクスポートする。

**クエリパラメータ**
| パラメータ | 型 | 必須 | 説明 |
|-----------|---|------|------|
| `scope_type` | String | Yes | `TEAM` / `ORGANIZATION` |
| `scope_id` | Long | Yes | 対象スコープの ID |
| `include_past` | Boolean | No | `true` = 退去済み居住者を含む（デフォルト: `false`） |

**レスポンス（200 OK）**: `Content-Type: text/csv`、`Content-Disposition: attachment; filename="resident_registry_{scope_id}_{timestamp}.csv"`

**CSV カラム**: `unit_number`, `floor`, `layout`, `resident_name`, `resident_type`, `move_in_date`, `move_out_date`, `is_primary`, `ownership_share`, `lease_end_date`, `verified_at`, `emergency_contact_name`

**備考**: `resident_phone` / `emergency_contact_phone` は復号して平文で出力（CSV は管理者のみダウンロード可能）

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 403 | 権限不足 |

---

#### `GET /api/v1/dwelling-units/residents/search`

**認可**: ADMIN / DEPUTY_ADMIN（MANAGE_RESIDENTS）

スコープ内の全居室を横断して居住者を氏名検索する。

**クエリパラメータ**
| パラメータ | 型 | 必須 | 説明 |
|-----------|---|------|------|
| `scope_type` | String | Yes | `TEAM` / `ORGANIZATION` |
| `scope_id` | Long | Yes | 対象スコープの ID |
| `name` | String | Yes | 居住者氏名の部分一致検索 |
| `include_past` | Boolean | No | `true` = 退去済みを含む（デフォルト: `false`） |

**レスポンス（200 OK）**
```json
{
  "data": [
    {
      "resident_id": 10,
      "resident_name": "山田太郎",
      "resident_type": "OWNER",
      "dwelling_unit": {
        "id": 1,
        "unit_number": "101",
        "floor": 1
      },
      "move_in_date": "2020-04-01",
      "is_primary": true,
      "verified_at": "2020-04-15T10:00:00+09:00"
    }
  ]
}
```

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 400 | `name` が2文字未満 |
| 403 | 権限不足 |

---

#### `PATCH /api/v1/dwelling-units/bulk-privacy`

**認可**: ADMIN / DEPUTY_ADMIN（MANAGE_RESIDENTS）

スコープ内の全現入居者の `is_public` を一括変更する。

**リクエストボディ**
```json
{
  "scope_type": "ORGANIZATION",
  "scope_id": 1,
  "is_public": true
}
```

**レスポンス（200 OK）**
```json
{
  "data": {
    "updated_count": 185
  }
}
```

**備考**: 対象は現入居者（`move_out_date IS NULL`）のみ。退去済み居住者には影響しない

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 403 | 権限不足 |

---

#### `POST /api/v1/dwelling-units/{unitId}/residents/{id}/documents`

**認可**: ADMIN / DEPUTY_ADMIN（MANAGE_RESIDENTS）

居住者に書類を添付する。

**リクエスト（multipart/form-data）**
| フィールド | 型 | 必須 | 説明 |
|-----------|---|------|------|
| `file` | MultipartFile | Yes | 書類ファイル（10MB以下。PDF/JPEG/PNG/WebP） |
| `document_type` | String | No | `CONTRACT` / `ID_PROOF` / `OTHER`（デフォルト: `OTHER`） |

**レスポンス（201 Created）**
```json
{
  "data": {
    "id": 1,
    "original_filename": "賃貸借契約書_101号室.pdf",
    "document_type": "CONTRACT",
    "file_size": 2097152,
    "uploaded_by": { "id": 10, "display_name": "管理人 佐藤" },
    "created_at": "2026-04-01T10:00:00+09:00"
  }
}
```

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 400 | 対応していないファイル形式 / 添付数上限（3件）超過 |
| 403 | 権限不足 |
| 413 | ファイルサイズ超過（10MB） |

---

#### `POST /api/v1/property-listings/{id}/inquire`

**認可**: MEMBER 以上（スコープメンバー）

物件掲示に「興味あり」を送信する。

**リクエストボディ**
```json
{
  "message": "内覧希望です。来週の土日で可能でしょうか？"
}
```

- `message`: 任意（最大500文字）

**レスポンス（201 Created）**
```json
{
  "data": {
    "listing_id": 1,
    "inquired_at": "2026-03-15T10:00:00+09:00"
  }
}
```

**副作用**:
1. `property_listings.inquiry_count` をアトミック更新（+1）
2. 投稿者にプッシュ通知（「{user_name} さんが {title} に興味を示しています」）
3. `message` がある場合: 通知に本文を含める

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 403 | スコープメンバーでない / SUPPORTER は問い合わせ不可 |
| 409 | 既に問い合わせ済み / 自分自身の掲示 / ACTIVE でない掲示 |

---

#### `GET /api/v1/property-listings/{id}/inquiries`

**認可**: 投稿者本人 / ADMIN

物件掲示への問い合わせ一覧を取得する。

**レスポンス（200 OK）**
```json
{
  "data": [
    {
      "id": 1,
      "user": { "id": 200, "display_name": "鈴木次郎" },
      "message": "内覧希望です。来週の土日で可能でしょうか？",
      "created_at": "2026-03-15T10:00:00+09:00"
    }
  ]
}
```

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 403 | 投稿者本人でも ADMIN でもない |
| 404 | 掲示が存在しない |

---

#### `POST /api/v1/dwelling-units/{id}/invite`

**認可**: ADMIN / DEPUTY_ADMIN（MANAGE_RESIDENTS）

居住者セルフ登録用の招待リンクを生成する。

**リクエストボディ**
```json
{
  "resident_type": "OWNER",
  "expires_hours": 168
}
```

- `resident_type`: 必須。招待される居住者の種別（`OWNER` / `RENTER`）。セルフ登録時に変更不可
- `expires_hours`: 任意。招待リンクの有効期限（時間）。デフォルト: 168（7日間）

**レスポンス（201 Created）**
```json
{
  "data": {
    "invite_url": "https://app.mannschaft.io/dwelling-units/register?token=abc123...",
    "token": "abc123...",
    "dwelling_unit_id": 1,
    "unit_number": "101",
    "resident_type": "OWNER",
    "expires_at": "2026-04-07T09:00:00+09:00"
  }
}
```

**備考**:
- トークンは `SecureRandom` + Base64URL 方式で生成（`invite_tokens` テーブルと同一方式）
- 1居室に対して複数の招待リンクを同時発行可能（夫婦等の複数居住者用）
- 招待リンクには居室 ID と居住者種別が紐付き、セルフ登録時に自動設定される
- 有効期限切れ・使用済みトークンは無効

---

#### `POST /api/v1/dwelling-units/self-register`

**認可**: ログイン済みユーザー（招待トークンで認証）

招待リンク経由で居住者情報をセルフ登録する。ADMIN の verify 待ち状態で INSERT される。

**リクエストボディ**
```json
{
  "token": "abc123...",
  "resident_name": "山田太郎",
  "resident_phone": "090-1234-5678",
  "resident_email": "yamada@example.com",
  "move_in_date": "2020-04-01",
  "emergency_contact_name": "山田一郎",
  "emergency_contact_phone": "080-9999-8888"
}
```

**レスポンス（201 Created）**: 居住者詳細（`verified_at = NULL`、`is_public = TRUE`）

**副作用**:
1. `resident_registry` に INSERT（`user_id = currentUser`、`verified_at = NULL`）
2. `dwelling_units.resident_count` をアトミック更新（+1）
3. ADMIN にプッシュ通知（「○○号室に居住者がセルフ登録しました。確認をお願いします」）
4. 招待トークンを使用済みに変更

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 400 | バリデーションエラー / トークン無効 or 期限切れ |
| 409 | 既にこの居室の現入居者として登録済み |

---

#### `PATCH /api/v1/dwelling-units/{id}/move-out-all`

**認可**: ADMIN / DEPUTY_ADMIN（MANAGE_RESIDENTS）

居室の全現入居者を一括退去処理する。

**リクエストボディ**
```json
{
  "move_out_date": "2026-03-31"
}
```

**レスポンス（200 OK）**
```json
{
  "data": {
    "dwelling_unit_id": 1,
    "unit_number": "101",
    "moved_out_count": 3,
    "resident_count": 0
  }
}
```

**副作用**:
1. 居室の全現入居者（`move_out_date IS NULL`）に `move_out_date` を設定
2. `dwelling_units.resident_count` を 0 に更新
3. 掲示中の物件があれば `WITHDRAWN` に自動遷移
4. 紐付き `user_id` がある居住者にプッシュ通知

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 400 | `move_out_date` が不正 / 現入居者がいない |
| 403 | 権限不足 |
| 404 | 居室が存在しない |

---

#### `PATCH /api/v1/dwelling-units/{unitId}/residents/{id}/renew-lease`

**認可**: ADMIN / DEPUTY_ADMIN（MANAGE_RESIDENTS）

賃貸借契約を更新する（`lease_end_date` 延長 + 通知カウンターリセット）。

**リクエストボディ**
```json
{
  "new_lease_end_date": "2028-03-31"
}
```

**レスポンス（200 OK）**: 居住者詳細（`lease_end_date` 更新済み）

**副作用**:
1. `lease_end_date` を更新
2. `last_lease_notified_days` を NULL にリセット（次回の通知サイクルを再開）
3. 監査ログ: `LEASE_RENEWED` を記録
4. `user_id` がある場合: 居住者にプッシュ通知（「賃貸借契約が更新されました。新しい満了日: {new_lease_end_date}」）

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 400 | `new_lease_end_date` <= 現在の `lease_end_date` / `resident_type` が `OWNER` |
| 403 | 権限不足 |
| 404 | 居住者が存在しない |

---

## 5. ビジネスロジック

### 居室・居住者の管理フロー

```
1. ADMIN が居室マスターを一括 or 個別登録
   a. 部屋番号・階数・面積・間取り・種別を入力
   b. CSV 一括インポート（POST /dwelling-units/import）で初期導入をサポート（最大500件）

2. 居住者の登録（2方式）
   方式 A: ADMIN が手動登録
     a. 居住者種別（OWNER / RENTER）を選択
     b. プラットフォーム登録済みユーザーなら user_id を紐付け
     c. 未登録の高齢者等は氏名・連絡先のみ手動入力
     d. 共有持分比率を設定（区分所有者の場合）
     e. is_public を設定（本人への公開可否）
   方式 B: セルフ登録（招待リンク）
     a. ADMIN が POST /dwelling-units/{id}/invite で招待リンクを生成
     b. 居住者にリンクを共有（メール・紙配布・掲示等）
     c. 居住者がリンクからログイン → POST /self-register で氏名・連絡先・入居日を入力
     d. verified_at = NULL（未確認）の状態で INSERT。ADMIN に通知
     e. ADMIN が PATCH /verify で確認（本人確認書類等）
     ※ 大規模マンション導入時は方式 B で居住者に自己入力してもらい、ADMIN は verify のみ行う運用を推奨

3. 居住者の退去処理
   a. ADMIN が PATCH /move-out で退去日を記録
   b. resident_count を自動更新
   c. 主居住者が退去した場合は次の居住者に自動移譲

4. 居住者の確認（verify）
   a. ADMIN が本人確認書類等を確認後、PATCH /verify で確認印を付与
   b. verified_at / verified_by が記録される
   c. UI 上で「確認済み」バッジが表示される
```

### 物件掲示板フロー

```
1. 現入居者が POST /property-listings で掲示を投稿
   a. 1居室につき ACTIVE な掲示は1件のみ
   b. 掲示期限（expires_at）を任意で設定
   c. 写真を最大5枚添付可能（S3 にアップロード、photo_keys に格納）

2. スコープ内メンバーが掲示を閲覧
   a. 閲覧時に view_count をアトミック更新
   b. 連絡方法に応じてコンタクト（IN_APP / EMAIL / PHONE）

3. 成約・取り下げ
   a. PATCH /close で成約処理（ACTIVE → CLOSED）
   b. PATCH /withdraw で取り下げ（ACTIVE → WITHDRAWN）
   c. CLOSED / WITHDRAWN は復帰不可（再投稿で対応）

4. 期限切れ事前通知 + 自動取り下げ
   a. @Scheduled バッチ（1日1回）
   b. expires_at の 7 日前: 投稿者にプッシュ通知（「物件掲示の期限が7日後に切れます: {title}。延長する場合は掲示を更新してください」）
   c. expires_at 到達: ACTIVE 掲示を WITHDRAWN に自動遷移。投稿者に通知（「物件掲示が期限切れにより取り下げられました」）
```

### 賃貸借契約満了通知バッチ

```
1. @Scheduled で日次実行（毎朝 9:00）
2. 対象: resident_registry の move_out_date IS NULL AND lease_end_date IS NOT NULL
3. 通知タイミング:
   a. lease_end_date まで残り 90 日以内: 初回通知
   b. lease_end_date まで残り 30 日以内: リマインド通知
4. 通知先: スコープの ADMIN / DEPUTY_ADMIN（MANAGE_RESIDENTS）
5. 通知内容: 「賃貸借契約が○○日後に満了します: {unit_number} {resident_name}（満了日: {lease_end_date}）」
6. 重複防止: `resident_registry.last_lease_notified_days` で制御
   a. 残日数が 90 日以内かつ last_lease_notified_days IS NULL → 通知送信、last_lease_notified_days = 90 に更新
   b. 残日数が 30 日以内かつ last_lease_notified_days = 90 → 通知送信、last_lease_notified_days = 30 に更新
   c. last_lease_notified_days = 30 → 送信済みのためスキップ
   d. lease_end_date が更新（契約更新）された場合: last_lease_notified_days を NULL にリセット（Service 層で PUT /residents/{id} 時に実装）
```

### プライバシー管理

```
1. 台帳のデフォルトは非公開（is_public = FALSE）
   - ADMIN / DEPUTY_ADMIN のみが全居住者情報を閲覧可能
   - 個人は自室・自身の情報すら閲覧不可（明示的に is_public = TRUE にするまで）

2. ADMIN が is_public = TRUE に設定した居住者のみ:
   - 本人が GET /dwelling-units/my で自分の情報を閲覧可能
   - 他居住者の情報は見えない（本人の情報のみ）

3. 物件掲示板は is_public とは独立
   - スコープ内メンバー全員が閲覧可能（SUPPORTER 含む）
   - 投稿はメンバー以上かつ現入居者のみ
```

### 重要な判定ロジック

- **居室削除の制約**: `resident_count > 0` の居室は論理削除不可。先に全居住者の退去処理が必要
- **共有持分の整合性**: 同一居室の `OWNER` の `ownership_share` 合計が 100.00 を超えないことを検証
- **主居住者の唯一性**: 1居室あたり `is_primary = TRUE` は最大1名。自動判定: 最初の居住者は自動 TRUE、2人目以降はデフォルト FALSE。明示的に TRUE を指定した場合は既存の主居住者を FALSE に自動変更（排他制御）
- **物件掲示の排他**: 1居室あたり `ACTIVE` な `property_listings` は最大1件
- **退去時の連鎖処理**: 主居住者退去 → `is_primary` 自動移譲 + 掲示の自動取り下げ
- **議決権行使（F08.3）との関係**: `eligible_count`（議決権総数）と居住者台帳は**独立管理**。`eligible_count` = スコープ内 MEMBER 以上のユーザー数（プラットフォーム上の権限）、居住者台帳 = 物理的な居住実態（不動産の権利関係）であり、概念が異なる。台帳未登録の区分所有者、台帳登録済みだが MEMBER 未登録の高齢者、MEMBER だが区分所有者でない管理会社スタッフ等のケースで不一致が生じるため、統合すると柔軟性が失われる
- **CSV インポート**: 居室マスター（`dwelling_units`）のみ対象。居住者情報は個人情報のため CSV インポート対象外（ADMIN が手動登録）
- **ユーザー紐付け**: `resident_registry.user_id` は ADMIN の手動紐付けのみ。メールアドレス一致での自動紐付けは行わない（共有メール・typo による誤紐付け → 個人情報漏洩リスク）
- **物件掲示の写真**: `photo_keys` JSON で最大5枚。別テーブルではなく JSON カラムを採用（メタデータの個別管理不要・最大5枚と少量・掲示の CRUD と一体管理）
- **セルフ登録**: 招待リンク方式。ADMIN が居室ごとにトークンを発行 → 居住者がログイン後にセルフ登録 → `verified_at = NULL` で INSERT → ADMIN が verify。大規模導入時の ADMIN 負荷を軽減
- **居住者の重複防止**: 同一居室 + 同一 `user_id` + 現入居（`move_out_date IS NULL`）の組み合わせを Service 層で検証。退去後の再入居（例: 一時転居→復帰）は許可
- **一括退去**: `PATCH /move-out-all` で居室の全現入居者を一括処理。`resident_count` は 0 にリセット
- **契約更新**: `PATCH /renew-lease` で `lease_end_date` を延長し、`last_lease_notified_days` を NULL リセット。監査ログに `LEASE_RENEWED` を記録
- **緊急連絡先**: `emergency_contact_name` / `emergency_contact_phone` で家族・親族の連絡先を管理。F03.6 安否確認で ADMIN が参照可能
- **物件掲示の問い合わせ**: 1ユーザー1掲示につき1回のみ（UNIQUE KEY で重複防止）。自己問い合わせ不可。投稿者にリアルタイム通知
- **居住者書類**: ADMIN のみアップロード・閲覧可能。居住者本人には非公開（管理者保管の書類）。1居住者最大3件
- **駐車場クロスリファレンス**: GET /{id} で F09.3 モジュールが有効な場合のみ `parking_assignments` を返却（モジュール無効時は省略）
- **グローバル検索**: 検索キーワードは2文字以上必須（1文字での全件マッチを防止）

---

## 6. セキュリティ考慮事項

- **認可チェック**: 全 API でスコープメンバーシップとロールを検証。台帳の CRUD は ADMIN / DEPUTY_ADMIN（MANAGE_RESIDENTS）に限定
- **個人情報保護**: `resident_phone` は AES-256 で暗号化して保存。API レスポンスでは ADMIN と本人にのみ返却
- **プライバシーバイデフォルト**: `is_public = FALSE` がデフォルト。居住者情報は明示的に公開設定しない限り、本人にも非公開
- **IDOR 防止**: `GET /dwelling-units/my` は `user_id = currentUser` に限定。他人の居住者情報を ID 推測でアクセスできない
- **写真の安全性**: 物件掲示の写真は Content-Type + マジックバイトの二重チェック。S3 に保存し CloudFront 経由の Pre-signed URL（有効期限1時間）で配信
- **CSV インポートのバリデーション**: ファイルサイズ上限 1MB。500行超過は拒否。各行の unit_number にスクリプトインジェクション文字列が含まれていないことを検証
- **CSV エクスポートの情報管理**: `resident_phone` / `emergency_contact_phone` は復号して平文出力。エクスポートは ADMIN のみ。監査ログに `DWELLING_UNITS_EXPORTED` を記録
- **セルフ登録の安全性**: 招待トークンは `SecureRandom` + Base64URL。有効期限付き・1回限り使用。セルフ登録した居住者は `verified_at = NULL`（未確認）状態であり、ADMIN の verify まで台帳上に「未確認」マークが表示される
- **レートリミット**: `Bucket4j` で以下の制限を適用
  - 居室・居住者 CRUD: 1分間20件
  - CSV インポート: 1分間1件
  - CSV エクスポート: 1分間5件
  - 物件掲示投稿: 1分間5件
  - 招待リンク生成: 1分間10件
  - セルフ登録: 1分間5件
  - 居住者書類アップロード: 1分間10件
  - 物件掲示問い合わせ: 1分間10件
  - 居住者検索: 1分間20件
  - 統計 API: 1分間10件
- **監査ログ**: 以下のイベントを記録
  - `DWELLING_UNIT_CREATED`: 居室作成
  - `DWELLING_UNIT_DELETED`: 居室削除（論理削除）
  - `DWELLING_UNITS_IMPORTED`: CSV 一括インポート（件数を含む）
  - `RESIDENT_ADDED`: 居住者追加
  - `RESIDENT_UPDATED`: 居住者情報更新（user_id 紐付け変更を含む）
  - `RESIDENT_MOVED_OUT`: 退去処理
  - `RESIDENT_VERIFIED`: 居住者確認
  - `LISTING_POSTED`: 物件掲示投稿
  - `LISTING_CLOSED`: 成約処理
  - `RESIDENT_SELF_REGISTERED`: セルフ登録
  - `RESIDENT_INVITE_CREATED`: 招待リンク生成
  - `LEASE_RENEWED`: 契約更新
  - `RESIDENTS_BULK_MOVED_OUT`: 一括退去
  - `RESIDENTS_PRIVACY_BULK_CHANGED`: is_public 一括変更
  - `LISTING_INQUIRY_RECEIVED`: 物件掲示への問い合わせ

---

## 7. Flywayマイグレーション

```
V9.010__add_manage_residents_permission.sql     -- MANAGE_RESIDENTS パーミッション追加
V9.011__create_dwelling_units.sql               -- 居室マスターテーブル作成
V9.012__create_resident_registry.sql            -- 居住者情報テーブル作成
V9.013__create_resident_documents.sql           -- 居住者書類テーブル作成
V9.014__create_property_listings.sql            -- 物件掲示板テーブル作成
V9.015__create_property_listing_inquiries.sql   -- 物件掲示問い合わせテーブル作成
```

**マイグレーション上の注意点**
- V9.010: `MANAGE_RESIDENTS` を `permissions` テーブルに INSERT し、`role_permissions` に ADMIN 用のシードを追加。scope = TEAM / ORGANIZATION
- `dwelling_units.team_id` / `organization_id` は Phase 1 で作成済みのテーブルへの FK
- `resident_registry.user_id` は `users` テーブルへの nullable FK
- V9.001〜V9.009 は他の Phase 9 機能（広告・外部連携・プロモーション配信等）で使用される可能性があるため、V9.010 以降を本機能で使用

---

## 8. 未解決事項

- [x] ~~居室の CSV 一括インポート機能の要否~~ → **Phase 9 スコープに含める**。`POST /dwelling-units/import`（CSV、最大500件、all-or-nothing）。対象は居室マスターのみ（居住者情報は個人情報のため CSV 対象外）。マンション新規導入時に100室以上を手動登録するのは非現実的であり、導入障壁の除去が目的
- [x] ~~`resident_registry` と `users` テーブルの自動紐付け~~ → **ADMIN の手動紐付けのみ。自動紐付けは行わない**。メールアドレスの一致で自動紐付けすると、共有メール（夫婦・家族）での誤紐付け → 個人情報漏洩リスクがある。代わりに居住者詳細画面の「ユーザー紐付け」UIでスコープ内ユーザー検索 → 選択 → 紐付けのフローを提供。API は既存の `PUT /residents/{id}` で `user_id` を更新するだけなので追加 API 不要
- [x] ~~賃貸借契約満了通知~~ → **導入する**。日次バッチ（毎朝9時）で `lease_end_date` の90日前・30日前に ADMIN に通知。既存の通知基盤（F04.3）を利用。契約更新の見逃しは管理上の重大リスクであり、低コストで高効果
- [x] ~~議決権行使（F08.3）との統合深度~~ → **独立管理のまま。統合しない**。`eligible_count`（プラットフォーム上の MEMBER 数）と居住者台帳（物理的居住実態）は異なる概念。台帳未登録の区分所有者、MEMBER 未登録の高齢者、MEMBER だが非所有者の管理会社スタッフ等で不一致が生じる。統合すると柔軟性が失われるため、ADMIN がセッション作成時に手動確認する現行設計を維持
- [x] ~~物件掲示の写真添付~~ → **Phase 9 スコープに含める**。`property_listings.photo_keys` JSON カラムで S3 キー配列を保持（最大5枚）。別テーブルではなく JSON を採用（メタデータ個別管理不要・少量・掲示の CRUD と一体管理）。S3 + CloudFront Pre-signed URL（有効期限1時間）で配信。POST/PUT を multipart/form-data に変更

---

## 9. 変更履歴

| 日付 | 変更内容 |
|------|---------|
| 2026-03-15 | UX・保守性改善5件: (1) 居住者グローバル検索（GET /residents/search。スコープ内全居室横断の氏名検索）(2) is_public 一括変更（PATCH /bulk-privacy。初期導入時の一括設定）(3) 居住者書類添付（`resident_documents` テーブル + POST/DELETE API。契約書・本人確認書類を ADMIN が保管）(4) 物件掲示への問い合わせ機能（`property_listing_inquiries` テーブル + POST /inquire + GET /inquiries。「興味あり」ボタン + inquiry_count denormalize + 投稿者通知）(5) 駐車場区画クロスリファレンス（GET /{id} レスポンスに F09.3 parking_assignments を表示。ADMIN のみ）。DB 5テーブル（+2）、API 33本（+6）、Flyway V9.010-015 |
| 2026-03-15 | UX・保守性改善8件: (1) 居住者セルフ登録（招待リンク方式。POST /invite + POST /self-register。ADMIN は verify のみで導入負荷を大幅軽減）(2) 一括退去（PATCH /move-out-all。家族の引越し等を1操作で処理）(3) 契約更新アクション（PATCH /renew-lease。lease_end_date 延長 + 通知カウンターリセット + 監査ログ LEASE_RENEWED）(4) 台帳 CSV エクスポート（GET /export。resident_phone は復号して平文出力。ADMIN のみ）(5) 緊急連絡先（emergency_contact_name / emergency_contact_phone カラム追加。F03.6 安否確認と連携可能）(6) 物件掲示の期限切れ事前通知（7日前に投稿者へ通知）(7) 統計 API の階数別内訳（floors 配列追加）(8) 居住者の重複登録防止（同一居室 + user_id + 現入居の組み合わせを Service 層で検証）。API 27本（+6） |
| 2026-03-15 | 精査②: (1) resident_phone VARCHAR(20)→VARCHAR(255)（AES暗号化+Base64後のデータサイズに対応）(2) GET /{id} の認可修正 — MEMBER は is_public に関わらず自室にアクセス可能（/my との整合性確保）。レスポンス内容をロールで制御（MEMBER は連絡先・備考・過去居住者を非表示）(3) is_primary のデフォルトを TRUE→FALSE に変更 + Service 層で自動判定（最初の居住者は自動TRUE、明示指定時は排他切替）(4) DELETE /residents を退去済みのみに制限（resident_count との不整合を防止）(5) 欠落API仕様4本追記（GET /residents, PUT /property-listings, property_listings論理削除制約）(6) view_count の重複カウント方針を明記（シンプルな総閲覧数、ユニーク管理なし） |
| 2026-03-15 | 精査①: (1) UNIQUE KEY の NULL カラム問題修正 — team_id/organization_id 別に2本の UNIQUE KEY に分離（MySQL の NULL 重複許容を回避）(2) 管理フローの重複ステップ b を削除 (3) resident_registry.user_id の ON DELETE SET NULL を明記 (4) 契約満了通知の重複防止を `last_lease_notified_days` カラムで実装（90日前/30日前の各ポイントで1回のみ送信）(5) GET /property-listings レスポンスに photo_urls 追加 (6) 欠落 API 仕様5本追記（POST /dwelling-units, PATCH /verify, /close, /withdraw, --- 区切り）(7) 監査ログに居室 CRUD・CSV インポート・居住者更新の4イベント追加 |
| 2026-03-15 | 未解決事項5件解決: (1) CSV一括インポート導入（POST /dwelling-units/import、居室マスターのみ最大500件、all-or-nothing）(2) ユーザー紐付けは ADMIN 手動のみ（メール自動紐付けは誤紐付け→個人情報漏洩リスクのため不採用）(3) 賃貸借契約満了通知導入（日次バッチ、90日前/30日前に ADMIN 通知）(4) F08.3 議決権行使とは独立管理（eligible_count とは概念が異なるため統合しない）(5) 物件掲示の写真添付導入（photo_keys JSON、最大5枚、Pre-signed URL 配信）。API 21本（+1） |
| 2026-03-15 | 初版作成: DB 3テーブル（dwelling_units / resident_registry / property_listings）、API 20本、居室・居住者管理フロー、物件掲示板フロー、プライバシーバイデフォルト設計（is_public=FALSE）、退去時の連鎖処理（主居住者移譲・掲示取り下げ）、共有持分比率管理、居住者確認（verify）機能 |
