# F08.12: 運営領収書・適格請求書発行（PLATFORM スコープ）

> **ステータス**: 🟡 設計中
> **実装フェーズ**: 未着手
> **最終更新**: 2026-09-02
> **モジュール種別**: 運営基盤（プラットフォーム収入の証憑発行）
> **関連ドキュメント**: F08.4 領収書発行（団体スコープ。器を共有する）、F19 広告プラットフォーム（`ad_invoices`）、F09.13 通知プリペイドクレジット（`notification_credit_purchases`）、F20.1 課金センター（`billing_invoices`。**投影実装なし**）、`docs/architecture/domain_db_design_principles.md`

---

## 1. 概要

Mannschaft **運営（プラットフォーム事業者）自身が発行者**となる領収書および適格請求書（インボイス）を発行する機能。

F08.4 が扱うのは「チーム・組織の ADMIN が、会費・イベント参加費に対して**自分の団体名義で**発行する領収書」である。本書が扱うのはその逆で、「**Mannschaft 運営が、運営の収入に対して支払者（広告主・組織）へ発行する証憑**」である。発行者が違うだけで文書としての性質・法的要件（インボイス制度・電子帳簿保存法）は同じであるため、**テーブル・エンティティ・PDF 生成基盤を F08.4 と共有し、スコープ種別（`PLATFORM`）で分離する**。

### 対象となる運営の収入 3 系統

| 系統 | 実装状況（2026-09-02 実測） | 本戦役での扱い |
|------|--------------------------|---------------|
| 広告費（`ad_invoices`） | 稼働形。月次バッチ + Stripe Invoice + 手動 markPaid | **第1段で対応** |
| 通知プリペイドクレジット（`notification_credit_purchases`） | 稼働形。Stripe Checkout → Webhook → 残高加算 | **第1段で対応** |
| 月謝サブスク（`billing_invoices`） | **DDL のみ実在。Java エンティティが 1 つも無く投影実装が空** | **第2段（後追い）** — §10 |

### 本ドキュメントのスコープ

| 機能 | 説明 |
|------|------|
| PLATFORM 発行者設定 | 運営の発行者名・住所・登録番号（インボイス）を 1 行だけ保持する |
| 運営領収書の発行 | 広告費入金・クレジット購入完了を契機に自動発行 |
| 適格請求書（請求書 PDF）の是正 | `InvoicePdfService`（日本語不可・宛名なし・登録番号なし）を PDF 基盤へ寄せる |
| 支払者向け閲覧 | 広告主・組織が自分宛の領収書／請求書を一覧・PDF 取得 |
| 運営コンソール | SYSTEM_ADMIN による一覧・詳細・無効化・再発行 |
| 電子帳簿保存法の原本保存 | PDF 原本を保存し改ざん・削除を防止する共通基盤（**団体側 F08.4 と共用**） |

**スコープ外**:
- 団体（TEAM / ORGANIZATION）が発行する領収書 → F08.4
- Stripe が自動送付するレシート（`receipt_url`）→ 正式な証憑ではないため本書の対象外
- 運営の支出側（仕入・外注費）の証憑受領・保存 → 将来検討
- 会計ソフト（freee / MF）との API 連携 → 将来検討（CSV エクスポートまでを本書の範囲とする）

---

## 2. スコープ

### 対象ロール
| ロール | 操作可能な範囲 |
|--------|--------------|
| SYSTEM_ADMIN | 運営領収書の発行・一覧・詳細・無効化・再発行・PLATFORM 発行者設定 |
| 広告主（`advertiser_accounts` の所有ユーザー） | 自分のアカウント宛の領収書・請求書の閲覧と PDF 取得のみ |
| 組織 ADMIN（通知クレジット購入元） | 自組織宛の領収書の閲覧と PDF 取得のみ |
| 団体 ADMIN / DEPUTY_ADMIN / MEMBER | **PLATFORM スコープの管理 API へは一切アクセス不可**（§2.1） |

### 対象レベル
- [ ] 組織 (Organization)
- [ ] チーム (Team)
- [ ] 個人 (Personal)
- [x] **プラットフォーム (Platform)** ← 本書で新設するスコープ

### 2.1 認可の前提（実コードで裏取り済み・当初の想定は誤りだった）

`AccessControlService.resolveEffectiveRole()` は **`isSystemAdmin(userId)` を最初に評価して priority 1 で即 return する**（`AccessControlService.java`:236-239）。したがって `checkAdminOrAbove(userId, 0L, "PLATFORM")` は SYSTEM_ADMIN に対しては 403 にならず**通る**。

実際の問題はそこではなく、**非 SYSTEM_ADMIN が `ScopeType.valueOf(scopeType)`（同 :256）に到達して `IllegalArgumentException` を投げ、500 になる**ことである。`ScopeType` enum に `PLATFORM` は存在しない。

```
非SYSTEM_ADMIN → resolveEffectiveRole(userId, 0, "PLATFORM")
  → isSystemAdmin=false
  → ScopeType.valueOf("PLATFORM") → IllegalArgumentException → 500
```

**設計方針**: `AccessControlService` に PLATFORM 用の分岐を先に置く。

```java
// AccessControlService（新設ガード。全 check 系の入口）
public void checkPlatformAdmin(Long userId, ReceiptScopeType scopeType) { ... }

// resolveEffectiveRole の冒頭（isSystemAdmin 判定の直後）
if (PLATFORM_SCOPE_TYPE.equals(scopeType)) {
    // SYSTEM_ADMIN は上の分岐で解決済み。ここに来る = 非 SYSTEM_ADMIN
    return Optional.empty();   // → 403（500 にしない）
}
```

これにより、団体 ADMIN が PLATFORM スコープの管理 API を叩いた場合の応答は **403（`COMMON_002`）** に確定する。ただし本設計では**運営領収書の管理 API はそもそも `/api/v1/system-admin/**` 配下に置く**ため、`SecurityConfig.java`:527 の `.requestMatchers("/api/v1/system-admin/**").hasRole("SYSTEM_ADMIN")` によりフィルタチェーンで先に 403 になる。上記のガードは、Service 層が別経路（バッチ・イベントリスナー）から呼ばれた場合の二重防御である。

Controller には `@AuthorizedByPathConfig("/api/v1/system-admin/**")` をクラス付与する。これで `AuthzControllerGuardArchTest` を満たす（既存の `SystemActivityPresetController` と同じ型）。

---

## 3. DB設計

### 3.0 器の再利用（案A採用。運営専用テーブルは作らない）

マスターの裁可により、**既存 receipt ドメインに `PLATFORM` スコープを足して再利用する**。実測で以下を確認済み。

| 確認項目 | 実測結果 | 帰結 |
|---|---|---|
| `receipts.scope_type` / `receipt_issuer_settings.scope_type` の型 | `VARCHAR(20)`（ENUM ではない）。`V8.023__create_receipts.sql`:4 / `V8.022`:4 | 値の追加に **DDL 変更・マイグレーション不要** |
| JPA マッピング | `@Enumerated(EnumType.STRING)`（`ReceiptEntity.java`:34-36） | Java enum に定数を足すだけ |
| `scope_id` への FK | **無い**（FK は `recipient_user_id` / `issued_by` / `voided_by` の 3 本のみ） | センチネル値でも FK 違反が起きない |
| UNIQUE 制約 | `uq_ris_scope (scope_type, scope_id)` / `uq_r_receipt_number (scope_type, scope_id, receipt_number)` | `scope_type='PLATFORM'` で他スコープと衝突しない |
| `member_payment_id` | **NULL 許容・FK 無し**、単独インデックス | NULL のまま運営領収書を作れる |

したがって `ReceiptScopeType` に定数を 1 つ足す。

```java
public enum ReceiptScopeType {
    ORGANIZATION,
    TEAM,
    /** 運営（Mannschaft 自身）。scope_id は常に PLATFORM_SCOPE_ID。 */
    PLATFORM
}
```

**センチネル `scope_id`**: `scope_id` は `BIGINT UNSIGNED NOT NULL` のため NULL を置けない。`0` を運営スコープの固定値とし、**定数 1 箇所に閉じ込める**。

```java
public final class ReceiptScopes {
    /** PLATFORM スコープの scope_id。実在する team/organization の id は 1 以上のため衝突しない。 */
    public static final Long PLATFORM_SCOPE_ID = 0L;
    private ReceiptScopes() {}
}
```

> **番人（要実装）**: `scope_type = 'PLATFORM'` の行で `scope_id <> 0` のものが作られないことを保証するテスト、および `0L` リテラルが `ReceiptScopes` 以外に散らないことを検査する ArchUnit テストを置く。

### 3.1 既存テーブルの変更

#### `receipts` — 元データ参照の汎用化

現状 `member_payment_id` しか元データを指せない。運営領収書の元データは `ad_invoices` / `notification_credit_purchases` /（将来）`billing_invoices` であるため、汎用列を**追加**する。`member_payment_id` は既存互換のため**残す**（Java 側の参照 74 箇所を無改修にするため）。

| カラム名 | 型 | NULL | デフォルト | 説明 |
|---------|---|------|-----------|------|
| `source_type` | VARCHAR(30) | YES | NULL | 元データ種別（`MEMBER_PAYMENT` / `AD_INVOICE` / `NOTIFICATION_CREDIT_PURCHASE` / `BILLING_INVOICE` / `MANUAL`） |
| `source_ref` | VARCHAR(64) | YES | NULL | 元データ ID の**文字列表現**（FK は張らない。クロスドメイン FK 禁止原則1） |

```sql
ALTER TABLE receipts
  ADD COLUMN source_type VARCHAR(30) NULL COMMENT '元データ種別',
  ADD COLUMN source_ref  VARCHAR(64) NULL COMMENT '元データIDの文字列表現（FK なし・アプリ層で整合性保証）',
  ADD INDEX idx_r_source (source_type, source_ref);
```

##### なぜ ID 列を `BIGINT` ではなく `VARCHAR(64)` にするのか（実測に基づく判断）

収入 3 系統の主キー型を実測したところ、**揃っていない**。

| 元データ | 主キー型 | 出典 |
|---|---|---|
| `member_payments` | `BIGINT UNSIGNED` | 既存 `receipts.member_payment_id` が `BIGINT UNSIGNED`（`V8.023`:7） |
| `ad_invoices` | `BIGINT UNSIGNED AUTO_INCREMENT` | `V10.062__create_ad_invoices.sql`:2 |
| `notification_credit_purchases` | `BIGINT UNSIGNED AUTO_INCREMENT` | `V9.118__create_notification_credit_purchases.sql`:6 |
| **`billing_invoices`** | **`BINARY(16)`（UUIDv7）** | `V196.20260831142049__expand_billing_center.sql`:494 |

`source_id BIGINT UNSIGNED` で定義すると、**第2段（サブスク）で `billing_invoices.id` を保存も返却もできない**。法的文書であり行数も増える表の列型を後から変えるのは避けたいため、**最初から両方入る型で切る**。

**型と命名の根拠（このリポの既存流儀に合わせた）**:
- **「種別 + ID」の持ち方**は `seal_stamp_logs` に倣う — `target_type VARCHAR(30)` / `target_id` + `INDEX idx_seal_stamp_logs_target (target_type, target_id)`（`V5.013__create_seal_stamp_logs_table.sql`:7-8, 16）。列の並びと複合索引の形はこの流儀どおりにする。
- **「FK を張らない論理参照を文字列で持つ」**場合の命名は billing ドメインの `_ref VARCHAR(...)` に倣う（`psp_invoice_ref` / `psp_subscription_ref` 等。`V151.20260710123257__expand_billing_contracts_psp.sql`:22 のコメントに「論理参照」と明記されている）。
- 両者を合わせて **`source_type` + `source_ref VARCHAR(64)`** とする。`seal_stamp_logs` の `target_id BIGINT` をそのまま真似ると UUID が入らないため、**ID の型だけ billing 流儀に寄せた**。

**格納形式（アプリ層の規約。ぶれると照合が壊れるため厳密に定める）**:
- `BIGINT` 系: 10 進数の文字列。**ゼロ埋めしない**（`"12345"`）
- `UUIDv7` 系: `java.util.UUID#toString()` の**小文字ハイフン付き 36 文字**
- 変換は値オブジェクト `ReceiptSourceRef` の 1 箇所に閉じ込め、`Long` / `UUID` からの生成と復元だけを公開する。**呼び出し側に `String.valueOf(id)` を書かせない**
- `VARCHAR(64)` は UUID 36 文字・`BIGINT UNSIGNED` 最大 20 桁のいずれにも足り、複合索引 `(source_type, source_ref)` も `utf8mb4` の索引長上限に収まる

> **番人（要実装）**: `source_ref` が上記の格納形式（`^\d+$` または小文字 UUID）に一致することを検証するテストを置く。`source_type` ごとに期待する形式が違うため、`ReceiptSourceRef` の生成経路以外から値が入らないことも合わせて検査する。

**冪等性**: 同一 `(scope_type, source_type, source_ref)` に対して有効な領収書は 1 枚に限る。ただし団体側は「分割領収書」ユースケースのため UNIQUE を張らない設計（F08.4 §8）であり、**PLATFORM だけ UNIQUE にすることは MySQL の部分索引が無いためできない**。代替として:

- アプリ層で発行前に `existsByScopeTypeAndSourceTypeAndSourceRefAndVoidedAtIsNull` を検査し、既発行なら `RECEIPT_026`（409）
- 発行契機は自動（イベント）であり、**冪等キーを持つ**（§5.2）

#### `receipts` — PDF の生成・保存状態

**現状の問題（実測）**: `ReceiptService.java`:661 は
`String pdfStatus = receipt.getPdfStorageKey() != null ? "READY" : "GENERATING";`
と書かれており、状態は `pdf_storage_key` の NULL 判定から**導出**されている。しかも
`ReceiptEntity.updatePdfStorageKey()`（:172）の呼び出し元は **0 件**であるため、
**`pdfStatus` は常に `GENERATING` を返す**。失敗を記録する場所が無く、
「失敗したのか、まだ生成中なのか」を区別できないため、再試行の判断もできない。

`receipt_pdf_archives`（§3.3）は `storage_key` / `content_sha256` が `NOT NULL` の
**成功した原本だけを載せる表**である。失敗は原本ではないので、そこには置けない。
したがって **PDF の生成・保存状態は `receipts` 側に持つ**。この配置を設計上の確定事項とする。

| カラム名 | 型 | NULL | デフォルト | 説明 |
|---------|---|------|-----------|------|
| `pdf_status` | VARCHAR(20) | NO | `'GENERATING'` | `GENERATING` / `READY` / `FAILED` |
| `pdf_attempt_count` | INT UNSIGNED | NO | 0 | 生成・保存の試行回数 |
| `pdf_failed_at` | DATETIME | YES | NULL | 直近の失敗時刻 |
| `pdf_failure_reason` | VARCHAR(500) | YES | NULL | 直近の失敗理由（エラーコード + 要約。**スタックトレースは入れない**） |

```sql
ALTER TABLE receipts
  ADD COLUMN pdf_status         VARCHAR(20)  NOT NULL DEFAULT 'GENERATING' COMMENT 'PDF生成状態（GENERATING/READY/FAILED）',
  ADD COLUMN pdf_attempt_count  INT UNSIGNED NOT NULL DEFAULT 0            COMMENT 'PDF生成・保存の試行回数',
  ADD COLUMN pdf_failed_at      DATETIME     NULL                          COMMENT '直近の失敗時刻',
  ADD COLUMN pdf_failure_reason VARCHAR(500) NULL                          COMMENT '直近の失敗理由',
  ADD INDEX idx_r_pdf_status (pdf_status, updated_at);
```

**既存行への影響**: `DEFAULT 'GENERATING'` が入る。既存の団体領収書は
`pdf_storage_key` が 1 行も書かれていないため、これは**現状の導出結果と同じ値**であり、
挙動は変わらない（後方互換）。

**状態遷移**:

```
   [発行時]  pdf_status = GENERATING, attempt_count = 0
        │
        ├─ @Async 生成 → 原本保存 成功
        │      → receipt_pdf_archives に ORIGINAL 行を INSERT
        │      → pdf_status = READY, pdf_failed_at = NULL, pdf_failure_reason = NULL
        │
        └─ 生成 or 保存 失敗
               → pdf_status = FAILED, attempt_count += 1,
                 pdf_failed_at = now, pdf_failure_reason = <ErrorCode + 要約>
               → 監査ログ PLATFORM_RECEIPT_PDF_FAILED
                      │
                      └─ 次に PDF 取得 API が呼ばれたとき、FAILED なら再試行する
                         （attempt_count が上限（5）を超えたら再試行せず RECEIPT_031 を返す。
                           無限リトライで外部ストレージを叩き続けない）
```

**`receipts` のデータ項目 UPDATE 禁止（§9.3）との関係**: 禁止対象は
`description` / `amount` / `tax_rate` / `recipient_name` 等の**証憑の内容**であり、
`pdf_status` 系は**内容ではなく生成の進行状態**である。`voided_at` と同じ扱いで
UPDATE を許可する。Service 層の UPDATE 許可列ホワイトリストに明記する。

**`ReceiptService.java`:661 は是正対象**とする。導出をやめ、`pdf_status` 列を読む。

#### `receipt_issuer_settings` — 変更なし

運営の発行者情報は**この表に PLATFORM 行を 1 行入れるだけ**で足りる（`scope_type='PLATFORM'`, `scope_id=0`）。列の追加は不要。初期行は Flyway で投入する（§7）。

`next_receipt_number` は §3.2 の採番テーブル導入後は PLATFORM 行では**使わない**（団体行は従来どおり使う）。

### 3.2 新規テーブル `receipt_number_sequences`（採番の直列化解消）

**現状の問題（実測）**: 採番は 3 箇所（`ReceiptService.java`:81-83 / :192-197 / :355-361）で `findByScopeTypeAndScopeIdForUpdate`（`PESSIMISTIC_WRITE`、`ReceiptIssuerSettingsRepository.java`:26-31）により**発行者設定行そのものをロック**して行う。団体スコープでは行がテナントごとに分かれるため実害が小さいが、**PLATFORM は全プラットフォームで 1 行**であり、月次一括発行が全件直列化する。

さらに `bulkCreateReceipts`（:183-259）は :194 でロックを取り :258 まで保持したまま、ループ内で 1 件あたり `memberPaymentRepository.findById` + `resolveUserDisplayName` + `save` の N+1 を最大 50 件回す。PLATFORM で同じ形を使うと、ロック保持時間が件数に比例する。

**反面教師**: `MonthlyInvoiceBatchService.java`:241-244 の `generateInvoiceNumber` は `adInvoiceRepository.count() + 1` で採番している。並行実行すればほぼ確実に重複し `uk_invoice_number` で INSERT が落ちる。**同じ轍を踏まない。**

| カラム名 | 型 | NULL | デフォルト | 説明 |
|---------|---|------|-----------|------|
| `id` | BINARY(16) | NO | — | PK（UUIDv7。`UuidV7Entity` 継承） |
| `scope_type` | VARCHAR(20) | NO | — | `PLATFORM` / `TEAM` / `ORGANIZATION` |
| `scope_id` | BIGINT UNSIGNED | NO | — | スコープ ID（PLATFORM は 0） |
| `period_key` | VARCHAR(8) | NO | — | 期間キー（`YYYYMM`。将来 `YYYY` も許容できる長さ） |
| `next_number` | INT UNSIGNED | NO | 1 | 次に払い出す番号 |
| `created_at` / `updated_at` | DATETIME | NO | — | |

```sql
UNIQUE KEY uq_rns_scope_period (scope_type, scope_id, period_key)
```

**採番方式（3点セット）**:
1. **発行者設定行のロックから切り離す** — 採番は本表の行だけをロックする。発行者設定の読み取りはロックなし（`findByScopeTypeAndScopeId`）。
2. **`period_key` で分割** — `YYYYMM` 単位で行が分かれるため、別月の発行が競合しない。番号も可読になる（例: `PLAT-202609-00001`）。既存 UNIQUE は `receipt_number` の**文字列**に対するものなので、フォーマットを変えても DDL 変更は不要。
3. **短命トランザクションで分離** — 「番号レンジを取る」処理を `REQUIRES_NEW` の独立トランザクションにし、レコード本体を書く親トランザクションのロック保持時間から切り離す。

```java
@Transactional(propagation = Propagation.REQUIRES_NEW)
public int reserveRange(ReceiptScopeType scopeType, Long scopeId, String periodKey, int count) {
    // 行が無ければ INSERT（uq 違反は 1 回だけリトライ）、あれば SELECT ... FOR UPDATE
    // next_number を count 進めて開始番号を返す。呼び出し元はここでコミット済み。
}
```

> **番号の欠番を許容する**: レンジ確保後に本体トランザクションがロールバックすると番号が飛ぶ。**インボイス制度・電子帳簿保存法とも連番の連続性は要件ではない**（重複禁止・改ざん禁止が要件）ため、欠番を許容する。この判断は設計書上の明示的な選択である。

**移行**: 本表の導入は PLATFORM スコープに**先行適用**し、団体スコープは `next_receipt_number` 方式のまま据え置く（後方互換。§8）。団体側の移行は別戦役とする。

### 3.3 新規テーブル `receipt_pdf_archives`（電子帳簿保存法の原本保存）— §9

| カラム名 | 型 | NULL | デフォルト | 説明 |
|---------|---|------|-----------|------|
| `id` | BINARY(16) | NO | — | PK（UUIDv7） |
| `receipt_id` | BIGINT UNSIGNED | NO | — | 対象領収書（`receipts.id`。同一ドメイン内のため FK 可） |
| `archive_kind` | VARCHAR(20) | NO | — | 原本の種別（`ORIGINAL` / `VOIDED`） |
| `storage_key` | VARCHAR(500) | NO | — | オブジェクトストレージ上のキー |
| `content_sha256` | CHAR(64) | NO | — | PDF 原本の SHA-256（改ざん検知） |
| `byte_size` | BIGINT UNSIGNED | NO | — | バイト数 |
| `archived_at` | DATETIME | NO | CURRENT_TIMESTAMP | 保存日時 |
| `retention_until` | DATE | NO | — | 保存期限（`archived_at` + 7年。§9） |
| `retention_backend` | VARCHAR(30) | NO | — | 実際に効いた不変性の担保手段（`S3_OBJECT_LOCK` / `R2_BUCKET_LOCK` / `APP_ONLY`） |
| `created_at` / `updated_at` | DATETIME | NO | — | |

```sql
UNIQUE KEY uq_rpa_receipt_kind (receipt_id, archive_kind)  -- 1 領収書につき種別ごとに 1 原本
INDEX idx_rpa_receipt (receipt_id)
INDEX idx_rpa_retention (retention_until)
CONSTRAINT fk_rpa_receipt FOREIGN KEY (receipt_id) REFERENCES receipts (id)
```

**一意制約を `(receipt_id, archive_kind)` にした理由**: 当初案の `UNIQUE (receipt_id)` は
「1 領収書 = 原本 1 つ」を意図したものだったが、**§3.4 の状態遷移と矛盾していた**。
無効化（void）時には無効化 PDF を**同じ領収書の新しいアーカイブ行として追加**するため、
`receipt_id` 単独の一意制約では void が制約違反で失敗し、AC-42 を満たせない。

電子帳簿保存法の観点でも、**元の原本を上書きせず両方を残すことが要件**である
（無効化は元データの改ざんではなく状態の追加。§3.4）。したがって
「1 領収書に複数のアーカイブ行が並ぶ」ことを前提にした制約へ改める。

- `ORIGINAL` … 発行時に生成した原本。**1 領収書に 1 つだけ。以後どんな操作でも書き換えない**
- `VOIDED` … 無効化時に生成した「無効」表示の PDF。**1 領収書に高々 1 つ**

種別を複合キーに含めることで、「原本の二重登録」と「無効化 PDF の二重登録」の
**両方を DB 制約で防ぎつつ、両者の共存を許す**。

**再発行（reissue）との整合**: 再発行は§3.4 のとおり**新しい `receipts` 行を起こす**
（元の行を書き換えない）。新しい行は自分の `receipt_id` を持つため、
自分の `ORIGINAL` 行を新規に作れる。既存の制約と衝突しない。

**`retention_backend` を列として持つ理由**: R2 と S3 で担保できる強度が違う（§9）。「どの手段で守られた原本か」を行として残さないと、監査時にどの文書が Compliance 相当で守られているか説明できない。

**UUIDv7 / テナント原則との整合**（`docs/architecture/domain_db_design_principles.md`）:
- 原則6（新規テーブルは `UuidV7Entity` 継承）に**従う**。`receipt_number_sequences` / `receipt_pdf_archives` はいずれも UUIDv7 主キー。
- 原則1（クロスドメイン FK 禁止）に**従う**。`receipt_id` は同一 receipt ドメイン内なので FK 可。`source_ref` はクロスドメインなので FK なし。
- 原則7（テナントスコープ Repository は `AbstractTenantAwareRepository` 継承）は、**PLATFORM スコープにはテナントが無い**ため該当しない。既存 receipt ドメインの Repository も `AbstractTenantAwareRepository` を使っていない（`scope_type` + `scope_id` の明示条件で絞る方式）ので、既存パターンを踏襲する。

### 3.4 状態遷移

運営領収書は F08.4 の状態機械をそのまま使う。ただし **PLATFORM では `DRAFT` を使わない**（承認者が SYSTEM_ADMIN しかおらず、承認ワークフローの意味がないため）。

```
[発行契機イベント]
      │  (入金確定: AdInvoiceEntity.markPaid / purchase.markAsPaid)
      ▼
   ISSUED ──(SYSTEM_ADMIN が void)──▶ ISSUED + voided_at ≠ NULL
      │                                        │
      │                                        │ (reissue)
      │                                        ▼
      │                                  新しい ISSUED 行
      │                                  (source_type/source_ref は同じ、
      │                                   description に元番号を記載)
      ▼
  PDF 原本を receipt_pdf_archives に保存（不可逆）
```

**無効化は原本を消さない**。`voided_at` を立て、無効化用 PDF（`pdf/receipt-voided`）を
**`archive_kind = 'VOIDED'` の新しいアーカイブ行として追加**する（§3.3）。
元の `ORIGINAL` 行は書き換えも削除もされず、保持期限まで残る。
`receipt_pdf_archives` の一意制約は `(receipt_id, archive_kind)` であり、この追加を許す。

**PDF の生成・保存状態**は `receipts.pdf_status`（`GENERATING` → `READY` / `FAILED`）で
別途追跡する（§3.1）。証憑の状態機械（`ISSUED` / `voided_at`）とは独立した軸である。

---

## 4. API設計

すべて `/api/v1/system-admin/**` 配下（運営用）と、既存の支払者向け経路に分ける。

### 4.1 運営コンソール（SYSTEM_ADMIN）

クラスに `@AuthorizedByPathConfig("/api/v1/system-admin/**")` を付与。`SecurityConfig.java`:527 のパス認可が根拠。

| メソッド | パス | 認証 | 説明 |
|---------|-----|------|------|
| GET | `/api/v1/system-admin/receipt-settings` | 必要 | PLATFORM 発行者設定の取得 |
| PUT | `/api/v1/system-admin/receipt-settings` | 必要 | PLATFORM 発行者設定の更新（登録番号取得時に使う。§11） |
| GET | `/api/v1/system-admin/receipts` | 必要 | 運営領収書の一覧（検索・ページング） |
| GET | `/api/v1/system-admin/receipts/{id}` | 必要 | 詳細 |
| GET | `/api/v1/system-admin/receipts/{id}/pdf` | 必要 | PDF 取得（原本があれば原本、無ければ生成して保存） |
| POST | `/api/v1/system-admin/receipts/{id}/void` | 必要 | 無効化 |
| POST | `/api/v1/system-admin/receipts/{id}/reissue` | 必要 | 無効化済みからの再発行 |
| POST | `/api/v1/system-admin/receipts/backfill` | 必要 | 発行漏れの補填発行（`source_type` + `source_ref` 指定。運用リカバリ用） |
| GET | `/api/v1/system-admin/receipts/export` | 必要 | CSV エクスポート（BOM 付き UTF-8。最大 10,000 件） |

#### `GET /api/v1/system-admin/receipts`

クエリパラメータ（電子帳簿保存法の検索 3 要件を満たす）:

| 名前 | 型 | 必須 | 説明 |
|---|---|---|---|
| `source_type` | string | 任意 | `AD_INVOICE` / `NOTIFICATION_CREDIT_PURCHASE` / `BILLING_INVOICE` |
| `issued_from` / `issued_to` | date | 任意 | **日付**要件 |
| `amount_min` / `amount_max` | integer | 任意 | **金額**要件 |
| `recipient_name` | string | 任意 | **取引先**要件（部分一致） |
| `include_voided` | boolean | 任意 | 既定 `false` |
| `page` / `size` | integer | 任意 | 既定 0 / 20。`size` 上限 100 |

レスポンス（`ApiResponse<PageResponse<PlatformReceiptSummaryResponse>>`）:

| フィールド | 型 | null | 説明 |
|---|---|---|---|
| `id` | number | 不可 | |
| `receiptNumber` | string | 不可 | 例 `PLAT-202609-00001` |
| `sourceType` | string | 不可 | |
| `sourceRef` | string | 不可 | 元データ ID の文字列表現（`BIGINT` 系は 10 進、UUID 系は 36 文字） |
| `recipientName` | string | 不可 | 宛名（復号済み） |
| `amount` | number | 不可 | 税込合計 |
| `taxAmount` | number | 不可 | |
| `amountExclTax` | number | 不可 | |
| `isQualifiedInvoice` | boolean | 不可 | 発行時点の登録状態のスナップショット |
| `invoiceRegistrationNumber` | string | **可** | 未登録期に発行したものは `null` |
| `issuedAt` | string(date-time) | 不可 | |
| `voidedAt` | string(date-time) | **可** | |
| `pdfStatus` | string | 不可 | `READY` / `GENERATING` / `FAILED` |

> **`recipient_name` の検索について（要注意）**: `ReceiptEntity.recipientName` は `EncryptedStringConverter` による**アプリ層暗号化**（`ReceiptEntity.java`:52-55）。DDL 上は `VARCHAR(200)` だがエンティティは `TEXT` を要求しており、DDL とエンティティが乖離している。**暗号化列に対する `LIKE` 部分一致は原理的に効かない**。F08.4 が張っている `FULLTEXT INDEX ft_r_recipient (recipient_name)` も暗号文に対する索引であり、機能しない。
> → **本設計では PLATFORM の宛名検索を「暗号化しない別列 `recipient_name_search`（正規化済み・法人名のみ）」で解くか、あるいは `source_type` + `source_ref` 経由で `advertiser_accounts.company_name` / `organizations.name` を JOIN して絞る**。**要マスター裁可**（§12-①）。

### 4.2 支払者向け（広告主）

既存の広告主向け経路（`/api/v1/advertiser/**`）に追加する。認可は既存の `advertiserAccountId` 所有チェックを踏襲する。

| メソッド | パス | 認証 | 説明 |
|---------|-----|------|------|
| GET | `/api/v1/advertiser/receipts` | 必要 | 自アカウント宛の運営領収書一覧 |
| GET | `/api/v1/advertiser/receipts/{id}/pdf` | 必要 | 領収書 PDF |
| GET | `/api/v1/advertiser/invoices/{id}/pdf` | 必要 | **請求書 PDF（既存。中身を PDF 基盤へ置換）** |

他人の `advertiserAccountId` に紐づく領収書 ID を渡した場合は **404**（存在オラクルを消す。`docs/security` の PARKING_020 流儀に合わせる）。既存 `InvoicePdfService.java`:50-52 は 403（`COMMON_002`）を返しており不一致であるため、**本戦役で 404 に揃える**。

### 4.3 支払者向け（組織 ADMIN・通知クレジット）

| メソッド | パス | 認証 | 説明 |
|---------|-----|------|------|
| GET | `/api/v1/organizations/{orgId}/receipts` | 必要 | 自組織宛の運営領収書一覧（ADMIN 以上） |
| GET | `/api/v1/organizations/{orgId}/receipts/{id}/pdf` | 必要 | PDF |

`orgId` は既存の `checkAdminOrAbove(userId, orgId, "ORGANIZATION")` で守る。領収書行の `source_type='NOTIFICATION_CREDIT_PURCHASE'` かつ `source_ref` が指す購入行の `organization_id = orgId` を照合し、不一致は 404。

> **`/api/v1/my/receipts` は使わない**。通知クレジットは**組織に対する売上**であり、購入操作をした個人宛ではない。個人の `recipient_user_id` で引く既存 `my` 経路に混ぜると、退職した購入者の領収書が組織から見えなくなる。

---

## 5. ビジネスロジック

### 5.1 PDF 生成の一本化（裁可2）

**現状の `InvoicePdfService` は適格請求書として成立していない**（実測・全文確認済み）:

| 欠陥 | 実コード |
|---|---|
| フォントが Helvetica 固定 → **日本語が 1 文字も出ない** | `InvoicePdfService.java`:76-80 |
| 宛名（誰への請求か）が無い | 出力に `advertiser_accounts.company_name` を一切載せていない |
| 適格請求書発行事業者の登録番号が無い | 同上 |
| 通貨書式が `Locale.JAPAN`（`￥`）だが Helvetica では描画不能 | :44, :214 |
| キャンペーン名が日本語だと空白になる | :129 `truncate(item.getCampaignName(), 40)`（`null` なら NPE も） |

**是正方針**: `InvoicePdfService` の OpenPDF 直書きを廃し、`PdfGeneratorService.generateFromTemplate` に寄せる。テンプレートのみ分ける。

```
pdf/receipt          … 領収書（既存・団体と運営で共用）
pdf/receipt-voided   … 無効化領収書（既存）
pdf/platform-invoice … 請求書（新規。宛名・登録番号・税率別内訳を持つ）
```

`ReceiptPdfGeneratorImpl.java`:24,30 は `PdfGeneratorService` への薄いブリッジであり、発行者名・住所・登録番号は `ReceiptEntity` のスナップショット列から出る。**運営名義は `receipt_issuer_settings` に PLATFORM 行を 1 行入れるだけで出る**ため、生成側の改修は不要。

> **前提整備（本戦役の必須先行タスク。未実装として正直に記載する）**
>
> 1. **日本語フォントの実体がリポジトリに無い。** `PdfFontConfig.java`:23-24 が `fonts/NotoSansJP-Regular.ttf` / `NotoSerifJP-Regular.ttf` を登録しようとするが、`backend/src/main/resources/fonts/` には `.gitkeep` しか存在しない。`registerFont(..., optional=true)` のため**例外にならず黙ってスキップ**され、`registeredFonts` は空になる。結果、**Flying Saucer 経由の PDF はすべて日本語が出ない**。`InvoicePdfService` だけの問題ではなく、既存の領収書 PDF も同じ穴を抱えている。
> 2. **`pdf/receipt` テンプレートが現行エンティティと合っていない。** `templates/pdf/receipt.html`:19 が `receipt.issuedDate`、:30 が `receipt.totalAmount` を参照しているが、`ReceiptEntity` にあるのは `issuedAt` / `amount` である（`getIssuedDate` / `getTotalAmount` は存在しない）。**現状 `pdf/receipt` の生成は Thymeleaf の評価例外で必ず失敗する。**
> 3. **`pdf_storage_key` は書かれたことがない。** `ReceiptEntity.updatePdfStorageKey()`（:172）の呼び出し元は**0 件**。そのため `ReceiptService.java`:661 の `pdfStatus` は**常に `GENERATING`** を返す。
> 4. **設計書と実装が乖離している。** F08.4 §5「PDF 生成の技術選定」は「OpenPDF の Java API で直接レイアウト構築。Thymeleaf は不採用」と書いているが、実装は Thymeleaf + Flying Saucer である。F08.4 の当該節は実装に合わせて是正すべきである（本書のスコープ外。別 PR）。
>
> **これらは本戦役の受け入れ条件に含める**（AC-11〜AC-14）。ここを直さないと運営領収書 PDF も同じく落ちる。

### 5.2 発行契機（単一地点に寄せる）

| 系統 | 契機 | 実装方針 |
|---|---|---|
| 広告費 | `AdInvoiceEntity.markPaid()` — Webhook（`StripeAdInvoiceWebhookController.java`:114）と手動（`AdInvoiceService.java`:84）の**唯一の合流点**（`markPaid` の呼び出し元は実測でこの 2 箇所のみ） | `AdInvoicePaidEvent` を `AdInvoiceService` から `@TransactionalEventListener(phase = AFTER_COMMIT)` で発行し、receipt ドメインのリスナーが領収書を作る |
| 通知クレジット | `NotificationCreditCheckoutService.handlePurchaseCompleted()`:190 の `purchase.markAsPaid`。経路 1 本・冪等ガード（:183-186 の PAID スキップ）あり | 同上。`NotificationCreditPurchasePaidEvent` |
| サブスク | `BillingSubscriptionWebhookService` の `invoice.paid` | **第2段。§10** |

**イベントはドメイン境界を越えるため ID 参照のみを載せる**（原則1・5）。リスナー側は `source_type` + `source_ref` で元データを引き直す。

**トランザクション境界**: リスナーは `AFTER_COMMIT` + `@Transactional(REQUIRES_NEW)`。**領収書の発行失敗が入金確定をロールバックしてはならない**。失敗時は WARN ログ + 監査ログ（`PLATFORM_RECEIPT_ISSUE_FAILED`）を残し、`POST /api/v1/system-admin/receipts/backfill` で手動リカバリする。握りつぶしではなく、**未発行として観測可能な状態にする**。

### 5.3 消費税の扱い

| 系統 | 現状 | 領収書での扱い |
|---|---|---|
| 広告費 | **外税**。`total_amount`（税抜）+ `tax_amount` = `total_with_tax`。`tax_rate DECIMAL(5,2) DEFAULT 10.00` | そのまま転記。`amount = total_with_tax`, `amount_excl_tax = total_amount`, `tax_amount = tax_amount` |
| 通知クレジット | **税額の列が無い**（`V9.118` に tax 列 0 個。実測） | **裁可4により `price_jpy` を税込として扱い、内税で逆算**する。`tax_amount = price_jpy × 10 ÷ 110`、`amount_excl_tax = price_jpy − tax_amount`。**既存購入者の支払額は変えない** |
| サブスク | `tax_behavior IN ('INCLUSIVE','EXCLUSIVE')` / `tax_rate_basis_points` / `tax_master_snapshot JSON`（`V196.20260831142049`:145-150）。**税率別内訳を出せる唯一の系統** | 第2段 |

**内税逆算の丸め**: `RoundingMode.HALF_UP`。既存 `bulkCreateReceipts`（`ReceiptService.java`:213）が `amount × 10 ÷ 110` を `HALF_UP` で計算しており、これに揃える。

#### 広告費の丸め不一致（構造的な 1 円ズレ。本戦役で解く）

| 箇所 | 丸め |
|---|---|
| 自社 DB の税額計算 `MonthlyInvoiceBatchService.java`:180 | `RoundingMode.FLOOR` |
| Stripe へ送る金額 同 :217 | `RoundingMode.HALF_UP` |

同一請求内で丸めが食い違うため、**Stripe から実際に引き落とされた額と `ad_invoices.total_with_tax` が 1 円ズレうる**。領収書は「実際に受け取った金額」を書く文書なので、ここがズレると領収書が事実と異なる。

**方針**:
1. **丸めを `HALF_UP` に統一**する（`MonthlyInvoiceBatchService.java`:180 を修正）。日本の消費税の実務慣行としても切り捨て・四捨五入いずれも可だが、**2 箇所で違うことが問題**。
2. その上で、**領収書の金額は「請求書の額」ではなく「実際の入金額」を正とする**。Stripe 経路では webhook の `amount_paid` を採用し、`ad_invoices.total_with_tax` と差があれば領収書は入金額で発行しつつ差額を監査ログに残す。
3. 既発行の `ad_invoices` に遡及修正は**しない**（過去の請求書を書き換えることこそ改ざんであるため）。

### 5.4 発行者情報のスナップショット

`receipts` の `issuer_name` / `issuer_address` / `invoice_registration_number` / `is_qualified_invoice` は**発行時点のスナップショット列**（`ReceiptEntity.java`:66-84、DDL `V8.023`:12-18）。運営の登録番号を後から設定しても、**既発行文書は遡って書き換わらない**。§11 の移行はこの性質だけで成立する。

---

## 6. エラーコード

`ReceiptErrorCode` の現在の最大は **`RECEIPT_024`**（`QUEUE_NOT_PENDING`。実測。家老報告の「023 が最大」は誤り）。新規は `RECEIPT_025` から予約する。

> **採番の注記**: 以下は「現在の最大＋1 で**予約**」した番号である。並行作業と衝突しうるため、**確定はマージ時に `ReceiptErrorCode` を再確認して行う**。

| コード | 定数名 | メッセージ | HTTP | Severity |
|---|---|---|---|---|
| `RECEIPT_025` | `PLATFORM_ISSUER_NOT_CONFIGURED` | 運営の発行者設定が未登録です | 409 CONFLICT | WARN |
| `RECEIPT_026` | `SOURCE_ALREADY_RECEIPTED` | この取引には既に領収書が発行されています | 409 CONFLICT | WARN |
| `RECEIPT_027` | `SOURCE_NOT_PAID` | 入金が確定していない取引には領収書を発行できません | 409 CONFLICT | WARN |
| `RECEIPT_028` | `UNSUPPORTED_SOURCE_TYPE` | この取引種別の領収書発行は未対応です | 400 BAD_REQUEST | WARN |
| `RECEIPT_029` | `PLATFORM_SCOPE_FORBIDDEN` | 運営スコープの操作権限がありません | 403 FORBIDDEN | WARN |
| `RECEIPT_030` | `ARCHIVE_NOT_FOUND` | 領収書の原本が見つかりません | 404 NOT_FOUND | ERROR |
| `RECEIPT_031` | `ARCHIVE_WRITE_FAILED` | 領収書原本の保存に失敗しました | 500 INTERNAL_SERVER_ERROR | ERROR |
| `RECEIPT_032` | `ARCHIVE_INTEGRITY_MISMATCH` | 領収書原本のハッシュが一致しません（改ざんの疑い） | 500 INTERNAL_SERVER_ERROR | ERROR |

### 6.1 既知の欠陥 — status マップへの未登録

`GlobalExceptionHandler` の status マップに登録されている `RECEIPT_*` は **001 / 002 / 003 / 004 / 005 / 008 / 009 / 019 / 021 / 024 の 10 件のみ**（実測。:1560-1572, :2434）。**`RECEIPT_020`（`LOGO_UPLOAD_FAILED`）を含む 14 件が未登録で既定ステータスに落ちている。**

**本設計の必須事項**: 新規に足す `RECEIPT_025`〜`RECEIPT_032` は、**上表の HTTP ステータスを `GlobalExceptionHandler` の status マップに登録するところまでを 1 つの作業単位とする**。「enum に足しただけ」で終わらせない。

> 既存未登録 14 件（006/007/010〜018/020/022/023）の是正は**本書のスコープ外**とし、別戦役の課題として `docs/task-list.md` に起票する（本 PR では台帳に触らない）。

---

## 7. Flywayマイグレーション

**採番規約**（`backend/.claudecode.md` §18）: `V{origin/main 全体の最大 major + 1}.{yyyyMMddHHmmss}__{説明}.sql`。minor はタイムスタンプ必須（連番禁止。`FlywayTimestampNamingGuardTest` が拒否）。

2026-09-02 時点の本 worktree（origin/main ベース）での最大 major は **198**（実測）。したがって本戦役は **major = 199** を予約する。

> **major はマージ時に再確認して確定する。** 並行セッションが 199 を先に取っている可能性があるため、着地直前に `origin/main` を取り直して最大 major を数え直す。

```
V199.{yyyyMMddHHmmss}__add_source_ref_to_receipts.sql
    -- receipts に source_type / source_ref + idx_r_source を追加

V199.{yyyyMMddHHmmss}__add_pdf_status_to_receipts.sql
    -- receipts に pdf_status / pdf_attempt_count / pdf_failed_at /
    --   pdf_failure_reason + idx_r_pdf_status を追加（§3.1）
    -- pdf_status は DEFAULT 'GENERATING'。既存行は現状の導出結果と同値のため挙動不変

V199.{yyyyMMddHHmmss}__create_receipt_number_sequences.sql
    -- 採番テーブル（UUIDv7 主キー）

V199.{yyyyMMddHHmmss}__create_receipt_pdf_archives.sql
    -- 原本保存テーブル（UUIDv7 主キー）

V199.{yyyyMMddHHmmss}__seed_platform_receipt_issuer.sql
    -- receipt_issuer_settings に PLATFORM 行を 1 行投入
    -- scope_type='PLATFORM', scope_id=0, is_qualified_invoicer=FALSE,
    -- invoice_registration_number=NULL（§11）
    -- INSERT ... ON DUPLICATE KEY UPDATE id=id で冪等にする
```

**マイグレーション上の注意点**:
- `receipts` への列追加のうち `source_type` / `source_ref` / `pdf_failed_at` / `pdf_failure_reason` は **NULL 許容の ADD COLUMN** であり、既存行への影響は無い。`pdf_status` / `pdf_attempt_count` は `NOT NULL DEFAULT` 付きだが、既定値が**現状の導出結果と同値**（既存行は `pdf_storage_key` が全件 NULL のため `GENERATING`）なので挙動は変わらない。既存の団体領収書は `source_type` / `source_ref` とも `NULL` のまま残る（`MEMBER_PAYMENT` への埋め戻しは行わない。行数が多く、`member_payment_id` から一意に導出できるため不要）。
- `scope_type` は `VARCHAR(20)` なので `PLATFORM` を入れるための DDL 変更は不要（§3.0）。
- **`test` プロファイルは Entity 由来の DDL を使い Flyway シードが走らない**（既知の罠）。したがって PLATFORM 発行者行のシードに依存する統合テストは、**テスト側でフィクスチャとして自分で 1 行入れる**こと。Flyway シードに依存したテストは CI で偽陰性になる。

---

## 8. 非機能要件

### 8.1 性能

| 項目 | 見積り | 方針 |
|---|---|---|
| 広告費の月次発行件数 | 広告主アカウント数 × 1 通/月。初期は 3 桁想定 | 月次バッチの後段で 1 件ずつイベント駆動。一括ロックを取らない |
| 通知クレジット | 購入のたびに 1 通。散発 | イベント駆動で 1 件ずつ |
| 採番の直列化 | `period_key = YYYYMM` で月ごとに 1 行。**同一月内の同時発行は依然として直列**だが、ロック区間は「1 行の UPDATE」のみで PDF 生成を含まない（数ミリ秒） | §3.2。100 件/分程度までは問題にならない。これを超える場合はレンジ確保（`reserveRange(count)`）でまとめ取りする |
| 一覧 API の N+1 | 現行 `bulkCreateReceipts` は 1 件あたり SELECT 2 + INSERT 1 の N+1（実測） | PLATFORM の一覧・発行では `source_ref` の `IN` 句で一括取得する。**PLATFORM 経路で `bulkCreateReceipts` を再利用しない** |
| PDF 生成 | 1 件あたり Thymeleaf + Flying Saucer。数百 ms 〜 | 発行と同期しない。`@Async` で生成 → 原本保存 → `pdf_storage_key` を UPDATE |

### 8.2 移行・後方互換

- `ReceiptScopeType` への定数追加は**既存の switch / if を壊さない**ことを確認する必要がある。`ReceiptScopeType` を `switch` している箇所が網羅的でないと、default 節に落ちて静かに誤動作しうる。**出陣時に全 `switch (…ScopeType)` を洗う**こと。
- 既存の団体領収書は `next_receipt_number` 方式のまま。**採番テーブルは PLATFORM のみ使う**（§3.2）。
- 既存 API・レスポンス形状は変更しない。`source_type` / `source_ref` はレスポンスに**追加**するのみ（`OpenAPI` の手書き契約型は optional で追随させる）。

### 8.3 段階拡張として残す範囲

- 団体スコープの採番テーブル移行
- 既存未登録 `RECEIPT_*` 14 件の status マップ整備
- 会計ソフト連携（freee / MF）
- 運営の支出側証憑の受領・保存

---

## 9. 電子帳簿保存法の原本保存基盤（裁可3）

### 9.1 現状 — 規定はあるがコードが 1 行も無い

F08.4 §5「電子帳簿保存法への対応」および §6 は「S3 Object Lock（Compliance モード、Retention 7年）」「専用バケット `mannschaft-receipts`」を規定している。**実装は存在しない**（実測）:

- `pdf_storage_key` 列と `ReceiptEntity.updatePdfStorageKey()` は**定義だけで呼び出し 0 件**
- PDF はリクエストのたびにオンデマンド生成しており、原本という概念が無い
- `mannschaft-receipts` バケットの定義も無い（`StorageProperties` はバケット 1 つ）

したがって「7 年保存・改ざん防止」は**紙の上だけの規定**である。本戦役で団体側・運営側**共通の保存基盤**として実装する。

### 9.2 重大な検証結果 — Cloudflare R2 は S3 Object Lock に対応していない

現在の既定ストレージ実装は **`R2StorageService`（Cloudflare R2）** である。`S3StorageService` は `@ConditionalOnProperty(prefix="storage", name="provider", havingValue="s3")` が付いており、`R2StorageService` は**無条件 `@Service`**（実測）。`application.yml`:196-202 も R2 の設定のみで、`storage.provider` の記述が無い。**つまり本番は R2 で動く。**

Cloudflare 公式ドキュメントを確認した結果:

| 機能 | R2 の対応 |
|---|---|
| `PutObjectLockConfiguration` / `GetObjectLockConfiguration` | **未実装** |
| `PutObjectRetention` / `PutObjectLegalHold` | **未実装** |
| `x-amz-object-lock-mode`（COMPLIANCE / GOVERNANCE） | **未実装** |
| バケットのバージョニング（`PutBucketVersioning`） | **未実装** |
| **Bucket Lock**（Cloudflare 独自） | **対応**。プレフィックス単位の保持ルール（期間指定・日付指定・無期限）。複数ルールが重なる場合は最も長い保持が優先。ルールが 1 つでもあるとバケットを空にできない |

**つまり F08.4 が規定する「S3 Object Lock Compliance モード」は R2 上では実現できない。** これは設計書に書かれていた前提の誤りであり、本書で是正する。

出典:
- https://developers.cloudflare.com/r2/api/s3/api/
- https://developers.cloudflare.com/r2/buckets/bucket-locks/

### 9.3 設計 — 3 層の不変性担保

`StorageService` インターフェースに保存操作を足すのではなく、**保持ポリシーの適用は実装ごとに違う**ため、専用の抽象を切る。

```java
public interface ImmutableArchiveService {
    /** 原本を書き込み、保持期限を適用する。既存キーへの上書きは拒否する。 */
    ArchiveResult archive(String key, byte[] content, String contentType, LocalDate retentionUntil);
    byte[] read(String key);
    /** どの手段で不変性を担保したか（receipt_pdf_archives.retention_backend に入る） */
    RetentionBackend backend();
}
```

| 実装 | 環境 | 担保手段 | 強度 |
|---|---|---|---|
| `S3ObjectLockArchiveService` | AWS S3 を使う場合（`storage.provider=s3`） | `PutObject` に `ObjectLockMode=COMPLIANCE` + `ObjectLockRetainUntilDate` | **最強**。ルート権限でも期限内は削除不可 |
| `R2BucketLockArchiveService` | **現行の本番（R2）** | バケットの `receipts/` プレフィックスに Bucket Lock（7年）を Terraform / Wrangler で事前設定。アプリは「上書きしない」規律を守る | **中**。プレフィックス単位の保持。個別オブジェクトの retain-until は持てない |
| `LocalArchiveService` | ローカル開発（MinIO） | 保持ルールは適用しない。**代わりに `content_sha256` の一致検証を必須にする** | **弱**（開発用） |

**3 層で守る**:
1. **ストレージ層** — 上記の保持ルール
2. **キー設計で上書きを起こさない** — `receipts/{scope_type}/{yyyy}/{receipt_id}/{sha256の先頭16桁}.pdf`。内容が変われば必ず別キーになるため、同一キーへの上書きが原理的に発生しない
3. **アプリ層のハッシュ検証** — 読み出しのたびに `content_sha256` を照合し、不一致なら `RECEIPT_032`（改ざんの疑い）で拒否 + 監査ログ。**R2 の担保が S3 より弱い分をここで埋める**

さらに、`receipts` の `description` / `amount` / `tax_rate` / `recipient_name` 等の
**証憑の内容にあたるデータ項目は発行後 UPDATE 禁止**（Service 層でホワイトリスト制約）。
UPDATE を許可するのは `voided_at` / `voided_by` / `voided_reason` と
`pdf_status` / `pdf_attempt_count` / `pdf_failed_at` / `pdf_failure_reason` /
`pdf_storage_key` に限る。無効化も PDF 状態も**状態の追加であり内容の改ざんではない**。

**アーカイブ行自体は不変**とする。`receipt_pdf_archives` の行は INSERT のみで、
`storage_key` / `content_sha256` の UPDATE と行の DELETE を Service 層で禁止する。
生成の失敗はこの表に行を作らず、`receipts.pdf_status = FAILED` として記録する（§3.1）。

### 9.4 ローカル開発（MinIO）

`docker-compose.yml` の `minio` サービス（profile: `storage`、バケット `mannschaft-storage`）を使う。

- MinIO は Object Lock に対応するが、**バケット作成時に有効化する必要がある**ため、既存バケットには後付けできない。`minio-init` に Object Lock 有効の**別バケット `mannschaft-receipts`** を作る手順を足す。
- ただしローカルでの Object Lock 有効化は開発者を詰まらせる（オブジェクトが消せなくなる）ため、**既定は `LocalArchiveService`（保持なし + ハッシュ検証のみ）** とし、Object Lock を試したい場合だけ環境変数で切り替える。
- 選択は `mannschaft.storage.archive.backend` プロパティ（`s3-object-lock` / `r2-bucket-lock` / `local`。既定 `local`）で行う。

### 9.5 保持期限

- **7 年**（法人税法の帳簿書類保存期間。欠損金がある事業年度は 10 年になりうる）
- `retention_until = archived_at + 7年`
- **7 年経過後の自動削除は本戦役では実装しない**。10 年保存が必要になる年度があり、機械的な削除は取り返しがつかない。期限到来分を一覧する API のみ用意し、削除は運用判断とする。**要マスター裁可**（§12-②）

---

## 10. 段階リリースの方針

**広告費・通知クレジットを第1段、サブスクを第2段とする 2 段構えを採用する。**

**理由**:
- サブスク系（F20.1 課金センター）は `V196.20260831142049__expand_billing_center.sql` が `billing_invoices`(:494) / `billing_invoice_adjustments`(:536) / `billing_invoice_lines`(:568) を CREATE しているが、**対応する Java エンティティが 1 つも存在しない**（`billing_invoices` を参照する Java コードは 0 件。実測）。`BillingSubscriptionWebhookService` の `invoice.paid` ハンドラは請求書行を 1 件も作っていない。
- **書かれていない表から領収書は作れない。** ここに手を出すと本戦役が「課金センターの投影実装」を含む別の戦役になり、範囲が崩壊する。
- 一方で広告費・通知クレジットは**発行契機が単一地点に収束しており**（§5.2）、器（receipt ドメイン）を共有する形が素直に成立する。

**第2段の前提条件**（満たされたら着手）:
1. `billing_invoices` / `billing_invoice_lines` の Java エンティティと投影処理が実装されていること
2. `tax_behavior` / `tax_rate_basis_points` / `tax_master_snapshot` が実際に書き込まれていること（**税率別内訳を出せる唯一の系統**であり、複数税率の領収書はここでしか作れない）
3. `billing_name_snapshot` 等の宛名列が書き込まれていること

第2段では `receipt_line_items`（F08.4 の複数税率対応）を PLATFORM でも使う。第1段は単一税率（10%）で足りるため使わない。

### 返金・取消の扱い

| 系統 | 現状 | 第1段での扱い |
|---|---|---|
| 広告費 | **返金経路が無い。** `InvoiceStatus` は `DRAFT / ISSUED / PAID / OVERDUE` の 4 値のみで `VOID` / `CANCELLED` が無い（実測） | 領収書の `void` + 再発行のみ提供。返金そのものは未実装であり、**実装したふりをしない** |
| 通知クレジット | enum に `REFUNDED` はあるが**遷移コードが 0 件** | 同上 |
| サブスク | `billing_invoice_adjustments` が `REFUND` / `CREDIT_NOTE` / `DISPUTE` を持つ（設計済み・未実装） | 第2段 |

**返金時の適格返還請求書（返還インボイス）は本戦役では未対応**とする。返金経路自体が存在しないため。これは正直に未実装として記載する。

---

## 11. 運営の登録番号未取得からの移行

運営はまだ適格請求書発行事業者の登録番号（T + 13桁）を取得していない可能性がある。**取得を待たずに機能を出せる**設計とする。

1. **開始時**: `receipt_issuer_settings` の PLATFORM 行を `is_qualified_invoicer = FALSE`、`invoice_registration_number = NULL` で作る（§7 のシード）。
   - PDF には登録番号を印字せず、「適格請求書発行事業者以外の者からの領収書」である旨を明記する（F08.4 §1 の非登録事業者対応と同じ扱い）。
2. **取得後**: `PUT /api/v1/system-admin/receipt-settings` で `is_qualified_invoicer = TRUE` + `invoice_registration_number` を設定するだけ。**コード変更もマイグレーションも不要。**
3. **遡及しない**: `receipts.is_qualified_invoice` / `invoice_registration_number` は**発行時点のスナップショット列**（§5.4）。取得前に発行済みの文書は `FALSE` / `NULL` のまま残り、**遡って適格請求書に化けない**。これは電子帳簿保存法の改ざん防止要件から見ても正しい挙動である。
4. **バリデーション**: `is_qualified_invoicer = TRUE` にするとき `invoice_registration_number` が `/^T\d{13}$/` に一致しなければ `RECEIPT_006` / `RECEIPT_007`（既存コードを再利用）。

---

## 12. 受け入れ条件

**検証層**: BE=BE統合テスト、E2E=FE E2E、UT=FE unit。

### 器・スコープ

| # | 誰が | 何をしたら | どうなる | 層 |
|---|---|---|---|---|
| AC-01 | SYSTEM_ADMIN | `GET /api/v1/system-admin/receipt-settings` を呼ぶ | 200。PLATFORM の発行者設定が返る（`scopeType='PLATFORM'`, `scopeId=0`） | BE |
| AC-02 | 団体 ADMIN（非 SYSTEM_ADMIN） | `GET /api/v1/system-admin/receipts` を呼ぶ | **403**。500 にならない（§2.1） | BE |
| AC-03 | 未認証 | 同上 | 401 | BE |
| AC-04 | 開発者 | `receipts` に `scope_type='PLATFORM', scope_id=0` の行を 2 件 INSERT する | `receipt_number` が異なれば両方成功する（`uq_r_receipt_number` は 3 列複合） | BE |
| AC-05 | 開発者 | `receipt_issuer_settings` に PLATFORM 行を 2 件目 INSERT する | `uq_ris_scope` 違反で失敗する | BE |
| AC-06 | 開発者 | `PLATFORM` スコープで `scope_id ≠ 0` の領収書を作る | 拒否される（アプリ層ガード） | BE |

### 発行（広告費）

| # | 誰が | 何をしたら | どうなる | 層 |
|---|---|---|---|---|
| AC-07 | Stripe | `invoice.paid` webhook で `AdInvoiceEntity.markPaid` が走る | AFTER_COMMIT で運営領収書が 1 通 ISSUED で作られる。`sourceType='AD_INVOICE'`, `sourceRef='<請求書ID>'` | BE |
| AC-08 | SYSTEM_ADMIN | 同じ請求書を手動で `markPaid`（既に PAID） | 領収書は**増えない**（`markPaid` が `IllegalStateException`、または重複ガードで `RECEIPT_026`） | BE |
| AC-09 | — | 領収書の金額 | `amount = ad_invoices.total_with_tax`, `amountExclTax = total_amount`, `taxAmount = tax_amount` が一致する | BE |
| AC-10 | — | 丸めの一致 | `MonthlyInvoiceBatchService` の自社 DB 計算と Stripe 送信額が**同じ丸め（HALF_UP）**で一致する。1 円ズレが出ない（§5.3） | BE |

### PDF 基盤の是正

| # | 誰が | 何をしたら | どうなる | 層 |
|---|---|---|---|---|
| AC-11 | 開発者 | `PdfFontConfig.getRegisteredFonts()` を呼ぶ | **空でない**。NotoSansJP / NotoSerifJP が実際にクラスパスに存在する（現状は `.gitkeep` のみで空） | BE |
| AC-12 | SYSTEM_ADMIN | 運営領収書 PDF を取得する | 200 で PDF が返り、**PDF 内のテキスト抽出に日本語（宛名・但し書き）が含まれる**。空文字にならない | BE |
| AC-13 | 団体 ADMIN | 既存の団体領収書 PDF を取得する | 200 で返る（現状は `receipt.issuedDate` / `receipt.totalAmount` の評価例外で失敗する。§5.1） | BE |
| AC-14 | 広告主 | `GET /api/v1/advertiser/invoices/{id}/pdf` | PDF に**宛名（`company_name`）と登録番号（登録済みなら）と税率別内訳**が含まれる。日本語のキャンペーン名が読める | BE |
| AC-15 | 開発者 | `campaign_name` が `null` の明細を含む請求書の PDF を生成する | NPE にならない（現状 `truncate` は null で落ちる） | BE |

### 採番

| # | 誰が | 何をしたら | どうなる | 層 |
|---|---|---|---|---|
| AC-16 | 並行 20 スレッド | 同一 `period_key` で同時に採番する | **重複した `receipt_number` が 1 件も出ない**。DB の UNIQUE で落ちるのでもなく、正しく直列化される | BE |
| AC-17 | — | 月をまたぐ | `period_key` が変わり、番号が `00001` から再開する。既存の別月の番号と衝突しない | BE |
| AC-18 | — | 本体トランザクションがロールバックする | 番号は**欠番になる**（許容）。次の発行は次の番号を取る。同じ番号が再利用されない | BE |
| AC-19 | — | 採番中に発行者設定を更新する | **ブロックされない**（発行者設定行のロックから切り離されている） | BE |

### 消費税・境界値

| # | 誰が | 何をしたら | どうなる | 層 |
|---|---|---|---|---|
| AC-20 | — | 通知クレジット `price_jpy = 11000` の領収書 | `taxAmount = 1000`, `amountExclTax = 10000`（内税 10%、HALF_UP） | BE |
| AC-21 | — | `price_jpy = 1`（最小） | `taxAmount = 0`, `amountExclTax = 1`。負値にならない | BE |
| AC-22 | — | `price_jpy = 0` | 領収書を発行しない（`RECEIPT_027` または発行スキップ）。0 円の領収書を作らない | BE |
| AC-23 | — | `price_jpy` が `DECIMAL(12,0)` の上限付近 | オーバーフローせず `receipts.amount DECIMAL(10,0)` の桁溢れを検知して明示エラー（**桁数が合っていないことに注意**） | BE |

### 認可・IDOR

| # | 誰が | 何をしたら | どうなる | 層 |
|---|---|---|---|---|
| AC-24 | 広告主 A | 広告主 B の領収書 ID で `GET /api/v1/advertiser/receipts/{id}/pdf` | **404**（403 ではない。存在オラクルを消す） | BE |
| AC-25 | 組織 X の ADMIN | 組織 Y 宛の領収書 ID で PDF 取得 | **404** | BE |
| AC-26 | 組織 X の MEMBER | 自組織の領収書一覧 | **403**（ADMIN 以上のみ） | BE |
| AC-27 | 団体 ADMIN | PLATFORM スコープの領収書 ID を団体側 API（`/api/v1/admin/receipts/{id}`）に渡す | **404**（`scope_type` が一致しないため findReceiptOrThrow で弾かれる） | BE |
| AC-28 | 広告主 | 他人の領収書 ID で `void` を試みる | 404 または 403。**void できない**（void は SYSTEM_ADMIN のみ） | BE |

### 途中失敗

| # | 誰が | 何をしたら | どうなる | 層 |
|---|---|---|---|---|
| AC-29 | — | 領収書発行リスナーが例外を投げる | **入金確定（`markPaid`）はロールバックしない**。領収書だけ未発行になり、監査ログに `PLATFORM_RECEIPT_ISSUE_FAILED` が残る | BE |
| AC-30 | SYSTEM_ADMIN | 未発行の取引に `POST /system-admin/receipts/backfill` | 領収書が発行される。二重呼び出しは `RECEIPT_026` | BE |
| AC-31 | — | 原本保存（S3/R2 書き込み）が失敗する | 領収書レコードは残る。**`receipts.pdf_status = 'FAILED'` が永続化され**、`pdf_failed_at` / `pdf_failure_reason` / `pdf_attempt_count=1` が入る。`receipt_pdf_archives` には行が作られない。次回 PDF 取得で再試行される。**握りつぶさない** | BE |
| AC-32 | — | PDF 生成が失敗する | 発行トランザクションはコミット済み。領収書は存在し `pdf_status='FAILED'`。**API の `pdfStatus` が導出ではなく `pdf_status` 列を返す**（現状は常に `GENERATING`） | BE |
| AC-33 | — | 採番だけ成功して本体保存が失敗 | 領収書行が中途半端に残らない（本体は 1 トランザクション） | BE |

### 空・0件・null

| # | 誰が | 何をしたら | どうなる | 層 |
|---|---|---|---|---|
| AC-34 | SYSTEM_ADMIN | 領収書が 0 件の状態で一覧を呼ぶ | 200 + 空配列（404 にしない） | BE / E2E |
| AC-35 | 広告主 | 領収書が 0 件 | 200 + 空配列 + FE に「まだ領収書はありません」の空状態が出る | E2E |
| AC-36 | — | `invoiceRegistrationNumber` が `null`（登録前発行） | レスポンスに `null` が入り、PDF に登録番号欄が出ない。FE が落ちない | BE / UT |
| AC-37 | — | PLATFORM 発行者設定が未作成の状態で発行契機が起きる | `RECEIPT_025`（409）。**領収書を空の発行者名で作らない** | BE |

### 原本保存（電子帳簿保存法）

| # | 誰が | 何をしたら | どうなる | 層 |
|---|---|---|---|---|
| AC-38 | — | 領収書を発行する | `receipt_pdf_archives` に `archive_kind='ORIGINAL'` の行が 1 件作られ、`content_sha256` と `retention_until`(=+7年) と `retention_backend` が入る。`receipts.pdf_status='READY'` になる | BE |
| AC-39 | — | 同じ領収書を再度 PDF 取得する | **再生成せず原本を返す**（`pdf_storage_key` が使われる。現状は呼び出し 0 件で常に再生成） | BE |
| AC-40 | 攻撃者 | ストレージ上の PDF を差し替える | 読み出し時にハッシュ不一致を検知し `RECEIPT_032` + 監査ログ。差し替えられた PDF を返さない | BE |
| AC-41 | — | 発行済み領収書の `amount` を UPDATE しようとする | Service 層で拒否される | BE |
| AC-42 | SYSTEM_ADMIN | 領収書を void する | 元の `ORIGINAL` 行は削除も更新もされず、`archive_kind='VOIDED'` の行が**追加**される（同一 `receipt_id` で 2 行並ぶ）。**一意制約違反にならない** | BE |
| AC-43 | SYSTEM_ADMIN | 日付・金額・取引先で検索する | 電子帳簿保存法の検索 3 要件が満たされる（§4.1 の注記の解決策に依存） | BE |

### 性能

| # | 誰が | 何をしたら | どうなる | 層 |
|---|---|---|---|---|
| AC-44 | SYSTEM_ADMIN | 領収書 100 件の一覧を取得 | 発行 SQL 本数が件数に比例しない（N+1 なし）。SQL カウントガードで検証 | BE |
| AC-45 | 月次バッチ | 広告主 500 件分の入金が同時に確定 | 採番ロックの保持時間が件数に比例しない（§3.2） | BE |
| AC-46 | — | 一覧の `source_type` フィルタ | `idx_r_source (source_type, source_ref)` が使われる（EXPLAIN で確認） | BE |
| AC-50 | SYSTEM_ADMIN | 同じ領収書を 2 回 void する | 2 回目は `RECEIPT_008`（既に無効化済み）。`VOIDED` アーカイブ行は 1 件のまま増えない | BE |
| AC-51 | — | `pdf_attempt_count` が上限（5）に達した領収書の PDF を取得する | 再試行せず `RECEIPT_031` を返す。**無限リトライで外部ストレージを叩き続けない** | BE |
| AC-52 | — | 第2段で `BILLING_INVOICE`（UUIDv7 主キー）の領収書を作る | `source_ref` に 36 文字の UUID 文字列が保存され、レスポンスの `sourceRef` でそのまま返る。**桁溢れも型エラーも起きない** | BE |
| AC-53 | 開発者 | `ad_invoices`（BIGINT）と `billing_invoices`（UUID）の領収書が混在する一覧を引く | 両方が `sourceRef` として返り、`source_type` で識別できる | BE |

### FE

| # | 誰が | 何をしたら | どうなる | 層 |
|---|---|---|---|---|
| AC-47 | SYSTEM_ADMIN | `/system-admin/receipts` を開く | 一覧が表示され、日本語文言がすべて `$t()` 経由（直書きゼロ） | E2E / UT |
| AC-48 | 広告主 | 広告主ダッシュボードから領収書 PDF をダウンロードする | PDF が落ちてくる | E2E |
| AC-49 | — | 6 言語すべて | `receipt.json` の新規キーが 6 言語に存在する（欠落キーガード） | UT |

### 該当なしと判断した類型

- **なし**。5 類型（空・0件・null / 境界値 / 途中失敗 / 認可 / 性能）すべてに AC を置いた。

---

## 13. i18n

新規ロケール名前空間 **`receipt.json`** を追加する。

> **実測**: 現在 `receipt` という名前空間は存在せず、`nuxt.config.ts` にも `receipt` の登録が 0 件。既存の `frontend/app/pages/admin/receipts.vue` / `me/payments/receipts.vue` は **`$t()` を 1 箇所も使っておらず文字列を直書きしている**（i18n ルール違反）。本戦役では新設分を `receipt.json` に置き、既存 2 画面の i18n 化は別戦役の課題とする。

ファイル: `frontend/app/locales/{ja,en,zh,ko,es,de}/receipt.json`（6 言語すべてに追加。未翻訳は日本語と同値で可）
登録: `frontend/nuxt.config.ts` の 6 ロケール分の `files` 配列に `'{locale}/receipt.json'` を追加（**登録しないとキーが解決されない既知の罠**）。

| キー | ja |
|---|---|
| `platform.title` | 運営領収書 |
| `platform.settings.title` | 運営発行者設定 |
| `platform.settings.issuerName` | 発行者名 |
| `platform.settings.registrationNumber` | 適格請求書発行事業者登録番号 |
| `platform.settings.qualifiedInvoicer` | 適格請求書発行事業者として発行する |
| `platform.settings.notQualifiedNotice` | 登録番号が未設定のため、適格請求書ではない領収書として発行されます |
| `platform.list.empty` | まだ領収書はありません |
| `platform.list.sourceType.adInvoice` | 広告費 |
| `platform.list.sourceType.notificationCredit` | 通知クレジット |
| `platform.list.sourceType.billingInvoice` | 月謝サブスク |
| `platform.list.voided` | 無効 |
| `platform.action.void` | 無効化する |
| `platform.action.reissue` | 再発行する |
| `platform.action.backfill` | 発行漏れを補填する |
| `platform.action.downloadPdf` | PDF をダウンロード |
| `platform.confirm.void` | この領収書を無効化します。取り消せません。 |
| `platform.pdfStatus.ready` | 発行済み |
| `platform.pdfStatus.generating` | 生成中 |
| `platform.pdfStatus.failed` | 生成失敗 |
| `platform.error.alreadyReceipted` | この取引には既に領収書が発行されています |
| `platform.error.issuerNotConfigured` | 運営の発行者設定が未登録です |
| `platform.error.integrityMismatch` | 領収書の原本が破損しています。運営にご連絡ください |
| `myInvoice.title` | 請求書・領収書 |
| `myInvoice.empty` | まだ発行された書類はありません |

---

## 14. 未解決事項 / 要マスター裁可

- [ ] **① 暗号化された宛名の検索方法**（§4.1）。`recipient_name` はアプリ層で暗号化されており、`LIKE` 検索も `FULLTEXT` も原理的に効かない（F08.4 が張った `ft_r_recipient` は暗号文に対する索引で機能していない）。電子帳簿保存法の「取引先検索」要件を満たすため、(a) 暗号化しない検索用列を足す（法人名のみなので個人情報性は低い）か、(b) `source_type` + `source_ref` から `advertiser_accounts` / `organizations` を JOIN して絞るか。**推奨は (b)**（新たな平文列を増やさずに済み、運営の取引先は法人に限られるため）。ただし (b) は団体側では使えず、電子帳簿保存法要件の充足が PLATFORM に限定される。
- [ ] **② 7 年経過後の削除ポリシー**（§9.5）。自動削除するか、期限一覧を出して運用判断にするか。**推奨は後者**（欠損金がある事業年度は 10 年保存が必要になりうるため、機械的削除は危険）。
- [ ] **③ Bucket Lock の運用主体**（§9.3）。R2 の Bucket Lock は S3 API ではなく Cloudflare 独自 API / Wrangler / ダッシュボードで設定する。アプリから設定するのではなく **IaC（Terraform）で管理する**のが筋だが、現状 IaC は未整備（CMP-013 の課題）。当面は手順書 + 手動設定とし、IaC 化は後追いとするか。
- [ ] **④ 通知クレジットの宛名**。Stripe Customer は**購入操作をした個人の email** で作られている（`NotificationCreditCheckoutService.java`:96-105）が、残高は**組織**に付く。領収書の宛名を「組織名」にするのが正しい（推奨）が、Stripe 側の領収書とは宛名が食い違うことになる。この不一致を許容するか、Stripe Customer を組織単位に作り直すか。**推奨は前者**（既存購入者の Stripe Customer を作り直すのは影響が大きく、正式な証憑は本機能が発行する領収書であるため）。
- [ ] **⑤ 請求書と領収書の両方を出すか**。広告費は「請求書（未入金時）→ 領収書（入金後）」の 2 文書になる。適格請求書としての要件は**どちらか一方**が満たせばよい。**推奨は「請求書を適格請求書とし、領収書は入金の事実を示す補助文書とする」**（実務で一般的）。ただし現行 `InvoicePdfService` が適格請求書として成立していないため、まずそこを是正するのが前提（§5.1）。
- [ ] **⑥ `receipts.amount` の桁数**（AC-23）。`DECIMAL(10,0)` に対し `notification_credit_purchases.price_jpy` は `DECIMAL(12,0)`、`ad_invoices.total_with_tax` も要確認。運営の請求額は団体の会費より桁が大きくなりうる。列拡張が必要か要判断。

---

## 15. 実装しない／未実装であることの明記

本設計書は以下を「未実装である」と正直に記録する。設計上そう見えるだけで動いていない、という誤解を避けるためである。

1. **F08.4 が規定する S3 Object Lock による原本保存は 1 行も実装されていない。** `pdf_storage_key` は書かれたことがなく、PDF は毎回オンデマンド再生成されている。
2. **PDF 用の日本語フォントがリポジトリに存在しない。** `fonts/` には `.gitkeep` しか無く、`PdfFontConfig` は黙ってスキップする。Flying Saucer 経由の PDF すべてに影響する。
3. **`pdf/receipt` テンプレートは現行エンティティと不整合で、生成すれば必ず失敗する。**
4. **`InvoicePdfService` は適格請求書として成立していない**（日本語不可・宛名なし・登録番号なし）。
5. **F20.1 課金センターの `billing_invoices` 系 3 表は DDL のみで Java エンティティが存在しない。**
6. **広告費・通知クレジットに返金／取消の遷移コードが存在しない。** よって返還インボイスも未対応。
7. **`GlobalExceptionHandler` の status マップに `RECEIPT_*` は 10 件しか登録されておらず、`RECEIPT_020` を含む 14 件が既定ステータスに落ちている。**
8. **`Cloudflare R2` は S3 Object Lock・バージョニングのいずれにも対応していない。** F08.4 の「S3 Object Lock Compliance モード」という規定は、現行の本番ストレージでは実現不可能である。

---

## 16. 変更履歴

| 日付 | 変更内容 |
|------|---------|
| 2026-09-02 | Codex 検分の指摘3件を是正: (1) `receipt_pdf_archives` の一意制約を `UNIQUE (receipt_id)` から **`UNIQUE (receipt_id, archive_kind)`** へ変更し `archive_kind`（`ORIGINAL` / `VOIDED`）列を追加。当初案は §3.4 の「void 時に無効化 PDF を新しいアーカイブ行として追加する」と矛盾しており、void が制約違反で失敗して AC-42 を満たせなかった (2) **PDF の失敗状態を永続化できない**問題を是正。`receipts` に `pdf_status` / `pdf_attempt_count` / `pdf_failed_at` / `pdf_failure_reason` を追加し、状態遷移と再試行上限（5回）を定義。`ReceiptService.java`:661 の「`pdf_storage_key` の NULL 判定から導出」を是正対象として明記 (3) `source_id BIGINT UNSIGNED` を **`source_ref VARCHAR(64)`** へ変更。`billing_invoices.id` が `BINARY(16)`（UUIDv7）であり第2段で保存も返却もできなかった。命名は `seal_stamp_logs` の「種別+ID」と billing ドメインの `_ref VARCHAR`（論理参照）の流儀を合わせた。AC を 49 → 53 件に拡張 |
| 2026-09-02 | 初版作成: 運営（PLATFORM スコープ）の領収書・適格請求書発行。器は既存 receipt ドメイン再利用（`ReceiptScopeType.PLATFORM` + `scope_id=0` センチネル。DDL 変更不要）。`receipts` に `source_type`/`source_id` を追加して元データを汎用化。採番を `receipt_number_sequences`（`period_key=YYYYMM`・短命トランザクション）へ分離。広告費請求書 PDF を `PdfGeneratorService` へ寄せる。電子帳簿保存法の原本保存基盤（`receipt_pdf_archives` + `ImmutableArchiveService`）を団体・運営共通で新設。**Cloudflare R2 が S3 Object Lock 非対応であることを確認し、Bucket Lock + ハッシュ検証の 3 層設計へ是正。** 段階リリースは広告費・通知クレジット先行、サブスク後追い。エラーコード `RECEIPT_025`〜`032` を予約。Flyway major 199 を予約。受け入れ条件 53 件（5 類型網羅） |
