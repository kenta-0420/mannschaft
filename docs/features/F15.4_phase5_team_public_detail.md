# F15.4 Phase 5: 店舗詳細ページの未ログイン公開 + 地図表示

> **ステータス**: ⚠️ **廃止（F19.1 に統合・上位化）**。本 Phase 5 の公開詳細ページ実装
> （`/public/teams/[slug]/index.vue` = `layout:'default'` + `useTeamApi().getPublicTeam`）は
> **F19.1 公開ページ（`layout:'public'` + `usePublicApi()`）が厳密なスーパーセットとして全内包**する。
> 同一 URL `/public/teams/:slug` を二重定義していたため親子ルート地雷で本ページは未マウント（死蔵）となっており、
> #2425 後続でページ本体・専用コンポーネント `PublicTeamLoginCta.vue`・専用 E2E
> （`F15.4-phase5-public-team-detail.spec.ts` / `F15.4-phase5-leakage-smoke.spec.ts`）を撤去した。
> 店舗情報（所在地・理念・地図）の表示要件は F19.1 ページが継承している。
> 設計根拠: [`F19.1_public_pages_identity_disclosure.md`](F19.1_public_pages_identity_disclosure.md) §1.2-5 / §2.3。
> **以下は歴史的経緯としてのみ参照すること。**
>
> **旧ステータス**: ✅ Phase 5 全フェーズ実装完了（2026-05-18）
> **依存**: F15.4 Phase 1〜4（PR #718/#746 マージ済み 2026-05-17）
> **最終更新**: 2026-05-18 (Phase 5-δ 完了)

## 1. 背景と目的

F15.4 Phase 1〜4 で「組織トップ→検索→結果カード」までの未ログイン公開導線が完成したが、
**カードクリック後の店舗詳細が auth middleware で弾かれる**ため、コンバージョン経路
（「行ってみよう」→「アカウント作る」）が成立しない。

Phase 5 で店舗詳細ページの未ログイン公開と地図表示を追加し、F15.4 の使命を完遂する。

## 2. スコープ

### In
- 新規 API `GET /api/v1/public/teams/{id}` (未ログイン可、レート制限あり)
- 抑制版 DTO `TeamPublicDetailResponse`
- 新規フロントページ `/public/teams/[id]` (middleware なし)
- ログイン誘導 CTA (タブ「メンバー」「お知らせ」等にオーバーレイ)
- `teams.map_embed_url` カラム追加 (Google Maps 埋め込み URL)
- 管理画面の Google Maps URL 入力欄 + バリデーション + 警告文
- i18n 6 言語

### Out
- 緯度経度ベースの半径検索 (別 Phase)
- 複数店舗の地図一覧表示 (別 Phase)
- PRIVATE チームの公開
- メンバー一覧の公開
- チャット履歴/告知/ファイル/出席情報の公開

## 3. DTO 設計 (抑制版)

### 3.1 TeamPublicDetailResponse (新規)

```java
record TeamPublicDetailResponse(
  Long id, String name, String nameKana,
  String nickname1, String nickname2,
  String template, String prefecture, String city,
  String iconUrl, String bannerUrl,
  String homepageUrl,
  LocalDate establishedDate, String establishedDatePrecision,
  String philosophy,           // 理念 (公開フラグ ON の場合のみ)
  Integer memberCount,         // ★家老推奨: 含める
  String mapEmbedUrl           // Phase 5-β で追加
)
```

### 3.2 禁則フィールド (絶対に含めない)
- メンバー一覧 / 氏名 / アイコン (個人情報)
- 連絡先 (電話 / メール)
- 番地レベルの住所
- 出席情報 / 統計
- チャット履歴 / 告知 / ファイル
- 内部ドキュメント
- supporterEnabled (公開チームは常時受付前提に統一)
- archivedAt / deletedAt (内部状態漏洩)
- version (楽観ロックトークン)

### 3.3 既存 TeamResponse (ログイン用) との関係
- **完全分離**。`TeamResponse` は無傷で残す
- 公開用は別 record として独立、互いに影響しない

## 4. API 設計

### 4.1 エンドポイント
`GET /api/v1/public/teams/{id}` (新規・認証不要・レート制限あり)

### 4.2 認可方針
- Spring Security: `.requestMatchers(HttpMethod.GET, "/api/v1/public/teams/*").permitAll()` を追加
- パターンは `*` (1 階層) で限定。`/**` (再帰) は使わない (サブパス事故防止)
- 既存 `GET /api/v1/teams/{id}` は変更なし (ログイン必須のまま)

### 4.3 ステータスコード
| コード | 条件 |
|---|---|
| 200 | 取得成功 |
| 404 | チーム不在 / 削除済み / visibility != PUBLIC / archived チーム |
| 429 | レート制限超過 |

### 4.4 レート制限
F15.4 既存 `OrganizationTeamSearchRateLimitFilter` を改名 (`PublicTeamApiRateLimitFilter`)、
対象パスを両方 (`/api/v1/organizations/*/teams/search` と `/api/v1/public/teams/*`) に拡張。

- 未ログイン: 60 req/min/IP (検索 30 より緩め、ページ滞在中の API 再叩き許容)
- ログイン: 200 req/min/user

## 5. 地図表示方針 (3 案比較)

| 案 | 内容 | 詳細表示 | 半径検索 | 地図一覧 | DDL | 運用 |
|---|---|---|---|---|---|---|
| 案 1 | `map_embed_url` テキスト保存 | ✅ | ❌ | ❌ | 小 | 小 |
| 案 2 | `lat`/`lng` + Spatial Index | ✅ | ✅ | ✅ | 中 | 中 |
| 案 3 | 段階的 (Phase 5 は案 1、別 Phase で案 2) | ✅ | △ | △ | 段階的 | 段階的 |

**採用: 案 3 (段階的)。Phase 5 のスコープは案 1 部分のみ。**

### 5.1 採用理由
- Phase 5 のミッション「店舗詳細ページに地図を出す」は案 1 だけで完結する（埋め込み iframe を貼るだけ）
- 案 2（緯度経度 + Spatial Index）は半径検索が要件になってから着手しないと過剰投資。ジオコーディング運用（住所→lat/lng 変換）は別途バッチ・API キー管理が発生する
- 案 1 は DDL が 1 カラム追加（`map_embed_url VARCHAR(2048)`）のみで済み、運用も「管理画面から Google Maps の共有 URL を貼る」だけ
- 将来 lat/lng が必要になっても **案 1 で追加した `map_embed_url` カラムは捨てなくて済む**（埋め込みは独立した UX 価値を持つ）→ 手戻りリスクが低い

### 5.2 セキュリティ要件
- 保存時バリデーション: 正規表現 `^https://www\.google\.com/maps/embed\?` で始まる URL のみ受理
- CSP: `frame-src https://www.google.com` を追加
- 管理画面側で「貼り付け方法」のヘルプリンクと「未ログインにも公開される」警告

### 5.3 既知のリスク
- **Google Maps Embed の利用規約**: API キーなしの共有リンク埋め込みは規約上グレー領域がある。Google Maps Platform の公式 Embed API（無料枠あり）への移行を中期的に検討
- **iframe の CSP**: `frame-src https://www.google.com` を CSP に追加する必要あり

## 6. フロント UI 設計

### 6.1 新規ページ
`frontend/app/pages/public/teams/[id]/index.vue`
- `definePageMeta({ layout: 'default' })` のみ。middleware なし
- 既存 `/teams/[id]/index.vue` は変更しない (ログイン必須維持)

### 6.2 表示要素
- ヘッダー (アイコン / バナー / 表示名 / template ラベル)
- 基本情報 (所在地: 都道府県＋市区町村まで / 設立日 / 公式サイト)
- 理念 (philosophy が公開フラグ ON のときのみ)
- 地図 (map_embed_url が存在する場合のみ iframe で埋め込み)
- ログイン誘導 CTA カード (「メンバー / 告知 / 詳細を見るにはログイン」)

### 6.3 検索結果カード → 詳細遷移
- F15.4 Phase 2 の検索結果カードのリンク先を `/public/teams/{id}` に変更
- ログイン済みなら従来通り `/teams/{id}` に飛ばす (useAuth().isAuthenticated 分岐)

### 6.4 i18n キー (ネームスペース `publicTeamDetail`)
- `loginToSeeMembers` / `loginToSeeAnnouncements` / `loginToSeeChat`
- `mapTitle` / `noMap`
- `philosophyTitle` / `establishedLabel` / `homepageLabel`
- `backToSearch` / `signupCta` / `signupCtaSubtext`
- (約 10 キー × 6 言語)

## 7. SEO / i18n
- `<meta name="robots" content="index,follow">` (F15.4 §10 と整合)
- OGP タグ (`og:title` = チーム名, `og:image` = bannerUrl or iconUrl)
- 構造化データ (schema.org `LocalBusiness`) は Phase 5 では見送り、別 Phase で検討
- i18n: Phase 5-γ で ja 完全 + 他 5 言語 ja フォールバック

## 8. 監査ログ
F15.4 §6.6 と統一。通常時は記録なし、レート違反時のみ `AuditEvent` に
`PUBLIC_TEAM_DETAIL_RATE_LIMIT_EXCEEDED` カテゴリで記録 (orgId なし、teamId + IP ハッシュ)。

## 9. テスト計画

### 9.1 Backend
- `@WebMvcTest`: PUBLIC team 200 / PRIVATE team 404 / archived team 404 / deleted team 404 / 429
- **抑制 DTO 禁則フィールド検出テスト** (CI 必須):
  - レスポンス JSON を文字列化 → `members` / `email` / `phone` / `version` / `archivedAt` 等の禁則ワードが含まれないことを assert
  - 抑制 DTO 仕様変更時にレビュアーが気付けるよう、禁則ワードは定数で管理

### 9.2 Frontend
- Vitest: useTeamApi.getPublicTeam のクエリ組み立て
- E2E: 未ログイン → 検索 → カードクリック → /public/teams/{id} 表示 → 「メンバーを見るにはログイン」CTA 表示 → ログインボタン押下で /login 遷移
- E2E (ログイン): 検索 → カードクリック → /teams/{id} に直接遷移できる

### 9.3 GDPR 観点スモークテスト
- シードデータでメンバー氏名「漏洩太郎」を作成 → /public/teams/{id} のレスポンス本文に出現しないこと

## 10. 未解決事項

| 論点 | 家老推奨 | マスター裁可 |
|---|---|---|
| 抑制 DTO に memberCount を含めるか | 含める | 要裁可 |
| 抑制 DTO に description/philosophy を含めるか | 含める + 管理画面警告 | 要裁可 |
| 「他店舗一覧」リンクを置くか | 置く | 要裁可 |
| 旧 `/api/v1/teams/{id}` との統合 or 分離 | 分離 | 要裁可 |
| archived チームの未ログイン扱い | 404 | 要裁可 |
| 地図表示 3 案 | 案 3 (Phase 5 は案 1 部分のみ) | 要裁可 |
| Google Maps 埋め込み URL の保存上限 | 2048 文字 | (実装判断) |

## 11. 段階リリース計画

| Phase | 内容 | 想定工数 | 依存 | ステータス |
|---|---|---|---|---|
| 5-α | Backend (DTO/Controller/Service/Security/RateLimit/テスト) | 2〜3 日 | なし | ✅ 完了 (2026-05-18 / PR #779) |
| 5-β | DDL (map_embed_url) + 管理画面入力欄 | 1〜2 日 | なし (α と並列可) | ✅ 完了 (2026-05-18 / PR #766) |
| 5-γ | Frontend 新ページ + CTA + i18n + 検索カード遷移先変更 | 3〜4 日 | α 完了 | ✅ 完了 (2026-05-18 / PR #802) |
| 5-δ | E2E + 漏洩スモークテスト + Lighthouse + CSP | 1〜2 日 | γ 完了 | ✅ 完了 (2026-05-18) |

最小所要営業日: α(2-3) + γ(3-4) + δ(1-2) = **6〜9 営業日**

## 12. ロールバック方針
- SecurityConfig の `.requestMatchers(HttpMethod.GET, "/api/v1/public/teams/*").permitAll()` 行を削除すれば即時無効化
- 検索結果カードの遷移先を `/teams/{id}` に戻せばフロントもログイン必須に復帰
- `map_embed_url` カラムは残置 (DROP マイグレーションは別 PR)

## 13. 変更履歴
| 日付 | 変更内容 |
|---|---|
| 2026-05-18 | 初版作成 (家老による軍議報告ベース) |
| 2026-05-18 | Phase 5-β 実装完了 (PR #766) — DDL + 管理画面入力欄 + バリデーション |
| 2026-05-18 | Phase 5-α 実装完了 — Public API + 抑制 DTO + レート制限フィルタ拡張 + 監査ログ + テスト網羅。`OrganizationTeamSearchRateLimitFilter` を `PublicTeamApiRateLimitFilter` にリネームし `/api/v1/public/teams/*` も同フィルタで管理（60/min/IP・200/min/user）。`AuditEventType.PUBLIC_TEAM_DETAIL_RATE_LIMIT_EXCEEDED` 新設。`TEAM_001` を 404 にマップ。|
| 2026-05-18 | Phase 5-γ 実装完了 (PR #802) — `/public/teams/[id]` ページ + `PublicTeamLoginCta` + i18n 6 言語 + `useTeamApi.getPublicTeam` + 検索カード遷移先分岐（未ログイン → `/public/teams/`、ログイン済 → `/teams/`）|
| 2026-05-18 | Phase 5-δ 実装完了 — Phase 5 シリーズ完全クローズ。(1) E2E 漏洩スモークテスト拡充 (`F15.4-phase5-leakage-smoke.spec.ts` 新設: 個人情報語の HTML 全文不在 + 汚染レスポンス防衛 + API JSON 禁則キー検査 + 404 ケース 5 種網羅)。(2) CSP `frame-src 'self' https://www.google.com` をページ単位の `<meta http-equiv>` で付与（フロント全体 CSP 未導入のため本ページ限定で適用、将来 nuxt-security へ統合予定）。(3) Lighthouse 検証手順を §14 に追記（採用選択肢: C 案=手動 Chrome DevTools 実行手順を残置。playwright-lighthouse 依存追加を本 Phase スコープに含めない判断）。(4) `PublicTeamControllerTest.FORBIDDEN_FIELDS` を 13 → 25 件に拡張（`emails`/`phones`/`addressLine`/`chatHistory`/`documents`/`attendances`/`attendanceRecords`/`memberRoster` 等を追加）。(5) F15.4 本体設計書 §12 の Phase 5 行ステータスを「✅ 完了」に更新。|

---

## 14. Lighthouse 検証手順（Phase 5-δ 追加）

### 採用方針
本 Phase では **選択肢 C（手動 Chrome DevTools 実行手順）** を採用する。理由:

- `playwright-lighthouse` プラグインの依存追加は Phase 5-δ のスコープ（E2E 拡充 + CSP + Lighthouse 整備）を超え、CI ランナーへの Chromium 起動オプション調整・スコア閾値の False Positive 対応など別軍議のコスト発生が見込まれる
- Phase 5 リリース直後の SEO/Accessibility は手動確認で十分（公開店舗詳細はテンプレ構造が単純で、リグレッション源が限定的）
- 将来的に CI で自動化する場合は `playwright-lighthouse` 導入時に本セクションの期待値を流用する

### 手動実行手順

1. dev サーバー起動: `cd frontend && npm run dev`（既定で `http://localhost:3000`）
2. Chrome で `http://localhost:3000/public/teams/1` を開く（テストデータの実在 PUBLIC チーム ID を使用）
3. DevTools → Lighthouse タブ → カテゴリ「SEO」「Accessibility」「Best Practices」を選択
4. デバイス: Desktop / Mobile 両方で実施
5. Analyze page load を実行

### 期待スコア

| カテゴリ | 期待値 | コメント |
|---|---|---|
| SEO | ≥ 90 | OGP + robots index + i18n hreflang は Phase 5-γ で実装済み |
| Accessibility | ≥ 80 | PrimeVue 既定値 + `aria-label` / `data-testid` 付与済み |
| Best Practices | ≥ 80 | CSP `frame-src` を本 Phase で対応済み |

### スコア未達時の対応

- SEO: meta description / canonical URL の追加検討
- Accessibility: 色コントラスト / 画像 alt の不足を Lighthouse レポートから個別対応
- Best Practices: CSP の警告が出れば §5.2 の方針を再検討し、nuxt-security の本格導入を起票
