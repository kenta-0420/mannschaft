# F09.7: 広告表示

> **ステータス**: 🟢 設計完了
> **実装フェーズ**: Phase 9
> **最終更新**: 2026-07-07
> **モジュール種別**: デフォルト機能 #27（広告表示）
> **関連ドキュメント**: F09.19 広告枠サービング（placement 統一語彙・掲載面・サービング API の正本）、F09.11 広告主ダッシュボード、F09.17 広告主ターゲット配信キャンペーン、F09.2 プロモーション配信（マーケティング基盤・SegmentEvaluator 共有）、F01.3 テンプレート・モジュール（フィーチャーフラグ `FEATURE_V9_ENABLED`）、F08.2 会費・決済（プラン判定）、F10.1 管理者ダッシュボード（SYSTEM_ADMIN 管理画面）

---

## 1. 概要

フリープラン利用中のチーム/組織のページにプラットフォーム広告を自動表示し、プラットフォーム収益を確保する機能。有料プラン加入済みのチーム/組織では広告を完全非表示にし、広告非表示を有料プランへのアップグレードインセンティブとして機能させる。

**Phase 9（初期実装）**: Amazon アソシエイト・楽天アフィリエイトの静的バナー表示。SYSTEM_ADMIN が管理ダッシュボード（F10.1）からプロバイダ・タグ ID・配置位置・バナークリエイティブ・有効期間を設定し、フロントエンドが SSR 時に描画する。また、F07.3 備品管理との連携により **備品補充リンク**（商品ページへの商品直リンク）も同一の `affiliate_configs` タグ設定から生成する。

**拡張の実装状況（2026-07-07 追記）**: かつて「将来拡張（Phase 10+）」として設計方針のみ記載していた広告主出稿・CPM/CPC 課金・計測は、**F09.11（広告主アカウント・料金・請求）/ F09.17（メッセージ型ターゲット配信）として大部分が実装済み**である（§10 参照）。掲載面（表示側）・運用型キャンペーン CRUD・日次集計バッチと placement 統一語彙は **F09.19 広告枠サービング** が正本として設計する。本ドキュメントの責務は「アフィリエイト広告（affiliate_configs）と広告非表示ポリシー」に限定される。

**Phase 1〜8 は `FEATURE_V9_ENABLED=false` により UI・API を完全非表示。DB テーブルは Phase 1 で先行作成。**

---

## 2. スコープ

### 対象ロール
| ロール | 操作可能な範囲 |
|--------|--------------|
| SYSTEM_ADMIN | アフィリエイト設定の CRUD・広告配信状況の監視・将来: 広告クリエイティブの審査 |
| ADMIN | 広告の表示/非表示は不可（プラン変更でのみ制御） |
| DEPUTY_ADMIN | — |
| MEMBER | 広告を閲覧（フリープラン時） |
| SUPPORTER | 広告を閲覧（フリープラン時） |
| GUEST | 公開ページで広告を閲覧（フリープラン時） |

### 対象レベル
- [x] 組織 (Organization) — 組織ページでの広告表示
- [x] チーム (Team) — チームページでの広告表示
- [x] 個人 (Personal) — 個人ダッシュボードでの広告表示（閲覧のみ）

---

## 3. DB設計

### テーブル一覧
| テーブル名 | 役割 | 論理削除 |
|-----------|------|---------|
| `affiliate_configs` | アフィリエイト設定（Amazon アソシエイト等のタグ・配置管理） | あり（`deleted_at`） |

### テーブル定義

#### `affiliate_configs`

SYSTEM_ADMIN が管理するアフィリエイト設定。Amazon アソシエイト・楽天アフィリエイトに対応。

| カラム名 | 型 | NULL | デフォルト | 説明 |
|---------|---|------|-----------|------|
| `id` | BIGINT UNSIGNED | NO | AUTO_INCREMENT | PK |
| `provider` | VARCHAR(20) | NO | — | アフィリエイトプロバイダ（`AMAZON` / `RAKUTEN`） |
| `tag_id` | VARCHAR(100) | NO | — | アフィリエイトタグ ID（Amazon: 'mannschaft-22'、楽天: アフィリエイト ID） |
| `placement` | VARCHAR(30) | NO | — | 表示配置。**正の語彙は `AdPlacement` enum（F09.19 §3 の placement 統一語彙表）**: `SIDEBAR_RIGHT` / `BANNER_FOOTER` / `BANNER_HEADER` / `IN_FEED` / `DASHBOARD_TILE`。第1弾で掲載面が実在するのは `DASHBOARD_TILE` と `IN_FEED` のみ（他は管理画面の選択肢に出さない） |
| `description` | VARCHAR(200) | YES | NULL | 管理者メモ（配置の用途説明等） |
| `banner_image_url` | VARCHAR(500) | YES | NULL | バナー画像 URL（NULL: FE がプロバイダ別デフォルト画像を使用） |
| `banner_width` | SMALLINT UNSIGNED | YES | NULL | バナー幅 px（NULL: FE がプロバイダ別デフォルトサイズを使用） |
| `banner_height` | SMALLINT UNSIGNED | YES | NULL | バナー高さ px |
| `alt_text` | VARCHAR(200) | YES | NULL | バナー代替テキスト（アクセシビリティ・SEO。NULL: プロバイダ名をデフォルト表示） |
| `is_active` | BOOLEAN | NO | TRUE | 有効/無効（手動トグル。スケジューリングとは独立） |
| `active_from` | DATETIME | YES | NULL | 有効開始日時（NULL: 即時有効） |
| `active_until` | DATETIME | YES | NULL | 有効終了日時（NULL: 無期限） |
| `display_priority` | SMALLINT UNSIGNED | NO | 0 | 同一配置に複数設定がある場合の優先度（昇順。最小値が優先表示） |
| `created_at` | DATETIME | NO | CURRENT_TIMESTAMP | |
| `updated_at` | DATETIME | NO | CURRENT_TIMESTAMP ON UPDATE | |
| `deleted_at` | DATETIME | YES | NULL | 論理削除日時 |

**インデックス**
```sql
INDEX idx_affiliate_configs_provider_placement (provider, placement)                  -- プロバイダ×配置の検索用（一意性はアプリ層で保証）
INDEX idx_affiliate_configs_placement_active_priority (placement, is_active, display_priority)  -- 配置別の優先度順取得
```

**制約・備考**
- `provider` は VARCHAR で ENUM ではない（新プロバイダ追加時に ALTER TABLE 不要）
- `placement` も VARCHAR（新しい配置位置の追加を容易にする）
- 同一 `placement` に異なる `provider` を設定可能（例: サイドバーに Amazon、フッターに楽天）。同一 `placement` × `provider` の組み合わせはアプリ層で一意性を保証（INSERT/UPDATE 前に `SELECT ... WHERE provider = ? AND placement = ? AND deleted_at IS NULL` でチェック）。MySQL の UNIQUE KEY に `deleted_at`（NULL許容）を含めると NULL 同士が等しくない扱いとなり制約が機能しないため、DB レベルの一意制約は使用しない
- **設定上限**: 全体で最大20件（アプリ層で検証）。プロバイダ × 配置の組み合わせは現実的に10件未満だが、将来の配置追加を見越して余裕を持たせる
- `tag_id`: プロバイダごとにフォーマットが異なるため汎用 VARCHAR。Amazon は「xxxx-22」形式、楽天はアフィリエイト ID（数値文字列）
- `banner_image_url` / `banner_width` / `banner_height` / `alt_text`: 全て NULL 許容。NULL の場合 FE がプロバイダ別デフォルト（Amazon 公式バナー / 楽天公式バナー）を描画。SYSTEM_ADMIN が管理画面から任意のバナーに差し替え可能（FE デプロイ不要）
- `active_from` / `active_until`: スケジューリング用。`is_active` とは独立して評価。広告が表示されるには「`is_active = TRUE` AND スケジュール期間内」の両方を満たす必要がある。バッチジョブ不要（`/ads/active` のクエリ条件で制御）
- 論理削除: 設定変更の履歴追跡用。`deleted_at` は `NOW(6)`（マイクロ秒精度）で設定

---

## 4. API設計

### エンドポイント一覧
| メソッド | パス | 認証 | 説明 |
|---------|-----|------|------|
| GET | `/api/v1/system-admin/affiliate-configs` | 必要 | アフィリエイト設定一覧（SYSTEM_ADMIN） |
| POST | `/api/v1/system-admin/affiliate-configs` | 必要 | アフィリエイト設定の作成 |
| PUT | `/api/v1/system-admin/affiliate-configs/{id}` | 必要 | アフィリエイト設定の更新 |
| PATCH | `/api/v1/system-admin/affiliate-configs/{id}/toggle` | 必要 | 有効/無効の切り替え |
| PATCH | `/api/v1/system-admin/affiliate-configs/toggle-all` | 必要 | 全設定の一括有効/無効切り替え |
| DELETE | `/api/v1/system-admin/affiliate-configs/{id}` | 必要 | アフィリエイト設定の削除（論理削除） |
| GET | `/api/v1/system-admin/affiliate-configs/preview` | 必要 | バナープレビュー取得（SYSTEM_ADMIN） |
| GET | `/api/v1/ads/active` | 不要 | 表示すべき広告の取得（フロントエンド用。認証不要） |

### リクエスト／レスポンス仕様

#### `GET /api/v1/ads/active`

**認可**: 認証不要（公開 API。SSR 時にサーバーサイドから呼び出す）

表示すべき広告設定を取得する。フリープラン判定はフロントエンド側で実行。

**クエリパラメータ**
| パラメータ | 型 | 必須 | 説明 |
|-----------|---|------|------|
| `placement` | String | No | 配置フィルタ（`SIDEBAR_RIGHT` / `BANNER_FOOTER` / 将来追加の配置も対応）。未指定: 全配置 |

**レスポンス（200 OK）**
```json
{
  "data": [
    {
      "provider": "AMAZON",
      "affiliate_url": "https://www.amazon.co.jp/?tag=mannschaft-22",
      "placement": "SIDEBAR_RIGHT",
      "display_priority": 0,
      "banner_image_url": null,
      "banner_width": null,
      "banner_height": null,
      "alt_text": null
    },
    {
      "provider": "RAKUTEN",
      "affiliate_url": "https://hb.afl.rakuten.co.jp/hgc/XXXXXXXX/?pc=https%3A%2F%2Fwww.rakuten.co.jp%2F",
      "placement": "BANNER_FOOTER",
      "display_priority": 0,
      "banner_image_url": "https://example.com/rakuten-banner-728x90.png",
      "banner_width": 728,
      "banner_height": 90,
      "alt_text": "楽天市場でお買い物"
    }
  ]
}
```

**補足**
- `is_active = TRUE` かつ `deleted_at IS NULL` かつスケジュール期間内（`active_from <= NOW()` AND `active_until IS NULL OR active_until >= NOW()`）の設定のみ返却
- `placement` ごとに `display_priority ASC, id ASC`（同一優先度は登録順）でソート
- **内部 DB の `id` は返却しない**: 認証不要の公開 API で AUTO_INCREMENT id を露出すると、レコード数の推測が可能になる。FE は `placement + provider` の組み合わせで一意識別する。Phase 10+ のインプレッション/クリック計測 API では UUID ベースの公開キーを別途導入する
- `affiliate_url`: バックエンドが `tag_id` + プロバイダ固有のベース URL から完成済みアフィリエイトリンクを構築して返却。`tag_id` フィールド自体は返却しない（管理情報と公開情報の分離が目的）
- `banner_image_url` / `banner_width` / `banner_height` / `alt_text`: NULL の場合 FE がプロバイダ別デフォルトバナーを描画。値がある場合はそのまま使用。SYSTEM_ADMIN がバナーを差し替えても FE デプロイ不要
- プロバイダ別の URL 構築ルール:
  - `AMAZON`（バナー広告）: `https://www.amazon.co.jp/?tag={tag_id}`
  - `AMAZON`（備品補充リンク）: `https://www.amazon.co.jp/dp/{amazon_asin}?tag={tag_id}`（F07.3 連携。ASIN は `equipment_items.amazon_asin` から取得）
  - `RAKUTEN`: `https://hb.afl.rakuten.co.jp/hgc/{tag_id}/?pc=https%3A%2F%2Fwww.rakuten.co.jp%2F`（`tag_id` = 楽天アフィリエイト ID）
  - 将来追加のプロバイダも同様にバックエンドで URL を構築（FE はリンクをそのまま使用）
- **キャッシュ**: Valkey TTL 10分（キー: `mannschaft:cache:ads:active:{placement}`、placement 未指定時は `mannschaft:cache:ads:active`）。設定変更時にキャッシュ無効化
- **フリープラン判定**: フロントエンド（Nuxt SSR）がチーム/組織のプラン情報を確認し、フリープランの場合のみ広告コンポーネントを描画。バックエンド API は判定しない（`/ads/active` は常に設定を返す）。FE 判定の理由: 広告非表示はユーザーに有利な操作であり改ざんによるセキュリティ被害がない、認証不要にすることで SSR キャッシュ効率を最大化できる、Phase 10+ のターゲティング広告移行時に BE 判定に切り替え予定

#### `POST /api/v1/system-admin/affiliate-configs`

**認可**: SYSTEM_ADMIN のみ

**リクエストボディ**
```json
{
  "provider": "RAKUTEN",
  "tag_id": "XXXXXXXX",
  "placement": "BANNER_FOOTER",
  "description": "フッター楽天バナー",
  "display_priority": 0,
  "banner_image_url": "https://example.com/rakuten-banner-728x90.png",
  "banner_width": 728,
  "banner_height": 90,
  "alt_text": "楽天市場でお買い物",
  "active_from": "2026-04-01T00:00:00",
  "active_until": "2026-06-30T23:59:59"
}
```

**フィールド補足**
| フィールド | 必須 | デフォルト | 説明 |
|-----------|------|-----------|------|
| `provider` | Yes | — | アフィリエイトプロバイダ（`AMAZON` / `RAKUTEN`） |
| `tag_id` | Yes | — | アフィリエイトタグ ID |
| `placement` | Yes | — | 表示配置（`SIDEBAR_RIGHT` / `BANNER_FOOTER` / `BANNER_HEADER`） |
| `description` | No | NULL | 管理者メモ |
| `display_priority` | No | 0 | 表示優先度（昇順） |
| `banner_image_url` | No | NULL | バナー画像 URL（NULL: FE プロバイダ別デフォルト） |
| `banner_width` | No | NULL | バナー幅 px |
| `banner_height` | No | NULL | バナー高さ px |
| `alt_text` | No | NULL | バナー代替テキスト |
| `active_from` | No | NULL | 有効開始日時（NULL: 即時有効） |
| `active_until` | No | NULL | 有効終了日時（NULL: 無期限） |

**レスポンス（201 Created）**
```json
{
  "data": {
    "id": 1,
    "provider": "RAKUTEN",
    "tag_id": "XXXXXXXX",
    "placement": "BANNER_FOOTER",
    "description": "フッター楽天バナー",
    "is_active": true,
    "display_priority": 0,
    "banner_image_url": "https://example.com/rakuten-banner-728x90.png",
    "banner_width": 728,
    "banner_height": 90,
    "alt_text": "楽天市場でお買い物",
    "active_from": "2026-04-01T00:00:00",
    "active_until": "2026-06-30T23:59:59",
    "created_at": "2026-03-18T10:00:00",
    "updated_at": "2026-03-18T10:00:00"
  }
}
```

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 400 | provider が空 / tag_id が空 / placement が不正 / 設定上限（20件）超過 |
| 403 | SYSTEM_ADMIN 以外 |
| 409 | 同一 provider × placement の組み合わせが既に存在 |

#### `GET /api/v1/system-admin/affiliate-configs`

**認可**: SYSTEM_ADMIN のみ

**クエリパラメータ**
| パラメータ | 型 | 必須 | 説明 |
|-----------|---|------|------|
| `include_deleted` | Boolean | No | `true`: 論理削除済みレコードも含めて返却。デフォルト: `false`（`deleted_at IS NULL` のみ） |

件数が少ないためページネーション不要。全件返却。

**レスポンス（200 OK）**
```json
{
  "data": [
    {
      "id": 1,
      "provider": "AMAZON",
      "tag_id": "mannschaft-22",
      "placement": "SIDEBAR_RIGHT",
      "description": "メインサイドバー広告枠",
      "is_active": true,
      "display_priority": 0,
      "banner_image_url": null,
      "banner_width": null,
      "banner_height": null,
      "alt_text": null,
      "active_from": null,
      "active_until": null,
      "created_at": "2026-03-18T10:00:00",
      "updated_at": "2026-03-18T10:00:00",
      "deleted_at": null
    }
  ]
}
```

**補足**
- デフォルトはアクティブレコードのみ返却。`include_deleted=true` 時は `deleted_at` フィールドを含めて返却し、FE 側で削除済みの表示制御を行う
- 論理削除の目的は設定変更の履歴追跡であるため、管理画面から参照手段を残す

#### `PUT /api/v1/system-admin/affiliate-configs/{id}`

**認可**: SYSTEM_ADMIN のみ

**リクエストボディ**
```json
{
  "provider": "AMAZON",
  "tag_id": "mannschaft-22",
  "placement": "SIDEBAR_RIGHT",
  "description": "メインサイドバー広告枠（更新）",
  "display_priority": 1
}
```

**レスポンス（200 OK）**: POST と同一形式（更新後のリソース全体を返却）

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 400 | バリデーションエラー |
| 403 | SYSTEM_ADMIN 以外 |
| 404 | 指定 id が存在しない（論理削除済み含む） |
| 409 | 変更後の provider × placement が既存レコードと重複 |

#### `PATCH /api/v1/system-admin/affiliate-configs/{id}/toggle`

**認可**: SYSTEM_ADMIN のみ

**リクエストボディ**: なし（現在の `is_active` を反転）

**レスポンス（200 OK）**
```json
{
  "data": {
    "id": 1,
    "is_active": false,
    "updated_at": "2026-03-18T11:00:00"
  }
}
```

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 403 | SYSTEM_ADMIN 以外 |
| 404 | 指定 id が存在しない |

#### `DELETE /api/v1/system-admin/affiliate-configs/{id}`

**認可**: SYSTEM_ADMIN のみ

**レスポンス（200 OK）**（論理削除のため 200 + deleted_at を返却。.claudecode.md §25 準拠）
```json
{
  "data": {
    "id": 1,
    "deleted_at": "2026-03-18T12:00:00"
  }
}
```

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 403 | SYSTEM_ADMIN 以外 |
| 404 | 指定 id が存在しない（既に論理削除済み含む） |

#### `PATCH /api/v1/system-admin/affiliate-configs/toggle-all`

**認可**: SYSTEM_ADMIN のみ

障害時やコンテンツポリシー違反発覚時に、全広告を即座に停止/再開する。

**リクエストボディ**
```json
{
  "is_active": false
}
```

**レスポンス（200 OK）**
```json
{
  "data": {
    "updated_count": 3,
    "is_active": false,
    "updated_at": "2026-03-18T15:00:00"
  }
}
```

**補足**
- `deleted_at IS NULL` の全レコードの `is_active` を一括更新
- 監査ログ: `AFFILIATE_CONFIG_BULK_TOGGLED`（`updated_count` を記録）
- Valkey キャッシュを即時無効化

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 403 | SYSTEM_ADMIN 以外 |

#### `GET /api/v1/system-admin/affiliate-configs/preview`

**認可**: SYSTEM_ADMIN のみ

管理画面のプレビューパネル用。`is_active = false` やスケジュール外の設定も含めて、実際のバナー描画と同じ形式で返却する。

**クエリパラメータ**
| パラメータ | 型 | 必須 | 説明 |
|-----------|---|------|------|
| `placement` | String | No | 配置フィルタ。未指定: 全配置 |

**レスポンス（200 OK）**: `/ads/active` と同一形式（ただし `is_active` / `active_from` / `active_until` フィルタなし。全設定を返却）

```json
{
  "data": [
    {
      "id": 1,
      "provider": "AMAZON",
      "affiliate_url": "https://www.amazon.co.jp/?tag=mannschaft-22",
      "placement": "SIDEBAR_RIGHT",
      "display_priority": 0,
      "banner_image_url": null,
      "banner_width": null,
      "banner_height": null,
      "alt_text": null,
      "is_active": true,
      "active_from": null,
      "active_until": null
    }
  ]
}
```

**補足**
- 管理画面専用のため `id`・`is_active`・`active_from`/`active_until` も返却（公開 API `/ads/active` とは異なる）
- `deleted_at IS NULL` のレコードのみ対象
- FE 管理画面のプレビューパネルで、実際のバナー表示をシミュレーション可能

---

## 5. ビジネスロジック

### tag_id バリデーション

プロバイダ別にアフィリエイトタグの形式をバリデーションする（SYSTEM_ADMIN の入力ミス防止。不正な tag_id が設定されると全ユーザーのアフィリエイト収益がゼロになるため影響大）。

```
■ プロバイダ別バリデーションルール:
  - AMAZON: 正規表現 ^[a-zA-Z0-9]+-[0-9]{2}$ （例: mannschaft-22）
  - RAKUTEN: 正規表現 ^[a-f0-9]{8}$ （楽天アフィリエイト ID。8桁16進数）
  - 将来プロバイダ追加時: AffiliateProviderValidator Strategy パターンで拡張

■ 実装方針:
  - POST / PUT 時にプロバイダに応じたバリデーションを実行
  - 不正な場合は 400 Bad Request + fieldErrors（field: "tag_id"）

■ スケジュールバリデーション:
  - active_from / active_until が両方指定された場合: active_from < active_until であること
  - active_until が過去日時の場合: 警告ログを出力するがエラーにはしない（履歴として保持）
```

### 広告表示判定フロー

> **実装状況（2026-07-07）**: `GET /api/v1/ads/active` を呼ぶ FE コードは origin/main に存在せず、本フローは**未実装**のまま掲載面の実体が F09.19 に移った。表示側は F09.19 の `GET /api/v1/spotlight/content`（認証必須・CSR）に一本化し、`/ads/active` は管理・互換用として残置する。広告非表示のトリガー（後述）とアップグレード CTA の原則は F09.19 §7.5 / §8.1 に引き継がれており引き続き有効。

```
1. フロントエンド（Nuxt SSR）がページレンダリング時に実行:
   a. チーム/組織のプラン情報を Pinia ストアから取得
   b. フリープランの場合のみ広告コンポーネントを描画対象にする
   c. `GET /ads/active` を呼び出し（Valkey キャッシュで高速応答）
   d. placement に応じた位置に広告バナーを挿入

2. SSR 描画:
   a. バックエンドから返却された `affiliate_url` をそのまま HTML に埋め込み
   b. `banner_image_url` が返却されている場合はその画像を表示。NULL の場合は FE がプロバイダ別デフォルトバナーを描画:
      - AMAZON: Amazon 公式バナーコード（PA-API または静的バナー）
      - RAKUTEN: 楽天公式バナーコード
   c. `banner_width` / `banner_height` が指定されている場合は明示的にサイズ指定（CLS 防止）
   d. `alt_text` が NULL の場合はプロバイダ名をデフォルト表示（例: 「Amazon」「楽天市場」）
   e. FE 側で tag_id を直接扱うことはない（URL 構築はバックエンド責務）

3. アップグレード CTA（Call to Action）:
   a. 広告バナーの下部に小さなテキストリンクを常時表示: 「広告なしで使う →」
   b. F08.2 のプランアップグレード画面に直接遷移
   c. フリープランユーザーが広告を見るたびに有料プランの存在を自然に認知させる
   d. 実装: FE のみ（BE 変更なし。広告コンポーネント内に静的リンクを配置）

4. SPA ナビゲーション時:
   a. Pinia ストアのキャッシュから広告設定を使用（API 再呼び出し不要）
   b. プラン変更時（有料 → フリー or フリー → 有料）に広告コンポーネントの表示/非表示を即時切り替え
```

### 備品補充リンク連携（F07.3）

`affiliate_configs` の `tag_id` は広告バナー表示だけでなく、F07.3 備品管理の「補充する」ボタン（Amazon 商品直リンク）にも使用される。

```
■ 用途の違い:
  - バナー広告: https://www.amazon.co.jp/?tag={tag_id}             ← トップページへの汎用リンク
  - 備品補充:  https://www.amazon.co.jp/dp/{ASIN}?tag={tag_id}    ← 特定商品への直リンク

■ 仕組み:
  1. ADMIN が備品登録時に Amazon 商品 URL を入力
  2. バックエンドが ASIN を正規表現 /\/dp\/([A-Z0-9]{10})/ で抽出して保存
  3. ADMIN が「補充する」ボタンをクリック
  4. バックエンドが GET /equipment/{id}/replenish-link を処理:
     - equipment_items.amazon_asin + affiliate_configs(AMAZON).tag_id を結合
     - 構築したリンクをフロントエンドに返却
  5. フロントエンドが新規タブで Amazon 商品ページを開く
  6. 購入が発生するとアフィリエイト報酬が発生

■ 収益性の観点:
  - バナー広告（トップページ）よりも商品直リンクのほうが購入意欲が高いため CVR が高い
  - 備品管理者（ADMIN）が補充タイミングで自然にアフィリエイトリンクを経由する
  - 同一の tag_id を使い回すため SYSTEM_ADMIN の追加設定は不要

■ affiliate_configs に有効な AMAZON 設定が存在しない場合:
  - has_replenish_link = false（補充ボタン自体を非表示）
  - GET /equipment/{id}/replenish-link は 503 を返却
```

---

### 広告非表示のトリガー

```
以下のいずれかに該当する場合、広告を非表示:
  - チーム/組織ページ: 当該チーム/組織が有料プラン加入中（subscription_status != 'FREE'）
  - 個人ダッシュボード: ユーザーが所属するチーム/組織のいずれかが有料プラン加入中
    → FE の Pinia ストアで所属先のプラン情報を確認（追加 API 不要）
    → 有料プラン加入済みユーザーに広告を表示するのは UX を損なうため、所属先の最上位プランに準拠
  - FEATURE_V9_ENABLED = false（Phase 1〜8）
  - affiliate_configs に is_active = TRUE かつスケジュール期間内の設定が0件
```

### キャッシュ戦略

> **実装状況と正本の移管（2026-07-07）**: 本節の Valkey キャッシュは origin/main 時点で**未実装**（`AffiliateConfigService` は毎回 DB 読み）。表示側の一本化に伴い、アフィリエイト候補のキャッシュは **F09.19 §6.2 のサービング用キー体系**（`mannschaft:cache:ads:affiliate:{placement}:{template}:{prefecture}:{locale}`、TTL 10 分）で実現する。以下の `/ads/active` 用キー設計は互換参考として残すが、新規実装は F09.19 に従うこと。

```
■ /ads/active のキャッシュ:
  - Valkey キー: mannschaft:cache:ads:active (placement パラメータなし) / mannschaft:cache:ads:active:{placement}
  - TTL: 10分
  - 無効化タイミング: affiliate_configs の CREATE / UPDATE / DELETE / TOGGLE / BULK_TOGGLE 時に全キャッシュを削除
  - キャッシュミス時: DB から SELECT → Valkey にセット → レスポンス返却

■ フロントエンド側:
  - Pinia ストア `adsStore` に広告設定をキャッシュ（TTL: 10分。Valkey TTL と統一し、設定変更の反映遅延を最小化）
  - SSR 初回レンダリング時に API 呼び出し → ストアに保存
  - SPA ナビゲーション時はストアから取得（API 呼び出しなし）

■ グレースフルデグラデーション:
  - Valkey 接続エラー時: DB から直接取得（Valkey 復旧まで毎回 DB アクセス）
  - DB も応答しない場合: 空配列を返却（広告なし表示）。ページ描画はブロックしない
  - FE 側: /ads/active の API エラー時は広告コンポーネントを非表示（ページ本体の描画に影響させない）
  - 監視: Valkey 接続エラーを ERROR ログ出力 + アラート通知
```

---

## 6. エラーコード定義

| コード | メッセージ | 用途 |
|--------|-----------|------|
| `AFF_001` | 指定されたアフィリエイト設定が見つかりません | GET/PUT/PATCH/DELETE で id 不在時（404） |
| `AFF_002` | 同一プロバイダ×配置の設定が既に存在します | POST/PUT で provider × placement 重複時（409） |
| `AFF_003` | アフィリエイト設定の上限（20件）に達しています | POST で上限超過時（400） |
| `AFF_004` | タグ ID のフォーマットが不正です | POST/PUT で tag_id バリデーション失敗時（400） |
| `AFF_005` | 有効期間の指定が不正です | active_from >= active_until の場合（400） |

---

## 7. セキュリティ考慮事項

- **SYSTEM_ADMIN 限定**: アフィリエイト設定の CRUD は SYSTEM_ADMIN のみ。一般ユーザーが広告設定を変更・閲覧する API はない（`/ads/active` は設定値のみ返却し、管理情報は含まない）
- **`tag_id` の情報分離**: `/ads/active` では `tag_id` フィールド自体は返却せず、構築済み `affiliate_url` のみを返す。アフィリエイト URL にはタグが含まれるため完全な秘匿ではないが、管理画面情報（`description` 等）と公開情報の分離が目的。SYSTEM_ADMIN API でのみ管理情報を返す
- **XSS 防止**: バナー HTML はフロントエンドのテンプレートで生成。バックエンドが `affiliate_url` を構築する際に `tag_id` を URL エンコードしてからパラメータに埋め込む。`banner_image_url` はバックエンドが URL 形式バリデーション（`https://` で始まること）を実施し、FE は `<img>` の `src` にのみ使用。FE は返却された URL をそのまま `<a>` タグの `href` / `<img>` の `src` に設定し、プロバイダ公式バナーコード以外の任意 HTML は挿入しない
- **広告ブロッカー対策**: CSS クラス名は保守性を維持しつつ、広告専用であることを直接示す名前（`ad-`, `affiliate-`, `banner-`, `sponsor-`, `promo-` 等）は避ける。**2026-07-07 改訂: 本原則は F09.19 §4「中立命名原則」に体系化した**。新設の露出面（API URL・コンポーネント名・DOM class/id/data-testid・ウィジェットキー）は中立語 `spotlight` を用いる（`promo-card` / `sponsor-section` のような命名は `promo` / `sponsor` トークン自体がフィルタ対象のため**不採用に変更**）。完全なブロッカー回避は目指さない（Amazon URL 自体が検出対象のため効果が限定的）が、自社ファーストパーティ配信の表示・計測欠損を防ぐ。ブロッカー検出時は代替メッセージ（「広告収入でサービスを維持しています」）を表示する
- **レート制限**: `/ads/active` は認証不要の公開 API のため、IP ベースのレート制限を適用（Nginx: 30 req/min per IP）
- **監査ログ**: `AFFILIATE_CONFIG_CREATED` / `AFFILIATE_CONFIG_UPDATED` / `AFFILIATE_CONFIG_DELETED` / `AFFILIATE_CONFIG_TOGGLED` / `AFFILIATE_CONFIG_BULK_TOGGLED` を記録

---

## 8. 運用監視

### 監視メトリクス

| メトリクス | 閾値 | アラート条件 |
|-----------|------|------------|
| `mannschaft:cache:ads:active` キャッシュヒット率 | 90% | 未満が5分継続 |
| `/ads/active` レスポンスタイム p95 | 200ms | 超過が5分継続（SSR クリティカルパス上） |
| `is_active = TRUE` かつスケジュール期間内のレコード数 | 1件以上 | 0件（= 広告収益ゼロ状態。意図的でなければ要確認） |
| Valkey 接続エラー率 | 0% | 1件以上発生 |

### SYSTEM_ADMIN ダッシュボード連携（F10.1）

管理ダッシュボードの「広告管理」セクションに以下を表示:

```
■ サマリーカード:
  - 現在アクティブな広告設定数（プロバイダ別内訳）
  - 配置別の設定状況（SIDEBAR_RIGHT: Amazon、BANNER_FOOTER: 楽天、等）
  - スケジュール待ち設定数（active_from が未来の設定）
  - 本日有効期限切れ予定の設定数

■ 設定管理テーブル:
  - 全 affiliate_configs のCRUD操作（作成・編集・トグル・削除）
  - 一括無効化ボタン（緊急停止用。確認ダイアログ付き）
  - プレビューパネル（各配置のバナー表示をシミュレーション）
  - 設定変更履歴（監査ログから直近の操作を表示）

■ プロバイダ設定ガイド:
  - Amazon: アソシエイトタグの取得方法・フォーマット説明（xxxx-22）
  - 楽天: アフィリエイト ID の取得方法・フォーマット説明
```

---

## 9. Flywayマイグレーション

```
V9.001__create_affiliate_configs_table.sql                  -- affiliate_configs テーブル作成
V9.057__add_targeting_columns_to_affiliate_configs.sql      -- ターゲティング列（template/prefecture/locale）追加
```

**マイグレーション上の注意点**
- **ドリフト是正（2026-07-07）**: 旧版は「V1.074 で Phase 1 先行作成」と記載していたが、実装の実体は `V9.001`（+ ターゲティング列 `V9.057`）である。本書は実装に合わせて是正した
- Amazon アソシエイトタグの初期データは Flyway シードではなく、SYSTEM_ADMIN が管理画面から手動設定（環境ごとにタグが異なるため）
- placement 関連の追加 DDL（`ads.placement` 等）は F09.19 §5 が正本

---

## 10. 拡張の実装状況（旧「将来拡張: ターゲティング広告プラットフォーム」）

**ドリフト是正（2026-07-07）**: 旧版で「Phase 10+ で実装予定」としていた項目は大部分が実装済みである。現状と正本の対応を以下に示す。

### テーブル（旧「追加予定 6 テーブル」→ 全て実装済み）

| テーブル名 | 実装 | 正本ドキュメント |
|-----------|------|------|
| `ads` | ✅ V10.050（placement 列は F09.19 V144.001 で追加） | F09.19 |
| `ad_campaigns` | ✅ V10.051（scope 化は F09.19 V144.005/006） | F09.19 |
| `ad_targeting_rules` | ✅ V10.052 | F09.19 |
| `ad_impressions` | ✅ V10.053（+ V10.077 messaging_campaign_id） | F09.19（計測） |
| `ad_clicks` | ✅ V10.054（+ V10.078 messaging_campaign_id） | F09.19（計測） |
| `ad_daily_stats` | ✅ V10.055（**書き込みバッチは未実装 → F09.19 §7.3 で充填**） | F09.19 |

### API（旧「追加予定 API」→ 実装状況）

- インプレッション/クリック計測: `AdImpressionService` / `AdClickService` / `POST /api/v1/ads/{adId}/click` 実装済み。**新掲載面の計測は F09.19 の `/spotlight/{creativeId}/view|visit` に一本化**（既存 click API は Deprecation 残置）
- クリエイティブ審査: `AdvertiserAdCreativeController` / `SystemAdminAdCreativeController` 実装済み（`ads.status` DRAFT→ACTIVE）
- 運用型キャンペーン CRUD・サービング・審査: **未実装 → F09.19 §6 が設計正本**
- 広告主ダッシュボード: **F09.11** として実装済み。メッセージ型ターゲット配信: **F09.17** として実装済み

### プライバシー・法的対応

- オプトアウト UI・同意管理は F09.17（`user_ad_preferences` / `consented_at` / unsubscribe）として実装済み
- `ad_impressions` / `ad_clicks` の保持期間（90 日）と集約後削除バッチは**未実装のまま残存**。F09.19 §14 の将来課題ではなく本書の未解決事項として §11 に記録する

---

## 11. 未解決事項

| # | 項目 | 状態 |
|---|---|---|
| 1 | `ad_impressions` / `ad_clicks` の 90 日保持・集約後削除バッチが未実装 | F09.19 第1弾で計測流量が発生するため、F09.19.3 実装後の運用課題として別タスク化する（設計は本書 §10 旧記載のとおり: `ad_daily_stats` へ集約完了分から順次物理削除） |

---

## 12. 変更履歴

| 日付 | 変更内容 |
|------|---------|
| 2026-03-18 | 初版作成（スタブから本格設計に書き換え）: DB 1テーブル（affiliate_configs）、API 6本（SYSTEM_ADMIN CRUD 5本 + 公開取得1本）、ビジネスロジック（広告表示判定・SSR 描画・キャッシュ戦略）、セキュリティ（SYSTEM_ADMIN 限定・XSS 防止・レート制限・広告ブロッカー対策）、Flyway V9.001。将来拡張（Phase 10+）の設計方針を記載 |
| 2026-03-18 | 暗黙の未解決事項5件を解決: 広告ブロッカー対策CSS命名を確定（難読化不採用）、FE判定の設計判断理由を補足、deleted_at UNIQUE KEYのマイクロ秒精度を明記、tag_idセキュリティ記述を実態に合わせ修正、display_priorityセカンダリソート（id ASC）を確定 |
| 2026-03-18 | 精査修正（自動）: Valkeyキーを`mannschaft:cache:ads:active`に規約準拠化、SSR描画フローのBE/FE責務矛盾を修正、PUT/PATCH/DELETE/GET一覧の4API仕様を追加、POSTレスポンスにupdated_at追加、エラーレスポンスに404/409を追加、DELETE論理削除レスポンス(200+deleted_at)を明記 |
| 2026-03-18 | 精査修正（判断付き5件）: 個人レベルを広告表示対象に変更（README.md準拠）、公開API(/ads/active)からDB内部id返却を削除、tag_idプロバイダ別バリデーション追加、UNIQUE KEY(provider,placement,deleted_at)をINDEXに変更しアプリ層一意性保証に切替（MySQL NULL非等値問題対策）、CSSクラス名をブロッカー基本フィルタ回避命名に変更 |
| 2026-03-18 | 2次精査修正（自動6件）: エラーコード定義（AFF_001〜004）追加、インデックス名を`idx_affiliate_configs_*`に規約準拠化、キャッシュ無効化にTOGGLE追加、POSTリクエストにdisplay_priority+フィールド補足テーブル追加、XSS防止のBE/FE責務記述修正、providerカラムの不要なDBデフォルト値削除 |
| 2026-03-18 | 2次精査修正（判断付き4件）: 個人ダッシュボードの広告非表示条件追加（所属先プラン準拠）、GET一覧APIにinclude_deletedパラメータ追加、FE Pinia TTLを30分→10分に統一、FlywayをV9.001→V1.074に変更（Phase 1先行作成の番号体系整合） |
| 2026-03-18 | 機能拡充（8件）: 楽天アフィリエイト対応（RAKUTEN プロバイダ・URL構築ルール・バリデーション追加）、バナークリエイティブ管理（banner_image_url/width/height/alt_text 4カラム追加）、有効期間スケジューリング（active_from/active_until 追加）、アップグレードCTA導線、一括トグルAPI（toggle-all）、プレビューAPI、グレースフルデグラデーション（Valkey/DB障害時フォールバック）、運用監視メトリクス+SYSTEM_ADMINダッシュボード連携定義 |
| 2026-07-07 | F09.19 新設に伴う双方向ドリフト解消: §10「将来拡張」を実装状況表に書き換え（ads 系 6 テーブル・計測・審査は実装済み、ad_daily_stats 書込バッチ未実装は F09.19 §7.3 へ）、§5 キャッシュ戦略・広告表示判定フローに未実装注記 + F09.19 への正本移管を明記、placement 語彙を AdPlacement enum（+IN_FEED/DASHBOARD_TILE）に統一（正本 = F09.19 §3）、§7 広告ブロッカー対策を F09.19 §4 中立命名原則（spotlight）に体系化（promo/sponsor 命名は不採用に変更）、§9 Flyway 記載を実体（V9.001/V9.057）に是正、§11 に保持期間バッチ未実装を記録 |
