# F22.1: 市（Market）— 募集ハブ

> **ステータス**: 🟢 設計確定
> **実装フェーズ**: Phase 1（ハブ層・公開閲覧・札の3操作・フレンド宛非公開札）
> **最終更新**: 2026-05-30

---

## 0. この設計書の構成

複合形（F13.1 / F03.5 と同じ分割方式）で構成する。

| ファイル | 内容 |
|---|---|
| `README.md`（本書） | 概要・スコープ・既存資産再利用方針・市の正体・段階ロードマップ・変更履歴 |
| [`01_data_model.md`](01_data_model.md) | DB設計（地域列追加・visibility拡張・フレンド宛先テーブル・confirmable拡張・Flyway・ER） |
| [`02_api_design.md`](02_api_design.md) | API設計（公開市API・地域ファサード・札立て/応じる/下げる/最終認証・エラーコード） |
| [`03_ui_i18n.md`](03_ui_i18n.md) | 画面設計（市一覧・フィルタ連動・パンくず・空状態・ダッシュボード導線・通知文言）・i18n 6言語 |
| [`04_security.md`](04_security.md) | セキュリティ考慮事項（§6相当）・§8 未解決事項・ステータス確定条件 |
| [`payment/`](payment/README.md)（サブ設計） | **Phase 2 後半 謝礼決済（Stripe Connect エスクロー）**。受領者を札ごとに個人/チーム/組織選択・案A（Destination Charge + 手動キャプチャ）・既存 payment 資産の Connect 増築。README/01_data_model/02_api_design/03_security/04_ui_i18n の5本構成 |

---

## 1. 概要

> **追加仕様・段階導入（2026-08-29 御裁可）**: 本書の既存記述で「個人は札主になれない」「市から直接は札を立てられない」とする箇所は、既存のチーム/組織札の挙動を説明するものである。以後は本節以下の Phase 0 仕様を正典として、既存機能を壊さず個人・チーム・組織別の管理市を段階導入する。

「練習試合」「審判募集」「メンバー募集」「助っ人募集」など各種の**募集（札）**が、これまで各チーム/組織/村の画面に散在していた。本機能「**市（いち）**」は、それらを**地域（都道府県・市区町村）×ジャンル**で横断的に探せる**ひとつの市場（マーケット）**として束ねる。

ミクシィの「村（コミュニティ）」が内向きの居場所であるのに対し、「市」は**村の外で人やチームが出会い、募集に応じる外向きの場**である。和風メタファーで操作語彙を統一する。

| 概念 | 語彙 | 実体（既存資産へのマッピング） |
|---|---|---|
| 市（場） | 市（いち） | 地域コード × ジャンルで絞った募集の**論理ビュー**（実体テーブルなし） |
| 募集を出す | **札を立てる** | F03.11 募集の作成→公開（`DRAFT`→`OPEN`） |
| 応募する | **札に応じる** | F03.11 参加申込（`APPLIED`/`CONFIRMED`） |
| 募集を終える | **札を下げる** | F03.11 充足（`FULL`）→最終認証→`COMPLETED` / 期限切れ`AUTO_CANCELLED` / 手動`CANCELLED` |

### 本機能の設計思想 ―「束ねるハブ層」

要件の大半は**既に実装済みの資産で賄える**。したがって本機能は **ゼロから新ドメインを作らず、既存資産を“束ねて見せる”薄いハブ層**として設計する（CLAUDE.md「再利用優先・重複実装禁止」原則）。

| 要件 | 充当する既存資産 | 状態 |
|---|---|---|
| 札の作成/公開/定員/自動キャンセル/テンプレート/配信先/カテゴリ | **F03.11 募集機能** | ✅ 実装済 |
| 応募・キャンセル待ち・自動繰上げ・リマインド | **F03.11**（`recruitment_participants` / `recruitment_reminders` + バッチ） | ✅ 実装済 |
| 最終認証（札主の確認応答） | **F04.9 確認通知**（`confirmable_notifications`） | ✅ 通知基盤は実装済。ただし `source_type`/`source_id` の JPAマッピングが欠落していたため第一陣で追加（乖離A）。`MARKET_FINALIZE` 連携の発火・確認後リスナは第二陣で新規実装 |
| 懇意のチーム（フレンドチーム）宛の非公開札 | **F01.5 フレンドチーム関係**（`team_friends` / フレンドフォルダ） | 🟢 設計済 |
| 都道府県・市区町村マスタ | **既存 `prefectures` / `cities`**（全国約1,900件 seed 済、`PrefectureEntity`/`CityEntity`） | ✅ 実装済 |
| 謝礼決済（Phase 2 後半） | **F13.1 の資金フロー設計図を流用し、市が独自に Stripe Connect 決済基盤を構築**（[`payment/`](payment/README.md) サブ設計） | 🟢 設計確定（実装未着手）。※F13.1 の決済自体は **enum/コメントのみで実装ゼロ**＝設計図のみ流用。受領者は札ごとに個人/チーム/組織を選択 |

新規に作るのは「**地域×ジャンルで束ねて見せる薄い集約層（`com.mannschaft.app.market`）**」と、既存テーブルへの**最小限の拡張（地域列・visibility値・フレンド宛先テーブル）**のみ。

---

## 2. スコープ

### 対象ロール
| ロール | 操作可能な範囲 |
|--------|--------------|
| SYSTEM_ADMIN | 全市の閲覧・モデレーション（不適切な札の取下げ）※モデレーションは F03.11 既存に委譲 |
| ADMIN | 自 scope（チーム/組織）の札立て・札下げ・最終認証・フレンド宛先指定 |
| DEPUTY_ADMIN | `MANAGE_RECRUITMENTS` 権限保有時のみ ADMIN と同等（F03.11 §2 準拠） |
| MEMBER | 市の閲覧・札に応じる（自身が所属するチームでの応募は ADMIN 経由） |
| SUPPORTER | 市の閲覧・札に応じる（visibility に応じる） |
| GUEST（未ログイン） | **公開札の閲覧のみ**（PII 抑制 DTO）。応募はログイン誘導 |

### 対象レベル
- [x] 組織 (Organization) — 札主になれる
- [x] チーム (Team) — 札主になれる
- [ ] 個人 (Personal) — **札主にはなれない**（身元保証のためダッシュボードを持つチーム/組織のみ）。個人は「札に応じる」側として参加

### 札立ての導線制約（重要・セキュリティ）
**札を立てられるのはチーム/組織のダッシュボードからのみ**とする。市の画面から直接は立てられない。

- 理由: 「どこの誰か」が検証済みの scope（チーム/組織）に必ず紐づけ、匿名・身元不明の投稿を**deny-by-default で排除**する。
- 市画面には「札を立てるには自分のチーム/組織ダッシュボードへ」という**導線リンクのみ**を置く（UX 配慮、§03_ui_i18n）。

---

## 3. 市の正体 ―「論理ビュー」

市は**実体テーブルを持たない**。「市」とは `recruitment_listings` を以下で絞り込んだ**保存可能なフィルタ条件（論理ビュー）**である。

```
市 ＝ recruitment_listings
        WHERE visibility = 'PUBLIC'
          AND status IN ('OPEN','FULL')         -- 立っている札のみ
          AND (prefecture_code = :pref  OR :pref IS NULL)
          AND (city_code       = :city  OR :city IS NULL)
          AND (category_id     = :cat   OR :cat  IS NULL)
```

### 階層（県の市 ⊃ 市区町村の市）
`cities.code`（CHAR(5)）の上位2桁が `prefecture_code`（CHAR(2)）に一致する構造を利用し、**県の市は配下市区町村の札をロールアップ**して集約表示する。

```
大分県の市（pref=44）
 ├ 別府市の市（city=44202）   ← 別府市に紐づく札
 ├ 大分市の市（city=44201）
 └ …（配下市区町村）
```

- パンくず: `大分県 ＞ 別府市`
- 「地域なし」の札（地域未指定）は **"地域を問わない" 区画**に集約（§8-1）。
- 命名衝突は起きない（市は実体でなくマスタ駆動の論理ビュー。地域は `cities.code` の一意PKで識別）。「村（village）」とは UI/i18n を明確に分離する。

---

## 4. 段階ロードマップ

| フェーズ | 範囲 | 主な作業 | 本書での扱い |
|---|---|---|---|
| **Phase 1**（本設計の実装対象） | 市の閲覧（未ログイン公開・PII抑制）/ 地域×ジャンルフィルタ・パンくず / ダッシュボードからの札立て導線 / 札に応じる・札を下げる（自動・手動・最終認証）/ 既応募者通知・リマインド / **フレンド宛非公開札（全体・フォルダ・個別の3粒度）** | 地域列追加・visibility拡張・フレンド宛先テーブル・market 集約層・market i18n | ◎ 詳細設計 |
| **Phase 2**（将来） | **Stripe 謝礼決済**（[`payment/` サブ設計](payment/README.md)・🟢設計確定）/ **フリマ**（物品カテゴリ追加）/ teams 所在地文字列→`cities`コード正規化 / 複数地域募集（N:N） | `payment_enabled`/`price` を Connect エスクローに接続（F13.1設計図流用・市が独自構築）、カテゴリ追加、正規化マッパ | ○ 謝礼決済は別サブ設計で詳細化済（実装は別途出陣） |

> Phase 2 は別途 `/軍議` で個別設計する。本書は Phase 2 の**拡張点（フック）が破綻しないこと**だけを保証する。

> **Phase 2 足場C（teams 所在地正規化）進捗 — 第三陣まで実装済（2026-06-01）:**
> - **第一陣（Expand）**: `teams.prefecture_code`/`city_code` 列追加 + 逆引きノーマライザ + `TeamRegionBackfillService`（ドライラン基盤）。
> - **第二陣**: チーム検索のコード化（dual-support＝code 指定時 code 一致・無指定は従来名称一致）/ team 系 DTO へ `prefectureCode`/`cityCode`（camelCase）追加 / scope=TEAM 札立てのサーバ側地域既定補完。
> - **第三陣（本実装）**: FE 検索のコード送信化（`teams/search.vue`・`organizations/[id]/teams/search.vue`）/ チーム作成（`EntityCreateDialog`）・編集（`TeamDetailInfo` 所在地コード編集セクション）の地域コード選択 / 札立てフォーム（`MarketListingFormExtension`）の team 地域コード初期プリフィル / **バックフィルの SYSTEM_ADMIN 管理 UI 起動導線**。
>
> **バックフィル起動手順（SYSTEM_ADMIN 限定）**: 管理画面 `/system-admin/batches`（バッチ起動）に以下 2 バッチが自動登録される（`@BatchEndpoint` ＋ `SystemAdminBatchController` 経由・`/api/v1/system-admin/**` は `hasRole("SYSTEM_ADMIN")`）。
> - `team-region-backfill-dryrun`: **ドライラン（書込なし）**。県/市/未マッチ件数とマッチ率を集計ログ出力。まずこれを実行して結果を確認する。
> - `team-region-backfill`: **本実行**。解決できた行のみ `prefecture_code`/`city_code` を書き込む。冪等（既コード行スキップ）。ドライランで妥当性を確認後に実行する。

**実装依存（フレンド宛非公開札）**: 宛先解決は F01.5 に依存する。`team_friends`（V9.072）/ フレンドフォルダ `team_friend_folders`（V9.073）はいずれも**実装済**。

> **乖離C 是正（第一陣・部隊1 / 2026-05-30）**: 当初「`FOLDER` 粒度はフレンドフォルダ未実装ゆえ gating」と記していたが、`team_friend_folders` は **V9.073 で既に実装済**であることを確認した。よって **`ALL_FRIENDS`/`FOLDER`/`TEAM` の3粒度すべてを Phase 1 で実装する（FOLDER の UI/API gating は不要）**。

---

## 5. 既存設計書への波及（相互参照追記）

本機能の実装に伴い、以下の既存ドキュメントへ最小限の追記を行う（詳細は各章）。

| ドキュメント | 追記内容 |
|---|---|
| `F03.11_recruitment_listing.md` | §3.4 に地域列（`prefecture_code`/`city_code`）、§3.10 にフレンド宛先テーブル、visibility に `FRIEND_TEAMS_ONLY`、関連ドキュメントに F22.1 |
| `F01.5_team_friend_relationships.md` | 「市の非公開札が `team_friends`/フレンドフォルダを宛先解決に参照する」旨を波及修正節に追記 |
| `docs/security/01_authorization_baseline.md` §3.3 | 公開許可リストに `/api/v1/public/market/**`・`/api/v1/public/regions` を追加 |
| `F13.1_short_term_job_matching/README.md` | 「F22.1 が F13.1 の資金フロー設計図を流用し、市向けに独自の決済基盤を構築する」相互参照を追記。**F13.1 の決済は未実装（設計図のみ）である旨も明記** |
| `F22.1_market/payment/`（本サブ設計） | Phase 2 後半 謝礼決済の詳細設計（5本）。本 README §1/§4/ロードマップから参照 |

---

## 6. 関連ドキュメント

- F03.11 募集機能（札の実体・状態遷移・配信・確認通知の正典）
- F01.5 フレンドチーム関係（非公開札の宛先解決）
- F04.9 確認通知システム（最終認証の確認応答UI）
- F19.1 公開ページ・段階開示（未ログイン公開のPII除外DTO・404存在秘匿・公開レート制限・CI禁則ワードテストの前例）
- F15.4 店舗詳細パブリック公開（未ログイン公開の前例）
- F13.1 短期業務マッチング（将来の謝礼決済の委譲先）
- F08.1 マッチング（`prefectures`/`cities` マスタの出自）

---

## 9. 変更履歴

| 日付 | 変更内容 |
|------|---------|
| 2026-05-30 | 初版作成（軍議完了・2周精査完了・🟢設計確定）。市＝既存F03.11/F01.5を束ねるハブ層として設計。地域×ジャンル論理ビュー、フレンド宛非公開札（3粒度）、最終認証、将来Stripe謝礼決済フックを定義 |
| 2026-06-02 | Phase 2 後半 **謝礼決済サブ設計** [`payment/`](payment/README.md) を新設（5本）。**§1 対応表の「将来の謝礼決済 ← F13.1 ✅実装済（委譲フックのみ）」を是正**: 実態は F13.1 決済が enum/コメントのみで実装ゼロ＝設計図のみ流用し、市が独自に Stripe Connect 決済基盤を構築する（受領者は札ごとに個人/チーム/組織選択・案A Destination Charge + 手動キャプチャ）。ロードマップ Phase 2 / §0 構成 / §5 波及を更新 |
| 2026-08-29 | **個人・チーム・組織別の市（Phase 0）を正本化**。公開市は横断閲覧のまま維持し、主体別の管理市・個人札主・公開時の個人情報最小化・個人初期の非決済を追加仕様として段階導入する。 |

---

## 10. 追加仕様: 主体別の市と段階導入（Phase 0 正本）

### 10.1 市の二層構造

`/market` は引き続き全札主区分を横断する**公開閲覧市**であり、未ログインでも公開札だけを閲覧できる。一方、各主体のサイドバーに置く「市」は、その主体が立てた札の作成・履歴・マッチング管理を行う**スコープ管理市**とする。

| 主体 | 管理市 | 表示・管理対象 |
|---|---|---|
| 個人 | `/me/market` | 本人が札主の札のみ |
| チーム | `/teams/[slug]/market` | 当該チームが札主の札のみ |
| 組織 | `/organizations/[slug]/market` | 当該組織が札主の札のみ |

札の属性は混同しない。`scope_type=PERSONAL/TEAM/ORGANIZATION`（札主区分。`PERSONAL` は本追加仕様の Phase 2 で追加）、既存 `participation_type=INDIVIDUAL/TEAM`（応募形式）、既存 `payee_kind=USER/TEAM/ORG`（謝礼受領者）は独立した軸である。

### 10.2 個人札の公開・初期制約

- 個人札主は本人の認証主体からのみ確定し、リクエストで他人の user ID・札主 ID を指定できない。
- `PERSONAL` 札の公開市 DTO は公開プロフィールの**表示名とアイコンのみ**を返す。氏名、メールアドレス、電話番号、住所、内部 ID、連絡先は返さない。
- 個人札では `FRIEND_TEAMS_ONLY` を禁止する。Phase 2〜3 は本人だけが管理できる DRAFT を対象とし、公開市への掲載は Phase 4 で `PUBLIC` の公開範囲・通報・凍結・レート制限を同時に実装してから許可する。
- 個人札は Phase 2〜4 では `payment_enabled=true` をサーバー側で拒否する。Stripe Connect は Phase 5 の別実装であり、自己応募・自己支払いも同時に禁止する。

### 10.3 フェーズ

以下は**主体別管理市の追加仕様に限った導入フェーズ**であり、§4 の既存 F22.1 ロードマップや payment サブ設計の同名 Phase を改番しない。実装・PR・進捗表では「主体別管理市 Phase N」と表記して区別する。

| Phase | 範囲 | 完了条件 |
|---|---|---|
| 0 | 仕様固定、既存のサイドバー誤遷移を再現テスト化、認可・PII・決済禁止方針を確定 | 本節と下記 AC を全設計・テスト計画の正典にする |
| 1 | チーム/組織の管理市導線 | 各サイドバーから現在主体を保って既存作成・履歴・参加者管理へ遷移 |
| 2 | 個人札主バックエンド | `PERSONAL`、本人固定の作成/編集/取消/履歴/認可。DRAFT限定、決済は禁止 |
| 3 | 個人市 UI とマッチング管理 | `/me/market`、マッチング状況、空状態、ページング |
| 4 | 公開市の札主区分、個人向け公開範囲・通知・ペナルティ | `PUBLIC` 掲載、表示名/アイコンだけの公開DTO、通報・凍結・レート制限を同時実装 |
| 5 | 個人札の Stripe Connect | 受領者、返金、口座状態、自己取引禁止を確定して実装 |

### 10.4 受け入れ条件

- AC-01〜03: 各管理市は同じ札主区分だけを表示し、主体を保持して遷移し、チーム/組織の作成は既存画面へ接続する。
- AC-04〜08: 個人札主は本人固定。履歴・マッチング管理は当該主体だけ、未ログインは公開閲覧のみ、越境は 403 または存在秘匿 404 とする。
- AC-09〜11: 個人公開 DTO は PII を含めず、自己応募・自己支払いを禁止し、既存 TEAM/ORGANIZATION の ADMIN/権限保有 DEPUTY_ADMIN の権限を変更しない。
- AC-12〜15: 0件は 200+空一覧と作成導線、途中失敗は原子的に再試行可能、スコープ/状態/地域/カテゴリでページングし N+1 を起こさない。
- AC-16: 個人札の決済有効化は Phase 5 前に必ずサーバー側で拒否する。

既存 `/me/recruitment-listings` は応募・参加履歴であり、札主が立てた札の履歴へ意味を変えない。TEAM/ORGANIZATION の既存一覧・作成 API も後方互換で維持し、主体別管理市は追加エンドポイント/画面として導入する。未知の `scope_type` は公開、作成、認可、決済の全経路で fail-closed とする。
