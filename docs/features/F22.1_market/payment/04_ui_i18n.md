# F22.1 市（Market）統一決済 — 04. 画面設計・i18n

> 親: [README.md](README.md) ／ 関連: [02_api_design.md](02_api_design.md) / [03_security.md](03_security.md)

> i18n 原則（CLAUDE.md）: UI 文字列は**直書き禁止**。`payment` / `market.payment` 名前空間に追加し `$t('key')` 参照。6言語（ja/en/zh/ko/es/de）に追加（未翻訳は ja 値で暫定可）。
>
> **統一基盤・正典更新（2026-06-03）**: (1) **決済確認画面に手数料内訳（額面/支払手数料2.5%/合計）と手数料非返還の注意書きを明示**（§3.1）、(2) **受取側の謝礼/会費設定画面に「受取額＝額面−2.5%（手数料）」を表示**（§2）、(3) 会費（即時モード）も同 UI コンポーネントを共用。
>
> **手数料ランク化・正典更新（2026-06-04）**: 手数料は `fee_policies`（率%＋固定額¥）で解決するため、受取額表示・支払手数料表示は**固定の 2.5% でなく解決した policy の半額**を出す（DEFAULT は従来どおり 2.5%）。**システム管理者向けの手数料パターン管理画面**（`fee_policies` CRUD・§7）を追加。i18n は `market.payment.*`（既存・§6）＋ `systemAdmin.feePolicy.*`（新規・専用ファイル `system_admin_fee_policy.json`・§7.1）。

---

## 1. 口座登録導線（Connect onboarding）

### 1.1 個人の口座登録
- **マイページ → 「謝礼の受取口座」** セクション（`/me/payment/payout-account` 想定）。
- 状態バッジ `<ConnectStatusIndicator>`: `未登録 / 登録中 / 受取可能 / 要対応 / 停止中`（`onboarding_status` に対応）。
- 「口座を登録する」ボタン → `POST /connect/onboarding-link`（scopeKind=USER）→ Stripe hosted onboarding へリダイレクト → return_url で着地し状態再取得。
- `RESTRICTED`（要対応）時は `requirements_due` を平易な文言で提示（「本人確認書類の提出が必要です」等）。

### 1.2 チーム/組織の口座登録
- **チーム/組織ダッシュボード → 「謝礼の受取口座」** セクション（scope ADMIN のみ表示）。
- 「チームの受取口座を登録する」→ `POST /connect/onboarding-link`（scopeKind=TEAM/ORG, scopeId）。
- 非 ADMIN には**セクションを表示しない**（認可・03 §3）。

---

## 2. 札立て時の謝礼設定 ＋ 受領主体選択 UI

札作成フォーム（チーム/組織ダッシュボード）に謝礼ブロックを追加（市から直接立てない原則・F22.1 本体 §2）。

- **「この札に謝礼を設定する」トグル**（`paymentEnabled`）。
- ON で展開:
  - **謝礼額** 入力（`price`・JPY 整数・円単位）。
  - **受領者を選ぶ** ラジオ（`payeeKind`）:
    - 「**個人が受け取る**」（USER）→ 受領者選択（札主チーム所属者から選ぶ・審判/助っ人本人）。例文「審判を担う個人が謝礼を受け取ります」。
    - 「**チームが受け取る**」（TEAM）→ 札主チームの受取口座へ。例文「遠征費などチームの活動費として受け取ります」。
    - 「**組織が受け取る**」（ORG）→ 組織の受取口座へ。
  - **受取口座の状態表示**: 選んだ受領主体の `connect/status` を即時取得。`payouts_enabled=false` なら**警告**「受取口座が未登録です。札は立てられますが、口座登録が完了するまで謝礼は確定（払出）されません」（02 §5.2・症状を隠さず案内）。
  - **受取額の表示（手数料の透明性・正典）**: 額面入力に応じて **「受取額 ＝ 額面 − 2.5%（手数料）」をリアルタイム表示**する。例: 額面 10,000 円入力 →「受取額 9,750 円（手数料 250 円・2.5% を差し引いた額）」（README §3.4 / 02 §3.5）。会費設定画面（チーム/組織）も同表示を共用。
- ADHD 配慮（memory `user_adhd_tendency`）: 受領者は**既定で「個人＝自分」または「チーム」をプリフィル**し、入力摩擦を最小化。詳細は後から編集可能。

---

## 3. 応募時の謝礼提示（支払者側）

- **札詳細・応募ダイアログ**に「謝礼 ¥10,000」（額面）を明示。
- 応募確定で **Stripe Elements（カード直送・PCI SAQ-A・03 §1）** を表示し `clientSecret` で confirm。
- 「**いまは課金されません。最終認証（札の確定）時に確定します**」と**与信＝エスクロー**の説明文を必ず添える（誤解防止）。
- 与信成功で「応募が成立しました（謝礼は確定時にお支払い）」、与信失敗（カード拒否）で原因を提示（実コード `PAYMENT_C041` AUTHORIZATION_FAILED・握りつぶさない）。

### 3.1 決済確認画面の手数料内訳表示（マスター御指示・正典）

カード入力直前の決済確認画面に、**手数料内訳を必ず明示**する（謝礼・会費共通の `<PaymentBreakdown>` コンポーネント）。

```
謝礼（または会費）   10,000円
支払手数料（2.5%）     +250円
─────────────
お支払い合計       10,250円
※お支払い合計には決済手数料が含まれます。
※ご返金の場合、決済手数料が返金されないことがあります（返金条件は募集主の設定によります）。
```

- 金額は `face_amount`（額面）／`round(face×0.025)`（支払手数料）／`amount`（合計）を 02 §3.5 の計算で表示。
- 注意書き（手数料内包・**返金時の手数料は条件付き**）は i18n キー（§6 `market.payment.breakdown.{includesFeeNote,refundFeeConditionalNote}`）で管理（直書き禁止）。**内訳ボックス直下に明示**する。
- **返金経済モデル＝feeBearer 2モード（02 §6.1・マスター確定 2026-06-03）**: 返金時の手数料負担者は**受取側（募集主）の管理者が返金時に選択**する（PAYER=支払者負担／PAYEE=受取側負担）。支払者は決済時点でどちらになるか分からないため、確認画面の文言は**断定でなく条件付き**（「決済手数料が返金されないことがあります（返金条件は募集主の設定によります）」）にする。これにより「全額返金でも手数料が必ず差し引かれる」という誤った断定を避ける。
- 会費の即時決済（F08.2・P2-e）でも同コンポーネントを共用し、ラベルを「会費」に切替える。

---

## 4. 払出・返金の通知文言

| 契機 | 宛先 | 文言キー（例） |
|---|---|---|
| 与信成立 | 支払者 | `market.payment.notify.authorized` 「応募が成立しました。謝礼 {amount} は札の確定時にお支払いされます」 |
| 払出確定（capture） | 支払者・受領者 | `market.payment.notify.captured` 「謝礼 {amount} のお支払いが確定し、受取口座へ送金されました」 |
| 払出保留（HELD） | 札主・受領者 | `market.payment.notify.held` 「受取口座の登録が未完了のため謝礼が保留されています。72時間以内に登録してください」 |
| 保留期限切れで取消 | 支払者・札主 | `market.payment.notify.heldExpired` 「受取口座が未登録のため謝礼を確定できず、与信を取り消しました」 |
| 返金（全額/部分） | 支払者 | `market.payment.notify.refunded` 「謝礼 {amount} が返金されました（理由: {reason}）」 |
| 与信取消（札下げ） | 支払者 | `market.payment.notify.cancelled` 「札が取り下げられたため、謝礼の与信を取り消しました（課金はありません）」 |
| Connect 制限/停止 | 札主・受領者 | `market.payment.notify.accountRestricted` 「受取口座に対応が必要です。謝礼の受け取りには本人確認の完了が必要です」 |

> 通知は F04.9 確認通知 / F04.x 通知基盤に委譲（市で再実装しない）。

---

## 5. 共通コンポーネント（FE）

| コンポーネント | 役割 |
|---|---|
| `<ConnectStatusIndicator :status>` | Connect 口座状態バッジ（未登録/登録中/受取可能/要対応/停止中） |
| `<PayoutAccountSection :scope-kind :scope-id>` | 口座登録セクション（マイページ/ダッシュボード共通） |
| `<PaymentSettingsBlock v-model>` | 札立て/会費フォームの設定ブロック（額面＋受領主体ラジオ＋口座状態警告＋**受取額（額面−2.5%）表示**） |
| `<PayeeSelector v-model :scope-id>` | 受領主体選択（個人/チーム/組織・所属者ピッカー） |
| `<PaymentBreakdown :face-amount :label>` | **決済確認画面の手数料内訳（額面/支払手数料2.5%/合計＋手数料内包・返金条件の注意書き）**。謝礼・会費共用（§3.1） |
| `<EscrowStatusBadge :escrow-id>` | エスクロー状態（与信中/保留中/確定済/返金済） |
| `<RefundDialog :escrow-id v-model:fee-bearer>` | **受取側 ADMIN の返金ダイアログ**。金額（任意=全額）＋**手数料負担者ラジオ（PAYER 既定/PAYEE）**＋各モード説明（`market.payment.refund.feeBearerHint.*`）＋PAYEE 選択時の確認（`confirmPayee`）。受取側 ADMIN にのみ表示（03 §3） |
| `<StripeCardElement @confirmed>` | Stripe Elements ラッパ（カード直送・clientSecret confirm） |

> `any` 禁止（CLAUDE.md）。型は `frontend/app/types/` に手動定義（生成型があれば優先）。

---

## 6. i18n キー骨子（6言語に追加・抜粋）

`frontend/app/locales/{ja,en,zh,ko,es,de}/` の `payment.json`（無ければ新設・`market` 名前空間配下でも可）。

```jsonc
// ja（暫定値・他5言語は同値で追加後に翻訳）
{
  "payment": {
    "payout": {
      "account": "謝礼の受取口座",
      "register": "口座を登録する",
      "status": {
        "PENDING": "未登録",
        "ONBOARDING": "登録中",
        "READY": "受取可能",
        "RESTRICTED": "要対応",
        "DISABLED": "停止中"
      },
      "requirementsDueHint": "本人確認の対応が必要です"
    }
  },
  "market": {
    "payment": {
      "enableToggle": "この札に謝礼を設定する",
      "amount": "謝礼額（円）",
      "receiveAmountHint": "受取額 {amount}（手数料 {fee}・2.5% を差し引いた額）",
      "breakdown": {
        "feeLabel": "謝礼",
        "membershipFeeLabel": "会費",
        "payerSurcharge": "支払手数料（2.5%）",
        "total": "お支払い合計",
        "includesFeeNote": "※お支払い合計には決済手数料が含まれます。",
        "refundFeeConditionalNote": "※ご返金の場合、決済手数料が返金されないことがあります（返金条件は募集主の設定によります）。"
      },
      "refund": {
        "title": "返金",
        "feeBearerLabel": "決済手数料の負担者",
        "feeBearer": {
          "PAYER": "支払者が負担（支払者都合のキャンセル）",
          "PAYEE": "受取側が負担（こちらの都合・中止）"
        },
        "feeBearerHint": {
          "PAYER": "支払者へ受取額（手数料を差し引いた額）を返金します。決済手数料は支払者の負担です。",
          "PAYEE": "支払者へ満額を返金します。決済手数料はこちら（受取側）の負担となり、最終的に次回入金等で精算されます。"
        },
        "submit": "返金する",
        "confirmTitle": "返金の確認",
        "confirmPayee": "受取側負担での返金です。支払者へ満額が返金され、決済手数料はこちらの負担となります。よろしいですか？"
      },
      "payee": {
        "label": "受領者を選ぶ",
        "USER": "個人が受け取る",
        "TEAM": "チームが受け取る",
        "ORG": "組織が受け取る",
        "userHint": "審判・助っ人など個人が謝礼を受け取ります",
        "teamHint": "遠征費などチームの活動費として受け取ります"
      },
      "accountNotReadyWarning": "受取口座が未登録です。札は立てられますが、口座登録が完了するまで謝礼は確定されません",
      "authorizeNotice": "いまは課金されません。最終認証（札の確定）時に確定します",
      "notify": {
        "authorized": "応募が成立しました。謝礼 {amount} は札の確定時にお支払いされます",
        "captured": "謝礼 {amount} のお支払いが確定し、受取口座へ送金されました",
        "held": "受取口座の登録が未完了のため謝礼が保留されています。72時間以内に登録してください",
        "heldExpired": "受取口座が未登録のため謝礼を確定できず、与信を取り消しました",
        "refunded": "謝礼 {amount} が返金されました（理由: {reason}）",
        "cancelled": "札が取り下げられたため、謝礼の与信を取り消しました（課金はありません）",
        "accountRestricted": "受取口座に対応が必要です。謝礼の受け取りには本人確認の完了が必要です"
      },
      "error": {
        "PRICE_REQUIRED": "謝礼額を入力してください",
        "PAYEE_REQUIRED": "受領者を選んでください",
        "PAYEE_USER_REQUIRED": "受領する個人を選んでください",
        "PAYEE_NOT_IN_SCOPE": "選んだ受領者はこの札主に属していません",
        "ONBOARDING_NOT_READY": "受取口座の登録が完了していません",
        "AUTHORIZE_FAILED": "謝礼の与信に失敗しました。カード情報をご確認ください"
      }
    }
  }
}
```

> 通貨表示は既存の通貨フォーマッタ（JPY は小数なし）を用いる。`{amount}` は円整数を `¥5,000` 形式に整形。
> **手数料表示はパターン依存（2026-06-04）**: `receiveAmountHint`/`breakdown.payerSurcharge` の「2.5%」は DEFAULT 前提の固定文言ではなく、解決した policy の半額（率＋固定額の折半）を金額で出す。率を文言に焼き込まず `{fee}`（金額）と任意の `{rateLabel}` で表現する（固定額入りパターンで「2.5%」が誤りになるのを防ぐ）。

---

## 7. システム管理者：手数料パターン管理（`fee_policies` CRUD・P2-f）

| 画面 | パス（Nuxt・想定） | 主体 | 内容 |
|---|---|---|---|
| 手数料パターン一覧/編集 | `pages/system-admin/fee-policies/index.vue` | SYSTEM_ADMIN | `fee_policies` の一覧（policyKey/率/固定額/enabled）・新規/編集/無効化（`DEFAULT` は編集のみ・無効化不可）。02 §11 |
| 手数料割当 | `pages/system-admin/fee-policies/assignments.vue` | SYSTEM_ADMIN | source_kind＋任意 sub_key → policy_key の割当・解除。助っ人＝`recruitment_category` 等 |

- 既存 system-admin 管理画面（`SystemAdminNavFeaturesController` 同型）に倣う。一般ユーザー・テナント管理者には**表示しない**（認可・03 §3）。
- パターン編集時、**安全ガードの想定検証ヒント**（当該 source_kind の想定最小額面に対し total_fee ≤ face か）を表示し、固定額の付けすぎを事前に警告（02 §3.5.2 / §11）。

### 7.1 i18n キー骨子（system-admin・6言語）

> **実装ファイル（R2 #1328 で投入済・正典）**: `frontend/app/locales/{ja,en,zh,ko,es,de}/system_admin_fee_policy.json`（system-admin 文言は専用ファイルに分離）。名前空間は `systemAdmin.feePolicy.*`。以下は実ファイルのキー構成に一致させたもの（実装が正典）。

```jsonc
// system_admin_fee_policy.json（ja・他5言語は同構成で翻訳投入済）
{
  "systemAdmin": {
    "feePolicy": {
      "title": "手数料パターン管理",
      "description": "統一決済の手数料パターン（率＋固定額）と適用先を管理します",
      "policyKey": "パターンキー",
      "displayName": "表示名",
      "percentRate": "率（%）",
      "flatFee": "固定額（円）",
      "enabled": "有効",
      "assignmentCount": "割当数",
      "totalFeeHint": "総手数料 = 率 × 額面 ＋ 固定額（支払者・受取側で折半）",
      "create": "パターンを追加",
      "update": "更新する",
      "disable": "無効にする",
      "defaultImmutable": "標準（DEFAULT）パターンは無効化できません",
      "feeExceedsHint": "この固定額は少額決済で手数料が額面を超える可能性があります",
      "validation": {
        "policyKeyFormat": "パターンキーは英大文字・数字・アンダースコアのみ使用できます",
        "displayNameRequired": "表示名を入力してください",
        "percentRateRange": "率は 0 以上 1 未満で入力してください",
        "flatFeeNonNegative": "固定額は 0 以上で入力してください",
        "feeZeroNotAllowed": "率と固定額を両方 0 にはできません",  // 実装: FEE_POLICY_INVALID_RATE（PAYMENT_C053）
        "alreadyExists": "同じキーのパターンが既に存在します（更新してください）",  // 実装: FEE_POLICY_ALREADY_EXISTS（PAYMENT_C054）
        "notFound": "対象の手数料パターンが見つかりません"  // 実装: FEE_POLICY_NOT_FOUND（PAYMENT_C051）
      },
      "assignment": {
        "title": "手数料の割当",
        "sourceKind": "対象種別",
        "subKey": "細分キー（任意・例: 助っ人カテゴリ）",
        "policy": "適用パターン",
        "add": "割当を追加",
        "remove": "割当を解除",
        "applyNote": "割当の変更は新規の課金にのみ反映されます（既存の取引は変更されません）",
        "duplicate": "同じ条件の割当が既に存在します",  // 実装: FEE_POLICY_ASSIGNMENT_DUPLICATE（PAYMENT_C055）
        "policyDisabled": "割当先の手数料パターンが無効です"  // 実装: FEE_POLICY_ASSIGNMENT_POLICY_DISABLED（PAYMENT_C056）
      }
    }
  }
}
```
