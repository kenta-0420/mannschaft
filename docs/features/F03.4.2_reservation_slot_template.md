# F03.4.2: 枠のライン軸導入＋週間テンプレート一括生成（機能F）

> **ステータス**: 🟢 設計完了（精査: 6観点2パス＋実機E2E耐性クリティック済・2026-07-05）
> **親設計書**: [F03.4 予約管理](F03.4_reservation.md)（機能A〜Dの正本。用語・共通制約は親に従う）
> **実装フェーズ**: v2 **第一弾**（F03.4.1 と同弾。DB土台＋テンプレ生成＋管理UI）
> **最終更新**: 2026-07-05
> **関連枝番**: [F03.4.1 予約メニュー](F03.4.1_reservation_menu.md)、[F03.4.3 予約グループ](F03.4.3_reservation_group_booking.md)（ライン軸枠の消費者）、[F03.4.4 マトリックスUI](F03.4.4_reservation_matrix_ui.md)（ライン軸グリッドの消費者）
> **写経元（実装方針）**: `backend/src/main/java/com/mannschaft/app/schedule/dto/RecurrenceRuleDto.java`（type/interval/daysOfWeek/endType/endDate/count）、`ScheduleRecurrenceService.expandRecurrenceSchedules`（展開・THIS_ONLY/THIS_AND_FOLLOWING/ALL の scope 分岐）、FE 曜日チェックUI `frontend/app/components/schedule/event-form/ScheduleEventRecurrenceInput.vue`、マスタ CRUD 金型 `frontend/app/components/reservation/LineManager.vue`

---

## 1. 概要

予約枠（`reservation_slots`）に**ライン軸（`line_id`）**を導入し、「どの予約対象（席・ベッド・施術台）の枠か」を枠自体が持てるようにする。あわせて、曜日別の**週間テンプレート**（`reservation_slot_templates`）から**30分セル枠を冪等に一括生成**する仕組みを設計する。一括作成の主軸は**週間テンプレート繰返し**であり、単発の bulk API・ドラッグ範囲選択は採らない（ドラッグは既存実装ゼロのため第三弾任意 — マスター御裁可）。

- **ライン上限を 5 → 20 に拡張**する（§5.6 で妥当性精査・推奨値 20 で確定）。
- 生成の入力に **`reservation_business_hours`（曜日別営業時間）** を使う（現状は宣言のみで slot 生成に未使用 — 実測確認済み。本機能で初めて生成に配線する）。
- 既存の休眠足場（`recurrence_rule` / `parent_slot_id` / `is_exception`）は**流用しない（廃止方針）**。判断根拠は §3.3。

> **過去決定の撤回**: 親 §4 の「`POST /reservation-slots/bulk` は Phase 3 スコープ外」は **2026-07-05 マスター御裁可で正式撤回**。ただし後継は単発 bulk API ではなく本テンプレート方式である。

### 実装事実の追認（設計書ドリフトの是正・実測 2026-07-05）

親 §3 の記述と main 実装に以下のドリフトがある。本枝番は**実装事実を正**として設計する:

| 親 §3 の記述 | main の実態（実測） | 本枝番の扱い |
|---|---|---|
| 「`booked_count` がチームの**有効ライン数**に達したら FULL」 | **V140** で `reservation_slots.capacity`（既定 1）が導入済み。FULL 判定・アトミック UPDATE（`incrementBookedCountIfAvailable` / `decrementBookedCountAndReopen`）は **`capacity` 基準**（有効ライン数は使わない） | capacity 基準を正とし、ライン軸枠は capacity=1 を標準とする（§3.1） |
| `uq_rv_slot_line_active`（同一枠×同一ラインの有効予約 UNIQUE） | **実 DDL に不存在**（V3.062 実測。生成カラム方式は設計のみ）。重複ガードはアプリ層 `existsByReservationSlotIdAndUserIdAndStatusIn`（同一枠×同一ユーザー）のみ | ライン軸導入後の重複制御は「ライン軸枠 capacity=1 のアトミック UPDATE」が主防御（§3.1）。UNIQUE 制約の追加是非は F03.4.3 §5 で扱う |
| 繰り返し枠（`recurrence_rule`）の展開・日次バッチ | **展開実装ゼロ**（`createSlot` が文字列を保存するのみ。バッチ・`is_exception` 更新・親子削除は全て未実装） | 休眠足場は流用せず廃止（§3.3） |

---

## 2. スコープ

### 対象ロール

| ロール | 操作可能な範囲 |
|--------|--------------|
| ADMIN / DEPUTY_ADMIN（**role ベース・権限フラグ不問**。`isScopeAdmin` は純ロール判定で `MANAGE_RESERVATIONS` を参照しない — AccessGuard.java:46 実測・既存 `TeamReservationLineController` と同一。DEPUTY_ADMIN は権限有無に関わらず 200） | テンプレートの CRUD・一括生成の実行・ラインの CRUD（上限20） |
| MEMBER / SUPPORTER / 公開ユーザー | 生成された枠の閲覧・予約（既存 API 経由。本枝番に新規会員向け API はない） |

### スコープ外

- ドラッグ範囲選択による枠作成（第三弾・任意）
- テンプレートのチーム間コピー・組織共有
- シフト（F03.5）連動の自動テンプレート

---

## 3. DB設計

### テーブル一覧（変更・追加）

| テーブル名 | 変更内容 | 論理削除 |
|-----------|------|---------|
| `reservation_slots` | **ALTER**: `line_id`（ライン軸・nullable）+ `template_id`（生成元・nullable）+ インデックス/UNIQUE 追加 | 既存のまま |
| `reservation_slot_templates` | **新規**: 週間テンプレート（UUIDv7） | なし（`is_active` で無効化・物理削除可） |
| `reservation_lines` | DDL 変更なし（上限 5→20 は Service 定数・`display_order` 範囲の変更のみ） | 既存のまま |

### 3.1 `reservation_slots` への ALTER（ライン軸＋生成元）

| カラム名 | 型 | NULL | デフォルト | 説明 |
|---------|---|------|-----------|------|
| `line_id` | BIGINT UNSIGNED | YES | NULL | **ライン軸**。FK → `reservation_lines.id` **ON DELETE RESTRICT**（同一ドメイン内FK・クロスドメインではない）。**NULL = 共通枠 = 既存互換**（従来の全枠は NULL のまま） |
| `template_id` | BINARY(16) | YES | NULL | 生成元テンプレート。FK → `reservation_slot_templates.id` **ON DELETE SET NULL**（同一ドメイン。テンプレ物理削除後も生成済み枠は独立して残す）。NULL = 手動作成枠 |

**インデックス・制約（追加）**
```sql
INDEX idx_rs_team_date_line (team_id, slot_date, line_id)          -- ライン軸グリッド・空き検索
UNIQUE KEY uq_rs_template_cell (template_id, slot_date, start_time) -- 生成冪等の最終防御（§5.3）
```

**設計判断の根拠（明記）**

1. **`line_id` は nullable・既存 slot / 既存チームの backfill はしない**。
   - 既存の全 slot は `staff_user_id` 軸（または共通枠）で運用されており、「その枠がどのラインか」を機械的に復元する情報が存在しない（予約行の `line_id` は「ユーザーが選んだライン」であって枠の帰属ではない）。誤った backfill は既存予約の表示・グリッドを壊すため、**backfill しないことを宣言**する。
   - `line_id IS NULL` の枠は従来どおり「共通枠」（予約時にユーザーがラインを選ぶ）。ライン軸枠（`line_id` 非 NULL）は「そのライン専用の枠」（予約時のライン選択は枠から自動決定）。両者は無期限に併存できる（挙動後退ゼロ）。
   - **`reservations.line_id`（NOT NULL・V3.062 実DDL）との関係を固定**: 予約行のラインは既存仕様上**常に必須**である。ライン軸枠の予約では `slot.line_id` がそのまま `reservations.line_id` に入り、**共通枠（`slot.line_id IS NULL`）の予約ではユーザーが選択したラインが `reservations.line_id` に入る**。「枠の帰属が NULL」と「予約行のライン必須」は矛盾しない（前者は枠の属性・後者は予約の属性）。
2. **`line_id` の FK はクロスドメインではない**。`reservation_slots` と `reservation_lines` はともに reservation ドメイン。アーキ原則1（クロスドメインFK禁止）の対象外であり、同一ドメイン内の整合は FK で守る（`reservations.line_id` → `reservation_lines` の既存 FK と同じ判断）。RESTRICT はラインの物理削除防止の番人（運用は論理削除）。
3. **capacity との関係（V140 追認）**: ライン軸枠は「1ライン=1資源」なので **capacity=1 を標準**とする（テンプレの `capacity` 既定 1）。共通枠は従来どおり任意の capacity。FULL 判定・並行制御は既存のアトミック UPDATE 2 本（`incrementBookedCountIfAvailable` / `decrementBookedCountAndReopen`・capacity 基準）を**そのまま使う**（変更しない。F03.4.3 が N 枠ループで流用する）。
4. **`staff_user_id` と `line_id` の併存**: 両軸は排他にしない（ライン軸枠に担当スタッフを併記できる: 例「席1・田中」）。機能B（予約不可枠）の STAFF 軸判定・機能C グリッドの staff 列はそのまま動く（§5.7 波及棚卸し）。

### 3.2 `reservation_slot_templates`（新規）

チームの週間パターン定義。**1行 = 1曜日 × 1連続時間帯 × 1ライン（または共通）**。「週間テンプレート」はチームのテンプレ行の集合であり、生成時に 30 分セルへ分割される（§5.2）。

**新規テーブルのため主キーは UUIDv7（アーキ原則6・`UuidV7Entity` 継承）。`team_id`/`created_by` はクロスドメイン参照のため FK なし（アーキ原則1）。**

| カラム名 | 型 | NULL | デフォルト | 説明 |
|---------|---|------|-----------|------|
| `id` | BINARY(16) | NO | UUIDv7 | PK |
| `team_id` | BIGINT UNSIGNED | NO | — | チームID（FKなし・INDEX。既存 `reservation_slots.team_id`＝V3.061 と同型の UNSIGNED に統一） |
| `name` | VARCHAR(100) | YES | NULL | テンプレ名（管理用メモ。例「平日午前・席1」）。NULL 可（曜日＋時間で自動表示） |
| `line_id` | BIGINT UNSIGNED | YES | NULL | 対象ライン。FK → `reservation_lines.id` ON DELETE RESTRICT（同一ドメイン）。**NULL = 共通枠テンプレ** |
| `day_of_week` | VARCHAR(3) | NO | — | 曜日。**正準は「3文字大文字 `MON`/`TUE`/`WED`/`THU`/`FRI`/`SAT`/`SUN`」で一意固定**（既存 `reservation_business_hours.day_of_week`＝VARCHAR(3)・実DDLと**完全同一表現**。営業時間突合が文字列一致で成立することの根拠）。`MONDAY` 等のフルネーム・小文字は保存禁止（§4 の入口検証で 400） |
| `start_time` | TIME | NO | — | 帯の開始（30分単位: `:00` / `:30`） |
| `end_time` | TIME | NO | — | 帯の終了（30分単位・`start_time` より後） |
| `capacity` | INT | NO | 1 | 生成する各セル枠の定員（V140 の `capacity` へコピー）。ライン軸テンプレは 1 を標準 |
| `staff_user_id` | BIGINT UNSIGNED | YES | NULL | 生成枠の担当スタッフ（任意・FKなし=usersドメイン参照。既存 `reservation_slots.staff_user_id` と同型の UNSIGNED に統一）。NULL = 担当なし |
| `title` | VARCHAR(200) | YES | NULL | 生成枠の `title` へコピー（任意） |
| `price` | DECIMAL(10,2) | YES | NULL | 生成枠の `price` へコピー（表示用・任意） |
| `approval_mode` | VARCHAR(10) | YES | NULL | 生成枠の枠単位承認モード上書きへコピー。NULL = チーム既定（親 §3 の解決規則） |
| `is_active` | BOOLEAN | NO | TRUE | FALSE のテンプレは生成対象外（既生成枠は不変） |
| `created_by` | BIGINT UNSIGNED | YES | NULL | 作成者（FKなし。既存 `reservation_slots.created_by` と同型の UNSIGNED に統一） |
| `created_at` | DATETIME(6) | NO | — | |
| `updated_at` | DATETIME(6) | NO | — | |

**インデックス**
```sql
INDEX idx_rst_team (team_id, day_of_week, start_time)   -- チーム別テンプレ一覧・生成スキャン
INDEX idx_rst_line (line_id)                              -- ライン削除時の参照チェック
```

**制約・備考**
- **上限: 1チームあたり最大 500 行**（RESERVATION_037）。試算根拠: 20ライン × 7曜日 × 帯3本/日 = 420 行 < 500（フル運用チームを包含しつつ生成暴走を防ぐ）。
- `start_time`/`end_time` は 30 分単位・`start < end`・最小 30 分（既存の枠バリデーション `INVALID_TIME_RANGE`=007 / `INVALID_SLOT_GRANULARITY`=022 を**再利用**。テンプレ専用コードは設けない）。
- **同一 (team, line_id, day_of_week) 内の時間帯重複は 400**（Service 層検証・再利用 007）。重複テンプレは同一セルを二重生成しようとして UNIQUE 制約エラーになるため入口で止める。共通枠テンプレ（line_id NULL）同士の重複は**許可**（既存の「共通枠同士の重複許可」と整合・親 §3）。
- 定休日（`reservation_business_hours.is_open=FALSE`）の曜日のテンプレ**作成は許可**する（後で営業日に変わる運用があるため）。生成時にスキップされるだけ（§5.2）。既存親設計の「定休日曜日の繰り返し作成は 400」より緩和し、**チェックは生成時の一点に集約**する（二重管理しない）。

### 3.3 休眠足場（`recurrence_rule` / `parent_slot_id` / `is_exception`）の扱い — **廃止（不流用）を確定**

**実コード確認結果（2026-07-05 実測）**:
- `recurrence_rule`: `CreateSlotRequest.recurrenceRule`（String）→ `createSlot` がエンティティへ**保存するのみ**。展開ロジック・日次バッチ・`generated_slots_count` 応答は**一切実装されていない**。FE の予約系コンポーネントから送信している箇所もない（`SlotFormDialog.vue` 等に参照なし・型定義のみ）。
- `parent_slot_id`: リポジトリに `findByParentSlotIdOrderBySlotDateAsc` が宣言されているが**呼び出し元ゼロ**。self-FK（ON DELETE RESTRICT）だけが存在。
- `is_exception`: 常に既定 FALSE で INSERT され、TRUE にする経路が存在しない。

**判断: 機能F はこの3点を流用せず、テンプレート方式（別テーブル）を正とする。**

| 観点 | 休眠足場の流用（不採用） | テンプレート方式（採用） |
|---|---|---|
| 管理 UI | ルールが slot 行の JSON に埋まり、一覧・編集・無効化の UI 操作に不向き | テンプレは独立行。一覧・編集・ON/OFF が素直な CRUD |
| ライン軸 | 親枠複製方式では「20ライン×曜日別」の帯定義が親枠の爆発になる | 1行=曜日×帯×ラインで正規化 |
| 例外編集 | `is_exception`/THIS_AND_FOLLOWING の伝播実装が丸ごと必要（実装ゼロから） | 生成後の枠は**独立した通常枠**。個別編集は既存の枠 CRUD がそのまま使える（伝播概念なし） |
| 削除連鎖 | self-FK RESTRICT で親削除が子に阻まれる運用複雑性 | `template_id` は SET NULL。テンプレ削除で枠は孤立せず残る |
| 冪等 | `uq_rs_parent_date` は未作成（実DDL不存在） | `uq_rs_template_cell` を新設（§5.3） |

**廃止の実施内容**:
1. **第一弾**: **入力側のみ削除**（B7 で範囲確定）— `CreateSlotRequest.recurrenceRule` を OpenAPI から削除（`@Schema(deprecated)` を経ず即削除。FE 送信箇所ゼロ・保存されても機能しない dead input のため破壊なし）。`createSlot` の `recurrenceRule` 保存行を削除。**応答側（`ReservationSlotResponse.RecurrenceDto` の `recurrenceRule`/`parentSlotId`）は後方互換のため残置**する（既存クライアント/生成型が参照し得るフィールドの削除は破壊変更。新規保存が止まるため値は既存行由来を除き実質 null になっていき、フィールド削除は将来のクリーンアップ（下記 2 と同時・要裁可）で行う）。
2. **カラム自体（`recurrence_rule`/`parent_slot_id`/`is_exception`）と self-FK は残置**（DROP しない）。理由: 既存行のデータ保全・DROP は後戻り不能でリスクに見合う利益がない。将来のクリーンアップ migration（別 PR・要裁可）で DROP を検討する旨を親 §8 バックログに残す。
3. リポジトリの未使用メソッド `findByParentSlotIdOrderBySlotDateAsc` を削除。
4. 親 §3 の「繰り返しルール（recurrence_rule）」節は**将来形として据え置き**（本枝番が正であることを親の枝番一覧から辿れる）。

### 3.4 `reservation_lines` 上限 5→20

DDL 変更なし（`display_order INT` のまま・範囲制約は Service 層）。変更点:

| 箇所 | 変更 |
|---|---|
| `ReservationLineService.MAX_LINES_PER_TEAM` | `5L` → `20L`（実定数・実在確認済み） |
| `display_order` 許可範囲 | 1〜5 → **1〜20**（`MIN_DISPLAY_ORDER`/`MAX_DISPLAY_ORDER`） |
| `ReservationErrorCode.LINE_LIMIT_EXCEEDED`（024）のメッセージ | 「最大5本」→「最大20本」（コードは再利用・番号変更なし） |
| `INVALID_DISPLAY_ORDER`（025）のメッセージ | 「1〜5」→「1〜20」 |
| **`ReservationLineServiceTest` の境界値ハードコード** | **5/6 境界の既存 UT が実在**（`ReservationLineServiceTest` L174/192/208 付近: 「5本目まで可・6本目拒否・display_order 範囲」）。定数だけ変えると**既存 green が割れる**ため、同テストの境界値を **20/21 へ同時改訂**する（19本目可・20本目可・21本目拒否・display_order 20 可/21 不可に書き換え。F-10 が改訂後の番人） |
| i18n（`reservation.*` の上限文言） | 6言語で 5→20 |
| 親 §1/§2/§3 の「最大5本」記述 | 本枝番を正とする（親は枝番一覧から参照） |

**推奨値 20 の妥当性精査（確定根拠）**:
1. **業態カバレッジ**: 飲食テーブル数（小規模店 5〜15 卓）・美容室セット面（3〜12 席）・整骨院ベッド（3〜10 台）・マンション共用施設（2〜8 室）の P90 を包含。20 超が必要な業態（大型飲食・複合施設）はチーム分割（フロア別・施設別）が運用上も自然。
2. **マトリックスUI の実用限界**: 列=時間軸の場合、縦に並ぶ「日付×予約対象」行は **7日 × 21 列（20 ライン＋共通列）= 147 行**が描画・スクロールの実用上限（F03.4.4 §5 と同一のカウント方式・モバイル横スクロール規約と両立）。
3. **生成行数**: §11 の試算どおり 20 ラインのフル運用でも horizon 内 slot 行はチームあたり約 1.3 万行で、`(team_id, slot_date)` インデックス下の日次クエリは数百行に収まる。
4. **50 や 100 にしない理由**: 上限は後から上げるのは容易だが下げるのは既存データがあると不可能。UI 実用限界と業態 P90 を満たす最小の切りの良い値として 20 で確定する。

### ER図（追加分）

```
reservation_lines (1) ──── (N) reservation_slots           [line_id, nullable=共通枠]      ★機能F
reservation_slot_templates (1) ──── (N) reservation_slots  [template_id, SET NULL]          ★機能F
reservation_lines (1) ──── (N) reservation_slot_templates  [line_id, nullable]              ★機能F
teams (1) ──── (N) reservation_slot_templates              [FKなし]                         ★機能F
reservation_business_hours ──(生成入力)──> reservation_slot_templates 生成バッチ            ★機能F
```

---

## 4. API設計

### エンドポイント一覧（追加）

| メソッド | パス | 認証 | 説明 |
|---------|-----|------|------|
| GET | `/api/v1/teams/{teamId}/reservation-slot-templates` | ADMIN+ | テンプレ一覧（曜日・ライン別） |
| POST | `/api/v1/teams/{teamId}/reservation-slot-templates` | ADMIN+ | テンプレ作成（最大500行） |
| PATCH | `/api/v1/teams/{teamId}/reservation-slot-templates/{templateId}` | ADMIN+ | テンプレ更新（部分更新） |
| DELETE | `/api/v1/teams/{teamId}/reservation-slot-templates/{templateId}` | ADMIN+ | テンプレ物理削除（生成済み枠は SET NULL で残置） |
| POST | `/api/v1/teams/{teamId}/reservation-slot-templates/generate` | ADMIN+ | **一括生成**（チームの active テンプレ全件を対象・冪等） |

- 全 5 本とも `@PreAuthorize("@accessGuard.isScopeAdmin(authentication, #teamId, 'TEAM')")`（self-gate・実シグネチャ3引数）。
- 既存の枠 CRUD（`/reservation-slots`）は変更最小: `CreateSlotRequest`/`UpdateSlotRequest` に `lineId`（nullable）を追加、`ReservationSlotResponse` に `lineId`/`lineName`/`templateId` を追加、`recurrenceRule` を削除（§3.3）。

### `POST /api/v1/teams/{teamId}/reservation-slot-templates`

**リクエストボディ**
```json
{
  "name": "平日午前・席1",
  "lineId": 1,
  "dayOfWeek": "MON",
  "startTime": "10:00",
  "endTime": "13:00",
  "capacity": 1,
  "staffUserId": 456,
  "title": null,
  "price": null,
  "approvalMode": null
}
```

| フィールド | 型 | 必須 | バリデーション |
|-----------|---|------|-------------|
| `name` | String | No | 最大100文字 |
| `lineId` | Long | No | 当該チームの active ライン。NULL = 共通枠テンプレ。不正 ID は 400（LINE_NOT_FOUND=001 再利用） |
| `dayOfWeek` | String(enum) | Yes | **3文字大文字 `MON`..`SUN` のみ**。検証方式を具体化（従来の「Jackson enum 400」は enum 実体が未定義で内部矛盾だった）: **reservation パッケージに専用 enum `ReservationDayOfWeek { MON, TUE, WED, THU, FRI, SAT, SUN }` を新設**し request DTO のフィールド型にする → 不正値（`MONDAY`/小文字/その他）は Jackson の enum デシリアライズ失敗で 400 になる（これで「Jackson enum 400」が実体を伴って成立）。DB へは `name()`（=3文字）で保存。**`java.time.DayOfWeek` は使わない**（`MONDAY` フルネームで business_hours の VARCHAR(3) と表現がズレるため） |
| `startTime` / `endTime` | String(time) | Yes | 30分単位・start < end・最小30分（007/022 再利用） |
| `capacity` | Integer | No | 1〜99。省略時 1 |
| `staffUserId` | Long | No | チーム所属メンバー（親 §3 の所属チェックと同じ） |
| `title` | String | No | 最大200文字 |
| `price` | BigDecimal | No | 0 以上 |
| `approvalMode` | String(enum) | No | `AUTO`/`MANUAL`。NULL = チーム既定継承（親の枠単位上書きと同義） |

> request DTO は親 §4.B の Jackson 手本（`@Getter @RequiredArgsConstructor`＋全 final＋単一コンストラクタ）に倣う。既定値（capacity=1）は Service 層で null→1 正規化（既存 `normalizeCapacity` と同じ考え方）。

**レスポンス（201 Created）**
```json
{
  "data": {
    "id": "0198zz...uuid",
    "name": "平日午前・席1",
    "lineId": 1,
    "lineName": "席1",
    "dayOfWeek": "MON",
    "startTime": "10:00",
    "endTime": "13:00",
    "capacity": 1,
    "staffUserId": 456,
    "staffName": "田中スタッフ",
    "title": null,
    "price": null,
    "approvalMode": null,
    "isActive": true,
    "cellCount": 6,
    "createdAt": "2026-07-05T10:00:00+09:00"
  }
}
```

- `cellCount` = この帯が 1 日あたり生成する 30 分セル数（`(end - start) / 30`。BE 導出）。
- GET 一覧は上記の配列＋`meta.totalTemplates`/`meta.limit`（500）を返す（FE の上限表示用）。

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 400 | 時刻不正（007/022）/ dayOfWeek 不正 / lineId 不正（001）/ 同一 (line, 曜日) 内の時間帯重複（007 再利用・メッセージで区別） |
| 400 | テンプレ上限 500 行到達（RESERVATION_037） |
| 401 / 403 | 未認証 / ADMIN+ 以外 |

### `PATCH /api/v1/teams/{teamId}/reservation-slot-templates/{templateId}`

部分更新（null=据え置き）。`isActive` の切替を含む。`clearLineId: true` で共通枠テンプレへ戻す（親 `clearApprovalMode` と同形）。**更新は既生成枠へ遡及しない**（§5.4）。

**エラーレスポンス**: POST と同じ＋404（テンプレ不存在 = RESERVATION_036。他チームは IDOR 秘匿の同一 404）。

### `DELETE /api/v1/teams/{teamId}/reservation-slot-templates/{templateId}`

物理削除。生成済み枠は `template_id` が SET NULL になり**通常枠として残る**（予約が入っている可能性があるため枠は消さない）。未来の未予約枠も自動削除しない（枠削除は既存の枠 DELETE API で明示的に行う運用）。

**レスポンス（200 OK）**: `{ "data": { "id": "...", "deleted": true, "orphanedSlotCount": 42 } }`（`orphanedSlotCount` = SET NULL された枠数・情報提供のみ）

### `POST /api/v1/teams/{teamId}/reservation-slot-templates/generate`

チームの **active テンプレ全件**を対象に、明日〜horizon（既定 28 日先）までの枠を生成する（冪等・§5.3）。

**リクエストボディ**
```json
{ "weeks": 4 }
```

| フィールド | 型 | 必須 | バリデーション |
|-----------|---|------|-------------|
| `weeks` | Integer | No | 1〜4。省略時 4（=28日先まで） |

**レスポンス（200 OK）**
```json
{
  "data": {
    "generatedCount": 288,
    "skippedExistingCount": 96,
    "skippedClosedDayCount": 24,
    "skippedOutsideHoursCount": 4,
    "horizonFrom": "2026-07-06",
    "horizonTo": "2026-08-02"
  }
}
```

| フィールド | 説明 |
|---|---|
| `generatedCount` | 新規 INSERT された枠数 |
| `skippedExistingCount` | 冪等スキップ（既に同一セルが存在） |
| `skippedClosedDayCount` | 定休日（`is_open=FALSE`）でスキップされたセル数 |
| `skippedOutsideHoursCount` | 営業時間外にはみ出してスキップされたセル数 |

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 400 | `weeks` 範囲外（Bean Validation） |
| 400 | active テンプレが 0 件 — これは入力不正ではなく**状態検証**のため Bean Validation ではなく Service 層の `BusinessException`（汎用 400・新規コードなし・メッセージ「有効なテンプレートがありません。先にテンプレートを作成してください」）で返す |
| 401 / 403 | 未認証 / ADMIN+ 以外 |

---

## 5. ビジネスロジック

### 5.1 実装方針（クラス構成）

| 層 | 新規クラス | 役割 |
|---|---|---|
| Controller | `ReservationSlotTemplateController` | §4 の 5 エンドポイント |
| Service | `ReservationSlotTemplateService` | テンプレ CRUD・重複/上限検証 |
| Service | `ReservationSlotGenerationService` | **生成の単一実装**（手動 generate と日次バッチの両方が呼ぶ・別実装厳禁） |
| Batch | `ReservationSlotGenerationBatchService` | `@Scheduled(cron = "0 15 0 * * *")`（日次 AM 0:15 JST）＋ `@SchedulerLock`（多重起動防止・既存 `ReservationReminderDispatchBatchService` の写経） |
| Entity/Repo | `ReservationSlotTemplateEntity extends UuidV7Entity` / `ReservationSlotTemplateRepository` | `countByTeamId` / `findByTeamIdAndIsActiveTrue` / `findDistinctTeamIds`（バッチの対象チーム列挙） |

- 繰り返し展開の date 反復は `ScheduleRecurrenceService.expandRecurrenceSchedules` の曜日反復（WEEKLY・daysOfWeek）を**写経**する。ただし schedule の RecurrenceRule JSON は持たず、テンプレ行の `day_of_week` を直接使う（endType/endDate/count の概念は horizon に置換されるため不要）。
- 更新スコープ（THIS_ONLY/THIS_AND_FOLLOWING/ALL）は**導入しない**: 生成後の枠は独立した通常枠で、伝播概念がない（§3.3 の設計判断）。

### 5.2 生成アルゴリズム（セル分割・営業時間突合）

```
generate(teamId, dateRange):                             # dateRange: 手動 generate = horizon 全域 / 日次バッチ = 差分日のみ（§5.4）
  horizon 全域の既定 = [tomorrow, tomorrow + weeks*7 - 1] # 当日は生成しない（当日枠は手動作成の領分）
  businessHours = getBusinessHours(teamId)               # 曜日→(is_open, open_time, close_time)。行の無い曜日はキー欠損
  templates = findByTeamIdAndIsActiveTrue(teamId)
  existingCells = dateRange 内の (template_id, slot_date, start_time) 組を1クエリで先読みし Set 化   # §5.3・N+1回避
  for date in dateRange:
    dow = to3Letter(date)   # ★表現一致（B1）: java.time.DayOfWeek.name() は 'MONDAY' フルネームのため、
                            #   date からの導出は必ず正準の3文字大文字（'MON'..'SUN'）へ変換してから
                            #   templates.day_of_week / business_hours.day_of_week と突合する
                            #   （DayOfWeek.name().substring(0,3) で MON/TUE/WED/THU/FRI/SAT/SUN に一致）
    bh = businessHours.get(dow)                          # ★行なしなら null
    for tpl in templates where tpl.day_of_week == dow:
      if bh == null or not bh.is_open
         or bh.open_time == null or bh.close_time == null:
        skip（skippedClosedDayCount += cellCount）; continue
        # ★営業時間の防御分岐（NPE 根絶・確定仕様）。以下の2ケースを定休日と同列にスキップする:
        #   (1) 当該曜日の行が存在しない — reservation_business_hours は新規チームで 0 行があり得る（実測）
        #   (2) is_open=TRUE だが open_time / close_time が NULL — V3.063 実 DDL は時刻 NULL 許容
        # いずれも「営業時間が定義されていない日」として skippedClosedDayCount に加算。
        # 既定営業時間（9:00-18:00 等）へのフォールバック生成はしない（管理者が意図しない枠を勝手に作らない）
      for cellStart in [tpl.start_time, tpl.end_time) step 30min:       # ★30分セル分割
        cellEnd = cellStart + 30min
        if cellStart < bh.open_time or cellEnd > bh.close_time:
          skip（skippedOutsideHoursCount++）; continue                   # 営業時間外セル
        if (tpl.id, date, cellStart) in existingCells:
          skip（skippedExistingCount++）; continue                       # 冪等（§5.3・先読み Set とメモリ突合）
        INSERT reservation_slots(
          team_id, line_id=tpl.line_id, staff_user_id=tpl.staff_user_id,
          template_id=tpl.id, slot_date=date,
          start_time=cellStart, end_time=cellEnd,
          capacity=tpl.capacity, title=tpl.title, price=tpl.price,
          approval_mode=tpl.approval_mode, created_by=<実行者 or NULL(バッチ)>)
        generatedCount++
```

- **30分セル分割が標準**: テンプレの帯（例 10:00〜13:00）は 30 分セル 6 枠に分割して生成する。マトリックスUI（F03.4.4）と予約グループの連続確保（F03.4.3）は 30 分セルを前提とするため。手動作成の長尺枠（60分等）は従来どおり作成可能だが、メニュー連続確保の対象外（単枠予約のみ・F03.4.3 §5）。
- **予約不可枠（機能B）は生成時にチェックしない（確定方針）**: 機能B の enforcement は「runtime overlap ユーティリティ1本」（親 §5.B・別実装厳禁）。生成時にもスキップすると「ブロック解除後に枠が存在しない」穴と二重管理が生じる。枠は生成し、表示/予約時に runtime 判定で落とす（既存 3 箇所の判定がそのまま効く）。
- **営業時間の後変更**: 生成済み枠への遡及 CLOSE は行わない（既存実装も `BUSINESS_HOURS` 自動 CLOSE は未配線 — 親 §8 バックログの範疇）。生成時点の営業時間だけを入力に使う。
- **トランザクション**: 手動 generate（weeks=4）の最悪 INSERT 数は **13,440 行/チーム**（20ライン×24セル×28日・§11 と同一試算）。巨大単一 tx を避けるため、**日付単位のチャンク tx**（1日 = 最大 480 INSERT = 1 tx）で実装する。チャンク間で失敗しても、冪等キー（§5.3）により **generate の再実行が安全**（既生成分は先読み Set / UNIQUE でスキップされ、二重生成も欠損も起きない）ことがチャンク分割を許す根拠である。バッチは行単位 try/catch ではなく**チーム単位** try/catch（1チームの失敗が他チームを巻き込まない・`log.error` で正直に記録。チーム内は日付チャンクごとにコミット済み分が残る=再実行で追い付く）。

### 5.3 冪等性（同一テンプレ再生成で重複枠を作らない）

- **一意キー**: `UNIQUE KEY uq_rs_template_cell (template_id, slot_date, start_time)`。
  - 1テンプレ行は 1 日に同じ `start_time` のセルを 1 つしか作らないため、この 3 つ組で生成セルが一意になる。
  - `template_id IS NULL`（手動枠）は MySQL の UNIQUE 仕様（NULL は重複比較で distinct）により**制約の対象外**＝手動枠の自由度を奪わない。
- **スキップ規則（アプリ層一次判定・一括先読み）**: セル単位の `existsBy...` を都度発行すると最悪 13,440 クエリの N+1 になるため、**対象 dateRange の既存 (template_id, slot_date, start_time) 組を 1 クエリで先読みして Set 化し、メモリ突合でスキップ**する（§5.2 の `existingCells`。専用クエリ `findGeneratedCellKeysByTeamIdAndSlotDateBetween` を新設）。並行実行（手動 generate と日次バッチの競合）で先読みをすり抜けた場合は **UNIQUE 制約違反を捕捉してスキップ扱い**（DB が最終防御。`DataIntegrityViolationException` → skippedExistingCount。エラーにしない）。
- **テンプレ変更後の再生成**: テンプレの時刻・capacity を変更しても**既生成枠は更新しない**（予約が入っている可能性があり、黙って変えると予約者との合意を壊す）。変更後の初回生成から新定義が効く。既生成の未予約枠を新定義に合わせたい場合は「未来の未予約枠を削除（既存枠 DELETE・active 予約ありは既存 409=RESERVATION_020）→ 再生成」の明示 2 操作（UI にこの手順をガイド表示: i18n `reservation.template.regenerate_guide`）。
- **`slot_date`+`start_time` が同じでもテンプレが異なれば別枠**: 同一ライន同一時刻の二重定義はテンプレ作成時の重複検証（§3.2）で入口拒否するため、実運用では発生しない（共通枠テンプレ同士は許可＝意図的な並行枠）。

### 5.4 日次バッチ（horizon 延伸）

- `@Scheduled(cron = "0 15 0 * * *", zone = "Asia/Tokyo")` + `@SchedulerLock(name = "reservationSlotGeneration")`。リマインドバッチ（1分間隔）と時間帯を分離。
- 対象: `reservation_slot_templates` に active 行を持つ全チーム（`findDistinctTeamIds`）。
- **バッチの生成対象は「テンプレごとの差分レンジ」**（精査2パス A3 で是正）: 固定「horizon 末尾 1 日のみ」では、①手動 generate を `weeks=1` で実行したチームの day8〜27 が**永久に空白**になる ②generate 実行後に作成された新規テンプレの day1〜27 が**永久に未生成**になる、という恒久生成穴が生じる。是正後のバッチは**テンプレ 1 行ごとに差分レンジを計算**して生成する:
  - `lastGeneratedDate(tpl)` = そのテンプレが生成したセルの最終日を **horizon 上限でクランプして**導出する: `SELECT MAX(slot_date) FROM reservation_slots WHERE template_id = :tplId AND slot_date <= :tomorrow + 27日`（専用カラムは持たず導出 — 生成実績そのものが正であり、二重管理しない）。生成実績ゼロ（新規テンプレ）は NULL。
    - **クランプ条件の追補（2026-07-08・F03.4.5 §3.3.2 対応）**: F03.4.5 の単日テンプレ適用 API（臨時営業・対象日は最大 +90 日）は horizon 外に `template_id` 付きセルを生成し得る。クランプなしの素の `MAX(slot_date)` だとウォーターマークが horizon 外へ跳ね、`range = [max(tomorrow, MAX+1), tomorrow+27日]` が**恒久に空**になって当該テンプレの通常週次枠が最大 2 ヶ月生成されない欠落が起きる。`slot_date <= tomorrow + 27日` の WHERE 句クランプにより、horizon 外の単日生成はウォーターマークに影響せず、差分レンジは horizon 内の実績のみで決まる（F03.4.5 AC S-8 が番人）。
  - `range(tpl) = [max(tomorrow, lastGeneratedDate + 1), tomorrow + 27日]`（NULL なら `[tomorrow, tomorrow + 27日]` の全域）。range が空（`lastGeneratedDate >= tomorrow + 27日`）ならそのテンプレはスキップ。
  - 通常運転（毎日実行・全テンプレ生成済み）では range = 末尾 1 日に自然収束し、weeks=1 チーム・新規テンプレは初回バッチで残り全域が埋まる。**冪等キー（§5.3）が二重生成を最終防御**するため、lastGeneratedDate の導出が並行 generate とズレても安全。
  - 先読み（§5.3 の existingCells）は**チーム単位で `[tomorrow, tomorrow + 27日]` を 1 クエリ一括**のまま（テンプレごとにクエリを割らない — N+1 回避の方式と整合）。
  - バッチ停止・障害からの復旧もこの差分レンジが自動で追い付く（手動 generate は即時反映用の任意手段に格下げ）。
- `@Scheduled`＋`@Transactional` 境界は最小 context 実起動の番人テストで恒久検証（既存 `ReservationReminderDispatchBatchServiceContextTest` の写経・親 §3 の既知地雷対策）。
- **テンプレ CRUD 直後の horizon 薄さ対策（UI 動線）**: ~~テンプレを作成/更新しても翌 0:15 のバッチまで枠は生成されない。FE は**テンプレ CRUD 成功時のトースト/ガイドに「今すぐ枠を作成」ボタン（generate 呼び出し）を常設**し、`reservation.template.regenerate_guide`（§5.3 の再生成手順ガイド）と統合した 1 つの案内カードで提示する（「変更を枠に反映するには: ①未来の未予約枠を削除（必要な場合）②枠を作成ボタン」）。テンプレ一覧画面にも generate ボタンを常設する（§4 の 5 本目）。~~
  **→ [F03.4.5](F03.4.5_reservation_schedule_first.md) §3.1 で改訂（2026-07-08・マスターフィードバック起点）**: テンプレ create/update 成功時に**当該テンプレのみを対象とした同期自動生成（horizon 28日）**を実行する方式へ変更。「今すぐ枠を作成」ボタンと `weeks` 選択 UI は**廃止**（`POST .../generate` API 自体は互換のため残置＋OpenAPI `deprecated: true`。`weeks` パラメータも API 上残置）。`regenerate_guide` は「変更は保存時に自動反映（追加分のみ）。既存未予約枠を新定義に合わせたい場合は枠削除→再保存」の文言へ改訂。日次バッチの差分レンジ生成（本節上記）は無変更で整合する（自動生成が `MAX(slot_date)` を前進させ、バッチは末尾差分へ自然収束）。

### 5.5 ライン削除フロー（機能E/F 連動・**精査2パス A1 で再設計**）

> **旧設計の欠陥（撤回）**: 当初は「未来のライン軸枠あり → 409」ガードと「テンプレ `is_active=FALSE` 化は**ライン削除成功時**の副作用」としていたが、これは**循環デッドロック**になる — テンプレが毎晩枠を生成し続ける限り 409 が常に成立し、削除は永遠に不能・テンプレも止まらない。**生成停止をガードより前に置き、予約のない枠は削除処理が自ら片付ける**設計へ再設計した。

ライン論理削除（`DELETE /reservation-lines/{id}`）は**単一トランザクション内で以下の番号順**に実行する:

```
1. 生成停止（★枠存在ガードより前・第一手順）:
   当該ラインを line_id に持つ active テンプレを全て is_active=FALSE 化
2. 409 ガード（唯一のガード・事由を限定）:
   当該ラインに active 予約（reservations.line_id = 対象ライン AND status IN (PENDING, CONFIRMED)）
   が存在する場合のみ 409
   - メッセージ:「このラインには有効な予約があります。先に振替またはキャンセルしてください」
   - ★「予約のない未来のライン軸枠が存在する」ことは 409 事由にしない（旧設計の循環の根を除去）
3. 予約なし未来枠の自動 purge:
   line_id = 対象ライン AND slot_date >= 今日 AND active 予約が紐づかない枠を一括論理削除
   （予約が紐づく枠・過去枠は履歴として残す）
4. 当該ラインを参照する reservation_menu_lines 行をアプリ層で削除（F03.4.1 §3 の RESTRICT 判断に対応）
5. ライン本体を論理削除（deleted_at セット）
```

- **1→2 の順序が本質**（生成停止が先・ガードが後）。2 で 409 になった場合は tx ロールバックで 1 も巻き戻るが、管理者が予約を振替 → 再実行すれば 1〜5 が一気に通る（再実行可能）。409 の間に翌晩の生成が走っても増えるのは「予約のない枠」だけであり、次回実行の 3 が purge する — **追いかけっこが構造的に終端する**。
- 3 の purge と冪等キー: 論理削除行も `uq_rs_template_cell` の対象に残るが、テンプレは停止済みのため同一セルの再生成は起きない。将来テンプレを再 ON した場合の UNIQUE 衝突は生成側の「UNIQUE 違反 → スキップ」（§5.3）で吸収される。
- UI ガイド（i18n `reservation.line.delete_guide`）:「削除すると、このラインの枠テンプレートは停止し、予約のない未来の枠は自動削除されます。有効な予約がある場合は先に振替/キャンセルしてください」。

### 5.6 既存 FULL 判定・アトミック UPDATE への波及（棚卸し）

| 既存資産 | 波及 | 対応 |
|---|---|---|
| `incrementBookedCountIfAvailable` / `decrementBookedCountAndReopen`（capacity 基準・実在確認済） | **変更なし**。ライン軸枠も capacity=1 の同一経路 | F03.4.3 が N 枠ループで呼ぶ（同一tx） |
| `createReservation` のライン検証（現状: `lineId` は無検証で保存 — 実測） | ライン軸枠では `request.lineId != slot.lineId` を 400 で拒否（枠の帰属と矛盾する予約を防ぐ）。**エラーコードはグループ側と同じ RESERVATION_038 を共用**（「選択枠とラインの不一致」という同一意味論。単枠専用の新規採番はしない）。共通枠は従来どおり（＋F03.4.3 で有効ライン検証を追加） | F03.4.3 §5 に統合 |
| `listAvailableSlots` | 変更なし（ライン軸枠も同一クエリで返る）。レスポンスに `lineId`/`lineName` を追加 | 追加フィールドのみ |
| 枠削除ガード（`SLOT_HAS_ACTIVE_RESERVATIONS`=020） | 変更なし（テンプレ生成枠にも同じガードが効く） | — |

### 5.7 機能B（予約不可枠）・機能C（グリッド）への波及（棚卸し）

機能B/C は「slot はライン軸を持たない」前提で組まれている（親 §3.B の明記）。ライン軸導入後:

| 資産 | 現状の前提 | ライン軸導入後 | 対応 |
|---|---|---|---|
| `reservation_blocked_times.resource_type` | `TEAM`/`STAFF` のみ enforce。`LINE` は「slot に軸が入る将来に有効化」と enum 拡張点を予約済み（親 §3.B） | slot に `line_id` が入るため **`LINE` 軸を有効化できる条件が成立** | **第二弾で `LINE` を enforce**: `ReservationUnavailabilityChecker` の resourceMatch に `LINE → resourceId == slot.lineId` 分岐を追加（単一ユーティリティ内・3箇所自動追従）。UI の「予約対象から予約不可設定」も `default_staff_user_id` 経由の間接指定から**ライン直接指定**へ置換（親 §3.B の暫定運用を解消）。第一弾では追加しない（B の挙動不変） |
| `ReservationUnavailabilityChecker.isBlocked`（単一 overlap ユーティリティ） | slot の `staffUserId` のみ参照 | シグネチャに `slotLineId` を追加（コア引数版）。TEAM/STAFF の判定結果は不変 | 後方互換（既存テスト green のまま拡張） |
| 機能C グリッド（`ReservationGridService`・列=staffUserId） | 列導出・`lineIds` プリセットが `default_staff_user_id` 依存 | ライン軸枠は「スタッフ列」に紐づかない場合がある（staff NULL のライン枠が共通列へ集約されてしまう） | **F03.4.4 の axis=LINE 拡張で解消**（列=ライン）。第一弾の間は既存 staff 軸グリッドの表示劣化を許容せず、staff NULL×line 非NULL の枠も共通列に出す（漏れなし・見た目の帰属だけが粗い）ことを明記 |
| impact API / 409 ガードの overlap クエリ（`findActiveReservationsOverlappingUnavailability`） | STAFF 軸は `s.staffUserId = :resourceId` | LINE 軸 enforce 時に `s.lineId = :resourceId` 分岐を追加（第二弾） | クエリパラメータ追加のみ |

---

## 6. セキュリティ考慮事項

- **認可 self-gate**: テンプレ CRUD・generate の全 5 本に `@PreAuthorize("@accessGuard.isScopeAdmin(authentication, #teamId, 'TEAM')")` を実装者が付与（親 §6 方針）。
- **IDOR**: `{templateId}` は `findByIdAndTeamId` 解決・他チームは 404（RESERVATION_036）で秘匿。
- **生成の資源保護**: generate は同期実行で最悪 **13,440 INSERT/チーム**（weeks=4・§5.2/§11 と同一試算）。`weeks` 上限 4・テンプレ上限 500 行・**日付単位チャンク tx**（1 tx 最大 480 INSERT・§5.2）で暴走とロング tx を構造的に抑止。レートリミット: generate は `Bucket4j` で 1 チーム 1 分間に 2 回まで（親 §6 のレートリミット方針に追加）。
- **監査ログ**: テンプレの作成・更新・削除・generate 実行（件数付き）を `audit_logs` に記録。
- **PII**: テンプレは PII を含まない（staff_user_id は内部 ID・レスポンスの staffName は管理画面のみ）。

---

## 7. Flywayマイグレーション

```
V<採番未定F>.001__create_reservation_slot_templates_table.sql          -- 週間テンプレート（UUIDv7）
V<採番未定F>.002__alter_reservation_slots_add_line_and_template.sql   -- line_id/template_id + idx_rs_team_date_line + uq_rs_template_cell
```

> **⚠ 採番は未定・非実在プレースホルダ**。実 major は「マージ時点の origin/main 全体の最大 major + 1」（観測時点最大 **V140**）。F03.4.1（E）とは**異なる major** を採る。マージ直前に origin/main を再確認すること。
>
> **付番順は `.001` = create templates → `.002` = alter slots**（`.002` の `template_id` FK が `reservation_slot_templates` を参照するため、テーブル作成が先）。
>
> **ALTER の既存データ番人テスト（必須）**: `reservation_slots` は既存データを持つ。`line_id`/`template_id` とも NULL 許容 & デフォルトなしのため既存行は NULL のまま（後方互換フォールバック=共通枠/手動枠）。**from-scratch 既存行シード番人テスト**（`FlywayExistingData...MigrationTest` 系の写経）で「既存行が NULL 充足で migration を通過し、`uq_rs_template_cell` が NULL 行を制約しない」ことを恒久検証する（親 §7 の機能B ALTER と同じ作法）。

---

## 8. 受け入れ条件（テスト可能・正常/異常/境界）

### 実機E2E の前提データ手順（第一弾・/実機 用）

1. **準備順序を厳守**: チーム作成 → `PUT /reservation-settings/business-hours` で**営業時間を非 NULL の実時刻で設定**（未設定 0 行・`is_open=TRUE` かつ時刻 NULL のままだと generate が全セル skip=F-7b の仕様どおり「枠ゼロ」になり偽陰性と誤認しやすい）→ ライン作成 → テンプレ作成 → generate。
2. **当日・直近時刻の枠は generate では作れない**（生成は tomorrow 以降のみ・§5.2）。当日枠・時刻境界を要するシナリオは**手動 `POST /reservation-slots`** で用意する。
3. 時刻境界の AC は**固定 Clock 注入＋手動枠**で検証する（実時刻依存の flaky 化を避ける）。
4. 認可 E2E は使い捨てチームの新規作成で行う（`feedback_authz_e2e_seed_membership_pollution`）。

- **F-1（正常・テンプレ作成）**: ADMIN が `lineId=1, dayOfWeek=MON, 10:00-13:00` で POST すると 201・`cellCount=6`。**観測点**: `reservation_slot_templates` に 1 行（BINARY(16) PK・line_id=1）。
- **F-2（境界・時刻検証）**: `10:15-11:00`（非30分グリッド）は 400=022、`10:00-10:00` は 400=007。既存コード再利用のため新コードが増えないこと。
- **F-3（境界・上限）**: テンプレ 499 行のチームで 500 行目 POST は 201、500 行のとき 501 行目は 400=RESERVATION_037。
- **F-4（異常・重複帯）**: 同一 (lineId=1, MON) に `10:00-13:00` がある状態で `12:00-14:00` POST は 400（時間帯重複）。共通枠テンプレ（lineId NULL）同士の同時間帯は 201（許可）。
- **F-5（正常・生成）**: MON `10:00-13:00`×lineId=1 のテンプレ 1 行で `generate(weeks=1)` すると、horizon 内の月曜 1 日について 30 分セル 6 枠が INSERT される。**観測点**: `reservation_slots` に `line_id=1, template_id=<tpl>, capacity=1` の 6 行、`start_time` が 10:00/10:30/11:00/11:30/12:00/12:30。
- **F-6（冪等）**: F-5 の直後にもう一度 `generate(weeks=1)` を実行しても新規 INSERT は 0 件（`skippedExistingCount=6`）。**観測点**: slot 行数不変・レスポンスのカウント。並行実行の UNIQUE 違反はエラーでなくスキップ扱い。
- **F-7（営業時間突合）**: 定休日（`is_open=FALSE`）の曜日はセルを生成しない（`skippedClosedDayCount` 加算）。営業時間 `9:00-12:00` の日にテンプレ帯 `10:00-14:00` は 10:00〜12:00 の 4 セルのみ生成し、12:00 以降は `skippedOutsideHoursCount` 加算（境界: `close_time` ちょうどで終わるセルは生成される＝半開でなく「セル全体が営業時間内」判定）。
- **F-7b（営業時間欠損・NPE 防御）**: (1) **`reservation_business_hours` に当該曜日の行が存在しないチーム**（営業時間未設定・0 行）で generate を実行しても例外にならず、当該日のセルは全て `skippedClosedDayCount` に加算される。 (2) **`is_open=TRUE` かつ `open_time`/`close_time` が NULL** の曜日も同様にスキップされ例外にならない。**観測点**: いずれも generate が 200 で返り、`reservation_slots` に当該日の行が増えず、NullPointerException がログに出ない（実 DB 統合テストで両ケースをシードして検証）。
- **F-8（遡及なし）**: テンプレの `startTime` を変更後に再生成しても、既生成枠は不変で新定義のセルだけが追加される。テンプレ DELETE 後、生成済み枠は `template_id=NULL` で残る（`orphanedSlotCount` が枠数と一致）。
- **F-9（日次バッチ・差分レンジ）**: ①通常運転: 前日実行済み環境でバッチ実行すると新規 INSERT = horizon 末尾 1 日分のみ（`range` が自然収束） ②**生成穴の自己修復（A3）**: `weeks=1` で手動 generate 済みのチームでバッチを実行すると day8〜27 が埋まる。generate 実行後に追加した新規テンプレ（生成実績ゼロ）はバッチで `[tomorrow, +27日]` 全域が埋まる。**観測点**: 実行前後の `reservation_slots` の template_id 別 MAX(slot_date)。③`@SchedulerLock` により多重起動しない。ApplicationContext 起動の番人テスト（`@EnableScheduling` 最小 context）が green。
- **F-10（ライン上限 5→20）**: ライン 19 本のチームで 20 本目 POST は 201、20 本のとき 21 本目は 400=024（メッセージ「最大20本」）。`display_order=20` は有効・`21` は 400=025。
- **F-11（ライン軸枠の後方互換）**: `line_id=NULL` の既存枠は従来どおり一覧・予約・グリッドに現れる（既存 E2E/UT が無修正で green）。ライン軸枠は `listAvailableSlots`/`getSlot` レスポンスに `lineId`/`lineName` が載る。
- **F-12（recurrenceRule 廃止）**: `CreateSlotRequest` に `recurrenceRule` を送っても無視される（フィールド削除・unknown property は Jackson 既定で無視）。OpenAPI から当該フィールドが消えている。
- **F-13（認可）**: テンプレ CRUD/generate は非 ADMIN で 403・未認証で 401・他チーム templateId は 404=RESERVATION_036。
- **F-14（generate の状態検証）**: active テンプレが 0 件のチームで generate を実行すると 400（`BusinessException` 経由・メッセージ「有効なテンプレートがありません…」）。**観測点**: `reservation_slots` に行が増えないこと・Bean Validation ではなく Service 層検証であること（`weeks` 省略でも同じ 400 になる=リクエスト形は正当）。
- **F-15（ライン削除の非循環・A1）**: ①active 予約なし・未来のライン軸枠あり・active テンプレありのラインを DELETE すると **200**（旧設計なら 409 だったケース）。**観測点**: 当該テンプレが `is_active=FALSE`・予約なし未来枠が論理削除・`reservation_menu_lines` の当該行が削除・ライン本体に `deleted_at`。②active 予約（PENDING/CONFIRMED）を持つラインの DELETE は 409 で、tx ロールバックによりテンプレの `is_active` も**元のまま**（部分適用なし）。③409 → 予約振替 → 再 DELETE で 200（再実行可能）。

---

## 9. エラーコード（採番仮置き）

> `ReservationErrorCode` 最大 = **RESERVATION_031**（実 enum 確認済 2026-07-05）。v2 通し採番の F03.4.2 分。**マージ直前に enum 実物で再確認**。

| コード | 定数名（案） | 用途 | HTTP | Severity |
|---|---|---|---|---|
| RESERVATION_036 | `TEMPLATE_NOT_FOUND` | テンプレ不存在（IDOR 秘匿含む） | 404 | WARN（個別マップで 404） |
| RESERVATION_037 | `TEMPLATE_LIMIT_EXCEEDED` | テンプレ上限 500 行超過 | 400 | WARN |
| RESERVATION_044 | `TEMPLATE_GENERATE_RATE_LIMITED` | generate レートリミット超過（§6由来） | 429 | WARN（個別マップで 429） |
| RESERVATION_045 | `LINE_HAS_ACTIVE_RESERVATIONS` | ライン削除の409ガード（active予約あり・§5.5由来） | 409 | WARN（個別マップで 409） |

再利用: 時刻検証 007/022・ライン不正 001・ライン上限 024（メッセージ 5→20）・display_order 025（同）・枠削除ガード 020。

---

## 10. i18n（新規キー・6言語・直書き禁止）

`reservation.json` に追加（裸 `@` 禁止・`{'@'}` エスケープ）:

- `reservation.template.title`（週間テンプレート）/ `reservation.template.add` / `reservation.template.edit` / `reservation.template.delete` / `reservation.template.delete_confirm`（「生成済みの枠は残ります」）
- `reservation.template.day_of_week` / `reservation.template.time_range` / `reservation.template.line` / `reservation.template.line_common`（共通枠）/ `reservation.template.capacity` / `reservation.template.staff` / `reservation.template.is_active`
- `reservation.template.cell_count`（「30分×{n}枠/日」）
- `reservation.template.generate`（枠を作成）/ `reservation.template.generate_weeks`（何週先まで）/ `reservation.template.generate_result`（「{generated}枠を作成・{skipped}枠は作成済み」）
- `reservation.template.regenerate_guide`（テンプレ変更後の反映手順ガイド。「今すぐ枠を作成」動線と統合・§5.4）
- `reservation.template.limit_reached`（上限500行）
- `reservation.template.empty_state`（テンプレ未登録の空状態＋「テンプレートを作る」CTA。空状態は #2148 系の空状態カード＋CTA パターンに整合させる）
- `reservation.line.limit`（既存キーの上限文言を 5→20 に更新・6言語）
- `reservation.line.delete_guide`（ライン削除の説明: テンプレ停止＋予約なし未来枠の自動削除＋有効予約は先に振替。§5.5）
- 曜日ラベルは既存 `common.json`/schedule 系の曜日キーを再利用（新設しない）

FE: 曜日チェック UI は `ScheduleEventRecurrenceInput.vue` の曜日トグルを写経する。**ただし写経元は `'MONDAY'` フルネームを emit する（実測）ため、流用するのはラベル・トグルの見た目/操作のみとし、value 値は必ず正準の 3 文字大文字（`'MON'` 形式）へ変換して API に送ること**（フルネームをそのまま送ると `ReservationDayOfWeek` のデシリアライズで 400）。テンプレ管理は `LineManager.vue` 金型で `SlotTemplateManager.vue` を新設し、`teams/[slug]/reservations.vue` の管理タブに組み込む。新規ページ相当の UI には `/統一`・`/手助け` を適用（`feedback_new_pages_use_touitsu_and_tasuke`）。

**UX改善（2026-07-08 追記・第三弾第一波AB）**: 作成ダイアログの曜日トグルは**複数選択可**に変更した（編集ダイアログは従来どおり既存行単位の単一選択）。保存時は選択曜日ぶん `createSlotTemplate` を順に呼び、曜日ごとのテンプレ行に展開する（DDL/API変更なし・1行=1曜日のモデルは不変）。複数作成時のトースト文言として `reservation.message.template_create_success_multi`（「{n}件のテンプレートを作成しました」）を新設（1件作成時は既存 `template_create_success` のまま）。一覧は同一ライン×同一時間帯×同一capacityのグルーピング表示は行わず、従来どおり1行=1テンプレの展開表示のまま（グルーピングは表示ロジックの複雑化・エッジケースリスクに対して効果が限定的なため第一波では見送り。将来必要になれば別途設計）。

**部分失敗の扱い（複数曜日の順次作成・検分指摘反映）**: 途中の曜日で失敗した場合（RESERVATION_037 上限到達が現実的な発生経路）、①成功済み曜日を選択から除去してダイアログは開いたまま（そのまま再試行しても成功分を重複作成しない）、②`reservation.message.template_create_partial`（「{succeeded}/{total}件のテンプレートを作成済み・残りは失敗しました」）を warn トーストで伝達、③一覧を再取得して実状態（成功分のみ作成済み）へ同期する。成功分が1件でもあれば regenerate_guide も表示する（成功分の反映には「今すぐ枠を作成」が必要なため）。

---

## 11. 非機能・アーキ制約

- **生成枠数の上限見積り**: 最悪ケース = 20ライン × 12時間営業（24セル/日）× 7日 × 4週 = **13,440 slot 行/チーム（horizon 内）**。テンプレ上限 500 行 × 4 週 × 平均 6 セル/行 ≒ 同水準で頭打ち。
  - `(team_id, slot_date, line_id)` インデックス下で日次画面クエリは最大 480 行（20ライン×24セル）に収まり性能問題なし。
  - 全体規模: 1 万チームがフル運用しても 1.3 億行/28日。**予約が紐づかない過去枠の物理削除バッチ（slot_date が 90 日超過去・active/終端予約なし）を第二弾で導入**し、テーブルの無限成長を防ぐ（アーキ Phase 3 パーティショニングの前段）。予約が紐づく過去枠は履歴のため残す。
- **UUIDv7（アーキ原則6）**: `reservation_slot_templates` は UuidV7Entity。`reservation_slots` は既存 BIGINT のまま（原則6「既存テーブルの ID は変更しない」）。
- **クロスドメインFK禁止（原則1）/ CASCADE 同一ドメインのみ（原則2）**: 新規 FK（line_id/template_id）はいずれも reservation ドメイン内。CASCADE は使わず RESTRICT/SET NULL。
- **@Transactional ドメイン内（原則5）**: 生成は reservation ドメインに閉じる（business_hours も同ドメイン）。
- **シャーディング**: テンプレ・枠とも `team_id` を持ち、将来の organization_id シャードキー移行時はチーム→組織解決の一括 backfill で対応（新規表に organization_id を今持たせないのは既存 reservation 系テーブルとの整合を優先）。
- **OpenAPI**: BE 変更後に `docs/openapi.json` 再生成＋`npm run generate:types` を同一 PR で（`project_openapi_json_chronic_drift`）。

---

## 12. 変更履歴

| 日付 | 変更内容 |
|------|---------|
| 2026-07-08 | **精査2パス（F03.4.5 側）A 指摘対応: 日次バッチのウォーターマーク・クランプ追補（§5.4）**: `lastGeneratedDate` の導出に `slot_date <= tomorrow + 27日` の WHERE 句クランプを追加。F03.4.5 の単日テンプレ適用（臨時営業・最大+90日）が horizon 外に template_id 付きセルを作ると、素の MAX(slot_date) では range が恒久に空 → 当該テンプレの週次枠が最大2ヶ月未生成になる欠落を根治 |
| 2026-07-08 | **F03.4.5（営業スケジュール中心モデル）による UX 改訂の反映（§5.4）**: テンプレ保存＝同期自動生成へ変更に伴い「今すぐ枠を作成」ボタン・`weeks` 選択 UI を廃止（API は残置＋deprecated）。`regenerate_guide` 文言改訂。詳細・受け入れ条件は F03.4.5 §3 が正 |
| 2026-07-06 | 検分追従: §9に044/045を追記—実装で消費済みの採番を明文化 |
| 2026-07-05 | **精査第2パス＋実機E2E耐性クリティックの反映**: ①(A1・重大) ライン削除フローを再設計 — 旧「未来枠あり→409＋テンプレ停止は成功時副作用」は循環デッドロック（毎晩生成→409 恒常成立→削除不能→テンプレも止まらない）のため撤回。新フロー=単一 tx で「1.テンプレ is_active=FALSE（ガードより前）→2.409 ガードは active 予約ありの場合のみ→3.予約なし未来枠を自動 purge（論理削除）→4.menu_lines 行削除→5.ライン論理削除」の番号順に確定。AC F-15 追加 ②(A3) 日次バッチを「テンプレごとに `[max(tomorrow, MAX(slot_date)+1), tomorrow+27日]` の差分レンジ生成」へ変更（weeks=1 の day8〜27 空白・generate 後の新規テンプレ未生成の恒久穴を自己修復。lastGeneratedDate は導出・先読みはチーム単位一括のまま）。F-9 に穴修復の観測点を追加 ③(A5) 共通列カウントを「20 ライン＋共通=21 列・147 行」に統一 ④(A6) 単枠 createReservation のライン不一致は RESERVATION_038 共用と明記 ⑤(B1・壊れる) 曜日表現を正準化 — 正準は 3 文字大文字 MON..SUN（business_hours VARCHAR(3) と完全同一）。BE は `java.time.DayOfWeek` を使わず専用 enum `ReservationDayOfWeek` を新設（Jackson enum 400 が実体を伴って成立）。生成突合は `to3Letter(date)` 変換を §5.2 に明記。写経元 `ScheduleEventRecurrenceInput.vue` は 'MONDAY' を emit するため UI のみ流用・value は 'MON' へ変換と明示 ⑥(B2) MANAGE_RESERVATIONS 表記を一掃（role ベース・AccessGuard.java:46 実測） ⑦(B6) 実機E2E 前提データ手順節を新設（business_hours 非NULL・当日枠は手動 POST・固定 Clock・準備順序） ⑧(B7) recurrenceRule 削除範囲を確定 — 入力（CreateSlotRequest）のみ削除・応答（RecurrenceDto）は後方互換で残置（値は将来 null） |
| 2026-07-05 | **精査第1パス指摘の反映**: ①(重大1) 生成アルゴリズムに営業時間欠損の防御分岐を確定（曜日行なし=新規チーム0行・is_open=TRUE だが時刻 NULL〔V3.063 実DDL〕→ いずれも生成スキップ＋skippedClosedDayCount。フォールバック生成なし。AC F-7b 追加） ②(重大2) ライン上限 5→20 の波及に `ReservationLineServiceTest` の 5/6 境界ハードコード（L174/192/208 付近）の 20/21 改訂を明記 ③(中5) templates の team_id/staff_user_id/created_by を BIGINT UNSIGNED に統一（V3.060/061 整合） ④(中8) 生成 tx 見積りを 13,440（weeks=4 最悪）に統一し日付単位チャンク tx を採用（冪等キーが再実行安全を担保） ⑤(中9) 冪等スキップをセル単位 existsBy から dateRange 一括先読み Set 突合へ変更・日次バッチは horizon 末尾の差分日のみ対象と明記 ⑥(中10) テンプレ CRUD 後の「今すぐ枠を作成」UI 動線（regenerate_guide 統合）を明記 ⑦(中11) 共通枠と `reservations.line_id NOT NULL`（V3.062）の関係を一文固定 ⑧(軽19) `reservation.template.empty_state` キー追加（#2148 空状態 CTA 整合） ⑨(軽20) generate の「active テンプレ 0 件→400」を Bean Validation でなく状態検証（BusinessException）と訂正・AC F-14 追加 |
| 2026-07-05 | 初版作成（v2根本再設計・マスター御裁可 2026-07-05）。親 §4「bulk は Phase 3 スコープ外」を撤回し週間テンプレート方式を正式設計。`reservation_slots` に line_id（nullable=共通枠・backfillなし）/template_id を追加、`reservation_slot_templates`（UUIDv7・上限500行）を新設、30分セル分割生成＋営業時間突合＋冪等キー `uq_rs_template_cell`＋日次バッチ（AM0:15 JST・rolling 28日）を設計。休眠足場（recurrence_rule/parent_slot_id/is_exception）は実装ゼロを実測確認のうえ**不流用・廃止**（カラム残置・API入力は削除）。ライン上限 5→20 確定（業態P90/UI実用限界/生成試算の3根拠）。V140 capacity・uq_rv_slot_line_active 不存在など実装事実の追認を明記。エラーコード 036〜037 仮置き |
