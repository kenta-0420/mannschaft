# F03.4.5: 営業スケジュール中心モデル＋定期予約不可枠＋呼称設定＋実務機能（第三弾第二波）

> **ステータス**: 🟢 設計完了（精査: 6観点2パス＋実機E2E耐性クリティック済・2026-07-08）
> **親設計書**: [F03.4 予約管理](F03.4_reservation.md)（機能A〜Dの正本。用語・共通制約は親に従う）
> **実装フェーズ**: v2 **第三弾第二波（W2）**。弾割りは §13
> **最終更新**: 2026-07-08
> **関連枝番**: [F03.4.2 週間テンプレート](F03.4.2_reservation_slot_template.md)（生成基盤の正本。本書 §3 が UX を改訂）、[F03.4.4 マトリックスUI](F03.4.4_reservation_matrix_ui.md)（グリッドセルへの事由表示の消費者）、[F03.4.1 メニュー](F03.4.1_reservation_menu.md)、[F03.4.3 予約グループ](F03.4.3_reservation_group_booking.md)（定期予約・レートリミットの消費者）
> **写経元（実装方針）**: `ReservationSlotGenerationService`（生成単一実装・日付チャンクtx・INSERT IGNORE 冪等）、`ReservationUnavailabilityChecker`（単一 overlap ユーティリティ）、`ReservationReminderDispatchBatchService`（`@Scheduled`+`@SchedulerLock`+行単位 try/catch）、`ReservationSlotTemplateService`（ValkeyRateLimiter 利用例）、`SlotTemplateManager.vue`／`ReservationUnavailabilityManager.vue`（管理UI金型）

---

## 1. 概要（マスターの実使用フィードバックが設計の動機）

> **マスターのフィードバック（2026-07-08・原文の意図）**:
> 「チーム側が予約枠を設定するのが使いにくい。通常の業務であれば、テンプレートを登録すれば通常時はなにもせずとも営業時間が更新されていく。編集したいのは、通常営業をクローズさせる時か、振替で営業する時。現状は営業枠を個別で作ることから始まっているので脳内摩擦が多い」

現状の管理UIは「個別枠の作成（SlotManager）」が主役で、テンプレートを保存しても**「今すぐ枠を作成」ボタンを別途押さないと枠が生まれない**（`SlotTemplateManager.vue` 実測: 保存成功 → `regenerate_guide` メッセージ → 手動 generate の 3 ステップ）。さらに**営業時間を編集する UI がチーム管理タブに存在しない**（実測: `useReservationApi.updateBusinessHours` の参照コンポーネントはゼロ。営業時間は API 直叩きでしか設定できない）。これがマスターの言う「脳内摩擦」の実体である。

本書は予約管理の管理側 UX を**「営業スケジュール中心モデル」**へ改訂する:

| 節 | 内容 | 一言で |
|---|---|---|
| §3 (A) | テンプレ保存＝即自動生成・管理タブ再編・「休業にする/臨時営業する」例外操作 | **通常時は何もしない。編集するのは例外だけ** |
| §4 (B) | 週次定期の予約不可枠（事由ラベル＋公開可否） | 「毎週火曜19-20時は研修」を1回だけ登録 |
| §5 (C) | 予約対象の呼称チーム設定化（席/コート/ベッド/レーン/担当スタッフ/自由入力） | 業種の言葉で画面が話す |
| §6 (D) | キャンセル待ち・定期予約・仮押さえ自動失効・予約作成レートリミット（**設計のみ・実装は別裁可**） | 実務でほしくなる4点 |

---

## 2. スコープ

### 対象ロール

| ロール | 操作可能な範囲 |
|--------|--------------|
| ADMIN / DEPUTY_ADMIN（role ベース・`isScopeAdmin` 3引数。既存テンプレ CRUD と同一） | §3 営業時間/例外操作、§4 定期予約不可枠 CRUD、§5 呼称設定、§6 の管理系 API |
| MEMBER / SUPPORTER / 公開ユーザー | §4 の事由ラベル閲覧（is_public のみ）、§6 キャンセル待ち登録・定期予約作成（view ゲート） |

### スコープ外

- 営業時間変更の既存枠への遡及 CLOSE（既存方針維持・親 §8 バックログの範疇。§3.4）
- 単発の予約不可枠（`reservation_blocked_times`）への `is_public` 拡張（§4.5 バックログ起票のみ）
- シフト（F03.5）連動の自動テンプレート
- §6（D群）の**実装**（本書は設計のみ。着手はマスターの別裁可）

---

## 3. A) 営業スケジュール中心モデル（本丸）

### 3.1 テンプレ保存＝即自動生成

#### 設計判断: 同期実行を採用（AFTER_COMMIT 非同期は不採用）

テンプレの create / update 成功時に、**該当テンプレ 1 行のみを対象に** horizon 28 日（`[tomorrow, tomorrow+27日]`）の生成を自動実行する。

| 観点 | **同期（採用）** | AFTER_COMMIT 非同期（不採用） |
|---|---|---|
| 利用者への結果提示 | 保存レスポンスに生成カウントを同梱でき、**トーストで「保存し、28日先までの枠を◯件作成しました」を即時提示**できる。「保存したのに枠がない」というマスター指摘の混乱が構造的に消える | 生成結果を返せない。失敗時はログのみ＝症状の黙殺（根治原則違反）になりやすい |
| 処理量 | **単一テンプレ scope なら最大 24セル/日 × 4回（28日中の同一曜日の出現数）= 96 INSERT**（12時間営業の帯でも。1テンプレ行は 1 曜日のみを持つため「×28日」ではない——精査2パス H 訂正。全域 13,440 = 上限500行に近い約140テンプレ × 96 と整合）。既存の日付チャンク tx（1日=1 tx・REQUIRES_NEW・`ReservationSlotGenerationService` 実装済）をそのまま流用すれば応答遅延は実用圏（1秒未満想定・AC S-2 で観測） | 96 INSERT を非同期化する利得がない |
| 追加基盤 | 不要（Service 呼び出し 1 本） | イベント＋`@TransactionalEventListener(REQUIRES_NEW)`＋番人テストが増える |
| 失敗時の挙動 | チャンク tx 失敗はテンプレ保存自体には波及させない（保存 tx は既にコミット済み後に生成を実行・下記実装順序）。生成失敗はエラートーストで正直に報告し、日次バッチの差分レンジ（F03.4.2 §5.4）が翌朝自己修復する | 同左だがユーザーに見えない |

**実装順序と tx 境界（確定・精査1パス反映）**: 生成は「保存 tx がコミットした**後**」に、**`@Transactional` サービスメソッドの外側**で実行する。具体的には `createTemplate` / `updateTemplate`（いずれも `@Transactional`・実測）を先に完了させ、**コントローラまたは非トランザクションのファサードメソッド**（既存 `generate()` の `@Transactional(propagation = Propagation.SUPPORTS, readOnly = true)`＋コントローラ呼びと**同型**・実測）から `ReservationSlotGenerationService` の**単一テンプレ版**を呼ぶ。生成が失敗しても保存は成立済みで、レスポンスは `generation.failed=true` で正直に返す（下記契約）。

> **⚠ tx 境界の罠（実装者向け・必読）**: `reservation_slots` は FK `fk_rs_template`（`template_id` → `reservation_slot_templates`・**V142.002 実DDL 根拠**）を持つ。もし生成を保存 tx の**内側**から呼ぶと、生成側の日付チャンク tx は `REQUIRES_NEW`（`TransactionTemplate`・実装済）のため**別コネクション**となり、**親 tx が未コミットのテンプレ行を FK 参照する slot INSERT が親の行ロック解放を待つ自己デッドロック → lock wait timeout** に陥る。「保存 tx コミット → 外側で生成」の順序はこの構造的デッドロックの回避条件であり、実装時に順序を崩してはならない。

#### レスポンス契約 — `SlotTemplateSaveResponse`（新設・テンプレ POST/PATCH 共通）

保存＋生成結果を 1 レスポンスに包む（AC S-1/S-3 の観測点を型で固定）:

```json
{
  "data": {
    "template": { "id": "0198...", "dayOfWeek": "MON", "startTime": "10:00", "...": "（SlotTemplateResponse と同形）" },
    "generation": {
      "generatedCount": 24,
      "skippedExistingCount": 0,
      "skippedClosedDayCount": 4,
      "skippedOutsideHoursCount": 2,
      "failed": false
    }
  }
}
```

| フィールド | 型 | null | 説明 |
|-----------|---|------|------|
| `template` | Object | 不可 | 既存 `SlotTemplateResponse` と同形（保存結果） |
| `generation` | Object | 不可 | 生成結果。カウント 4 種は既存 `GenerateSlotsResponse` と同名・同義 |
| `generation.failed` | boolean | 不可 | 生成段の失敗。TRUE のときカウント各値は**コミット済みチャンク分**（日付単位 tx のため部分実行があり得る）。翌朝の日次バッチ差分レンジが残りを自己修復する |

- テンプレ POST/PATCH の応答型が `SlotTemplateResponse` 単体から変わるため **OpenAPI 再生成＋`generate:types`＋FE 改修を同一 PR** で行う（現行の応答消費者は `SlotTemplateManager.vue` のみ・実測。破壊範囲は閉じている）。

**複数曜日ループとの集約規則（精査2パス D・W1 実装を前提）**: W1（PR #2198）の新規作成ダイアログは選択曜日ぶん `createSlotTemplate` を **N 回順次呼ぶループ**（`selectedDays`・W1 ブランチ実測）。W2 では各呼びが `SlotTemplateSaveResponse` を返すため、FE は **N 件の `generation` を合算して 1 トースト**で報告する（生成/スキップの合計件数。`reservation.template.auto_generated` の `{generated}` に合算値）。**部分失敗**（一部曜日で `failed=true` または呼び自体が 4xx/5xx）時は「**{total}曜日中{failedCount}曜日で枠の作成に失敗しました（保存済みの曜日は有効です）**」表現の警告トースト 1 本に集約する（曜日ごとに N 連トーストを出さない・§11 `auto_generated_partial`）。

- **生成 scope はチーム全テンプレではなく当該テンプレのみ**（設計判断）: チーム全域 generate（最悪 13,440 INSERT・F03.4.2 §5.2）を CRUD 応答に載せると保存が重くなる。他テンプレの horizon 延伸は日次バッチの責務のまま。
- **`update` で `isActive=FALSE` にした場合・時刻を変更した場合**: 既生成枠への遡及なし原則（F03.4.2 §5.3）は**不変**。無効化は「以後生成されない」だけ、時刻変更は「新定義セルの追加生成」だけが起きる。旧定義の未予約枠を消したい場合の 2 操作ガイド（枠削除 → 保存）は文言を改訂して残す（§11 `regenerate_guide` 改訂）。
- **自動生成はレートリミット（RESERVATION_044）の対象外**（設計判断）: 保存操作そのものが頻度の自然な上限であり、単一テンプレ scope＋冪等（INSERT IGNORE + `uq_rs_template_cell`）のため暴走リスクがない。手動 generate API に残るリミットはそのまま。

#### 「今すぐ枠を作成」ボタンと `weeks` 選択 UI の廃止（API は残置・deprecate）

| 対象 | 決定 | 根拠 |
|---|---|---|
| FE `SlotTemplateManager.vue` の「今すぐ枠を作成」ボタン＋`weeks` Select（実測: `generateNow()`／`generateWeeks` ref） | **撤去** | 保存＝反映になるため操作の存在意義が消える。残すと「押さないと反映されないのでは」という疑念（脳内摩擦）が残る |
| BE `POST /reservation-slot-templates/generate` | **残置＋`@Deprecated`（OpenAPI `deprecated: true`）** | 生成型（`generate:types`）を参照する既存クライアント・E2E・運用リカバリ（バッチ障害時の手動追い付き）用に互換維持。**削除は将来の別 PR・要裁可**（§14 要裁可論点） |
| `weeks` パラメータ | API 上残置（1〜4・既定 4）。**FE からは常に省略**（=28日） | additive 契約を壊さない |
| i18n `reservation.template.generate` / `generate_result` / `generate_rate_limited` / `generate_skipped_closed` | **キー残置・値流用**（自動生成・臨時営業の結果トーストが再利用） | 6言語の再翻訳コストを避ける |
| i18n `reservation.template.generate_weeks` ほか UI 撤去で参照ゼロになるキー | **削除/改訂**（廃止波及キーの**全数列挙は §11 の一覧表が正**・精査2パス C） | 死蔵キー化の防止（§5.4 と同方針） |

### 3.2 管理タブの再編（初期セットアップの流れ＝画面の並び）と営業時間 UI の新設

#### 実測の現状（`TeamReservationsPanel.vue` 管理タブ）

```
LineManager → MenuManager → SlotTemplateManager → SlotManager → 詳細設定Accordion
  （詳細設定 = PublicToggle / PolicySettings / UnavailabilityManager / NotifyRecipients）
※ 営業時間の編集 UI は存在しない（updateBusinessHours の参照コンポーネントゼロ・実測）
```

#### 改訂後の並び（確定）

```
① 営業時間（BusinessHoursManager.vue 新設）
② 予約対象（LineManager・呼称は §5 で動的化・並び順編集を追加 §5.4）
③ メニュー（MenuManager・既存のまま)
④ 週間スケジュール（WeeklyScheduleManager.vue = SlotTemplateManager 発展 + §4 定期予約不可枠）
⑤ 例外日カレンダー（§3.3 の「休業にする/臨時営業する」起点）
⑥ 詳細設定 Accordion（既存4種 ＋ SlotManager をここへ移設）
```

- **①〜④の並び = 初期セットアップの思考順**（場所を開ける時間 → 何を貸すか → 何を提供するか → 週の設計図）。ADHD 配慮（`user_adhd_tendency`）: ページは上から順に埋めるだけ・例外操作は⑤⑥へ段階開示。
- **`BusinessHoursManager.vue`（新設）**: 曜日 7 行 ×（営業トグル `isOpen`・開始/終了 30 分刻み Select）。取得は既存 `GET /reservation-settings/business-hours` のまま。**PUT の応答は §3.1 と同型の生成結果ラッパへ変更**する（`BusinessHoursSaveResponse { hours: BusinessHourResponse[], generation: { generatedCount, skippedExistingCount, skippedClosedDayCount, skippedOutsideHoursCount, failed } }`。保存成功時に同期 generate を実行し結果を同梱——営業時間の拡大で `skippedOutsideHoursCount` に落ちていたセルが埋まる。縮小方向は遡及 CLOSE しない=スコープ外の明記どおり）。生成の tx 境界・失敗時挙動は §3.1 と同一規約（保存 tx コミット後・外側実行・`failed=true`＋エラートーストで正直に報告・翌朝バッチが自己修復）。
  - **生成 scope は「変更のあった曜日」の差分に限定**（精査1パス反映・INSERT 量の抑制）: PUT リクエストと現行値を突合し、`isOpen`/`openTime`/`closeTime` のいずれかが変わった曜日の active テンプレのみを対象に horizon 28 日を生成する。通常の変更は 1〜2 曜日＝全テンプレ方式の約 1/7〜2/7（最悪の 7 曜日全変更でも従来案と同量 13,440 INSERT が上限）。
  - **生成中の UX（規定）**: 保存ボタンは生成完了まで loading 表示（`saving`）＋「枠を更新しています…」のインライン文言（§11 `business_hours.generating`）。完了トーストは生成カウント同梱（`business_hours.save_success`）。応答は**既定の API タイムアウト内に 200 を返すこと**を回帰 AC とする（S-6 観測点）。
  - **PUT/GET の非対称（精査2パス G）**: 応答型の変更は `updateBusinessHours`（PUT）**のみ**。`getBusinessHours`（GET）は `BusinessHourResponse[]` のまま不変であり、現行の GET 消費者 `ReservationUnavailabilityManager.vue`（テンプレ自動充填用・実測）への影響はない。
  - **営業時間の縮小・定休化時のガイド（精査2パス I）**: BusinessHoursManager は保存前に現行値と突合し、**縮小方向の変更**（`isOpen` true→false・時間帯の短縮）を検知したら「この曜日のすでに作成済みの未来の枠はそのまま残ります（遡って閉じません）。特定日を恒久的に閉じる場合は例外日カレンダーの『この日を休業にする』を使ってください」のガイドを保存確認に表示する（§11 `business_hours.shrink_note`・遡及 CLOSE しない既存方針をユーザーに正直に伝える。AC S-6⑤）。
- **営業時間未設定チームの初回体験（精査2パス E）**: `ReservationSettingsResponse.hasBusinessHours=false`（実在フィールド・実測）のとき、④週間スケジュール画面の空状態に「**先に営業時間を設定してください**」ガイド＋①営業時間セクションへのスクロール/フォーカス導線を表示する（§11 `template.need_business_hours_hint`）。また保存時自動生成の結果が **`generatedCount=0` かつ `skippedOutsideHoursCount>0`（または `skippedClosedDayCount>0`）** のときは、トーストに**原因を明示**する（「枠は作成されませんでした（営業時間外/定休日のため）。営業時間を確認してください」§11 `template.generated_zero_hint`）。「保存したのに枠が 0 件」の無言の混乱——本設計の動機そのもの——を再発させない（AC S-11）。
- **`SlotManager`（個別枠 CRUD）の格下げ**: 詳細設定 Accordion 内へ移設し、セクションラベルを「個別の枠を手動管理（例外操作）」とする（§11 i18n）。機能・API は無変更（枠 CRUD は臨時営業・当日枠・長尺枠の正規手段として残る）。
- **タブ再編は表示順の変更のみ**で、各コンポーネントの props / API 契約は不変（`SlotTemplateManager` → `WeeklyScheduleManager` は §4.4 のリネーム＋統合。`pathPrefix:false` 規約により**コンポーネント名=ファイル名**で参照されるため、参照箇所の同時改修を弾内タスクに含める — `project_nuxt_pathprefix_false_component_name_misuse`）。

### 3.3 例外操作の前面化 — 「この日を休業にする」「臨時営業する」

管理タブ⑤に**例外日カレンダー**（月表示ミニカレンダー・`ScheduleExceptionPanel.vue` 新設）を置き、日クリックで 2 択ダイアログを出す。

#### 3.3.1 「この日を休業にする」= 既存の予約不可枠（全日・TEAM軸）作成のショートカット

- **新 API なし**（設計判断）: 実体は既存 `POST /reservation-settings/blocked-times`（`blockedDate=選択日, startTime/endTime=null（全日）, resourceType=TEAM, reason=任意`）。enforcement は機能B の runtime overlap（親 §5.B）がそのまま効く。
- **既存予約がある場合の警告と一括対応導線**: ダイアログ表示時に既存 `GET /blocked-times/impact`（親 §4.B）を全日条件で呼び、`affectedCount >= 1` なら
  1. 警告カード「この日には有効な予約が {n} 件あります」＋予約一覧（impact レスポンスの氏名・時間帯）
  2. 各行に「予約一覧タブで対応」導線（予約一覧タブへ遷移し、既存の管理者キャンセル/承認 UI で振替・キャンセル）
  3. 登録ボタンは disabled（BE の 409=RESERVATION_027 が最終防御・既存挙動不変）
- **会員へ告知したい休業は既存「緊急休業」へ誘導**: ダイアログに「予約者への通知・お知らせ配信が必要な場合は緊急休業を使う」リンク（既存タブ `EmergencyClosureForm`）。**予約不可枠（受付を閉じる）と緊急休業（告知・確認フロー）は別機能のまま**とし、統合しない（設計判断: 緊急休業は確認レコード・リマインド等の重い独自フローを持ち、混ぜると両方の単純さを失う）。

#### 3.3.2 「臨時営業する」= 定休日・営業時間外への単日テンプレ適用

**設計判断（2案比較 → 単日テンプレ適用 API を採用）**:

| 案 | 内容 | 評価 |
|---|---|---|
| (i) 個別枠の手動作成に誘導 | 既存 `POST /reservation-slots` を30分セル数ぶん繰り返す | 「振替で日曜営業・内容は平日と同じ」で最大 24 回の枠作成操作＝脳内摩擦の再生産。**不採用**（ただし例外操作としては⑥で引き続き可能） |
| **(ii) 単日テンプレ適用 API（採用）** | 指定日に、指定曜日の active テンプレ構成（ライン・帯・capacity）で 30 分セルを一括生成。**営業時間突合をスキップ**（臨時営業は定休日/時間外が前提のため） | 1 操作で完結。冪等キー `uq_rs_template_cell` がそのまま効く（同一日への再実行はスキップ）。生成単一実装（`ReservationSlotGenerationService`）に「営業時間チェック省略フラグ＋単日レンジ」を渡すだけで、**別実装を作らない** |

#### `POST /api/v1/teams/{teamId}/reservation-slot-templates/generate-single-day`

| フィールド | 型 | 必須 | バリデーション |
|-----------|---|------|-------------|
| `date` | String(date) | Yes | 明日以降（当日・過去は 400=PAST_DATE_SLOT 023 再利用。生成系の「当日は手動枠の領分」原則と統一）。上限: 今日から 90 日以内（horizon 外の遠未来生成を防ぐ・汎用 400） |
| `sourceDayOfWeek` | String(enum) | No | `MON`..`SUN`（`ReservationDayOfWeek`・3文字正準）。**省略時 = `date` の実曜日**。「日曜だが平日ダイヤで営業」は `sourceDayOfWeek=MON` 等を指定 |

**レスポンス（200 OK）**: 既存 `GenerateSlotsResponse` と同型（`generatedCount` / `skippedExistingCount`。営業時間チェックを行わないため closed/outside 系カウントは常に 0）。

**エラー**: active テンプレ 0 件（対象曜日に 1 行もない）→ 400（`BusinessException`・「この曜日のテンプレートがありません」メッセージ・generate の状態検証と同作法）／401・403（ADMIN+）。**§4 の定期予約不可枠は生成をスキップしない**（機能B と同じ「生成はする・runtime で落とす」一貫方針 §4.2）。

- **horizon 外生成とウォーターマークの扱い（精査2パス A・必読）**: 本 API は horizon（28日）の**外**（tomorrow+28〜+90日）に `template_id` 付きセルを作れる唯一の経路である。日次バッチの差分レンジが参照する `lastGeneratedDate` は **horizon 上限でクランプした導出**（`MAX(slot_date) WHERE template_id=T AND slot_date <= tomorrow+27日`・F03.4.2 §5.4 追補）であるため、horizon 外の単日生成は**ウォーターマークに影響せず**、通常の週次生成は欠落しない（AC S-8③）。クランプを実装しないまま本 API を出すことは不可（W2-1 で同時実装）。
- **同日の全日休業（機能B・全日 TEAM blocked_times）との衝突（精査2パス F・挙動を一意に定義）**: FE は「臨時営業する」ダイアログ表示時に対象日の全日 TEAM 予約不可枠の有無を検査し、**存在する場合は警告カード「この日は休業に設定されています」＋「休業を解除してから実行してください」導線（当該 blocked_time の削除ダイアログへ）を表示し、実行ボタンを disabled** にする（生成しても全セルが runtime で UNAVAILABLE になり誰も予約できない=無意味な操作を入口で止める）。**BE はブロックしない**（生成はする・runtime で落とすの一貫方針 §4.2。休業解除を後で行う前提で先に枠だけ用意する運用を API 層で禁じない）。AC S-12。
- 認可: `@PreAuthorize("@accessGuard.isScopeAdmin(authentication, #teamId, 'TEAM')")`（テンプレ系 5 本と同一）。
- レートリミット: 既存 generate と同一 zone（`reservation-template-generate`・1チーム 2回/分・RESERVATION_044 再利用）を**共有**する（同種の生成資源保護。別 zone にする理由がない）。
- 監査ログ: `audit_logs` に記録（テンプレ系と同じ作法）。
- **FE**: 例外日カレンダーの「臨時営業する」ダイアログ =「適用する曜日ダイヤ」Select（既定=当日曜日）＋実行ボタン＋結果トースト。生成後にその日のセル一覧（マトリックスの単日ビュー）へ遷移して確認できる導線。

### 3.4 既存の生成基盤との整合棚卸し

| 既存資産（F03.4.2） | 本節の変更による影響 | 対応 |
|---|---|---|
| 日次バッチ（テンプレごと差分レンジ `[max(tomorrow, lastGeneratedDate+1), tomorrow+27日]`） | **クランプ追補が必要**（精査2パス A・F03.4.2 §5.4 改訂済）: 単日テンプレ適用（§3.3.2・最大+90日）が horizon 外に `template_id` 付きセルを作ると、素の `MAX(slot_date)` ではウォーターマークが跳ねて range が恒久に空 → **当該テンプレの通常週次枠が最大 2 ヶ月未生成**になる。`lastGeneratedDate` の導出を `MAX(slot_date) WHERE template_id=T AND slot_date <= tomorrow+27日` に**クランプ**することで根治。保存時自動生成（horizon 内）はクランプ後も従来どおりウォーターマークを前進させ、バッチは末尾 1 日へ収束する | **F03.4.2 §5.4 のクランプ追補を W2-1 で実装**（AC S-4 回帰＋S-8③が番人） |
| 冪等（先読み Set＋INSERT IGNORE＋`uq_rs_template_cell`） | 自動生成・単日適用とも同一経路を流用。並行（保存直後にバッチ/手動 generate）でも二重生成なし | 変更なし |
| ライン削除フロー（テンプレ停止 → 409 ガード → 未来枠 purge の番号順・F03.4.2 §5.5） | 無変更で整合。ライン削除がテンプレを止めるため、保存時自動生成の対象からも自然に外れる | 変更なし |
| 生成時に予約不可枠をチェックしない方針 | **単日テンプレ適用・§4 の定期不可枠にも同方針を貫徹**（生成はする・runtime で落とす） | §4.2 で明文化 |
| `ReservationSlotGenerationService` の公開シグネチャ | 単一テンプレ版・単日版（営業時間チェック省略）を**同クラスに追加**（別実装厳禁の原則維持）。内部の日付ループ・チャンク tx・冪等はプライベート共通部を共有 | 実装時にリファクタ（挙動番人: 既存 F-5〜F-9 green 維持） |

---

## 4. B) 定期予約不可枠（週次繰り返し・事由ラベル・公開可否）

「毎週火曜 19:00-20:00 = 研修」「毎週水曜 9:00-12:00 = スクール」のような**週次繰り返しの予約不可**を 1 回の登録で恒久化する。単発の `reservation_blocked_times`（機能B）は日付ごとに行が要るため、この用途では毎週の登録作業が発生していた。

### 4.1 DB設計 — `reservation_recurring_blocked_times`（新規）

**新規テーブルのため UUIDv7（アーキ原則6・`UuidV7Entity` 継承）。`team_id`/`created_by` はクロスドメイン参照のため FK なし（原則1）。`line_id` は同一 reservation ドメイン内 FK。**

| カラム名 | 型 | NULL | デフォルト | 説明 |
|---------|---|------|-----------|------|
| `id` | BINARY(16) | NO | UUIDv7 | PK |
| `team_id` | BIGINT UNSIGNED | NO | — | チームID（FKなし・INDEX） |
| `line_id` | BIGINT UNSIGNED | YES | NULL | 対象ライン。FK → `reservation_lines.id` ON DELETE RESTRICT。**NULL = チーム全体**（全ライン・共通枠に効く） |
| `day_of_week` | VARCHAR(3) | NO | — | 曜日。**3文字大文字正準 `MON`..`SUN`**（`ReservationDayOfWeek` enum・`reservation_slot_templates.day_of_week` と完全同一表現） |
| `start_time` | TIME | NO | — | 開始（30分単位）。**全日型は許可しない**（§4.3 設計判断） |
| `end_time` | TIME | NO | — | 終了（30分単位・start より後） |
| `reason` | VARCHAR(100) | NO | — | 事由ラベル（例「研修」「スクール」）。**必須**（is_public=FALSE でも管理画面の一覧で「何のブロックか」が判るように） |
| `is_public` | BOOLEAN | NO | FALSE | TRUE = 会員のマトリックス該当セルに `reason` を表示。FALSE = 従来どおり UNAVAILABLE 表示のみ |
| `is_active` | BOOLEAN | NO | TRUE | FALSE は判定対象外（一時停止） |
| `created_by` | BIGINT UNSIGNED | YES | NULL | 作成者（FKなし） |
| `created_at` / `updated_at` | DATETIME(6) | NO | — | |

**インデックス**
```sql
INDEX idx_rrbt_team (team_id, day_of_week)   -- チーム別・判定時の読み込み
INDEX idx_rrbt_line (line_id)                 -- ライン削除時の参照チェック
```

**制約・備考**
- **上限: 1チームあたり 50 行**（RESERVATION_047）。試算: 20ライン × 主要曜日数本で十分・判定は毎リクエストのメモリ突合のため小さく保つ。
- 時刻検証は既存 007/022 再利用（30分単位・start < end・最小30分）。
- **同一 (team, line, 曜日) 内のルール同士の時間帯重複は許可**（設計判断: 「研修」と「清掃」が重なっても判定は any-match で正しく、入口で禁じる利益がない。テンプレの重複禁止とは目的が違う——テンプレは二重生成の物理的衝突があるが、不可ルールは論理判定のみ）。
- **ライン削除フロー（F03.4.2 §5.5）への追補**: 手順 1（テンプレ停止）の直後に「当該ラインの定期予約不可枠を `is_active=FALSE` 化」を追加する（手順 1.5）。ラインが論理削除された後に RESTRICT FK が残るのは既存テンプレと同じ扱い。

### 4.2 enforcement — **runtime overlap 統合を採用**（生成時スキップは不採用）

機能B の思想（親 §5.B「enforcement は runtime overlap ユーティリティ 1 本・blocked_times に行を書くだけ・slot は CLOSE しない」）と、週次定期の「テンプレ的」性質のどちらに寄せるかの設計判断:

| 観点 | **runtime 統合（採用）** | 生成時スキップ（不採用） |
|---|---|---|
| 効く範囲 | テンプレ生成枠・**手動枠・既存枠・単日臨時営業枠のすべて**に即効 | テンプレ生成枠にしか効かない（手動枠・既生成枠は素通り＝穴） |
| ルール解除の反映 | 即時（次のリクエストから予約可能に戻る） | 「スキップされたセルは枠が存在しない」ため、解除後に**再 generate するまで枠が無い**（機能B 設計時に退けたのと同一の穴） |
| 事由ラベル表示（is_public） | セル（=枠）が存在するから **UNAVAILABLE セルに「研修」を載せられる** | 枠が無い＝セルは「−（枠なし）」となり、**事由を表示する場所自体が消える**（マスター要件と両立しない） |
| 二重管理 | 判定 1 本（機能B と同居） | 生成時と runtime の二重判定になる |
| 機能B との整合 | 「テンプレは BE に持たず具体時刻のみ」という機能B の単発設計とは異なり週次ルールを持つが、**enforcement 経路は同じ 1 本**に統合されるため思想の核（単一判定・行を書くだけ）は維持 | — |

**結論: 生成はする・表示/予約時に runtime で落とす**。機能B の 3 箇所（`listAvailableSlots` / `createReservation`（グループ経由含む）/ `ReservationGridService`）が自動追従する形で `ReservationUnavailabilityChecker` を拡張する。

**チェッカー拡張（実装指針）**

```
// 新メソッド（既存 isBlocked と並置・別実装厳禁の原則で 3 箇所が共有）
boolean isRecurringBlocked(slotDate, slotStart, slotEnd, slotLineId, rule):
    if !rule.isActive: return false
    if to3Letter(slotDate) != rule.dayOfWeek: return false      # F03.4.2 §5.2 と同じ 3 文字正準変換
    # 対象軸マッチ
    if rule.lineId == null:            resourceMatch = true      # チーム全体（共通枠含む全 slot）
    else:                              resourceMatch = (rule.lineId == slot.lineId)
    #   ※ slot.lineId == null（共通枠）はライン指定ルールの対象外（STAFF 軸の null 非一致と同じ規則）
    if not resourceMatch: return false
    # 時間帯 overlap（半開区間・機能B と同一）
    return slotStart < rule.endTime and rule.startTime < slotEnd
```

- 既存 `isBlocked`（9引数コア）は**変更しない**（既存テスト green 維持）。呼び出し 3 箇所は「単発 blocked_times の any-match **OR** 定期ルールの any-match」で合成する集約メソッド `isBlockedByAny(slot, blocks, recurringRules)`（オーバーロード追加）を使う。
- ルールの読み込みは `findByTeamIdAndIsActiveTrue(teamId)` の全件メモリ突合（上限 50 行・単発 blocked_times の日付ロードと同じ作法・N+1 なし）。
- **実測注記**: 現行チェッカーのシグネチャに `slotLineId` は無い（LINE 軸は未 enforce・実測）。本機能でコアに `slotLineId` を渡す必要が生じるため、F03.4.2 §5.7 が予告した「機能B の LINE 軸 enforce」（`reservation_blocked_times.resource_type='LINE'`）を**同じ弾で併せて配線する**（同一の引数追加で両方が成立し、二度手間を避ける）。

### 4.3 全日型・409 ガード・impact

- **全日型（start/end NULL）の週次ルールは作らない**（設計判断）: 「毎週火曜は終日休み」は営業時間（`reservation_business_hours.is_open=FALSE`）の領分であり、二重の表現手段を作ると「どちらを直せば休みが消えるのか」が分からなくなる。ダイアログに「終日休みにしたい場合は営業時間で火曜を定休日にする」ガイドを表示（§11 i18n）。
- **作成/更新時の 409 ガード（RESERVATION_027 再利用）＋ impact**: 機能B と同じく、overlap する active 予約（PENDING/CONFIRMED）が存在すれば 409。週次ルールは将来無限に効くため、**判定対象は「今日から 90 日先までの active 予約」**とする（枠の生成 horizon は 28 日・手動枠を含めても 90 日で実用上全件。90 日超の先の予約が後から衝突した場合は runtime 判定が新規予約を止めるだけで、既存予約はそのまま有効——この限界を設計書に明記し、AC R-6 で境界を固定する）。
  - impact: `GET /reservation-recurring-blocked-times/impact?dayOfWeek=&startTime=&endTime=&lineId=`（副作用ゼロ・氏名込み一覧＋対象日付リスト。機能B impact と同じ管理画面用途）。
  - クエリ: 既存 `findActiveReservationsOverlappingUnavailability`（機能B・親 §5.B）を写経し、「日付レンジ × 曜日フィルタ（アプリ層）× 時間帯 overlap × line 軸」で新設。

### 4.4 会員向け表示 — グリッドセルへの事由ラベル（additive・PII 非該当）

`ReservationGridResponse.GridCellDto`（record・実測 5 フィールド: slotId/startTime/endTime/state/price）に追加:

| フィールド | 型 | null | 説明 |
|-----------|---|------|------|
| `unavailableReason` | String | 可 | `state=UNAVAILABLE` かつ判定元が **`is_public=TRUE` の定期ルール**のときのみ `reason` を載せる。それ以外（単発 blocked_times 由来・is_public=FALSE）は null（従来表示と完全一致） |

- **record のため正準コンストラクタのシグネチャ変更**であり、既存の `GridCellDto` 構築箇所の同時改修が必須（F03.4.4 H-1 と同じ「additive=BE 無改修ではない」注意。既存構築箇所は null を渡す機械的改修・アサーション変更ゼロが合格条件）。OpenAPI 再生成＋`generate:types` 同一 PR（`project_openapi_json_chronic_drift`）。
- 複数ルール該当時は**開始時刻昇順で最初の public ルール**の reason を採用（決定的・AC R-4）。
- **単発 blocked_times（機能B・常に非公開）と public 定期ルールが同一セルに重畳する場合は `unavailableReason=null`（非公開優先・精査1パス反映）**: 単発ブロックは臨時休業・私用等の非公開事由であり、そこへ「研修」ラベルを重ねて表示すると誤情報（休業日なのに研修と案内される）になる。判定順は「単発該当 → null 確定」→「public 定期のみ該当 → reason 表示」（AC R-4 混在ケース）。
- `reason` は設備・業務ラベルとして設計するが、**管理者の自由入力である以上、個人名の混入を実防御する**（精査1パス反映）: `is_public=TRUE` のとき reason 入力欄の直下に「**事由に個人名やお客様情報を含めないでください（会員全員に公開されます）**」の注意ガイドを**必須表示**し（§11 `reason_no_pii`・AC R-9）、プレースホルダに推奨語彙（「例: 研修・スクール・清掃・メンテナンス」§11 `reason_placeholder`）を提示する。is_public トグル直下の「会員に公開されます」注意（§11 `is_public_note`）と併用。
- FE（`SlotMatrixPicker`。※`SlotGridPicker` は 2026-08-04 に撤去済み・F03.4.4 §5.4）: UNAVAILABLE セルに小さくラベル文字列を描画（×印＋「研修」）。ツールチップにも同値。**単発 blocked_times の `reason` は従来どおり非公開のまま**（is_public の単発拡張は §4.5）。

### 4.5 管理UI — 週間スケジュール画面への統合（「営業の設計図」が1画面）

- `SlotTemplateManager.vue` を **`WeeklyScheduleManager.vue`** へ発展（§3.2 ④）: 曜日ごとのグルーピング表示に、**枠テンプレ（青系）と定期予約不可（赤系・事由ラベル付き）を並べて表示**する。各曜日行に「＋枠テンプレ」「＋予約不可」の 2 つの追加ボタン。定期不可の追加ダイアログは `ReservationUnavailabilityManager` の金型（時刻 Select・impact 警告）を写経し、`reason`（必須）・`is_public` トグル・`line` Select（既定=チーム全体）を持つ。
- 単発の予約不可枠（機能B・`ReservationUnavailabilityManager`）は詳細設定 Accordion に残置（単発は「例外」、週次は「設計図」という位置づけ分離）。
- **バックログ起票**: 単発 `reservation_blocked_times` への `is_public` 追加（会員に「7/20 は設備点検」を見せたいユース）。本書スコープ外・親 §8 バックログへ追記は実装弾で行う。

### 4.6 API設計

| メソッド | パス | 認証 | 説明 |
|---------|-----|------|------|
| GET | `/api/v1/teams/{teamId}/reservation-recurring-blocked-times` | ADMIN+ | 一覧（曜日→開始時刻順） |
| POST | 同上 | ADMIN+ | 作成（上限50・409 ガード） |
| PATCH | `.../{ruleId}` | ADMIN+ | 部分更新（null=据え置き・`clearLineId` で全体へ・`isActive` 切替。更新も 409 ガード） |
| DELETE | `.../{ruleId}` | ADMIN+ | 物理削除（履歴価値なし・論理削除にしない。`is_active=FALSE` が一時停止の手段） |
| GET | `.../impact` | ADMIN+ | 90日 horizon の overlap active 予約プレビュー（副作用ゼロ） |

- 全 5 本 `@PreAuthorize("@accessGuard.isScopeAdmin(authentication, #teamId, 'TEAM')")`。IDOR: `{ruleId}` は `findByIdAndTeamId` 解決・他チームは 404=RESERVATION_046。
- request DTO は親 §4.B の Jackson 手本（`@Getter @RequiredArgsConstructor`＋全 final＋単一コンストラクタ。`@JsonCreator` 地雷回避）。`dayOfWeek` は `ReservationDayOfWeek` enum 型（不正値は Jackson 400）。

**POST リクエスト例**
```json
{
  "lineId": null,
  "dayOfWeek": "TUE",
  "startTime": "19:00",
  "endTime": "20:00",
  "reason": "研修",
  "isPublic": true
}
```

**レスポンス（201）**: `{ "data": { "id": "0198...", "lineId": null, "lineName": null, "dayOfWeek": "TUE", "startTime": "19:00", "endTime": "20:00", "reason": "研修", "isPublic": true, "isActive": true, "createdAt": "..." } }`

---

## 5. C) 予約対象の呼称チーム設定化

現状は全業種で「予約対象」（実測: `reservation.tab.line_manage`=「予約対象の管理」等）に固定。美容室なら「担当スタッフ」、飲食なら「席」、テニスコートなら「コート」が自然。**マスター確定: プリセット = 担当スタッフ／席／コート／ベッド／レーン ＋ 自由入力**。

### 5.1 DB設計 — `reservation_team_settings` への ALTER

既存テーブル（V108・1チーム1行・UUIDv7）に追加:

| カラム名 | 型 | NULL | デフォルト | 説明 |
|---------|---|------|-----------|------|
| `resource_name_type` | VARCHAR(10) | NO | `'DEFAULT'` | 呼称プリセット。enum `ReservationResourceNameType { DEFAULT, STAFF, SEAT, COURT, BED, LANE, CUSTOM }` |
| `resource_name_custom` | VARCHAR(30) | YES | NULL | `CUSTOM` のときの自由入力呼称（全言語共通でそのまま表示）。`CUSTOM` 以外では NULL に正規化 |

- **`DEFAULT` の追加はマスター確定プリセットの変更ではない**（設計判断の明記）: 確定 6 種（STAFF/SEAT/COURT/BED/LANE/CUSTOM）はそのまま。`DEFAULT` は「未設定チームが従来の汎用呼称『予約対象』のまま動く」ための**後方互換フォールバック**であり、既存挙動の無変更を保証するために必要（DB DEFAULT で既存行・レコード未作成チームとも自動充足）。
- `CUSTOM` かつ `resource_name_custom` 空は 400（Bean Validation + Service 正規化）。30 文字上限は UI 幅（タブ名・フォームラベル）由来。
- 更新 API は**既存 `PATCH /reservation-settings`**（`UpdateReservationSettingRequest`・部分更新 null=据え置き・実測）に `resourceNameType` / `resourceNameCustom` を追加（additive）。`ReservationSettingsResponse` にも同 2 フィールド追加。設定 UI は詳細設定 Accordion ではなく**②予約対象セクションの先頭**（LineManager 上部）に置く（呼称は予約対象管理の入口概念のため）。

### 5.2 FE — 呼称の動的差し込み

`useResourceName(teamId)` composable（新設）が `reservationSettings` から表示名を解決し、以下へ差し込む:

| 差し込み先 | 現状キー（実測） | 改訂 |
|---|---|---|
| 管理タブ名 | `reservation.tab.line_manage`（「予約対象の管理」） | `{resourceName}` パラメータ化（「{resourceName}の管理」） |
| LineManager 見出し・追加ボタン | `reservation.line_manage_title` / `reservation.button.add_line` | 同上 |
| 予約フォーム・グループ予約のライン選択ラベル | `reservation.form.*` 系 | 同上 |
| マトリックス行ヘッダ | `reservation.matrix.date_line_header`（「日付・予約対象」） | 同上 |
| グリッド絞り込み | `reservation.grid.staff_filter.label`（「予約対象で絞り込む」） | 同上 |
| 使い方ガイド（`TeamReservationGuideContent` 等）の呼称箇所 | ガイド本文 | 同上（ガイドは文章のため置換箇所を実装弾で全数列挙し 1:1 照合——`feedback_subagent_enumeration_authoritative_source`） |

### 5.3 i18n 設計 — **パラメータ化＋プリセット呼称キーを採用**（呼称別キー全複製は不採用）

| 方式 | 内容 | 評価 |
|---|---|---|
| **(a) `{resourceName}` パラメータ化（採用）** | 文言キーは 1 本（例 `reservation.tab.line_manage = "{resourceName}の管理"`）。呼称はプリセット別キー `reservation.resource_name.{STAFF\|SEAT\|COURT\|BED\|LANE\|DEFAULT}` を 6 言語で持ち、`CUSTOM` は `resource_name_custom` をそのまま挿入 | キー数 = 文言 N ＋ 呼称 6。呼称追加・文言修正が独立 |
| (b) 呼称別キー複製 | 文言 N × 呼称 6 × 6言語のキー爆発（タブ名だけで 36 キー） | 保守不能・**不採用** |

- **6 言語での自然さ（複数形問題）への対策**: 文言テンプレートは**単複中立の名詞句構文**に統一する。例: 英 `"{resourceName} management"`（"Seats management" のような複数形変化を要求しない）、独 `"{resourceName}-Verwaltung"`、西 `"Gestión de {resourceName}"`。動詞句（"Manage seats"）のような数・格変化を要求する構文は使わない。プリセット呼称キー側は各言語で最も自然な単数名詞（en: `Staff member / Seat / Court / Bed / Lane`）を持つ。翻訳観点の検証は AC N-3。
- `CUSTOM` は利用者入力の生文字列を全ロケールでそのまま表示（翻訳しない・XSS は Vue の既定エスケープで担保、`v-html` 禁止）。

### 5.4 死蔵キー・関連ギャップの扱い（ここで確定）

| 対象 | 実測 | 決定 |
|---|---|---|
| `reservation.template.staff` / `reservation.template.staff_none` | **参照ゼロ**（grep 全 .vue/.ts。F03.4.2 §10 で定義されたがフォームに staff 欄が実装されなかった） | **6 言語とも削除**。テンプレの `staffUserId` は BE API に残置（互換）・FE は引き続き非露出（呼称 STAFF のチームは「ライン=担当者」として運用する整理） |
| `reservation.field.display_order` / `reservation.column.display_order` | `pages/admin/reservation-settings.vue`（スコープ切替型の旧ライン管理ページ）が使用中＝死蔵ではない | **キー残置・再利用**: `LineManager` に並び順編集（上下移動ボタン・`displayOrder` PATCH）を追加し同キーを参照する（現状 LineManager は name/description のみで並び替え不能＝機能ギャップの解消） |
| `pages/admin/reservation-settings.vue` そのもの | チーム管理タブの LineManager と機能重複 | 本書では**現状維持**。統廃合は要裁可論点（§14）として記録 |
| `reservation.menu.display_order` | 参照ゼロ（grep） | メニューの並び順編集を実装する際に再利用予定のため**残置**（削除しない。F03.4.1 の管轄） |

---

## 6. D) 実務機能 3 種＋レートリミット（**設計のみ・実装はマスターの別裁可**）

> 本節の 4 機能は設計を確定するが、**実装着手・Flyway 採番は行わない**。着手時に本節を正として軍議→試練→出陣に乗せる。

### 6.1 キャンセル待ち（waitlist）

> 親 §8 解決済み事項の「キャンセル待ちは Phase 3 スコープ外」は、本設計をもって**マスター指示により再起票**する（設計まで。実装は別裁可）。

#### 通知方式の設計判断 — **全員一斉通知・早い者勝ちを採用**（先着独占ホールドは不採用）

| 観点 | **全員一斉通知（採用）** | 先着 1 名へ順番通知＋N時間独占ホールド（不採用） |
|---|---|---|
| 並行制御 | 不要の追加なし。空き枠の取り合いは**既存のアトミック UPDATE（`incrementBookedCountIfAvailable`・capacity 基準）が既に解決済み** | 「ホールド中は他者の予約を弾く」仮予約状態が capacity 機構に割り込む＝FULL 判定の二重化 |
| バッチ | 不要（通知して終わり） | ホールド失効→次順位へ通知の連鎖バッチが必須 |
| 公平性 | 通知は同時・確保は先着（透明で説明しやすい） | 登録順に厳密だが、無反応者のホールドで枠が塩漬けになる |
| ADHD 配慮 | 「通知が来たら押す」の単純動線 | 「あなたの番です・期限あり」はプレッシャー型 |

#### DB — `reservation_waitlist_entries`（新規・UUIDv7）

| カラム名 | 型 | NULL | デフォルト | 説明 |
|---------|---|------|-----------|------|
| `id` | BINARY(16) | NO | UUIDv7 | PK |
| `team_id` | BIGINT UNSIGNED | NO | — | FKなし・INDEX |
| `slot_id` | BIGINT UNSIGNED | NO | — | FK → `reservation_slots.id` **ON DELETE CASCADE**（同一ドメイン・枠が物理削除されたら待ちも消滅が正） |
| `user_id` | BIGINT UNSIGNED | NO | — | FKなし（users ドメイン） |
| `status` | VARCHAR(20) | NO | `'WAITING'` | `WAITING` / `CANCELLED`（本人取消）/ `CONVERTED`（予約成立）。「期限切れ」は行を残さず**枠開始時刻経過で導出**（クリーンアップは §6.1 バッチ節） |
| `notified_at` | DATETIME(6) | YES | NULL | 最終通知時刻（再通知間隔制御） |
| `created_at` / `updated_at` | DATETIME(6) | NO | — | |

```sql
INDEX idx_rwe_slot_status (slot_id, status)
INDEX idx_rwe_user_status (user_id, status)
```

- **重複登録ガードはアプリ層**（`existsBySlotIdAndUserIdAndStatus(WAITING)` → 409=RESERVATION_047）。DB UNIQUE は張らない（設計判断: `UNIQUE(slot_id,user_id)` は「取消→再登録」を恒久に塞ぐ。MySQL に部分 UNIQUE がなく、生成カラム方式の複雑さはこの規模のレースリスク——同一ユーザーの同時二重クリック——に見合わない）。
- 上限: 1 ユーザー同時 WAITING 10 件・1 枠あたり 50 件（RESERVATION_049）。
- 対象なし（本人取消の対象 WAITING が無い・IDOR 秘匿含む）: 404=RESERVATION_046。

> **⚠ 本節のエラーコードは §10 の「実採番の確定結果」表（RESERVATION_046〜050）に合わせて確定済み**（W2-4 実装時に固定。design 初版が仮置きしていた 049〜051 は誤り・実物ではない。以下 API 表・実装（`ReservationWaitlistDialog.vue`/`ReservationMyWaitlistList.vue`・W2-4-FE）は確定コードで結線済み）。

#### API・結線

| メソッド | パス | 認証 | 説明 |
|---------|-----|------|------|
| POST | `/api/v1/teams/{teamId}/reservation-slots/{slotId}/waitlist` | 会員/公開（view ゲート＝予約作成と同一 `ReservationViewAccessGuard`） | 登録。**枠が FULL のときのみ**（AVAILABLE 枠は 400=RESERVATION_048「空きがあるためそのまま予約してください」）・過去/CLOSED 枠は既存検証再利用 |
| DELETE | 同上 | 本人 | 自分の WAITING を CANCELLED に（対象なしは 404=RESERVATION_046） |
| GET | `/api/v1/users/me/reservation-waitlist` | 本人 | 自分の待ち一覧（枠情報同梱） |
| GET | `.../reservation-slots/{slotId}/waitlist/count` | ADMIN+ | 枠別の待ち人数（管理画面表示用。**会員には人数を見せない**——競争心理の煽り防止・PII なし）。チーム解決と検査順序（D 群注記・精査2パス N）: パスの `{teamId}` に対する `@PreAuthorize isScopeAdmin` が先（非 ADMIN は **403**）→ その後 `slotId` を `findByIdAndTeamId(teamId)` で解決し、他チームの slot は **404** 秘匿（403→404 の順で一意） |

- **通知トリガ**: 空き復帰点は `decrementBookedCountAndReopen`（実測: キャンセル・却下経路が呼ぶ）に集約されている。この呼び出し成功（1 行更新）後に `ReservationSlotReopenedEvent` を発行し、`@TransactionalEventListener(phase = AFTER_COMMIT)` ＋ `@Async`（親 §「ADMIN 通知フロー」と同型・REQUIRES_NEW 規約 `feedback_transactional_event_listener_requires_new`）で当該 slot の WAITING 全員へ `NotificationHelper.notify`（アプリ内＋WebPush・新 `NotificationType.RESERVATION_WAITLIST_OPENING`・HIGH・"RESERVATION" カテゴリ、`RESERVATION_REMINDER` の隣に追加）。
- **再通知の抑制**: 同一 entry への通知は `notified_at` から **60 分未満なら送らない**（キャンセル連発時の通知洪水防止）。
- **成立時の消し込み**: `createReservation` / グループ作成の成功時、同一 (slot, user) の WAITING を CONVERTED へ（同一 tx 内・reservation ドメイン内）。
- **失効クリーンアップ**: 日次バッチ（生成バッチと同じ AM 0:15 帯を避け 0:45・`@SchedulerLock`）で「slot 開始時刻を過ぎた WAITING」を物理削除（履歴価値なし）。
- **FE（W2-4-FE・2026-07-30 実装済）**: `SlotMatrixPicker.vue`/`SlotGridPicker.vue`（後者は 2026-08-04 撤去済み・F03.4.4 §5.4）の満席（BOOKED）セルをクリック可能にし、`ReservationWaitlistDialog.vue`（新規）で登録/取消を行う。既に自分が WAITING 登録済みのセルはラベルを「待機中」に切り替える（`listMyWaitlist` を picker マウント時に1回だけ取得し slotId 集合化。グリッド全セル分の N+1 呼び出しは避ける）。ADMIN 向けの件数（`GET .../waitlist/count`）はダイアログを開いたときのみ1回取得（会員には出さない・上表の方針どおり）。自分の WAITING 一覧・取消は `ReservationMyWaitlistList.vue`（新規）として「予約一覧」タブに埋め込み。**文言は「全員一斉通知・早い者勝ち」方式に統一**し、「優先的に確保」「自動で予約されます」等の独占確保／自動繰り上げ表現は使わない（`reservation.waitlist.notice`）。

### 6.2 定期予約（毎週繰り返し・会員側）

会員が予約確定時に「この予約を毎週繰り返す」を選ぶと、**同一ライン・同一曜日・同一時間帯**の枠を N 週分まとめて予約する。

#### 満席週の扱い — **スキップ＋結果明細を採用**（中断は不採用)

| 観点 | **スキップ（採用）** | 満席週で中断 |
|---|---|---|
| 結果 | 「8週指定→6件成立・2件スキップ（7/21 満席・8/4 枠なし）」を**1 回の操作で確定**し明細を返す | 途中で止まり残り週の再操作が必要（ADHD 配慮に反する） |
| 整合 | 各週は独立予約（後述 series）なので歯抜けでも矛盾なし | — |

#### 設計

- **リクエスト拡張（additive）**: 単枠 `POST /reservations` とグループ `POST /reservation-groups` に `repeatWeeks: Integer`（省略/1 = 従来どおり・2〜**12** 上限。13 以上は 400=RESERVATION_052）。
- **週ごとの枠解決**: 起点予約の (lineId, dayOfWeek, startTime, endTime〔グループは構成セル列〕) を `slot_date + 7k` で検索。「枠が存在しない」「FULL」「予約不可枠 overlap」の週はスキップし、明細（`skippedWeeks[] = {date, reason: NOT_GENERATED|FULL|BLOCKED}`）で返す。horizon（28日）超の週は枠未生成のため大半 NOT_GENERATED になる——**FE は repeatWeeks の選択肢を「先の週は枠が作られ次第の手動予約」ガイド付きで 4 週までを推奨表示**し、5〜12 週は展開して選べる段階開示にする（枠生成が 28 日 rolling である限り、12 週指定の後半は構造的にスキップされる。この制約を UI で正直に伝える——症状を隠さない原則）。
- **series 表現**: `reservations` に `recurring_series_id BINARY(16) NULL` を ALTER 追加（アプリ層採番 UUIDv7・専用親テーブルなし——`group_id` 案(b) と同じ方式。`group_id`（同日連続枠の横軸）とは**独立直交**し併存可能）。INDEX `idx_rv_series (recurring_series_id)`。
- **キャンセル動線**: 予約詳細に「この回のみ / 以降すべて」の 2 択（`DELETE /reservations/{id}/cancel` に `scope: THIS_ONLY|THIS_AND_FOLLOWING` を追加・既定 THIS_ONLY。THIS_AND_FOLLOWING は series 内の当該日以降の active 予約を既存キャンセル検証（締切・権限）を**各行に適用**しながらループ）。グループ予約の series はグループ単位で同 2 択。
- **MANUAL 承認チームの負荷**: N 週分がすべて PENDING になるため、管理者の予約一覧に series まとめ承認（`scope=SERIES` の confirm）を追加。単票 confirm も従来どおり可能。
- レートリミット: §6.4 の予約作成バケットで **1 series = 1 消費**（週数ぶん消費しない。1 操作は 1 意思決定のため）。

#### W2-5 実装で確定した事項（2026-07-30・実装が正）

> 以下は W2-5（BE 実装弾）で実物と突き合わせて確定した内容。上記の初版記述と齟齬がある場合は本節が正。

**Flyway / ErrorCode**: `V173.20260730000842__alter_reservations_add_recurring_series.sql`（**当初 V172 で採番したが、push 直前の `git fetch origin main` で `V172.20260729233557__add_expired_at_to_signage_access_tokens.sql` が並行着地していたため V173 へ再採番。タイムスタンプ minor は維持**——W2-2 の V151→V168 と同じ作法）／`RESERVATION_054 = RECURRING_RESERVATION_LIMIT_EXCEEDED`（400・実 enum 最大 053 の次）。初版 §10 の「repeatWeeks 上限 = 052」は仮置きであり実物は **054**。

**API 契約（additive・既存フィールドの型変更・削除なし）**

| 対象 | 追加フィールド | 意味 |
|---|---|---|
| `POST /teams/{teamId}/reservations`（body） | `repeatWeeks: Integer`（`@Min(1)`・省略/1 = 従来同一） | 2〜12 で定期予約。**上限検証は Service 層**（`@Max` にすると汎用 400 になり専用文言を出せない） |
| `POST /reservations/{id}/cancel`（body） | `scope: THIS_ONLY \| THIS_AND_FOLLOWING`（省略 = THIS_ONLY） | 会員のキャンセル動線専用。管理者キャンセル（`cancelByAdmin`）は参照しない |
| `POST /teams/{teamId}/reservations/{id}/confirm`（**query**） | `scope: THIS_ONLY \| SERIES`（省略 = THIS_ONLY） | series 一括承認。管理者ゲートは従来どおり `isScopeAdmin(#teamId)` |
| `ReservationResponse` | **`recurringSeriesId`（UUID・nullable・トップレベル）** | **一覧・詳細 GET でも常に返る**。FE がキャンセルスコープ 2 択 UI / series 一括承認ボタンを出し分けるための唯一の判定材料（検分 MUST①） |
| `ReservationResponse` | `recurring` / `recurringCancel` / `recurringConfirm`（いずれも従来経路では **null**） | 各操作の**結果明細**。Mapper は `ignore` するため<b>GET では常に null</b>。`skippedWeeks[] = {date, reason, reservationId}`・`reason` は `RecurringWeekSkipReason` の **enum**（検分 MUST②） |

> **`recurringSeriesId` をトップレベルに置く理由（検分 MUST①）**: 結果明細 3 DTO は作成・キャンセル・承認の応答でしか埋まらないため、series 所属をそこに閉じ込めると FE は一覧・詳細から「この予約が定期予約の一部か」を判定できず UI を出し分けられない。API 境界は BE 弾で閉じる（`feedback_fe_be_parallel_api_boundary_after_generated_types`：API 境界は generate:types 後でなければ FE/BE を並行できない）。

**HTTP ステータスの明記（AC-5-13 との差異・正直な記録）**: AC-5-13 は「全週スキップでも **200**」と表記していたが、`POST /reservations` は<b>作成 EP であり成功は 201</b>である（既存契約）。実装は「2 週目以降が全スキップでも**エラーにせず 201 で明細を返し、`series_id` は発行しない**」とした。AC の意図（エラーにしない・明細を返す・series を発行しない）は満たすが、**ステータスコードは 200 ではなく 201**である。

**スキップ理由の語彙（初版 3 値 → 実装 9 値）**: 初版は `NOT_GENERATED|FULL|BLOCKED` の 3 値だったが、実装では `CLOSED`（受付停止）・`ALREADY_RESERVED`（その枠に既に自分の予約がある）・`UNAVAILABLE`（上記以外の競合）を追加した（`RecurringWeekSkipReason`）。3 値に丸めると会員に**嘘の理由**を伝えることになるため（「症状を隠さない」原則）。加えてキャンセル/承認の明細用に `NOT_CANCELLABLE` / `CANCEL_DEADLINE_PASSED` / `NOT_PENDING` を同一 enum に持たせている。判定順は `CLOSED → FULL → BLOCKED → ALREADY_RESERVED`（決定的・FE 文言の非決定性を防ぐ）。

**トランザクション境界（AC-5-5 の根治点）**: オーケストレーター `ReservationRecurringService` には **`@Transactional` を付けない**。週ごとの作成は別 Bean（`ReservationService.createReservationForSeries`）へ委譲することで週ごと独立トランザクションになる。外側に tx を張ると、内側の `@Transactional` から例外が抜けた時点で participating tx が rollback-only にマークされ、外側が例外を握っても最終コミットが `UnexpectedRollbackException` で失敗する＝**1 週の失敗が全週を巻き込み**スキップ設計が実質無効化される（`ReservationPendingExpireService` が同じ理由でバッチ本体を非トランザクションにしているのと同型）。この制約から、**`repeatWeeks` の分岐は Controller（tx を開く前の層）に置く**。

**N+1 の禁止（AC-5-10）**: 週次枠解決 `ReservationRecurringSlotResolver` は固定本数クエリ（① 候補枠の `slot_date` **範囲検索 1 回** ② 単発 blocked_times の範囲 1 回 ③ 定期ルールのチーム単位 1 回 ④ 自分の既存 active 予約を候補枠 ID でまとめて 1 回。候補ゼロなら ④ は発行しない）。`ReservationSlotRepository` に **(teamId, slotDate 範囲, startTime, endTime) の finder が存在しなかった**ため `findRecurringCandidateSlots` を新設した。ライン軸は SQL 条件に入れず `Objects.equals` でメモリ突合する（共通枠 `line_id IS NULL` との null 同値比較を JPQL に書くと方言依存になるため）。

**IDOR の構造的防御（AC-5-8 / AC-5-9）**: series を引く finder は **`...AndUserIdOrderById`（キャンセル用）と `...AndTeamIdOrderById`（一括承認用）の 2 本のみ**を用意し、**スコープ条件なしの finder は意図的に作らない**。「認可条件を Service 層のフィルタに置く」と、いつか誰かが素の finder を使って漏れる（`feedback_centralized_helper_not_proof_all_paths_use_it`）。

**締切判定は行ごと**: `THIS_AND_FOLLOWING` の締切検証は**行のチームのポリシーで行ごとに**適用する（チーム単位でメモ化しクエリは増やさない）。週次 series は「後の回ほど締切に余裕がある」ため、起点と同じ判定を流用する実装でも実用上は同じ結果になるが、それでは AC-5-7 の「各行に適用」を満たさない。

**起点回も他の回と同じ規則でスキップする（検分 MUST④）**: `THIS_AND_FOLLOWING` では起点回も他の回と同列に扱い、締切超過・状態不整合なら**例外にせずスキップ明細に載せて以降の回の処理を続ける**。起点回だけ 400 で全体を失敗させると「今日の回は締切だが来週以降はまとめて消したい」という当然の操作ができない。全回がスキップされて 0 件になってもエラーにせず明細を返す（AC-5-13 と同じ思想）。会員キャンセル通知は**起点回が実際にキャンセルされたときだけ** 1 回発行する（スキップされた回について「キャンセルされました」と通知しない）。なお **`THIS_ONLY`（既定・単票キャンセル）の締切超過は従来どおり 400** で挙動不変。

**i18n（AC-5-14）の扱い**: BE 側に i18n 追加は**不要**であることを確認した。通知本文・`cancel_reason` は DB 保存用の定型文（`ReservationPendingExpireService.CANCEL_REASON` と同じ扱い・FE の翻訳対象ではない）で、通知種別も既存 `RESERVATION_CANCELLED` を再利用するため新規 MessageSource ラベルキーも生じない。`reservation.recurring.*`（`repeat_weekly` / `weeks` / `result` / `cancel_scope_this` / `cancel_scope_following` / `horizon_hint`）×6 言語は **FE 隊（第四陣）の担当**として §11-D の記載どおり据え置く。

**スコープ外（W2-5 では実装しない）**: グループ予約（`POST /reservation-groups`）の `repeatWeeks`。初版 §6.2 は「グループは構成セル列で解決」と述べているが、AC-5-1〜17 は単枠 series のみを規定しており、グループ series は「グループ単位の 2 択キャンセル」まで含めて別弾とする（`recurring_series_id` は `group_id` と直交して併存できる設計なので、後から追加しても DDL 変更は不要）。FE（`repeatWeeks` の段階開示 UI・`horizon_hint`・キャンセルスコープ 2 択・series 承認ボタン）も別隊。

#### W2-5-FE 実装で確定した事項（2026-07-30・実装が正）

> BE（上記）は PR #2536 で main 着地済み。以下は **FE 隊（第四陣）** の実装内容。生成型（`frontend/app/types/generated/index.ts`）を単一ソースとし、`frontend/app/types/reservation.ts` は再エクスポートのみ（`RecurringSeriesDto`/`RecurringCancelDto`/`RecurringConfirmDto`/`RecurringWeekOutcomeDto`）で手書き型を新規に起こしていない。

**① 会員: 段階開示トグル（`ReservationForm.vue`）**
- 「この予約を毎週繰り返す」トグル（既定 OFF）。ON にすると 2〜4 週の推奨 pill（quick select）＋「もっと長く（5〜12週）」が現れ、後者を開いた先にのみ 5〜12 週の `Select` が出る（最初から 1〜12 のスピナーを晒さない・AC-5-14 の段階開示指示に対応）。
- 12 を超える値は選択肢自体に存在しないため UI からそもそも送信できない（BE の 400=RESERVATION_054 に頼り切らない・FE 側でも構造的に弾く）。
- `repeatWeeks` は `repeatEnabled ? repeatWeeks : undefined` として `createReservation` に additive で渡す（トグル OFF・省略時は従来と完全同一の単発予約）。

**② 会員: 結果明細（作成直後・同一 `ReservationForm.vue`）**
- 応答 `data.recurring`（`RecurringSeriesDto`）が非 null のとき、ダイアログを通常フォームから結果パネルへ切り替える（`close()` を呼ばずダイアログ内に留める）。「{created}件を予約しました（{skipped}件はスキップ）」＋スキップ週の日付と理由一覧を表示する。
- **成立0件（全週スキップ）でもエラーにせず、`all_skipped_notice` メッセージ＋理由つき明細を表示する**（検分 MUST④・AC-5-13 と同じ「黙殺しない」思想を FE 側でも徹底）。
- `RecurringWeekSkipReason` の**9値すべて**に `reservation.recurring.skip_reason.*` の i18n 文言を用意（3値等へ丸めない）。

**③ 会員: キャンセル2択（`ReservationList.vue`）**
- `data.recurringSeriesId`（トップレベル・一覧/詳細 GET でも常に返る唯一の判定材料）が非 null かつグループ行でない場合、本人キャンセル（`cancelMine`）は通常の単一確認（`confirm.require`）ではなく、専用の 2 択ダイアログ（「この回だけ」＝`THIS_ONLY`／「この回以降すべて」＝`THIS_AND_FOLLOWING`）を経由する。null の予約・グループ行は従来どおり単一確認のまま（グループ series は W2-5 のスコープ外）。
- `scope` は `cancelByUser`（`POST /api/v1/reservations/{id}/cancel`）専用（`cancelByAdmin` は scope を参照しない・実装 `ReservationService.cancelByUser`/`cancelByAdmin` を実測して確認済み）。`useReservationApi.ts` の `cancelMyReservation` にのみ `opts.scope` を additive 追加し、`cancelReservation`（管理者用）のシグネチャは変更していない。
- 「この回以降すべて」の結果（`data.recurringCancel`）はキャンセル件数・スキップ週の理由つき明細を表示する。**0件でも正直に伝える**（全回が締切超過等ならその旨を明示）。

**④ 会員: series バッジ**
- `data.recurringSeriesId` が非 null の一覧行に「定期」バッジ（`Tag severity="info"`）を表示する。

**⑤ 管理者: series 一括承認（`ReservationList.vue`）**
- 承認制チームの一覧で `data.recurringSeriesId` が非 null かつ `PENDING` の行にのみ「series一括承認」ボタンを追加し、`confirmReservation(teamId, id, 'SERIES')`（scope はクエリパラメータ）を呼ぶ。認可は各行に BE 側で適用される（FE は導線を出すだけ）。

**⑥ 管理者: 定期予約不可枠の強行登録（`WeeklyScheduleManager.vue`）**
- 既存の impact プレビュー（`getRecurringBlockedTimeImpact`・作成/更新前に自動実行）で `affectedCount > 0` のとき、通常の保存ボタンは従来どおり disabled のまま（回帰なし）。加えて「衝突する予約をキャンセルして登録する」ボタンを表示する。
- クリックすると**必ず確認ダイアログ**（件数と「申込者に通知が送られます」を明示）を経由してから `createRecurringBlockedTime`/`updateRecurringBlockedTime` に `forceCancelConflicting: true` を additive で渡す（既定 false・確認なしでは絶対に送信されない＝うっかり押せない形）。
- 応答 `forceCancelledCount` を通知（0件超のときのみ警告トーストで実件数を伝える。成功トースト自体は通常どおり別途出る）。

**⑦ i18n**: `reservation.recurring.*`（27キー）＋`reservation.recurring_block.force_cancel.*`（5キー）＝計32キーを新設。6言語（ja/en/zh/ko/es/de）で計601キー・完全一致をスクリプトで機械確認済み（キー数の差分なし）。§11 に実キー名を確定。

**⑧ vitest（12件追加・回帰ゼロ）**: `ReservationForm.spec.ts`（+5・段階開示/repeatWeeks送信/結果明細成立あり/全滅）・`ReservationList.spec.ts`（+4・バッジ/series一括承認/キャンセル2択×2）・`WeeklyScheduleManager.spec.ts`（+3・強行登録ボタン表示/確認後送信/件数通知/衝突なし時非表示）。既存122件は無修正 green。

**スコープ外（W2-5-FE でも実装しない）**: グループ予約（`GroupBookingDialog.vue`）の `repeatWeeks` UI・グループ series の2択キャンセル（BE 側が既にスコープ外のため対応するFEも無し）。

#### 🔴 §4.3 の 409 ガードとの構造的衝突と根治（殿の裁定・2026-07-30）

**問題**: §4.3 は「定期予約不可枠の登録時、今日から 90 日先までに active な予約があれば 409（`RESERVATION_027`）」と定める。一方 §6.2 は最大 12 週 = **約 84 日分**の予約を並べる。両節は互いに言及しておらず、結果として**会員 1 人が定期予約を入れるだけで、管理者は「毎週火曜19時は研修」を恒久的に登録できなくなる**。

**裁定（実装済み）**: 定期予約不可枠の **POST / PATCH** に `forceCancelConflicting: boolean`（既定 `false`）を追加する。

| 値 | 挙動 |
|---|---|
| 未指定 / `false` | **従来どおり 409**（挙動不変・回帰なし） |
| `true` | 90 日 horizon 内で overlap する active 予約を一括 CANCELLED（`cancelledBy=ADMIN`・`cancel_reason` は定型文）にし、**各申込者へ通知してから**登録する。レスポンスの `forceCancelledCount` に件数を返す（0 件でも 0＝force で実行された事実を管理者 UI が区別できる） |

- 管理者は事前に既存の `GET .../impact`（副作用ゼロ・氏名込み一覧）で件数と対象を確認できる。
- **グループ予約は兄弟行まで展開**して一括キャンセルする（1 行だけ消すと F03.4.3 が禁じる部分キャンセル＝`booked_count` 不整合・グループ状態の分裂になる）。
- **通知は「overlap 判定に直接ヒットした行」につき 1 通**（兄弟行の巻き添え分については送らない）。したがって<b>1 グループが兄弟スロット 2 枠でルール時間帯に跨る場合は、同一ユーザーへ 2 通飛ぶ</b>。通知を userId 単位に束ねて本文に消えた枠を全部列挙する集約は**別チケット**（検分 MUST⑤ で記述を実装の実挙動に合わせて訂正した）。
- 対象集合の解決は `impact`（事前確認）と強行キャンセル（実行）で**同一メソッドを共有**する。片方だけがグループ兄弟行を展開すると「impact で 3 件と確認して force を押したら 5 件消える」＝管理者に嘘の数を見せることになるため（検分 MUST③・ずれを固定するテストあり）。
- **通知は既存 `NotificationType.RESERVATION_CANCELLED`（HIGH / sourceType=RESERVATION）を再利用**する。実態が「管理者による予約キャンセル」と完全に一致しており、全ドメイン共有 enum に種別を足すと種別数ガード（`NotificationTypeTest`）や通知設定 UI まで巻き添えにするため。送出は `ReservationForceCancelledByBlockEvent` → `@TransactionalEventListener(AFTER_COMMIT)` + `@Async("event-pool")`（ロールバックされた登録では通知が飛ばない）。
- 枠復帰は `ReservationSlotService.decrementAndReopen` を経由するため、FULL→AVAILABLE 遷移が起きた枠では §6.1 のキャンセル待ち通知が自動連鎖する。
- 監査ログ: `RESERVATION_RECURRING_BLOCKED_TIME_FORCE_CANCELLED`（`cancelledCount` / `dayOfWeek` を meta に記録）。
- 既に終端状態（CANCELLED / COMPLETED / NO_SHOW）の行は二重キャンセルしない（`booked_count` の二重減算防止）。

「409 のまま運用で回避」は不可という判断であり、これは対処療法ではなく**機能の構造的破綻の根治**である。

### 6.3 仮押さえ自動失効（PENDING の期限切れ自動キャンセル）

MANUAL 承認チームで承認されないまま放置された PENDING が枠を塞ぎ続ける問題への対処。

- **チーム設定**: `reservation_policies` に ALTER 追加 `pending_expire_hours INT NULL DEFAULT 24`（**NULL = 自動失効しない／DB DEFAULT 24 で新規・既存とも既定 24 時間**——マスター確定「チーム設定・既定24h」。範囲 1〜168・更新は既存 `PATCH /reservation-settings` に additive 追加、`clearPendingExpireHours: true` で無効化——`clearApprovalMode` と同形）。
- **バッチ**: `ReservationPendingExpireBatchService`（新設・**`ReservationReminderDispatchBatchService` / `ReservationWaitlistCleanupBatchService` 写経**）。`@BatchEndpoint(name = "reservation-pending-expire")` ＋ `@Scheduled(fixedDelay = 300_000)`（5 分間隔・リマインドの 1 分と負荷帯を分離）＋ `@SchedulerLock(name = "reservationPendingExpireBatch")`。
- **失効対象（W2-6 実装時に改訂・殿の裁定 2026-07-29）**: チームの `pending_expire_hours` が有効（NULL でない）ことを前提に、以下の**いずれか**を満たす `status=PENDING`。
  - **(a)** `booked_at + pending_expire_hours <= now(注入 Clock)`
  - **(b)** **枠の終了時刻を経過している**（＝承認されないまま予約日を過ぎた仮押さえ）

  > **⚠ 初版からの変更点と理由**: 初版は (b) を「枠開始を過ぎた PENDING は既存の自動 NO_SHOW バッチの管轄」として**対象外**にしていた。しかし **予約ドメインに自動 NO_SHOW バッチは実在しない**（実測。存在するのは管理者手動の `POST /{reservationId}/no-show` のみ。`RecruitmentNoShowDetectBatch` は recruitment ドメインの別機能）。初版のままでは「承認されないまま予約日を過ぎた仮押さえ」が**永久に PENDING で残り枠を塞ぎ続ける**——本機能の目的を半分取りこぼす。よって (b) を対象に含める。
  >
  > **ポリシー行が無いチームの扱い**: `reservation_policies` は初回の設定変更で初めて行が作られるため大半のチームは行を持たない。行が無いチームは **既定 24 時間**として扱う（`COALESCE`。本節の「新規・既存とも既定 24 時間」および `getOrDefault` が GET で 24 を返すことと挙動を一致させる）。一方、行が存在して `pending_expire_hours IS NULL` のチーム（管理者が `clearPendingExpireHours` で明示的に無効化）は (a)(b) とも対象外。
- **処理**: `ReservationEntity.cancel(reason, CancelledBy.SYSTEM)`（**enum 実在・実測**）で `status=CANCELLED`・`cancelReason` に定型文 → **`ReservationSlotService.decrementAndReopen`**（設計初版の `decrementBookedCountAndReopen` は実在せず、実メソッド名は `decrementAndReopen`）で枠復帰。同メソッドは DB が実際に FULL→AVAILABLE 遷移を起こしたときのみ `ReservationSlotReopenedEvent` を発行し、§6.1 のキャンセル待ち通知が AFTER_COMMIT で連鎖発火する（**独自にイベントを撃たない**統合点）→ 申込者へ通知（新 `NotificationType.RESERVATION_PENDING_EXPIRED`・NORMAL）。
- **トランザクション境界**: 失効 1 単位（単枠 1 件 / グループ 1 組）を `REQUIRES_NEW` の独立トランザクションにし、バッチ本体には `@Transactional` を付けない。バッチ全体を 1 tx で囲むと、内側の `@Transactional` メソッドから例外が抜けた時点で participating tx が rollback-only になり、**行単位 try/catch が実質無効化**される（1 件の失敗が全件を巻き込む）ため。これにより「単位内は原子的（部分失効なし）・単位間は独立」を両立する。対象抽出は slot・policy を join した固定 3 クエリ（候補・グループ兄弟行・枠）で N+1 を出さない。注入 `Clock`・最小 context 番人テスト（写経元と同じ作法）。
- グループ予約（PENDING の series/group）: 代表行基準でグループ単位に失効（部分失効を作らない——グループの原子性維持・F03.4.3 と整合）。
- **FE（W2-6-FE・2026-07-30 実装済）**: 管理者向け・会員向けの両方を実装した。
  - **管理者向け**: `ReservationPolicySettings.vue`（TeamReservationsPanel の詳細設定）に `pendingExpireHours`（1〜168）の数値入力＋「自動キャンセルしない」トグル（`clearPendingExpireHours`）を追加。NULL（自動失効なし）かつ承認モードが MANUAL のチームには「仮押さえは自動で取り消されません」の注意書きを表示する（AUTO チームは仮押さえが発生しないため非表示）。
  - **会員向け（当初案どおり実装）**: `ReservationForm.vue`（単枠）・`GroupBookingDialog.vue`（グループ・プレビュー画面）の両方で、予約確認ダイアログを開いたときに `GET /reservation-settings` を取得し、`approvalMode=MANUAL` かつ `pendingExpireHours` が非 NULL のときのみ「{n}時間以内に承認されない場合は自動的にキャンセルされます」（`reservation.pending_expire_notice.form_note`）を表示する。GET は ADMIN 限定ではなく view ゲート（会員/公開）のため会員側からも取得できる（`ReservationBusinessHourController` の `viewAccessGuard` フィールド Javadoc に明記）。NULL のとき・AUTO 承認のときは表示しない（管理者向け注意書きと同一方針）。

### 6.4 予約作成の頻度レートリミット（親 §8 バックログ⑩の設計確定）

- **単枠 `POST /reservations` とグループ `POST /reservation-groups` を同一バケット**で数える（親 §6 既存記述どおり。別バケットだと 5+5 の買い占めが可能）。
- 実装: `ValkeyRateLimiter`（`ReservationWaitlistService` のユーザー軸実装を写経。`ReservationSlotTemplateService` の generate リミットはチーム軸）。zone `reservation-create`・**1 ユーザー 1 分 5 回**・超過は **429 = RESERVATION_053**（新規コード。044 は generate 専用のため共用しない——チーム軸/ユーザー軸の意味が異なる）。
  - **zone 名とリミット値は 2 クラスに重複定義せず** `ReservationCreateRateLimiter`（小さな共有 Component）に一元化し、単枠 `ReservationService.createReservation`（`viewAccessGuard.assertCanView` 直後）とグループ `ReservationGroupService.createGroup`（tx を無駄に開かないよう `transactionTemplate.execute` の外側）の**両方がそこを呼ぶ**。「共通ヘルパを作ったが片方の経路が使っていない」は当リポの典型事故のため、**両経路の到達をテストで証明**する（AC-6-12）。
- キャンセル待ち登録（§6.1 POST）は同 zone に**含めない**（別 zone `reservation-waitlist`・1 ユーザー 1 分 10 回・エラーコードは既存の **RESERVATION_050**——登録は軽量操作のため予約バケットを消費させない）。
- **FE（W2-6-FE・2026-07-30 実装済）**: `ReservationForm.vue`（単枠）・`GroupBookingDialog.vue`（グループ）の両方で 429=RESERVATION_053 受信時に汎用エラーではなく専用文言「予約作成の間隔が短すぎます。しばらく待ってから再度お試しください」（`reservation.message.rate_limited`）のトーストを表示する。

---

## 7. セキュリティ考慮事項

- **認可**: §3/§4/§5 の管理系は全て `@PreAuthorize("@accessGuard.isScopeAdmin(authentication, #teamId, 'TEAM')")` self-gate（実シグネチャ 3 引数）。§6 の会員系（waitlist・定期予約）は予約作成と同一の view ゲート（`ReservationViewAccessGuard` / Service 層 isMember or allow_public）。
- **IDOR**: 新規 `{ruleId}` / waitlist entry は `findByIdAndTeamId`（entry は `AndUserId`）解決・他チーム/他人は 404 で秘匿。
- **PII**: `reason`（事由ラベル）・呼称は設計上 PII 非該当だが、**管理者の自由入力に個人名が混入するリスクは UI で実防御**する（is_public=ON 時の必須注意ガイド＋推奨語彙プレースホルダ・§4.4/R-9）。impact レスポンスの予約者氏名は管理画面用途のみ（機能B と同一整理）。waitlist の会員向け応答に**他人の user_id/氏名/人数を含めない**（count は ADMIN 専用）。
- **資源保護**: 定期ルール上限 50・waitlist 上限（ユーザー10/枠50）・repeatWeeks 上限 12・generate-single-day は既存レートリミット zone 共有・予約作成レートリミット（§6.4）。
- **監査ログ**: 定期ルール CRUD・呼称変更・単日テンプレ適用・pending_expire 設定変更を `audit_logs` に記録。
- **XSS**: `resource_name_custom`・`reason` は Vue 既定エスケープ表示のみ（`v-html` 禁止）。

---

## 8. Flywayマイグレーション

```
-- W2 実装弾（§13 の W2-1〜W2-3）で採番
V146.<タイムスタンプ>__create_reservation_recurring_blocked_times.sql
V146.<タイムスタンプ>__alter_reservation_team_settings_add_resource_name.sql

-- D群（別裁可・着手時に採番し直す）
V<採番未定D>.<タイムスタンプ>__create_reservation_waitlist_entries.sql
V<採番未定D>.<タイムスタンプ>__alter_reservations_add_recurring_series.sql

-- W2-6 実装で確定（PR 直前に origin/main を再確認したところ V170 が並行着地していたため V171 へ再採番・2026-07-29）
V171.20260729082512__alter_reservation_policies_add_pending_expire.sql

-- W2-5 実装で確定（PR 直前に origin/main を再確認したところ V172 が並行着地していたため V173 へ再採番・2026-07-30）
V173.20260730000842__alter_reservations_add_recurring_series.sql
```

> **⚠ 採番規約**: major は「マージ時点の origin/main 全体の最大 major + 1」。観測時点の最大は **V145**（`V145.20260707153053__alter_reservations_add_group_and_menu.sql`・実測 2026-07-08）のため仮に V146 と表記するが、**マージ直前に origin/main を必ず再確認**すること（`feedback_migration_version_collision`）。**minor はタイムスタンプ必須**（`date -u '+%Y%m%d%H%M%S'`。連番 `.001` は番人テスト `FlywayTimestampNamingGuardTest` が機械的に拒否）。
>
> **既存データ番人**: `reservation_team_settings` の ALTER は既存行を DB DEFAULT（`'DEFAULT'`/NULL）で自動充足。from-scratch 既存行シード番人テスト（`FlywayExistingData...` 系写経）で「既存行が DEFAULT 充足で migration を通過し、応答の呼称が従来表示と一致する」ことを恒久検証する。`reservation_policies` の ALTER（D群）も同様に DEFAULT 24 充足を番人化。

---

## 9. 受け入れ条件（テスト可能・正常/異常/境界）

### 実機E2E の前提データ手順（/実機 用）

1. 準備順序: チーム作成 → **営業時間を新設 UI（BusinessHoursManager）から設定**（本書からは API 直叩き不要になったことが観測点 S-6）→ ライン → メニュー → 週間スケジュール（テンプレ保存で枠が自動生成されることを確認）。
2. 当日枠・時刻境界は手動 `POST /reservation-slots`（生成は tomorrow 以降のみ・F03.4.2 と同じ）。時刻境界 AC は固定 Clock 注入。
3. 認可 E2E は使い捨てチーム（`feedback_authz_e2e_seed_membership_pollution`）。

### A) 営業スケジュール中心モデル

- **S-1（保存＝自動生成・正常）**: テンプレ POST（MON 10:00-13:00・line=1）成功で、`reservation_slots` へ horizon 28 日内の月曜ぶん（30分×6セル×該当月曜数）が INSERT され、**応答が `SlotTemplateSaveResponse` 型で `data.generation.generatedCount` に生成数・`data.template.id` に保存結果が載る**（§3.1 契約が観測点）。**「今すぐ枠を作成」ボタンを押さずに**マトリックスへ枠が現れる。
- **S-2（自動生成の scope・境界）**: テンプレ A を保存しても、同チームの別テンプレ B の未生成分は増えない（当該テンプレのみ scope）。応答時間の観測点: 単一テンプレ最大 **96 INSERT**（24セル×同一曜日4回・§3.1 訂正値）で 1 秒未満（統合テストの目安・CI では件数のみ検証）。**複数曜日の新規作成**（W1 の `selectedDays` ループ・最大 7 曜日 = 7 呼び × 96 INSERT）でも全体が既定 API タイムアウト内に完了し、トーストは合算 1 本で出る（§3.1 集約規則・部分失敗時は `auto_generated_partial` 1 本）。
- **S-3（自動生成の失敗が保存を壊さない・異常）**: 生成段で例外を注入してもテンプレ行は保存済みで 200 系＋**`data.generation.failed=true`・カウント各値はコミット済みチャンク分**（§3.1 契約）。翌日次バッチ（差分レンジ）が全域を自己修復する（F-9 の観測点流用）。
- **S-3b（tx 境界・番人）**: 保存 tx の内側から生成を呼ばない実装であること（生成呼び出しが `@Transactional` 境界の外側＝コントローラ/SUPPORTS ファサードにあることのコードレビュー観点＋統合テストで「保存直後の生成が lock wait timeout を起こさない」実走確認。§3.1 の `fk_rs_template` 自己デッドロック罠）。
- **S-4（既存バッチ整合・回帰）**: 保存自動生成後の日次バッチは新規 INSERT = horizon 末尾差分のみ（F03.4.2 F-9 が無修正 green）。
- **S-5（UI 廃止）**: `SlotTemplateManager`（改め WeeklyScheduleManager）に generate ボタン・weeks Select が存在しない。`POST .../generate` は API として 200 を返し続け、OpenAPI 上 `deprecated: true`。
- **S-6（営業時間 UI・正常/異常/境界）**: ①BusinessHoursManager で火曜を `isOpen=false→true, 9:00-18:00` に保存すると、PUT が成功し、**応答（`BusinessHoursSaveResponse`）の `generation.generatedCount` に火曜テンプレぶんの生成数**が載る（営業時間拡大の自動追い付き）。②**生成 scope は変更曜日差分**: 火曜のみ変更した PUT で、無変更の月曜テンプレの未生成分は増えない。③**生成失敗注入時も PUT は 200 で返り `generation.failed=true`** が表示される（営業時間の保存自体は成立・トーストで失敗を正直に報告）。④タイミング系回帰: 7 曜日全変更（最悪ケース）でも**既定の API タイムアウト内に 200** が返る。⑤**縮小/定休化ガイド**: 営業時間を縮小（または `isOpen` を false 化）する保存操作で、確認 UI に `shrink_note`（「既存の未来枠は残る・恒久的に閉じるなら例外日カレンダー」）が表示される（vitest: 縮小検知の表示分岐）。
- **S-7（休業ショートカット・正常/異常）**: 例外日カレンダーで日付選択→「この日を休業にする」→ `reservation_blocked_times` に全日 TEAM 行が 1 行増え、その日の全セルが UNAVAILABLE になる（機能B 経路の再利用確認）。**ダイアログ表示時に impact API が全日条件（startTime/endTime なし）で呼ばれ、`affectedCount` と予約者氏名込み一覧が返って警告カードに描画される**（観測点: impact レスポンスと警告表示の一致）。既存 active 予約がある日は登録ボタン disabled、強行 API 直叩きは 409=027。
- **S-8（臨時営業・正常/境界/ウォーターマーク）**: ①定休日（火曜 `is_open=FALSE`）に `generate-single-day {date=火曜日}` を実行すると、火曜テンプレ構成のセルが**営業時間チェックなしで**生成される。`sourceDayOfWeek=MON` 指定で月曜ダイヤが適用される。②同一日への再実行は `skippedExistingCount` のみ増え枠は増えない（冪等）。当日・過去日は 400=023。対象曜日テンプレ 0 件は 400。91 日以降の日付は 400。③**horizon 外実行後の週次生成が欠落しない（精査2パス A）**: `date = tomorrow+40日` へ臨時営業を実行した後に日次バッチを走らせても、当該テンプレの**通常の週次枠（horizon 内の未生成分）が欠落なく生成される**（観測点: バッチ前後の horizon 内 slot 行。クランプなし実装だと range が空になり 0 件のまま＝red になる番人）。
- **S-9（タブ再編・UI）**: 管理タブの並びが 営業時間→予約対象→メニュー→週間スケジュール→例外日→詳細設定 で、SlotManager が詳細設定 Accordion 内にある。既存の各コンポーネント機能はタブ移設後も動作（回帰）。
- **S-11（初回体験・「保存したのに0件」防止）**: ①`hasBusinessHours=false` のチームで週間スケジュール画面を開くと空状態に `need_business_hours_hint`（「先に営業時間を設定してください」）＋営業時間セクションへの導線が表示される。②営業時間 9:00-12:00 のチームで 14:00-16:00 のテンプレを保存すると、`generation.generatedCount=0`・`skippedOutsideHoursCount>0` が返り、トーストに**原因（営業時間外）を明示する** `generated_zero_hint` が出る（成功トーストの「0件作成」だけで終わらないこと）。
- **S-12（臨時営業×全日休業の衝突）**: 同日に全日 TEAM の予約不可枠が存在する日へ「臨時営業する」を開くと、警告 `blocked_conflict`＋「休業を解除」導線が表示され**実行ボタンが disabled**。当該 blocked_time を削除すると実行可能になる。**BE の generate-single-day 自体は同条件でも 200 で生成する**（runtime 判定で全セル UNAVAILABLE・§3.3.2 の一意定義どおり）。

### B) 定期予約不可枠

- **R-1（正常・作成と enforcement）**: `TUE 19:00-20:00・lineId=null・reason=研修・isPublic=true` を作成すると、以後すべての火曜 19:00-20:00 のセルが（テンプレ生成枠・手動枠とも）`listAvailableSlots` から除外され、該当枠への `createReservation` は 400=009、グリッドは UNAVAILABLE。
- **R-2（ライン軸・正常）**: `lineId=1` のルールは line=1 の枠のみブロックし、line=2・共通枠（line NULL）は残る。`lineId=null` は共通枠含む全 slot に効く。
- **R-3（境界・半開区間/曜日）**: 19:00-20:00 ルールで 18:30-19:00 / 20:00-20:30 のセルは非ブロック。水曜の 19:00 セルは非ブロック（曜日一致のみ）。`is_active=FALSE` にすると即時（次リクエストから）予約可能に戻る（生成スキップ方式でないことの観測点）。
- **R-4（公開ラベル・正常/境界/混在）**: `isPublic=true` ルール由来の UNAVAILABLE セルは `unavailableReason="研修"`、`isPublic=false` 由来・単発 blocked_times 由来は null（従来 JSON と一致）。同一セルに public ルールが複数該当する場合は開始時刻昇順の最初の reason。**混在ケース: 同一セルに「単発 blocked_times」と「public 定期ルール」の両方が該当する場合は `unavailableReason=null`（非公開優先・§4.4）**。PII 不在（セル DTO に氏名・userId フィールドが構造的にない C-4 踏襲）。
- **R-5（上限・異常）**: 50 行のチームで 51 行目 POST は 400=RESERVATION_047。他チーム ruleId の PATCH/DELETE は 404=RESERVATION_046。非 ADMIN 403・未認証 401。
- **R-6（409 ガード・境界）**: 今後 90 日以内の火曜 19:00-20:00 に active 予約があるチームで当該ルール POST は 409=027（`reservation_recurring_blocked_times` に行が増えない）。impact は同一クエリ件数＋対象日付一覧を返し副作用ゼロ。91 日以降にのみ予約がある場合は 201 で通る（horizon 境界の明文化）。
- **R-7（全日型拒否・異常）**: `startTime`/`endTime` 欠落は 400（Bean Validation・「終日は営業時間の定休日で設定」ガイドは FE 表示）。
- **R-8（機能B 回帰）**: 単発 blocked_times の B-1〜B-10（親 §8）が無修正 green（チェッカーのコア 9 引数メソッド無変更の観測点）。
- **R-9（PII 実防御・UI）**: 定期ルール作成/編集ダイアログで `is_public` を ON にすると、reason 入力欄直下に**「個人名やお客様情報を含めない」注意ガイド（`reason_no_pii`）が必ず表示**され、プレースホルダに推奨語彙（`reason_placeholder`）が出る（vitest: トグル ON/OFF での表示切替）。

### C) 呼称設定

- **N-1（正常）**: `PATCH /reservation-settings {resourceNameType: "SEAT"}` 後、`GET` 応答に `resourceNameType=SEAT` が載り、FE のタブ名・LineManager 見出し・マトリックス行ヘッダが「席」系表示に変わる。`CUSTOM`＋`resourceNameCustom="施術台"` はそのまま表示。
- **N-2（境界・後方互換）**: 設定レコード未作成チーム・ALTER 前既存行は `DEFAULT` として従来の「予約対象」表示（既存 E2E/表示スナップショットが無修正 green）。`CUSTOM` で custom 空は 400。`CUSTOM` 以外で custom 送信は NULL 正規化。31 文字は 400。
- **N-3（i18n・6言語）**: プリセット 6 種＋DEFAULT の呼称キーが 6 言語に存在し、文言テンプレートが単複中立構文（en で "Seats management" のような複数形要求が発生しない）であることをロケール線形チェック＋レビューで確認。
- **N-4（死蔵キー）**: `reservation.template.staff` / `staff_none` が 6 言語から削除され、参照エラーが出ない（vitest ロケール整合）。LineManager の並び順上下ボタンで `displayOrder` が PATCH され一覧・グリッド列順に反映される。

### D) 実務機能（設計検証用・実装弾で red 化する）

- **W-1（waitlist 登録・異常系込み)**: FULL 枠に登録 201。AVAILABLE 枠は 400=050。二重登録 409=049。ユーザー 11 件目/枠 51 件目は 400=051。**他人の waitlist entry への DELETE / 自分以外の entry の GET は 404=048（IDOR 秘匿）**。
- **W-2（通知・正常）**: FULL 枠の予約キャンセル → `decrementBookedCountAndReopen` 成功 → WAITING 全員に RESERVATION_WAITLIST_OPENING が届く（通知テーブル観測）。60 分以内の再キャンセルでは同一 entry に再送しない。予約成立したユーザーの entry は CONVERTED。
- **W-3（定期予約・正常/境界）**: `repeatWeeks=4` で 4 週分（満席週スキップ・明細返却）。全行同一 `recurring_series_id`。THIS_AND_FOLLOWING キャンセルで当該日以降のみ CANCELLED。`repeatWeeks=13` は 400=052。
- **W-4（仮押さえ失効・境界／W2-6 実装時に改訂・殿の裁定 2026-07-29）**: `pending_expire_hours=24`・判定基準時刻を固定して 24h+1分経過の PENDING がバッチで CANCELLED（`cancelledBy=SYSTEM`）・枠復帰・申込者へ通知。23h59分は不変。**枠の終了時刻を経過した PENDING は経過時間に関わらず失効する**（初版の「枠開始済みは対象外＝NO_SHOW バッチ管轄」は、当該バッチが予約ドメインに実在しないため撤回。§6.3 の変更理由を参照）。`pending_expire_hours=NULL` チームは (a)(b) とも失効しない。ポリシー行が無いチームは既定 24 時間で失効する。グループ PENDING は構成全行が一括 CANCELLED（部分失効ゼロ）。枠が FULL→AVAILABLE に遷移した場合のみ `ReservationSlotReopenedEvent` が発火しキャンセル待ちへ通知が飛ぶ。
- **W-5（レートリミット）**: 同一ユーザーの 6 回目の予約作成（単枠 3＋グループ 3 の混合）が 60 秒窓で 429=053。61 秒後は 201。

---

## 10. エラーコード（採番仮置き）

> `ReservationErrorCode` 現最大 = **RESERVATION_045**（`LINE_HAS_ACTIVE_RESERVATIONS`・実 enum 確認済 2026-07-08）。以下は v2 通し採番の仮置き。**並行 PR による衝突があり得るため、実装弾のマージ直前に enum 実物で必ず再確認**すること。

| コード | 定数名（案） | 用途 | HTTP | Severity |
|---|---|---|---|---|
| RESERVATION_046 | `RECURRING_BLOCKED_TIME_NOT_FOUND` | 定期ルール不存在（IDOR 秘匿含む） | 404 | WARN（個別マップ 404） |
| RESERVATION_047 | `RECURRING_BLOCKED_TIME_LIMIT_EXCEEDED` | 定期ルール上限 50 行 | 400 | WARN |
| RESERVATION_048 | `WAITLIST_ENTRY_NOT_FOUND` | 待ち行不存在/他人（D群） | 404 | WARN（個別マップ 404） |
| RESERVATION_049 | `WAITLIST_ALREADY_REGISTERED` | 同一枠への二重登録（D群） | 409 | WARN（個別マップ 409） |
| RESERVATION_050 | `WAITLIST_SLOT_NOT_FULL` | 満席でない枠への待ち登録（D群） | 400 | WARN |
| RESERVATION_051 | `WAITLIST_LIMIT_EXCEEDED` | 待ち上限（ユーザー10/枠50・D群） | 400 | WARN |
| RESERVATION_052 | `RECURRING_RESERVATION_LIMIT_EXCEEDED` | repeatWeeks 上限 12 超（D群） | 400 | WARN |
| RESERVATION_053 | `RESERVATION_CREATE_RATE_LIMITED` | 予約作成レートリミット（D群） | 429 | WARN（個別マップ 429） |

再利用: 時刻検証 007/022・過去日 014/023・ブロック競合 009・overlap active 予約 027（定期ルールの 409 ガード）・generate レートリミット 044（単日テンプレ適用が zone 共有）・ライン不正 001。

> **確定範囲の注記（精査1パス反映）**: W2 実装弾（W2-1〜W2-3）で消費が確定するのは **046/047 のみ**。**048〜053 は D 群（別裁可）の暫定であり、D 群実装弾の着手時に enum 実物の最大値から再採番**する（それまで他機能が同番号を消費し得る）。

> **⚠ 実採番の確定結果（上表は仮置きであり実物と一致しない。実物が正）**
>
> | 実コード | 定数名 | 用途 | HTTP | 確定 |
> |---|---|---|---|---|
> | RESERVATION_046 | `WAITLIST_ENTRY_NOT_FOUND` | 待ち行不存在/他人 | 404 | W2-4 |
> | RESERVATION_047 | `WAITLIST_ALREADY_REGISTERED` | 二重登録 | 409 | W2-4 |
> | RESERVATION_048 | `WAITLIST_SLOT_NOT_FULL` | 満席でない枠への待ち登録 | 400 | W2-4 |
> | RESERVATION_049 | `WAITLIST_LIMIT_EXCEEDED` | 待ち上限 | 400 | W2-4 |
> | RESERVATION_050 | `WAITLIST_RATE_LIMITED` | 待ち登録レートリミット | 429 | W2-4 |
> | RESERVATION_051 | `RECURRING_BLOCKED_TIME_NOT_FOUND` | 定期ルール不存在 | 404 | W2-2 |
> | RESERVATION_052 | `RECURRING_BLOCKED_TIME_LIMIT_EXCEEDED` | 定期ルール上限 50 行 | 400 | W2-2 |
> | RESERVATION_053 | `RESERVATION_CREATE_RATE_LIMITED` | 予約作成レートリミット | 429 | **W2-6（2026-07-29）** |
>
> | RESERVATION_054 | `RECURRING_RESERVATION_LIMIT_EXCEEDED` | repeatWeeks 上限 12 超 | 400 | **W2-5（2026-07-30）** |
>
> **次に ErrorCode を採番する隊は 055 から**。着手時に必ず enum 実物の最大値を再確認すること。
> 精査2パス M の「053 を予約作成と waitlist 登録で共用するか」は、**共用しない（waitlist は 050・予約作成は 053）** で確定した（zone も別・意味論も別のため）。

---

## 11. i18n（新規キー・6言語・直書き禁止・裸 `@` は `{'@'}`）

`reservation.json` に追加/改訂（ロケールは nuxt.config の files 配列 6 言語登録済み前提）:

**A) 営業スケジュール中心**
- `reservation.business_hours.title`（営業時間）/ `.day_open`（営業する）/ `.save_success`（「保存し、枠を{generated}件更新しました」）/ `.generating`（「枠を更新しています…」）/ `.generation_failed`（「営業時間は保存しましたが、枠の自動更新に失敗しました。明朝に自動で追い付きます」）
- `reservation.template.auto_generated`（「保存し、{days}日先までの枠を{generated}件作成しました」— `generate_result` を置換継承）
- `reservation.template.regenerate_guide` **値改訂**（「変更は保存時に自動反映されます（追加分のみ）。既存の未予約枠を新しい内容に合わせたい場合は、枠を削除してから保存し直してください」）
- `reservation.exception_day.title`（例外日カレンダー）/ `.close_day`（この日を休業にする）/ `.open_special`（臨時営業する）/ `.close_day_impact_warning`（「この日には有効な予約が {n} 件あります」）/ `.goto_reservations`（予約一覧で対応）/ `.notify_hint`（「予約者への通知が必要な場合は緊急休業を使ってください」）/ `.special_source_day`（適用する曜日ダイヤ）/ `.special_done`（「{date} に {generated} 件の枠を作成しました」）
- `reservation.template.auto_generated_partial`（「{total}曜日中{failed}曜日で枠の作成に失敗しました（保存済みの曜日は有効です）」・§3.1 集約規則）
- `reservation.template.need_business_hours_hint`（「先に営業時間を設定してください」・hasBusinessHours=false の空状態 §3.2/S-11）
- `reservation.template.generated_zero_hint`（「枠は作成されませんでした（営業時間外/定休日のため）。営業時間を確認してください」・S-11）
- `reservation.business_hours.shrink_note`（縮小/定休化時の遡及なしガイド・§3.2/S-6⑤）
- `reservation.exception_day.blocked_conflict`（「この日は休業に設定されています。休業を解除してから実行してください」・§3.3.2/S-12）
- `reservation.slot_manage.advanced_label`（個別の枠を手動管理（例外操作））

**「今すぐ枠を作成」廃止に伴う改訂/削除キーの全数列挙（精査2パス C）**

> 列挙は権威実物 = `origin/feat/reservation-ux-wave1-guides` の `frontend/app/locales/ja/reservation.json` から実測で拾った（2026-07-08）。**W2-1 実装弾で W1 マージ後の実物と改めて 1:1 照合**すること（`feedback_subagent_enumeration_authoritative_source`）。

| キー | W1 時点の値（要旨） | W2-1 での処置 |
|---|---|---|
| `reservation.template.regenerate_guide` | 「…変更を反映するには『今すぐ枠を作成』を押してください」 | **値改訂**（「変更は保存時に自動反映（追加分のみ）。既存未予約枠を新定義に合わせたい場合は枠削除→再保存」） |
| `reservation.template.generate_weeks` | 「何週先まで」 | **削除**（週数選択 UI 廃止で参照ゼロ・死蔵化防止） |
| `reservation.template.empty_state_hint` | 「…設定すると、まとめて枠を作成できます」 | **値改訂**（「…設定すると、保存と同時に枠が自動で作成されます」） |
| `reservation.team_guide.line_manage.setup3_steps.1` | 「保存後に『今すぐ枠を作成』を押すと、直近の枠がすぐに用意されます」 | **値改訂**（「保存すると、28日先までの枠が自動で作成されます」） |
| `reservation.team_guide.line_manage.setup3_steps.2` | 「押さなくても、翌日以降は毎日自動で枠が延伸されます…」 | **値改訂**（「以後は毎日自動で枠が延伸されます（何もしなくてOKです）」——「押さなくても」の前提を除去） |
| `reservation.team_guide.line_manage.exception_steps.0` | 「…下の『枠管理』で個別の枠を作成・編集・受付停止できます」 | **値改訂**（SlotManager の詳細設定移設＋例外日カレンダー新設に合わせ「例外日カレンダー（休業/臨時営業）または詳細設定の『個別の枠を手動管理』…」へ） |
| `reservation.team_guide.line_manage.exception_steps.1` | 「特定の時間帯だけ…詳細設定の『予約不可枠』…」 | **値改訂**（週次定期不可（週間スケジュール内）と単発（詳細設定）の使い分けを追記） |
| `reservation.template.generate` / `generate_result` / `generate_rate_limited` / `generate_skipped_closed` | ボタン/トースト文言 | **残置・流用**（自動生成・臨時営業の結果トースト） |
| `reservation.template.generate_no_active_templates` | 「有効なテンプレートがありません…」 | **流用改訂**（臨時営業ダイアログの「この曜日のテンプレートがありません」状態エラーへ） |

**B) 定期予約不可枠**
- `reservation.recurring_block.title`（毎週の予約不可）/ `.add` / `.edit` / `.delete_confirm` / `.reason`（事由・必須）/ `.reason_no_pii`（「事由に個人名やお客様情報を含めないでください（会員全員に公開されます）」・is_public=ON 時必須表示 §4.4/R-9）/ `.reason_placeholder`（「例: 研修・スクール・清掃・メンテナンス」）/ `.is_public`（会員に事由を表示する）/ `.is_public_note`（「ONにすると空き状況にこのラベルが表示されます」）/ `.line_all`（チーム全体）/ `.limit_reached`（上限50件）/ `.full_day_hint`（「終日休みにする場合は営業時間で定休日に設定してください」）/ `.impact_warning`（「今後90日間に重複する予約が {n} 件あります」）

**C) 呼称**
- `reservation.resource_name.DEFAULT`（予約対象）/ `.STAFF`（担当スタッフ）/ `.SEAT`（席）/ `.COURT`（コート）/ `.BED`（ベッド）/ `.LANE`（レーン）
- `reservation.resource_name.setting_title`（予約対象の呼び方）/ `.custom_label`（自由入力の呼称）/ `.custom_required`（自由入力の呼称を入力してください）
- **既存キーのパラメータ化改訂**: `reservation.tab.line_manage` → 「{resourceName}の管理」等（§5.2 の表の全キー・6言語で単複中立構文）
- **削除**: `reservation.template.staff` / `reservation.template.staff_none`（6言語・参照ゼロ実測）

**D) 実務機能（実装弾で追加）**

> **⚠ 実キー名の確定結果（下記は当初案。W2-4-FE/W2-6-FE 実装時に実キー名で確定・2026-07-30）**: `reservation.waitlist.*`・`reservation.settings.policy.pending_expire.*`・`reservation.message.rate_limited` として実装した（以下は当初の仮キー名。実キーは `frontend/app/locales/*/reservation.json` が正）。

- （実キー）`reservation.waitlist.register_button`（キャンセル待ちに登録）/ `.cancel_button`（キャンセル待ちを取消）/ `.registered_badge`（待機中）/ `.dialog_title`（キャンセル待ち）/ `.notice`（「空きが出たら通知でお知らせします。順番の保証はなく、早い者勝ちで予約できます」）/ `.register_success` / `.cancel_success` / `.admin_count`（「キャンセル待ち {n} 人」・ADMIN専用表示）/ `.already_registered` / `.not_found` / `.limit_exceeded` / `.rate_limited` / `.slot_not_full` / `.my_list_title`（自分のキャンセル待ち）/ `.no_entries`
- **⚠ 実キー名の確定結果（2026-07-30 W2-5-FE 実装時に確定・下記は当初案との差分）**: `reservation.recurring.*` は当初案「`repeat_weekly`/`.weeks`/`.result`/`.cancel_scope_this`/`.cancel_scope_following`/`.horizon_hint`」の6キー案から、実装時に**トグル・段階開示・結果明細（9値スキップ理由込み）・キャンセル2択・series一括承認・バッジまでを1階層下の名前空間で網羅する27キー構成**へ確定した（`reservation.recurring_block.force_cancel.*` の5キーは別区分・§6.2⑥参照）。当初案との対応:

  | 当初案キー | 実キー | 差分の理由 |
  |---|---|---|
  | `reservation.recurring.repeat_weekly` | `reservation.recurring.toggle_label` | トグルのラベルであることを明示（"repeat_weekly" は値ではなくトグル自体の名前だったため） |
  | `reservation.recurring.weeks` | `reservation.recurring.weeks_option` / `.weeks_more` / `.weeks_label` | 段階開示（quick pill 2〜4週 / 「もっと長く」/ 5〜12週 Select のラベル）を実装した結果、単一キーでは表現できず3キーに分割 |
  | `reservation.recurring.result` | `reservation.recurring.result.summary` / `.skipped_list_title` / `.all_skipped_notice` | 結果明細UI（成立/スキップ件数＋スキップ週一覧＋全滅時の注意書き）を実装した結果、3キーへ分割。加えて `reservation.recurring.skip_reason.*`（9値enum）を新設（初版どおりBE語彙が3→9値に拡張されたことに追随） |
  | `reservation.recurring.cancel_scope_this` / `.cancel_scope_following` | `reservation.recurring.cancel_scope.this_only` / `.this_and_following`（＋`.dialog_title`/`.result_summary`/`.all_skipped_notice`） | 2択の選択肢名だけでなく、ダイアログ見出し・結果明細まで実装したため5キーへ拡張 |
  | `reservation.recurring.horizon_hint` | `reservation.recurring.horizon_hint`（変更なし） | 当初案どおり据え置き |
  | （当初案になし） | `reservation.recurring.close_button` / `.badge` / `.confirm_series.button` / `.confirm_series.confirm_message` / `.confirm_series.success` | series バッジ（④）・series一括承認（⑤）は当初の6キー案には無かった機能のため新規追加 |
  | （当初案になし） | `reservation.recurring_block.force_cancel.button` / `.confirm_title` / `.confirm_message` / `.confirm_ok` / `.result_notice` | 定期予約不可枠の強行登録（⑥・「殿の裁定」）は§6.2本文の当初i18n案には含まれておらず新規追加。既存の `reservation.recurring_block.*` 名前空間（§4）に相乗り（会員向け「定期予約」機能の `reservation.recurring.*` とは別名前空間で衝突しない） |

  合計 **32キー×6言語＝192エントリ**を新設し、ja/en/zh/ko/es/de の6ファイルでキー集合が完全一致することをスクリプトで機械確認した（§6.2「W2-5-FE 実装で確定した事項」⑦参照）。§6.2 定期予約は **W2-5 で BE 実装済**（2026-07-30）・**W2-5-FE で FE 実装済**（2026-07-30・第四陣）。
- （実キー）`reservation.settings.policy.pending_expire.label`（仮押さえ(PENDING)の自動失効）/ `.hint` / `.unit`（「 時間で自動キャンセル」）/ `.no_expire_toggle`（自動キャンセルしない）/ `.no_expire_warning`（MANUAL チーム向け注意書き）/ `.error_range`（FE バリデーション文言）
- （実キー）`reservation.message.rate_limited`（「予約作成の間隔が短すぎます。しばらく待ってから再度お試しください」・単枠/グループ両方の 429=RESERVATION_053 で使用）
- （実キー）`reservation.pending_expire_notice.form_note`（「{n}時間以内に承認されない場合は自動的にキャンセルされます」・`ReservationForm.vue`/`GroupBookingDialog.vue` の会員向け注意書き。設定側の `pending_expire.*` とは別名前空間で衝突しない）

FE 実装規約: 新設 UI（BusinessHoursManager・例外日カレンダー・WeeklyScheduleManager）には `/統一`・`/手助け`（または `/手助けモーダル`）を適用（`feedback_new_pages_use_touitsu_and_tasuke`）。曜日ラベルは既存 `schedule.recurrence.days.*` 再利用（SlotTemplateManager と同じ）。

---

## 12. 非機能・アーキ制約

- **UUIDv7（原則6）**: 新規テーブル（recurring_blocked_times / waitlist_entries）は `UuidV7Entity` 継承。既存テーブルの ALTER は ID 型不変。
- **クロスドメインFK禁止（原則1）/ CASCADE 同一ドメインのみ（原則2）**: 新規 FK は `line_id`→reservation_lines（RESTRICT）・waitlist `slot_id`→reservation_slots（CASCADE・同一ドメイン）のみ。`team_id`/`user_id`/`created_by` は FK なし。
- **@Transactional ドメイン内（原則5）**: 全処理 reservation ドメインに閉じる。通知は AFTER_COMMIT リスナー経由（既存規約）。
- **horizon 一覧（意図的な差・精査1パス反映）**: 本書に登場する期間はすべて**意図して別値**であり、統一しない:

  | 用途 | 期間 | 根拠 |
  |---|---|---|
  | テンプレ自動生成・日次バッチ | **28 日 rolling**（`tomorrow + 27日`） | F03.4.2 既定 horizon（週間テンプレの実用先読み） |
  | 単日テンプレ適用（臨時営業）の対象日上限 | **今日から 90 日以内** | 遠未来の誤生成防止。振替営業の告知実務（数週間〜2ヶ月前）を包含 |
  | 定期ルールの 409 ガード / impact | **今日から 90 日先まで** | 予約は生成 horizon（28日）＋手動枠にしか存在せず 90 日で実用上全件。無限走査の回避（§4.3） |

- **判定コスト**: 定期ルールはチームあたり最大 50 行のメモリ突合。grid レンジ呼び（最大 3,528 セル）× 50 ルールでも 18 万回の軽量比較で実用圏（既存の単発 blocked 判定と同オーダー）。クエリはチーム単位 1 本の定数（N+1 なし）。**予約作成のホットパス（`createReservation`／グループ作成）には `findByTeamIdAndIsActiveTrue` の 1 クエリが恒常追加**される（精査1パス反映・単発 blocked_times の日付ロードに並ぶ 2 本目。インデックス `idx_rrbt_team` 下の最大 50 行読みで許容。キャッシュは導入しない——鮮度優先・機能B と同方針）。
- **バッチ負荷帯の分離**: 生成 AM0:15 / waitlist クリーンアップ AM0:45 / リマインド 1 分間隔 / pending 失効 5 分間隔（`@SchedulerLock` 必須・最小 context 番人テスト各 1 本）。
- **OpenAPI**: 全 API 変更は additive（既存フィールドの型変更・削除なし。generate の `deprecated: true` 付与は非破壊）。BE 変更後 `docs/openapi.json` 再生成＋`npm run generate:types` を同一 PR（`project_openapi_json_chronic_drift`）。
- **シャーディング**: 新規表は `team_id` 保持（既存 reservation 系と同整理）。

---

## 13. 実装フェーズ分割（W2 の弾割り）

> **W1（第三弾第一波）との調停（精査1パス反映・2パスで改修基準を確定・必読）**: W1 の成果——**曜日複数選択＋テンプレ管理のアコーディオン化（PR #2198）／タブ別使い方ガイド（PR #2197）**——と本書 §3 は同じ `SlotTemplateManager.vue`／`TeamReservationsPanel.vue` を触る。**マージ順は「#2198（機構）→ #2197（ガイド）→ 本書 W2-1」の直列**で確定し、並行出陣しない。
> - **W2-1 の改修基準（出発点となる版）**: 改修対象ファイルは **W1 マージ後の版**とする。具体的には `origin/feat/reservation-ux-wave1-mechanics` の `SlotTemplateManager.vue` ——**新規作成時に `selectedDays` で選択曜日ぶん `createSlotTemplate` を N 回順次呼ぶループ実装**（実測確認済み・§3.1 の集約規則の前提）——を出発点とし、main 直系の旧版（単一曜日 `form.dayOfWeek`）から改修しないこと。
> - `SlotTemplateManager` → `WeeklyScheduleManager` のリネーム（§3.2/§4.5）は **W1 マージ後に実施**し、**W1 の曜日複数選択ダイアログは捨てずに WeeklyScheduleManager の枠テンプレダイアログへそのまま載せ替える**（W1 の UI 資産を正として引き継ぐ）。
> - ガイド文言（#2197 の `team_guide.*`）は §3.2 のタブ再編・自動生成化に合わせて W2-1 内で改訂する（**改訂/削除キーの全数列挙は §11 の一覧表**・精査2パス C）。

| 弾 | 内容 | 依存 | 規模感 |
|---|---|---|---|
| **W2-1** | §3 A) 全量: 単一テンプレ自動生成＋generate-single-day（BE）／BusinessHoursManager・タブ再編・例外日カレンダー・generate UI 撤去（FE） | **W1（PR #2197/#2198）マージ後**（上記調停） | BE小〜中＋FE中 |
| **W2-2** | §4 B) 全量: 新テーブル＋CRUD＋チェッカー拡張（機能B LINE 軸 enforce 同梱）＋グリッド `unavailableReason`＋WeeklyScheduleManager 統合 | W2-1（画面統合先） | BE中＋FE中 |
| **W2-3** | §5 C) 全量: settings ALTER＋呼称 API＋useResourceName＋i18n 全キー改訂＋死蔵キー削除＋LineManager 並び順編集 | なし（W2-1 と並行可） | BE小＋FE横断 |
| **W2-4**（**別裁可**） | §6.1 キャンセル待ち | なし | BE中＋FE小 |
| **W2-5**（**別裁可**） | §6.2 定期予約 — **BE 実装済（2026-07-30・Flyway V173 / ErrorCode 054）＋FE 実装済（2026-07-30・W2-5-FE）**: 単枠 series（`repeatWeeks` 2〜12）・`THIS_AND_FOLLOWING` キャンセル・`scope=SERIES` 一括承認・**定期不可枠の強行登録（`forceCancelConflicting`・殿の裁定）**、FE は段階開示トグル・結果明細（9値スキップ理由）・キャンセル2択・seriesバッジ・series一括承認導線・強行登録の確認ダイアログ・`reservation.recurring.*` 27キー＋`reservation.recurring_block.force_cancel.*` 5キー×6言語。残: グループ予約の `repeatWeeks`（BE/FEともスコープ外） | F03.4.3 実装済（済）／W2-6 レートリミット（済） | BE中＋FE中（**完了**） |
| W2-6（**別裁可**） | §6.3 仮押さえ失効＋§6.4 レートリミット | なし | BE小×2 |

- 各弾とも **BE/API はテスト先行**（軍議AC → 試練red → 出陣green → 検分。`feedback_test_first_be_api`）。
- FE/BE 並行は API 境界の生成型確定後（`feedback_fe_be_parallel_api_boundary_after_generated_types`）。
- W2-1 と W2-3 は独立 worktree で並列出陣可能（ただし i18n 同一ファイル編集の競合に注意——`reservation.json` を触る弾は直列マージ推奨）。

**進捗（2026-07-30）**: W2-6 は BE（PR #2525・main 着地済）に続き **FE（W2-6-FE）実装済**——`ReservationPolicySettings.vue` の `pendingExpireHours`/`clearPendingExpireHours` UI と、単枠/グループ両経路の 429=RESERVATION_053 専用トースト。W2-4 は BE（PR #2206・main 着地済）に続き **FE（W2-4-FE）実装済**——`SlotMatrixPicker.vue`/`SlotGridPicker.vue` の満席セルからのキャンセル待ち登録・取消導線、自分の一覧。詳細は §6.1/§6.3/§6.4 の各 FE 実装済み注記・§15 変更履歴を参照。W2-5（定期予約・§6.2）は**BE 実装済（PR #2536・main 着地済・2026-07-30）に続き FE（W2-5-FE）実装済（2026-07-30）**——段階開示トグル（`ReservationForm.vue`）・結果明細（9値スキップ理由）・キャンセル2択（`ReservationList.vue`）・seriesバッジ・series一括承認・定期予約不可枠の強行登録確認ダイアログ（`WeeklyScheduleManager.vue`）・`reservation.recurring.*`/`reservation.recurring_block.force_cancel.*` i18n（計32キー×6言語）。残はグループ予約の `repeatWeeks`（BE/FEともスコープ外・別弾）のみ（§13弾割り表・§11-D・§6.2「W2-5-FE 実装で確定した事項」参照）。

---

## 14. 要裁可論点（マスターへの上奏事項）

1. **D群（§6）の実装着手可否と順序**（本書は設計のみ。推奨順: W2-4 キャンセル待ち → W2-6 失効/レートリミット → W2-5 定期予約）
2. **`POST .../generate` API の将来削除時期**（deprecated 残置の期限。推奨: 第四弾まで残置後に利用実測ゼロで削除）
3. **`pages/admin/reservation-settings.vue`（旧ライン管理ページ）の統廃合**（チームタブと機能重複・§5.4）
4. **単発予約不可枠への `is_public` 拡張**（§4.5 バックログ。「7/20 は設備点検」を会員に見せるユース）
5. **営業時間保存時の同期 generate（変更曜日差分・§3.2）の応答時間**が実測で API タイムアウト近傍になった場合の非同期化（S-6④のタイミング回帰が割れたら上奏・裁可は実測後）

---

## 15. 変更履歴

| 日付 | 変更内容 |
|------|---------|
| 2026-07-30(W2-5-FE) | **W2-5-FE §6.2 定期予約（毎週繰り返し）FE実装（第四陣・BE PR #2536 の続き）**: ①`ReservationForm.vue` に段階開示トグル（既定OFF・ON で2〜4週の推奨pill＋「もっと長く」の先に5〜12週Select・12超は構造的に送信不可）＋定期予約の結果明細（応答 `data.recurring` 非nullのときダイアログを結果パネルへ切替・`close()`を呼ばず留める・成立0件でも`all_skipped_notice`で黙殺しない・9値スキップ理由すべてにi18n） ②`ReservationList.vue` に `data.recurringSeriesId`（トップレベル・GETでも常に返る唯一の判定材料）が非nullの単票行専用のキャンセル2択ダイアログ（「この回だけ」=THIS_ONLY／「この回以降すべて」=THIS_AND_FOLLOWING）を追加。`scope` は `cancelByUser`（会員自己キャンセル・`cancelMyReservation`）専用で `cancelByAdmin`（管理者キャンセル）は参照しないことを実装（Java実測で確認済み）。「以降すべて」の結果（`data.recurringCancel`）は0件でも明細を表示。同ファイルに seriesバッジ（`Tag`）とPENDING行のseries一括承認ボタン（`confirmReservation(...,'SERIES')`）も追加 ③`WeeklyScheduleManager.vue` に定期予約不可枠の強行登録導線を追加——impact で衝突ありのとき「衝突する予約をキャンセルして登録する」ボタンを表示し、**必ず確認ダイアログ（件数＋「申込者に通知が送られます」を明示）を経由してから** `forceCancelConflicting:true` を送信（既定false・うっかり押せない形）。実装中に「変数名 `confirm` が既存の `window.confirm()`（`remove`/`removeRecurring` の削除確認）と衝突する」不具合を発見・`confirmDialog` へ改名して根治（typecheckで検出） ④`useReservationApi.ts`: `createReservation` に `repeatWeeks`（optional・additive）、`cancelMyReservation` に `opts.scope`（`ReservationCancelScope`）、`confirmReservation` に `scope`（クエリパラメータ・`ReservationConfirmScope`）を追加。返り値の型も `unknown` から `ReservationResponse` へ強化（`recurring`/`recurringCancel`/`recurringConfirm`/`recurringSeriesId` を呼び出し元で参照できるように）。`createRecurringBlockedTime`/`updateRecurringBlockedTime` は生成型に既に `forceCancelConflicting` を含むため関数シグネチャ変更なし ⑤`types/reservation.ts` に `RecurringSeriesDto`/`RecurringCancelDto`/`RecurringConfirmDto`/`RecurringWeekOutcomeDto` を生成型の再エクスポートで追加（`GroupSummaryDto` と同一方式・手書き型を新規に起こしていない）・`ReservationResponse` に `recurringSeriesId`/`recurring`/`recurringCancel`/`recurringConfirm` を追加 ⑥i18n: `reservation.recurring.*`（27キー）＋`reservation.recurring_block.force_cancel.*`（5キー）＝計32キーを6言語（ja/en/zh/ko/es/de）に追加。当初案6キー（`repeat_weekly`/`weeks`/`result`/`cancel_scope_this`/`cancel_scope_following`/`horizon_hint`）からの拡張差分は §11 に対応表で記録。6言語計601キー・キー集合完全一致をスクリプトで機械確認 ⑦vitest 12件追加（`ReservationForm.spec.ts` +5・`ReservationList.spec.ts` +4・`WeeklyScheduleManager.spec.ts` +3）・既存122件は無修正 green（計134件）。**スコープ外**: グループ予約（`GroupBookingDialog.vue`）の `repeatWeeks` UI（BE側が既にスコープ外のため） |
| 2026-07-30(検分是正) | **検分（家老実走＋殿実測）で発覚した実バグ1件＋テスト不備の是正**: 🔴**MUST①（実害バグ）**: `ReservationMyWaitlistList.vue`/`SlotGridPicker.vue`/`SlotMatrixPicker.vue` が `props.teamId`（slug。`pages/teams/[slug]/reservations.vue` が渡す）と `WaitlistEntryResponse.teamId`（BE数値DB id・`Long`）を直接文字列比較しており**常に false**——一度登録すると会員が UI から永久に降りられない実害（一覧は常に空表示・満席セルは登録済みでも「登録」ボタンのまま→409）。是正: `SlotMatrixPicker`/`SlotGridPicker` は既にそのチームの枠一覧（`allCells`/`days`）を読み込み済みのため「slotId が読み込み済みの枠id集合に含まれるか」で絞り込み（team id 解決不要・最も安い）。`ReservationMyWaitlistList` は `useActivityScopeId().resolveScopeId('TEAM', slug)`（`EmergencyClosureForm.vue` の `resolveTeamId` と同型の既存作法）で数値解決してから比較。テストの `teamId` も実際の slug 値（'team-slug' 等）へ修正（数値文字列のままでは同じ穴を再度見逃すため）。**openapi.json は触れていない**（第三陣 #2536 との衝突回避・FE内で閉じた） 🔴**MUST②（テスト不備）**: `ReservationWaitlistDialog.spec.ts`/`ReservationForm.spec.ts`/`ReservationPolicySettings.spec.ts` にも `ReservationMyWaitlistList.spec.ts` と同型の `beforeAll` ウォームアップマウントを追加（transform timeout の根絶）。あわせて timeout 中断時の teleport 残骸対策として `afterEach` に明示的 `wrapper.unmount()` を追加（DOM除去だけでなく Vue ライフサイクルも確実に畳む）。**教訓**: PrimeVue Dialog は Teleport 先が `body` 直下のため、`wrapper.find()`（非Teleport専用の `findInWrapper` パターン）は届かない——`document.body.querySelector` 方式（`GroupBookingDialog.spec.ts` の既存作法）が正しい。一度 `findInWrapper` へ転用して全滅させ、撤回・原状復帰した 🟡**MUST③**: `SlotGridPicker.vue`/`SlotMatrixPicker.vue` の `loadMyWaitlist` の catch がエラーを完全に握りつぶしていたため `captureQuiet` パターンへ是正（`ReservationForm.vue` と同一方針） 🟡**MUST④**: `ReservationWaitlistDialog.spec.ts` に RESERVATION_049（上限超過）/050（レートリミット）分岐のテストを追加（従来は046/047/048のみ） 🟡**MUST⑤**: `GroupBookingDialog.spec.ts` に会員向け告知3ケース＋429=RESERVATION_053のテストを追加（従来は単枠のみ） 🟡**MUST⑥**: `GroupBookingDialog.vue` の告知取得を `onMounted` 一回 fetch から `watch(visible, {immediate:true})` に変更（常設mount+KeepAliveのため設定変更が画面遷移まで反映されない問題を是正） 🟡**ついでに直した安い是正**: `SlotGridPicker.vue`/`SlotMatrixPicker.vue` の BOOKED セルは `slotId==null` のとき disabled に、aria-label に「押すとキャンセル待ちに登録/取消できます」ヒントを追加（6言語 `reservation.waitlist.aria_hint_register`/`.aria_hint_cancel`）。i18n合計569キー・6言語集合完全一致を機械確認。**続報（同日追加是正）**: グリッド再取得の一部経路（週/表示切替の watch・KeepAlive 再活性化）が `loadMyWaitlist` を呼んでおらず、週移動後に登録済み満席枠の「待機中」表示が消える実害バグが取り残されていたため、`loadGrid` 単体呼び出しの全経路を新設 `loadGridAndWaitlist()` 経由に一本化して構造的に呼び忘れを禁止（`SlotMatrixPicker.spec.ts`/新設 `SlotGridPicker.spec.ts` で週・日付移動後の「待機中」復元をテスト固定）。あわせて `ReservationMyWaitlistList.vue` の `numericTeamId` が `props.teamId` 変更時に再解決されない不備（永続シェルで別チームへ遷移した際に古い数値idで絞り込み続ける誤表示）も是正 |
| 2026-07-30 | **W2-6-FE（仮押さえ自動失効の設定UI＋会員向け注意書き＋429トースト）＋ W2-4-FE（キャンセル待ち登録導線）実装**: ①`ReservationPolicySettings.vue` に `pendingExpireHours`（1〜168・FE でも範囲検証）＋「自動キャンセルしない」トグル（`clearPendingExpireHours`）を追加し、NULL かつ承認モード MANUAL のチームにのみ「仮押さえは自動で取り消されません」の注意書きを表示（§6.3・管理者向け）。**会員向け注意書きも同一 PR で実装**——`ReservationForm.vue`（単枠）・`GroupBookingDialog.vue`（グループ・プレビュー画面）が予約確認ダイアログを開いたときに `GET /reservation-settings`（ADMIN 限定ではなく view ゲート=会員/公開のため会員側からも取得可能・`ReservationBusinessHourController` の `viewAccessGuard` Javadoc で確認済み）を取得し、`approvalMode=MANUAL` かつ `pendingExpireHours` 非 NULL のときのみ「{n}時間以内に承認されない場合は自動的にキャンセルされます」（`reservation.pending_expire_notice.form_note`）を表示する ②`ReservationForm.vue`/`GroupBookingDialog.vue` の両経路で 429=RESERVATION_053 に専用トースト `reservation.message.rate_limited` を追加（§6.4）③新規 `ReservationWaitlistDialog.vue`（登録/取消・ADMIN専用件数表示）＋`ReservationMyWaitlistList.vue`（自分の一覧・予約一覧タブに埋め込み）を実装し、`SlotMatrixPicker.vue`/`SlotGridPicker.vue` の満席（BOOKED）セルから開けるよう結線（§6.1）④**§6.1 のエラーコード記述を訂正**——設計初版が仮置きしていた「重複登録=049」「上限超過=051」「空きあり=050」は誤りで、§10 の確定表どおり**重複登録=047・上限超過=049・空きあり=048・対象なし=046**が実物（`ReservationErrorCode` 実測・BE PR #2525/#2206 で main 着地済）。FE はこの確定コードで実装 ⑤`useReservationApi.ts` に `joinWaitlist`/`leaveWaitlist`/`getWaitlistCount`/`listMyWaitlist` を追加、`types/reservation.ts`（手書き契約型）に `pendingExpireHours`/`clearPendingExpireHours` を追随 ⑥i18n 6言語（`reservation.json`）に `settings.policy.pending_expire.*`・`message.rate_limited`・`waitlist.*`・`pending_expire_notice.form_note` を追加（§11 の当初案キー名は実キー名に置き換えて確定）。**文言方針**: 「優先的に確保」「自動で予約されます」等の独占確保／自動繰り上げ表現は使わない（設計は先着独占を採らない全員一斉通知方式のため）⑦**vitest 実装中に発見・修正した実バグ2件**——(a) `ReservationWaitlistDialog.vue` の `watch(() => props.visible, ...)` に `{ immediate: true }` が無く、ダイアログが最初から `visible=true` で描画された場合に ADMIN 向け `getWaitlistCount` が発火しない欠陥（`{ immediate: true }` 追加で根治）。(b) いずれも実装コードではなくテスト側の不備——`ReservationPolicySettings.spec.ts` で `cancelDeadlineHours` と `pendingExpireHours` に同値（24）を与えたため `InputNumber` の特定が曖昧になり偽の PASS/FAIL を生んでいた（値を分離して根治）。**環境注記**: 本セッション中、Nuxt テスト環境の `beforeAll(setupNuxt())` が既定の 120秒を大幅に超えて（実測 130〜380秒の `transform` フェーズ）ハングする事象を多数観測したが、W2-4-FE/W2-6-FE を一切触っていない既存スペック（`TeamReservationsPanel.spec.ts`・`WeeklyScheduleManager.spec.ts`・`ReservationList.spec.ts`）でも同一症状が再現し、`hookTimeout`/`testTimeout` を引き上げれば全ケース green になることを確認した（44並列worktreeによる共有マシン負荷が原因と推定・コード側の欠陥ではない） |
| 2026-07-30 | **W2-5 §6.2 定期予約（毎週繰り返し）BE 実装（試練先行・green）＋殿の裁定2「強行登録」**: ①`reservations.recurring_series_id BINARY(16) NULL` ＋ `idx_rv_series`（**Flyway V173**・当初 V172 で採番したが push 直前の再確認で V172 が並行着地していたため V173 へ再採番・タイムスタンプ minor は維持）／`ReservationCancelScope`（THIS_ONLY / THIS_AND_FOLLOWING・**語彙は schedule ドメインと同一・型は共有しない**）・`ReservationConfirmScope`（THIS_ONLY / SERIES）・`RecurringWeekSkipReason`（9値）を新設 ②`ReservationRecurringSlotResolver`（週次枠解決・**固定本数クエリで N+1 なし**・`findRecurringCandidateSlots` を新設＝既存に (teamId, slot_date 範囲, start, end) の finder が無かった）＋`ReservationRecurringService`（**非トランザクションのオーケストレーター**＝週ごと独立 tx。`repeatWeeks` の分岐は tx を開く前の Controller に置く） ③キャンセル `scope=THIS_AND_FOLLOWING`（当該日より後の**自分の行のみ**・過去回不変・締切判定は行ごと・超過はスキップ明細）／承認 `scope=SERIES`（**teamId スコープ finder のみ**で他チームの行を掴めない） ④**IDOR 構造防御**: series を引く finder は userId 版と teamId 版の 2 本のみ用意し、スコープ条件なしの finder は作らない ⑤エラーコード **RESERVATION_054**（`RECURRING_RESERVATION_LIMIT_EXCEEDED`・400・実 enum 最大 053 の次） ⑥**§4.3 の 409 ガードと §6.2 が構造的に衝突**（12 週≒84 日 > 90 日 horizon → 会員 1 人の定期予約で管理者が恒久ルールを登録不能）を根治: 定期不可枠 POST/PATCH に `forceCancelConflicting`（既定 false = 従来 409）を追加し、true で衝突予約を一括 ADMIN キャンセル＋申込者へ通知（既存 `NotificationType.RESERVATION_CANCELLED` を再利用し**共有 enum は変更しない**・グループは兄弟行まで展開・監査ログ記録） ⑦AC の表記との差異を正直に記録: **AC-5-13 の「200」は実装では 201**（作成 EP の既存契約）・**スキップ理由は初版 3 値から 9 値へ拡張**（作成用に `CLOSED`/`ALREADY_RESERVED`/`UNAVAILABLE`、キャンセル用に `NOT_CANCELLABLE`/`CANCEL_DEADLINE_PASSED`、承認用に `NOT_PENDING` を追加。丸めると嘘の理由を伝えるため）。**`reason` は OpenAPI で enum として露出**（文字列だと FE の網羅性チェックが効かない・検分 MUST②）。**スコープ外**: FE 全量（段階開示 UI・キャンセル 2 択・series 承認・`reservation.recurring.*` i18n）／グループ予約の `repeatWeeks` |
| 2026-07-29 | **W2-6 §6.3 仮押さえ自動失効 ＋ §6.4 予約作成レートリミット BE 実装（試練先行・green）**: ①`reservation_policies.pending_expire_hours INT NULL DEFAULT 24`（**Flyway V171**・PR 直前に origin/main を再確認したところ V170 が並行着地していたため V171 へ再採番）／`UpdateReservationSettingRequest` に `pendingExpireHours`（1〜168）＋`clearPendingExpireHours`（枠側 `clearApprovalMode` 同形・**両方指定は clear 優先**）を additive 追加・`ReservationSettingsResponse` に露出・設定変更を `audit_logs`（`RESERVATION_PENDING_EXPIRE_SETTING_UPDATED`）に記録 ②`ReservationPendingExpireBatchService`（宣言）＋`ReservationPendingExpireService`（実処理・失効 1 単位を `REQUIRES_NEW`）を新設。**バッチカタログ／`ShedLockConfig` Javadoc 台帳に登録**（併せて W2-4 で追記漏れしていた `reservationWaitlistCleanupBatch`・`reservationReminderDispatchBatch`・`reservationSlotGeneration` も補記） ③**§6.3 の失効条件を改訂**——「枠開始済みは NO_SHOW バッチ管轄で対象外」を撤回し「枠終了経過も失効対象」に変更（**予約ドメインに自動 NO_SHOW バッチが実在しない**ことを実測。初版のままでは承認されず予約日を過ぎた仮押さえが永久に残る）。AC W-4 も同時改訂 ④設計初版の `decrementBookedCountAndReopen` は実在せず実名は **`decrementAndReopen`** であることを反映（枠復帰＝キャンセル待ち連鎖の単一統合点） ⑤`NotificationType.RESERVATION_PENDING_EXPIRED`（NORMAL/RESERVATION）新設 ⑥§6.4 レートリミット: zone `reservation-create`・1 ユーザー 1 分 5 回を `ReservationCreateRateLimiter` に一元化し**単枠 `createReservation` とグループ `createGroup` の両経路が同一バケットを消費**（買い占め防止）・超過は **429 = RESERVATION_053**（`ReservationErrorCode` 実物最大 052 の次で採番。W2-5 は 054 を使うこと）・`GlobalExceptionHandler` に 429 マップ追加・キャンセル待ちは別 zone のまま。**スコープ外**: FE（予約フォームの MANUAL チーム注意書き §11 / 設定 UI / 429 トースト）は別隊 |
| 2026-07-10 | **W2-2 §4 B) 定期予約不可枠 BE 実装（試練先行・green）**: 新テーブル `reservation_recurring_blocked_times`（**Flyway V168**・当初 V151 で採番したが origin/main 取り込み時に同一 major の既存 migration（V151.20260710104426／V151.20260710123257）と衝突したため、origin/main 全体の最大 major+1 である V168 へ再採番。タイムスタンプ minor は維持・UUIDv7・クロスドメインFKなし・`line_id` のみ同一ドメインFK）／`ReservationRecurringBlockedTimeService`＋`Controller`（CRUD5本・全API `isScopeAdmin` ゲート・IDOR404）／`ReservationUnavailabilityChecker` 拡張（**既存 isBlocked 9引数コアは無変更**のまま `slotLineId` 追加の10引数オーバーロード＋`isRecurringBlocked`＋3引数 `isBlockedByAny` 集約を新設。**機能B LINE 軸 enforce を同梱配線**）／機能B の3箇所（listAvailableSlots・createReservation・GridService）＋グループ作成が定期ルールに自動追従／`GridCellDto.unavailableReason`（additive・public定期のみreason・単発重畳は非公開優先null）／エラーコード **RESERVATION_051（RECURRING_BLOCKED_TIME_NOT_FOUND・404）/052（RECURRING_BLOCKED_TIME_LIMIT_EXCEEDED・400）** 採番。UT（Checker/Service/Grid）＋Controller認可宣言＋実MySQL結合テスト追加。**スコープ外**: W2-2-FE（WeeklyScheduleManager統合・会員セル表示）は第二隊 |
| 2026-07-08 | **精査第2パス＋実機E2E耐性クリティック指摘14項目の反映**: ①(A・重大) 単日テンプレ適用（最大+90日）による**日次バッチのウォーターマーク汚染**を根治——`lastGeneratedDate` を `MAX(slot_date) WHERE template_id=T AND slot_date <= tomorrow+27日` で horizon クランプ（案1採用・F03.4.2 §5.4 へ変更履歴つき追補）。§3.4 整合表の「無変更で整合」を「クランプ追補が必要」に訂正・§3.3.2 に horizon 外生成のウォーターマーク扱いを明記・S-8③（+40日実行後もバッチが週次枠を欠落させない番人）追加 ②(B・P1) W2-1 の改修基準を `origin/feat/reservation-ux-wave1-mechanics` の `SlotTemplateManager.vue`（selectedDays で N 回順次 createSlotTemplate ループ・実測）と確定・W1 マージ順 #2198→#2197 を明記 ③(C・P1) 「今すぐ枠を作成」廃止に伴う改訂/削除 i18n キーを W1 ブランチ ja/reservation.json（権威実物）から全数列挙する一覧表を §11 に追加（regenerate_guide/generate_weeks 削除/empty_state_hint/team_guide.line_manage.setup3_steps ほか・実装弾で 1:1 照合注記）・§3.1 の i18n 行を整合 ④(D・P1) 複数曜日ループ×同期生成の集約規則（N 件合算 1 トースト・部分失敗「{total}曜日中{failed}曜日で失敗」）を §3.1 に規定・S-2 に最大 7 曜日の応答時間 AC 追記 ⑤(E・P2) hasBusinessHours=false の空状態ガイド＋generatedCount=0 の原因明示トースト（need_business_hours_hint/generated_zero_hint）・AC S-11 新設 ⑥(F・P2) 臨時営業×同日全日休業の衝突挙動を一意定義（FE=警告＋実行 disabled＋解除導線／BE=ブロックしない）・AC S-12 新設 ⑦(G・P2) PUT のみ応答型変更・GET 不変（消費者 ReservationUnavailabilityManager 無影響）を §3.2 に明記 ⑧(H・中) 単一テンプレ処理量を 96 INSERT（24セル×同一曜日4回）へ訂正（672 は 7 倍過大・全域 13,440≒140 テンプレ×96 と整合）・S-2 同時訂正 ⑨(I・中) 営業時間縮小/定休化時の遡及なしガイド（shrink_note）＋S-6⑤ ⑩(J・中) §6.3 の AC P-3 参照を W-4 へ訂正（grep 全文で残存ゼロ確認） ⑪(K) S-7 に impact 全日クエリの観測点 ⑫(L) W-1 に他人 entry の DELETE/GET 404=048 ⑬(M) 053 共用の D 群再採番時整理を §10 注記 ⑭(N) waitlist count のチーム解決経路と 403→404 順序を §6.1 に一意定義 |
| 2026-07-08 | **精査第1パス指摘の反映（重大1・中5・軽微8）**: ①(重大・§3.1) テンプレ保存=同期自動生成の tx 境界を明文化——生成は保存 tx コミット後に `@Transactional` の外側（コントローラ or 非トランザクションファサード・既存 `generate()` の SUPPORTS+コントローラ呼びと同型）で実行。`fk_rs_template`（V142.002 実DDL）による自己デッドロック罠（親 tx 未コミット行を REQUIRES_NEW チャンク tx が FK 参照 → lock wait timeout）を注記。保存+生成結果を包む `SlotTemplateSaveResponse{template, generation{4カウント+failed}}` を契約定義し S-1/S-3 の観測点を型で固定・S-3b（tx境界番人）追加 ②(中・§3.2) 営業時間 PUT を同型ラッパ `BusinessHoursSaveResponse` へ変更し生成失敗報告契約を固定・生成 scope を**変更曜日差分**に限定（INSERT 量 1/7〜）・生成中 loading/トースト UX 規定・S-6 に失敗注入(200+failed)とタイムアウト内応答の観測点追加 ③(中・§4.4/§11) is_public=ON 時の reason 入力欄に「個人名を含めない」必須ガイド＋推奨語彙プレースホルダ（`reason_no_pii`/`reason_placeholder`）・AC R-9 新設・§7 PII 行を実防御表現へ ④(中・§4.4) 単発 blocked_times×public 定期ルール重畳セルは `unavailableReason=null`（非公開優先）・R-4 に混在ケース追加 ⑤(中・§13) W1（曜日複数選択・アコーディオン化=PR #2198／タブ別ガイド=PR #2197）→W2-1 の直列マージ・リネームは W1 マージ後・W1 曜日複数選択ダイアログを WeeklyScheduleManager へ載せ替えの調停を明記 ⑥(軽・§10) 046/047 のみ W2 確定・048〜053 は D 群着手時に再採番と補強 ⑦(軽・§12) horizon 一覧表（生成28日/単日例外90日/409判定90日=意図的差）固定・予約作成ホットパスへの定期ルール 1 クエリ恒常追加を明記 ⑧(軽・§14) 論点5を変更曜日差分・タイムアウト基準へ整合 |
| 2026-07-08 | 初版作成（マスターの実使用フィードバック起点・第三弾第二波）。A) 営業スケジュール中心モデル: テンプレ保存＝同期自動生成（単一テンプレscope・horizon28日・AFTER_COMMIT非同期は不採用）、「今すぐ枠を作成」/weeks UI 廃止（API残置+deprecated）、営業時間編集UI新設（updateBusinessHours参照ゼロの実測ギャップ解消）、管理タブ再編（営業時間→予約対象→メニュー→週間スケジュール・SlotManagerは詳細設定へ格下げ）、例外日カレンダー（休業=既存blocked-times全日TEAMショートカット・臨時営業=単日テンプレ適用API generate-single-day）。B) 定期予約不可枠: 新テーブル reservation_recurring_blocked_times（UUIDv7・週次・reason必須・is_public）、enforcement は runtime overlap 統合を採用（生成スキップ不採用・比較表）、409=027再利用（90日horizon）、GridCellDto.unavailableReason（additive）。C) 呼称: reservation_team_settings ALTER（resource_name_type/custom・DEFAULT後方互換）、i18nはパラメータ化+プリセット呼称キー採用（単複中立構文）、死蔵キー template.staff/staff_none 削除・display_order キーはLineManager並び順編集で再利用。D) 設計のみ: キャンセル待ち（全員一斉通知採用）・定期予約（スキップ+明細採用・series_id）・仮押さえ失効（pending_expire_hours・CancelledBy.SYSTEM）・予約作成レートリミット（親§8⑩確定・単枠+グループ同一バケット）。エラーコード 046〜053 仮置き（現最大045実確認）・Flyway V146仮（現最大V145実確認・タイムスタンプminor） |
