# F16.1 組織サイドバーナビゲーション

ステータス: 🟡 実装中
最終更新: 2026-05-13

---

## 概要

`organizations/[id]/*` 配下にアコーディオン式左サイドバーを新設する。
MEMBER 以上のみ表示し、（A）組織が有効化したモジュール × （B）ユーザーのロール権限で項目を制御する（方式C）。

---

## 表示制御ロジック

- サイドバー表示条件: MEMBER / DEPUTY_ADMIN / ADMIN / SYSTEM_ADMIN
- 非表示: SUPPORTER / GUEST / 未加入
- 各項目: moduleSlug が null（常時表示） または 組織で有効化済み、かつ requiredRole 以上

---

## DB スキーマ

```sql
CREATE TABLE organization_enabled_modules (
    id              BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
    organization_id BIGINT UNSIGNED NOT NULL,
    module_id       BIGINT UNSIGNED NOT NULL,
    is_enabled      TINYINT(1) NOT NULL DEFAULT 1,
    enabled_at      DATETIME NULL,
    disabled_at     DATETIME NULL,
    enabled_by      BIGINT UNSIGNED NULL,
    created_at      DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at      DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    PRIMARY KEY (id),
    UNIQUE KEY uq_org_module (organization_id, module_id),
    INDEX idx_org_enabled (organization_id, is_enabled),
    CONSTRAINT fk_oem_org    FOREIGN KEY (organization_id) REFERENCES organizations(id),
    CONSTRAINT fk_oem_module FOREIGN KEY (module_id) REFERENCES module_definitions(id),
    CONSTRAINT fk_oem_user   FOREIGN KEY (enabled_by) REFERENCES users(id) ON DELETE SET NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

### 設計根拠（CLAUDE.md 原則6 例外適用）

`organization_enabled_modules` は organization_id × module_id の中間テーブルであり、テナントごとに行が増えるが、
シャーディングのシャードキーは `organization_id` であり、この中間テーブル自体は組織単体に属する。
`module_definitions.id` が BIGINT であることとの整合性を保つため、`BaseEntity`（BIGINT AUTO_INCREMENT）を継承する。
（マスタ例外には該当しないが、中間テーブル例外として主キーを BIGINT AUTO_INCREMENT で設計した）

---

## バックエンド API

### GET /api/v1/organizations/{orgId}/modules

**認可**: MEMBER 以上（`checkMembership` で検証）

**レスポンス例**:
```json
{
  "data": [
    {
      "moduleId": 1,
      "moduleName": "スケジュール管理",
      "moduleSlug": "schedule",
      "isEnabled": true,
      "enabledAt": "2026-05-13T10:00:00"
    }
  ]
}
```

### PATCH /api/v1/organizations/{orgId}/modules/{moduleId}/toggle

**認可**: ADMIN のみ

**リクエスト例**:
```json
{
  "moduleId": 1,
  "enabled": false
}
```

**エラーコード**:
| コード | 意味 |
|---|---|
| TMPL_002 | モジュールが見つかりません |
| TMPL_003 | 無料プランでの選択式モジュール上限（10個）に達しています |
| TMPL_005 | このレベルではモジュールを利用できません |
| TMPL_006 | DEFAULTモジュールは設定を変更できません |
| COMMON_002 | 権限不足（403） |

---

## カテゴリグルーピング（10カテゴリ）

| カテゴリ | 説明 |
|---|---|
| ホーム | ダッシュボード・お知らせ |
| スケジュール | カレンダー・予定 |
| メンバー | 組織メンバー管理 |
| 業務・運用 | シフト・タスク・ワークフロー |
| 施設・設備 | 修繕・設備台帳 |
| 住民・利用者 | 住民情報・ケア記録 |
| データ・分析 | 統計・レポート |
| その他 | 汎用モジュール |
| 収益・外部連携 | 決済・API連携 |
| 設定 | 組織設定 |

---

## 実装フェーズ

| フェーズ | 内容 | 状態 |
|---|---|---|
| Phase 1 | DB（V2.043）+ バックエンド API（本 PR） | 🟡 実装中 |
| Phase 2 | フロントエンド（OrganizationSidebar + layouts/organization.vue + i18n） | 未着手 |
| Phase 3 | 全機能ページへのレイアウト適用 + E2E テスト | 未着手 |

---

## セキュリティ

- フロントエンドの表示制御は UX 目的のみ
- バックエンド API で `checkMembership` / `isAdmin` を必ず実行
- URL を直接打った場合もバックエンド API が 403 を返す

---

## フロントエンド設計（Phase 2 で実装）

- `layouts/organization.vue`: lg 以上 260px 固定サイドバー、lg 未満は PrimeVue Drawer
- `components/OrganizationSidebar.vue`: `useRoleAccess` + `useOrganizationModuleApi` を組み合わせ
- `organizations/[id]/index.vue` は `layout: 'default'` のまま維持（タブ UI を保護）

---

## 関連ファイル（Phase 1 実装分）

| ファイル | 説明 |
|---|---|
| `backend/src/main/resources/db/migration/V2.043__create_organization_enabled_modules_table.sql` | Flyway マイグレーション |
| `backend/.../template/entity/OrganizationEnabledModuleEntity.java` | エンティティ |
| `backend/.../template/repository/OrganizationEnabledModuleRepository.java` | リポジトリ |
| `backend/.../template/dto/OrgModuleResponse.java` | レスポンス DTO |
| `backend/.../template/service/ModuleService.java` | getOrganizationModules / toggleOrganizationModule / isModuleEnabledForOrg 追加 |
| `backend/.../template/TemplateErrorCode.java` | TMPL_006 追加 |
| `backend/.../template/controller/OrganizationModuleController.java` | コントローラー |
