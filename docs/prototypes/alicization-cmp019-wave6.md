# CMP-019 Wave 6 アリシゼーション観測（2026-09-25）

- runId: `CMP019-W6-1790328294335`
- 対象: インシデント一覧・詳細の関係者限定閲覧
- 環境: 実BE `127.0.0.1:8090`、実FE `127.0.0.1:3010`、実MySQL。住民3人は別アカウント・別BrowserContextで並列探索した。
- テスト用インシデント ID 30 は観測後に関連レコードとともに削除し、残存0件を確認した。

| 住民 | 人格 × 攻め口 | 観測 |
| --- | --- | --- |
| 足軽1（P02相当） | 一般 × 素直 | 認証API成功。`/dashboard` は約119秒で loading、約179秒でチーム選択欄を表示。インシデント一覧・詳細には未到達。関連APIは200。Hydration mismatch警告を1回観測。 |
| 足軽2（P07相当） | 低リテラシー × 隙間狙い | 同じチームの無関係者として共有リンクを開くと、詳細・コメントAPIは404。秘密語はDOMに出ず、画面の「一覧へ戻る」で一覧へ移動。一覧は200、空状態を表示。 |
| 足軽3（P06相当） | ADHD傾向 × 隙間狙い | 別チームから共有リンクを開くと詳細・コメントAPIは404、秘密語はDOMに出ない。「一覧へ戻る」後の一覧APIは403。修正前画面は取得失敗と空一覧を同時表示した。 |

足軽3の画面矛盾は、一覧取得失敗時に空表示を抑止し、再試行とダッシュボードへの戻り先を示す修正をPR #3455へ追加した。管理者の一覧・詳細操作は別途、実機E2Eで通過した。足軽1の通常導線探索は開発用FEの描画遅延で未完走のため、アリシゼーションで通過したとは扱わない。

開発用FEの初回表示遅延は実測したが、同時住民数・端末負荷・開発サーバーの影響を切り分けていない。製品の性能不備とは確定していない。
