package com.mannschaft.app.advertising.campaign.enums;

/**
 * F09.17 セグメント評価モード。
 * INCLUDE 同士を AND し、その結果から EXCLUDE を AND NOT する。
 */
public enum AdSegmentInclusionMode {
    INCLUDE,
    EXCLUDE
}
