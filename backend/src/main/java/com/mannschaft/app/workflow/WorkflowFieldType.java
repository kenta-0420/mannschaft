package com.mannschaft.app.workflow;

/**
 * ワークフローフィールドタイプ。テンプレートの入力フィールド種別を表す。
 */
public enum WorkflowFieldType {
    TEXT,
    TEXTAREA,
    NUMBER,
    DATE,
    DATETIME,
    SELECT,
    MULTI_SELECT,
    CHECKBOX,
    FILE,
    USER_SELECT
}
