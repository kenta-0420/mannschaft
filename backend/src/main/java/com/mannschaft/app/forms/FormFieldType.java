package com.mannschaft.app.forms;

/**
 * フォームフィールドの種類。フォームビルダーで使用可能なフィールドタイプを定義する。
 */
public enum FormFieldType {
    TEXT,
    TEXTAREA,
    NUMBER,
    DATE,
    SELECT,
    MULTI_SELECT,
    CHECKBOX,
    RADIO,
    FILE,
    SIGNATURE,
    SEAL,
    LABEL
}
