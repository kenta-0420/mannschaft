package com.mannschaft.app.tournament;

/**
 * 試合結果。
 */
public enum MatchResult {
    HOME_WIN,
    AWAY_WIN,
    DRAW,
    PENDING,
    CANCELLED,
    BYE,
    FORFEIT_HOME_WIN,
    FORFEIT_AWAY_WIN
}
