# F05.2: 回覧板

> **ステータス**: 🟢 設計完了
> **実装フェーズ**: Phase 5
> **最終更新**: 2026-03-15
> **関連ドキュメント**: F05.3 電子印鑑（押印連携）、F04.3 プッシュ通知（リマインド通知）、[統合ストレージクォータ](../cross-cutting/storage_quota.md)（添付ファイルの容量管理）

---

## 1. 概要

文書・お知らせをチーム・組織内のメンバーに回覧し、全員の確認（電子印鑑の押印）をもって完結する回覧板機能。**順番回覧**（前の人が押印後に次の人へ通知）、**ハイブリッド回覧**（上位者は順番、以降は一斉配布）、**一斉回覧**（全員に同時配布）の3方式を提供する。確認は電子印鑑機能（F05.3）と連携し、押印のたびに証跡ログが記録される。押印の表示スタイルとして日本のビジネス慣習に基づく**お辞儀ハンコ**（上位者に向かって傾けて押印）にも対応し、スコープ別・回覧別に設定可能。未確認者への自動リマインド通知で確認漏れを防止し、全員の押印完了時に自動アーカイブする。添付ファイル（PDF・画像等）に対応し、稟議書・安全点検記録・月次報告などの回覧ワークフローを電子化する。

---

## 2. スコープ

### 対象ロール
| ロール | 操作可能な範囲 |
|--------|--------------|
| SYSTEM_ADMIN | 全組織・チームの回覧参照 |
| ADMIN | 回覧作成・編集・削除・回覧先メンバー管理・回覧ステータス管理・催促送信・強制完了 |
| DEPUTY_ADMIN | `MANAGE_CONTENT` 権限を持つ場合: 回覧作成・編集・回覧先管理・催促送信 |
| MEMBER | 回覧作成（チーム設定 `allow_member_circulation` で制御）・回覧の押印（確認）・自分宛の回覧一覧・コメント投稿 |
| SUPPORTER | 回覧の押印（確認）・自分宛の回覧一覧（回覧作成は不可） |
| GUEST | 対象外 |

### 対象レベル
- [x] 組織 (Organization)
- [x] チーム (Team)
- [ ] 個人 (Personal)

---

## 3. DB設計

### テーブル一覧
| テーブル名 | 役割 | 論理削除 |
|-----------|------|---------|
| `circulation_documents` | 回覧文書（タイトル・本文・回覧方式・ステータス・表示スタイル） | あり |
| `circulation_recipients` | 回覧先メンバー（押印状態・順番・傾き角度） | なし |
| `circulation_attachments` | 回覧文書の添付ファイル | なし |
| `circulation_comments` | 回覧に対するコメント（押印時の一言等） | あり |

### テーブル定義

#### `circulation_documents`

回覧文書。1文書に対して複数のメンバーが押印（確認）する。

| カラム名 | 型 | NULL | デフォルト | 説明 |
|---------|---|------|-----------|------|
| `id` | BIGINT UNSIGNED | NO | AUTO_INCREMENT | PK |
| `scope_type` | VARCHAR(20) | NO | — | スコープ種別（`TEAM` / `ORGANIZATION`） |
| `scope_id` | BIGINT UNSIGNED | NO | — | teams.id または organizations.id |
| `created_by` | BIGINT UNSIGNED | NO | — | FK → users（作成者）。ON DELETE RESTRICT |
| `title` | VARCHAR(200) | NO | — | 回覧タイトル |
| `body` | TEXT | YES | NULL | 本文（Markdown 対応。添付ファイルのみの回覧では NULL 許可） |
| `circulation_mode` | VARCHAR(20) | NO | — | 回覧方式。`SEQUENTIAL`（順番回覧）/ `HYBRID`（途中まで順番＋以降一斉）/ `SIMULTANEOUS`（一斉回覧） |
| `sequential_count` | SMALLINT | YES | NULL | HYBRID モードで順番回覧する人数。先頭 N 人が順番、残りが一斉。SEQUENTIAL / SIMULTANEOUS の場合は NULL |
| `status` | VARCHAR(20) | NO | 'DRAFT' | 回覧ステータス（`DRAFT` / `IN_PROGRESS` / `COMPLETED` / `CANCELLED` / `ARCHIVED`） |
| `priority` | VARCHAR(10) | NO | 'NORMAL' | 緊急度（`URGENT` / `NORMAL`）。URGENT は通知時に強調表示 |
| `due_date` | DATE | YES | NULL | 確認期限日（任意。期限超過時にリマインド強化） |
| `reminder_enabled` | BOOLEAN | NO | TRUE | 自動リマインド通知の有効/無効 |
| `reminder_interval_hours` | INT | NO | 24 | リマインド通知の間隔（時間単位。デフォルト: 24時間。有効範囲: 1〜168） |
| `stamp_display_style` | VARCHAR(20) | NO | 'STANDARD' | 押印表示スタイル。`STANDARD`（通常）/ `OJIGI`（お辞儀ハンコ：上位者に向かって傾斜） |
| `total_recipient_count` | SMALLINT | NO | 0 | 回覧先の総人数（非正規化） |
| `stamped_count` | SMALLINT | NO | 0 | 押印済みの人数（非正規化。全員完了で自動 COMPLETED） |
| `completed_at` | DATETIME | YES | NULL | 全員押印完了日時 |
| `attachment_count` | SMALLINT | NO | 0 | 添付ファイル数（非正規化） |
| `comment_count` | SMALLINT | NO | 0 | コメント数（非正規化。論理削除されたコメントは含まない） |
| `created_at` | DATETIME | NO | CURRENT_TIMESTAMP | |
| `updated_at` | DATETIME | NO | CURRENT_TIMESTAMP ON UPDATE | |
| `deleted_at` | DATETIME | YES | NULL | 論理削除日時（SoftDeletableEntity 適用） |

**インデックス**
```sql
INDEX idx_cd_scope (scope_type, scope_id, status, created_at DESC)   -- スコープ別回覧一覧
INDEX idx_cd_created_by (created_by, created_at DESC)                -- 自分が作成した回覧一覧
```

**制約・備考**
- `scope_type` + `scope_id` はポリモーフィック参照。FK 制約なし（アプリ層で検証）
- `status` 遷移: `DRAFT` → `IN_PROGRESS`（回覧開始）→ `COMPLETED`（全員押印完了）→ `ARCHIVED`（自動アーカイブ）。`DRAFT` / `IN_PROGRESS` → `CANCELLED`（取消）。`COMPLETED` → `ARCHIVED` はバッチのみ（`completed_at` から1年経過後）。`ARCHIVED` からの遷移は不可
- 完了判定: `circulation_recipients` に `status = PENDING` が0件になった時点で `status = COMPLETED` + `completed_at = NOW()` を自動更新（SKIPPED を考慮するため stamped_count ではなく PENDING 件数で判定）
- `due_date` は表示・通知用であり、期限超過後も押印操作は可能（強制締切ではない）。自動強制完了は実装しない（安全点検等の法的証跡で「未確認なのに完了扱い」となるリスクを回避）
- DRAFT の回覧は作成者のみ閲覧可能。IN_PROGRESS で回覧先メンバーに公開
- `created_by` ON DELETE RESTRICT: 作成者が退会すると回覧が孤立するため、退会前に未完結回覧の処理を要求。**退会時の運用手順**: (1) IN_PROGRESS の回覧を `force-complete` または `cancel` → (2) DRAFT の回覧を `cancel` + 削除 → (3) ユーザー退会処理。作成者の変更（引継ぎ）APIは提供しない（証跡の作成責任者は事実として不変であるべきため）
- 論理削除: DRAFT / CANCELLED の回覧のみ削除可能。IN_PROGRESS / COMPLETED は削除不可（証跡保持）
- `sequential_count` バリデーション: HYBRID の場合 `1 ≤ sequential_count < total_recipient_count`。SEQUENTIAL / SIMULTANEOUS の場合は NULL であること
- `stamp_display_style`: スコープ別のデフォルト値はチーム設定（`default_stamp_display_style`）で管理。回覧作成時に上書き可能

---

#### `circulation_recipients`

回覧先メンバー。`sort_order` で押印グループを管理する。同じ `sort_order` のメンバーは同時に押印可能。

| カラム名 | 型 | NULL | デフォルト | 説明 |
|---------|---|------|-----------|------|
| `id` | BIGINT UNSIGNED | NO | AUTO_INCREMENT | PK |
| `circulation_id` | BIGINT UNSIGNED | NO | — | FK → circulation_documents（ON DELETE CASCADE） |
| `user_id` | BIGINT UNSIGNED | NO | — | FK → users（ON DELETE RESTRICT） |
| `sort_order` | SMALLINT | NO | 0 | 押印グループ順序。同じ値のメンバーは同時押印可能。SEQUENTIAL: 全員異なる値(0,1,2,...)。HYBRID: 先頭N人は異なる値、残りは同じ値。SIMULTANEOUS: 全員0 |
| `status` | VARCHAR(20) | NO | 'PENDING' | 確認状態（`PENDING` / `STAMPED` / `SKIPPED`）。SKIPPED = ADMIN が強制スキップ |
| `stamped_at` | DATETIME | YES | NULL | 押印日時 |
| `stamp_log_id` | BIGINT UNSIGNED | YES | NULL | FK → seal_stamp_logs（押印時に記録される証跡ログの ID） |
| `tilt_angle` | SMALLINT | NO | 0 | 押印傾き角度（度単位、-30〜+30）。お辞儀スタイル時に使用。正値=左傾き（上位者方向へお辞儀） |
| `is_flipped` | BOOLEAN | NO | FALSE | ランダム逆さまイースターエッグ（押印時に約0.1%の確率で TRUE） |
| `corrected_at` | DATETIME | YES | NULL | 逆さまハンコの訂正日時（訂正押印時にセット） |
| `correction_stamp_log_id` | BIGINT UNSIGNED | YES | NULL | FK → seal_stamp_logs（訂正押印の証跡ログ ID） |
| `read_at` | DATETIME | YES | NULL | 回覧詳細を初めて閲覧した日時（GET /{id} 初回アクセス時に自動セット） |
| `delegated_by` | BIGINT UNSIGNED | YES | NULL | FK → users（代理押印した ADMIN の ID。代理押印時のみ設定） |
| `skip_reason` | VARCHAR(255) | YES | NULL | ADMIN によるスキップ理由（SKIPPED 時のみ設定） |
| `last_reminded_at` | DATETIME | YES | NULL | 最後にリマインド通知を送った日時 |
| `created_at` | DATETIME | NO | CURRENT_TIMESTAMP | |

**インデックス**
```sql
UNIQUE KEY uq_cr_doc_user (circulation_id, user_id)              -- 同一回覧への重複登録防止
INDEX idx_cr_user (user_id, status)                               -- 自分宛の未確認回覧一覧
INDEX idx_cr_doc_order (circulation_id, sort_order)               -- 順番回覧の次の確認者取得
```

**制約・備考**
- `user_id` ON DELETE RESTRICT: 回覧対象者が退会する場合、未押印の回覧があればエラー。ADMIN が先に SKIPPED に変更するか回覧を完了させる必要がある
- 作成者自身を `recipient_ids` に含めることも可能（安全点検等で全員の押印が必要なケース）。フロントエンドの「チーム全員」ボタンで自分を除外する必要がない設計
- `sort_order` グループ管理: 同じ `sort_order` を持つメンバーは同時に押印可能。前のグループ（より小さい `sort_order`）に PENDING がなくなった時点で次のグループがアンロックされる
  - SEQUENTIAL: `sort_order` = 0, 1, 2, 3, ...（全員異なる → 1人ずつ順番）
  - HYBRID（`sequential_count=2`）: `sort_order` = 0, 1, 2, 2, 2, ...（先頭2人は順番、残りは一斉）
  - SIMULTANEOUS: `sort_order` = 0, 0, 0, ...（全員同じ → 全員同時）
- `stamp_log_id` は F05.3 の `seal_stamp_logs` への FK。押印操作は `CirculationService` 経由で `SealStampService.stamp()` を呼び出し、返却された `stamp_log_id` を保存
- SKIPPED: ADMIN が特定のユーザーを強制スキップ（長期不在等）。`stamped_count` にはカウントしないが、完了判定は `PENDING が0件` で行う（STAMPED + SKIPPED = total_recipient_count）
- `tilt_angle`: `stamp_display_style = 'OJIGI'` の場合に自動計算。デフォルト = `min(sort_order × 8, 30)` 度。ユーザーが押印前にフロントエンドで -30〜+30 の範囲で調整可能。STANDARD スタイルの場合は常に 0
- `is_flipped`: 押印時にバックエンドが `SecureRandom` で 0.1%（1/1000）の確率で TRUE を決定。TRUE の場合、フロントエンドでハンコを 180° 回転して表示（逆さまハンコ）。ユーザーは訂正 API で「二重線 + 押し直し」が可能
- `correction_stamp_log_id`: 訂正押印時に新たな `seal_stamp_logs` レコードを作成し、その ID を格納。フロントエンドでは元のハンコ（二重線付き）+ 訂正ハンコ（隣接配置）の両方を表示
- `read_at`: GET /circulation/{id} で回覧詳細を初めて閲覧した時点で `NOW()` をセット（2回目以降は更新しない）。押印状況画面で「未読 / 既読未押印 / 押印済み」の3段階表示に使用。リマインド通知の文面出し分けにも活用
- `delegated_by`: ADMIN が代理押印した場合にセット。`delegated_by IS NOT NULL` の場合、押印状況画面・PDF エクスポートで「代理: {ADMIN名}」と表示。ON DELETE RESTRICT（代理押印者の退会時にも証跡保持が必要）

---

#### `circulation_attachments`

回覧文書の添付ファイル。PDF・画像等をサポート。

| カラム名 | 型 | NULL | デフォルト | 説明 |
|---------|---|------|-----------|------|
| `id` | BIGINT UNSIGNED | NO | AUTO_INCREMENT | PK |
| `circulation_id` | BIGINT UNSIGNED | NO | — | FK → circulation_documents（ON DELETE CASCADE） |
| `file_key` | VARCHAR(500) | NO | — | S3 オブジェクトキー |
| `original_filename` | VARCHAR(255) | NO | — | アップロード時の元ファイル名 |
| `file_size` | INT UNSIGNED | NO | — | ファイルサイズ（バイト。上限 20MB） |
| `mime_type` | VARCHAR(100) | NO | — | MIMEタイプ（`application/pdf`, `image/webp` 等） |
| `sort_order` | SMALLINT | NO | 0 | 表示順 |
| `created_at` | DATETIME | NO | CURRENT_TIMESTAMP | |

**インデックス**
```sql
INDEX idx_ca_doc (circulation_id, sort_order)   -- 回覧別添付ファイル一覧
```

**制約・備考**
- `circulation_id` ON DELETE CASCADE: 回覧文書削除時にカスケード。削除時は `StorageQuotaService.recordDeletion()` で使用量を減算
- 対応ファイル形式: PDF, JPEG, PNG, WebP, GIF（画像は Lambda で WebP 変換。Lambda 生成ファイルはクォータ対象外）
- ファイルサイズ上限: 1ファイル20MB、1回覧あたり合計50MB
- S3 配信: CloudFront 経由の Pre-signed URL（有効期限: 1時間）
- 最大10ファイル/回覧

---

#### `circulation_comments`

回覧に対するコメント。押印時の一言や質問・確認事項を記録する。

| カラム名 | 型 | NULL | デフォルト | 説明 |
|---------|---|------|-----------|------|
| `id` | BIGINT UNSIGNED | NO | AUTO_INCREMENT | PK |
| `circulation_id` | BIGINT UNSIGNED | NO | — | FK → circulation_documents（ON DELETE CASCADE） |
| `user_id` | BIGINT UNSIGNED | NO | — | FK → users（ON DELETE RESTRICT） |
| `body` | TEXT | NO | — | コメント本文（最大1,000文字） |
| `created_at` | DATETIME | NO | CURRENT_TIMESTAMP | |
| `deleted_at` | DATETIME | YES | NULL | 論理削除日時 |

**インデックス**
```sql
INDEX idx_cc_doc (circulation_id, created_at)   -- 回覧別コメント一覧
```

**制約・備考**
- 押印時にコメントを付けることが可能（任意）
- 押印せずにコメントのみ投稿することも可能（質問・確認用）
- コメントの編集は不可（証跡性のため）。論理削除のみ

### ER図（テキスト形式）
```
circulation_documents (1) ──── (N) circulation_recipients
circulation_documents (1) ──── (N) circulation_attachments
circulation_documents (1) ──── (N) circulation_comments
users (1) ──── (N) circulation_documents [created_by]
users (1) ──── (N) circulation_recipients [user_id]
users (1) ──── (N) circulation_comments [user_id]
circulation_recipients (N) ──── (0..1) seal_stamp_logs [stamp_log_id, F05.3]
circulation_recipients (N) ──── (0..1) seal_stamp_logs [correction_stamp_log_id, F05.3]
```

---

## 4. API設計

### エンドポイント一覧
| メソッド | パス | 認証 | 説明 |
|---------|-----|------|------|
| GET | `/api/v1/circulation` | 必要 | 回覧一覧（スコープ別、ステータス・キーワード・期限超過フィルタ） |
| POST | `/api/v1/circulation` | 必要 | 回覧作成（DRAFT で保存） |
| GET | `/api/v1/circulation/{id}` | 必要 | 回覧詳細（押印状況含む） |
| PUT | `/api/v1/circulation/{id}` | 必要 | 回覧編集（DRAFT のみ） |
| DELETE | `/api/v1/circulation/{id}` | 必要 | 回覧削除（DRAFT / CANCELLED のみ） |
| POST | `/api/v1/circulation/{id}/start` | 必要 | 回覧開始（DRAFT → IN_PROGRESS） |
| POST | `/api/v1/circulation/{id}/cancel` | 必要 | 回覧取消（DRAFT / IN_PROGRESS → CANCELLED） |
| POST | `/api/v1/circulation/{id}/force-complete` | 必要 | 強制完了（ADMIN のみ） |
| POST | `/api/v1/circulation/{id}/stamp` | 必要 | 押印（確認） |
| POST | `/api/v1/circulation/{id}/stamp/correct` | 必要 | 逆さまハンコの訂正（二重線＋押し直し） |
| PATCH | `/api/v1/circulation/{id}/recipients/{userId}/skip` | 必要 | 対象者の強制スキップ（ADMIN） |
| POST | `/api/v1/circulation/{id}/remind` | 必要 | 手動リマインド送信（アンロック済みグループの未確認者） |
| GET | `/api/v1/circulation/{id}/status` | 必要 | 押印状況一覧 |
| GET | `/api/v1/circulation/{id}/export` | 必要 | 押印済み証跡付き PDF エクスポート（COMPLETED のみ。生成済みなら302、未生成なら202） |
| GET | `/api/v1/circulation/{id}/export/status` | 必要 | PDF 生成状況の確認 |
| GET | `/api/v1/circulation/my` | 必要 | 自分宛の回覧一覧（未確認/確認済みフィルタ） |
| POST | `/api/v1/circulation/{id}/attachments` | 必要 | 添付ファイル追加（DRAFT のみ） |
| DELETE | `/api/v1/circulation/{id}/attachments/{attachmentId}` | 必要 | 添付ファイル削除（DRAFT のみ） |
| GET | `/api/v1/circulation/{id}/comments` | 必要 | コメント一覧 |
| POST | `/api/v1/circulation/{id}/comments` | 必要 | コメント投稿 |
| DELETE | `/api/v1/circulation/{id}/comments/{commentId}` | 必要 | コメント削除（本人 or ADMIN） |
| POST | `/api/v1/circulation/{id}/duplicate` | 必要 | 回覧の複製（既存回覧から新規 DRAFT を作成） |
| POST | `/api/v1/circulation/{id}/stamp/delegate` | 必要 | 代理押印（ADMIN が不在者に代わり押印） |
| POST | `/api/v1/circulation/batch/force-complete` | 必要 | 一括強制完了（ADMIN。最大20件） |

### リクエスト／レスポンス仕様

#### `GET /api/v1/circulation`

**認可**: 対象スコープの SUPPORTER 以上 / SYSTEM_ADMIN（全スコープ参照可）

スコープ別の回覧一覧をカーソルベースのページネーションで取得する。DRAFT は作成者本人にのみ表示される。

**クエリパラメータ**
| パラメータ | 型 | 必須 | 説明 |
|-----------|---|------|------|
| `scope_type` | String | Yes | `TEAM` / `ORGANIZATION` |
| `scope_id` | Long | Yes | 対象スコープの ID |
| `status` | String | No | `DRAFT` / `IN_PROGRESS` / `COMPLETED` / `CANCELLED` / `ARCHIVED`（フィルタ。デフォルト: ARCHIVED 以外の全件。DRAFT は作成者本人の回覧のみ返却。ARCHIVED は明示指定時のみ） |
| `keyword` | String | No | タイトル部分一致検索（`title LIKE '%keyword%'`） |
| `overdue` | Boolean | No | `true` の場合、期限超過の IN_PROGRESS 回覧のみ（`due_date < NOW()` かつ `status = IN_PROGRESS`） |
| `cursor` | Long | No | カーソル |
| `limit` | Integer | No | 取得件数（デフォルト: 20、最大: 50） |

**レスポンス（200 OK）**
```json
{
  "data": [
    {
      "id": 1,
      "title": "3月度安全点検チェックリスト",
      "circulation_mode": "HYBRID",
      "sequential_count": 2,
      "status": "IN_PROGRESS",
      "priority": "NORMAL",
      "due_date": "2026-03-20",
      "stamp_display_style": "OJIGI",
      "created_by": {
        "id": 10,
        "display_name": "田中太郎",
        "avatar_url": "https://cdn.example.com/avatars/10.webp"
      },
      "total_recipient_count": 8,
      "stamped_count": 3,
      "current_recipients": [
        { "id": 25, "display_name": "佐藤花子" }
      ],
      "attachment_count": 2,
      "comment_count": 3,
      "created_at": "2026-03-10T09:00:00",
      "due_date_status": "ON_TRACK"
    }
  ],
  "meta": {
    "next_cursor": null,
    "limit": 20,
    "has_next": false
  }
}
```

**補足**
- `current_recipients`: 現在の確認待ちユーザー情報の配列。SEQUENTIAL: 1人。HYBRID: 順番部分では1人、一斉部分では残り全員。SIMULTANEOUS: 全 PENDING ユーザー
- `due_date_status`: `ON_TRACK`（期限内）/ `DUE_SOON`（期限3日以内）/ `OVERDUE`（期限超過）/ `null`（期限未設定）

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 403 | 対象スコープのメンバーでない |

---

#### `POST /api/v1/circulation`

**認可**: ADMIN / DEPUTY_ADMIN（`MANAGE_CONTENT`）。チーム設定 `allow_member_circulation = true` の場合は MEMBER も作成可能

回覧文書を DRAFT ステータスで作成する。添付ファイルがある場合は[統合ストレージクォータ](../cross-cutting/storage_quota.md)のチェックを事前に実行する。

**リクエスト（multipart/form-data）**
| フィールド | 型 | 必須 | 説明 |
|-----------|---|------|------|
| `scope_type` | String | Yes | `TEAM` / `ORGANIZATION` |
| `scope_id` | Long | Yes | 対象スコープの ID |
| `title` | String | Yes | タイトル（1〜200文字） |
| `body` | String | No | 本文（Markdown、最大10,000文字） |
| `circulation_mode` | String | Yes | `SEQUENTIAL` / `HYBRID` / `SIMULTANEOUS` |
| `sequential_count` | Integer | Conditional | HYBRID の場合のみ必須。先頭から何人を順番回覧にするか（1 ≤ N < recipient_ids.length） |
| `priority` | String | No | `URGENT` / `NORMAL`（デフォルト: `NORMAL`） |
| `due_date` | String | No | 確認期限日（ISO 8601 date） |
| `reminder_enabled` | Boolean | No | デフォルト: `true` |
| `reminder_interval_hours` | Integer | No | リマインド間隔（時間。1〜168。デフォルト: `24`） |
| `stamp_display_style` | String | No | `STANDARD` / `OJIGI`（デフォルト: スコープ設定値。未設定時は `STANDARD`） |
| `recipient_ids` | Long[] | Yes | 回覧先ユーザー ID の配列（最低1人。SEQUENTIAL / HYBRID の場合は配列順が回覧順序。重複不可） |
| `files` | MultipartFile[] | No | 添付ファイル（最大10ファイル、各20MB以下） |

**レスポンス（201 Created）**
```json
{
  "data": {
    "id": 1,
    "title": "3月度安全点検チェックリスト",
    "status": "DRAFT",
    "circulation_mode": "HYBRID",
    "sequential_count": 2,
    "priority": "NORMAL",
    "due_date": "2026-03-20",
    "stamp_display_style": "OJIGI",
    "total_recipient_count": 5,
    "recipients": [
      { "user_id": 20, "display_name": "部長 佐藤", "sort_order": 0, "status": "PENDING" },
      { "user_id": 21, "display_name": "課長 鈴木", "sort_order": 1, "status": "PENDING" },
      { "user_id": 22, "display_name": "田中一郎", "sort_order": 2, "status": "PENDING" },
      { "user_id": 23, "display_name": "山本花子", "sort_order": 2, "status": "PENDING" },
      { "user_id": 24, "display_name": "伊藤三郎", "sort_order": 2, "status": "PENDING" }
    ],
    "attachments": [
      {
        "id": 1,
        "original_filename": "点検表.pdf",
        "file_size": 1048576,
        "mime_type": "application/pdf",
        "url": "https://cdn.example.com/circulation/1/file1.pdf"
      }
    ],
    "created_at": "2026-03-10T09:00:00"
  }
}
```

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 400 | title 未入力 / recipient_ids 空 / recipient_ids に重複あり / recipient_ids に非メンバー含有 / ファイル数超過 / HYBRID で sequential_count 未指定 or 範囲外 / reminder_interval_hours が 1〜168 の範囲外 |
| 403 | 権限不足 / 対象スコープのメンバーでない / MEMBER で allow_member_circulation = false |
| 413 | ファイルサイズ超過 |

---

#### `GET /api/v1/circulation/{id}`

**認可**: 回覧先メンバー / 作成者 / ADMIN / SYSTEM_ADMIN（SEQUENTIAL/HYBRID でも全メンバーが閲覧可能。押印のみ順番制約）

回覧の詳細を取得する。押印状況、添付ファイルを含む。

**レスポンス（200 OK）**
```json
{
  "data": {
    "id": 1,
    "scope_type": "TEAM",
    "scope_id": 1,
    "title": "3月度安全点検チェックリスト",
    "body": "## 点検項目\n\n以下のチェックリストを確認し...",
    "circulation_mode": "HYBRID",
    "sequential_count": 2,
    "status": "IN_PROGRESS",
    "priority": "NORMAL",
    "due_date": "2026-03-20",
    "stamp_display_style": "OJIGI",
    "reminder_enabled": true,
    "reminder_interval_hours": 24,
    "created_by": {
      "id": 10,
      "display_name": "田中太郎",
      "avatar_url": "https://cdn.example.com/avatars/10.webp"
    },
    "total_recipient_count": 5,
    "stamped_count": 2,
    "recipients": [
      {
        "user_id": 20,
        "display_name": "部長 佐藤",
        "avatar_url": null,
        "sort_order": 0,
        "status": "STAMPED",
        "stamped_at": "2026-03-11T10:30:00",
        "seal_variant": "LAST_NAME",
        "seal_svg": "<svg>...</svg>",
        "tilt_angle": 0,
        "is_flipped": false,
        "correction": null
      },
      {
        "user_id": 21,
        "display_name": "課長 鈴木",
        "avatar_url": null,
        "sort_order": 1,
        "status": "STAMPED",
        "stamped_at": "2026-03-12T14:00:00",
        "seal_variant": "LAST_NAME",
        "seal_svg": "<svg>...</svg>",
        "tilt_angle": 8,
        "is_flipped": true,
        "correction": {
          "stamp_log_id": 99,
          "seal_svg": "<svg>...</svg>",
          "tilt_angle": 8,
          "corrected_at": "2026-03-12T14:01:00"
        }
      },
      {
        "user_id": 22,
        "display_name": "田中一郎",
        "avatar_url": null,
        "sort_order": 2,
        "status": "PENDING",
        "stamped_at": null,
        "seal_variant": null,
        "seal_svg": null,
        "tilt_angle": 0
      }
    ],
    "attachments": [
      {
        "id": 1,
        "original_filename": "点検表.pdf",
        "file_size": 1048576,
        "mime_type": "application/pdf",
        "url": "https://cdn.example.com/circulation/1/file1.pdf"
      }
    ],
    "comment_count": 3,
    "my_stamp_status": "PENDING",
    "can_stamp": true,
    "completed_at": null,
    "created_at": "2026-03-10T09:00:00"
  }
}
```

**補足**
- `my_stamp_status`: ログインユーザー自身の押印状態。回覧先でない場合は `null`
- `can_stamp`: 現在押印可能かどうか（PENDING かつ自分の sort_order グループがアンロック済み）
- `tilt_angle`: `stamp_display_style = 'OJIGI'` の場合のみ非0値。フロントエンドで `transform: rotate({tilt_angle}deg)` で表示
- `seal_svg`: 押印済みの場合のみ返却。押印時点の印鑑 SVG を `seal_stamp_logs` 経由で取得
- SEQUENTIAL / HYBRID でも全メンバーが回覧内容を閲覧可能。押印操作のみ sort_order による順番制約

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 403 | 回覧先メンバーでも作成者でも ADMIN でもない |
| 404 | 回覧が存在しない / 論理削除済み |

---

#### `PUT /api/v1/circulation/{id}`

**認可**: 作成者本人 / ADMIN（DRAFT のみ）

DRAFT 状態の回覧を編集する。IN_PROGRESS 以降は編集不可（押印済み内容の事後改ざん防止）。添付ファイルの追加・削除は専用 API（POST/DELETE /attachments）を使用する。

**リクエストボディ（application/json）**
```json
{
  "title": "3月度安全点検チェックリスト（修正版）",
  "circulation_mode": "HYBRID",
  "sequential_count": 3,
  "recipient_ids": [20, 21, 22, 23, 24]
}
```

| フィールド | 型 | 必須 | 説明 |
|-----------|---|------|------|
| `title` | String | No | タイトル（1〜200文字） |
| `body` | String | No | 本文（Markdown、最大10,000文字） |
| `circulation_mode` | String | No | `SEQUENTIAL` / `HYBRID` / `SIMULTANEOUS` |
| `sequential_count` | Integer | Conditional | HYBRID の場合のみ必須 |
| `priority` | String | No | `URGENT` / `NORMAL` |
| `due_date` | String | No | 確認期限日（ISO 8601 date。`null` で期限削除） |
| `reminder_enabled` | Boolean | No | 自動リマインドの有効/無効 |
| `reminder_interval_hours` | Integer | No | リマインド間隔（時間。1〜168） |
| `stamp_display_style` | String | No | `STANDARD` / `OJIGI` |
| `recipient_ids` | Long[] | No | 回覧先ユーザー ID の配列（指定時は全量置換。最低1人） |

**レスポンス（200 OK）**: GET /circulation/{id} と同一構造

**補足**
- `recipient_ids` を指定した場合、既存の `circulation_recipients` を全削除して再 INSERT（DRAFT なので押印データなし）
- `recipient_ids` 変更時は `total_recipient_count` も再計算
- `circulation_mode` 変更時に `recipient_ids` も再指定すること（sort_order の再割当が必要なため）

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 400 | バリデーションエラー（POST /circulation と同一条件） |
| 403 | 権限不足 |
| 404 | 回覧が存在しない |
| 409 | status が DRAFT でない |

---

#### `DELETE /api/v1/circulation/{id}`

**認可**: 作成者本人 / ADMIN

DRAFT / CANCELLED の回覧を論理削除する。IN_PROGRESS / COMPLETED は削除不可（証跡保持）。

**レスポンス（204 No Content）**

**補足**
- 論理削除（`deleted_at = NOW()`）。`circulation_attachments` は ON DELETE CASCADE だが、論理削除のため S3 上の添付ファイルはアプリ層で削除
- CANCELLED の回覧を削除した場合、押印済みの `seal_stamp_logs` は残存する（証跡保持）

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 403 | 権限不足 |
| 404 | 回覧が存在しない / 既に削除済み |
| 409 | status が DRAFT / CANCELLED でない |

---

#### `POST /api/v1/circulation/{id}/start`

**認可**: 作成者本人 / ADMIN

DRAFT の回覧を開始（IN_PROGRESS に遷移）する。回覧先メンバーに通知を送信する。

**リクエストボディ**: なし

**レスポンス（200 OK）**: GET /circulation/{id} と同一構造（`status: "IN_PROGRESS"`）

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 400 | `total_recipient_count` が0（回覧先が未設定の DRAFT を開始しようとした場合） |
| 403 | 権限不足 |
| 409 | status が DRAFT でない |

---

#### `POST /api/v1/circulation/{id}/cancel`

**認可**: 作成者本人 / ADMIN

回覧を取消する。DRAFT / IN_PROGRESS → CANCELLED に遷移。

**リクエストボディ**: なし

**レスポンス（200 OK）**: GET /circulation/{id} と同一構造（`status: "CANCELLED"`）

**補足**
- IN_PROGRESS からの取消: 全回覧先メンバーにプッシュ通知（「回覧が取り消されました: {title}」）
- DRAFT からの取消: 通知なし（回覧先メンバーに未公開のため）
- 押印済みの `seal_stamp_logs` は取消しない（証跡保持。回覧の取消と押印の取消は独立）
- 監査ログに `CIRCULATION_CANCELLED` イベントを記録（actor_id, circulation_id）

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 403 | 権限不足 |
| 404 | 回覧が存在しない |
| 409 | status が COMPLETED / CANCELLED（遷移不可） |

---

#### `POST /api/v1/circulation/{id}/stamp`

**認可**: 回覧先メンバー（本人）

回覧に押印（確認）する。電子印鑑（F05.3）を使用して押印証跡を記録する。

**リクエストボディ**
```json
{
  "seal_id": 1,
  "tilt_angle": 8,
  "comment": "確認しました。3階の消火器の期限が近いです。"
}
```

| フィールド | 型 | 必須 | 説明 |
|-----------|---|------|------|
| `seal_id` | Long | No | 使用する印鑑 ID。省略時はスコープ別デフォルト印鑑（F05.3 `seal_scope_defaults`） |
| `tilt_angle` | Integer | No | 押印傾き角度（-30〜+30度）。`stamp_display_style = 'OJIGI'` 時のみ有効。省略時は自動計算（`min(sort_order × 8, 30)`） |
| `comment` | String | No | 押印時のコメント（最大1,000文字） |

**レスポンス（200 OK）**
```json
{
  "data": {
    "stamp_log_id": 42,
    "seal_variant": "LAST_NAME",
    "seal_svg": "<svg>...</svg>",
    "tilt_angle": 8,
    "is_flipped": false,
    "stamped_at": "2026-03-11T10:30:00",
    "stamped_count": 3,
    "total_recipient_count": 5,
    "is_completed": false,
    "next_recipients": [
      { "user_id": 22, "display_name": "田中一郎" },
      { "user_id": 23, "display_name": "山本花子" },
      { "user_id": 24, "display_name": "伊藤三郎" }
    ]
  }
}
```

**補足**
- `next_recipients`: 次にアンロックされる確認者の配列。SEQUENTIAL: 1人。HYBRID の一斉部分突入時: 残り全員。SIMULTANEOUS: 他の PENDING 全員。最後の確認者が押印した場合は空配列
- `tilt_angle`: 実際に適用された傾き角度。STANDARD スタイルの場合は常に 0
- `is_flipped`: `true` の場合、ハンコが逆さま（180°回転）で押されたイースターエッグ発動。フロントエンドで訂正ボタンを表示
- `is_completed`: 全員の押印が完了した場合 `true`。自動的に `status = COMPLETED` に遷移
- `comment` が指定された場合、`circulation_comments` にも同時に INSERT

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 400 | seal_id が自分の印鑑でない / tilt_angle が -30〜+30 の範囲外 |
| 403 | 回覧先メンバーでない / 自分の sort_order グループがまだアンロックされていない |
| 404 | 回覧が存在しない |
| 409 | 既に押印済み / status が IN_PROGRESS でない |

---

#### `POST /api/v1/circulation/{id}/stamp/correct`

**認可**: 回覧先メンバー（本人、`is_flipped = true` の場合のみ）

逆さまに押されたハンコを訂正する。元のハンコに二重線を引き、隣に正しい向きで押し直す（日本のビジネス慣習「訂正印」を再現）。

**リクエストボディ**
```json
{
  "seal_id": 1
}
```

| フィールド | 型 | 必須 | 説明 |
|-----------|---|------|------|
| `seal_id` | Long | No | 使用する印鑑 ID。省略時はスコープ別デフォルト印鑑（元の押印と同じ印鑑を推奨） |

**レスポンス（200 OK）**
```json
{
  "data": {
    "original_stamp": {
      "stamp_log_id": 42,
      "seal_svg": "<svg>...</svg>",
      "is_flipped": true,
      "is_corrected": true,
      "stamped_at": "2026-03-11T10:30:00"
    },
    "correction_stamp": {
      "stamp_log_id": 99,
      "seal_variant": "LAST_NAME",
      "seal_svg": "<svg>...</svg>",
      "tilt_angle": 8,
      "stamped_at": "2026-03-11T10:31:00"
    }
  }
}
```

**補足**
- フロントエンド表示: 元のハンコ（180°回転 + 二重線 SVG オーバーレイ）の右隣に訂正ハンコを配置
- 二重線の描画: CSS `text-decoration: line-through` ではなく、SVG の `<line>` 2本（45°の対角線×2）をハンコの上に重ねる
- 訂正ハンコの `tilt_angle` は `stamp_display_style` に従う（OJIGI なら自動計算、STANDARD なら 0°）
- 訂正は1回のみ可能（既に訂正済みの場合は 409）
- 訂正しなくても回覧の進行には影響しない（逆さまのままでも押印は有効）

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 400 | seal_id が自分の印鑑でない |
| 403 | 回覧先メンバーでない |
| 404 | 回覧が存在しない |
| 409 | is_flipped = false（逆さまでない）/ 既に訂正済み / status が IN_PROGRESS でない |

---

#### `PATCH /api/v1/circulation/{id}/recipients/{userId}/skip`

**認可**: ADMIN

特定のメンバーを強制スキップする（長期不在・退職予定等）。

**リクエストボディ**
```json
{
  "reason": "長期出張のため"
}
```

| フィールド | 型 | 必須 | 説明 |
|-----------|---|------|------|
| `reason` | String | No | スキップ理由（最大255文字）。`circulation_recipients.skip_reason` に保存。PDF エクスポート時に「スキップ: {reason}」として出力 |

**レスポンス（200 OK）**: GET /circulation/{id}/status と同一構造

**補足**
- スキップにより前のグループの PENDING が0件になった場合、次のグループが自動アンロックされ、該当メンバーにプッシュ通知を送信（押印フローの通知ロジックと同一）
- 全 PENDING が0件になった場合は自動完了（`status = COMPLETED`、`completed_at = NOW()`）

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 403 | ADMIN でない |
| 404 | 対象ユーザーが回覧先に含まれていない |
| 409 | 対象ユーザーが既に STAMPED / SKIPPED / status が IN_PROGRESS でない |

---

#### `POST /api/v1/circulation/{id}/force-complete`

**認可**: ADMIN

未押印者がいる状態で回覧を強制完了する。未押印者は SKIPPED に変更される。

**リクエストボディ**
```json
{
  "reason": "期限超過のため強制完了"
}
```

| フィールド | 型 | 必須 | 説明 |
|-----------|---|------|------|
| `reason` | String | No | 強制完了の理由（最大255文字）。監査ログに記録 |

**レスポンス（200 OK）**: GET /circulation/{id} と同一構造（`status: "COMPLETED"`）

**補足**
- 全 PENDING ユーザーを SKIPPED に一括変更し、`skip_reason` に `reason` を一括コピー。`status = COMPLETED`、`completed_at = NOW()` を設定
- 個別に異なる理由を設定したい場合は、事前に `PATCH /recipients/{userId}/skip` で個別スキップしてから `force-complete` を実行する運用
- 監査ログに `CIRCULATION_FORCE_COMPLETED` イベントを記録（actor_id, circulation_id, skipped_user_ids, reason）
- 全回覧先メンバーにプッシュ通知（「回覧が強制完了されました: {title}」）

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 403 | ADMIN でない |
| 404 | 回覧が存在しない |
| 409 | status が IN_PROGRESS でない / PENDING ユーザーがいない（既に全員 STAMPED/SKIPPED） |

---

#### `POST /api/v1/circulation/{id}/remind`

**認可**: 作成者 / ADMIN / DEPUTY_ADMIN（`MANAGE_CONTENT`）

現在アンロック済みグループの未確認メンバーに手動リマインド通知を送信する。SEQUENTIAL/HYBRID で未到達のグループには送信しない（自動リマインドバッチと同一ロジック）。

**リクエストボディ**: なし

**レスポンス（200 OK）**
```json
{
  "data": {
    "reminded_count": 3,
    "reminded_users": [
      { "user_id": 22, "display_name": "高橋次郎" },
      { "user_id": 23, "display_name": "山本三郎" },
      { "user_id": 24, "display_name": "伊藤四郎" }
    ]
  }
}
```

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 403 | 権限不足 |
| 404 | 回覧が存在しない |
| 409 | status が IN_PROGRESS でない / アンロック済みグループに未確認者がいない |
| 429 | 手動リマインドのレートリミット超過（1回覧あたり1時間に1回） |

---

#### `GET /api/v1/circulation/{id}/status`

**認可**: 回覧先メンバー / 作成者 / ADMIN / SYSTEM_ADMIN

押印状況の一覧を取得する。誰がいつ押印したか、誰が未確認かを確認できる。

**レスポンス（200 OK）**
```json
{
  "data": {
    "circulation_id": 1,
    "circulation_mode": "HYBRID",
    "sequential_count": 2,
    "status": "IN_PROGRESS",
    "stamp_display_style": "OJIGI",
    "total_recipient_count": 5,
    "stamped_count": 2,
    "recipients": [
      {
        "user_id": 20,
        "display_name": "部長 佐藤",
        "sort_order": 0,
        "status": "STAMPED",
        "stamped_at": "2026-03-11T10:30:00",
        "seal_variant": "LAST_NAME",
        "tilt_angle": 0
      },
      {
        "user_id": 21,
        "display_name": "課長 鈴木",
        "sort_order": 1,
        "status": "STAMPED",
        "stamped_at": "2026-03-12T14:00:00",
        "seal_variant": "FULL_NAME",
        "tilt_angle": 8
      },
      {
        "user_id": 22,
        "display_name": "田中一郎",
        "sort_order": 2,
        "status": "PENDING",
        "stamped_at": null,
        "seal_variant": null,
        "tilt_angle": 0
      }
    ]
  }
}
```

---

#### `GET /api/v1/circulation/my`

**認可**: ログイン済みユーザー

自分宛の回覧一覧をカーソルベースで取得する。

**クエリパラメータ**
| パラメータ | 型 | 必須 | 説明 |
|-----------|---|------|------|
| `status` | String | No | `PENDING` / `STAMPED`（自分の押印状態でフィルタ） |
| `circulation_status` | String | No | `IN_PROGRESS` / `COMPLETED` / `CANCELLED`（回覧自体のステータスでフィルタ。デフォルト: 全件） |
| `cursor` | Long | No | カーソル |
| `limit` | Integer | No | 取得件数（デフォルト: 20） |

**レスポンス（200 OK）**: GET /circulation と同一構造 + `my_stamp_status` + `can_stamp` フィールド

**補足**
- 典型的な利用例: `status=PENDING&circulation_status=IN_PROGRESS` で「未確認かつ進行中の回覧」を取得（フロントエンドのバッジ数算出に使用）

---

#### `GET /api/v1/circulation/{id}/export`

**認可**: 作成者 / ADMIN

押印済み証跡付き PDF をエクスポートする。S3 に生成済み PDF が存在すれば CloudFront Pre-signed URL へリダイレクト、未生成の場合は非同期生成を開始して 202 を返す。

**レスポンス（302 Found）**: S3 に生成済み PDF が存在する場合
```
HTTP/1.1 302 Found
Location: https://cdn.example.com/circulation/1/export.pdf?X-Amz-...（Pre-signed URL、有効期限: 1時間）
```

**レスポンス（202 Accepted）**: PDF が未生成の場合（非同期生成を開始）
```json
{
  "data": {
    "status": "GENERATING",
    "poll_url": "/api/v1/circulation/1/export/status",
    "estimated_seconds": 10
  }
}
```

**PDF の内容**
- 表紙: タイトル、作成者、作成日、完了日
- 回覧本文（Markdown → HTML → PDF 変換）
- 押印一覧: 各メンバーの印鑑画像（SVG ラスタライズ）、押印日時、お辞儀角度の再現、スキップ者はスキップ理由を記載
- 添付ファイル一覧（ファイル名・サイズのリスト。添付本体は含まない）
- フッター: 検証用ハッシュ（全 stamp_log_id の SHA-256 連結ハッシュ）

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 403 | 権限不足 |
| 409 | status が COMPLETED でない |

**備考**: PDF 生成は OpenPDF ライブラリを使用。お辞儀スタイルの印鑑は `tilt_angle` に基づき SVG を回転してラスタライズ。生成済み PDF は S3 に保存し、同一 circulation_id の PDF が存在すれば再生成しない（302 リダイレクト）。DDoS 対策として PDF 生成リクエストは 1 回覧あたり 1 分間に 1 回のレートリミットを適用

---

#### `GET /api/v1/circulation/{id}/export/status`

**認可**: 作成者 / ADMIN

PDF 生成の進行状況を確認する。

**レスポンス（200 OK）**: 生成完了
```json
{
  "data": {
    "status": "READY",
    "url": "https://cdn.example.com/circulation/1/export.pdf?X-Amz-...",
    "generated_at": "2026-03-15T10:00:00"
  }
}
```

**レスポンス（200 OK）**: 生成中
```json
{
  "data": {
    "status": "GENERATING",
    "estimated_seconds": 5
  }
}
```

**レスポンス（200 OK）**: 生成失敗
```json
{
  "data": {
    "status": "FAILED",
    "error": "PDF generation failed. Please retry."
  }
}
```

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 403 | 権限不足 |
| 404 | エクスポートがリクエストされていない |

---

#### `POST /api/v1/circulation/{id}/attachments`

**認可**: 作成者 / ADMIN（DRAFT のみ）

DRAFT 状態の回覧に添付ファイルを追加する。アップロード前に[統合ストレージクォータ](../cross-cutting/storage_quota.md)のチェックを実行する。

**リクエスト（multipart/form-data）**
| フィールド | 型 | 必須 | 説明 |
|-----------|---|------|------|
| `file` | MultipartFile | Yes | 添付ファイル（20MB以下） |
| `sort_order` | Integer | No | 表示順（デフォルト: 末尾追加） |

**レスポンス（201 Created）**
```json
{
  "data": {
    "id": 2,
    "original_filename": "追加資料.pdf",
    "file_size": 524288,
    "mime_type": "application/pdf",
    "sort_order": 1,
    "url": "https://cdn.example.com/circulation/1/file2.pdf",
    "created_at": "2026-03-10T10:00:00"
  }
}
```

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 400 | 対応していないファイル形式 / 添付数が10を超過 |
| 403 | 権限不足 |
| 409 | status が DRAFT でない / ストレージ容量が不足（統合クォータ超過） |
| 413 | ファイルサイズ超過（単体 20MB / 合計 50MB） |

---

#### `DELETE /api/v1/circulation/{id}/attachments/{attachmentId}`

**認可**: 作成者 / ADMIN（DRAFT のみ）

DRAFT 状態の回覧から添付ファイルを削除する。

**レスポンス（204 No Content）**

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 403 | 権限不足 |
| 404 | 添付ファイルが存在しない |
| 409 | status が DRAFT でない |

**備考**: S3 上のファイルも削除。`circulation_documents.attachment_count` をアトミック更新（-1）

---

#### `GET /api/v1/circulation/{id}/comments`

**認可**: 回覧先メンバー / 作成者 / ADMIN / SYSTEM_ADMIN

回覧に対するコメント一覧を取得する。

**クエリパラメータ**
| パラメータ | 型 | 必須 | 説明 |
|-----------|---|------|------|
| `cursor` | Long | No | カーソル |
| `limit` | Integer | No | 取得件数（デフォルト: 50、最大: 100） |

**レスポンス（200 OK）**
```json
{
  "data": [
    {
      "id": 5,
      "user": {
        "id": 20,
        "display_name": "佐藤花子",
        "avatar_url": null
      },
      "body": "3階の消火器の交換手配をお願いします。",
      "created_at": "2026-03-11T10:35:00"
    }
  ],
  "meta": {
    "next_cursor": null,
    "limit": 50,
    "has_next": false
  }
}
```

**補足**
- 論理削除済みコメントは返却しない
- 全ステータスの回覧で閲覧可能（COMPLETED / CANCELLED でもコメント履歴は参照可）

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 403 | 閲覧権限がない |
| 404 | 回覧が存在しない |

---

#### `POST /api/v1/circulation/{id}/comments`

**認可**: 回覧先メンバー / 作成者 / ADMIN / SYSTEM_ADMIN

コメントを投稿する。

**リクエストボディ**
```json
{
  "body": "3階の消火器の交換手配をお願いします。"
}
```

**レスポンス（201 Created）**
```json
{
  "data": {
    "id": 5,
    "user": {
      "id": 20,
      "display_name": "佐藤花子",
      "avatar_url": null
    },
    "body": "3階の消火器の交換手配をお願いします。",
    "created_at": "2026-03-11T10:35:00"
  }
}
```

**補足**
- コメント投稿時の通知先: 回覧作成者 + 過去にコメント済みのユーザー（スレッド参加者パターン）。回覧先全員には送信しない（ノイズ回避）。SEQUENTIAL/HYBRID で未到達のユーザーにはコメント通知を送信しない

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 400 | body が空 / 1,000文字超過 |
| 403 | 閲覧権限がない |
| 409 | status が DRAFT / IN_PROGRESS でない（COMPLETED / CANCELLED への投稿は不可。証跡の完結性を保持するため） |

---

#### `DELETE /api/v1/circulation/{id}/comments/{commentId}`

**認可**: コメント投稿者本人 / ADMIN

コメントを論理削除する。編集機能は提供しない（証跡性のため）。

**レスポンス（204 No Content）**

**補足**
- 論理削除（`deleted_at = NOW()`）。`circulation_documents.comment_count` をアトミック更新（-1）
- 押印時に同時投稿されたコメントも削除可能（押印自体は取消されない）

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 403 | コメント投稿者でも ADMIN でもない |
| 404 | コメントが存在しない / 既に削除済み |

---

#### `POST /api/v1/circulation/{id}/duplicate`

**認可**: 作成者本人 / ADMIN / DEPUTY_ADMIN（`MANAGE_CONTENT`）

既存の回覧から設定をコピーして新規 DRAFT を作成する。全ステータスの回覧から複製可能。

**リクエストボディ**: なし

**レスポンス（201 Created）**: POST /circulation と同一構造（新規 DRAFT）

**補足**
- コピー対象: `title`（末尾に「(コピー)」を付与）、`body`、`circulation_mode`、`sequential_count`、`priority`、`stamp_display_style`、`reminder_enabled`、`reminder_interval_hours`、`recipient_ids`（元の回覧先メンバー全員。退会済みユーザーは除外）
- コピー対象外: `due_date`（新たに設定が必要）、添付ファイル（S3 の二重保持を避けるため）、コメント、押印データ
- Phase 8 のテンプレート機能までの繋ぎとして提供。月次点検・週次報告など同じ構成の回覧を繰り返し作成するケースに有効

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 403 | 権限不足 |
| 404 | 回覧が存在しない |

---

#### `POST /api/v1/circulation/{id}/stamp/delegate`

**認可**: ADMIN

不在のメンバーに代わって代理押印する。代理押印の事実は証跡として記録される。

**リクエストボディ**
```json
{
  "target_user_id": 20,
  "seal_id": 1,
  "tilt_angle": 8,
  "comment": "部長出張中のため代理確認"
}
```

| フィールド | 型 | 必須 | 説明 |
|-----------|---|------|------|
| `target_user_id` | Long | Yes | 代理押印対象のユーザー ID（回覧先メンバーで PENDING の人） |
| `seal_id` | Long | No | 使用する印鑑 ID。省略時は ADMIN 自身のデフォルト印鑑 |
| `tilt_angle` | Integer | No | 押印傾き角度（-30〜+30度） |
| `comment` | String | No | 代理押印時のコメント（最大1,000文字） |

**レスポンス（200 OK）**: POST /stamp と同一構造

**補足**
- `circulation_recipients.delegated_by` に ADMIN の user_id をセット
- `seal_stamp_logs` には ADMIN の user_id ではなく **対象ユーザーの user_id** で記録（証跡上は対象者の押印として扱う）。ただし `seal_stamp_logs` の `ip_address` / `user_agent` は ADMIN のものが記録され、`circulation_recipients.delegated_by` と合わせて代理の事実が追跡可能
- 押印状況画面: 「押印済み（代理: 管理者 田中）」と表示
- PDF エクスポート: 印鑑画像の下に「代理: {ADMIN名}」を小文字で記載
- SKIP との使い分け: SKIP = 確認不要と判断 / 代理押印 = 代わりに確認した（証跡の信頼性が高い）

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 400 | target_user_id が回覧先に含まれていない / seal_id が ADMIN の印鑑でない / tilt_angle 範囲外 |
| 403 | ADMIN でない |
| 404 | 回覧が存在しない |
| 409 | target_user_id が PENDING でない / status が IN_PROGRESS でない |

---

#### `POST /api/v1/circulation/batch/force-complete`

**認可**: ADMIN

複数の回覧を一括で強制完了する。期限超過の回覧を一括処理するケースで使用。

**リクエストボディ**
```json
{
  "circulation_ids": [1, 2, 3],
  "reason": "期限超過のため一括強制完了"
}
```

| フィールド | 型 | 必須 | 説明 |
|-----------|---|------|------|
| `circulation_ids` | Long[] | Yes | 強制完了する回覧 ID の配列（最大20件） |
| `reason` | String | No | 強制完了の理由（最大255文字）。全対象に共通適用 |

**レスポンス（200 OK）**
```json
{
  "data": {
    "completed_count": 3,
    "results": [
      { "circulation_id": 1, "status": "COMPLETED", "skipped_count": 2 },
      { "circulation_id": 2, "status": "COMPLETED", "skipped_count": 5 },
      { "circulation_id": 3, "status": "COMPLETED", "skipped_count": 1 }
    ],
    "errors": []
  }
}
```

**補足**
- 各回覧に対して個別の `force-complete` と同一ロジックを適用（`skip_reason` に `reason` を一括コピー、監査ログ記録、通知送信）
- 一部の回覧が条件不適合（IN_PROGRESS でない等）の場合はその回覧のみスキップし、他は正常に処理（部分成功を許容）。不適合の回覧は `errors` 配列に理由付きで返却
- 全対象が同一スコープに属していることを検証（スコープを跨ぐ一括操作は不可）

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 400 | circulation_ids が空 / 21件以上 / 異なるスコープの回覧が混在 |
| 403 | ADMIN でない |

---

## 5. ビジネスロジック

### 主要フロー

#### 既読トラッキング
```
1. ユーザーが GET /circulation/{id} で回覧詳細を取得
2. ユーザーが回覧先メンバーかつ read_at が NULL の場合:
   a. circulation_recipients.read_at = NOW() を更新（非同期。レスポンスをブロックしない）
   b. 2回目以降のアクセスでは更新しない（初回のみ）
3. 押印状況画面（GET /status）で3段階表示に活用:
   - 未読（read_at = NULL かつ PENDING）
   - 既読未押印（read_at IS NOT NULL かつ PENDING）
   - 押印済み（STAMPED）/ スキップ（SKIPPED）
```

#### 回覧の作成と開始
```
1. ADMIN / DEPUTY_ADMIN（または allow_member_circulation = true の MEMBER）が回覧作成画面で入力
2. POST /api/v1/circulation を呼び出し（DRAFT で保存）
3. バックエンドが以下を実行:
   a. スコープバリデーション（scope_type + scope_id + メンバーシップ確認）
   b. MEMBER の場合: チーム設定 allow_member_circulation を確認
   c. recipient_ids の全員がスコープメンバーであることを検証
   d. HYBRID の場合: sequential_count のバリデーション（1 ≤ N < recipient_ids.length）
   e. circulation_documents に INSERT（status = DRAFT）
   f. circulation_recipients に INSERT（recipient_ids の各ユーザー）
      - SEQUENTIAL: 配列の順序を sort_order に反映（0, 1, 2, ...）
      - HYBRID: 先頭 sequential_count 人は 0, 1, ..., N-1。残りは全員 sort_order = N
      - SIMULTANEOUS: 全員 sort_order = 0
   g. circulation_attachments に INSERT（添付がある場合）
4. 作成者が内容を確認後、POST /circulation/{id}/start を呼び出し
5. バックエンドが以下を実行:
   a. status = IN_PROGRESS に更新
   b. sort_order = 0 のユーザー全員に通知送信:
      - SEQUENTIAL: 1人のみ
      - HYBRID: 1人のみ（先頭の順番回覧者）
      - SIMULTANEOUS: 全員
   c. ApplicationEvent（CirculationStartedEvent）を発行
```

#### 押印（確認）フロー
```
1. メンバーが回覧詳細画面で「押印する」ボタンを押下
   - お辞儀スタイルの場合: 印鑑プレビューに傾き角度スライダーを表示（初期値は自動計算値）
2. POST /api/v1/circulation/{id}/stamp を呼び出し
3. バックエンドが以下を検証:
   a. 回覧が IN_PROGRESS であること
   b. 自分が回覧先メンバーであること
   c. 自分がまだ PENDING であること
   d. 自分の sort_order グループがアンロック済みであること:
      「自分の sort_order 未満の sort_order を持つレコードに PENDING が0件」
4. 以下を単一トランザクションで実行:
   a. circulation_documents の行ロックを取得（SELECT ... FOR UPDATE WHERE id = {id}）
      ※ 同一 sort_order グループの同時押印によるレースコンディション防止
        （完了判定・stamped_count 更新・次グループ通知の整合性を保証）
   b. SealStampService.stamp() を呼び出し:
      - seal_id を指定（省略時はスコープ別デフォルト印鑑）
      - target_type = 'CIRCULATION', target_id = circulation_id
      - seal_stamp_logs に INSERT → stamp_log_id を返却
   c. お辞儀角度の決定:
      - stamp_display_style = 'OJIGI' の場合:
        - tilt_angle がリクエストで指定されていればその値（-30〜+30 バリデーション）
        - 未指定の場合: min(sort_order × 8, 30)
      - stamp_display_style = 'STANDARD' の場合: 0
   d. circulation_recipients を更新（status = STAMPED, stamped_at = NOW(), stamp_log_id, tilt_angle）
   e. circulation_documents.stamped_count をアトミック更新（+1）
   f. comment がある場合: circulation_comments に INSERT + comment_count をアトミック更新（+1）
   g. 完了判定:
      - PENDING が0件 → status = COMPLETED, completed_at = NOW()
   h. 未完了の場合:
      - 次のアンロック対象グループ（現在の最小 PENDING sort_order）のユーザー全員に通知送信
5. ApplicationEvent（CirculationStampedEvent）を発行
   - 作成者に WebSocket 通知（CIRCULATION_STAMPED: 「佐藤花子が押印しました（3/8）」）
   - COMPLETED の場合: 作成者に WebSocket 通知（CIRCULATION_COMPLETED）
```

#### コメント通知フロー
```
1. ユーザーがコメントを投稿（POST /circulation/{id}/comments）
   - status が DRAFT / IN_PROGRESS でなければ 409 を返す
2. 通知対象を決定:
   a. 回覧作成者（常に通知）
   b. 過去にコメント済みのユーザー（circulation_comments で同一 circulation_id の DISTINCT user_id）
   c. 投稿者自身を除外
   d. SEQUENTIAL/HYBRID: 未到達ユーザー（sort_order グループがアンロックされていない）も
      閲覧は可能だがコメント通知は除外（ノイズ回避）
3. F04.3 プッシュ通知で送信:
   - 「[回覧板] {commenter_name} がコメントしました: {title}」
```

#### 自動リマインド通知（バッチ）
```
1. Spring @Scheduled バッチが1時間間隔で実行
2. circulation_documents から以下の条件で取得:
   - status = IN_PROGRESS
   - reminder_enabled = true
3. 各回覧について、現在アンロック済みのグループの PENDING メンバーを取得:
   - 最小の PENDING sort_order を持つグループのメンバー
4. 各未確認メンバーについて:
   a. last_reminded_at が NULL、または NOW() - last_reminded_at >= reminder_interval_hours の場合:
      ★ 冪等性保証: last_reminded_at = NOW() を先に更新してから通知送信
        （バッチ途中クラッシュ→再実行時の二重通知を防止。送信失敗分は次回バッチで再送）
      - 通知 priority の決定:
        - due_date 超過 かつ priority = NORMAL → 通知を HIGH に自動エスカレーション
        - それ以外 → circulation_documents.priority をそのまま使用
      - プッシュ通知（F04.3）を送信:
        - 未読者（read_at = NULL）: 「[回覧板] 未確認の回覧があります: {title}」
        - 既読未押印者（read_at IS NOT NULL）: 「[回覧板] 確認済みですが押印がまだです: {title}」
        - due_date 超過: 上記に「（期限: {due_date}）」を付与
5. まだアンロックされていないグループのメンバーにはリマインドを送信しない
   （前のグループの完了を待つ設計のため）
```

#### 強制完了フロー
```
1. ADMIN が POST /circulation/{id}/force-complete を呼び出し
2. バックエンドが以下を検証:
   a. status が IN_PROGRESS であること
   b. PENDING ユーザーが存在すること
3. 以下を単一トランザクションで実行:
   a. 全 PENDING ユーザーの status を SKIPPED に一括更新 + skip_reason に reason を一括コピー
      （個別に理由を変えたい場合は事前に PATCH /skip で個別スキップする運用）
   b. status = COMPLETED, completed_at = NOW() に更新
   c. 監査ログに CIRCULATION_FORCE_COMPLETED を記録（actor_id, circulation_id, skipped_user_ids, reason）
4. ApplicationEvent（CirculationForceCompletedEvent）を発行
   - 全回覧先メンバーにプッシュ通知（「回覧が強制完了されました: {title}」）
   - 作成者に WebSocket 通知（CIRCULATION_COMPLETED）
```

#### 回覧の取消
```
1. 作成者 / ADMIN が POST /circulation/{id}/cancel を呼び出し
2. バックエンドが以下を検証:
   a. status が DRAFT または IN_PROGRESS であること
3. status = CANCELLED に更新
4. ApplicationEvent（CirculationCancelledEvent）を発行
   - IN_PROGRESS から取消の場合: 全回覧先メンバーに通知（「回覧が取り消されました」）
5. 押印済みの seal_stamp_logs は取消しない（証跡保持。回覧自体の取消と押印の取消は独立）
```

#### 自動アーカイブ（バッチ）
```
1. Spring @Scheduled バッチが日次で実行（深夜帯）
2. circulation_documents から以下の条件で取得:
   - status = COMPLETED
   - completed_at < NOW() - INTERVAL 1 YEAR
3. status = ARCHIVED に一括更新
4. ARCHIVED の回覧はデフォルトの一覧 API から除外される:
   - GET /circulation: status フィルタに ARCHIVED を明示指定した場合のみ返却
   - GET /circulation/my: circulation_status=ARCHIVED で取得可能
5. ARCHIVED 後も GET /{id} での個別参照・PDF エクスポートは引き続き可能
6. データは一切削除しない（証跡保持）
```

#### S3 孤立ファイルクリーンアップ（バッチ）
```
1. Spring @Scheduled バッチが週次で実行（日曜深夜）
2. circulation_attachments に存在しない S3 キーを検出:
   - S3 の circulation/ プレフィックス配下を走査
   - circulation_attachments テーブルに file_key が存在しないファイルを抽出
3. 追加条件: ファイルの LastModified が30日以上前（直近のアップロード中ファイルを誤削除しない）
4. 対象ファイルを S3 から削除
5. 削除件数をアプリケーションログに記録
```

#### ダッシュボードウィジェット連携
```
1. 個人ダッシュボードに「回覧板」ウィジェットを表示（トグルで表示/非表示）
2. ウィジェット内容:
   a. 未確認回覧バッジ: GET /circulation/my?status=PENDING&circulation_status=IN_PROGRESS の
      件数を表示（例: 「未確認回覧: 3件」）
   b. 直近の回覧リスト: 上記の先頭3件をタイトル・期限・優先度で表示
   c. URGENT の回覧がある場合はバッジを強調表示（赤色）
3. クリックで /circulation/my?status=PENDING&circulation_status=IN_PROGRESS へ遷移
4. バッジ件数は WebSocket で CIRCULATION_STAMPED / CIRCULATION_STARTED イベント受信時に
   リアルタイム更新（ポーリング不要）
```

### 重要な判定ロジック

- **sort_order グループによる押印順序管理**: 同じ `sort_order` を持つメンバーは同時に押印可能。前のグループ（より小さい `sort_order`）に PENDING が0件になった時点で次のグループがアンロックされる。この統一ルールが3モード全てを実現:
  - SEQUENTIAL: 全員異なる sort_order → 1人ずつ順番
  - HYBRID: 先頭 N 人が異なる sort_order + 残り全員が同じ sort_order → 途中まで順番、以降一斉
  - SIMULTANEOUS: 全員 sort_order = 0 → 全員同時
- **HYBRID の sort_order 割当**: `recipient_ids = [A, B, C, D, E]` で `sequential_count = 2` の場合: A=0, B=1, C=2, D=2, E=2。A→B は順番、B の押印完了後に C/D/E が同時アンロック
- **完了判定**: `circulation_recipients` に `status = PENDING` のレコードが0件になった時点で完了。`stamped_count` ではなく `PENDING が0件` で判定する（SKIPPED を考慮）
- **閲覧と押印の分離**: SEQUENTIAL / HYBRID モードでも回覧先メンバー全員が回覧内容を閲覧可能。押印操作のみ sort_order による順番制約を適用。理由: 紙の回覧板でも「先に内容だけ確認しておく」ことは一般的であり、急ぎの周知事項が順番待ちでブロックされることを防ぐ
- **DRAFT の編集制限**: DRAFT の回覧のみ編集可能。IN_PROGRESS 以降は title / body / recipients の変更不可（押印済みの内容を事後変更すると証跡の信頼性が損なわれるため）
- **削除制限**: DRAFT と CANCELLED のみ論理削除可能。IN_PROGRESS / COMPLETED は削除不可（証跡保持）
- **押印の排他制御**: 押印トランザクション開始時に `circulation_documents` を `SELECT ... FOR UPDATE` で行ロック。同一 sort_order グループの同時押印による完了判定・stamped_count 更新・次グループ通知のレースコンディションを防止（F05.3 の `electronic_seals` ロックパターンと統一）
- **強制完了**: ADMIN が `force-complete` を実行すると、全 PENDING ユーザーを SKIPPED に変更（`skip_reason` に `reason` を一括コピー）し、`status = COMPLETED`、`completed_at = NOW()` に遷移。監査ログに記録。個別に理由を変えたい場合は事前に `PATCH /skip` で個別スキップ
- **ユーザー退会時の制約**: 回覧先メンバーが退会する場合、PENDING 状態の回覧があると退会をブロック。ADMIN が先に SKIPPED に変更するか回覧を完了/取消する必要がある
- **デフォルト印鑑の解決**: 押印時に `seal_id` が省略された場合: (1) `seal_scope_defaults` でスコープ別デフォルトを検索 → (2) なければグローバルデフォルト（`scope_type = DEFAULT`）→ (3) なければ `LAST_NAME` バリアントの印鑑
- **お辞儀ハンコの角度計算**: `stamp_display_style = 'OJIGI'` の場合、デフォルト傾き角度 = `min(sort_order × 8, 30)` 度。sort_order = 0（最上位者）は 0°（直立）、1 は 8°、2 は 16°、3 は 24°、≥ 4 は 30°（上限）。ユーザーはフロントエンドのスライダーで -30°〜+30° の範囲内で自由に調整可能
- **お辞儀の傾き方向**: 正値 = 左傾き（日本の慣習で、上位者の印鑑に向かってお辞儀する方向）。フロントエンドで `transform: rotate({tilt_angle}deg)` を適用。STANDARD スタイルの場合は常に 0°
- **リマインド通知の制御**: `reminder_enabled = false` の回覧は自動リマインド対象外。手動リマインド（POST /remind）は `reminder_enabled` の値に関わらず実行可能。手動リマインドは1回覧あたり1時間に1回のレートリミット。手動・自動ともにアンロック済みグループの PENDING メンバーのみが対象（SEQUENTIAL/HYBRID で未到達のグループには送信しない）
- **MEMBER の回覧作成権限**: チーム設定 `allow_member_circulation`（デフォルト: `false`）が `true` の場合のみ MEMBER が回覧を作成可能。組織レベルの回覧は ADMIN / DEPUTY_ADMIN のみ
- **コメント通知のスレッド参加者パターン**: コメント通知は回覧作成者 + 過去にコメント済みのユーザーに送信。回覧先全員には送信しない（ノイズ回避）
- **コメント投稿のステータス制限**: DRAFT / IN_PROGRESS の回覧のみコメント投稿可能。COMPLETED / CANCELLED への投稿は 409 で拒否（証跡の完結性を保持するため。完了後に内容が追記されると記録との齟齬が生じる）
- **期限超過回覧の管理**: 自動強制完了は実装しない。期限超過後もリマインド通知が継続し、ADMIN が `overdue=true` フィルタで期限超過回覧を一覧確認して手動で `force-complete` を判断する
- **スキップ理由の保存**: ADMIN がメンバーをスキップする際の `reason` は `circulation_recipients.skip_reason` に保存。PDF エクスポート時にスキップ者の欄に理由を出力し、証跡としての完全性を確保
- **作成者退会時の対応**: 作成者変更（引継ぎ）API は提供しない。証跡の作成責任者は事実として不変であるべきため。退会前に ADMIN が (1) IN_PROGRESS → `force-complete` / `cancel`、(2) DRAFT → `cancel` + 削除 の手順で未完結回覧を処理する
- **既読トラッキング**: `read_at` は GET /{id} 初回アクセス時に非同期で更新。レスポンス遅延を発生させない。押印状況画面で「未読 / 既読未押印 / 押印済み」を3段階表示し、リマインド文面の出し分けにも使用
- **代理押印と SKIP の使い分け**: SKIP = 確認が不要と判断された場合（長期不在・退職等）。代理押印 = 別の ADMIN が代わりに確認した場合。代理押印は `seal_stamp_logs` に証跡が残り、PDF 出力にも「代理: {ADMIN名}」として記録されるため、法的証跡の信頼性が SKIP より高い
- **優先度自動エスカレーション**: `due_date` 超過 かつ `priority = NORMAL` の回覧は、リマインドバッチで通知 priority を自動的に HIGH に引き上げる。DB の `priority` カラムは変更しない（通知の表示制御のみ）
- **自動アーカイブ**: `completed_at` から1年経過した COMPLETED 回覧は日次バッチで `ARCHIVED` に自動遷移。一覧 API のデフォルトフィルタから除外されるが、個別参照・PDF エクスポートは引き続き可能。データは削除しない
- **リマインドバッチの冪等性**: `last_reminded_at` を通知送信の前に更新し、送信失敗時は次回バッチで再送。二重送信を防止しつつ、通知漏れは最大1時間後に自然回復
- **ダッシュボードウィジェット**: 個人ダッシュボードに未確認回覧バッジ + 直近3件リストを表示。WebSocket で CIRCULATION_STAMPED / CIRCULATION_STARTED イベント受信時にリアルタイム更新

---

## 6. セキュリティ考慮事項

- **認可チェック**: 回覧作成は ADMIN / DEPUTY_ADMIN（`MANAGE_CONTENT`）+ チーム設定による MEMBER 許可。押印は回覧先メンバーのみ。回覧詳細の閲覧は回覧先メンバー + 作成者 + ADMIN に限定。スコープメンバーシップは全 API で検証
- **押印の不可逆性**: 押印操作は取消不可（`seal_stamp_logs` の `revoke` は F05.3 側の機能であり、回覧板では非推奨。押印取消が必要な場合は回覧自体を取消して再作成する運用）
- **DRAFT 編集の証跡**: IN_PROGRESS 以降の編集を禁止することで、「押印した時点の内容」と「現在の内容」の乖離を防止。回覧内容の事後改ざんリスクを排除
- **レートリミット**: 回覧作成 API は Bucket4j で 1分間に5回。押印 API は 1分間に10回。手動リマインドは 1回覧あたり1時間に1回。コメント投稿は 1分間に20回
- **添付ファイルのバリデーション**: ファイル形式は Content-Type + マジックバイトの二重チェック。PDF は悪意のある JavaScript 埋め込みチェック（PDF.js ライブラリで事前パース）
- **Pre-signed URL**: 添付ファイルは CloudFront 経由の署名付き URL で配信（有効期限: 1時間）
- **PDF エクスポートのセキュリティ**: 生成 PDF にはフッターに検証用ハッシュ（全 stamp_log_id の SHA-256 連結ハッシュ）を付与。改ざん検出に使用。PDF 生成は非同期ジョブで実行（202 Accepted → ポーリングで完了確認）し、DDoS リスクを軽減（1回覧あたり1分間に1回のレートリミット）。生成済み PDF は S3 にキャッシュし 302 リダイレクトで配信
- **お辞儀角度のバリデーション**: `tilt_angle` は -30〜+30 の範囲でバックエンド側で強制クランプ。範囲外の値は拒否（400 Bad Request）
- **監査ログ**: 強制完了（`CIRCULATION_FORCE_COMPLETED`）、取消（`CIRCULATION_CANCELLED`）、代理押印（`CIRCULATION_DELEGATED_STAMP`：actor_id, target_user_id, circulation_id）を `audit_logs` に記録。通常の押印は `seal_stamp_logs` が証跡を担うため `audit_logs` には記録しない
- **代理押印のセキュリティ**: 代理押印は ADMIN 限定。`seal_stamp_logs` には対象ユーザーの user_id で記録されるが、`ip_address` / `user_agent` は ADMIN のもの + `circulation_recipients.delegated_by` で代理の事実が完全に追跡可能。監査ログにも記録
- **レートリミット（追加）**: 回覧複製 API は 1分間に5回。一括強制完了 API は 1分間に3回。代理押印 API は 1分間に10回（通常押印と同一）

---

## 7. Flywayマイグレーション

```
V5.011__create_circulation_documents_table.sql          -- 回覧文書（HYBRID モード・stamp_display_style 含む）
V5.012__create_circulation_recipients_table.sql         -- 回覧先メンバー（tilt_angle 含む）
V5.013__create_circulation_attachments_table.sql        -- 添付ファイル
V5.014__create_circulation_comments_table.sql           -- コメント
```

**マイグレーション上の注意点**
- `circulation_documents` は `users` テーブルへの FK（`created_by`）を持つため、認証テーブルのマイグレーションが先行完了していること
- `circulation_recipients.stamp_log_id` および `correction_stamp_log_id` は `seal_stamp_logs` への FK だが、F05.3 のマイグレーション（V5.021〜）より先に実行されるため、V5.012 時点では FK 制約を設けない。V5.021 内で `ALTER TABLE circulation_recipients ADD CONSTRAINT` で両カラムの FK を追加する
- `circulation_recipients` / `circulation_attachments` / `circulation_comments` は `circulation_documents` への FK を持つため V5.011 の後に実行

**F05.3 への追加要請**
- `seal_stamp_logs` テーブルに `tilt_angle SMALLINT NOT NULL DEFAULT 0` カラムの追加を検討。回覧板以外の押印コンテキストでもお辞儀表示を将来サポートする場合に備える。Phase 5 初期実装では `circulation_recipients.tilt_angle` のみで対応し、F05.3 側の拡張は将来フェーズで検討

**F04.3 への追加要請**
- 以下の `notification_type` を F04.3 の通知タイプ一覧に追加:

| notification_type | priority | source_type | 送信タイミング |
|---|---|---|---|
| `CIRCULATION_STARTED` | NORMAL | CIRCULATION | 回覧開始時に sort_order=0 のメンバーへ |
| `CIRCULATION_STAMP_TURN` | NORMAL | CIRCULATION | 次の sort_order グループがアンロックされた時 |
| `CIRCULATION_COMPLETED` | NORMAL | CIRCULATION | 全員押印完了時に作成者へ |
| `CIRCULATION_CANCELLED` | HIGH | CIRCULATION | 回覧取消時に全回覧先メンバーへ |
| `CIRCULATION_REMIND` | NORMAL | CIRCULATION | 手動/自動リマインド送信時 |
| `CIRCULATION_FORCE_COMPLETED` | HIGH | CIRCULATION | 強制完了時に全回覧先メンバーへ |
| `CIRCULATION_COMMENT` | NORMAL | CIRCULATION | コメント投稿時にスレッド参加者へ |

---

## 8. 未解決事項

- [x] ~~回覧先にチーム全員を一括指定する機能~~ → **recipient_ids に全メンバーの ID を列挙する方式**。「チーム全員」ボタンはフロントエンドで `GET /teams/{id}/members` の結果を recipient_ids に自動セットする。DB に「全員」フラグは持たない（メンバー増減時の整合性問題を回避）
- [x] ~~回覧の転送（別チーム/組織への回覧共有）~~ → **Phase 5 では実装しない**。スコープを跨ぐ回覧は権限管理が複雑になるため、将来検討
- [x] ~~回覧テンプレート機能（定期回覧の雛形保存・再利用）~~ → **Phase 8 以降で検討**。実運用で定期回覧のパターン（月次点検・週次報告等）が定着した後に実装。既存テーブルへの影響なし（新規テーブル追加のみ）。FUTURE_CONSIDERATIONS.md に記載
- [x] ~~MEMBER の回覧作成権限制御~~ → **チーム設定 `allow_member_circulation`（デフォルト: false）** で制御。`true` の場合のみ MEMBER が回覧を作成可能。組織レベルは ADMIN / DEPUTY_ADMIN のみ
- [x] ~~コメント投稿時の通知先~~ → **作成者 + スレッド参加者（過去にコメント済みユーザー）** に通知。回覧先全員への通知はノイズが大きいため不採用。SEQUENTIAL/HYBRID の未到達ユーザーにはコメント通知を送信しない
- [x] ~~SEQUENTIAL モードでの閲覧範囲~~ → **全メンバーが回覧内容を閲覧可能。押印のみ順番制約**。紙の回覧板でも「先に内容を確認」は一般的。急ぎの周知事項が順番待ちでブロックされることを防止
- [x] ~~PDF エクスポート（押印済み証跡付き）~~ → **`GET /circulation/{id}/export`** で COMPLETED の回覧を PDF 出力。本文 + 押印一覧（印鑑画像・お辞儀角度再現）+ 検証用ハッシュ。OpenPDF ライブラリ使用
- [x] ~~回覧一覧のキーワード検索~~ → **`keyword` クエリパラメータ** を GET /circulation に追加。`title LIKE '%keyword%'` で検索。回覧数は少ないため LIKE で十分
- [x] ~~添付ファイルの個別追加・削除 API~~ → **`POST /circulation/{id}/attachments`** と **`DELETE /circulation/{id}/attachments/{attachmentId}`** を DRAFT 限定で追加
- [x] ~~期限超過の自動強制完了~~ → **自動完了は実装しない**。期限超過後もリマインド通知が継続。ADMIN 向けに `overdue=true` フィルタを提供し、期限超過回覧を一覧確認→手動で `force-complete` を判断
- [x] ~~PDF エクスポートの同期/非同期方式~~ → **非同期方式に統一**。初回リクエストは 202 Accepted + ポーリング URL を返却。生成完了後は S3 にキャッシュし 302 リダイレクト。DDoS 対策と UX を両立
- [x] ~~スキップ理由の保存先~~ → **`circulation_recipients.skip_reason VARCHAR(255)`** に保存。PDF エクスポート時にスキップ者の欄に理由を出力し証跡の完全性を確保
- [x] ~~COMPLETED / CANCELLED 後のコメント投稿可否~~ → **DRAFT / IN_PROGRESS のみ投稿可**。COMPLETED / CANCELLED は 409 で拒否。証跡の完結性を保持するため
- [x] ~~作成者退会時の引継ぎメカニズム~~ → **引継ぎ API は不要**。証跡の作成責任者は事実として不変。退会前に ADMIN が `force-complete` / `cancel` で未完結回覧を処理する運用手順を明記

---

## 9. 変更履歴

| 日付 | 変更内容 |
|------|---------|
| 2026-03-14 | 初版作成: 回覧板詳細設計。DB 4テーブル（circulation_documents / recipients / attachments / comments）、API 16本、ビジネスロジック4フロー（作成・押印・リマインドバッチ・取消）。F05.3 電子印鑑連携（seal_stamp_logs）、順番回覧/一斉回覧の2方式、自動リマインド通知、強制完了/スキップ機能を設計 |
| 2026-03-14 | 精査①: 回覧方式を3モードに拡張（SEQUENTIAL / HYBRID / SIMULTANEOUS）。sort_order グループ方式で統一管理。お辞儀ハンコ対応（stamp_display_style + tilt_angle）。MEMBER 作成権限のチーム設定制御（allow_member_circulation）。コメント通知のスレッド参加者パターン。SEQUENTIAL/HYBRID での閲覧自由化。PDF エクスポート API 追加。キーワード検索・期限超過フィルタ追加。添付ファイル個別操作 API 追加。テンプレート機能は Phase 8 延期。DB 4テーブル（カラム追加: sequential_count / stamp_display_style / tilt_angle）、API 19本 |
| 2026-03-15 | 精査②: 4件の設計ギャップを解決。(1) PDF エクスポートを非同期方式に統一（202 Accepted + ポーリング + S3 キャッシュ 302 リダイレクト、export/status エンドポイント追加）。(2) `circulation_recipients.skip_reason` カラム追加（スキップ理由の証跡保存 + PDF 出力対応）。(3) コメント投稿のステータス制限を明記（DRAFT / IN_PROGRESS のみ、COMPLETED / CANCELLED は 409）。(4) 作成者退会時の運用手順を明記（引継ぎ API は不要、`force-complete` / `cancel` で処理）。API 20本 |
| 2026-03-15 | 精査③: 16件の考慮漏れ・不整合・欠落を修正。(1) 欠落していた5 API仕様（PUT /{id}・DELETE /{id}・POST /cancel・GET /comments・DELETE /comments/{commentId}）を追加。(2) force-complete のエラーレスポンス・completed_at設定・通知・強制完了フローを補完。(3) file_size を INT → INT UNSIGNED に統一。(4) comment_count 非正規化カラムを追加（N+1防止）。(5) SYSTEM_ADMIN の認可条件を各APIに反映。(6) GET一覧のDRAFT表示ルールを明記。(7) remind APIの対象者をアンロック済みグループに修正（自動バッチと整合）。(8) ER図にcorrection_stamp_log_id関係を追加。(9) 押印の排他制御（SELECT FOR UPDATE）を明記。(10) F04.3への通知タイプ6種の追加要請。(11) force-complete時のskip_reason一括コピーを明記。API 20本（全エンドポイント仕様完備） |
| 2026-03-15 | 精査④: 11件の不整合・欠落を修正。(1) エンドポイント一覧の cancel / remind 説明を詳細仕様と整合。(2) PUT を application/json に変更（ファイルアップロードなし）。(3) correction_stamp_log_id の FK 追加をマイグレーション注意事項に明記。(4) GET /{id} レスポンスに comment_count 追加。(5) POST /comments レスポンスに avatar_url 追加。(6) SKIP 時の次グループアンロック通知・自動完了を明記。(7) start API に recipient >= 1 のバリデーション追加。(8) reminder_interval_hours のバリデーション範囲（1〜168）を追加。(9) recipient_ids の重複不可を明記。(10) 作成者自身を recipient_ids に含める許可を明記（全員押印ケース対応）。(11) GET /circulation/my に circulation_status フィルタを追加（取消済み除外・バッジ算出用） |
| 2026-03-15 | 機能拡張: 9件の改善を追加。【UX】(1) 既読トラッキング（`read_at` カラム追加。未読/既読未押印/押印済みの3段階表示）。(2) 回覧の複製 API（`POST /duplicate`。テンプレート機能 Phase 8 までの繋ぎ）。(3) 優先度自動エスカレーション（期限超過時に通知 priority を HIGH に引上げ）。(4) 一括強制完了 API（`POST /batch/force-complete`。最大20件一括処理）。(5) 代理押印 API（`POST /stamp/delegate`。SKIP との使い分けで証跡の信頼性向上。`delegated_by` カラム追加）。【保守】(6) 完了済み自動アーカイブ（ARCHIVED ステータス追加。日次バッチで1年経過分を自動移行）。(7) S3 孤立ファイルクリーンアップ（週次バッチ）。(8) リマインドバッチの冪等性保証（last_reminded_at を送信前に更新して二重通知防止）。【ダッシュボード】(9) 個人ダッシュボードに未確認回覧ウィジェット（バッジ + 直近3件リスト + WebSocket リアルタイム更新）。DB カラム追加: `read_at`・`delegated_by`。ステータス追加: `ARCHIVED`。API 23本。F04.3 通知タイプに `CIRCULATION_COMMENT` 追加要請 |
