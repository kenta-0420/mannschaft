# F04.2: チャット

> **ステータス**: 🟡 設計中
> **実装フェーズ**: Phase 4
> **最終更新**: 2026-03-14
> **関連ドキュメント**: F04.1 タイムライン、F04.3 プッシュ通知、F01.3 テンプレート・モジュール（デフォルト機能）

---

## 1. 概要

チーム・組織内のリアルタイムコミュニケーションを実現するチャット機能。Slack/Teams スタイルの階層化スレッド（特定メッセージへの返信）と絵文字リアクションを備え、情報の埋没を防止する。WebSocket (STOMP) + Redis メッセージブローカーでリアルタイム配信し、チーム内チャンネル・組織横断チャンネル・DM（1対1）をサポートする。メッセージの全文検索（MySQL FULLTEXT）、ファイル添付（Pre-signed URL）、メンション（@ユーザー名）に対応する。

---

## 2. スコープ

### 対象ロール
| ロール | 操作可能な範囲 |
|--------|--------------|
| SYSTEM_ADMIN | 全チャンネルの参照・管理（モデレーション）|
| ADMIN | チーム/組織内チャンネルの作成・管理・メッセージ削除。DM の利用 |
| DEPUTY_ADMIN | `MANAGE_CHAT` 権限を持つ場合: チャンネル管理・メッセージ削除 |
| MEMBER | チャンネルへの参加・メッセージ送受信・リアクション。DM の利用 |
| SUPPORTER | 参加済みチャンネルの閲覧のみ（投稿不可）|
| GUEST | 対象外 |

### 対象レベル
- [x] 組織 (Organization) — 組織横断チャンネル
- [x] チーム (Team) — チーム内チャンネル
- [x] 個人 (Personal) — DM（1対1）

---

## 3. DB設計

### テーブル一覧
| テーブル名 | 役割 | 論理削除 |
|-----------|------|---------|
| `chat_channels` | チャンネル/DM マスター | あり |
| `chat_messages` | メッセージ本体 | あり |
| `chat_channel_members` | チャンネルメンバーシップ・未読管理 | なし |
| `chat_message_reactions` | メッセージへの絵文字リアクション | なし |
| `chat_message_attachments` | メッセージ添付ファイル | なし |
| `chat_message_bookmarks` | メッセージの個人用ブックマーク（後で読む） | なし |

### テーブル定義

#### `chat_channels`

チャンネルまたは DM のマスター。1チャンネル = 1レコード。

| カラム名 | 型 | NULL | デフォルト | 説明 |
|---------|---|------|-----------|------|
| `id` | BIGINT UNSIGNED | NO | AUTO_INCREMENT | PK |
| `channel_type` | VARCHAR(20) | NO | — | チャンネル種別（TEAM / ORGANIZATION / CROSS_TEAM / DIRECT） |
| `team_id` | BIGINT UNSIGNED | YES | NULL | FK → teams。TEAM チャンネルの場合に設定 |
| `organization_id` | BIGINT UNSIGNED | YES | NULL | FK → organizations。ORGANIZATION チャンネルの場合に設定 |
| `name` | VARCHAR(100) | YES | NULL | チャンネル名。DM の場合は NULL |
| `icon_key` | VARCHAR(500) | YES | NULL | チャンネルアイコンの S3 オブジェクトキー。Pre-signed URL フローでアップロード |
| `description` | VARCHAR(500) | YES | NULL | チャンネルの説明 |
| `is_private` | BOOLEAN | NO | FALSE | TRUE = 招待制チャンネル。FALSE = スコープ内の全メンバーが参加可能 |
| `created_by` | BIGINT UNSIGNED | YES | NULL | FK → users。ON DELETE SET NULL |
| `last_message_at` | DATETIME | YES | NULL | 最新メッセージ日時（denormalize。チャンネル一覧のソート用） |
| `last_message_preview` | VARCHAR(100) | YES | NULL | 最新メッセージ冒頭100字（denormalize。チャンネル一覧のプレビュー用） |
| `is_archived` | BOOLEAN | NO | FALSE | アーカイブ済みフラグ。TRUE = 新規投稿不可・閲覧のみ |
| `version` | BIGINT | NO | 0 | 楽観的ロックバージョン（@Version） |
| `created_at` | DATETIME | NO | CURRENT_TIMESTAMP | |
| `updated_at` | DATETIME | NO | CURRENT_TIMESTAMP ON UPDATE | |
| `deleted_at` | DATETIME | YES | NULL | 論理削除日時 |

**インデックス**
```sql
INDEX idx_chat_channels_team (team_id, is_archived, last_message_at DESC)          -- チーム別チャンネル一覧（WHERE deleted_at IS NULL を Repository デフォルト条件に含む）
INDEX idx_chat_channels_org (organization_id, is_archived, last_message_at DESC)   -- 組織別チャンネル一覧
INDEX idx_chat_channels_type (channel_type, last_message_at DESC)                  -- 種別別の一覧
```

**制約・備考**
- `channel_type` のスコープ制約:
  - `TEAM`: `team_id` NOT NULL, `organization_id` NULL
  - `ORGANIZATION`: `team_id` NULL, `organization_id` NOT NULL
  - `CROSS_TEAM`: `team_id` NULL, `organization_id` NULL（ADMIN が作成し、メンバーを明示的に招待。同一プラットフォーム内の任意のユーザーを招待可能。招待は OWNER / チャンネル ADMIN のみ）
  - `DIRECT`: `team_id` NULL, `organization_id` NULL, `name` NULL（2人固定、メンバーテーブルで管理）
- DM は同一2人の組み合わせで1チャンネルのみ（Service 層で重複チェック）
- `last_message_at` / `last_message_preview`: メッセージ送信時にアトミック更新。チャンネル一覧 API で `chat_messages` への JOIN を排除。メッセージ削除時は直前の未削除メッセージから再計算する（`SELECT body FROM chat_messages WHERE channel_id = ? AND deleted_at IS NULL ORDER BY created_at DESC LIMIT 1`）。システムメッセージ（`is_system = TRUE`）はプレビューに反映しない（「田中太郎さんが参加しました」等は表示価値が低いため）
- `member_count` はレスポンス用に `chat_channel_members` の `COUNT` で算出（チャンネル数が限定的なため denormalize 不要。パフォーマンス課題が出た場合にカラム追加を検討）
- `is_archived`: ADMIN がチャンネルをアーカイブ可能。アーカイブ後も過去メッセージは閲覧可能

#### `chat_messages`

メッセージ本体。スレッド返信は `parent_id` で階層化。

| カラム名 | 型 | NULL | デフォルト | 説明 |
|---------|---|------|-----------|------|
| `id` | BIGINT UNSIGNED | NO | AUTO_INCREMENT | PK |
| `channel_id` | BIGINT UNSIGNED | NO | — | FK → chat_channels。ON DELETE CASCADE |
| `sender_id` | BIGINT UNSIGNED | YES | NULL | FK → users。ON DELETE SET NULL |
| `parent_id` | BIGINT UNSIGNED | YES | NULL | FK → chat_messages（自己参照）。NULL = トップレベルメッセージ、非 NULL = スレッド返信 |
| `body` | TEXT | NO | — | メッセージ本文（最大10,000文字） |
| `forwarded_from_id` | BIGINT UNSIGNED | YES | NULL | 転送元メッセージ ID（NULL = 通常メッセージ）。元メッセージへの参照リンクを表示 |
| `is_edited` | BOOLEAN | NO | FALSE | 編集済みフラグ |
| `is_system` | BOOLEAN | NO | FALSE | システムメッセージフラグ（メンバー参加・退出等） |
| `scheduled_at` | DATETIME | YES | NULL | 予約送信日時（NULL = 即時送信）。未来日時を指定すると送信待ちキューに入る |
| `reply_count` | INT UNSIGNED | NO | 0 | スレッド返信数（denormalize） |
| `reaction_count` | INT UNSIGNED | NO | 0 | リアクション総数（denormalize） |
| `is_pinned` | BOOLEAN | NO | FALSE | ピン留めフラグ。チャンネル OWNER / ADMIN / チーム ADMIN がピン留め可能 |
| `created_at` | DATETIME | NO | CURRENT_TIMESTAMP | |
| `updated_at` | DATETIME | NO | CURRENT_TIMESTAMP ON UPDATE | |
| `deleted_at` | DATETIME | YES | NULL | 論理削除日時。DB 側は `body` を保持したまま、API レスポンスでのみ `body = null`, `sender = null` として返却し「このメッセージは削除されました」と表示 |

**インデックス**
```sql
INDEX idx_chat_messages_channel (channel_id, created_at DESC)     -- チャンネル内のメッセージ一覧（カーソルページネーション）
INDEX idx_chat_messages_parent (parent_id, created_at ASC)        -- スレッド内の返信一覧
INDEX idx_chat_messages_sender (sender_id, created_at DESC)       -- ユーザー別の投稿検索
INDEX idx_chat_messages_pinned (channel_id, is_pinned, created_at DESC)  -- ピン留めメッセージ一覧
FULLTEXT INDEX ft_chat_messages_body (body)                       -- メッセージ全文検索
```

**制約・備考**
- `parent_id` は1階層のみ（スレッド返信への返信は不可。Slack と同じ仕様）。parent_id が非 NULL のメッセージに対する返信は、同じ parent_id を設定する
- `is_edited`: 本文を編集した場合に TRUE にセット。フロントエンドで「(編集済み)」ラベルを表示。**編集は送信後24時間以内のみ可能**（Service 層で `created_at + 24h > NOW()` を検証。超過時は 409 エラー）。過去の発言改ざんによるスレッド文脈の破壊を防止
- `is_system`: メンバー参加・退出・チャンネル名変更等のシステムメッセージ。`sender_id = NULL`
- `reply_count` / `reaction_count`: denormalize。スレッド返信追加/削除・リアクション追加/削除時にアトミック更新
- FULLTEXT: MySQL の `ngram` パーサーを使用（日本語対応）。`MATCH(body) AGAINST('+検索語' IN BOOLEAN MODE)`
- `is_pinned`: 1チャンネルあたり最大50件。超過した場合は 400 エラー（古いピンを解除してから追加）。チャンネル OWNER / ADMIN / チーム ADMIN のみピン留め可能
- `forwarded_from_id`: 転送メッセージの元 ID。FK 制約は付けない（元メッセージが削除されても転送メッセージは残す）。API レスポンスでは元メッセージの sender / body / channel 名をスナップショットとして返却
- `scheduled_at`: 予約送信。`status = 'SCHEDULED'` としてDB に INSERT し、バッチ（1分間隔）が `scheduled_at <= NOW()` のメッセージを通常送信フローで処理。送信前であれば編集・取り消し可能

#### `chat_channel_members`

チャンネルメンバーシップと未読管理。

| カラム名 | 型 | NULL | デフォルト | 説明 |
|---------|---|------|-----------|------|
| `id` | BIGINT UNSIGNED | NO | AUTO_INCREMENT | PK |
| `channel_id` | BIGINT UNSIGNED | NO | — | FK → chat_channels。ON DELETE CASCADE |
| `user_id` | BIGINT UNSIGNED | NO | — | FK → users。ON DELETE CASCADE |
| `role` | VARCHAR(20) | NO | 'MEMBER' | チャンネル内ロール（OWNER / ADMIN / MEMBER） |
| `unread_count` | INT UNSIGNED | NO | 0 | 未読メッセージ数（denormalize） |
| `last_read_at` | DATETIME | YES | NULL | 最終既読日時。この日時以降のメッセージが未読 |
| `is_muted` | BOOLEAN | NO | FALSE | ミュート設定。TRUE = このチャンネルの通知を抑制 |
| `is_pinned` | BOOLEAN | NO | FALSE | ピン留め。TRUE = チャンネル一覧で上位固定表示 |
| `category` | VARCHAR(50) | YES | NULL | 個人用カテゴリ名（例: 「プロジェクト」「雑談」）。サイドバーのセクション分けに使用 |
| `joined_at` | DATETIME | NO | CURRENT_TIMESTAMP | 参加日時 |

**インデックス**
```sql
UNIQUE KEY uq_chat_members_channel_user (channel_id, user_id)  -- 同一チャンネルへの重複参加防止
INDEX idx_chat_members_user (user_id, is_pinned DESC, last_read_at DESC)  -- ユーザーのチャンネル一覧
```

**制約・備考**
- `unread_count`: メッセージ送信時に送信者以外の全メンバーの `unread_count` を +1（バッチ UPDATE）。既読 API 呼び出し時に 0 リセット + `last_read_at` 更新
- `role`: `OWNER` = チャンネル作成者（1名のみ）。`ADMIN` = チャンネル管理権限（複数可）。`MEMBER` = 一般参加者
- `is_muted`: ミュート中もメッセージは受信するが、プッシュ通知は送信しない
- DM チャンネルの場合: 2レコード固定。退出不可（チャンネル削除で対応）

#### `chat_message_reactions`

メッセージへの絵文字リアクション。

| カラム名 | 型 | NULL | デフォルト | 説明 |
|---------|---|------|-----------|------|
| `id` | BIGINT UNSIGNED | NO | AUTO_INCREMENT | PK |
| `message_id` | BIGINT UNSIGNED | NO | — | FK → chat_messages。ON DELETE CASCADE |
| `user_id` | BIGINT UNSIGNED | NO | — | FK → users。ON DELETE CASCADE |
| `emoji` | VARCHAR(50) | NO | — | 絵文字（Unicode 絵文字 or カスタム絵文字コード） |
| `created_at` | DATETIME | NO | CURRENT_TIMESTAMP | |

**インデックス**
```sql
UNIQUE KEY uq_chat_reactions_user_emoji (message_id, user_id, emoji)  -- 同一ユーザーの同一絵文字重複防止
INDEX idx_chat_reactions_message (message_id)                          -- メッセージ別のリアクション一覧
```

**制約・備考**
- 1ユーザーが1メッセージに同一絵文字を複数回付与することは不可（UNIQUE 制約）
- 1ユーザーが1メッセージに異なる絵文字を複数付与することは可能（最大20種類、Service 層バリデーション）
- リアクション追加/削除時に `chat_messages.reaction_count` をアトミック更新

#### `chat_message_attachments`

メッセージの添付ファイル。Pre-signed URL フロー（§26）で S3 にアップロード後、メッセージ送信時に紐付ける。

| カラム名 | 型 | NULL | デフォルト | 説明 |
|---------|---|------|-----------|------|
| `id` | BIGINT UNSIGNED | NO | AUTO_INCREMENT | PK |
| `message_id` | BIGINT UNSIGNED | NO | — | FK → chat_messages。ON DELETE CASCADE |
| `file_key` | VARCHAR(500) | NO | — | S3 オブジェクトキー |
| `file_name` | VARCHAR(255) | NO | — | 元のファイル名 |
| `file_size` | BIGINT UNSIGNED | NO | — | ファイルサイズ（バイト） |
| `content_type` | VARCHAR(100) | NO | — | MIME タイプ |
| `created_at` | DATETIME | NO | CURRENT_TIMESTAMP | |

**インデックス**
```sql
INDEX idx_chat_attachments_message (message_id)  -- メッセージ別の添付一覧
```

**制約・備考**
- 1メッセージあたり最大10件の添付ファイル
- ファイルサイズ上限: 1ファイル10MB
- 許可する MIME タイプ: `image/jpeg`, `image/png`, `image/gif`, `image/webp`, `application/pdf`, `text/plain`, `application/zip`（チームの用途に応じて拡張可能）

#### `chat_message_bookmarks`

メッセージの個人用ブックマーク。ピン留め（チャンネル全体に公開）と異なり、個人が「後で読む」「参考にしたい」メッセージを保存する。

| カラム名 | 型 | NULL | デフォルト | 説明 |
|---------|---|------|-----------|------|
| `id` | BIGINT UNSIGNED | NO | AUTO_INCREMENT | PK |
| `message_id` | BIGINT UNSIGNED | NO | — | FK → chat_messages。ON DELETE CASCADE |
| `user_id` | BIGINT UNSIGNED | NO | — | FK → users。ON DELETE CASCADE |
| `note` | VARCHAR(200) | YES | NULL | ブックマークメモ（例: 「要対応」「参考資料」） |
| `created_at` | DATETIME | NO | CURRENT_TIMESTAMP | |

**インデックス**
```sql
UNIQUE KEY uq_chat_bookmarks_user_message (user_id, message_id)  -- 同一メッセージへの重複ブックマーク防止
INDEX idx_chat_bookmarks_user (user_id, created_at DESC)          -- ブックマーク一覧（新しい順）
```

**制約・備考**
- 他ユーザーには非公開（本人のみ参照可能）
- ブックマーク数の上限なし（ユーザーごとに自由に管理）
- 元メッセージが論理削除されても、ブックマークは保持（ただし API レスポンスではメッセージ本文を「削除されたメッセージ」と表示）

### ER図（テキスト形式）
```
teams (1) ──── (N) chat_channels（team_id; TEAM チャンネル）
organizations (1) ──── (N) chat_channels（organization_id; ORGANIZATION チャンネル）
chat_channels (1) ──── (N) chat_messages
chat_channels (1) ──── (N) chat_channel_members
chat_messages (1) ──── (N) chat_messages（parent_id; スレッド返信）
chat_messages (1) ──── (N) chat_message_reactions
chat_messages (1) ──── (N) chat_message_attachments

users (1) ──── (N) chat_channel_members（user_id）
users (1) ──── (N) chat_messages（sender_id）
users (1) ──── (N) chat_message_reactions（user_id）
users (1) ──── (N) chat_message_bookmarks（user_id）
chat_messages (1) ──── (N) chat_message_bookmarks
```

---

## 4. API設計

### エンドポイント一覧
| メソッド | パス | 認証 | 説明 |
|---------|-----|------|------|
| GET | `/api/v1/chat/channels` | 必要 | チャンネル一覧取得 |
| POST | `/api/v1/chat/channels` | 必要 | チャンネル作成 |
| GET | `/api/v1/chat/channels/{id}` | 必要 | チャンネル詳細取得 |
| PUT | `/api/v1/chat/channels/{id}` | 必要 | チャンネル更新 |
| DELETE | `/api/v1/chat/channels/{id}` | 必要 | チャンネル削除（論理削除）|
| PATCH | `/api/v1/chat/channels/{id}/archive` | 必要 | チャンネルアーカイブ/解除 |
| POST | `/api/v1/chat/channels/{id}/members` | 必要 | メンバー追加（招待）|
| DELETE | `/api/v1/chat/channels/{id}/members/{userId}` | 必要 | メンバー除外 / 退出 |
| POST | `/api/v1/chat/channels/{id}/join` | 必要 | パブリックチャンネルにセルフジョイン |
| PATCH | `/api/v1/chat/channels/{id}/members/{userId}/role` | 必要 | チャンネル内ロール変更（OWNER 移譲含む） |
| PATCH | `/api/v1/chat/channels/{id}/members/me` | 必要 | 自分のチャンネル設定更新（ミュート・ピン）|
| GET | `/api/v1/chat/channels/{id}/messages` | 必要 | メッセージ一覧取得（カーソルベース）|
| POST | `/api/v1/chat/channels/{id}/messages` | 必要 | メッセージ送信 |
| PUT | `/api/v1/chat/messages/{id}` | 必要 | メッセージ編集 |
| DELETE | `/api/v1/chat/messages/{id}` | 必要 | メッセージ削除（論理削除）|
| GET | `/api/v1/chat/messages/{id}/thread` | 必要 | スレッド取得 |
| POST | `/api/v1/chat/messages/{id}/reactions` | 必要 | リアクション追加 |
| DELETE | `/api/v1/chat/messages/{id}/reactions/{emoji}` | 必要 | リアクション削除 |
| POST | `/api/v1/chat/channels/{id}/read` | 必要 | 既読マーク |
| GET | `/api/v1/chat/channels/{id}/messages/search` | 必要 | メッセージ検索 |
| POST | `/api/v1/chat/channels/dm` | 必要 | DM チャンネル作成/取得 |
| PATCH | `/api/v1/chat/messages/{id}/pin` | 必要 | メッセージのピン留め/解除（トグル） |
| POST | `/api/v1/chat/channels/{id}/messages/upload-url` | 必要 | 添付ファイル用 Pre-signed URL 発行 |
| POST | `/api/v1/chat/channels/{id}/icon/upload-url` | 必要 | チャンネルアイコン用 Pre-signed URL 発行 |
| POST | `/api/v1/chat/messages/{id}/forward` | 必要 | メッセージを別チャンネルに転送 |
| POST | `/api/v1/chat/messages/{id}/bookmark` | 必要 | メッセージをブックマーク |
| DELETE | `/api/v1/chat/messages/{id}/bookmark` | 必要 | ブックマーク解除 |
| GET | `/api/v1/chat/bookmarks` | 必要 | 自分のブックマーク一覧取得 |
| POST | `/api/v1/chat/channels/{id}/convert-to-group` | 必要 | DM をグループチャンネルに拡張 |
| WS | `/ws/chat` | 必要 | WebSocket 接続（STOMP）|

### リクエスト／レスポンス仕様

#### `GET /api/v1/chat/channels`

ログインユーザーが参加しているチャンネル一覧を取得する。

**クエリパラメータ**
| パラメータ | 型 | 必須 | 説明 |
|-----------|---|------|------|
| `team_id` | Long | — | チーム ID でフィルタ |
| `organization_id` | Long | — | 組織 ID でフィルタ |
| `channel_type` | String | — | チャンネル種別でフィルタ（TEAM / ORGANIZATION / CROSS_TEAM / DIRECT） |
| `is_archived` | Boolean | — | アーカイブ状態でフィルタ（デフォルト: false） |
| `cursor` | String | — | ページネーションカーソル |
| `limit` | Integer | — | 取得件数（デフォルト: 30、最大: 50） |

**レスポンス（200 OK）**
```json
{
  "data": [
    {
      "id": 1,
      "channel_type": "TEAM",
      "team": { "id": 5, "name": "カフェ新宿店" },
      "name": "general",
      "description": "全体連絡用",
      "is_private": false,
      "is_archived": false,
      "last_message_at": "2026-03-14T10:30:00+09:00",
      "last_message_preview": "明日の朝礼は10時からです",
      "unread_count": 3,
      "is_muted": false,
      "is_pinned": true,
      "member_count": 12
    },
    {
      "id": 10,
      "channel_type": "DIRECT",
      "team": null,
      "name": null,
      "dm_partner": { "id": 11, "display_name": "佐藤花子" },
      "is_private": true,
      "is_archived": false,
      "last_message_at": "2026-03-14T09:00:00+09:00",
      "last_message_preview": "了解です！",
      "unread_count": 0,
      "is_muted": false,
      "is_pinned": false,
      "member_count": 2
    }
  ],
  "meta": {
    "next_cursor": "eyJpZCI6MTB9",
    "has_more": true
  }
}
```

- ソート順: ピン留め → `last_message_at` DESC
- `dm_partner`: DM チャンネルの場合のみ返却。相手ユーザーの情報
- `unread_count`: `chat_channel_members.unread_count` を返却

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 401 | 未認証 |

---

#### `POST /api/v1/chat/channels`

チャンネルを作成する。

**リクエストボディ**
```json
{
  "channel_type": "TEAM",
  "team_id": 5,
  "name": "プロジェクト打合せ",
  "description": "新メニュー開発の打合せ用",
  "is_private": true,
  "member_ids": [10, 11, 12]
}
```

- `channel_type`: 必須
- `team_id`: `TEAM` の場合は必須
- `organization_id`: `ORGANIZATION` の場合は必須
- `name`: `DIRECT` 以外は必須・1〜100文字
- `is_private`: 任意（デフォルト: false）
- `member_ids`: 任意。プライベートチャンネルの初期メンバー。作成者は自動追加（OWNER ロール）

**レスポンス（201 Created）**: チャンネル詳細を返却

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 400 | バリデーションエラー / channel_type とスコープ ID の不整合 |
| 403 | チーム/組織へのアクセス権限がない |

---

#### `GET /api/v1/chat/channels/{id}`

チャンネルの詳細を取得する。メンバー一覧とピン留めメッセージを含む。

**レスポンス（200 OK）**
```json
{
  "data": {
    "id": 1,
    "channel_type": "TEAM",
    "team": { "id": 5, "name": "カフェ新宿店" },
    "name": "general",
    "description": "全体連絡用",
    "is_private": false,
    "is_archived": false,
    "created_by": { "id": 10, "display_name": "田中太郎" },
    "members": [
      { "user": { "id": 10, "display_name": "田中太郎" }, "role": "OWNER", "joined_at": "2026-03-01T09:00:00+09:00" },
      { "user": { "id": 11, "display_name": "佐藤花子" }, "role": "MEMBER", "joined_at": "2026-03-01T09:00:00+09:00" }
    ],
    "member_count": 12,
    "pinned_messages": [
      {
        "id": 501,
        "sender": { "id": 10, "display_name": "田中太郎" },
        "body": "朝礼のルール変更について...",
        "created_at": "2026-03-10T09:00:00+09:00"
      }
    ],
    "version": 1,
    "created_at": "2026-03-01T09:00:00+09:00"
  }
}
```

- `members`: 最新参加順で最大30件を返却。30件超のチャンネルは `member_count` でフロントエンドが「他○名」と表示し、全メンバー一覧は別途 `GET /channels/{id}/members?cursor=&limit=` で取得（本ドキュメント API 一覧には含めず、実装時に追加）
- `pinned_messages`: ピン留めされたメッセージ（最新10件）。全件取得は `GET /channels/{id}/messages` に `is_pinned=true` フィルタで対応

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 403 | チャンネルに参加していない（プライベートチャンネルの場合） |
| 404 | チャンネルが存在しない |

---

#### `PUT /api/v1/chat/channels/{id}`

チャンネル名・説明を更新する。

**リクエストボディ**
```json
{
  "name": "【重要】プロジェクト打合せ",
  "description": "新メニュー開発の打合せ用（毎週月曜）",
  "icon_key": "uploads/chat/icons/abc123.webp",
  "version": 1
}
```

- `icon_key`: 任意。事前に `POST /channels/{id}/icon/upload-url` で取得した S3 キー。`null` を指定するとアイコンを削除

**レスポンス（200 OK）**: チャンネル詳細を返却

- チャンネル名が変更された場合: システムメッセージを自動投稿（「田中太郎さんがチャンネル名を「○○」から「△△」に変更しました」）
- WebSocket でチャンネル更新通知を配信

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 400 | バリデーションエラー |
| 403 | チャンネル OWNER / ADMIN / チーム ADMIN ではない |
| 404 | チャンネルが存在しない |
| 409 | 楽観的ロック競合 |

---

#### `DELETE /api/v1/chat/channels/{id}`

チャンネルを論理削除する。DM は削除不可（アーカイブで対応）。

**レスポンス（200 OK）**
```json
{
  "data": { "id": 1, "deleted_at": "2026-03-14T15:00:00+09:00" }
}
```

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 403 | チャンネル OWNER / チーム ADMIN ではない |
| 404 | チャンネルが存在しない |
| 409 | DM チャンネルは削除不可 |

---

#### `POST /api/v1/chat/channels/{id}/members`

チャンネルにメンバーを追加する。

**リクエストボディ**
```json
{
  "user_ids": [13, 14]
}
```

- 最大50人まで一括追加可能
- パブリックチャンネル: スコープ（チーム/組織）のメンバーであれば追加可能
- プライベートチャンネル: OWNER / ADMIN のみ追加可能

**レスポンス（200 OK）**: 追加されたメンバー一覧を返却

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 400 | 既に参加済みのユーザーを指定 |
| 403 | プライベートチャンネルで OWNER / ADMIN ではない |
| 404 | チャンネル/ユーザーが存在しない |

---

#### `DELETE /api/v1/chat/channels/{id}/members/{userId}`

チャンネルからメンバーを除外する、または自分が退出する。

- `userId` が自分の場合: **自発的退出**。チャンネル OWNER は退出不可（OWNER 権限の移譲が必要）
- `userId` が他人の場合: **管理者によるキック**。チャンネル OWNER / ADMIN / チーム ADMIN のみ実行可能
- DM チャンネルからの退出/キックは不可（409 エラー）
- 退出/キック時にシステムメッセージを自動投稿（「○○さんが退出しました」/「○○さんが除外されました」）

**レスポンス（204 No Content）**

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 403 | 他人をキックする権限がない（OWNER / ADMIN / チーム ADMIN ではない）|
| 404 | チャンネル/ユーザーが存在しない |
| 409 | DM チャンネルからの退出不可 / OWNER は退出不可（先に OWNER 権限を移譲すること）|

---

#### `PATCH /api/v1/chat/channels/{id}/members/{userId}/role`

チャンネルメンバーのロールを変更する。OWNER 移譲を含む。

**リクエストボディ**
```json
{
  "role": "OWNER"
}
```

- `role`: 必須。`OWNER` / `ADMIN` / `MEMBER` のいずれか
- **OWNER 移譲**: `role = "OWNER"` を指定すると、対象ユーザーが新 OWNER になり、現 OWNER は ADMIN に降格。OWNER のみ実行可能
- **ADMIN 昇格/降格**: OWNER のみ実行可能
- DM チャンネルではロール変更不可
- ロール変更時にシステムメッセージを自動投稿（「田中太郎さんが佐藤花子さんをチャンネル管理者に設定しました」）

**レスポンス（200 OK）**
```json
{
  "data": {
    "user": { "id": 11, "display_name": "佐藤花子" },
    "role": "OWNER"
  }
}
```

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 400 | 無効なロール値 |
| 403 | OWNER ではない（ロール変更権限なし）|
| 404 | チャンネル/ユーザーが存在しない |
| 409 | DM チャンネルではロール変更不可 |

---

#### `POST /api/v1/chat/channels/{id}/join`

パブリックチャンネルに自分自身が参加する。プライベートチャンネルには使用不可（招待が必要）。

**レスポンス（200 OK）**: チャンネル詳細を返却

- 参加時にシステムメッセージを自動投稿（「○○さんが参加しました」）

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 400 | 既に参加済み |
| 403 | プライベートチャンネル / スコープ外のチーム/組織 |
| 404 | チャンネルが存在しない |

---

#### `GET /api/v1/chat/channels/{id}/messages`

チャンネル内のメッセージを取得する。カーソルベースページネーション（最新メッセージから古い順）。

**クエリパラメータ**
| パラメータ | 型 | 必須 | 説明 |
|-----------|---|------|------|
| `cursor` | String | — | ページネーションカーソル（メッセージ ID）。指定なし = 最新から |
| `limit` | Integer | — | 取得件数（デフォルト: 30、最大: 100） |
| `direction` | String | — | `before`（デフォルト: カーソルより古いメッセージ）/ `after`（カーソルより新しいメッセージ） |
| `is_pinned` | Boolean | — | `true` を指定するとピン留めメッセージのみ返却 |

**レスポンス（200 OK）**
```json
{
  "data": [
    {
      "id": 501,
      "sender": { "id": 10, "display_name": "田中太郎" },
      "parent_id": null,
      "body": "明日の朝礼は10時からに変更です。よろしくお願いします。",
      "is_edited": false,
      "is_system": false,
      "reply_count": 2,
      "reactions": [
        { "emoji": "👍", "count": 3, "is_mine": true },
        { "emoji": "✅", "count": 1, "is_mine": false }
      ],
      "attachments": [
        {
          "id": 1,
          "file_name": "agenda.pdf",
          "file_size": 204800,
          "content_type": "application/pdf"
        }
      ],
      "created_at": "2026-03-14T10:30:00+09:00",
      "updated_at": "2026-03-14T10:30:00+09:00"
    }
  ],
  "meta": {
    "next_cursor": "500",
    "has_more": true
  }
}
```

- `reactions`: 絵文字ごとの集計 + `is_mine`（自分がリアクションしたか）
- 論理削除されたメッセージ: `body = null`, `sender = null`, `is_deleted = true` として返却
- `direction = after`: 未読メッセージのキャッチアップに使用。`last_read_at` 以降のメッセージを取得

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 403 | チャンネルに参加していない |
| 404 | チャンネルが存在しない |

---

#### `POST /api/v1/chat/channels/{id}/messages`

メッセージを送信する。WebSocket でリアルタイム配信も行う。

**リクエストボディ**
```json
{
  "body": "明日の朝礼は10時からに変更です。@佐藤花子 確認お願いします。",
  "parent_id": null,
  "attachment_keys": ["uploads/chat/5/abc123.pdf"]
}
```

- `body`: 必須・1〜10,000文字
- `parent_id`: 任意。スレッド返信の場合は親メッセージ ID。親メッセージは同一チャンネル内であること
- `also_to_channel`: 任意（デフォルト: false）。`parent_id` 指定時に `true` にすると、スレッド返信に加えてチャンネルのトップレベルにも同一メッセージを表示（Slack の「チャンネルにも投稿」機能）
- `scheduled_at`: 任意。未来日時を指定すると予約送信。指定日時に自動送信される。送信前は編集・取り消し可能
- `attachment_keys`: 任意。事前に `upload-url` API で取得した S3 キー。最大10件。S3 上に実在することを検証

**レスポンス（201 Created）**: メッセージ単体を返却

**副作用**:
1. `chat_channels.last_message_at` / `last_message_preview` をアトミック更新
2. 送信者以外の全チャンネルメンバーの `chat_channel_members.unread_count` を +1
3. WebSocket で全チャンネルメンバーにリアルタイム配信（STOMP destination: `/topic/chat.channel.{channelId}`）
4. メンション解析: `@ユーザー名` を検出し `mentions` テーブルに INSERT + メンション先ユーザーにプッシュ通知
5. ミュート中のメンバーにはプッシュ通知を送信しない（WebSocket 配信は行う）

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 400 | バリデーションエラー / parent_id が同一チャンネル内でない |
| 403 | チャンネルに参加していない / SUPPORTER は投稿不可 / アーカイブ済み |
| 404 | チャンネル/親メッセージが存在しない |

---

#### `PUT /api/v1/chat/messages/{id}`

自分のメッセージを編集する。

**リクエストボディ**
```json
{
  "body": "明日の朝礼は10時半からに変更です（訂正）"
}
```

- `is_edited` を TRUE に更新
- WebSocket で編集イベントを配信（フロントエンドが表示を更新）

**レスポンス（200 OK）**: メッセージ単体を返却

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 400 | バリデーションエラー |
| 403 | 投稿者本人ではない |
| 404 | メッセージが存在しない |
| 409 | 送信後24時間を超過（編集期限切れ） |

---

#### `DELETE /api/v1/chat/messages/{id}`

メッセージを論理削除する。投稿者本人または ADMIN が実行可能。

**レスポンス（200 OK）**
```json
{
  "data": { "id": 501, "deleted_at": "2026-03-14T15:00:00+09:00" }
}
```

- 論理削除後は「このメッセージは削除されました」と表示
- スレッドの親メッセージを削除しても、スレッド内の返信は保持（返信は独立して閲覧可能）
- WebSocket で削除イベントを配信

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 403 | 投稿者本人でも ADMIN でもない |
| 404 | メッセージが存在しない |

---

#### `GET /api/v1/chat/messages/{id}/thread`

スレッド内の返信一覧を取得する。

**クエリパラメータ**
| パラメータ | 型 | 必須 | 説明 |
|-----------|---|------|------|
| `cursor` | String | — | ページネーションカーソル |
| `limit` | Integer | — | 取得件数（デフォルト: 30、最大: 100） |

**レスポンス（200 OK）**
```json
{
  "data": {
    "parent": { /* 親メッセージ */ },
    "replies": [ /* メッセージ配列（古い順） */ ],
    "meta": { "next_cursor": "...", "has_more": false }
  }
}
```

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 403 | チャンネルに参加していない |
| 404 | メッセージが存在しない |

---

#### `POST /api/v1/chat/messages/{id}/reactions`

メッセージにリアクションを追加する。

**リクエストボディ**
```json
{
  "emoji": "👍"
}
```

- `emoji`: 必須。Unicode 絵文字（1〜10文字）
- 同一絵文字の重複は 409 エラー。異なる絵文字は追加可能（1メッセージ1ユーザーあたり最大20種類）

**レスポンス（201 Created）**

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 403 | チャンネルに参加していない / SUPPORTER は投稿・リアクション不可 |
| 404 | メッセージが存在しない |
| 409 | 同一絵文字で既にリアクション済み |

---

#### `DELETE /api/v1/chat/messages/{id}/reactions/{emoji}`

リアクションを取り消す。

**レスポンス（204 No Content）**

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 403 | チャンネルに参加していない |
| 404 | リアクションが存在しない |

---

#### `PATCH /api/v1/chat/messages/{id}/pin`

メッセージのピン留め/解除をトグルする。

**レスポンス（200 OK）**
```json
{
  "data": {
    "id": 501,
    "is_pinned": true
  }
}
```

- トグル方式: `is_pinned = !is_pinned`
- ピン留め時: 1チャンネルあたり50件上限チェック（超過時は 400 エラー）
- WebSocket で `MESSAGE_PINNED` / `MESSAGE_UNPINNED` イベントを配信

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 400 | ピン留め上限（50件）超過 |
| 403 | チャンネル OWNER / ADMIN / チーム ADMIN ではない |
| 404 | メッセージが存在しない |

---

#### `POST /api/v1/chat/channels/{id}/read`

チャンネルの既読をマークする。

**レスポンス（200 OK）**
```json
{
  "data": {
    "channel_id": 1,
    "unread_count": 0,
    "last_read_at": "2026-03-14T10:35:00+09:00"
  }
}
```

- `unread_count` を 0 にリセット
- `last_read_at` を現在日時に更新

---

#### `GET /api/v1/chat/channels/{id}/messages/search`

チャンネル内のメッセージを全文検索する。

**クエリパラメータ**
| パラメータ | 型 | 必須 | 説明 |
|-----------|---|------|------|
| `q` | String | 必須 | 検索クエリ（`MATCH() AGAINST()` 構文で検索） |
| `sender_id` | Long | — | 送信者でフィルタ |
| `from` | String | — | 日時範囲の開始 |
| `to` | String | — | 日時範囲の終了 |
| `cursor` | String | — | ページネーションカーソル |
| `limit` | Integer | — | 取得件数（デフォルト: 20、最大: 50） |

**レスポンス（200 OK）**: メッセージ一覧（検索結果）。`GET /messages` と同一形式。`deleted_at IS NULL` のメッセージのみ対象（削除済みメッセージは検索結果に含めない）

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 400 | 検索クエリが空 |
| 403 | チャンネルに参加していない |
| 404 | チャンネルが存在しない |

---

#### `POST /api/v1/chat/channels/dm`

DM チャンネルを作成する。既に同一相手との DM が存在する場合は既存チャンネルを返却する。

**リクエストボディ**
```json
{
  "partner_id": 11
}
```

- `partner_id`: 必須。DM の相手ユーザー ID

**レスポンス（200 OK / 201 Created）**: チャンネル詳細を返却。200 = 既存、201 = 新規作成

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 400 | 自分自身への DM |
| 403 | 相手ユーザーにブロックされている（`user_blocks` 参照） |
| 404 | 相手ユーザーが存在しない |

---

#### `PATCH /api/v1/chat/channels/{id}/archive`

チャンネルをアーカイブまたはアーカイブ解除する。

**リクエストボディ**
```json
{
  "is_archived": true,
  "version": 1
}
```

- `version`: 必須。楽観的ロック

**レスポンス（200 OK）**
```json
{
  "data": {
    "id": 1,
    "is_archived": true,
    "version": 2
  }
}
```

- アーカイブ時: 全メンバーに WebSocket でチャンネル更新通知を配信

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 403 | チャンネル OWNER / ADMIN / チーム ADMIN ではない |
| 404 | チャンネルが存在しない |
| 409 | DM チャンネルはアーカイブ不可 / 楽観的ロック競合 |

---

#### `PATCH /api/v1/chat/channels/{id}/members/me`

自分のチャンネル設定（ミュート・ピン留め）を更新する。

**リクエストボディ**
```json
{
  "is_muted": true,
  "is_pinned": false,
  "category": "プロジェクト"
}
```

- 各フィールドは任意。指定したフィールドのみ更新（PATCH セマンティクス）
- `category`: 個人用カテゴリ名（最大50文字）。サイドバーのセクション分けに使用。`null` を指定するとカテゴリ解除。フロントエンドはカテゴリ名でグルーピングして表示

**レスポンス（200 OK）**
```json
{
  "data": {
    "channel_id": 1,
    "is_muted": true,
    "is_pinned": false
  }
}
```

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 403 | チャンネルに参加していない |
| 404 | チャンネルが存在しない |

---

#### `POST /api/v1/chat/channels/{id}/messages/upload-url`

添付ファイルアップロード用の Pre-signed URL を発行する（§26 準拠）。

**リクエストボディ**
```json
{
  "file_name": "agenda.pdf",
  "content_type": "application/pdf",
  "file_size": 204800
}
```

- `content_type`: ホワイトリスト検証（`image/jpeg`, `image/png`, `image/gif`, `image/webp`, `application/pdf`, `text/plain`, `application/zip`）
- `file_size`: 最大 10MB（10,485,760 バイト）

**レスポンス（200 OK）**
```json
{
  "data": {
    "upload_url": "https://s3...",
    "file_key": "uploads/chat/5/abc123.pdf",
    "expires_in": 600
  }
}
```

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 400 | content_type が許可されていない / file_size が上限超過 |
| 403 | チャンネルに参加していない / SUPPORTER は投稿不可 / アーカイブ済み |
| 404 | チャンネルが存在しない |

---

#### `POST /api/v1/chat/messages/{id}/forward`

メッセージを別チャンネルに転送する。元メッセージの送信者・本文・送信日時をスナップショットとして含む。

**リクエストボディ**
```json
{
  "target_channel_id": 2,
  "comment": "この情報、共有します"
}
```

- `target_channel_id`: 必須。転送先チャンネル ID。自分が参加しているチャンネルであること
- `comment`: 任意。転送時に添えるコメント（最大1,000文字）

**レスポンス（201 Created）**: 転送先に作成された新メッセージを返却

- 新メッセージの `forwarded_from_id` に元メッセージ ID を設定
- フロントエンド: 元メッセージの引用ブロック（送信者名・本文・元チャンネル名・日時）+ コメントを表示

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 403 | 元メッセージのチャンネル or 転送先チャンネルに参加していない |
| 404 | メッセージ/転送先チャンネルが存在しない |

---

#### `POST /api/v1/chat/messages/{id}/bookmark`

メッセージをブックマーク（後で読む）に追加する。個人用で他ユーザーには非公開。

**リクエストボディ**
```json
{
  "note": "要対応"
}
```

- `note`: 任意・最大200文字

**レスポンス（201 Created）**

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 404 | メッセージが存在しない |
| 409 | 既にブックマーク済み |

---

#### `DELETE /api/v1/chat/messages/{id}/bookmark`

ブックマークを解除する。

**レスポンス（204 No Content）**

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 404 | ブックマークが存在しない |

---

#### `GET /api/v1/chat/bookmarks`

自分のブックマーク一覧を取得する。

**クエリパラメータ**
| パラメータ | 型 | 必須 | 説明 |
|-----------|---|------|------|
| `cursor` | String | — | ページネーションカーソル |
| `limit` | Integer | — | 取得件数（デフォルト: 20、最大: 50） |

**レスポンス（200 OK）**
```json
{
  "data": [
    {
      "id": 1,
      "note": "要対応",
      "message": {
        "id": 501,
        "body": "来週の会議資料を...",
        "sender": { "id": 10, "display_name": "田中太郎" },
        "channel": { "id": 1, "name": "general" },
        "created_at": "2026-03-14T10:30:00+09:00"
      },
      "created_at": "2026-03-14T11:00:00+09:00"
    }
  ],
  "meta": { "next_cursor": "...", "has_more": false }
}
```

---

#### `POST /api/v1/chat/channels/{id}/convert-to-group`

DM チャンネルをグループチャンネルに拡張する。新しいメンバーを追加しつつ、既存の DM を残すか統合するかを選択できる。

**リクエストボディ**
```json
{
  "additional_user_ids": [13, 14],
  "name": "プロジェクトチーム",
  "keep_original": true,
  "share_history": false
}
```

- `additional_user_ids`: 必須。追加するメンバーの user_id（1名以上）
- `name`: 必須。新しいグループチャンネル名（1〜100文字）
- `keep_original`: 必須。
  - `true`: 元の DM は残したまま、新しいグループチャンネルを作成
  - `false`: 元の DM をアーカイブし、新しいグループチャンネルに統合
- `share_history`: 必須。
  - `true`: 元の DM のメッセージ履歴を新メンバーにも閲覧可能にする（新チャンネルの `chat_channel_members.joined_at` を DM の最初のメッセージ日時に設定し、`last_read_at` より前のメッセージも表示）
  - `false`: 新メンバーには新チャンネル作成後のメッセージのみ表示（`joined_at` 以降のメッセージのみ閲覧可能）

**レスポンス（201 Created）**: 新しいグループチャンネルの詳細を返却

**処理フロー**:
1. 新しい CROSS_TEAM チャンネルを作成（`channel_type = 'CROSS_TEAM'`, `is_private = TRUE`）
2. 元の DM メンバー + 追加メンバーを `chat_channel_members` に INSERT
3. `keep_original = false` の場合: 元の DM チャンネルを `is_archived = TRUE` に更新
4. `share_history = true` の場合: 元 DM のメッセージを新チャンネルにコピー INSERT（`forwarded_from_id` は設定せず、元メッセージとして扱う）。大量メッセージのコピーは非同期ジョブで実行し、完了後に通知
5. システムメッセージ: 「○○さんが DM をグループに拡張しました。△△さん、□□さんが参加しました」

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 400 | DM チャンネル以外に対して実行 / additional_user_ids が空 |
| 403 | DM のメンバーではない |
| 404 | チャンネル/ユーザーが存在しない |

---

### WebSocket 仕様

#### 接続

```
WS /ws/chat
Authorization: Bearer <JWT>
```

- STOMP プロトコルで接続
- `HandshakeInterceptor` で JWT を検証
- 接続成功時に Redis にセッション登録（`mannschaft:ws:session:{sessionId}`）
- 切断時に Redis からセッション削除
- **SUBSCRIBE 認可**: `/topic/chat.channel.{channelId}` への SUBSCRIBE 時に `ChannelInterceptor` でチャンネルメンバーシップを検証。非メンバーの SUBSCRIBE はエラーフレームで拒否（有効な JWT を持っていても他人のチャンネルを盗聴できないようにする）

#### STOMP Destination

| Destination | 説明 |
|------------|------|
| `/topic/chat.channel.{channelId}` | チャンネルのメッセージ配信（SUBSCRIBE） |
| `/topic/chat.user.{userId}` | ユーザー個別通知（未読数更新、チャンネル更新等）|
| `/app/chat.send` | メッセージ送信（SEND。REST API と同一のバリデーション・副作用を実行。フォーマット: `{ "channel_id": 1, "body": "...", "parent_id": null }`。添付ファイルは REST API のみ対応） |
| `/app/chat.typing` | タイピングインジケーター送信 |

#### メッセージフレーム

**新規メッセージ（`/topic/chat.channel.{channelId}`）**
```json
{
  "type": "MESSAGE_CREATED",
  "data": { /* メッセージオブジェクト */ }
}
```

**メッセージ編集**
```json
{
  "type": "MESSAGE_UPDATED",
  "data": { "id": 501, "body": "編集後の内容", "is_edited": true }
}
```

**メッセージ削除**
```json
{
  "type": "MESSAGE_DELETED",
  "data": { "id": 501, "deleted_at": "2026-03-14T15:00:00+09:00" }
}
```

**リアクション更新**
```json
{
  "type": "REACTION_UPDATED",
  "data": { "message_id": 501, "reactions": [ /* 更新後の集計 */ ] }
}
```

**タイピングインジケーター（`/app/chat.typing`）**
```json
{
  "channel_id": 1,
  "user": { "id": 10, "display_name": "田中太郎" }
}
```

- クライアント → `/app/chat.typing` に SEND → サーバーが `/topic/chat.channel.{channelId}` に `TYPING` フレームとしてブロードキャスト（DB 保存なし。Redis Pub/Sub で全インスタンスに伝播）
- フロントエンドは `input` イベントをデバウンス（2秒間隔）で送信
- 受信側は3秒間「○○さんが入力中…」を表示（3秒間入力がなければ消える）
- 自分のタイピングは表示しない

---

## 5. ビジネスロジック

### メッセージ送信フロー

```
1. REST API（POST /channels/{id}/messages）またはWebSocket（/app/chat.send）でメッセージ受信
2. バリデーション:
   a. チャンネル存在確認 + メンバーシップ確認
   b. チャンネルがアーカイブ済みでないこと
   c. SUPPORTER は投稿不可
   d. メッセージ本文のサニタイズ（XSS 防止）
3. chat_messages INSERT
4. 添付ファイルの紐付け: attachment_keys の S3 存在確認 → chat_message_attachments INSERT
5. denormalize 更新（1トランザクション内）:
   a. chat_channels.last_message_at / last_message_preview を更新
   b. chat_channel_members.unread_count を送信者以外で +1（バッチ UPDATE）
   c. parent_id が指定されている場合: 親メッセージの reply_count を +1
6. メンション解析:
   a. body 内の @ユーザー名 を正規表現で抽出
   b. mentions テーブルに INSERT（target_type = 'CHAT_MESSAGE', target_id = message.id）
   c. メンション先ユーザーにプッシュ通知（ApplicationEvent: ChatMentionEvent）
   d. 特殊メンション（ADMIN / チャンネル OWNER のみ使用可能。一般 MEMBER が使用した場合は通常テキストとして扱う）:
      - @channel: チャンネル全メンバーにプッシュ通知（ミュート設定も無視）
      - @here: 現在 WebSocket 接続中（オンライン）のメンバーのみにプッシュ通知。
        オンライン判定: Redis の `mannschaft:ws:session:*` を SCAN し、接続中の userId を収集
7. 通知フィルタリング:
   a. notification_preferences テーブルを参照し、該当チーム/組織の通知を OFF にしているユーザーにはプッシュ通知を送信しない（WebSocket 配信は行う。通知設定はプッシュ通知のみに影響）
   b. is_muted = TRUE のメンバーにはプッシュ通知を送信しない（@channel を除く）
   c. user_blocks: ブロック中のユーザーからのメンション通知は抑制
8. WebSocket 配信:
   a. /topic/chat.channel.{channelId} に MESSAGE_CREATED フレームを送信
   b. Redis Pub/Sub で全アプリインスタンスに伝播（マルチインスタンス対応）
9. プッシュ通知:
   a. WebSocket 未接続のメンバーにプッシュ通知（ApplicationEvent: ChatMessageEvent）
   b. 通知フィルタリング（ステップ7）の結果に基づき送信先を決定
```

### 未読管理

```
1. メッセージ送信時:
   a. 送信者以外の全メンバーの unread_count を +1（バッチ UPDATE）
   b. バッチ UPDATE: UPDATE chat_channel_members SET unread_count = unread_count + 1
      WHERE channel_id = {id} AND user_id != {senderId}

2. 既読マーク（POST /channels/{id}/read）:
   a. unread_count を 0 にリセット
   b. last_read_at を現在日時に更新
   c. WebSocket で /topic/chat.user.{userId} にカウントリセットを通知（ヘッダーバッジ更新用）

3. フロントエンド:
   a. チャンネルを開いた時点で自動的に既読マーク API を呼び出す
   b. バックグラウンドのチャンネルは未読のまま保持
   c. ブラウザの Visibility API でフォーカスを検知し、フォーカス復帰時に既読処理
```

### パブリックチャンネルへの自動参加

```
1. TEAM チャンネル（is_private = FALSE）:
   a. チームに新メンバーが参加した際に自動的に全パブリックチャンネルへ追加
     （ApplicationEvent: TeamMemberJoinedEvent を購読）
   b. システムメッセージ:「田中太郎さんが参加しました」を自動投稿

2. ORGANIZATION チャンネル（is_private = FALSE）:
   a. 組織に新チームが参加した際に、そのチームの ADMIN を自動的に全パブリック組織チャンネルへ追加

3. プライベートチャンネル: 自動参加なし。OWNER / ADMIN が明示的に招待
```

### チャンネルアーカイブ

```
1. ADMIN がチャンネルをアーカイブ（PATCH /channels/{id}/archive）
2. is_archived = TRUE に更新
3. アーカイブ後:
   a. 新規メッセージの投稿不可（403 エラー）
   b. 既存メッセージの閲覧は可能
   c. リアクションの追加・削除は不可
   d. チャンネル一覧ではデフォルト非表示（is_archived=true でフィルタすると表示）
4. アーカイブ解除: 同じ API で is_archived = FALSE に戻す
```

### WebSocket リコネクション戦略

```
1. フロントエンド側の自動再接続（指数バックオフ）:
   a. 初回1秒 → 2秒 → 4秒 → 8秒 → 最大30秒の間隔で再接続を試行
   b. 5回連続失敗後: UI に「接続が切れています。再接続を試みています…」を表示
   c. ブラウザの Visibility API と連携:
      - タブがバックグラウンド: リコネクション間隔を60秒に延長（サーバーリソース節約）
      - フォアグラウンド復帰: 即座にリコネクト

2. 再接続成功時のメッセージキャッチアップ:
   a. 各チャンネルの最後に受信した messageId を IndexedDB に保持
   b. 再接続後: GET /channels/{id}/messages?cursor={lastMessageId}&direction=after で
      切断中のメッセージを REST API で取得
   c. WebSocket が受信するリアルタイムメッセージと REST 応答の重複を messageId で排除

3. JWT 期限切れ時の処理:
   a. WebSocket 接続で 401 を受信 → リフレッシュトークンで JWT を再取得
   b. リフレッシュ成功 → 新しい JWT で再接続
   c. リフレッシュ失敗 → ログイン画面に遷移

4. バックエンド側:
   a. JWT 期限切れ検知時に WebSocket セッションを DISCONNECT フレームで切断
   b. Redis の WebSocket セッション（mannschaft:ws:session:{sessionId}）にも TTL を設定し、
      異常切断時にゴミセッションが残らないようにする（TTL: JWT の有効期限と同一）
```

### メッセージのピン留め

```
1. PATCH /chat/messages/{id}/pin でトグル（is_pinned = !is_pinned）
2. ピン留め時のバリデーション:
   a. チャンネル OWNER / ADMIN / チーム ADMIN であること
   b. 1チャンネルあたりのピン留め上限（50件）チェック:
      SELECT COUNT(*) FROM chat_messages WHERE channel_id = {channelId} AND is_pinned = TRUE
      50件以上の場合は 400 エラー（「古いピンを解除してから追加してください」）
3. WebSocket で MESSAGE_PINNED / MESSAGE_UNPINNED イベントを配信
4. フロントエンド:
   a. チャンネルヘッダーにピン留めアイコン + 件数バッジを表示
   b. クリックでピン留めメッセージ一覧をサイドパネル表示
   c. 各メッセージのコンテキストメニューに「ピン留め」オプション（権限保持者のみ）
```

### 予約送信バッチ

```
1. @Scheduled で1分間隔で実行
2. 条件: scheduled_at <= NOW() AND deleted_at IS NULL
3. 対象メッセージを通常送信フローで処理（denormalize更新・WebSocket配信・通知等の全副作用を実行）
4. 送信完了後: scheduled_at カラムは保持（送信済みの識別用。is_system = FALSE で通常メッセージとして表示）
5. フロントエンド:
   a. 予約送信メッセージは送信前に「予約: 3/15 09:00 に送信」ラベルを表示
   b. 送信前のメッセージは本人のみ閲覧可能。編集（PUT /messages/{id}）・取り消し（DELETE）可能
   c. 送信後は通常メッセージとして表示（予約だったことは表示しない）
```

### 未読位置マーカー（フロントエンド）

```
1. チャンネルを開いた際に last_read_at を基準に「ここから未読です」のラインを描画
2. 実装:
   a. GET /channels/{id}/messages で取得したメッセージ配列を走査
   b. created_at > last_read_at の最初のメッセージの直前に区切り線を表示
   c. 区切り線には「N件の新着メッセージ」のラベルを付与
3. 既読マーク（POST /read）送信後も、区切り線はページ遷移まで保持（再表示時は消える）
```

### WebSocket / REST の統一アーキテクチャ

```
1. メッセージ送信は REST API（POST /channels/{id}/messages）と
   WebSocket（/app/chat.send）の2経路を提供するが、ビジネスロジックは1箇所に集約:
   a. ChatMessageService.sendMessage() を共通の Service メソッドとして実装
   b. REST Controller はリクエストを DTO に変換して Service を呼び出す
   c. WebSocket の @MessageMapping ハンドラも同一の Service を呼び出す
   d. バリデーション・denormalize 更新・メンション解析・通知は全て Service 内で実行
2. 利点: ロジックの二重実装を防止し、一方だけバグが入るリスクを排除
3. WebSocket 送信は REST のフォールバック（WebSocket 接続が不安定な場合に REST にフォールバック）
```

### 大規模チャンネルの未読カウント最適化

```
1. 現行方式: メッセージ送信時に chat_channel_members の全メンバーを UPDATE（unread_count + 1）
   - 小〜中規模チャンネル（〜100名）: 問題なし
   - 大規模チャンネル（500名超）: 1メッセージあたり499行の UPDATE が発生しレイテンシに影響

2. 最適化方式（メンバー200名超のチャンネルに自動適用）:
   a. Redis INCR で未読カウントを管理（key: mannschaft:chat:unread:{channelId}:{userId}）
   b. DB の unread_count は非同期バッチ（5分間隔）で Redis の値と同期
   c. GET /channels 一覧取得時は Redis を優先参照（Redis に値がなければ DB にフォールバック）
   d. 既読マーク時: Redis DEL + DB unread_count = 0 を同一処理で実行

3. 閾値（200名）は application.yml で設定可能:
   mannschaft.chat.unread-redis-threshold: 200
```

### 重要な判定ロジック

- **チャンネル作成権限**: TEAM チャンネルはチームの MEMBER 以上。ORGANIZATION / CROSS_TEAM チャンネルは ADMIN のみ
- **メッセージ削除権限**: 投稿者本人 or チーム/組織 ADMIN or チャンネル OWNER/ADMIN
- **DM の重複防止**: `POST /channels/dm` で既存 DM を検索。`chat_channel_members` で `channel_type = 'DIRECT'` かつ `user_id IN (自分, partner_id)` のチャンネルを検索。存在すれば既存を返却
- **スレッドの階層制限**: `parent_id` を指定する場合、その親メッセージの `parent_id` が NULL であること（2階層目まで）。parent_id が非 NULL のメッセージへの返信は、同一の parent_id を使用する
- **チャンネルメンバーシップの可視性**: パブリックチャンネルは非メンバーでも存在を確認可能（参加を促す UI 用）。プライベートチャンネルは非メンバーには完全非公開
- **SUPPORTER の制約**: チャンネルには参加可能だが投稿・リアクション不可（閲覧のみ）
- **unread_count とメッセージ削除**: メッセージ削除時に `unread_count` の減算は行わない。既読マーク時に 0 リセットされるため実害は小さく、「そのメッセージを未読だったメンバー」の特定が複雑なため費用対効果に見合わない
- **メッセージ編集期限**: 送信後24時間以内のみ。ADMIN によるモデレーション削除は時間制限なし
- **DM のブロック**: プラットフォーム共通の `user_blocks` テーブルで管理。ブロック中のユーザーからの DM 作成は 403 で拒否（詳細: db_design_details.md「ユーザーブロック」参照）
- **OWNER 移譲**: OWNER のみ実行可能。移譲後は元 OWNER が ADMIN に降格。OWNER を移譲してから退出する運用
- **ピン留め権限**: チャンネル OWNER / ADMIN / チーム ADMIN のみ。一般 MEMBER は不可
- **ピン留め上限**: 1チャンネルあたり50件。超過時は 400 エラー

---

## 6. セキュリティ考慮事項

- **認可チェック**: 全 API エンドポイントでチャンネルメンバーシップを検証。プライベートチャンネルは非メンバーのアクセスを完全遮断
- **WebSocket 認証・認可**: 接続ハンドシェイク時に JWT を検証。無効なトークンでの接続を拒否。トークン期限切れ時は WebSocket セッションを切断。SUBSCRIBE 時に `ChannelInterceptor` でチャンネルメンバーシップを検証し、非メンバーの盗聴を防止
- **メッセージのサニタイズ**: XSS 防止のためメッセージ本文の HTML エスケープを徹底。リンクは自動検出してクリック可能にするが、`javascript:` スキーム等は除去
- **IDOR 防止**: `PUT /chat/messages/{id}` は投稿者本人チェック必須。`DELETE /chat/channels/{id}/members/{userId}` は本人退出 or チャンネル OWNER/ADMIN のアクション
- **監査ログ**: 以下のイベントを監査ログに記録
  - `CHAT_CHANNEL_DELETED`: チャンネル削除
  - `CHAT_MESSAGE_DELETED_BY_ADMIN`: ADMIN によるメッセージ削除（投稿者本人以外の削除）
  - `CHAT_MEMBER_REMOVED`: メンバーの強制退出
- **レートリミット**: `Bucket4j` で以下の制限を適用
  - メッセージ送信 API（`POST /messages`）: 1分間60件
  - リアクション追加 API（`POST /reactions`）: 1分間30件
  - WebSocket メッセージ送信: 1秒間5件
- **添付ファイル**: アップロード前に MIME タイプをホワイトリスト検証。ファイルサイズ上限 10MB。Pre-signed URL の有効期限 10分

---

## 7. Flywayマイグレーション

```
V4.001__add_manage_chat_permission.sql           -- MANAGE_CHAT パーミッション追加
V4.002__create_chat_channels.sql                 -- チャンネルテーブル作成
V4.003__create_chat_messages.sql                 -- メッセージテーブル作成（FULLTEXT インデックス含む）
V4.004__create_chat_channel_members.sql          -- チャンネルメンバーシップテーブル作成
V4.005__create_chat_message_reactions.sql         -- リアクションテーブル作成
V4.006__create_chat_message_attachments.sql       -- 添付ファイルテーブル作成
V4.007__create_chat_message_bookmarks.sql         -- ブックマークテーブル作成
```

**マイグレーション上の注意点**
- V4.001: `MANAGE_CHAT` を `permissions` テーブルに INSERT し、`role_permissions` に SYSTEM_ADMIN・ADMIN 用のシードを追加。DEPUTY_ADMIN は権限グループで明示設定。scope = TEAM
- `chat_channels.team_id` / `organization_id` は Phase 1 で作成済みのテーブルへの FK
- V4.003: `FULLTEXT INDEX ft_chat_messages_body (body)` は `WITH PARSER ngram` を付与（日本語全文検索対応）
- V4.004: `chat_channel_members.channel_id` は `chat_channels.id` への FK（ON DELETE CASCADE）→ V4.002 が先に実行されること
- V4.005: `chat_message_reactions.message_id` は `chat_messages.id` への FK（ON DELETE CASCADE）→ V4.003 が先に実行されること
- Phase 4 の他機能（F04.1 タイムライン等）が V4.001 以前を使用済みの場合は採番を調整すること

---

## 8. 未解決事項

- [x] ~~メッセージの保持期間ポリシー~~ → **3ティア方式を採用**。ホットティア（12ヶ月、MySQL）→ コールドティア（S3 圧縮アーカイブ、ストレージ枠消費）→ 削除。詳細は FUTURE_CONSIDERATIONS.md に記載。Phase 4 開始前に確定
- [x] ~~WebSocket 切断時のリコネクション戦略~~ → **指数バックオフ自動再接続（1s→2s→4s→8s→最大30s）+ REST API キャッチアップ**。再接続成功時に `GET /messages?cursor={lastMessageId}&direction=after` で切断中のメッセージを取得。バックグラウンドタブはリコネクション間隔60秒に延長。JWT 期限切れ時はリフレッシュトークンで再取得→再接続
- [x] ~~メッセージの暗号化~~ → **E2E 暗号化は導入しない**。E2E だと全文検索・ADMIN モデレーション・新メンバーの過去メッセージ閲覧が不可能になる。TLS（通信経路暗号化）+ InnoDB Tablespace Encryption（保存時暗号化）で実用的なセキュリティレベルを確保
- [x] ~~ファイルプレビュー~~ → **Phase 4 はフロントエンドでの遅延読み込み + CSS リサイズ表示**（`<img loading="lazy">` + `max-width: 400px; max-height: 300px`）。サーバーサイドサムネイル生成は導入しない。将来画像投稿量が増えた段階で S3 + Lambda パイプラインを検討（FUTURE_CONSIDERATIONS.md に記載）
- [x] ~~メッセージのピン留め~~ → **`chat_messages` に `is_pinned BOOLEAN` カラムを追加**。別テーブル方式より UPDATE 1発で済みシンプル。1チャンネルあたり最大50件。`PATCH /chat/messages/{id}/pin` でトグル。チャンネル OWNER / ADMIN / チーム ADMIN のみ操作可能

---

## 9. 変更履歴

| 日付 | 変更内容 |
|------|---------|
| 2026-03-14 | 初版作成: 5テーブル（chat_channels, chat_messages, chat_channel_members, chat_message_reactions, chat_message_attachments）、21 API エンドポイント + WebSocket、4種チャンネル（TEAM/ORGANIZATION/CROSS_TEAM/DIRECT）、階層化スレッド、絵文字リアクション、全文検索、DM、タイピングインジケーター、未読管理、チャンネルアーカイブ、メンション連携、Pre-signed URL 添付ファイル |
| 2026-03-14 | 機能拡張10件: ①ブックマーク（chat_message_bookmarks テーブル + 3 API）②メッセージ転送（forwarded_from_id カラム + POST /forward API）③チャンネルカテゴリ（chat_channel_members.category + PATCH /members/me 拡張）④スレッド「チャンネルにも投稿」（also_to_channel フラグ）⑤予約送信（scheduled_at カラム + 1分間隔バッチ）⑥未読位置マーカー（フロントエンド設計）⑦WS/REST統一アーキテクチャ（Service メソッド共通化）⑧大規模チャンネル unread Redis 最適化（200名超閾値）⑨チャンネルアイコン（icon_key カラム + upload-url API）⑩DM→グループ拡張（POST /convert-to-group。keep_original/share_history オプション）。Flyway V4.007 追加、テーブル計6個 |
| 2026-03-14 | 精査③+: PATCH /members/{userId}/role API 追加（OWNER 移譲 + ADMIN 昇格/降格）。OWNER 移譲の判定ロジック追加 |
| 2026-03-14 | 精査③: DELETE /members/{userId} 詳細仕様追加（自発退出 vs 管理者キックの区別・OWNER退出不可・DM退出不可）・POST /join セルフジョイン API 追加・POST /dm にブロック 403 追加・GET /channels/{id} の members 配列30件上限注記・検索の deleted_at IS NULL 明記・/app/chat.send リクエストフォーマット定義・@here の Redis オンライン判定方法明記・notification_preferences 連携追加・user_blocks の通知抑制追加・PUT /channels のチャンネル名変更時システムメッセージ追加・メッセージ送信フローを9ステップに再構成 |
| 2026-03-14 | 精査②: メッセージ編集24時間制限追加（409エラー）・unread_count メッセージ削除時の補正なし方針明記・@channel/@here 特殊メンション追加（ADMIN/OWNER限定）・DM ブロック機能の共通設計参照追加・メッセージ編集期限の判定ロジック追加 |
| 2026-03-14 | 精査①: 欠落API仕様3件追加（PATCH /archive・PATCH /members/me・POST /upload-url）・WebSocket SUBSCRIBE 認可チェック追加（ChannelInterceptor でメンバーシップ検証）・deleted_at 時の body 保持方針明確化・last_message_preview の削除時再計算ロジック追加・システムメッセージのプレビュー除外・SUPPORTER リアクション制限の403追加・CROSS_TEAM チャンネル招待ルール明記・member_count 算出方式記載・PATCH /archive に楽観的ロック追加・タイピングインジケーターの配信フロー詳細化・インデックスの deleted_at 条件注記 |
| 2026-03-14 | 未解決事項4件解決: WebSocket 指数バックオフリコネクション戦略確定・E2E暗号化は導入しない（TLS+InnoDB暗号化で代替）・ファイルプレビューはフロントエンドCSS リサイズ（サムネイル生成はFUTURE_CONSIDERATIONS.md へ）・ピン留めは chat_messages.is_pinned カラム方式（PATCH /pin トグルAPI + 50件上限）。ピン留め用インデックス追加、チャンネル詳細に pinned_messages 追加、GET /messages に is_pinned フィルタ追加 |
