# F09.4: LINE/SNS 連携

> **ステータス**: 🟢 設計完了
> **実装フェーズ**: Phase 9
> **最終更新**: 2026-03-15
> **関連ドキュメント**: F04.3 プッシュ通知（NotificationChannel 連携）、F08 外部連携（OAuth・トークン管理パターン）、F01.1 認証（ソーシャルログイン）

---

## 1. 概要

LINE Messaging API と Instagram フィードを統合し、組織・チームの外部コミュニケーションと情報発信を強化する機能。

**LINE 連携**: LINE 公式アカウント（BOT）をチーム/組織に紐付け、プラットフォームの通知（スケジュール変更・出欠リマインド・回覧板・掲示板等）を LINE 経由で配信する。ユーザーは自分の LINE アカウントをリンクすることで、プッシュ通知の代わりに LINE メッセージで受信できる。LINE Webhook による双方向メッセージングも対応し、簡易な返信（出欠回答等）を LINE 上で完結させる。

**SNS 連携**: Instagram のフィードをチームページに埋め込み表示する。DB にデータを持たず Redis キャッシュのみで運用し、外部 API の変更に対する保守負荷を最小化する。

> **注**: X（旧 Twitter）フィード連携は API 利用料（Basic プラン月$100〜）の観点から Phase 9 では実装しない。将来的に収益に余裕ができた段階で追加する（FUTURE_CONSIDERATIONS.md に記載）。`sns_feed_configs` テーブルの `provider` カラムは `X` を将来追加可能な設計としている。

---

## 2. スコープ

### 対象ロール
| ロール | LINE 連携 | SNS 連携 |
|--------|----------|---------|
| SYSTEM_ADMIN | 全組織・チームの LINE/SNS 設定参照 | 同左 |
| ADMIN | LINE BOT 設定・Webhook 管理・配信履歴参照 | Instagram フィード設定（API キー・アカウント連携） |
| DEPUTY_ADMIN | `MANAGE_CONTENT` 権限: 配信履歴参照 | フィード表示設定 |
| MEMBER | 自分の LINE アカウントリンク・通知チャンネル設定 | Instagram フィード閲覧 |
| SUPPORTER | 自分の LINE アカウントリンク・通知受信 | Instagram フィード閲覧 |
| GUEST | 対象外 | Instagram フィード閲覧（公開設定の場合） |

### 対象レベル
- [x] 組織 (Organization) — LINE BOT 設定・SNS フィード
- [x] チーム (Team) — LINE BOT 設定・SNS フィード
- [x] 個人 (Personal) — LINE アカウントリンク・通知チャンネル選択

---

## 3. DB設計

### テーブル一覧
| テーブル名 | 役割 | 論理削除 |
|-----------|------|---------|
| `line_bot_configs` | LINE BOT 設定（チーム/組織別） | あり |
| `user_line_connections` | ユーザーと LINE アカウントの紐付け | なし |
| `line_message_logs` | LINE 送信メッセージの履歴（監査・デバッグ用） | なし |
| `sns_feed_configs` | SNS フィード設定（チーム/組織別。Instagram / X） | あり |

> **注**: SNS フィードデータ自体は DB に保存しない（Redis キャッシュのみ）

### テーブル定義

#### `line_bot_configs`

チーム/組織レベルの LINE 公式アカウント（BOT）設定。

| カラム名 | 型 | NULL | デフォルト | 説明 |
|---------|---|------|-----------|------|
| `id` | BIGINT UNSIGNED | NO | AUTO_INCREMENT | PK |
| `scope_type` | VARCHAR(20) | NO | — | `TEAM` / `ORGANIZATION` |
| `scope_id` | BIGINT UNSIGNED | NO | — | teams.id / organizations.id |
| `channel_id` | VARCHAR(100) | NO | — | LINE Messaging API のチャンネル ID |
| `channel_secret_enc` | VARBINARY(512) | NO | — | チャンネルシークレット（AES-256-GCM 暗号化） |
| `channel_access_token_enc` | VARBINARY(1024) | NO | — | チャンネルアクセストークン（AES-256-GCM 暗号化） |
| `encryption_key_version` | INT UNSIGNED | NO | 1 | 暗号化キーのバージョン（ローテーション対応） |
| `webhook_secret` | VARCHAR(64) | NO | — | Webhook 検証用シークレット（URL-safe ランダム文字列） |
| `bot_user_id` | VARCHAR(50) | YES | NULL | LINE BOT のユーザー ID（初回 Webhook 受信時に自動設定） |
| `is_active` | BOOLEAN | NO | TRUE | BOT の有効/無効 |
| `notification_enabled` | BOOLEAN | NO | TRUE | 通知配信の有効/無効（一時停止用） |
| `configured_by` | BIGINT UNSIGNED | NO | — | FK → users（設定した ADMIN） |
| `created_at` | DATETIME | NO | CURRENT_TIMESTAMP | |
| `updated_at` | DATETIME | NO | CURRENT_TIMESTAMP ON UPDATE | |
| `deleted_at` | DATETIME | YES | NULL | 論理削除 |

**インデックス**
```sql
INDEX idx_lbc_scope (scope_type, scope_id)                     -- スコープ別検索（UNIQUE はアプリ層で保証。deleted_at を含む UNIQUE KEY は MySQL の NULL 挙動で機能しないため）
INDEX idx_lbc_channel (channel_id)                            -- チャンネル ID で検索（Webhook 受信時）
```

**制約・備考**
- `channel_secret_enc` / `channel_access_token_enc`: AES-256-GCM で暗号化。暗号化キーは環境変数 or AWS Secrets Manager で管理（F08 Google Calendar と同一パターン）
- `encryption_key_version`: キーローテーション時にインクリメント。旧バージョンのキーも保持し、復号時にバージョンに応じたキーを使用
- `webhook_secret`: Webhook URL に含めるシークレット。`/api/v1/line/webhook/{webhook_secret}` の形式で外部からのリクエストを検証
- 1スコープにつき1つの LINE BOT のみ設定可能。DB の UNIQUE 制約ではなくアプリ層で `deleted_at IS NULL` 条件付きのバリデーションで保証（MySQL は UNIQUE KEY に NULL を含むと重複を許容するため）
- `is_active`: BOT 全体の有効/無効。`false` の場合は Webhook イベントも無視し、通知も送信しない
- `notification_enabled`: 通知配信のみの一時停止。`false` でも Webhook は受信・処理する（follow/unfollow/accountLink は動作）。メンテナンス時や配信数調整時に使用
- `configured_by` ON DELETE RESTRICT: 設定した ADMIN が退会する場合、先に別の ADMIN が設定を引き継ぐか削除する必要がある

#### `user_line_connections`

ユーザーと LINE アカウントの紐付け。LINE のアカウント連携フロー（リンクトークン方式）で確立。

| カラム名 | 型 | NULL | デフォルト | 説明 |
|---------|---|------|-----------|------|
| `id` | BIGINT UNSIGNED | NO | AUTO_INCREMENT | PK |
| `user_id` | BIGINT UNSIGNED | NO | — | FK → users（ON DELETE CASCADE） |
| `line_user_id` | VARCHAR(50) | NO | — | LINE のユーザー ID（U で始まる33文字） |
| `scope_type` | VARCHAR(20) | NO | — | どのスコープの BOT と連携したか |
| `scope_id` | BIGINT UNSIGNED | NO | — | teams.id / organizations.id |
| `display_name` | VARCHAR(100) | YES | NULL | LINE 上の表示名（プロフィール取得時に更新） |
| `line_login_sub` | VARCHAR(50) | YES | NULL | LINE ログイン（OpenID Connect）の sub 値。LINE ログイン済みユーザーの自動連携に使用 |
| `quiet_start` | TIME | NO | '22:00' | おやすみ時間の開始（この時間以降の通知はキューに保留） |
| `quiet_end` | TIME | NO | '07:00' | おやすみ時間の終了（この時間にキュー内の通知をダイジェスト送信） |
| `is_active` | BOOLEAN | NO | TRUE | 連携の有効/無効 |
| `linked_at` | DATETIME | NO | CURRENT_TIMESTAMP | アカウント連携日時 |
| `unlinked_at` | DATETIME | YES | NULL | 連携解除日時（解除しても行は保持。再連携時に更新） |

**インデックス**
```sql
UNIQUE KEY uq_ulc_user_scope (user_id, scope_type, scope_id)   -- ユーザー×スコープで1つの LINE 連携
INDEX idx_ulc_line_user (line_user_id)                           -- LINE ユーザー ID で検索（Webhook 受信時）
```

**制約・備考**
- `user_id` ON DELETE CASCADE: ユーザー退会時に連携も削除
- 1ユーザーは複数スコープ（チームA + チームB + 組織）それぞれの BOT と連携可能
- `line_user_id` は LINE BOT ごとに異なる（同一ユーザーでも BOT が違えば別の ID）
- `line_login_sub`: LINE ログイン（F01.1）済みのユーザーは、Social API（`GET /friendship/v1/status`）で BOT との友だち関係と `userId` を取得し、自動的にアカウント連携を成立させる（リンクトークン方式の手動フロー不要）。LINE ログインを使わないユーザーは従来通りリンクトークン方式で連携
- 連携解除時は `is_active = false` + `unlinked_at = NOW()`（行は削除しない。再連携時の履歴保持）
- `quiet_start` / `quiet_end`: おやすみ時間帯の LINE 通知を保留し、`quiet_end` 直後にダイジェスト形式（Flex Message で件数+概要リスト）でまとめて送信。おやすみ時間中に溜まった通知が1通に集約されるため、月間送信枠の節約にもなる。`quiet_start = quiet_end` で無効化

#### `line_message_logs`

LINE 送信メッセージの履歴。デバッグ・配信レート管理・ADMIN の配信確認に使用。

| カラム名 | 型 | NULL | デフォルト | 説明 |
|---------|---|------|-----------|------|
| `id` | BIGINT UNSIGNED | NO | AUTO_INCREMENT | PK |
| `config_id` | BIGINT UNSIGNED | NO | — | FK → line_bot_configs（ON DELETE RESTRICT。BOT 設定は論理削除のため物理削除されない） |
| `user_id` | BIGINT UNSIGNED | YES | NULL | FK → users（ON DELETE SET NULL。送信先の Mannschaft ユーザー。退会後は NULL で匿名化） |
| `line_user_id` | VARCHAR(50) | NO | — | 送信先の LINE ユーザー ID |
| `message_type` | VARCHAR(30) | NO | — | `TEXT` / `FLEX` / `TEMPLATE` |
| `notification_type` | VARCHAR(50) | YES | NULL | 元の通知タイプ（`SCHEDULE_UPDATED` / `CIRCULATION_REMIND` 等） |
| `content_summary` | VARCHAR(500) | NO | — | メッセージ内容の要約（本文は保存しない。個人情報保護） |
| `status` | VARCHAR(20) | NO | — | `SENT` / `FAILED` / `RATE_LIMITED` |
| `error_message` | VARCHAR(500) | YES | NULL | 送信失敗時のエラーメッセージ |
| `line_request_id` | VARCHAR(100) | YES | NULL | LINE API のレスポンスに含まれる request_id |
| `created_at` | DATETIME | NO | CURRENT_TIMESTAMP | |

**インデックス**
```sql
INDEX idx_lml_config (config_id, created_at DESC)      -- BOT 別の配信履歴
INDEX idx_lml_user (user_id, created_at DESC)           -- ユーザー別の配信履歴
INDEX idx_lml_status (status, created_at DESC)          -- 失敗メッセージの確認
```

**制約・備考**
- INSERT のみ（更新・削除不可。監査ログと同一設計）
- `user_id`: 送信時に `user_line_connections` から取得して非正規化で記録。配信履歴画面で JOIN なしにユーザー名を表示可能。ユーザー退会時は `ON DELETE SET NULL` で匿名化（ログ自体は保持）
- `content_summary`: メッセージ本文をそのまま保存しない。「[スケジュール変更] 3/20 定例会議の時間が変更されました」のような要約のみ
- 保持期間: 90日。日次バッチで古いレコードを物理削除
- LINE の配信レート制限（月間無料枠: 200通。有料プランで増枠）の管理にも使用

#### `sns_feed_configs`

チーム/組織レベルの SNS フィード表示設定。

| カラム名 | 型 | NULL | デフォルト | 説明 |
|---------|---|------|-----------|------|
| `id` | BIGINT UNSIGNED | NO | AUTO_INCREMENT | PK |
| `scope_type` | VARCHAR(20) | NO | — | `TEAM` / `ORGANIZATION` |
| `scope_id` | BIGINT UNSIGNED | NO | — | teams.id / organizations.id |
| `provider` | VARCHAR(20) | NO | — | `INSTAGRAM`（Phase 9）。将来 `X` を追加可能な設計 |
| `account_id` | VARCHAR(100) | NO | — | SNS アカウント ID（@username 等） |
| `api_key_enc` | VARBINARY(512) | YES | NULL | API キー（AES-256-GCM 暗号化。Instagram Graph API / X API v2） |
| `api_secret_enc` | VARBINARY(512) | YES | NULL | API シークレット（AES-256-GCM 暗号化） |
| `access_token_enc` | VARBINARY(1024) | YES | NULL | アクセストークン（AES-256-GCM 暗号化） |
| `encryption_key_version` | INT UNSIGNED | NO | 1 | 暗号化キーバージョン |
| `token_expires_at` | DATETIME | YES | NULL | アクセストークンの有効期限。リフレッシュバッチで毎回 API 問合せを回避 |
| `is_active` | BOOLEAN | NO | TRUE | フィード表示の有効/無効 |
| `is_public` | BOOLEAN | NO | FALSE | GUEST にも公開するか |
| `display_count` | SMALLINT | NO | 6 | フィードの表示件数（3〜12） |
| `cache_ttl_minutes` | SMALLINT | NO | 15 | Redis キャッシュの TTL（分。5〜60） |
| `configured_by` | BIGINT UNSIGNED | NO | — | FK → users |
| `created_at` | DATETIME | NO | CURRENT_TIMESTAMP | |
| `updated_at` | DATETIME | NO | CURRENT_TIMESTAMP ON UPDATE | |
| `deleted_at` | DATETIME | YES | NULL | 論理削除 |

**インデックス**
```sql
INDEX idx_sfc_scope_provider (scope_type, scope_id, provider)  -- スコープ×プロバイダ検索（UNIQUE はアプリ層で保証。deleted_at の NULL 問題回避）
```

**制約・備考**
- Phase 9 では1スコープにつき Instagram 1件のみ。将来 X を追加した場合は最大2件。重複チェックはアプリ層で `deleted_at IS NULL` 条件付きバリデーション
- `configured_by` ON DELETE RESTRICT: 設定した ADMIN が退会する場合、先に設定を引き継ぐか削除する必要がある
- `api_key_enc` / `api_secret_enc` / `access_token_enc`: トークンが不要なプロバイダ（埋め込み表示のみ等）では NULL 許可
- `is_public = true` の場合、未ログインの GUEST も SNS フィードを閲覧可能（チーム紹介ページ等）
- フィードデータ自体は Redis のみ: `sns_feed:{scope_type}:{scopeId}:{provider}` にキャッシュ
- **Instagram 審査モデル**: Mannschaft プラットフォームとして Meta App Review を1回だけ取得（`instagram_basic` / `pages_read_engagement` パーミッション）。`api_key_enc` / `api_secret_enc` はプラットフォーム共通のアプリ資格情報、`access_token_enc` はチーム/組織ごとの OAuth 接続トークン。Hootsuite / Buffer と同じモデル
- **X API**: Phase 9 では実装しない（API 利用料の観点）。将来追加時のプラン選定基準は FUTURE_CONSIDERATIONS.md を参照

### ER図（テキスト形式）
```
line_bot_configs (1) ──── (N) user_line_connections [scope_type + scope_id]
line_bot_configs (1) ──── (N) line_message_logs [config_id]
users (1) ──── (N) user_line_connections [user_id]
organizations / teams (1) ──── (0..1) line_bot_configs [scope_type + scope_id]
organizations / teams (1) ──── (0..1) sns_feed_configs [scope_type + scope_id + provider]  ※Phase 9はInstagramのみ
```

---

## 4. API設計

### エンドポイント一覧

#### LINE 連携
| メソッド | パス | 認証 | 説明 |
|---------|-----|------|------|
| POST | `/api/v1/line/webhook/{webhookSecret}` | 不要 | LINE Webhook 受信（署名検証で認証） |
| GET | `/api/v1/line/configs` | 必要 | LINE BOT 設定一覧（スコープ別） |
| POST | `/api/v1/line/configs` | 必要 | LINE BOT 設定作成（ADMIN） |
| PUT | `/api/v1/line/configs/{id}` | 必要 | LINE BOT 設定更新（ADMIN） |
| DELETE | `/api/v1/line/configs/{id}` | 必要 | LINE BOT 設定削除（ADMIN） |
| POST | `/api/v1/line/configs/{id}/test` | 必要 | テストメッセージ送信（ADMIN） |
| POST | `/api/v1/line/link` | 必要 | ユーザー LINE アカウントリンク開始 |
| DELETE | `/api/v1/line/link/{connectionId}` | 必要 | LINE アカウントリンク解除 |
| GET | `/api/v1/line/link/status` | 必要 | 自分の LINE 連携状態一覧 |
| GET | `/api/v1/line/configs/{id}/logs` | 必要 | 配信履歴（ADMIN） |
| GET | `/api/v1/line/configs/{id}/stats` | 必要 | 配信統計（ADMIN） |
| POST | `/api/v1/line/configs/{id}/broadcast` | 必要 | 一斉配信（ADMIN） |

#### SNS 連携
| メソッド | パス | 認証 | 説明 |
|---------|-----|------|------|
| GET | `/api/v1/sns/feeds` | 条件付 | SNS フィード取得（is_public なら認証不要） |
| GET | `/api/v1/sns/configs` | 必要 | SNS フィード設定一覧（ADMIN） |
| POST | `/api/v1/sns/configs` | 必要 | SNS フィード設定作成（ADMIN） |
| PUT | `/api/v1/sns/configs/{id}` | 必要 | SNS フィード設定更新（ADMIN） |
| DELETE | `/api/v1/sns/configs/{id}` | 必要 | SNS フィード設定削除（ADMIN） |
| POST | `/api/v1/sns/configs/{id}/refresh` | 必要 | キャッシュ手動リフレッシュ（ADMIN） |
| GET | `/api/v1/sns/instagram/oauth/start` | 必要 | Instagram OAuth 開始（ADMIN。認可画面へリダイレクト） |
| GET | `/api/v1/sns/instagram/oauth/callback` | 不要 | Instagram OAuth コールバック（認可コード→トークン交換） |

### リクエスト／レスポンス仕様

#### `POST /api/v1/line/webhook/{webhookSecret}`

**認可**: 不要（LINE プラットフォームからのリクエスト。署名検証で認証）

LINE Messaging API からの Webhook イベントを受信する。

**リクエストヘッダ**
```
X-Line-Signature: {HMAC-SHA256 署名}
```

**リクエストボディ**: LINE Webhook Event Object（LINE 公式仕様に準拠）

**レスポンス（200 OK）**: 空レスポンス（LINE は 200 以外をリトライするため即時返却）

**処理フロー**
```
1. webhookSecret で line_bot_configs を検索
   → 存在しない or is_active = false の場合も 200 を返却（LINE は200以外を無限リトライするため）
2. X-Line-Signature を channel_secret で HMAC-SHA256 検証
   → 不一致は 200 を返却してログに WARNING（403 を返すと LINE がリトライする）
3. 200 を即時返却（非同期処理の完了を待たない）
4. イベント種別に応じて非同期処理（@Async）:
   a. follow: ウェルカムメッセージを送信。ユーザー紐付けは行わない
      （follow 時点では Mannschaft の user_id が不明。紐付けは accountLink で確定）
   b. unfollow: line_user_id で user_line_connections を検索し is_active = false に更新
   c. message: 双方向メッセージング処理（出欠回答等。line_user_id で連携ユーザーを特定）
   d. accountLink: nonce 検証 → user_line_connections に INSERT（user_id + line_user_id 確定）
```

---

#### `POST /api/v1/line/configs`

**認可**: ADMIN

LINE BOT 設定を作成する。

**リクエストボディ**
```json
{
  "scope_type": "TEAM",
  "scope_id": 1,
  "channel_id": "1234567890",
  "channel_secret": "abc123...",
  "channel_access_token": "eyJ..."
}
```

**レスポンス（201 Created）**
```json
{
  "data": {
    "id": 1,
    "scope_type": "TEAM",
    "scope_id": 1,
    "channel_id": "1234567890",
    "webhook_url": "https://api.mannschaft.app/api/v1/line/webhook/x7k9m2...",
    "is_active": true,
    "notification_enabled": true,
    "created_at": "2026-03-15T10:00:00"
  }
}
```

**補足**
- `channel_secret` / `channel_access_token` はレスポンスに含めない（セキュリティ）
- `webhook_url` は自動生成。ADMIN がこの URL を LINE Developers Console の Webhook URL に設定する
- 作成後に `POST /configs/{id}/test` でテストメッセージ送信を推奨

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 400 | 必須フィールド未入力 / channel_id 形式不正 |
| 403 | ADMIN でない |
| 409 | 同一スコープに既に BOT 設定が存在（`deleted_at IS NULL` で判定） |

---

#### `GET /api/v1/line/configs`

**認可**: ADMIN / SYSTEM_ADMIN

自分が管理するスコープの LINE BOT 設定一覧を取得する。

**クエリパラメータ**
| パラメータ | 型 | 必須 | 説明 |
|-----------|---|------|------|
| `scope_type` | String | Yes | `TEAM` / `ORGANIZATION` |
| `scope_id` | Long | Yes | 対象スコープの ID |

**レスポンス（200 OK）**: POST /line/configs のレスポンスと同一構造（配列）。`channel_secret` / `channel_access_token` は含まない

---

#### `PUT /api/v1/line/configs/{id}`

**認可**: ADMIN

LINE BOT 設定を更新する。トークンの再設定・通知の一時停止等に使用。

**リクエストボディ**
```json
{
  "channel_secret": "new_secret...",
  "channel_access_token": "new_token...",
  "is_active": true,
  "notification_enabled": false
}
```

**補足**
- `channel_secret` / `channel_access_token` は指定された場合のみ上書き（部分更新）
- `webhook_secret` は変更不可（変更する場合は削除 → 再作成）

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 403 | ADMIN でない |
| 404 | 設定が存在しない |

---

#### `DELETE /api/v1/line/configs/{id}`

**認可**: ADMIN

LINE BOT 設定を論理削除する。関連する連携ユーザー全員に通知を送信してから削除。

**レスポンス（204 No Content）**

**補足**
- 削除前に連携済みユーザー（`user_line_connections.is_active = true`）全員にプッシュ通知（F04.3）を送信:「LINE 連携が管理者により解除されました。通知はプッシュ通知で受信します。」（LINE 経由では送信不可。BOT が削除されるため）
- 関連する `user_line_connections` は一括で `is_active = false` + `unlinked_at = NOW()` に更新
- `line_message_logs` は保持（監査ログとして残す）

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 403 | ADMIN でない |
| 404 | 設定が存在しない |

---

#### `POST /api/v1/line/configs/{id}/test`

**認可**: ADMIN

テストメッセージを送信する。BOT 設定の疎通確認に使用。

**リクエストボディ**
```json
{
  "target_line_user_id": "U1234567890abcdef..."
}
```

**補足**
- `target_line_user_id` 省略時は `configured_by` の ADMIN 自身に送信（LINE 連携済みの場合）
- テストメッセージ: 「[Mannschaft] LINE BOT のテスト送信です。このメッセージが届いていれば設定は正常です。」
- `line_message_logs` に `notification_type = TEST` で記録

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 400 | target_line_user_id が不正 |
| 403 | ADMIN でない |
| 404 | 設定が存在しない |
| 502 | LINE API への送信に失敗 |

---

#### `POST /api/v1/line/link`

**認可**: ログイン済みユーザー（SUPPORTER 以上）

LINE アカウント連携を開始する。リンクトークン方式で安全に連携。

**リクエストボディ**
```json
{
  "scope_type": "TEAM",
  "scope_id": 1
}
```

**レスポンス（200 OK）**
```json
{
  "data": {
    "link_url": "https://access.line.me/dialog/bot/accountLink?linkToken=xxx&nonce=yyy",
    "expires_in": 600
  }
}
```

**補足**
- LINE のリンクトークン API を呼び出し、ユーザーを LINE のアカウント連携画面にリダイレクト
- ユーザーが LINE 側で承認すると、Webhook の `accountLink` イベントが発火 → `user_line_connections` に INSERT
- リンクトークンの有効期限: 10分

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 400 | scope_type / scope_id 不正 |
| 403 | SUPPORTER 以上でない / 対象スコープのメンバーでない |
| 404 | 対象スコープに LINE BOT が設定されていない |
| 409 | 既にリンク済み |

---

#### `DELETE /api/v1/line/link/{connectionId}`

**認可**: 本人 / ADMIN

LINE アカウント連携を解除する。

**レスポンス（204 No Content）**

**補足**
- `is_active = false` + `unlinked_at = NOW()` に更新（行は削除しない）
- 解除後は LINE 経由の通知が停止し、プッシュ通知にフォールバック

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 403 | 本人でも ADMIN でもない |
| 404 | 連携が存在しない / 既に解除済み |

---

#### `GET /api/v1/line/link/status`

**認可**: ログイン済みユーザー

自分の LINE 連携状態を全スコープ横断で取得する。

**レスポンス（200 OK）**
```json
{
  "data": [
    {
      "connection_id": 1,
      "scope_type": "TEAM",
      "scope_id": 1,
      "scope_name": "開発チーム",
      "display_name": "田中太郎",
      "is_active": true,
      "linked_at": "2026-03-10T09:00:00"
    }
  ]
}
```

---

#### `GET /api/v1/line/configs/{id}/logs`

**認可**: ADMIN / DEPUTY_ADMIN（`MANAGE_CONTENT`）

LINE BOT の配信履歴を取得する。

**クエリパラメータ**
| パラメータ | 型 | 必須 | 説明 |
|-----------|---|------|------|
| `status` | String | No | `SENT` / `FAILED` / `RATE_LIMITED`（フィルタ） |
| `cursor` | Long | No | カーソル |
| `limit` | Integer | No | 取得件数（デフォルト: 50） |

**レスポンス（200 OK）**
```json
{
  "data": [
    {
      "id": 1,
      "user_id": 20,
      "line_user_id": "U1234...",
      "display_name": "田中太郎",
      "message_type": "FLEX",
      "notification_type": "SCHEDULE_UPDATED",
      "content_summary": "[スケジュール変更] 3/20 定例会議の時間が変更されました",
      "status": "SENT",
      "created_at": "2026-03-15T10:00:00"
    }
  ],
  "meta": { "next_cursor": null, "limit": 50, "has_next": false }
}
```

**補足**
- `user_id` / `display_name` は `line_message_logs.user_id` → `users` から取得（JOIN 不要で高速）。退会済みユーザーは `user_id = null` + `display_name = "退会済みユーザー"`
- FAILED のログには `error_message` フィールドも返却

---

#### `GET /api/v1/line/configs/{id}/stats`

**認可**: ADMIN / DEPUTY_ADMIN（`MANAGE_CONTENT`）

LINE BOT の配信統計を取得する。月間の送信数・失敗率・残枠を確認できる。

**クエリパラメータ**
| パラメータ | 型 | 必須 | 説明 |
|-----------|---|------|------|
| `month` | String | No | 対象月（`YYYY-MM` 形式。デフォルト: 当月） |

**レスポンス（200 OK）**
```json
{
  "data": {
    "period": "2026-03",
    "total_sent": 156,
    "total_failed": 3,
    "total_rate_limited": 0,
    "monthly_quota": 200,
    "remaining_quota": 44,
    "quota_usage_percentage": 78.0,
    "breakdown_by_type": [
      { "notification_type": "SCHEDULE_UPDATED", "count": 80 },
      { "notification_type": "CIRCULATION_REMIND", "count": 50 },
      { "notification_type": "ATTENDANCE_REMINDER", "count": 26 }
    ],
    "linked_users": 25,
    "active_users": 22,
    "blocked_users": 3
  }
}
```

**補足**
- `monthly_quota`: LINE 公式アカウントのプランに基づく月間送信枠。LINE Developers Console で確認した値を `line_bot_configs` には保存せず、`line_message_logs` の SENT 件数から算出
- `active_users`: `is_active = true` の連携ユーザー数
- `blocked_users`: 直近のブロック検出バッチで検出された数

---

#### `POST /api/v1/line/configs/{id}/broadcast`

**認可**: ADMIN

連携済みユーザー全員にカスタムメッセージを一斉送信する。

**リクエストボディ**
```json
{
  "message": "【重要】明日の活動は雨天のため中止です。詳細はスケジュールをご確認ください。",
  "confirm": true
}
```

| フィールド | 型 | 必須 | 説明 |
|-----------|---|------|------|
| `message` | String | Yes | 送信メッセージ（最大5,000文字） |
| `confirm` | Boolean | Yes | 送信確認フラグ。`false` の場合はプレビュー（対象人数・消費枠のみ返却） |

**レスポンス（200 OK）**: `confirm = false`（プレビュー）
```json
{
  "data": {
    "target_count": 22,
    "estimated_quota_usage": 22,
    "remaining_quota_after": 22,
    "warning": null
  }
}
```

**レスポンス（200 OK）**: `confirm = true`（送信実行）
```json
{
  "data": {
    "sent_count": 22,
    "failed_count": 0,
    "remaining_quota": 22
  }
}
```

**補足**
- LINE の Multicast API を使用（最大500人/リクエスト。500人超は分割送信）
- おやすみ時間中のユーザーにもブロードキャストは即時送信（緊急連絡の性質上、Quiet Hours を無視）
- `remaining_quota_after` が0以下の場合は `warning: "月間送信枠を超過します"` を返却（送信自体は可能。LINE 側の課金に従う）
- `line_message_logs` に `notification_type = BROADCAST` で全件記録
- レートリミット: 1時間に1回

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 400 | message が空 / 5,000文字超過 / confirm 未指定 |
| 403 | ADMIN でない |
| 404 | 設定が存在しない |
| 429 | ブロードキャストのレートリミット超過（1時間に1回） |

---

#### `GET /api/v1/sns/instagram/oauth/start`

**認可**: ADMIN

Instagram OAuth 認可フローを開始する。Instagram の認可画面にリダイレクトする。

**クエリパラメータ**
| パラメータ | 型 | 必須 | 説明 |
|-----------|---|------|------|
| `scope_type` | String | Yes | `TEAM` / `ORGANIZATION` |
| `scope_id` | Long | Yes | 対象スコープの ID |

**レスポンス（302 Found）**
```
HTTP/1.1 302 Found
Location: https://api.instagram.com/oauth/authorize?client_id={app_id}&redirect_uri={callback_url}&scope=instagram_basic,pages_read_engagement&response_type=code&state={encrypted_state}
```

**補足**
- `state` パラメータに `scope_type + scope_id + CSRF トークン` を暗号化して含める（コールバック時に検証）
- ADMIN が Instagram でログイン → ビジネスアカウントを選択 → 権限を承認 → callback にリダイレクト

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 403 | ADMIN でない |
| 409 | 同一スコープに既に Instagram 設定が存在 |

---

#### `GET /api/v1/sns/instagram/oauth/callback`

**認可**: 不要（Instagram からのリダイレクト。state パラメータで CSRF 検証）

Instagram OAuth コールバック。認可コードをアクセストークンに交換し、`sns_feed_configs` に保存する。

**クエリパラメータ**（Instagram が付与）
| パラメータ | 型 | 必須 | 説明 |
|-----------|---|------|------|
| `code` | String | Yes | 認可コード |
| `state` | String | Yes | CSRF 検証用（暗号化された scope_type + scope_id + token） |

**処理フロー**
```
1. state を復号して scope_type + scope_id + CSRF トークンを検証
2. 認可コードで短期アクセストークンを取得:
   POST https://api.instagram.com/oauth/access_token
3. 短期トークンを長期トークンに交換:
   GET https://graph.instagram.com/access_token?grant_type=ig_exchange_token&access_token={short_token}
4. Instagram アカウント情報を取得:
   GET https://graph.instagram.com/me?fields=id,username
5. sns_feed_configs に INSERT（access_token_enc + token_expires_at + account_id）
6. フロントエンドの SNS 設定画面にリダイレクト（成功メッセージ付き）
```

**エラー時**: フロントエンドの SNS 設定画面にリダイレクト（エラーメッセージ付き）

---

#### `POST /api/v1/sns/configs`

**認可**: ADMIN

Instagram フィード設定を作成する。OAuth フロー（`/sns/instagram/oauth/start`）で自動作成されるため、通常は直接呼び出さない。手動でのトークン設定が必要な場合のフォールバック用。

**リクエストボディ**
```json
{
  "scope_type": "TEAM",
  "scope_id": 1,
  "provider": "INSTAGRAM",
  "account_id": "@mannschaft_official",
  "access_token": "IGQ...",
  "display_count": 6,
  "cache_ttl_minutes": 15,
  "is_public": false
}
```

**補足**
- `access_token` は Instagram OAuth フローで取得した長期トークンを設定。AES-256-GCM で暗号化して保存
- `api_key_enc` / `api_secret_enc` はプラットフォーム共通のため、リクエストには含まない（環境変数から取得）

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 400 | 必須フィールド未入力 / display_count 範囲外（3〜12）/ cache_ttl_minutes 範囲外（5〜60） |
| 403 | ADMIN でない |
| 409 | 同一スコープ×プロバイダに既に設定が存在 |

---

#### `PUT /api/v1/sns/configs/{id}`

**認可**: ADMIN

SNS フィード設定を更新する。

**リクエストボディ**: POST /sns/configs と同一構造（部分更新対応）

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 403 | ADMIN でない |
| 404 | 設定が存在しない |

---

#### `DELETE /api/v1/sns/configs/{id}`

**認可**: ADMIN

SNS フィード設定を論理削除する。Redis キャッシュも同時に削除。

**レスポンス（204 No Content）**

---

#### `POST /api/v1/sns/configs/{id}/refresh`

**認可**: ADMIN

Redis キャッシュを手動でクリアし、次回アクセス時に外部 API から再取得させる。

**レスポンス（200 OK）**
```json
{
  "data": {
    "cleared_cache_key": "sns_feed:TEAM:1:INSTAGRAM",
    "message": "キャッシュをクリアしました。次回アクセス時に最新データを取得します。"
  }
}
```

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 403 | ADMIN でない |
| 404 | 設定が存在しない |

---

#### `GET /api/v1/sns/feeds`

**認可**: SUPPORTER 以上。`is_public = true` の場合は認証不要（GUEST も閲覧可能）

SNS フィードを取得する。Redis キャッシュから返却し、キャッシュミス時は外部 API にフェッチ。

**クエリパラメータ**
| パラメータ | 型 | 必須 | 説明 |
|-----------|---|------|------|
| `scope_type` | String | Yes | `TEAM` / `ORGANIZATION` |
| `scope_id` | Long | Yes | 対象スコープの ID |
| `provider` | String | No | `INSTAGRAM`（Phase 9。将来 `X` 追加時に複数対応） |

**レスポンス（200 OK）**
```json
{
  "data": {
    "instagram": {
      "account_id": "@mannschaft_official",
      "posts": [
        {
          "id": "17895695668004550",
          "media_type": "IMAGE",
          "media_url": "https://scontent.cdninstagram.com/...",
          "caption": "チーム活動の様子 #mannschaft",
          "permalink": "https://www.instagram.com/p/...",
          "timestamp": "2026-03-14T15:30:00+0900"
        }
      ],
      "cached_at": "2026-03-15T10:00:00",
      "cache_ttl_minutes": 15
    }
  }
}
```

**補足**
- 将来 X フィードを追加した場合、`"x": { ... }` フィールドがレスポンスに追加される。Phase 9 では `instagram` のみ返却
- Redis キャッシュキー: `sns_feed:{scope_type}:{scopeId}:{provider}`
- キャッシュヒット: Redis から即時返却
- キャッシュミス: 外部 API にリクエスト → Redis にセット（TTL = `cache_ttl_minutes`）→ 返却
- 外部 API エラー時: 期限切れキャッシュがあれば stale データを返却 + `is_stale: true` フラグ付与。なければ空配列

---

## 5. ビジネスロジック

### 主要フロー

#### LINE 通知配信フロー
```
1. F04.3 の NotificationService が通知イベントを生成
2. NotificationChannel の実装として LineChannel.send() が呼び出される:
   a. サーキットブレーカー判定（Resilience4j）:
      - OPEN 状態 → LINE 送信をスキップ、プッシュ通知にフォールバック（即時 return）
   b. 対象ユーザーの user_line_connections を検索（is_active = true）
   c. 連携がなければスキップ（LINE 以外のチャンネルにフォールバック）
   d. 通知種別フィルタ:
      - notification_preferences で当該 notification_type の line_enabled を確認
      - line_enabled = false → スキップ
   e. 連携が複数ある場合、スコープ最近接ルールで1つを選択（重複送信防止）:
      - 通知の発生元スコープに一致する連携がある → そのBOTを使用
      - なければ上位スコープ（組織）の連携がある → 組織BOTを使用
      - 1通知につき1 LINE メッセージのみ送信
   f. おやすみ時間チェック:
      - 現在時刻が quiet_start〜quiet_end の範囲内 → Redis キュー
        （line_quiet_queue:{user_id}:{scope}）に保留。quiet_end バッチで一括送信
      - 範囲外 → 即時送信
   g. 即時送信の場合:
      - line_bot_configs から channel_access_token を復号
      - 通知タイプに応じてメッセージ構築:
        - TEXT: シンプルなテキストメッセージ
        - FLEX: リッチコンテンツ（ボタン付き。出欠回答等）
      - LINE Messaging API の Push API を呼び出し
      - line_message_logs に結果を INSERT
3. LINE API レスポンスに応じた処理:
   a. 200: status = SENT。サーキットブレーカーに成功を記録
   b. 429 (Rate Limited): status = RATE_LIMITED → 指数バックオフでリトライ（最大3回）
   c. 400/401/403: status = FAILED → error_message に記録。リトライしない
   d. 5xx / タイムアウト: status = FAILED。サーキットブレーカーに失敗を記録
```

#### おやすみダイジェスト送信（バッチ）
```
1. Spring @Scheduled バッチが1分間隔で実行
2. user_line_connections から quiet_end が現在時刻（分単位丸め）に一致するユーザーを取得
3. 各ユーザーについて Redis キュー（line_quiet_queue:{user_id}:{scope}）を確認:
   a. キューが空 → スキップ
   b. キューに保留通知あり → ダイジェスト Flex Message を構築:
      - 「おやすみ中に届いた通知（3件）」
      - 各通知の概要リスト（タイトル + 通知タイプ + 時刻）
      - 1通の Flex Message にまとめて送信（月間送信枠を節約）
   c. 送信完了後にキューを削除
4. line_message_logs に notification_type = QUIET_DIGEST で記録
```

#### BOT ブロック検出（バッチ）
```
1. Spring @Scheduled バッチが週次で実行（日曜深夜）
2. 全 line_bot_configs（is_active = true）を走査
3. 各 BOT の user_line_connections（is_active = true）について:
   a. LINE の Get Profile API を呼び出し:
      GET https://api.line.me/v2/bot/profile/{line_user_id}
   b. 200: 正常（何もしない）
   c. 404: ユーザーが BOT をブロック or 退会 → is_active = false に更新
4. 検出結果を ADMIN に通知:
   - 「LINE BOT ブロック検出: {count}人のユーザーが BOT をブロックしています」
5. Get Profile API は月間送信枠にカウントされないため追加コストなし
```

#### LINE アカウント連携フロー
```
1. ユーザーが POST /line/link を呼び出し
2. バックエンドが LINE のリンクトークン API を呼び出し:
   - POST https://api.line.me/v2/bot/user/{botUserId}/linkToken
   - linkToken + nonce を取得
3. フロントエンドが link_url にリダイレクト
4. ユーザーが LINE アプリ上で「連携する」を承認
5. LINE が Webhook で accountLink イベントを送信:
   a. nonce を検証（リプレイ攻撃防止）
   b. user_line_connections に INSERT（user_id + line_user_id + scope）
   c. ウェルカムメッセージを送信:「Mannschaft との連携が完了しました。通知を LINE で受信できます。」
```

#### LINE 双方向メッセージング（出欠回答）
```
1. Flex Message で出欠確認を送信:
   - 「3/20 定例会議に出席しますか？」
   - ボタン: [出席] [欠席] [未定]
2. ユーザーがボタンをタップ → Webhook の postback イベント受信
3. postback.data をパース（例: action=attendance&schedule_id=123&response=ATTEND）
4. F05 スケジュール機能の出欠回答 API を内部呼び出し
5. 応答メッセージ送信:「出席で登録しました ✓」
```

#### SNS フィードキャッシュフロー
```
1. GET /sns/feeds リクエスト受信
2. Redis キャッシュチェック:
   a. キャッシュヒット → 即時返却
   b. キャッシュミス → 3 へ
3. sns_feed_configs から API クレデンシャルを復号
4. Instagram Graph API を呼び出し:
   - GET /me/media?fields=id,media_type,media_url,caption,permalink,timestamp
5. レスポンスを整形 → Redis にセット（TTL = cache_ttl_minutes）
6. クライアントに返却
7. 外部 API エラー時:
   a. stale キャッシュがあれば返却（is_stale: true 付与）
   b. なければ空配列 + error フラグ
```

#### Instagram トークン自動リフレッシュ（バッチ）
```
1. Spring @Scheduled バッチが日次で実行
2. sns_feed_configs から以下の条件で取得:
   - provider = INSTAGRAM
   - is_active = true
   - deleted_at IS NULL
3. 各設定について token_expires_at を確認（DB カラムで判定。API 呼び出し不要）:
4. トークン期限判定:
   a. 残り30日以上: 何もしない
   b. 残り7〜30日: ADMIN に通知（「Instagram トークンの期限が近づいています」）
      + 自動リフレッシュ:
        GET /oauth/access_token?grant_type=ig_refresh_token&access_token={token}
      → 新しいトークンを暗号化して access_token_enc + token_expires_at を更新
   c. 残り7日未満: ADMIN に緊急通知 + 自動リフレッシュ試行
   d. 期限切れ: ADMIN に通知（「Instagram トークンが期限切れです。再接続してください」）
      → フィード取得をスキップ（空配列返却）
5. リフレッシュ失敗時: error をログに記録し、次回バッチで再試行
```

#### LINE 配信ログパージ（バッチ）
```
1. Spring @Scheduled バッチが日次で実行（深夜帯）
2. line_message_logs から created_at < NOW() - INTERVAL 90 DAY のレコードを物理削除
3. バッチサイズ: 1,000件ずつ DELETE（ロック長期化を防止）
4. 削除件数をアプリケーションログに記録
```

### 重要な判定ロジック

- **LINE 通知のチャンネル優先度**: ユーザーが LINE 連携済みの場合、F04.3 の通知設定で「LINE 優先」を選択可能。未選択の場合はプッシュ通知と LINE の両方に送信（デフォルト: 両方）
- **通知種別ごとの LINE 配信設定**: `notification_preferences` で通知タイプ別に `line_enabled` を制御。ユーザーはフロントエンドで「LINE で受信する通知を選択」チェックボックスリストで設定。例: SCHEDULE_UPDATED=ON, CHAT_MENTION=OFF
- **複数スコープ連携時の重複送信防止（スコープ最近接ルール）**: ユーザーがチーム BOT と組織 BOT の両方に連携している場合、通知の発生元スコープに最も近い BOT から1通のみ送信。例: チームAのスケジュール通知 → チームA BOT が優先。チームA BOT がなければ組織 BOT にフォールバック。同一通知が複数 BOT から届くことはない
- **おやすみ時間（Quiet Hours）**: `quiet_start`〜`quiet_end` の時間帯は LINE 通知を Redis キューに保留。`quiet_end` 直後にダイジェスト Flex Message でまとめて1通送信。ブロードキャストは緊急連絡の性質上 Quiet Hours を無視して即時送信
- **サーキットブレーカー（Resilience4j）**: LINE API 障害時にプッシュ通知に自動フォールバック。閾値: 直近10件中5件以上 FAILED で OPEN（5分間 LINE 送信スキップ）。HALF_OPEN で1件テスト送信→成功なら CLOSED に自動復帰
- **BOT ブロック検出**: 週次バッチで Get Profile API（送信枠外）を呼び出し、ブロック済みユーザーを `is_active = false` に自動更新。送信枠の無駄遣いと FAILED ログの蓄積を防止
- **LINE 月間送信数の管理**: LINE の無料プランは月200通。`line_message_logs` の月間 SENT 件数で残数を管理。残数が10%以下になったら ADMIN に通知。0通になったら送信をスキップし、プッシュ通知にフォールバック
- **LINE BOT の複数スコープ**: 同一の LINE チャンネルを複数スコープで使い回すことは不可（`channel_id` の UNIQUE 制約は設けないが、アプリ層で警告表示）。理由: Webhook の振り分けが複雑になるため
- **SNS トークンの有効期限管理**: Instagram のアクセストークンは60日で期限切れ。期限30日前・7日前・1日前に ADMIN に通知。期限切れ後はフィード取得をスキップし、空配列を返却
- **SNS API 障害時の対応**: 外部 API のタイムアウト（5秒）後、stale キャッシュがあればそれを返却。完全なキャッシュミスの場合はフロントエンドで「フィードを取得できませんでした」を表示

---

## 6. セキュリティ考慮事項

- **トークン暗号化**: `channel_secret_enc` / `channel_access_token_enc` / `api_key_enc` 等は AES-256-GCM で暗号化。暗号化キーは環境変数 or AWS Secrets Manager（F08 Google Calendar と同一パターン）
- **Webhook 署名検証**: LINE からの Webhook リクエストは `X-Line-Signature` を `channel_secret` で HMAC-SHA256 検証。不一致でも 200 を返却（LINE は200以外を無限リトライするため）し、WARNING ログを記録して非同期処理をスキップ
- **Webhook URL のシークレット**: `/api/v1/line/webhook/{webhookSecret}` の形式で、`webhookSecret` を知らない第三者は Webhook URL にアクセスできない（署名検証の前段階のフィルタ）
- **リプレイ攻撃防止**: アカウント連携の nonce は1回限り有効。使用済み nonce は Redis に TTL 付きで保存し、重複使用を拒否
- **レートリミット**: LINE 通知は LINE API のレートリミットに従う（1秒あたりの送信数制限）。ブロードキャストは 1時間に1回。SNS フィード取得は 1分間に10回（キャッシュにより通常は1回/TTL）。配信統計 API は 1分間に30回
- **個人情報保護**: `line_message_logs` にメッセージ本文を保存しない（content_summary のみ）。`line_user_id` は LINE BOT 固有の ID であり、LINE アカウントの電話番号・メールアドレスとは紐付かない
- **SNS トークン露出防止**: SNS フィード API のレスポンスにトークン情報を含めない。管理 API でも暗号化カラムの値は返却しない（存在の有無のみ `has_api_key: true/false` で表示）
- **監査ログ**: LINE BOT 設定の作成・更新・削除、アカウント連携・解除を `audit_logs` に記録

---

## 7. Flyway マイグレーション

```
V9.031__create_line_bot_configs_table.sql           -- LINE BOT 設定
V9.032__create_user_line_connections_table.sql       -- ユーザー LINE 連携
V9.033__create_line_message_logs_table.sql           -- LINE 送信履歴
V9.034__create_sns_feed_configs_table.sql            -- SNS フィード設定
```

**マイグレーション上の注意点**
- `line_bot_configs` / `user_line_connections` は `users` テーブルへの FK を持つため、認証テーブルのマイグレーションが先行完了していること
- `line_message_logs` の90日パージバッチは Phase 9 デプロイ時に Spring @Scheduled で有効化

**F04.3 への追加要請**
- `NotificationChannel` に `LineChannel` 実装を追加（Resilience4j サーキットブレーカー適用）
- `notification_preferences` テーブルに `line_enabled BOOLEAN DEFAULT TRUE` カラムを追加（ユーザーが LINE 通知の ON/OFF を切り替え）
- 通知種別ごとの LINE 配信制御: `notification_type_preferences` テーブル（`user_id`, `notification_type`, `line_enabled`, `push_enabled`）を新設。ユーザーが通知種別×チャンネルのマトリクスで配信先を選択可能にする

---

## 8. 未解決事項

- [x] ~~LINE Rich Menu の実装範囲~~ → **Phase 9 ではテキスト + Flex Message のみ**。Rich Menu（BOT 画面下部の固定メニュー）は Phase 9 で LINE 連携の利用パターンが定着した後に検討。FUTURE_CONSIDERATIONS.md に記載。理由: Rich Menu には各チーム/組織ごとのメニュー項目カスタマイズ UI が必要で設計コストが大きい。Phase 9 の主要ユースケース（通知配信・出欠回答）は Rich Menu なしで成立する
- [x] ~~LINE ログインとの統合~~ → **`user_line_connections.line_login_sub` カラムを追加**。LINE ログイン（F01.1）済みユーザーは Social API（`GET /friendship/v1/status`）で BOT の友だち関係 + userId を取得し、自動的にアカウント連携を成立させる。リンクトークン方式の手動フローが不要になる。LINE ログイン未使用者は従来通りリンクトークン方式。別テーブルではなく1カラム追加で完結
- [x] ~~LINE 送信の月間無料枠の管理単位~~ → **LINE チャンネル単位で管理。Mannschaft 側のスコープ別配分は行わない**。`line_message_logs` の月間 SENT 件数で残数を ADMIN に表示・通知するのみ。実際の課金管理は LINE Developers Console に委ねる。理由: Mannschaft 側で配分ロジックを持つと LINE のプラン変更・カウント仕様変更への追従コストが発生する
- [x] ~~Instagram Graph API の審査~~ → **Mannschaft プラットフォームとして1回だけ Meta App Review を取得**（`instagram_basic` / `pages_read_engagement`）。各チーム/組織は OAuth で自分の Instagram ビジネスアカウントを接続するだけ（個別審査不要）。Hootsuite / Buffer と同じモデル。`api_key_enc` / `api_secret_enc` = プラットフォーム共通、`access_token_enc` = チーム/組織個別
- [x] ~~X API の料金プラン~~ → **Phase 9 では X フィード連携を実装しない**。API 利用料（Basic プラン月$100〜）が個人開発段階では負担が大きいため。収益に余裕ができた段階で追加する。`sns_feed_configs.provider` に `X` を追加可能な設計は維持。プラン選定基準は FUTURE_CONSIDERATIONS.md に記載

---

## 9. 変更履歴

| 日付 | 変更内容 |
|------|---------|
| 2026-03-15 | 初版作成: LINE/SNS 連携の詳細設計。DB 4テーブル（line_bot_configs / user_line_connections / line_message_logs / sns_feed_configs）+ Redis キャッシュ（SNS フィード）。API 16本（LINE 10本 + SNS 6本）。LINE Webhook 受信・アカウント連携・通知配信・双方向メッセージング（出欠回答）の4フロー。SNS はキャッシュオンリー設計（DB 不使用）。F04.3 NotificationChannel 連携。AES-256-GCM トークン暗号化。F08 パターン踏襲 |
| 2026-03-15 | F09.3 → F09.4 に番号変更。未解決事項5件を解決: (1) Rich Menu は Phase 9 対象外（FUTURE_CONSIDERATIONS.md へ）。(2) `line_login_sub` カラム追加で LINE ログインとの自動連携。(3) 月間送信枠は LINE チャンネル単位管理（Mannschaft 側配分なし）。(4) Instagram は Mannschaft として一括 App Review 取得。(5) X フィードは Phase 9 実装対象外（API 利用料の観点）、判断基準を FUTURE_CONSIDERATIONS.md に記載 |
| 2026-03-15 | X フィード連携を Phase 9 対象外に変更（個人開発段階での API 利用料負担を考慮）。SNS 連携は Instagram のみで Phase 9 リリース。`sns_feed_configs.provider` に `X` を将来追加可能な設計は維持。概要・スコープ・ER図・API仕様・ビジネスロジックから X 固有の記述を削除/注記化 |
| 2026-03-15 | 精査①: 14件の不具合・欠落・不整合を修正。(1) Webhook follow イベントの処理を修正（user_id 不明のため INSERT 不可→accountLink で確定）。(2) Webhook のエラーレスポンスを全て200に統一（LINE のリトライ問題対策）。(3) UNIQUE KEY の deleted_at NULL 問題をアプリ層バリデーションに変更。(4) POST /line/link の認可を SUPPORTER 以上に修正。(5) configured_by の ON DELETE RESTRICT を明記。(6) is_active と notification_enabled の動作差分を明確化。(7) SNS feeds レスポンスから x:null を削除。(8) Instagram トークン自動リフレッシュバッチを追加。(9) 欠落していた12本の API 詳細仕様を追加（全16本完備）。(10) line_message_logs の config_id FK の ON DELETE RESTRICT を明記。(11) BOT 削除時の連携ユーザーへのプッシュ通知を追加。(12) line_message_logs に user_id カラム追加（非正規化で配信履歴検索を高速化、退会時は SET NULL で匿名化） |
| 2026-03-15 | 精査②: 8件の不整合・欠落を修正。(1) SNS feeds レスポンスの cached_at 重複を削除。(2) セキュリティ節の Webhook 署名検証を200返却に統一（処理フローと整合）。(3) GET /logs レスポンスに user_id を追加。(4) GET /sns/feeds の認可を SUPPORTER 以上に修正（スコープ表と整合）。(5) `sns_feed_configs.token_expires_at` カラム追加（リフレッシュバッチの API 問合せ不要化）。(6) LINE 配信ログパージバッチをビジネスロジック節に追加。(7) POST /line/link の API 仕様配置を LINE グループ内の正しい位置に移動。(8) 複数スコープ連携時の重複送信防止（スコープ最近接ルール: 発生元スコープ優先→上位スコープフォールバック→1通のみ送信） |
| 2026-03-15 | 機能拡張: 7件の改善を追加。【UX】(1) 通知種別ごとの LINE 配信設定（notification_type_preferences テーブル新設を F04.3 に要請）。(2) おやすみ時間（`quiet_start` / `quiet_end` カラム追加 + ダイジェスト送信バッチ）。(3) Instagram OAuth フロー簡略化（`/sns/instagram/oauth/start` + `/callback` API 2本追加）。(4) LINE 一斉配信（`POST /broadcast` API 追加。Multicast API + プレビュー確認 + Quiet Hours 無視）。【保守】(5) LINE API サーキットブレーカー（Resilience4j。OPEN→プッシュ通知フォールバック→自動復帰）。(6) BOT ブロック検出バッチ（週次。Get Profile API で404→is_active=false）。(7) 配信統計ダッシュボード（`GET /stats` API 追加。月間送信数・失敗率・残枠・種別内訳）。API 20本（+4本） |
