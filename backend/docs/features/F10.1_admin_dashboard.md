# F10.1: 管理者ダッシュボード・通報モデレーション

> **ステータス**: 🟡 設計中
> **実装フェーズ**: Phase 10
> **最終更新**: 2026-03-15
> **モジュール種別**: デフォルト機能 #1（ダッシュボード）の管理者向け拡張 + 通報・モデレーション（デフォルト機能 #10）
> **関連ドキュメント**: F10.3 監査ログ（監査証跡の閲覧）、F01.1 認証（ロール・パーミッション基盤）、F04.3 プッシュ通知（通報通知）

---

## 1. 概要

チーム/組織の ADMIN 向け管理画面と、プラットフォーム全体を管理する SYSTEM_ADMIN 向けシステム管理画面を提供する。全管理機能を `/admin`（チーム/組織管理）と `/system-admin`（プラットフォーム管理）に集約し、ユーザー管理・権限設定・モジュール制御・課金管理・通報対応等を一元的に操作できる。

また、不適切コンテンツの通報・レビュー・対応（削除・警告・凍結）のモデレーションワークフローを提供し、プラットフォームの健全性を維持する。

### 本ドキュメントのスコープ

| 機能 | 対象 | 説明 |
|------|------|------|
| 管理者ダッシュボード（AA） | ADMIN / DEPUTY_ADMIN | チーム/組織レベルの管理画面。権限グループ・モジュール ON/OFF・MEMBER 権限調整 |
| システム管理者ダッシュボード（AB） | SYSTEM_ADMIN | プラットフォーム全体の管理。価格設定・テンプレート管理・ユーザーBAN・エラーレポート |
| 通報・モデレーション | 全ロール（通報）/ ADMIN・SYSTEM_ADMIN（対応） | 不適切コンテンツの通報 → レビュー → 段階的対応 |

**ダッシュボードウィジェット（個人/チーム/組織）** は Phase 2 で基盤実装済み。本ドキュメントでは Phase 10 で追加する**管理画面機能**に焦点を当てる。

---

## 2. スコープ

### 対象ロール
| ロール | 操作可能な範囲 |
|--------|--------------|
| SYSTEM_ADMIN | システム管理者ダッシュボード全機能。プラットフォーム全体の管理・設定・監視 |
| ADMIN | 管理者ダッシュボード全機能。チーム/組織内のユーザー管理・権限設定・モジュール管理・通報対応 |
| DEPUTY_ADMIN | ADMIN が権限グループで付与した範囲の管理機能。通報レビュー（権限グループに含まれる場合） |
| MEMBER | コンテンツの通報（投稿）のみ |
| SUPPORTER | コンテンツの通報（投稿）のみ |
| GUEST | 対象外 |

### 対象レベル
- [x] 組織 (Organization) — 組織全体の管理
- [x] チーム (Team) — チーム内の管理
- [ ] 個人 (Personal)

---

## 3. DB設計

### テーブル一覧
| テーブル名 | 役割 | 論理削除 |
|-----------|------|---------|
| `content_reports` | コンテンツ通報（ポリモーフィック） | なし |
| `report_actions` | 通報への対応アクション履歴 | なし |
| `feature_flags` | フィーチャーフラグ（機能の ON/OFF 制御） | なし |
| `maintenance_schedules` | メンテナンス予定（スケジュール告知） | なし |
| `platform_announcements` | 運営からのお知らせ（全ユーザー向け一括通知） | あり |

**既存テーブル（他フェーズで作成済み）の参照**:
- `dashboard_widget_settings`（Phase 2）: ウィジェット表示/非表示・並び順
- `permission_groups` / `permission_group_permissions` / `user_permission_groups`（Phase 1）: DEPUTY_ADMIN 権限グループ
- `team_role_permissions`（Phase 1）: MEMBER 権限調整
- `error_reports`（Phase 2）: エラーレポート
- `audit_logs`（Phase 1、F10.3）: 監査ログ
- `seasonal_themes`（Phase 2）: シーズナル壁紙

### テーブル定義

#### `content_reports`

コンテンツ通報。ポリモーフィック参照で任意のコンテンツを通報対象にできる。

| カラム名 | 型 | NULL | デフォルト | 説明 |
|---------|---|------|-----------|------|
| `id` | BIGINT UNSIGNED | NO | AUTO_INCREMENT | PK |
| `target_type` | VARCHAR(30) | NO | — | 通報対象の種別（`TIMELINE_POST` / `CHAT_MESSAGE` / `BULLETIN_THREAD` / `BULLETIN_COMMENT` / `MATCHING_REQUEST` / `PROPERTY_LISTING` / `USER_PROFILE`） |
| `target_id` | BIGINT UNSIGNED | NO | — | 通報対象の ID |
| `reported_by` | BIGINT UNSIGNED | NO | — | FK → users。通報者 |
| `scope_type` | VARCHAR(20) | NO | — | スコープ（`TEAM` / `ORGANIZATION`） |
| `scope_id` | BIGINT UNSIGNED | NO | — | スコープ ID |
| `reason` | VARCHAR(30) | NO | — | 通報理由（`SPAM`: スパム / `HARASSMENT`: ハラスメント / `INAPPROPRIATE`: 不適切 / `MISINFORMATION`: 誤情報 / `COPYRIGHT`: 著作権侵害 / `OTHER`: その他） |
| `target_user_id` | BIGINT UNSIGNED | YES | NULL | FK → users。通報対象コンテンツの作成者（WARNING / FREEZE の対象。`USER_PROFILE` 通報の場合は対象ユーザー自身） |
| `content_snapshot` | JSON | YES | NULL | 通報時点のコンテンツスナップショット（通報後の編集・削除に備えた証拠保全。タイトル・本文・画像URL等を保存） |
| `description` | TEXT | YES | NULL | 通報の詳細説明（任意。最大1,000文字） |
| `status` | VARCHAR(20) | NO | 'PENDING' | ステータス（`PENDING`: 未対応 / `REVIEWING`: 確認中 / `RESOLVED`: 対応済み / `DISMISSED`: 却下） |
| `reviewed_by` | BIGINT UNSIGNED | YES | NULL | FK → users。レビューした管理者 |
| `reviewed_at` | DATETIME | YES | NULL | レビュー日時 |
| `created_at` | DATETIME | NO | CURRENT_TIMESTAMP | |
| `updated_at` | DATETIME | NO | CURRENT_TIMESTAMP ON UPDATE | |

**インデックス**
```sql
INDEX idx_cr_scope (scope_type, scope_id, status, created_at DESC)  -- スコープ別の通報一覧
INDEX idx_cr_target (target_type, target_id)                         -- 対象別の通報件数
INDEX idx_cr_status (status, created_at DESC)                        -- ステータス別一覧（SYSTEM_ADMIN 用）
INDEX idx_cr_reported_by (reported_by)                               -- 通報者別一覧
UNIQUE KEY uq_cr_target_reporter (target_type, target_id, reported_by)  -- 同一ユーザーによる同一コンテンツの重複通報防止
INDEX idx_cr_target_user (target_user_id, status)                    -- 対象ユーザー別の通報（エスカレーション閾値チェック用）
```

**制約・備考**
- ポリモーフィック参照（FK 制約なし、アプリ層で検証）
- 同一ユーザーが同一コンテンツを複数回通報不可（UNIQUE KEY `uq_cr_target_reporter` で DB 制約）
- `PENDING` → `REVIEWING`（ADMIN がレビュー開始）→ `RESOLVED`（対応実施）/ `DISMISSED`（問題なしとして却下）
- `RESOLVED` への遷移時に `report_actions` にアクションを記録
- SYSTEM_ADMIN は全スコープの通報を閲覧可能。ADMIN はスコープ内の通報のみ
- `target_user_id`: 通報作成時にバックエンドが対象コンテンツの作成者を自動取得して設定。`USER_PROFILE` の場合は `target_id` = `target_user_id`
- `content_snapshot`: 通報作成時にバックエンドが対象コンテンツの現在の状態を JSON で保存。通報後にコンテンツが編集・削除されても ADMIN が元の内容を確認可能
- `scope_type` / `scope_id`: 通報作成時にバックエンドが対象コンテンツのスコープを自動導出（例: タイムライン投稿 → 投稿の team_id / organization_id）。リクエストボディには含めない
- グルーピング解決: `RESOLVED` / `DISMISSED` 遷移時に、同一 `target_type + target_id` を持つ全通報のステータスを一括更新

---

#### `report_actions`

通報への対応アクション履歴。1件の通報に対して複数のアクションを記録可能（警告→削除等の段階的対応）。

| カラム名 | 型 | NULL | デフォルト | 説明 |
|---------|---|------|-----------|------|
| `id` | BIGINT UNSIGNED | NO | AUTO_INCREMENT | PK |
| `report_id` | BIGINT UNSIGNED | NO | — | FK → content_reports。ON DELETE CASCADE |
| `action_type` | VARCHAR(20) | NO | — | アクション種別（`WARNING`: 投稿者への警告 / `CONTENT_DELETE`: コンテンツ削除 / `USER_FREEZE`: ユーザー凍結 / `DISMISS`: 問題なし却下） |
| `action_by` | BIGINT UNSIGNED | NO | — | FK → users。アクション実行者 |
| `note` | TEXT | YES | NULL | 管理者メモ（対応理由等） |
| `created_at` | DATETIME | NO | CURRENT_TIMESTAMP | |

**インデックス**
```sql
INDEX idx_ra_report (report_id, created_at)  -- 通報別のアクション履歴
```

**制約・備考**
- `CONTENT_DELETE` 実行時: 対象コンテンツの論理削除を同一トランザクションで実行
- `USER_FREEZE` 実行時: `users.is_frozen = TRUE` を設定。凍結中ユーザーはログイン不可
- `WARNING` 実行時: 対象ユーザーにプッシュ通知 + メール送信（「コミュニティガイドラインに違反するコンテンツが報告されました」）
- 監査ログに `REPORT_ACTION_TAKEN` イベントを記録

#### `feature_flags`

フィーチャーフラグ。新機能のコードデプロイ後に、ダッシュボードから ON/OFF を制御する。

| カラム名 | 型 | NULL | デフォルト | 説明 |
|---------|---|------|-----------|------|
| `id` | BIGINT UNSIGNED | NO | AUTO_INCREMENT | PK |
| `flag_key` | VARCHAR(100) | NO | — | フラグキー（例: `FEATURE_PARKING_MODULE`）。UNIQUE |
| `is_enabled` | BOOLEAN | NO | FALSE | 有効/無効 |
| `description` | VARCHAR(500) | YES | NULL | フラグの説明（どの機能を制御するか） |
| `updated_by` | BIGINT UNSIGNED | YES | NULL | FK → users。最後に変更した SYSTEM_ADMIN |
| `created_at` | DATETIME | NO | CURRENT_TIMESTAMP | |
| `updated_at` | DATETIME | NO | CURRENT_TIMESTAMP ON UPDATE | |

**インデックス**
```sql
UNIQUE KEY uq_ff_key (flag_key)  -- フラグキーの一意制約
```

**制約・備考**
- フラグの追加は Flyway マイグレーション or `POST /system-admin/feature-flags` で実施
- フラグの値は Redis にキャッシュ（`mannschaft:feature:{key}`、TTL なし）。PUT 時に即座にキャッシュ更新
- API 層は `FeatureFlagService.isEnabled(key)` で判定。無効なフラグキーは `false` を返却
- フラグの物理削除は不可（非活性化のみ。誤って削除するとコード側の参照が壊れるため）

---

#### `maintenance_schedules`

メンテナンス予定。事前告知 + 自動切替を管理する。

| カラム名 | 型 | NULL | デフォルト | 説明 |
|---------|---|------|-----------|------|
| `id` | BIGINT UNSIGNED | NO | AUTO_INCREMENT | PK |
| `title` | VARCHAR(200) | NO | — | メンテナンスタイトル（例: 「定期メンテナンス v2.5.0」） |
| `message` | TEXT | YES | NULL | ユーザー向けメッセージ |
| `mode` | VARCHAR(20) | NO | 'MAINTENANCE' | 切替先モード（`READ_ONLY` / `MAINTENANCE`） |
| `starts_at` | DATETIME | NO | — | メンテナンス開始日時 |
| `ends_at` | DATETIME | NO | — | メンテナンス終了予定日時 |
| `status` | VARCHAR(20) | NO | 'SCHEDULED' | ステータス（`SCHEDULED`: 予定 / `ACTIVE`: 実行中 / `COMPLETED`: 完了 / `CANCELLED`: キャンセル） |
| `created_by` | BIGINT UNSIGNED | NO | — | FK → users。作成した SYSTEM_ADMIN |
| `created_at` | DATETIME | NO | CURRENT_TIMESTAMP | |
| `updated_at` | DATETIME | NO | CURRENT_TIMESTAMP ON UPDATE | |

**インデックス**
```sql
INDEX idx_ms_status (status, starts_at)  -- 予定・実行中のメンテナンス検索
```

**制約・備考**
- `starts_at` < `ends_at`（Service 層バリデーション）
- `SCHEDULED` → `ACTIVE`（開始時刻到達で自動遷移、バッチ1分間隔）→ `COMPLETED`（終了時刻到達で自動遷移 + メンテナンスモード → NORMAL に復帰）
- `SCHEDULED` → `CANCELLED`（手動キャンセル）
- 事前通知: 登録時 + 24時間前 + 1時間前にプッシュ通知 + フロントエンドにバナー表示
- メンテナンスモードの状態は Redis（`mannschaft:maintenance:mode`）で管理。Spring Filter で全リクエストをインターセプト
- SYSTEM_ADMIN の `/system-admin/**` API はメンテナンスモード中も常に利用可能

---

#### `platform_announcements`

運営からのお知らせ。SYSTEM_ADMIN がプラットフォーム全ユーザーに一括通知する。

| カラム名 | 型 | NULL | デフォルト | 説明 |
|---------|---|------|-----------|------|
| `id` | BIGINT UNSIGNED | NO | AUTO_INCREMENT | PK |
| `title` | VARCHAR(200) | NO | — | お知らせタイトル |
| `body` | TEXT | NO | — | 本文（Markdown 対応） |
| `priority` | VARCHAR(10) | NO | 'NORMAL' | 優先度（`URGENT`: 緊急 / `NORMAL`: 通常 / `LOW`: 低優先度） |
| `target_scope` | VARCHAR(20) | NO | 'ALL' | 配信対象（`ALL`: 全ユーザー / `ADMINS_ONLY`: ADMIN 以上のみ） |
| `is_pinned` | BOOLEAN | NO | FALSE | ダッシュボードへの固定表示。TRUE = 全ユーザーのダッシュボード上部に常時表示 |
| `published_at` | DATETIME | YES | NULL | 公開日時。NULL = 下書き |
| `expires_at` | DATETIME | YES | NULL | 表示期限。到達後はダッシュボードから非表示（アーカイブ参照は可能） |
| `created_by` | BIGINT UNSIGNED | NO | — | FK → users。作成した SYSTEM_ADMIN |
| `created_at` | DATETIME | NO | CURRENT_TIMESTAMP | |
| `updated_at` | DATETIME | NO | CURRENT_TIMESTAMP ON UPDATE | |
| `deleted_at` | DATETIME | YES | NULL | 論理削除日時 |

**インデックス**
```sql
INDEX idx_pa_published (published_at DESC, expires_at)     -- 公開中のお知らせ一覧
INDEX idx_pa_priority (priority, published_at DESC)         -- 緊急度別一覧
```

**制約・備考**
- `published_at IS NOT NULL` のレコードのみユーザーに表示。NULL = 下書き状態
- `URGENT` 優先度: 公開時に全ユーザーにプッシュ通知。ダッシュボード上部に赤色バナーで固定表示
- `NORMAL` 優先度: ダッシュボードのお知らせウィジェットに表示。プッシュ通知なし
- `LOW` 優先度: お知らせウィジェットに表示。プッシュ通知なし。ダッシュボードの折りたたみセクションに格納
- `is_pinned = TRUE`: `expires_at` に関わらずダッシュボード上部に常時表示（SYSTEM_ADMIN が手動で解除するまで）
- `target_scope = 'ADMINS_ONLY'`: 運用上の連絡事項（料金改定予告・API 仕様変更等）を ADMIN にのみ配信
- 全ユーザーのダッシュボード API（`GET /dashboard`）のレスポンスに `platform_announcements` 配列を含める

### ER図（テキスト形式）
```
content_reports (1) ──── (N) report_actions
users (1) ──── (N) content_reports（reported_by）
users (1) ──── (N) content_reports（target_user_id）
users (1) ──── (N) report_actions（action_by）
users (1) ──── (N) platform_announcements（created_by）
users (1) ──── (N) maintenance_schedules（created_by）
```

---

## 4. API設計

### エンドポイント一覧

#### 通報・モデレーション
| メソッド | パス | 認証 | 説明 |
|---------|-----|------|------|
| POST | `/api/v1/reports` | 必要 | コンテンツを通報 |
| GET | `/api/v1/admin/reports` | 必要 | 通報一覧（スコープ内。ADMIN） |
| GET | `/api/v1/admin/reports/{id}` | 必要 | 通報詳細（対象コンテンツのスナップショット含む） |
| PATCH | `/api/v1/admin/reports/{id}/review` | 必要 | レビュー開始（PENDING → REVIEWING） |
| PATCH | `/api/v1/admin/reports/{id}/resolve` | 必要 | 対応実施（REVIEWING → RESOLVED） |
| PATCH | `/api/v1/admin/reports/{id}/dismiss` | 必要 | 却下（REVIEWING → DISMISSED） |
| POST | `/api/v1/admin/reports/bulk-resolve` | 必要 | 通報の一括対応（複数通報を同一アクションで処理） |
| GET | `/api/v1/admin/reports/stats` | 必要 | 通報の統計・トレンド（期間別推移・理由別内訳・常習者） |
| GET | `/api/v1/admin/users/{id}/violation-history` | 必要 | ユーザーの違反履歴（過去の通報・警告・凍結） |
| PATCH | `/api/v1/admin/users/{id}/restrict-reporting` | 必要 | 通報権限の一時停止（虚偽通報対策） |
| PATCH | `/api/v1/admin/reports/{id}/restore-content` | 必要 | 誤削除コンテンツの復元（72時間以内） |
| GET | `/api/v1/system-admin/reports` | 必要 | 全通報一覧（SYSTEM_ADMIN） |

#### 管理者ダッシュボード（AA）
| メソッド | パス | 認証 | 説明 |
|---------|-----|------|------|
| GET | `/api/v1/admin/dashboard` | 必要 | 管理者ダッシュボード（通報件数・未対応数・ユーザー統計等のサマリー） |
| GET | `/api/v1/admin/users` | 必要 | スコープ内ユーザー一覧 |
| PATCH | `/api/v1/admin/users/{id}/role` | 必要 | ユーザーロール変更 |
| GET | `/api/v1/admin/permission-groups` | 必要 | 権限グループ一覧 |
| POST | `/api/v1/admin/permission-groups` | 必要 | 権限グループ作成 |
| GET | `/api/v1/admin/permission-groups/{id}` | 必要 | 権限グループ詳細 |
| PUT | `/api/v1/admin/permission-groups/{id}` | 必要 | 権限グループ更新 |
| DELETE | `/api/v1/admin/permission-groups/{id}` | 必要 | 権限グループ削除 |
| POST | `/api/v1/admin/permission-groups/{id}/duplicate` | 必要 | 権限グループ複製 |
| PUT | `/api/v1/admin/permission-groups/{id}/assign/{userId}` | 必要 | ユーザーへのグループ割り当て |
| DELETE | `/api/v1/admin/permission-groups/{id}/assign/{userId}` | 必要 | ユーザーからのグループ割り当て解除 |
| GET | `/api/v1/admin/member-permissions` | 必要 | MEMBER のデフォルト権限一覧（team_role_permissions） |
| PUT | `/api/v1/admin/member-permissions` | 必要 | MEMBER のデフォルト権限更新（ON/OFF 切替） |
| GET | `/api/v1/admin/modules` | 必要 | モジュール有効/無効一覧 |
| PUT | `/api/v1/admin/modules/{moduleId}` | 必要 | モジュール ON/OFF 切替 |

#### システム管理者ダッシュボード（AB）
| メソッド | パス | 認証 | 説明 |
|---------|-----|------|------|
| GET | `/api/v1/system-admin/dashboard` | 必要 | システム管理者ダッシュボード（プラットフォーム統計） |
| GET | `/api/v1/system-admin/organizations` | 必要 | 全組織一覧 |
| GET | `/api/v1/system-admin/teams` | 必要 | 全チーム一覧 |
| GET | `/api/v1/system-admin/users` | 必要 | 全ユーザー一覧 |
| PATCH | `/api/v1/system-admin/users/{id}/freeze` | 必要 | ユーザー凍結 |
| PATCH | `/api/v1/system-admin/users/{id}/unfreeze` | 必要 | ユーザー凍結解除 |
| GET | `/api/v1/system-admin/module-prices` | 必要 | モジュール価格一覧 |
| PUT | `/api/v1/system-admin/module-prices/{id}` | 必要 | モジュール価格設定 |
| GET | `/api/v1/system-admin/packages` | 必要 | パッケージ一覧 |
| POST | `/api/v1/system-admin/packages` | 必要 | パッケージ作成 |
| PUT | `/api/v1/system-admin/packages/{id}` | 必要 | パッケージ更新 |
| DELETE | `/api/v1/system-admin/packages/{id}` | 必要 | パッケージ削除 |
| GET | `/api/v1/system-admin/discount-campaigns` | 必要 | 割引キャンペーン一覧 |
| POST | `/api/v1/system-admin/discount-campaigns` | 必要 | 割引キャンペーン作成 |
| PUT | `/api/v1/system-admin/discount-campaigns/{id}` | 必要 | 割引キャンペーン更新 |
| DELETE | `/api/v1/system-admin/discount-campaigns/{id}` | 必要 | 割引キャンペーン削除 |
| GET | `/api/v1/system-admin/discount-campaigns/{id}/usages` | 必要 | クーポン利用状況 |
| GET | `/api/v1/system-admin/tax-settings` | 必要 | 消費税設定取得 |
| PUT | `/api/v1/system-admin/tax-settings` | 必要 | 消費税設定更新 |
| GET | `/api/v1/system-admin/storage-plans` | 必要 | ストレージプラン一覧 |
| POST | `/api/v1/system-admin/storage-plans` | 必要 | ストレージプラン作成 |
| PUT | `/api/v1/system-admin/storage-plans/{id}` | 必要 | ストレージプラン更新 |
| DELETE | `/api/v1/system-admin/storage-plans/{id}` | 必要 | ストレージプラン削除 |
| GET | `/api/v1/system-admin/storage-usage` | 必要 | 全チームのストレージ使用状況 |
| GET | `/api/v1/system-admin/org-count-billing-tiers` | 必要 | 組織数課金設定 |
| PUT | `/api/v1/system-admin/org-count-billing-tiers` | 必要 | 組織数課金設定更新 |
| GET | `/api/v1/system-admin/org-count-billing/overview` | 必要 | 組織数課金の一覧状況 |
| GET | `/api/v1/system-admin/org-type-change-requests` | 必要 | 組織種別変更申請一覧 |
| PATCH | `/api/v1/system-admin/org-type-change-requests/{id}` | 必要 | 組織種別変更の承認/却下 |
| GET | `/api/v1/system-admin/seasonal-themes` | 必要 | シーズナル壁紙一覧 |
| POST | `/api/v1/system-admin/seasonal-themes` | 必要 | シーズナル壁紙作成 |
| PUT | `/api/v1/system-admin/seasonal-themes/{id}` | 必要 | シーズナル壁紙更新 |
| DELETE | `/api/v1/system-admin/seasonal-themes/{id}` | 必要 | シーズナル壁紙削除 |
| GET | `/api/v1/system-admin/error-reports` | 必要 | エラーレポート一覧 |
| PATCH | `/api/v1/system-admin/error-reports/{id}/status` | 必要 | エラーレポートステータス更新 |
| GET | `/api/v1/system-admin/affiliate-configs` | 必要 | アフィリエイト設定一覧 |
| PUT | `/api/v1/system-admin/affiliate-configs` | 必要 | アフィリエイト設定更新 |
| GET | `/api/v1/system-admin/modules/level-settings` | 必要 | モジュールのレベル別適用設定 |
| PUT | `/api/v1/system-admin/modules/level-settings/{moduleId}` | 必要 | モジュールのレベル別適用 ON/OFF |
| GET | `/api/v1/system-admin/role-permissions` | 必要 | ロール権限の上限設定一覧 |
| PUT | `/api/v1/system-admin/role-permissions` | 必要 | ロール権限の上限設定更新 |
| GET | `/api/v1/system-admin/moderation-settings` | 必要 | モデレーション設定（エスカレーション閾値等） |
| PUT | `/api/v1/system-admin/moderation-settings` | 必要 | モデレーション設定更新 |
| GET | `/api/v1/system-admin/module-usage-stats` | 必要 | モジュール利用状況分析（有効化チーム数・利用率） |
| GET | `/api/v1/system-admin/feature-flags` | 必要 | フィーチャーフラグ一覧 |
| POST | `/api/v1/system-admin/feature-flags` | 必要 | フィーチャーフラグ作成 |
| PUT | `/api/v1/system-admin/feature-flags/{key}` | 必要 | フィーチャーフラグ ON/OFF 切替 |
| PUT | `/api/v1/system-admin/maintenance-mode` | 必要 | メンテナンスモード即時切替 |
| GET | `/api/v1/system-admin/maintenance-schedules` | 必要 | メンテナンス予定一覧 |
| POST | `/api/v1/system-admin/maintenance-schedules` | 必要 | メンテナンス予定登録 |
| DELETE | `/api/v1/system-admin/maintenance-schedules/{id}` | 必要 | メンテナンス予定キャンセル |
| GET | `/api/v1/system-admin/announcements` | 必要 | 運営お知らせ一覧（下書き含む） |
| POST | `/api/v1/system-admin/announcements` | 必要 | 運営お知らせ作成 |
| PUT | `/api/v1/system-admin/announcements/{id}` | 必要 | 運営お知らせ更新 |
| DELETE | `/api/v1/system-admin/announcements/{id}` | 必要 | 運営お知らせ削除（論理削除） |
| PATCH | `/api/v1/system-admin/announcements/{id}/publish` | 必要 | 運営お知らせ公開（published_at 設定） |
| PATCH | `/api/v1/system-admin/announcements/{id}/unpin` | 必要 | 固定表示解除 |
| GET | `/api/v1/system-admin/data-export-requests` | 必要 | データエクスポートリクエスト一覧（監視用） |
| POST | `/api/v1/users/me/data-export` | 必要 | 自分のデータエクスポートリクエスト |
| GET | `/api/v1/users/me/data-export/{jobId}` | 必要 | エクスポートステータス確認 + ダウンロード |
| GET | `/api/v1/platform/announcements` | 必要 | 公開中の運営お知らせ一覧（全ユーザー） |

### リクエスト／レスポンス仕様

#### `POST /api/v1/reports`

**認可**: MEMBER 以上

コンテンツを通報する。

**リクエストボディ**
```json
{
  "target_type": "TIMELINE_POST",
  "target_id": 123,
  "reason": "HARASSMENT",
  "description": "特定のメンバーへの誹謗中傷を含む投稿です"
}
```

**レスポンス（201 Created）**
```json
{
  "data": {
    "id": 1,
    "target_type": "TIMELINE_POST",
    "target_id": 123,
    "reason": "HARASSMENT",
    "status": "PENDING",
    "created_at": "2026-04-01T10:00:00+09:00"
  }
}
```

**副作用**: スコープの ADMIN にプッシュ通知（「新しい通報があります」）

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 400 | バリデーションエラー / 対象コンテンツが存在しない |
| 403 | スコープメンバーでない |
| 409 | 同一コンテンツを既に通報済み |

---

#### `GET /api/v1/admin/reports`

**認可**: ADMIN / DEPUTY_ADMIN（権限グループに含まれる場合）

スコープ内の通報一覧を取得する。

**クエリパラメータ**
| パラメータ | 型 | 必須 | 説明 |
|-----------|---|------|------|
| `scope_type` | String | Yes | `TEAM` / `ORGANIZATION` |
| `scope_id` | Long | Yes | 対象スコープの ID |
| `status` | String | No | `PENDING` / `REVIEWING` / `RESOLVED` / `DISMISSED`（デフォルト: 全件） |
| `reason` | String | No | 通報理由フィルタ |
| `page` | Integer | No | ページ番号（デフォルト: 0） |
| `size` | Integer | No | 取得件数（デフォルト: 20） |

**レスポンス（200 OK）**
```json
{
  "data": [
    {
      "id": 1,
      "target_type": "TIMELINE_POST",
      "target_id": 123,
      "reason": "HARASSMENT",
      "status": "PENDING",
      "reported_by": { "id": 50, "display_name": "通報者A" },
      "report_count": 3,
      "created_at": "2026-04-01T10:00:00+09:00"
    }
  ],
  "meta": { "page": 0, "size": 20, "total_elements": 5 }
}
```

- `report_count`: 同一コンテンツへの通報件数（複数ユーザーが同じコンテンツを通報した場合にグルーピング）

---

#### `GET /api/v1/admin/reports/{id}`

**認可**: ADMIN / DEPUTY_ADMIN（権限グループに含まれる場合）

通報の詳細を取得する。対象コンテンツのスナップショットと対応履歴を含む。

**レスポンス（200 OK）**
```json
{
  "data": {
    "id": 1,
    "target_type": "TIMELINE_POST",
    "target_id": 123,
    "target_user": { "id": 99, "display_name": "投稿者X" },
    "reason": "HARASSMENT",
    "description": "特定のメンバーへの誹謗中傷を含む投稿です",
    "content_snapshot": {
      "title": null,
      "body": "○○さんは...",
      "created_at": "2026-03-30T15:00:00+09:00"
    },
    "status": "REVIEWING",
    "reported_by": { "id": 50, "display_name": "通報者A" },
    "related_reports": [
      { "id": 2, "reported_by": { "id": 51, "display_name": "通報者B" }, "reason": "INAPPROPRIATE" }
    ],
    "actions": [],
    "reviewed_by": { "id": 10, "display_name": "管理者 佐藤" },
    "reviewed_at": "2026-04-01T11:00:00+09:00",
    "created_at": "2026-04-01T10:00:00+09:00"
  }
}
```

- `content_snapshot`: 通報時点のコンテンツ。構造は `target_type` によって異なる
- `related_reports`: 同一 `target_type + target_id` に対する他の通報（グルーピング表示用）
- `actions`: この通報に対して実行されたアクション履歴（`report_actions`）

---

#### `PATCH /api/v1/admin/reports/{id}/review`

**認可**: ADMIN / DEPUTY_ADMIN（権限グループに含まれる場合）

通報のレビューを開始する（PENDING → REVIEWING）。

**リクエストボディ**: なし

**レスポンス（200 OK）**: 通報詳細（`status: "REVIEWING"`、`reviewed_by` / `reviewed_at` 設定済み）

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 403 | 権限不足 |
| 404 | 通報が存在しない |
| 409 | `PENDING` 以外のステータス |

---

#### `PATCH /api/v1/admin/reports/{id}/dismiss`

**認可**: ADMIN / DEPUTY_ADMIN（権限グループに含まれる場合）

通報を問題なしとして却下する（REVIEWING → DISMISSED）。

**リクエストボディ**
```json
{
  "note": "確認の結果、ガイドライン違反には該当しないと判断"
}
```

**レスポンス（200 OK）**: 通報詳細（`status: "DISMISSED"`）

**副作用**:
1. `report_actions` に `DISMISS` アクションを INSERT
2. 同一 `target_type + target_id` の全通報を一括 `DISMISSED` に遷移
3. 監査ログ: `REPORT_ACTION_TAKEN`（action_type=DISMISS）を記録

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 403 | 権限不足 |
| 404 | 通報が存在しない |
| 409 | `REVIEWING` 以外のステータス |

---

#### `PATCH /api/v1/admin/reports/{id}/resolve`

**認可**: ADMIN / DEPUTY_ADMIN（権限グループに含まれる場合）

通報に対してアクションを実施し、解決済みにする。

**リクエストボディ**
```json
{
  "action_type": "CONTENT_DELETE",
  "note": "コミュニティガイドライン違反を確認。コンテンツを削除しました"
}
```

- `action_type`: `WARNING`（警告）/ `CONTENT_DELETE`（コンテンツ削除）/ `USER_FREEZE`（ユーザー凍結。**SYSTEM_ADMIN のみ**）
- `note`: 任意。対応理由の記録

**レスポンス（200 OK）**: 通報詳細（`status: "RESOLVED"`）

**副作用**:
1. `report_actions` にアクション履歴を INSERT
2. `CONTENT_DELETE`: 対象コンテンツ（`content_snapshot` で保全済み）を論理削除
3. `USER_FREEZE`: 対象ユーザー（`target_user_id`）の `is_frozen = TRUE` を設定。Redis トークン無効化で即座にセッション切断
4. `WARNING`: 対象ユーザー（`target_user_id`）にプッシュ通知 + メール送信
5. 通報者にプッシュ通知（「ご報告いただいたコンテンツに対応しました」）
6. 同一 `target_type + target_id` の全通報を一括 `RESOLVED` に遷移
7. 監査ログ: `REPORT_ACTION_TAKEN` を記録

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 403 | 権限不足 / `USER_FREEZE` を ADMIN / DEPUTY_ADMIN が実行（SYSTEM_ADMIN のみ） |
| 404 | 通報が存在しない |
| 409 | `REVIEWING` 以外のステータス |

---

#### `POST /api/v1/admin/reports/bulk-resolve`

**認可**: ADMIN / DEPUTY_ADMIN（権限グループに含まれる場合）

複数の通報を同一アクションで一括処理する。

**リクエストボディ**
```json
{
  "report_ids": [1, 2, 3, 5, 8],
  "action_type": "CONTENT_DELETE",
  "note": "スパム投稿を一括削除"
}
```

- `report_ids`: 対象の通報 ID 配列（最大50件）
- `action_type` / `note`: 全通報に同一のアクションを適用
- 各通報のステータスは `PENDING` or `REVIEWING` のみ対象（`RESOLVED` / `DISMISSED` はスキップ）

**レスポンス（200 OK）**
```json
{
  "data": {
    "resolved_count": 5,
    "skipped_count": 0
  }
}
```

**副作用**: 各通報に対して `PATCH /resolve` と同等の処理を実行（report_actions INSERT・コンテンツ削除・通知・監査ログ）

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 400 | `report_ids` が空 / 50件超過 |
| 403 | 権限不足 / `USER_FREEZE` を ADMIN が実行 |

---

#### `GET /api/v1/admin/reports/stats`

**認可**: ADMIN / DEPUTY_ADMIN

通報の統計・トレンドを取得する。

**クエリパラメータ**
| パラメータ | 型 | 必須 | 説明 |
|-----------|---|------|------|
| `scope_type` | String | Yes | `TEAM` / `ORGANIZATION` |
| `scope_id` | Long | Yes | 対象スコープの ID |
| `period` | String | No | `WEEK` / `MONTH` / `QUARTER`（デフォルト: `MONTH`） |

**レスポンス（200 OK）**
```json
{
  "data": {
    "total_reports": 45,
    "by_status": {
      "pending": 3,
      "reviewing": 1,
      "resolved": 32,
      "dismissed": 9
    },
    "by_reason": {
      "SPAM": 18,
      "HARASSMENT": 12,
      "INAPPROPRIATE": 8,
      "OTHER": 7
    },
    "by_target_type": {
      "TIMELINE_POST": 20,
      "CHAT_MESSAGE": 15,
      "BULLETIN_THREAD": 10
    },
    "trend": [
      { "date": "2026-03-01", "count": 12 },
      { "date": "2026-03-08", "count": 8 },
      { "date": "2026-03-15", "count": 15 },
      { "date": "2026-03-22", "count": 10 }
    ],
    "repeat_offenders": [
      { "user_id": 99, "display_name": "ユーザーX", "report_count": 5 }
    ]
  }
}
```

- `trend`: 期間内の週別通報件数推移
- `repeat_offenders`: 同一 `target_user_id` で通報が2件以上のユーザー（上位5名）

---

#### `GET /api/v1/admin/users/{id}/violation-history`

**認可**: ADMIN / DEPUTY_ADMIN（権限グループに含まれる場合）

ユーザーの違反履歴を時系列で取得する。通報レビュー時の判断材料として使用。

**レスポンス（200 OK）**
```json
{
  "data": {
    "user_id": 99,
    "display_name": "ユーザーX",
    "total_reports_against": 8,
    "total_warnings": 2,
    "total_content_deletions": 3,
    "is_frozen": false,
    "history": [
      {
        "type": "REPORT",
        "report_id": 15,
        "target_type": "TIMELINE_POST",
        "reason": "HARASSMENT",
        "status": "RESOLVED",
        "action_type": "WARNING",
        "created_at": "2026-03-01T10:00:00+09:00"
      },
      {
        "type": "REPORT",
        "report_id": 22,
        "target_type": "CHAT_MESSAGE",
        "reason": "SPAM",
        "status": "RESOLVED",
        "action_type": "CONTENT_DELETE",
        "created_at": "2026-03-15T14:00:00+09:00"
      }
    ]
  }
}
```

- `history`: 対象ユーザーに対する全通報 + アクションを時系列降順で表示
- 全スコープの通報を含む（SYSTEM_ADMIN の場合。ADMIN はスコープ内のみ）

---

#### `GET /api/v1/system-admin/module-usage-stats`

**認可**: SYSTEM_ADMIN

プラットフォーム全体のモジュール利用状況を取得する。

**レスポンス（200 OK）**
```json
{
  "data": {
    "modules": [
      {
        "module_id": 1,
        "module_name": "QR会員証",
        "enabled_teams": 280,
        "enabled_rate": 87.5,
        "is_default": false
      },
      {
        "module_id": 9,
        "module_name": "住民台帳",
        "enabled_teams": 15,
        "enabled_rate": 4.7,
        "is_default": false
      }
    ],
    "packages": [
      {
        "package_id": 1,
        "package_name": "スポーツ基本パック",
        "active_subscriptions": 45
      }
    ],
    "total_teams": 320
  }
}
```

- 価格設定・パッケージ構成の意思決定に活用

---

#### `GET /api/v1/admin/dashboard`

**認可**: ADMIN / DEPUTY_ADMIN

管理者ダッシュボードのサマリーデータを取得する。

**クエリパラメータ**
| パラメータ | 型 | 必須 | 説明 |
|-----------|---|------|------|
| `scope_type` | String | Yes | `TEAM` / `ORGANIZATION` |
| `scope_id` | Long | Yes | 対象スコープの ID |

**レスポンス（200 OK）**
```json
{
  "data": {
    "member_stats": {
      "total_members": 150,
      "active_members": 142,
      "new_members_this_month": 5
    },
    "report_stats": {
      "pending_count": 3,
      "reviewing_count": 1,
      "resolved_this_month": 12
    },
    "module_stats": {
      "enabled_modules": 8,
      "total_available": 11
    },
    "billing_summary": {
      "current_month_total": 15000,
      "module_billing": 8000,
      "package_billing": 5000,
      "storage_billing": 2000,
      "next_billing_date": "2026-05-01",
      "currency": "JPY"
    },
    "moderator_stats": [
      { "user_id": 10, "display_name": "管理者 佐藤", "resolved_count": 8, "dismissed_count": 2 },
      { "user_id": 11, "display_name": "副管理者 鈴木", "resolved_count": 3, "dismissed_count": 1 }
    ],
    "recent_audit_events": [
      {
        "event_type": "USER_FROZEN",
        "user": { "id": 99, "display_name": "凍結ユーザー" },
        "created_at": "2026-04-01T09:00:00+09:00"
      }
    ]
  }
}
```

---

#### `GET /api/v1/system-admin/dashboard`

**認可**: SYSTEM_ADMIN

プラットフォーム全体のサマリーデータを取得する。

**レスポンス（200 OK）**
```json
{
  "data": {
    "platform_stats": {
      "total_organizations": 50,
      "total_teams": 320,
      "total_users": 15000,
      "active_users_this_month": 8500
    },
    "billing_stats": {
      "mrr": 450000,
      "active_subscriptions": 280,
      "overdue_count": 3
    },
    "report_stats": {
      "pending_count": 15,
      "resolved_this_month": 45
    },
    "error_report_stats": {
      "unresolved_count": 8,
      "resolved_this_month": 20,
      "oldest_unresolved_hours": 36
    },
    "storage_stats": {
      "total_used_gb": 1250.5,
      "teams_over_quota": 12
    }
  }
}
```

---

## 5. ビジネスロジック

### 通報・モデレーションワークフロー

```
1. ユーザーが不適切コンテンツを発見 → POST /reports で通報
   a. 通報理由を選択（SPAM / HARASSMENT / INAPPROPRIATE / MISINFORMATION / COPYRIGHT / OTHER）
   b. 詳細説明を任意で記入
   c. スコープの ADMIN にプッシュ通知

2. ADMIN が通報を確認
   a. GET /admin/reports で未対応の通報一覧を確認
   b. 同一コンテンツへの複数通報はグルーピング表示（report_count）
   c. PATCH /admin/reports/{id}/review でレビュー開始（PENDING → REVIEWING）

3. ADMIN が対応を決定
   a. 対象コンテンツを確認（通報詳細にスナップショットを含む）
   b. 段階的対応:
      - WARNING: 投稿者に警告通知。コンテンツはそのまま
      - CONTENT_DELETE: コンテンツを論理削除
      - USER_FREEZE: ユーザーを凍結（ログイン不可）。SYSTEM_ADMIN のみ実行可能
      - DISMISS: 問題なしとして却下
   c. PATCH /admin/reports/{id}/resolve で対応実施
   d. 通報者に「対応しました」通知

4. エスカレーション
   a. ADMIN が対応困難な場合: SYSTEM_ADMIN に報告（通報の status を REVIEWING のまま維持）
   b. SYSTEM_ADMIN が GET /system-admin/reports で全プラットフォームの通報を横断確認
   c. USER_FREEZE は SYSTEM_ADMIN のみ実行可能
```

### 権限グループ管理フロー

```
1. ADMIN が権限グループを作成
   a. グループ名・説明を入力（例: 「受付・安否確認担当」）
   b. パーミッションを個別に ON/OFF（SYSTEM_ADMIN が定めた上限の範囲内）
   c. コンテンツ削除パーミッション（CONTENT_DELETE）はデフォルト OFF

2. DEPUTY_ADMIN にグループを割り当て
   a. PUT /admin/permission-groups/{id}/assign/{userId}
   b. 1ユーザーに複数グループを割り当て可能
   c. グループの OR 結合でパーミッションが決定

3. グループの複製・テンプレート化
   a. POST /admin/permission-groups/{id}/duplicate で既存グループをコピー
   b. 「グループ1 のコピー」として作成 → 名前・パーミッションを調整
```

### SYSTEM_ADMIN の課金管理フロー

```
1. モジュール価格設定
   a. PUT /system-admin/module-prices/{id} で月額・年額を設定
   b. 変更は翌請求サイクルから適用

2. パッケージ管理
   a. POST /system-admin/packages でモジュールの組み合わせパッケージを作成
   b. パッケージ価格は個別合計より割引

3. 割引キャンペーン
   a. POST /system-admin/discount-campaigns で期間限定割引を作成
   b. クーポンコード発行（SecureRandom 生成）
   c. 利用状況は GET /discount-campaigns/{id}/usages で確認

4. 組織数課金設定
   a. PUT /system-admin/org-count-billing-tiers で無料枠・超過単価を設定
   b. GET /system-admin/org-count-billing/overview で各組織の課金状況を一覧確認
```

### デプロイ・リリース管理フロー

```
1. フィーチャーフラグによる段階的リリース
   a. 開発チームが新機能のコードをデプロイ（フラグ OFF の状態でリリース）
   b. SYSTEM_ADMIN が PUT /system-admin/feature-flags/{key} で ON に切替
   c. 問題発生時: 即座に OFF に切替（コードのロールバック不要）
   d. 安定確認後: コードからフラグ分岐を削除（次回デプロイで恒久化）

2. メンテナンスを伴うデプロイ
   a. SYSTEM_ADMIN が POST /maintenance-schedules でメンテナンス予定を登録
   b. 登録時 + 24時間前 + 1時間前にユーザーへ事前通知
   c. 開始時刻に自動でメンテナンスモード切替（バッチ1分間隔で監視）
   d. デプロイ作業実施
   e. 終了時刻に自動で NORMAL 復帰（or SYSTEM_ADMIN が手動で即時復帰）

3. 緊急メンテナンス（スケジュールなし）
   a. SYSTEM_ADMIN が PUT /maintenance-mode で即時切替
   b. mode: READ_ONLY（DB マイグレーション中）or MAINTENANCE（全面停止）
   c. 復旧後に PUT /maintenance-mode で NORMAL に切替

4. デプロイ後の異常検知
   a. @Scheduled 5分間隔で error_reports の直近5分間の件数をチェック
   b. 閾値（デフォルト: 5分間に10件以上）超過 → SYSTEM_ADMIN にプッシュ通知 + Webhook
   c. 閾値は moderation-settings で変更可能
```

### 運営からのお知らせフロー

```
1. SYSTEM_ADMIN が POST /system-admin/announcements でお知らせを作成（下書き状態）
   a. タイトル・本文・優先度・配信対象を設定
   b. is_pinned = TRUE でダッシュボード固定表示
   c. expires_at で自動非表示期限を設定

2. SYSTEM_ADMIN が PATCH /announcements/{id}/publish で公開
   a. published_at = NOW() を設定
   b. URGENT の場合: 全ユーザーにプッシュ通知（「運営からの重要なお知らせがあります」）
   c. NORMAL / LOW の場合: プッシュ通知なし（ダッシュボードのお知らせウィジェットに表示のみ）
   d. target_scope = 'ADMINS_ONLY' の場合: ADMIN 以上にのみ配信

3. ユーザーのダッシュボードに表示
   a. GET /dashboard のレスポンスに platform_announcements 配列を含める
   b. is_pinned = TRUE のお知らせはダッシュボード上部に常時表示
   c. expires_at 到達後はお知らせウィジェットから非表示（GET /platform/announcements で過去分を参照可能）

4. GET /platform/announcements でユーザーが過去のお知らせを一覧確認
```

### 虚偽通報対策フロー

```
1. 通報が DISMISSED されるたびに、通報者の却下率を再計算
   a. 却下率 = DISMISSED 件数 / 全通報件数（通報者ごと）
   b. GET /admin/reports の一覧に reporter_dismiss_rate を表示

2. 自動制限:
   a. 直近30日で DISMISSED が 5件以上 → 通報レートを 1日1件に自動制限
   b. 却下率 70% 以上 かつ 通報件数 5件以上 → 「要注意通報者」フラグ自動付与

3. 手動制限:
   a. ADMIN が PATCH /admin/users/{id}/restrict-reporting で通報権限を一時停止
   b. リクエスト: { "restricted_until": "2026-05-01" }（期間指定。最大90日）
   c. 制限中の通報は 403 エラー

4. users テーブルに reporting_restricted_until DATETIME nullable を追加
   - NULL = 制限なし。日時が未来の場合は通報不可
```

### 誤削除コンテンツ復元フロー

```
1. ADMIN が CONTENT_DELETE を誤って実行した場合
2. 72時間以内に PATCH /admin/reports/{id}/restore-content を実行
3. 処理:
   a. 対象コンテンツの deleted_at = NULL に復元
   b. 通報のステータスを REVIEWING に差し戻し
   c. report_actions に CONTENT_RESTORE アクションを INSERT
   d. 監査ログに CONTENT_RESTORED を記録
4. 72時間超過: 復元不可（content_snapshot で内容参照のみ可能）
```

### ユーザーデータエクスポートフロー

```
1. ユーザーが POST /users/me/data-export でリクエスト送信
2. バックエンドが非同期ジョブとしてキューに投入
3. ジョブが以下のデータを JSON ファイルとして生成:
   a. プロフィール情報（氏名・メール・住所等）
   b. 投稿一覧（タイムライン・掲示板・チャット）
   c. 投票・委任履歴
   d. 居住者情報（紐付いている場合）
   e. 通報履歴（自分が通報した/されたもの）
   f. 画像は URL リストのみ（バイナリは含まない）
4. ZIP に圧縮して S3 に保存
5. 完了後にプッシュ通知 + メール（ダウンロードリンク。Pre-signed URL、24時間有効）
6. GET /users/me/data-export/{jobId} でステータス確認 + ダウンロード
7. レートリミット: 1ユーザーにつき30日に1回
```

### 通報アーカイブバッチ

```
1. @Scheduled で月次実行（毎月1日 AM 3:00）
2. 対象: content_reports の status = 'RESOLVED' OR 'DISMISSED' かつ updated_at < NOW() - INTERVAL 6 MONTH
3. 処理:
   a. 対象の content_reports + 紐付く report_actions を content_reports_archive / report_actions_archive に INSESRT
   b. 移行完了後に元テーブルから DELETE
4. アーカイブテーブルは content_reports / report_actions と同一スキーマ
5. 統計 API（GET /admin/reports/stats）はアーカイブテーブルも UNION で参照（期間指定が6ヶ月以上前の場合）
6. バッチエラー時: ERROR ログ出力 + 次回実行で再試行
```

### 重要な判定ロジック

- **通報のグルーピング**: 同一 `target_type + target_id` への複数通報は UI 上でグルーピング表示。ADMIN は最初の通報を起点に対応し、関連通報は一括で `RESOLVED` に遷移
- **USER_FREEZE の権限制限**: ユーザー凍結は SYSTEM_ADMIN のみ実行可能。ADMIN / DEPUTY_ADMIN はスコープ内のコンテンツ削除・警告まで
- **DEPUTY_ADMIN のコンテンツ削除**: 権限グループで `CONTENT_DELETE` パーミッションが明示的に ON の場合のみ。デフォルト OFF
- **エスカレーション判断**: 同一ユーザー（`target_user_id`）に対する `PENDING` + `REVIEWING` 状態の通報が N 件以上蓄積した場合、SYSTEM_ADMIN に自動通知（常習的な違反の可能性）。N のデフォルト値は 3。SYSTEM_ADMIN が `PUT /system-admin/moderation-settings` で変更可能。`RESOLVED` / `DISMISSED` の通報はカウント対象外（対応済みの件で繰り返しエスカレーションしないため）
- **ADMIN の自己降格防止**: `PATCH /admin/users/{id}/role` でスコープ内の最後の ADMIN が自分自身を MEMBER / SUPPORTER に降格することを禁止（409 エラー）。管理者不在によるロック状態を防止
- **通報者の匿名性**: 通報者の情報は対象ユーザーには開示しない。ADMIN / SYSTEM_ADMIN のみ閲覧可能
- **凍結ユーザーの即座無効化**: `USER_FREEZE` 実行時、Redis のトークン無効化リストに追加し、既存セッションを即座に無効化
- **価格変更の適用タイミング**: モジュール価格・パッケージ価格・組織数課金の変更は翌請求サイクルから適用。現在の請求期間中は旧価格が維持される
- **モジュールレベル別適用**: SYSTEM_ADMIN が各モジュールの組織/チーム/個人レベルでの利用可否を ON/OFF で制御。OFF にしたレベルでは API アクセスも拒否
- **一括対応**: `POST /admin/reports/bulk-resolve` で最大50件を同一 `action_type` で一括処理。各通報のステータスが `PENDING` / `REVIEWING` でなければスキップ（エラーにならず skipped_count に加算）
- **違反履歴の参照範囲**: ADMIN はスコープ内の通報のみ、SYSTEM_ADMIN は全スコープの通報を含む違反履歴を返却
- **通報アーカイブ**: 対応済み（`RESOLVED` / `DISMISSED`）の通報は6ヶ月後にアーカイブテーブルに移行。統計 API は必要に応じてアーカイブテーブルも参照
- **フィーチャーフラグ**: Redis キャッシュで高速参照。PUT 時に即座にキャッシュ更新。フラグの物理削除は不可（コード側の参照保護）
- **メンテナンスモード**: Redis で状態管理。Spring Filter で全リクエストをインターセプト。SYSTEM_ADMIN の `/system-admin/**` は常にアクセス可能
- **メンテナンス予告**: 登録時 + 24時間前 + 1時間前 にリマインド通知。フロントエンドに予告バナーを開始24時間前から表示
- **運営お知らせ**: `URGENT` は公開時にプッシュ通知。`is_pinned` はダッシュボード上部に常時固定。`target_scope = 'ADMINS_ONLY'` で ADMIN 限定配信
- **虚偽通報対策**: 却下率 70%+ かつ 通報5件+ で「要注意」フラグ。直近30日で DISMISSED 5件以上で自動レート制限（1日1件）。ADMIN が手動で通報権限を最大90日停止可能
- **コンテンツ復元**: `CONTENT_DELETE` 後72時間以内のみ復元可能。ステータスを `REVIEWING` に差し戻し
- **デプロイ後異常検知**: 5分間のエラーレポート件数が閾値（デフォルト10件）を超えた場合に SYSTEM_ADMIN に自動アラート + Webhook
- **データエクスポート**: 非同期ジョブ。30日に1回制限。ZIP（JSON）を S3 に保存、Pre-signed URL で24時間ダウンロード可能
- **SYSTEM_ADMIN 最低2人体制**: SYSTEM_ADMIN が1名のみの場合にダッシュボードに警告表示。重要操作時に外部 Webhook（Slack）に通知

---

## 6. セキュリティ考慮事項

- **認可チェック**: `/admin/**` は ADMIN / DEPUTY_ADMIN（権限グループ依存）のみ。`/system-admin/**` は SYSTEM_ADMIN のみ。全エンドポイントでロール + スコープメンバーシップを検証
- **SYSTEM_ADMIN 権限の厳格管理**: SYSTEM_ADMIN の作成は DB 直接操作 or 初期シードのみ。API からの昇格は不可
- **通報者の匿名保護**: 通報者の `user_id` / `display_name` は対象ユーザーの API レスポンスに含めない。ADMIN 管理画面でのみ表示
- **コンテンツ削除の証跡保全**: 論理削除のみ（物理削除しない）。削除されたコンテンツは監査ログ + `report_actions` で追跡可能
- **凍結の即時性**: `USER_FREEZE` 時に Redis のトークン無効化リストに追加。次回 API アクセス時に 401 を返却
- **レートリミット**: `Bucket4j` で以下の制限を適用
  - 通報 API（`POST /reports`）: 1分間5件（通報スパム防止）
  - 一括対応 API（`POST /admin/reports/bulk-resolve`）: 1分間3件
  - フィーチャーフラグ切替: 1分間10件
  - データエクスポート: 30日に1件/ユーザー
  - 運営お知らせ（URGENT）: 1時間に3件（プッシュ通知スパム防止）
  - 管理者 API（`/admin/**`）: 1分間60件
  - システム管理者 API（`/system-admin/**`）: 1分間60件
- **監査ログ**: 以下のイベントを記録
  - `REPORT_CREATED`: 通報作成
  - `REPORT_ACTION_TAKEN`: 通報への対応（action_type を含む）
  - `USER_FROZEN`: ユーザー凍結
  - `USER_UNFROZEN`: ユーザー凍結解除
  - `PERMISSION_GROUP_CREATED`: 権限グループ作成
  - `PERMISSION_GROUP_ASSIGNED`: 権限グループ割り当て
  - `MODULE_PRICE_CHANGED`: モジュール価格変更
  - `PACKAGE_CREATED`: パッケージ作成
  - `DISCOUNT_CAMPAIGN_CREATED`: 割引キャンペーン作成
  - `ROLE_CHANGED`: ユーザーロール変更
  - `REPORTS_BULK_RESOLVED`: 通報の一括対応（件数を含む）
  - `FEATURE_FLAG_CHANGED`: フィーチャーフラグ ON/OFF（key + 新旧値）
  - `MAINTENANCE_MODE_CHANGED`: メンテナンスモード切替（mode + 実行者）
  - `MAINTENANCE_SCHEDULED`: メンテナンス予定登録
  - `ANNOUNCEMENT_PUBLISHED`: 運営お知らせ公開
  - `CONTENT_RESTORED`: 誤削除コンテンツの復元
  - `REPORTING_RESTRICTED`: ユーザーの通報権限停止
  - `DATA_EXPORT_REQUESTED`: データエクスポートリクエスト

---

## 7. Flywayマイグレーション

```
V10.001__create_content_reports.sql        -- コンテンツ通報テーブル作成
V10.002__create_report_actions.sql         -- 通報アクション履歴テーブル作成
V10.003__create_feature_flags.sql          -- フィーチャーフラグテーブル作成
V10.004__create_maintenance_schedules.sql  -- メンテナンス予定テーブル作成
V10.005__create_platform_announcements.sql -- 運営お知らせテーブル作成
V10.006__add_reporting_restricted_until_to_users.sql  -- users に reporting_restricted_until カラム追加
```

**マイグレーション上の注意点**
- `content_reports.reported_by` / `report_actions.action_by` は Phase 1 の `users` テーブルへの FK
- ポリモーフィック参照（`target_type + target_id`）は FK 制約なし
- `dashboard_widget_settings` / `permission_groups` 等の既存テーブルは Phase 1-2 で作成済み
- 管理者ダッシュボード・システム管理者ダッシュボードの API は新規テーブル不要（既存テーブルの集計・管理 API のみ）
- F10.3 の `audit_logs` テーブル（Phase 1 作成済み）を参照

---

## 8. 未解決事項

- [x] ~~通報の自動エスカレーション閾値~~ → **N=3 で確定。SYSTEM_ADMIN が管理画面から変更可能**（`PUT /system-admin/moderation-settings`）。初期値3件は妥当（1-2件は偶発的通報の可能性）。運用で調整が必要になるためハードコードではなく設定変更可能に
- [x] ~~凍結ユーザーのデータ保持期間~~ → **永続的に凍結保持。自動削除しない**。凍結は「調査中・対応待ち」の状態であり、データ削除は証跡消滅のリスク。法的問題に発展した場合に証拠が必要。ストレージ圧迫時は Phase 10 の非アクティブアーカイブバッチと統合して対応
- [x] ~~エラーレポートの対応 SLA~~ → **目標24時間以内の初動。システムによる強制なし**。SYSTEM_ADMIN ダッシュボードに「未対応○件（最長○時間経過）」を表示し意識付け。夜間・休日の強制対応は運用負荷が過大
- [x] ~~MRR 算出ロジック~~ → **アクティブなサブスクリプションの月額合計。プロレート計算なし**。`team_subscriptions` の `status = ACTIVE` の月額合計 + 年額の 1/12 で算出。プロレートは Stripe 側の請求処理が担うため、ダッシュボード表示の MRR には含めない（概況指標として十分）
- [x] ~~通報コンテンツのスナップショット~~ → **通報時点のコンテンツを JSON で `content_reports.content_snapshot` に保存**。通報後にコンテンツが編集・削除されても ADMIN が元の内容を確認可能。JSON なら任意の構造（タイトル・本文・画像URL・投稿者名等）を保持できる

---

## 9. 変更履歴

| 日付 | 変更内容 |
|------|---------|
| 2026-03-15 | 運用支援・安全性改善10件: (1) フィーチャーフラグ管理（`feature_flags` テーブル + API 3本。Redis キャッシュで高速参照。デプロイ後の段階的リリース・即時ロールバック）(2) メンテナンスモード（即時切替 API + スケジュール予定登録。`maintenance_schedules` テーブル。3段階: NORMAL/READ_ONLY/MAINTENANCE。バッチで自動切替・復帰）(3) メンテナンス予告（登録時+24h前+1h前にリマインド。フロントエンドにバナー表示）(4) 運営からのお知らせ（`platform_announcements` テーブル + API 5本。URGENT/NORMAL/LOW優先度、is_pinned固定表示、target_scope配信対象制御、ダッシュボード統合）(5) デプロイ後異常検知（5分間隔でエラーレポート件数チェック → 閾値超過で SYSTEM_ADMIN アラート + Webhook）(6) SYSTEM_ADMIN 不在対策（最低2人体制警告 + 重要操作の外部Webhook通知）(7) 虚偽通報対策（却下率表示 + 自動レート制限 + 手動通報権限停止 API。users に reporting_restricted_until 追加）(8) 誤削除コンテンツ復元（72時間以内復元 API。CONTENT_RESTORE アクション種別追加）(9) ADMIN通知ダイジェスト化（INSTANT/DIGEST/URGENT_ONLYの3モード選択）(10) ユーザーデータエクスポート（非同期ZIP生成 + Pre-signed URL。個人情報保護法対応）。DB 5テーブル（+3）、API 78本（+20）、Flyway V10.001-006 |
| 2026-03-15 | UX・保守性改善6件: (1) 通報の一括対応 API（POST /admin/reports/bulk-resolve。最大50件を同一アクションで処理）(2) ユーザー違反履歴 API（GET /admin/users/{id}/violation-history。過去の通報・警告・凍結を時系列表示。レビュー時の判断材料）(3) 通報の統計・トレンド API（GET /admin/reports/stats。期間別推移・理由別内訳・常習者ランキング）(4) モジュール利用状況分析 API（GET /system-admin/module-usage-stats。有効化チーム数・利用率。価格設定の意思決定に活用）(5) ADMIN 対応実績（admin dashboard レスポンスに moderator_stats 追加。担当者別の対応件数でワークロード可視化）(6) 通報アーカイブバッチ（月次。RESOLVED/DISMISSED の6ヶ月超を archive テーブルに移行）。API 58本（+5） |
| 2026-03-15 | 精査②7件修正: (1) ER図に target_user_id の関係を追加 (2) 通報重複防止を Service 層チェック → UNIQUE KEY に変更 + target_user_id 別インデックス追加 (3) GET /reports/{id}（content_snapshot・related_reports 含む）、PATCH /review、PATCH /dismiss の詳細仕様追記 (4) system-admin dashboard に oldest_unresolved_hours 追加（エラーレポート SLA 表示） (5) 権限グループ解除 API（DELETE /assign/{userId}）追加 (6) ADMIN の自己降格防止ルール追加（最後の ADMIN が自身を降格 → 409） (7) エスカレーションカウントを PENDING+REVIEWING のみに限定（RESOLVED/DISMISSED は対象外）。API 53本（+1） |
| 2026-03-15 | 未解決事項5件解決 + 精査①6件修正: 【未解決】(1) エスカレーション閾値 N=3、SYSTEM_ADMIN が設定変更可能（moderation-settings API 追加）(2) 凍結ユーザーは永続保持・自動削除なし (3) エラーレポート SLA 目標24時間・強制なし (4) MRR = アクティブ月額合計+年額1/12、プロレートなし (5) content_snapshot JSON カラムで通報時点のコンテンツ保全。【精査】(1) content_reports に target_user_id カラム追加（WARNING/FREEZE 対象の特定用）(2) scope_type/scope_id は対象コンテンツから自動導出する旨を明記 (3) resolve エンドポイントの USER_FREEZE を SYSTEM_ADMIN のみに制限 (4) MEMBER 権限調整 API 2本追加（GET/PUT /admin/member-permissions）(5) admin dashboard レスポンスに billing_summary 追加 (6) グルーピング解決時の一括ステータス遷移を明記。API 52本（+4） |
| 2026-03-15 | 初版作成: 管理者ダッシュボード（AA）+ システム管理者ダッシュボード（AB）+ 通報・モデレーション。DB 2テーブル（content_reports / report_actions）、API 48本（通報7 + 管理者12 + システム管理者29）、通報ワークフロー（段階的対応: 警告→削除→凍結）、権限グループ管理、課金管理フロー、エスカレーション設計 |
