# F08.3: 議決権行使・委任状

> **ステータス**: 🟡 設計中
> **実装フェーズ**: Phase 8
> **最終更新**: 2026-03-15
> **モジュール種別**: 選択式モジュール #8
> **関連ドキュメント**: F05.3 電子印鑑（委任状署名連携）、F05.4 アンケート・投票（汎用投票との棲み分け）、F04.3 プッシュ通知

---

## 1. 概要

総会・定期会議の議案に対して電子で賛否を投票（出席/賛成/反対/棄権）し、出席できない場合は委任状を電子提出する機能。NPO・協同組合・マンション管理組合・株主総会など「総会を持つ組織」で汎用利用可能。投票期限・定足数の設定、リアルタイム集計・結果表示を備え、電子印鑑（F05.3）と連携して提出の証跡を記録する。無記名/記名投票の切り替えに対応し、無記名時は個人の投票内容を ADMIN にも非公開とする。

### F05.4 アンケート・投票との棲み分け

| 観点 | F05.4 アンケート・投票 | F08.3 議決権行使・委任状 |
|------|----------------------|------------------------|
| 目的 | 意見収集・満足度調査 | 組織の意思決定（法的拘束力のある決議） |
| 質問形式 | 自由（単一選択/複数選択/自由記述/評価スケール） | 固定（賛成/反対/棄権 + 出席確認） |
| 委任 | なし | あり（代理人指定 or 白紙委任） |
| 署名 | 不要 | 電子印鑑連携 |
| 定足数 | なし（任意の回答率表示のみ） | あり（法的要件に基づく定足数チェック） |
| フェーズ | Phase 5 | Phase 8 |

---

## 2. スコープ

### 対象ロール
| ロール | 操作可能な範囲 |
|--------|--------------|
| SYSTEM_ADMIN | 全チーム/組織の投票セッション参照 |
| ADMIN | 投票セッションの CRUD・公開・締切・結果確認・委任状管理 |
| DEPUTY_ADMIN | `MANAGE_VOTES` 権限を持つ場合: 投票セッションの CRUD・公開・締切・結果確認 |
| MEMBER | 投票・委任状提出・自分の投票履歴閲覧・結果閲覧 |
| SUPPORTER | 結果閲覧のみ（投票・委任不可） |
| GUEST | 対象外 |

### 対象レベル
- [x] 組織 (Organization) — 組織総会の議案
- [x] チーム (Team) — チーム内の決議
- [ ] 個人 (Personal)

---

## 3. DB設計

### テーブル一覧
| テーブル名 | 役割 | 論理削除 |
|-----------|------|---------|
| `proxy_vote_sessions` | 投票セッション（総会・定期会議単位） | あり |
| `proxy_vote_motions` | 議案（セッション内の個別採決事項） | なし |
| `proxy_votes` | 個人の投票回答 | なし |
| `proxy_delegations` | 委任状 | なし |

### テーブル定義

#### `proxy_vote_sessions`

投票セッション。総会1回 = 1セッション。複数の議案を含む。

| カラム名 | 型 | NULL | デフォルト | 説明 |
|---------|---|------|-----------|------|
| `id` | BIGINT UNSIGNED | NO | AUTO_INCREMENT | PK |
| `scope_type` | VARCHAR(20) | NO | — | スコープ（TEAM / ORGANIZATION） |
| `team_id` | BIGINT UNSIGNED | YES | NULL | FK → teams。TEAM の場合に設定 |
| `organization_id` | BIGINT UNSIGNED | YES | NULL | FK → organizations。ORGANIZATION の場合に設定 |
| `title` | VARCHAR(200) | NO | — | セッション名（例: 「2026年度 第1回定期総会」） |
| `description` | TEXT | YES | NULL | セッション概要・議事要旨 |
| `status` | VARCHAR(20) | NO | 'DRAFT' | ステータス（DRAFT / OPEN / CLOSED / FINALIZED） |
| `voting_start_at` | DATETIME | YES | NULL | 投票受付開始日時 |
| `voting_end_at` | DATETIME | YES | NULL | 投票受付締切日時 |
| `is_anonymous` | BOOLEAN | NO | FALSE | 無記名投票フラグ。TRUE = 個人の投票内容は ADMIN にも非公開 |
| `quorum_type` | VARCHAR(20) | NO | 'MAJORITY' | 定足数算出方式（MAJORITY: 過半数 / TWO_THIRDS: 2/3 / CUSTOM: カスタム） |
| `quorum_threshold` | DECIMAL(5,2) | YES | NULL | カスタム定足数の閾値（%）。`quorum_type = 'CUSTOM'` の場合に使用 |
| `eligible_count` | INT UNSIGNED | NO | — | 議決権総数（投票権を持つメンバー総数。セッション作成時にスナップショット） |
| `is_auto_accept_delegation` | BOOLEAN | NO | FALSE | 委任状の自動承認。TRUE = 提出即受理（小規模チーム向け）。FALSE = ADMIN 承認フロー |
| `created_by` | BIGINT UNSIGNED | YES | NULL | FK → users。ON DELETE SET NULL |
| `version` | BIGINT | NO | 0 | 楽観的ロックバージョン（@Version） |
| `created_at` | DATETIME | NO | CURRENT_TIMESTAMP | |
| `updated_at` | DATETIME | NO | CURRENT_TIMESTAMP ON UPDATE | |
| `deleted_at` | DATETIME | YES | NULL | 論理削除日時 |

**インデックス**
```sql
INDEX idx_pvs_scope (scope_type, team_id, organization_id, status)  -- スコープ別セッション一覧
INDEX idx_pvs_status (status, voting_end_at)                         -- ステータス別・締切順
```

**制約・備考**
- `scope_type` のスコープ制約: TEAM → `team_id` NOT NULL、ORGANIZATION → `organization_id` NOT NULL
- ステータスのライフサイクル: `DRAFT` → `OPEN`（投票受付開始）→ `CLOSED`（投票締切）→ `FINALIZED`（結果確定・議事録化）
- `eligible_count`: セッション作成時にスコープ内の MEMBER 以上のユーザー数をスナップショット。投票後のメンバー増減に影響されない
- `is_anonymous = TRUE` の場合: `proxy_votes` にはレコードを INSERT するが、API レスポンスでは投票者の個人情報を一切返却しない。集計結果のみ公開。ADMIN でも個人の投票内容を確認不可
- `quorum_type`:
  - `MAJORITY`: 議決権総数の過半数（50%超）が出席（投票 + 委任）で定足数充足
  - `TWO_THIRDS`: 2/3 以上
  - `CUSTOM`: `quorum_threshold` で任意の割合を指定

#### `proxy_vote_motions`

議案。1セッションに複数の議案を紐付ける。

| カラム名 | 型 | NULL | デフォルト | 説明 |
|---------|---|------|-----------|------|
| `id` | BIGINT UNSIGNED | NO | AUTO_INCREMENT | PK |
| `session_id` | BIGINT UNSIGNED | NO | — | FK → proxy_vote_sessions。ON DELETE CASCADE |
| `motion_number` | INT UNSIGNED | NO | — | 議案番号（表示順。1始まり） |
| `title` | VARCHAR(200) | NO | — | 議案タイトル（例: 「第1号議案: 2025年度決算報告の承認」） |
| `description` | TEXT | YES | NULL | 議案の詳細説明 |
| `attachment_key` | VARCHAR(500) | YES | NULL | 議案資料の S3 キー（Pre-signed URL でダウンロード） |
| `required_approval` | VARCHAR(20) | NO | 'MAJORITY' | 可決要件（MAJORITY: 過半数 / TWO_THIRDS: 2/3 / UNANIMOUS: 全員一致） |
| `result` | VARCHAR(20) | YES | NULL | 採決結果（APPROVED / REJECTED / PENDING）。FINALIZED 時に確定 |
| `approve_count` | INT UNSIGNED | NO | 0 | 賛成票数（denormalize） |
| `reject_count` | INT UNSIGNED | NO | 0 | 反対票数（denormalize） |
| `abstain_count` | INT UNSIGNED | NO | 0 | 棄権票数（denormalize） |
| `display_order` | INT | NO | 0 | 表示順 |
| `created_at` | DATETIME | NO | CURRENT_TIMESTAMP | |
| `updated_at` | DATETIME | NO | CURRENT_TIMESTAMP ON UPDATE | |

**インデックス**
```sql
INDEX idx_pvm_session (session_id, display_order)  -- セッション内の議案一覧
```

**制約・備考**
- 1セッションあたり最大30議案（Service 層バリデーション）
- `required_approval`: 議案ごとに可決要件を設定可能（例: 予算は過半数、定款変更は 2/3 等）
- `result`: `CLOSED` → `FINALIZED` 遷移時に、投票結果 + 委任票を集計して自動判定
- `approve_count` / `reject_count` / `abstain_count`: 投票時にアトミック更新。委任票は委任先の投票に含算

#### `proxy_votes`

個人の投票回答。1ユーザー × 1議案 = 1レコード。

| カラム名 | 型 | NULL | デフォルト | 説明 |
|---------|---|------|-----------|------|
| `id` | BIGINT UNSIGNED | NO | AUTO_INCREMENT | PK |
| `motion_id` | BIGINT UNSIGNED | NO | — | FK → proxy_vote_motions。ON DELETE CASCADE |
| `user_id` | BIGINT UNSIGNED | NO | — | FK → users。投票者（本人投票 or 委任を受けた代理人） |
| `vote_type` | VARCHAR(20) | NO | — | 投票内容（APPROVE / REJECT / ABSTAIN） |
| `is_proxy_vote` | BOOLEAN | NO | FALSE | 委任による代理投票か |
| `delegation_id` | BIGINT UNSIGNED | YES | NULL | FK → proxy_delegations。代理投票の場合に紐付け |
| `voted_at` | DATETIME | NO | CURRENT_TIMESTAMP | 投票日時 |

**インデックス**
```sql
UNIQUE KEY uq_pv_motion_user (motion_id, user_id)       -- 同一ユーザーの二重投票防止
INDEX idx_pv_motion (motion_id, vote_type)               -- 議案別の集計
INDEX idx_pv_user (user_id)                               -- ユーザー別の投票履歴
```

**制約・備考**
- 投票は1議案1ユーザーにつき1回。変更する場合は DELETE → INSERT（Service 層で実装。`OPEN` ステータスの間のみ変更可能）
- `is_proxy_vote = TRUE`: 委任を受けた代理人が委任者の代わりに投票した場合。`delegation_id` で委任状と紐付け
- 白紙委任の場合: 委任を受けた代理人が全議案を一括投票する。代理人が投票しない議案は「棄権」扱い
- `is_anonymous = TRUE` のセッション: レコードは INSERT するが、API レスポンスでは `user_id` を返却しない

#### `proxy_delegations`

委任状。出席できないメンバーが代理人を指定、または白紙委任を行う。

| カラム名 | 型 | NULL | デフォルト | 説明 |
|---------|---|------|-----------|------|
| `id` | BIGINT UNSIGNED | NO | AUTO_INCREMENT | PK |
| `session_id` | BIGINT UNSIGNED | NO | — | FK → proxy_vote_sessions。ON DELETE CASCADE |
| `delegator_id` | BIGINT UNSIGNED | NO | — | FK → users。委任者（投票権を持つが出席しないメンバー） |
| `delegate_id` | BIGINT UNSIGNED | YES | NULL | FK → users。代理人。NULL = 白紙委任（議長に一任） |
| `is_blank` | BOOLEAN | NO | FALSE | 白紙委任フラグ（TRUE = 議長に一任。`delegate_id` は NULL） |
| `electronic_seal_id` | BIGINT UNSIGNED | YES | NULL | FK → electronic_seals。委任状提出の電子印鑑証跡 |
| `reason` | VARCHAR(500) | YES | NULL | 委任理由（任意） |
| `status` | VARCHAR(20) | NO | 'SUBMITTED' | ステータス（SUBMITTED / ACCEPTED / REJECTED / CANCELLED） |
| `reviewed_by` | BIGINT UNSIGNED | YES | NULL | FK → users。承認/却下した管理者 |
| `reviewed_at` | DATETIME | YES | NULL | 承認/却下日時 |
| `created_at` | DATETIME | NO | CURRENT_TIMESTAMP | |
| `updated_at` | DATETIME | NO | CURRENT_TIMESTAMP ON UPDATE | |

**インデックス**
```sql
UNIQUE KEY uq_pd_session_delegator (session_id, delegator_id)  -- 1セッション1委任者1件
INDEX idx_pd_session (session_id, status)                       -- セッション別の委任状一覧
INDEX idx_pd_delegate (delegate_id)                             -- 代理人が受けた委任一覧
```

**制約・備考**
- 委任者は MEMBER 以上で投票権を持つユーザーのみ
- 代理人は同一スコープ（チーム/組織）のメンバーであること。自分自身への委任は不可
- 白紙委任（`is_blank = TRUE`）: 議長（= セッション作成者 or ADMIN）が全議案を一括投票
- 委任状の承認フロー: `SUBMITTED` → ADMIN が `ACCEPTED` or `REJECTED`。`ACCEPTED` 後に代理人は投票可能
- 委任者が自分で投票した場合（心変わりで出席）: 委任状は自動的に `CANCELLED` に遷移し、委任は無効化
- `electronic_seal_id`: 電子印鑑で署名することで委任の正当性を担保。署名なしでも委任は可能だが、「署名あり」のマークが UI に表示される

### ER図（テキスト形式）
```
teams (1) ──── (N) proxy_vote_sessions（team_id; TEAM）
organizations (1) ──── (N) proxy_vote_sessions（organization_id; ORGANIZATION）
proxy_vote_sessions (1) ──── (N) proxy_vote_motions
proxy_vote_sessions (1) ──── (N) proxy_delegations
proxy_vote_motions (1) ──── (N) proxy_votes

users (1) ──── (N) proxy_votes（user_id）
users (1) ──── (N) proxy_delegations（delegator_id / delegate_id）
electronic_seals (1) ──── (N) proxy_delegations（electronic_seal_id）
```

---

## 4. API設計

### エンドポイント一覧
| メソッド | パス | 認証 | 説明 |
|---------|-----|------|------|
| GET | `/api/v1/proxy-votes` | 必要 | 投票セッション一覧取得 |
| POST | `/api/v1/proxy-votes` | 必要 | 投票セッション作成 |
| GET | `/api/v1/proxy-votes/{id}` | 必要 | セッション詳細取得（議案一覧含む） |
| PUT | `/api/v1/proxy-votes/{id}` | 必要 | セッション更新 |
| DELETE | `/api/v1/proxy-votes/{id}` | 必要 | セッション削除（論理削除。DRAFT のみ） |
| PATCH | `/api/v1/proxy-votes/{id}/open` | 必要 | 投票受付開始（DRAFT → OPEN） |
| PATCH | `/api/v1/proxy-votes/{id}/close` | 必要 | 投票締切（OPEN → CLOSED） |
| PATCH | `/api/v1/proxy-votes/{id}/finalize` | 必要 | 結果確定（CLOSED → FINALIZED） |
| POST | `/api/v1/proxy-votes/{id}/motions` | 必要 | 議案追加 |
| PUT | `/api/v1/proxy-votes/motions/{motionId}` | 必要 | 議案更新 |
| DELETE | `/api/v1/proxy-votes/motions/{motionId}` | 必要 | 議案削除（DRAFT のみ） |
| POST | `/api/v1/proxy-votes/{id}/cast` | 必要 | 投票（全議案一括） |
| PUT | `/api/v1/proxy-votes/{id}/cast` | 必要 | 投票変更（OPEN 中のみ） |
| POST | `/api/v1/proxy-votes/{id}/delegate` | 必要 | 委任状提出 |
| DELETE | `/api/v1/proxy-votes/{id}/delegate` | 必要 | 委任状取り下げ（SUBMITTED / ACCEPTED のみ） |
| PATCH | `/api/v1/proxy-votes/delegations/{delegationId}/review` | 必要 | 委任状承認/却下（管理者用） |
| GET | `/api/v1/proxy-votes/{id}/results` | 必要 | 投票結果取得 |
| GET | `/api/v1/proxy-votes/{id}/attendance` | 必要 | 出席・委任状況一覧（管理者用） |
| GET | `/api/v1/proxy-votes/my` | 必要 | 自分の投票・委任履歴 |
| POST | `/api/v1/proxy-votes/{id}/remind` | 必要 | 未投票者へリマインド送信（管理者用） |
| GET | `/api/v1/proxy-votes/{id}/minutes-pdf` | 必要 | 議事録 PDF エクスポート（FINALIZED のみ） |

### リクエスト／レスポンス仕様

#### `POST /api/v1/proxy-votes`

投票セッションを作成する。

**リクエストボディ**
```json
{
  "scope_type": "ORGANIZATION",
  "organization_id": 1,
  "title": "2026年度 第1回定期総会",
  "description": "2025年度決算報告・2026年度予算案・役員改選",
  "voting_start_at": "2026-04-01T09:00:00+09:00",
  "voting_end_at": "2026-04-15T23:59:00+09:00",
  "is_anonymous": false,
  "quorum_type": "MAJORITY",
  "motions": [
    {
      "title": "第1号議案: 2025年度決算報告の承認",
      "description": "添付の決算報告書をご確認ください",
      "attachment_key": "files/proxy/1/budget_2025.pdf",
      "required_approval": "MAJORITY"
    },
    {
      "title": "第2号議案: 定款変更の承認",
      "description": "第5条の会費改定について",
      "required_approval": "TWO_THIRDS"
    }
  ]
}
```

- `motions`: 任意。セッション作成時に議案を一括登録。後から `POST /motions` で追加も可能
- `eligible_count`: サーバーサイドで自動算出（スコープ内の MEMBER 以上のユーザー数）
- `voting_start_at` / `voting_end_at`: 任意。未指定の場合は手動で OPEN / CLOSE

**レスポンス（201 Created）**: セッション詳細を返却

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 400 | バリデーションエラー / scope_type とスコープ ID の不整合 |
| 403 | ADMIN / DEPUTY_ADMIN（MANAGE_VOTES）ではない |

---

#### `GET /api/v1/proxy-votes/{id}`

セッションの詳細を取得する。議案一覧・定足数状況を含む。

**レスポンス（200 OK）**
```json
{
  "data": {
    "id": 1,
    "scope_type": "ORGANIZATION",
    "organization": { "id": 1, "name": "○○協同組合" },
    "title": "2026年度 第1回定期総会",
    "description": "2025年度決算報告・2026年度予算案・役員改選",
    "status": "OPEN",
    "voting_start_at": "2026-04-01T09:00:00+09:00",
    "voting_end_at": "2026-04-15T23:59:00+09:00",
    "is_anonymous": false,
    "quorum_type": "MAJORITY",
    "eligible_count": 50,
    "quorum_status": {
      "required": 26,
      "current": 35,
      "is_met": true,
      "voted_count": 28,
      "delegated_count": 7,
      "not_responded_count": 15
    },
    "motions": [
      {
        "id": 1,
        "motion_number": 1,
        "title": "第1号議案: 2025年度決算報告の承認",
        "required_approval": "MAJORITY",
        "result": null,
        "approve_count": 22,
        "reject_count": 3,
        "abstain_count": 3
      }
    ],
    "my_status": {
      "has_voted": true,
      "has_delegated": false
    },
    "version": 1,
    "created_by": { "id": 10, "display_name": "理事長 山田" },
    "created_at": "2026-03-20T09:00:00+09:00"
  }
}
```

- `quorum_status`: 定足数の充足状況。`voted_count + delegated_count` が `required` 以上なら `is_met = true`
- `motions[].approve_count` 等: `is_anonymous = TRUE` の場合も集計数は表示（個人の投票内容が分からないため問題ない）
- `my_status`: ログインユーザー自身の投票/委任状況

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 403 | スコープへのアクセス権限がない |
| 404 | セッションが存在しない |

---

#### `POST /api/v1/proxy-votes/{id}/cast`

全議案に対して一括投票する。

**リクエストボディ**
```json
{
  "votes": [
    { "motion_id": 1, "vote_type": "APPROVE" },
    { "motion_id": 2, "vote_type": "REJECT" },
    { "motion_id": 3, "vote_type": "ABSTAIN" }
  ]
}
```

- 全議案への投票が必須（一部のみ投票は不可。投票しない議案は `ABSTAIN` を指定）
- 委任を受けている代理人の場合: `is_proxy_vote = TRUE` で委任者の分も投票（`delegation_id` を自動紐付け）

**レスポンス（201 Created）**
```json
{
  "data": {
    "session_id": 1,
    "voted_motions": 3,
    "voted_at": "2026-04-05T14:30:00+09:00"
  }
}
```

**副作用**:
1. `proxy_vote_motions` の `approve_count` / `reject_count` / `abstain_count` をアトミック更新
2. 委任者が自分で投票した場合: 既存の委任状を `CANCELLED` に自動遷移
3. ApplicationEvent: `ProxyVoteCastEvent`（管理者にリアルタイム集計更新を通知）

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 400 | バリデーションエラー / 全議案への投票が揃っていない |
| 403 | 投票権がない（SUPPORTER / GUEST）/ OPEN 以外のステータス |
| 404 | セッション/議案が存在しない |
| 409 | 既に投票済み（変更は PUT /cast を使用） |

---

#### `POST /api/v1/proxy-votes/{id}/delegate`

委任状を提出する。

**リクエストボディ**
```json
{
  "delegate_id": 11,
  "is_blank": false,
  "electronic_seal_id": 5,
  "reason": "出張のため出席できません"
}
```

- `delegate_id`: 代理人のユーザー ID。`is_blank = TRUE` の場合は NULL（議長に一任）
- `electronic_seal_id`: 任意。電子印鑑 ID で署名
- 既に投票済みの場合は委任不可（409 エラー）

**レスポンス（201 Created）**: 委任状詳細を返却

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 400 | 自分自身への委任 / delegate_id がスコープ外 |
| 403 | 投票権がない / OPEN 以外のステータス |
| 409 | 既に投票済み / 既に委任済み |

---

#### `PATCH /api/v1/proxy-votes/delegations/{delegationId}/review`

委任状を承認または却下する。

**リクエストボディ**
```json
{
  "status": "ACCEPTED"
}
```

**レスポンス（200 OK）**: 委任状詳細を返却

- `ACCEPTED` 後: 代理人に通知（「○○さんから委任を受けました。投票をお願いします」）
- `REJECTED` 後: 委任者に通知（「委任状が却下されました。直接投票してください」）

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 403 | ADMIN / DEPUTY_ADMIN（MANAGE_VOTES）ではない |
| 404 | 委任状が存在しない |
| 409 | SUBMITTED 以外のステータス |

---

#### `GET /api/v1/proxy-votes/{id}/results`

投票結果を取得する。

**レスポンス（200 OK）**
```json
{
  "data": {
    "session_id": 1,
    "status": "FINALIZED",
    "quorum_status": {
      "required": 26,
      "current": 42,
      "is_met": true
    },
    "motions": [
      {
        "id": 1,
        "motion_number": 1,
        "title": "第1号議案: 2025年度決算報告の承認",
        "required_approval": "MAJORITY",
        "result": "APPROVED",
        "approve_count": 30,
        "reject_count": 5,
        "abstain_count": 7,
        "approve_rate": 71.4,
        "total_votes": 42
      }
    ],
    "summary": {
      "total_eligible": 50,
      "total_voted": 35,
      "total_delegated": 7,
      "total_not_responded": 8,
      "participation_rate": 84.0
    }
  }
}
```

- `approve_rate`: `approve_count / (approve_count + reject_count + abstain_count) * 100`
- `is_anonymous = TRUE` の場合: 集計結果のみ返却。個人の投票詳細は含まない
- `OPEN` ステータスでも結果は参照可能（リアルタイム集計。ただし `result` は `null`）

**エラーレスポンス**
| ステータス | 条件 |
|-----------|------|
| 403 | スコープへのアクセス権限がない |
| 404 | セッションが存在しない |

---

## 5. ビジネスロジック

### 投票セッションのライフサイクル

```
1. ADMIN がセッション作成（DRAFT）
   a. 議案の追加・編集・削除が可能
   b. eligible_count をスコープ内メンバー数から自動算出してスナップショット

2. 投票受付開始（DRAFT → OPEN）
   a. PATCH /open で遷移。議案が1件以上存在することを検証
   b. 全メンバーにプッシュ通知（ApplicationEvent: VoteSessionOpenedEvent）
   c. voting_start_at が未来日時の場合はバッチで自動遷移（5分間隔）
   d. OPEN 後は議案の追加・削除不可（タイトル・説明の軽微な修正のみ可能）

3. 投票・委任の受付（OPEN 中）
   a. メンバーは全議案に一括投票（POST /cast）
   b. 出席できないメンバーは委任状を提出（POST /delegate）
   c. ADMIN が委任状を承認/却下
   d. 代理人が委任者の分を投票
   e. 投票変更は OPEN 中のみ可能（PUT /cast で上書き）

4. 投票締切（OPEN → CLOSED）
   a. PATCH /close で手動遷移、または voting_end_at 到達時にバッチで自動遷移
   b. CLOSED 後は投票・委任の追加/変更不可

5. 結果確定（CLOSED → FINALIZED）
   a. PATCH /finalize で遷移
   b. 各議案の result を自動判定:
      - approve_count > reject_count + abstain_count の場合 → APPROVED（MAJORITY の場合）
      - TWO_THIRDS: approve_count >= total_votes * 2/3
      - UNANIMOUS: reject_count == 0 AND abstain_count == 0
   c. 未投票の委任（代理人が投票しなかった白紙委任）は棄権扱い
   d. 監査ログ: VOTE_SESSION_FINALIZED を記録
```

### 定足数チェック

```
1. 定足数 = 投票権を行使した人数（自己投票 + 委任）
   - 自己投票: proxy_votes の DISTINCT user_id（is_proxy_vote = FALSE）
   - 委任: proxy_delegations の status = 'ACCEPTED' かつ CANCELLED でない件数
   - 合計が quorum_required 以上であれば定足数充足

2. 定足数算出:
   - MAJORITY: CEIL(eligible_count / 2) + 1（過半数）
   - TWO_THIRDS: CEIL(eligible_count * 2 / 3)
   - CUSTOM: CEIL(eligible_count * quorum_threshold / 100)

3. 定足数未充足の場合:
   - FINALIZE 遷移時に警告を表示（強制的に FINALIZED にはできるが、結果に「定足数未達」ラベルを付与）
   - 議案の result は判定するが、「参考決議（定足数未達）」として記録
```

### 委任投票フロー

```
1. 委任者がPOST /delegate で委任状を提出
2. 承認フロー分岐:
   a. is_auto_accept_delegation = TRUE（自動承認）:
      status = 'ACCEPTED' で即座に INSERT。代理人にプッシュ通知
   b. is_auto_accept_delegation = FALSE（手動承認）:
      status = 'SUBMITTED' で INSERT。ADMIN がPATCH /delegations/{id}/review で承認（ACCEPTED）
3. ACCEPTED 後: 代理人にプッシュ通知（「○○さんからの委任を受けました」）
4. 代理人がPOST /cast で投票:
   a. 自分の投票 + 委任者の代理投票を同時に処理
   b. 代理投票は is_proxy_vote = TRUE、delegation_id で紐付け
   c. 複数人の委任を受けている場合、全員分を一括投票
5. 委任者が心変わりで自分で投票した場合:
   a. 委任状は自動的に CANCELLED に遷移
   b. 代理人が既に代理投票済みの場合: 代理投票を DELETE し、本人の投票を INSERT
   c. proxy_vote_motions のカウントをアトミック補正
```

### 自動遷移バッチ

```
1. @Scheduled で5分間隔で実行
2. DRAFT → OPEN 自動遷移:
   - 条件: status = 'DRAFT' AND voting_start_at <= NOW() AND voting_start_at IS NOT NULL
3. OPEN → CLOSED 自動遷移:
   - 条件: status = 'OPEN' AND voting_end_at <= NOW() AND voting_end_at IS NOT NULL
4. バッチエラー時: ERROR ログ出力 + 次回実行で再試行
```

### 議事録 PDF エクスポート

```
1. GET /proxy-votes/{id}/minutes-pdf（FINALIZED セッションのみ。ADMIN / DEPUTY_ADMIN）
2. PDF 内容:
   a. セッション情報（タイトル・日時・定足数状況）
   b. 各議案: タイトル・説明・投票結果（賛成/反対/棄権の票数 + 可決/否決）
   c. 定足数充足状況（充足 or 未達 + 「参考決議」ラベル）
   d. 出席者リスト: 投票者 + 委任者（委任先を明記）
   e. is_anonymous = TRUE の場合: 投票者リストは「投票者○名」のみ表示（個人名なし）
3. 生成方式: Thymeleaf テンプレート → Flying Saucer で PDF 変換（F07.4 カルテ PDF と同一アーキテクチャ）
4. タイムアウト: 10秒（写真なしのため短め）
```

### 重要な判定ロジック

- **投票権**: MEMBER 以上（SUPPORTER / GUEST は閲覧のみ）
- **議案編集**: DRAFT 中のみ追加/削除可能。OPEN 中はタイトル・説明の軽微修正のみ
- **投票変更**: OPEN 中のみ。CLOSED 以降は変更不可
- **委任と投票の排他**: 同一セッションで投票と委任は同時に行えない。投票した場合は委任が自動取消、委任中に自分で投票した場合も委任が自動取消
- **無記名投票**: `is_anonymous = TRUE` の場合、API レスポンスで個人の投票内容は一切返却しない。DB にはレコードが存在するが、監査目的でのみ保持
- **結果確定後の変更禁止**: FINALIZED 後はセッション・議案・投票の全データが変更不可（議事録としての証跡保全）

---

## 6. セキュリティ考慮事項

- **認可チェック**: 全 API エンドポイントでスコープメンバーシップとロールを検証
- **無記名投票の保護**: `is_anonymous = TRUE` のセッションでは、DB の `proxy_votes.user_id` は監査用にのみ保持。API レスポンス・管理画面・CSV エクスポート等のいかなる経路でも個人の投票内容を返却しない。SQL 直接アクセス以外では確認不可能な設計
- **投票の改竄防止**: FINALIZED 後は全データの UPDATE / DELETE をアプリ層で厳密にブロック。DB 側には追加の制約を設けない（運用上の柔軟性を維持）
- **電子印鑑連携**: 委任状の `electronic_seal_id` で提出の正当性を担保。署名なしでも委任は有効だが、法的紛争時のエビデンスとしては署名ありが優位
- **監査ログ**: 以下のイベントを監査ログに記録
  - `VOTE_SESSION_CREATED`: セッション作成
  - `VOTE_SESSION_OPENED`: 投票受付開始
  - `VOTE_SESSION_CLOSED`: 投票締切
  - `VOTE_SESSION_FINALIZED`: 結果確定（各議案の result を含む）
  - `VOTE_CAST`: 投票（無記名セッションでは user_id を記録しない）
  - `DELEGATION_SUBMITTED`: 委任状提出
  - `DELEGATION_REVIEWED`: 委任状承認/却下
- **レートリミット**: `Bucket4j` で以下の制限を適用
  - 投票 API（`POST /cast`）: 1分間10件
  - 委任状提出（`POST /delegate`）: 1分間5件
  - リマインド送信（`POST /remind`）: 1セッションあたり1時間に1回
- **IDOR 防止**: `PUT /cast` は投票者本人チェック必須。`DELETE /delegate` は委任者本人のみ

---

## 7. Flywayマイグレーション

```
V8.010__add_manage_votes_permission.sql             -- MANAGE_VOTES パーミッション追加
V8.011__create_proxy_vote_sessions.sql              -- 投票セッションテーブル作成
V8.012__create_proxy_vote_motions.sql               -- 議案テーブル作成
V8.013__create_proxy_votes.sql                      -- 投票回答テーブル作成
V8.014__create_proxy_delegations.sql                -- 委任状テーブル作成
```

**マイグレーション上の注意点**
- V8.010: `MANAGE_VOTES` を `permissions` テーブルに INSERT し、`role_permissions` に SYSTEM_ADMIN・ADMIN 用のシードを追加。scope = TEAM / ORGANIZATION
- `proxy_vote_sessions.team_id` / `organization_id` は Phase 1 で作成済みのテーブルへの FK
- `proxy_delegations.electronic_seal_id` は `electronic_seals` テーブル（Phase 1）への FK
- V8.001〜V8.009 は決済（F08.1）等で使用済みの場合は採番を調整すること

---

## 8. 未解決事項

- [x] ~~議決権の重み付け~~ → **Phase 8 では1人1票で実装**。主要ターゲット（NPO・協同組合・管理組合）は法律上1人1票が原則。将来必要になった場合は `is_weighted BOOLEAN` + `proxy_vote_weights` テーブルで対応可能。FUTURE_CONSIDERATIONS.md に記載
- [x] ~~投票結果の PDF 議事録エクスポート~~ → **Phase 8 スコープに含める**。`GET /proxy-votes/{id}/minutes-pdf` で FINALIZED セッションの議事録 PDF を生成。F07.4 カルテ PDF と同一アーキテクチャ（Thymeleaf + Flying Saucer）。NPO・管理組合の議事録書面保管義務に対応
- [x] ~~委任状の自動承認オプション~~ → **導入する**。`proxy_vote_sessions.is_auto_accept_delegation BOOLEAN DEFAULT FALSE` を追加。`TRUE` = 委任提出即受理（小規模チーム向け）。`FALSE` = ADMIN 承認フロー（大規模組織向け）

---

## 9. 変更履歴

| 日付 | 変更内容 |
|------|---------|
| 2026-03-15 | 未解決事項3件解決: 議決権重み付けは Phase 8 で1人1票（将来 proxy_vote_weights で対応、FUTURE_CONSIDERATIONS.md に記載）・PDF 議事録エクスポート導入（GET /minutes-pdf + Thymeleaf/Flying Saucer）・委任状自動承認オプション導入（is_auto_accept_delegation カラム追加） |
| 2026-03-15 | 初版作成: 4テーブル（proxy_vote_sessions, proxy_vote_motions, proxy_votes, proxy_delegations）、20 API エンドポイント、投票セッション4段階ライフサイクル（DRAFT/OPEN/CLOSED/FINALIZED）、議案別可決要件（過半数/2/3/全員一致）、定足数チェック、委任投票ワークフロー（白紙委任対応）、無記名投票、電子印鑑連携、自動遷移バッチ |
